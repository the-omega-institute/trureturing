# open-problem-lane reference: Review target (gen_review.py input) template — refutation; placeholders __PR__/__HEAD__/__FREEZE__/__EVENT__/__STMT__/__IMPORTS__/__KERNEL__/__BASE__.
Open problem (external, no atom; OLD-ish (2014 OEIS %F, still Conjecture, no refutation note); REFUTATION by a single finite certificate; generality I; utility certified-instance/refutes): OEIS A000040 (`%A` N. J. A. Sloane, undated; the refuted formula is Gary Detlefs's %F, May 25 2014). `%N` (verbatim): "The prime numbers." Refuted `%F` (Detlefs, verbatim): "Conjecture: Sequence = {5 and n <> 5| ( Fibonacci(n) mod n = 1 or Fibonacci(n) mod n = n - 1) and 2^(n-1) mod n = 1}. - _Gary Detlefs_, May 25 2014". The claim: primes = {5} ∪ {n ≥ 1 : n ≠ 5 ∧ (F(n) mod n ∈ {1, n−1}) ∧ 2^(n−1) mod n = 1}, i.e. ∀ n ≥ 1, Prime n ↔ (n = 5 ∨ (n ≠ 5 ∧ fibTest n ∧ fermatTest n)). Refuted by the composite n = 219781 = 271 · 811: F(219781) mod 219781 = 1 and 2^219780 mod 219781 = 1, so 219781 lies in the right-hand set but is not prime. Preregistration issue #7792 (opened BEFORE the probe; comment 1 = attempt-1 report + route re-preregistration (form (2), no private lemma) + the orchestrator's proof-shape ruling; comment 2 = attempt-2 report @ 968743afaf; Stage B comment records the freeze). ONLY the May 25 2014 %F line is refuted; NOT claimed: the May 28 2014 variant "… and 2^(3*n) mod 3*n = 8" (only DISCLOSED that 219781 also satisfies it), Detlefs's Sep 10 2010 Wilson-type characterizations in the same %F block, any theory or novelty of Fibonacci/Fermat pseudoprimes (219781 is a KNOWN Fibonacci-type pseudoprime listed in A094401, A093372, A212424 — attributed as prior art), or a least-counterexample theorem (219781 is only the least composite in the checked range 1..10⁶; seven composites there: 219781, 252601, 399001, 512461, 722261, 741751, 852841). DISCLOSED boundary: the literal statement also fails at n = 2 (2^1 mod 2 = 0, so the prime 2 is outside the set); the witness used is the substantive composite, not the degenerate boundary.

Formal target (public surface EXACTLY five declarations, NO private declarations): `def fibTest (n : ℕ) : Prop := Nat.fib n % n = 1 ∨ Nat.fib n % n = n - 1`, `def fermatTest (n : ℕ) : Prop := 2 ^ (n - 1) % n = 1`, `def inDetlefsSet (n : ℕ) : Prop := n = 5 ∨ (n ≠ 5 ∧ fibTest n ∧ fermatTest n)`, `def claim : Prop := ∀ n : ℕ, 0 < n → (Nat.Prime n ↔ inDetlefsSet n)`, `theorem result : ¬ claim`. Escape content (form (2), inline in `result`): `Nat.fastFib 219781 % 219781 = 1` by `decide` transported through `Nat.fastFib_eq` to `Nat.fib 219781 % 219781 = 1`; `(2 : ZMod 219781) ^ 219780 = 1` by `reduce_mod_char` transported via `ZMod.natCast_eq_natCast_iff'` to `2 ^ 219780 % 219781 = 1`; `¬ Nat.Prime 219781` by `norm_num` (271 · 811); then `inDetlefsSet 219781` and `(hclaim 219781 _).mpr` yield `Nat.Prime 219781`, contradiction.

REVIEWERS: judge per §3.2/§3.3. (1) PROOF SHAPE: `result` is content — the escape witness is form (2), the public result produced on its live path by the finite certificate at 219781 (compositeness, Fermat residue, Fibonacci residue). Confirm all three facts are consumed on the live path (the `.mpr` of the iff and the final contradiction) and are NOT obtainable by instantiation/projection/normalization of pinned Mathlib or frozen prerequisites: no Mathlib theorem supplies these residues or this compositeness; `decide`/`norm_num`/`reduce_mod_char` here COMPUTE new atomic facts rather than normalize facts already supplied (§3.2 counts `decide` as normalization only "在给定假设和上述步骤已提供全部原子事实后"). This is the certified-instance/refutes shape — a single finite counterexample refuting a named ∀-iff — exactly A049591 (#7703), A103585 (#7717), A129598 (#7791), A319927 (#7675), all approved 3/3; the typed refutation edge is independent of whether the certificate uses decide/norm_num (CLAUDE.md §3.3 反驳保留). Note for the record: the attempt-1 probe seat self-reported "bind-only" under the probe brief's "try normalization first" rule; the orchestrator ruled otherwise on #7792 with the precedents above — judge the shape yourself against §3.2, not against either self-report. (2) UTILITY: `utility: kind=certified-instance; basis=refutes` is correct here — the WHOLE refutation is carried by finite computation at the single value 219781, unlike the symbolic tails of #7690/#7748 (`none`). direct frozen dependencies none; `admission_basis: escape-witness`. std3; no native_decide; no sorry. `fibTest`, `fermatTest`, `inDetlefsSet`, `claim` are definitions.

Module `D5/S0/Certificates/DetlefsFibonacciFermatPrimeCharacterizationRefutation.lean` (Certificates = kernel-checkable refutation certificates per Meta/domains.yaml; siblings A049591/A103585/A129598/A018800/A319927; fresh dev (merge-base __BASE__) 49 → 50/96 at HEAD by DIRECT file count `git ls-tree --name-only <rev> -- D5/S0/Certificates/` (the directory has four sub-buckets; a recursive count is NOT the policy count); DirectoryFileLimit = 96); imports EXACTLY `Mathlib.Data.Nat.Fib.Basic`, `Mathlib.Tactic.NormNum.Prime`, `Mathlib.Tactic.ReduceModChar` (each deletion EXIT=1, seat-reported; the tests seat re-probes; anchors in `mathlib/module/…` form). generality I. `#print axioms result` = `[propext, Classical.choice, Quot.sound]`. Kernel cost (seat-reported): __KERNEL__. Dossier `Problems/oeis-a000040-detlefs-fibonacci-fermat-prime-characterization-refutation.md` (doi null, url https://oeis.org/A000040, `motivation_gids` = the module GID, resolution refuted; discloses the n = 2 boundary, the May 28 variant and the prior art) + Library note `Library/Arith/sloane2014a000040.md` (Arith DOMAIN bucket, fresh dev 53 → 54/96; bibkey `sloane2014a000040` follows the %A sequence author Sloane, `authors: N. J. A. Sloane; Gary Detlefs` — %A author first, then the %F conjecture author Detlefs; the %A line is undated so year 2014 is the datable conjecture year, disclosed in the note; AUTHOR locator present; `LibraryNoteRef.Create("D5/L/Arith/sloane2014a000040")`). Scribe `Blueprint/D5/S0/Certificates/DetlefsFibonacciFermatPrimeCharacterizationRefutation.scribe.cs` renders `fibTest`/`fermatTest` with the corpus's mod rendering, `inDetlefsSet` as the disjunction, `claim` as the universal iff with `Prime`, `result` as `¬claim` with `ResolutionKind.Refuted`; NO `Formula.SetBuilder`; no `Formula.Fraction`. Freeze commit __FREEZE__ (event `__EVENT__`, module statement id `__STMT__`) is an ancestor of PR HEAD `__HEAD__`; post-freeze commits are only origin/dev merges (no post-freeze content commit; module bytes vs probe 968743afaf differ at most by the pre-freeze header alignment). Numerics (orchestrator, independent, two methods — fast doubling and 219781-step direct iteration agree): F(219781) mod 219781 = 1; 2^219780 mod 219781 = 1; 219781 = 271 · 811; 1..10⁶ has exactly seven composites passing both tests (219781 least) and 2 is the only prime failing; 2^(3·219781) mod 3·219781 = 8 (May 28 variant also fails at 219781). Bounded scans carry no proof. Search seat: NONE — the GPT Pro pool was unavailable that day; the candidate came from the orchestrator's local OEIS full-text scan (disclosed in the PR body); orchestrator: verbatim %N/%F, independent recomputation, OEIS current text (newest revisions; older history anonymous-locked, ASSUMED-UNVERIFIED), OEIS `seq:219781,252601,399001` no results, prior-art sequences, dedupe; probe/impl/review = codex (判卷面单一模型族，无 GPT Pro 参与). Literature (probe, 2026-09-14): arXiv 0, OpenAlex unrelated, MathOverflow 0, GitHub only #7792 itself; no priority claim. Review contract: public surface exactly the five declarations; definitions mirrored literally; `result : ¬claim` with the Refuted resolution; the May 28 variant / 2010 lines / pseudoprime theory NOT claimed; no bind-only public theorems; std3; no native_decide; every anchor import consumed (deletion EXIT=1) and no unused direct import; PR body readings measured at HEAD `__HEAD__` with /96 denominators by direct count (Certificates 50/96, Blueprint policy-counted 50/96, Library/Arith 54/96 at HEAD; fresh dev 49/49/53); the PR body literally carries `question_answered:`, `dominating_theorem_search:` and `build_seconds:` and a 「评审判词账」 section; read the LIVE body via `gh pr view __PR__ --json body -q .body`. The prereg issue #7792 carries the two probe comments and the Stage B comment (verify `gh issue view 7792 --json comments -q '.comments|length'` → 3).
