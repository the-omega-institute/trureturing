<!-- open-problem-lane reference: Stage B (implementation-seat) brief template — proof (utility none). -->

# sshx implementation-seat brief — lane `op-a001108` — Stage B for OEIS A001108 (Krizek 2016): PROVE T_n square ⟺ σ(n) and σ(T_n) both odd, n ≥ 1 (this is a PROOF; utility none; ResolutionKind.Proved)

You are the single `implementation_worker` (codex-cli). Return exactly one result envelope (contract appended by the runner). Follow `skills/codex-formalize/SKILL.md` and `tools/scripts/agent/implementation-brief-note.txt` (tracked; read first). Foreground commands; judge by bare exit codes; never pipe a judgment command. **Copy every path in this brief byte-for-byte. Commit + push after every compiling unit.**

## GoalArtifact (complete; cite in visible_inputs; you are `repo-prior-exposed`)
```yaml
raw_user_input: |
  使用 /sshx 最高效率推进数学形式化工作, 1席gpt pro, 其他都使用codex cli, 实现使用codex cli,
  独立复用worktree 工作, 检查主checkout lean缓存够热, 及时pr 到dev并同步, 持续到2027年.
  实施codex cli. gpt pro 传本仓库地址,搜索arvix, 找项目适合解决的开放问题, 持续然后努力解决这些开放问题.
  也没有必要吊在一个问题上, 持续搜接近的.以解决开放问题为第一要务, 可以持续搜索, 解决任何开放问题都可以,
  KPI 是解决开放问题的数量. 找一些老的, erdos上的问题试试.
normalized_goal: |
  常设循环(至 2027):GPT Pro 一席常驻搜题(arXiv + erdosproblems.com 老问题 + OEIS),codex-cli 席探针/实施/评审,
  把外部真开放陈述以 Lean 内核证明或反驳落地到 dev(Lean 模块 + Scribe 镜像 + 冻结 + Problems/ 卷宗 + 解决声明),
  KPI = 机器可数的已解决开放问题数(OPEN_PROBLEM_RESOLUTION 标记数)。任何领域皆可;不吊在单题上。
constraints:
  - 载体:1 席 nyxid-oracle(GPT Pro)搜题;其余一切席位 codex-cli;实施只用 codex-cli(用户 2026-09-13 指令,覆盖 sshx 默认布局)
  - 每席独立 worktree,可复用;本机 codex 席 ≤6–8,同一时刻一个 lean-report;主检出常驻 dev 只同步
  - 先库后证;已知结果不派席;候选进管线前答「文献里有没有」;进展以解决数计,不以模块/席位数计
  - 逃逸内容 + refutes/escape-witness 准入;禁 native_decide;禁 strict;禁 admin;merge=MERGED 才算完成
  - 通信即工件:接手/改判/结案留痕于 issue;PR 正文载产地三项
success_criteria:
  - 每轮:新增 ≥1 个机器可数的开放问题解决(Problems/<slug>.md + Scribe 解决声明 + 冻结定理)合入 dev,或如实报「本轮 0」并给淘汰读数
  - 主检出 .lake 与 dev tip 同步(make lean Built=0 后可作 donor)
  - 在飞 PR 及时合入 dev,不滞留
iteration_question: 距「又一个开放问题以内核证明/反驳落地 dev 并被机器计数」还差什么?
harness:
  provided_capabilities:
    - 宿主后台作业与完成通知(run_in_background)
    - 仓内派席器 tools/scripts/agent/seat/dispatch.sh(codex-cli)与 nyx.sh(nyxid)
    - make 器谱(worktree/lean/deposit/deposit-uncovered/cover/pr-open)与三 required check
    - 常设目标(/goal)由宿主持续驱动(boundary-owner=用户,已在 /goal 文本中确认「持续到2027年」)
  trust_boundary: 用户(τ=0 owner)可信非无误;codex/nyxid 席位不可信、其自报须亲验;机器门(CI 三门)是准入权威
  decision_ownership:
    product_governance_boundary: 用户(选题方向、载体布局、冻结面、元层)
    engineering: codex-cli 实施席 + orchestrator 亲验
    orchestration: claude 主循环(本会话 <session>)
revisions: []
```

**ANCHOR FORM (SL-015, learned on #7383):** the header's `anchors:` entries MUST be written `mathlib/module/Mathlib.<Module>` (one per consumed Mathlib import; D5 imports are not anchors); bare `Mathlib.<Module>` passes `header-check.sh` but is judged red by CI admission (`not a canonical external anchor`). Check with `grep -n '^   anchors:' <module>` BEFORE deposit and fix it first.

**Freeze-last choreography (the repository's live door order; every landed lane used it — #7379, #7383, #7398):** the final Scribe document carries `OpenProblemResolutionClaim` and the dossier carries `motivation_gids`, both of which `make emit` refuses BEFORE the module is frozen (`invalid-problem-resolution-source` / `dangling-problem-gid`), while `make deposit-uncovered` refuses to run without an emitted Blueprint mirror (`PLAYBOOK_INVALID missing Blueprint mirror`). Resolve this cycle deterministically: (a) write the Scribe document WITHOUT the resolution claim and hold the dossier out of the tree (e.g. `git stash`-free: just do not create it yet); `make emit` (claim-free mirror) → EXIT=0; (b) `make deposit-uncovered GID=… BASE=origin/dev` → EXIT=0 (this writes the Freeze event + state pin); (c) add the `OpenProblemResolutionClaim` back and create the dossier; `make emit` twice (second run 0 changed; marker present); `scribe-content-checks` red=0. STOP only on `LEDGER_ALIGN_FAILED`, or on a `PLAYBOOK_INVALID` whose reason is NOT the missing pre-freeze mirror. `PLAYBOOK_INVALID missing Blueprint mirror` before step (a) is the expected state, not a stop condition.




**OUTPUT DISCIPLINE (2026-09-14 incident: a seat was killed by the carrier's upstream prompt filter while echoing the `make preflight` OPEN/OBSERVE stream):** every long door command (`make preflight`, `make lean-report`, `make deposit-uncovered`, `make emit`, `scribe-content-checks.sh`, `serial-lean.sh`, `dotnet test`) MUST be run as `<cmd> > $OP_SCRATCH/<name>-op-a001108.log 2>&1; echo <NAME>_EXIT=$?` and you echo ONLY the exit line plus `tail -5` of the log; find verdicts with `grep` in the file (`^RED`, `DESCRIBE_STATUS`, `PLAYBOOK_`, `LEDGER_ALIGN`). Never print a door's full stream.

## Worktree / state
`<repo>-op-a001108`, branch `lane/math/op-a001108`. The probe seat landed and pushed `D5/S3/Arith/KrizekTriangularSquareSigmaParity.lean` at head `__HEAD__`; `git fetch origin && git merge origin/dev` (merge-only) first, `git status --short` empty. **This is a PROOF lane (utility none), NOT a refutation** — model it on the landed PROOF lanes `D5/S1/Recurrence/StephanA005590ZeroSetCharacterization.lean` (#7742) and `D5/S3/ArithSums/RatajczakGcdSumParityCharacterization.lean` (#7785), both utility none, `ResolutionKind.Proved`. Public declarations EXACTLY: __PUBLIC__. Content lemmas on the live path: __PRIVATE__ — apply the REUSE RULE below: classical characterizations / general-purpose lemmas become PUBLIC (Scribe node + a real consumer in this module; `git grep` origin/dev first for an existing public declaration of the same statement), one-off certificate steps stay private; list them ALL (public and private) in the PR body's escape-content section with their form label. `#print axioms` of every public declaration = std3; probe `lake env lean` EXIT=0; kernel cost __KERNEL__ (report yours). Toolchain: `eval "$(sed -n '/^export PATH=/p' tools/scripts/local-harness-gate.sh)"`, then `make -C tools dotnet` once. Build only with `bash tools/scripts/agent/serial-lean.sh <repo>-op-a001108`.

**Step 0 (BEFORE any Scribe/deposit work; module not frozen yet so editing it is allowed):** run the per-import deletion experiment (probe-reported anchors: __ANCHORS__; delete one, recompile with `lake env lean`, record EXIT, restore) and keep `anchors:` = exactly the consumed modules in `mathlib/module/...` form (never add a transitively-available module as a direct import). Header: `utility: none` (PROOF — do NOT write a certified-instance/refutes line), `generality: I`, `mirror-E: none(waiver:symbolic-proof-no-numeric-artifact)`. Keep the public declarations byte-identical. Add `#print axioms` lines for every public declaration if missing (comment-level edit; statement ids unaffected). Re-measure kernel cost once with `LC_ALL=C /usr/bin/time -l lake env lean -Dprofiler=true -Dtrace.profiler=true -Dtrace.profiler.threshold=1000 <file> > <log> 2>&1` and record wall / type checking / max RSS in the PR body. Commit an anchors/`#print axioms` edit as its own commit before Stage B only if the file needed editing; else skip.

**HARD RULE: after `make deposit-uncovered` has written the Freeze event and state pin, NEVER rewrite history** — no `git reset/rebase/cherry-pick -n/commit --amend/branch -f/push --force*`; synchronization is merge-only; the Freeze commit must remain an ancestor of the PR head.

**REUSE RULE (user directive 2026-09-15):** any helper that is a classical characterization or a general-purpose definition/lemma (σ/τ parity, residue classifications, next-prime, modular fast doubling, 2-adic ultrametric sums, …) must be PUBLIC — with a Scribe node and a real consumer in this module — so later lanes can `import` it as a frozen prerequisite; keep private only one-off certificate steps (concrete `decide` facts). Before making it public, `git grep` origin/dev for an existing public declaration of the same statement (reuse it instead).

## Authority
Preregistration issue **#7840** and its probe-report comment (route as preregistered: form (1) — abundancy product over prime powers + two max-element finset inductions bounding ∏ p/(p−1) ≤ |P|+1 and ∏ (1+1/p) ≤ |P| for |P| ≥ 2 via the counting bound "the largest of |P| distinct primes is ≥ |P|+1"; the seat reported __ROUTE_NOTE__). Proved statement: `result` = Krizek's Jun 08 2013 %C conjecture on A001108 ("Conjecture: the sequence contains exactly those k such that sigma(k) > k*BigOmega(k)"), formalized as ∀ k ≥ 1, (k · Ω(k) < σ(k)) ↔ (k = 1 ∨ Prime k). Do NOT change the public statement(s); keep std3, no `native_decide`. NOT claimed: Firoozbakht's 2014 φ(k)+σ(k) = 2k characterization (classical, different), other A001108 comments. DISCLOSE: R. J. Mathar's Jun 12 2013 note (unproven in general; heuristic to 4.4·10⁶; semiprime case easy) as the evidence of openness.

## Deliverables (all in ONE PR)
1. **Header** (7 lines, `header-check.sh` EXIT=0): `utility: none`; `generality: I`; `mirror-E: none(waiver:symbolic-proof-no-numeric-artifact)`; `anchors:` = consumed imports after minimization; digest ≤ 99 chars. Every PUBLIC declaration gets a Scribe node.
2. **Library note** `Library/Arith/sloane2016a001108.md` — the Arith DOMAIN bucket (the module lives in `D5/S3/Arith`). Copy the frontmatter + `## Verified locator` shape and the split-authorship shape of the landed `Library/Recurrence/sloane2003a005590.md` (#7742; same %A author Sloane) and check the Factorization-bucket note of #7690 (`Library/Factorization/erdos1985a…` — grep the bucket) for bucket conventions: bibkey `sloane2016a001108` (the %A sequence author FIRST), `authors: N. J. A. Sloane; Irina Krizek` (%A author Sloane FIRST, then the %C conjecture author Krizek), `year: 2016` (the %A line is undated for this old core sequence; 2016 is the datable conjecture year — state this in the body), title `"OEIS A001108, a(n)-th triangular number is a square"`, `doi: null`, `url: https://oeis.org/A001108`, `claim:` = the verbatim %N, the verbatim Krizek %C conjecture with attribution `- _Irina Krizek_, Jun 08 2013`, and the verbatim Mathar Jun 12 2013 note, `strata_touched` = the module GID, `license: citation-only`, `triage: anchor`; body records the NAME, the CONJECTURE, an AUTHOR locator `The AUTHOR line is:` / `> _N. J. A. Sloane_` (one sentence: the %A carries no date and 2016 is Krizek's conjecture date), and two sentences: the full characterization is proved, and Mathar's note is the openness evidence. `Library/Arith` is __LIBFAC__/96 on fresh dev (count fresh; limit 96). LibraryNoteRef `D5/L/Arith/sloane2016a001108`.
3. **Problems dossier** `Problems/oeis-a001108-krizek-triangular-square-sigma-parity.md`: copy the structure of the landed PROOF dossier `Problems/oeis-a008590-gcd-sum-parity-characterization.md` (#7785) (front matter `slug` / `bibkey: sloane2016a001108` / `doi: null` / `url: https://oeis.org/A001108` / `triage: theorem` / `motivation_gids:` BLOCK list with the module GID; sections Problem (verbatim %N + Krizek %C + Mathar note; the literal proved statement spelled out; Firoozbakht's comment named as NOT claimed) / Motivation / Gap (dated surfaces: OEIS %C still Conjecture since 2013 with Mathar's "general proof requires…" note, history no settlement; arXiv/MathOverflow as checked by the probe; no priority claim) / Route (the abundancy product; inductions A and B; squarefree vs non-squarefree assembly) / Falsifier / Evidence (module, `result` std3, kernel readings from Step 0; orchestrator + probe numerics) / Triage (`theorem`) / ASSUMED-UNVERIFIED (bounded scans; historical openness after checked surfaces). This is a PROOF: the dossier's resolution is `proved`, not `refuted`. Check the front-matter key set against the landed A008590 dossier (a Falsifier section is required by the live contract).
4. **Scribe** `Blueprint/D5/S3/Arith/KrizekTriangularSquareSigmaParity.scribe.cs`: copy the document shape of the landed PROOF `Blueprint/D5/S3/ArithSums/RatajczakGcdSumParityCharacterization.scribe.cs` (#7785) and look at a sibling in `Blueprint/D5/S3/Arith/` for the bucket's namespace/prefix conventions. Describe nodes for EVERY public declaration (verify the public set from the report): `result` (Theorem, FromRepo, `∀ k ∈ ℕ, k > 0 → (k · Ω(k) < σ(k) ⟺ (k = 1 ∨ Prime(k)))` — render σ and Ω faithfully as the divisor-sum and prime-factor-count functions named in prose; prose naming the two max-element inductions and the squarefree/non-squarefree split; `new OpenProblemResolutionClaim(ProblemSlugRef.Create("oeis-a001108-krizek-triangular-square-sigma-parity"), ResolutionKind.Proved)` — **Proved, NOT Refuted**) plus a Definition node (FromLiterature `LibraryNoteRef.Create("D5/L/Arith/sloane2016a001108")`) for any public definition the probe kept. Visible grouping via `Parenthesized`, never `Grp`; `And`/`Or` right-associative; no relation node inside `LatexGroup.Items`; no `Formula.Fraction` for natural-number division (none expected); NO `Formula.SetBuilder`; no governance vocabulary (SL-032). Then `dotnet test tools/tests/StrataLint.Scribe.Tests/StrataLint.Scribe.Tests.csproj --configuration Release --filter FullyQualifiedName~FormulaCorpusInventoryTests` EXIT=0 and the `DocumentDiscoveryTests` subset (SERIALLY; output to files); `make lean-report` EXIT=0 only if the report is missing/stale (one lean-report per machine — another Stage B seat may be running; on a lock/resource message wait and retry); read the emitted md value by value (binders consistent; no `&&`/`||`/`==`; no free identifier).
5. **Freeze-last door (two phases — do not stop on the pre-freeze `PLAYBOOK_INVALID: missing Blueprint mirror`)**: phase A — Scribe present WITHOUT the `OpenProblemResolutionClaim` node and WITHOUT the dossier → `make emit` (claim-free mirror) EXIT=0 → commit → `make deposit-uncovered GID=D5/S3/Arith/KrizekTriangularSquareSigmaParity.result BASE=origin/dev` EXIT=0 (STOP only on `LEDGER_ALIGN_FAILED`, or a `PLAYBOOK_INVALID` other than "missing Blueprint mirror"; on `DEPOSIT_HEADER_CHECK_INVALID protected base must be an ancestor of HEAD` merge origin/dev and retry; no hand edits under `Golden/`). Phase B — restore the claim node and the dossier → `make emit` twice (second `0 changed`; marker present with `proved`) → `B=$(git merge-base HEAD origin/dev); bash tools/scripts/workflow/scribe-content-checks.sh .lake/build/stratalint/raw-lean-report.json tools/StrataLint.Scribe.Documents/bin/Release/net10.0/StrataLint.Scribe.Documents.dll "$B" > <log> 2>&1` → `grep '^RED'` empty → header-check EXIT=0. Verify the accepted event's non-`_private` declarations equal the Lean report's public set and one Scribe truth anchor per public declaration — report both counts and list the `_private` names.
6. ONE builder commit for freeze + Scribe + note + dossier (subject `prove: OEIS A001108 — σ(k) > k·Ω(k) exactly when k = 1 or k is prime (Krizek 2013 conjecture)`); tree clean; late dedupe on fresh `origin/dev` (`A001108`, `KrizekTriangularSquare`, `sloane2016a001108`, `Krizek`); `make preflight BASE=$(git merge-base HEAD origin/dev)` once, output to a file (host noise does not stop you; only a content-level red naming this module does).
7. `git push`; PR message file `$OP_SCRATCH/pr-op-a001108.md` (line 1 = title `prove: OEIS A001108 — σ(k) > k·Ω(k) exactly when k = 1 or k is prime (Krizek 2013 conjecture)`; body = 产地三项【skill 上下文 / 载体与分工 / 混合方式】 as facts about THIS implementation — skill-context line MUST contain the literal `consensus-rnd:sshx`（会话 <session>，目标 #7333）; 载体: **orchestrator local OEIS full-text scan** (the GPT Pro search seat was unavailable that day — pool outage; state this plainly) → orchestrator (verbatim %N/%C + Mathar note; independent numerics by two methods: zero mismatches to 3·10⁵ and to 2·10⁴; dedupe; proof route) → prereg #7840 (before the probe) → codex probe `s<session>-p-a008578` → this codex Stage B seat; 混合方式 must end with the literal sentence 「三席盲评（architecture / quality / tests，均为 codex-cli，各自独立 worktree，互不见对方判词）由 orchestrator 派出并逐项亲验；**判卷面无异模型共识**：本候选由 orchestrator（claude 主循环）本地 OEIS 扫描得出、无 GPT Pro 搜题席参与（池当日不可用），探针、实施与评审均为 codex 模型族——判卷面单一模型族，不冒充多样性共识。未开启 auto-merge。」 then 「评审席判词见本正文『评审判词账』小节。」 + 目标与来源 (#7333, #7840, verbatim %N + Krizek %C + Mathar note) + 回声表 (`sigma(k)` ↔ `σ 1 k`; `BigOmega(k)` ↔ `Ω k`; "the sequence contains exactly those k" ↔ the iff with `k = 1 ∨ Prime k`; Firoozbakht comment not claimed) + 检索留痕 (commands + hit counts, dated) + 「逃逸内容」(`result` content; escape witness **form (1)** = the private content lemmas on the live path, listed by name (inductions A and B with the "largest prime ≥ |P|+1" counting bound; the abundancy product identity if proved as a private lemma); direct frozen deps none; admission_basis: escape-witness; `utility: none` per-declaration — a symbolic inequality classification proof, none of the four §3.3 classes (cf. A005590 #7742, A008590 #7785); `question_answered:` = are the k with σ(k) > k·Ω(k) exactly 1 and the primes? yes, proved (prereg #7840); `dominating_theorem_search:` = not-found-in-searched-scope; `build_seconds:` = null with kernel profile wall / tc / RSS from Step 0) + 读数 (HEAD SHA; exit codes; `#print axioms`; kernel-cost; Scribe test counts; freeze `event_hash` + `statement_id`; the emitted marker line with `proved`; per-import EXIT table; public-declaration count = truth-anchor count; bucket counts at HEAD with `/96` measured by DIRECT file count `git ls-tree --name-only HEAD -- <dir>/` (NOT recursive): `D5/S3/Arith` (fresh dev __FAC__ + 1, DIRECT count — the Arith root has sub-buckets), `Blueprint/D5/S3/Arith` policy-counted `.scribe.cs` (__BFAC__ + 1), `Library/Arith` (fresh dev __LIBFAC__ + 1)) + 「未做」(no claim on Firoozbakht's characterization or other comments; no arXiv-complete claim; no priority claim; no auto-merge). **No pending-tense sentence anywhere; no session URL (§2.10).** Then `make pr-open HEAD=lane/math/op-a001108 MESSAGE=$OP_SCRATCH/pr-op-a001108.md` (NO AUTO_MERGE; output to a file); report `PR_WATCH_RESULT` and the PR number; do not push after the PR opens.

## Result envelope
`conclusion` = {"verdict":"success|open|already-landed","pr_number":n,"branch":"lane/math/op-a001108","head":"<sha>","module_path":"...","theorem_names":[...],"public_declarations":[...],"private_declarations":[...],"scribe_truth_anchors":n,"print_axioms":{...},"kernel_cost":{"wall_s":x,"type_checking_ms":x,"max_rss_bytes":x},"exit_codes":{"serial_lean":n,"header_check":n,"scribe_tests":n,"make_lean_report":n,"make_emit":n,"scribe_content_checks":n,"deposit_uncovered":n,"preflight":n,"push":n,"pr_open":n},"imports_table":{"<import>":"EXIT when deleted"},"utility_kind":"none","resolution_kind":"proved","dossier_path":"...","library_note_path":"...","resolution_marker_present":true|false,"freeze_event_hash":"...","statement_id":"...","search_trace":[...],"plan_deviations":[...],"assumed_unverified":[...],"open_reasons":[...],"visible_inputs":["GoalArtifact(complete)","prereg #7840 + probe comment","probe module @ __HEAD__","repo-prior-exposed"]}; `log_ref` = a path.
