# open-problem-lane reference: Review target template — proof.
Open problem (external, no atom; OLD-ish (2013 OEIS %C, still Conjecture, with a same-year editor note that the general proof is missing); PROOF (not a refutation) by the abundancy product over prime powers plus two max-element inductions over the prime-factor set; generality I; utility none): OEIS A008578 (`%A` N. J. A. Sloane, undated; the proved conjecture is Irina Gerasimova's %C, Jun 08 2013; R. J. Mathar's Jun 12 2013 note records the general case as unproven, "heuristically correct at least up to k <= 4400000", with the semiprime case easy). `%N` (verbatim): "Prime numbers at the beginning of the 20th century (today 1 is no longer regarded as a prime)." — i.e. {1} ∪ primes. Proved `%C` (Gerasimova, verbatim): "Conjecture: the sequence contains exactly those k such that sigma(k) > k*BigOmega(k)." Preregistration issue #7819 (opened BEFORE the probe; probe comment @ __PROBEHEAD__ records the route; Stage B comment records the freeze). This is a PROOF: the resolution is `Proved`, NOT refuted. NOT claimed: Firoozbakht's Aug 03 2014 comment (φ(k) + σ(k) = 2k — a different, classical characterization) or any other A008578 comment.

Formal target (public surface EXACTLY __PUBLIC__): `theorem result : ∀ k : ℕ, 0 < k → (k * Ω k < σ 1 k ↔ (k = 1 ∨ Nat.Prime k))` with Mathlib `ArithmeticFunction.sigma 1` and `ArithmeticFunction.cardFactors` (Ω). Private helpers (KEEP private; on the live path; the form-(1) escape witnesses): __PRIVATE__ — the two max-element inductions over a finite set P of primes: (A) ∏_{p∈P} p/(p−1) ≤ |P| + 1 and (B) ∏_{p∈P} (1 + 1/p) ≤ |P| for |P| ≥ 2, both resting on the counting bound "the largest of |P| distinct primes is ≥ |P| + 1" (the other |P| − 1 primes are distinct integers in [2, q−1]); the abundancy identity σ(k)/k = ∏_{p^e ∥ k} (1 + 1/p + ⋯ + 1/p^e); and the assembly: non-squarefree k gives σ(k)/k < ω(k) + 1 ≤ Ω(k) via (A) and the geometric bound 1 + 1/p + ⋯ + 1/p^e < p/(p−1); squarefree composite k gives σ(k)/k ≤ ω(k) = Ω(k) via (B). The (⇐) direction is k = 1 (σ = 1 > 0) and primes (σ(p) = p + 1 > p).

REVIEWERS: judge per §3.2/§3.3. (1) PROOF SHAPE: `result` is content — the escape witnesses are form (1), the private inductions (A)/(B) (genuine finset inductions with a counting argument; not instantiations of any pinned Mathlib inequality) feeding the squarefree/non-squarefree classification; confirm they are on the live derivation path of `result` (delete-and-check). Contrast the bind-only trap (A187941): here nothing is a Mathlib-rewrite value lemma. This is the classification/induction shape §3.2 admits (cf. the landed proofs A005590 #7742, A008590 #7785, A226857 #7666). (2) UTILITY: `utility: none` is correct — a symbolic inequality classification, none of the four §3.3 computational classes. This is a PROOF, so `ResolutionKind.Proved` (verify the Scribe uses Proved, NOT Refuted). std3; no native_decide; no sorry. direct frozen dependencies none; `admission_basis: escape-witness`.

Module `D5/S3/Factorization/GerasimovaSigmaOmegaPrimeCharacterization.lean` (Factorization = prime-factorization structure per Meta/domains.yaml; sibling ErdosConsecutiveProductSquarefreeFactorRefutation #7690; fresh dev (merge-base __BASE__) 35 → 36/96 at HEAD by DIRECT file count; DirectoryFileLimit 96); imports __IMPORTS__ (anchors in `mathlib/module/…` form; seat-reported deletion table in the PR body); generality I; `mirror-E: none(waiver:symbolic-proof-no-numeric-artifact)`. `#print axioms` for every public declaration = `[propext, Classical.choice, Quot.sound]`. Kernel cost (seat-reported): __KERNEL__. Dossier `Problems/oeis-a008578-gerasimova-sigma-omega-prime-characterization.md` (doi null, url https://oeis.org/A008578, motivation_gids = module GID, resolution proved) + Library note `Library/Factorization/sloane2013a008578.md` (Factorization DOMAIN bucket, fresh dev 17 → 18/96; bibkey `sloane2013a008578` follows the %A sequence author Sloane, `authors: N. J. A. Sloane; Irina Gerasimova` — %A author first, then the %C conjecture author Gerasimova; the %A line is undated so year 2013 is the datable conjecture year, disclosed in the note; AUTHOR locator present; Mathar's note quoted as the openness evidence; `LibraryNoteRef.Create("D5/L/Factorization/sloane2013a008578")`). Scribe renders `result` as the guarded iff with σ and Ω named in prose, `ResolutionKind.Proved`; NO `Formula.SetBuilder`; no `Formula.Fraction` for natural division. Freeze commit __FREEZE__ (event `__EVENT__`, module statement id `__STMT__`) is an ancestor of PR HEAD `__HEAD__`; post-freeze commits are only origin/dev merges (no post-freeze content commit; module bytes vs probe __PROBEHEAD__ differ at most by the pre-freeze header alignment and `#print axioms` lines). Numerics (orchestrator, independent, two methods): zero mismatches for k ≤ 3·10⁵ (own sieve) and k ≤ 2·10⁴ (sympy); Mathar's heuristic check to 4.4·10⁶. Bounded scans carry no proof. Search seat: NONE — the GPT Pro pool was unavailable that day; the candidate came from the orchestrator's local OEIS full-text scan (disclosed in the PR body); orchestrator: verbatim %N/%C + Mathar note, independent recomputation, dedupe, proof route; probe/impl/review = codex (判卷面单一模型族，无 GPT Pro 参与). Literature (probe): as recorded in the PR body; no priority claim. Review contract: public surface exactly __PUBLIC__; `result` the guarded iff with Proved resolution; Firoozbakht's characterization NOT claimed; no bind-only public theorems (the private inductions are content); std3; no native_decide; every anchor import consumed (deletion EXIT=1) and no unused direct import; PR body readings measured at HEAD `__HEAD__` with /96 denominators by direct count (Factorization 36/96, Blueprint policy-counted 36/96, Library/Factorization 18/96; fresh dev 35/35/17); the PR body literally carries `question_answered:`, `dominating_theorem_search:` and `build_seconds:` and a 「评审判词账」 section; read the LIVE body via `gh pr view __PR__ --json body -q .body`. The prereg issue #7819 carries three comments: the probe seat's own short report, the orchestrator's probe transcription, and the Stage B comment (verify `gh issue view 7819 --json comments -q '.comments|length'` → 3).
