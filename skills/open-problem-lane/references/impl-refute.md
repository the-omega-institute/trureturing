<!-- open-problem-lane reference: Stage B (implementation-seat) brief template — refutation; `__PLACEHOLDER__` fields filled by sed, never by python f-strings. -->

# sshx implementation-seat brief — lane `op-a000040` — Stage B for OEIS A000040 (Detlefs 2014 %F): composite 219781 = 271·811 breaks "primes = {5} ∪ {n≠5 : F(n) ≡ ±1 (mod n) ∧ 2^(n−1) ≡ 1 (mod n)}" (REFUTED, finite certificate, form (2); utility certified-instance)

You are the single `implementation_worker` (codex-cli). Return exactly one result envelope (contract appended by the runner). Follow `skills/codex-formalize/SKILL.md` and `tools/scripts/agent/implementation-brief-note.txt` (tracked; read first). Foreground commands; judge by bare exit codes; never pipe a judgment command. **Copy every path in this brief byte-for-byte. Commit + push after every compiling unit.**

## GoalArtifact (complete; cite in visible_inputs; you are `repo-prior-exposed`)
```yaml
raw_user_input: |
  使用 /sshx 最高效率推进数学形式化工作, 1席gpt pro, 其他都使用codex cli, 实现使用codex cli,
  独立复用worktree 工作, 检查主checkout lean缓存够热, 及时pr 到dev并同步, 持续到2027年.
  实施codex cli. gpt pro 传本仓库地址,搜索arvix, 找项目适合解决的开放问题, 持续然后努力解决这些开放问题.
  也没有必要吊在一个问题上, 持续搜接近的.以解决开放问题为第一要务, 可以持续搜索, 解决任何开放问题都可以,
  KPI 是解决开放问题的数量. 找一些老的, erdos上的问题试试.
normalized_goal: |
  常设循环(至 2027):GPT Pro 一席常驻搜题(arXiv + erdosproblems.com 老问题 + OEIS),codex-cli 席探针/实施/评审,
  把外部真开放陈述以 Lean 内核证明或反驳落地到 dev(Lean 模块 + Scribe 镜像 + 冻结 + Problems/ 卷宗 + 解决声明),
  KPI = 机器可数的已解决开放问题数(OPEN_PROBLEM_RESOLUTION 标记数)。任何领域皆可;不吊在单题上。
constraints:
  - 载体:1 席 nyxid-oracle(GPT Pro)搜题;其余一切席位 codex-cli;实施只用 codex-cli(用户 2026-09-13 指令,覆盖 sshx 默认布局)
  - 每席独立 worktree,可复用;本机 codex 席 ≤6–8,同一时刻一个 lean-report;主检出常驻 dev 只同步
  - 先库后证;已知结果不派席;候选进管线前答「文献里有没有」;进展以解决数计,不以模块/席位数计
  - 逃逸内容 + refutes/escape-witness 准入;禁 native_decide;禁 strict;禁 admin;merge=MERGED 才算完成
  - 通信即工件:接手/改判/结案留痕于 issue;PR 正文载产地三项
success_criteria:
  - 每轮:新增 ≥1 个机器可数的开放问题解决(Problems/<slug>.md + Scribe 解决声明 + 冻结定理)合入 dev,或如实报「本轮 0」并给淘汰读数
  - 主检出 .lake 与 dev tip 同步(make lean Built=0 后可作 donor)
  - 在飞 PR 及时合入 dev,不滞留
iteration_question: 距「又一个开放问题以内核证明/反驳落地 dev 并被机器计数」还差什么?
harness:
  provided_capabilities:
    - 宿主后台作业与完成通知(run_in_background)
    - 仓内派席器 tools/scripts/agent/seat/dispatch.sh(codex-cli)与 nyx.sh(nyxid)
    - make 器谱(worktree/lean/deposit/deposit-uncovered/cover/pr-open)与三 required check
    - 常设目标(/goal)由宿主持续驱动(boundary-owner=用户,已在 /goal 文本中确认「持续到2027年」)
  trust_boundary: 用户(τ=0 owner)可信非无误;codex/nyxid 席位不可信、其自报须亲验;机器门(CI 三门)是准入权威
  decision_ownership:
    product_governance_boundary: 用户(选题方向、载体布局、冻结面、元层)
    engineering: codex-cli 实施席 + orchestrator 亲验
    orchestration: claude 主循环(本会话 <session>)
revisions: []
```

**ANCHOR FORM (SL-015, learned on #7383):** the header's `anchors:` entries MUST be written `mathlib/module/Mathlib.<Module>` (one per consumed Mathlib import; D5 imports are not anchors); bare `Mathlib.<Module>` passes `header-check.sh` but is judged red by CI admission (`not a canonical external anchor`). Check with `grep -n '^   anchors:' <module>` BEFORE deposit and fix it first.

**Freeze-last choreography (the repository's live door order; every landed lane used it — #7379, #7383, #7398):** the final Scribe document carries `OpenProblemResolutionClaim` and the dossier carries `motivation_gids`, both of which `make emit` refuses BEFORE the module is frozen (`invalid-problem-resolution-source` / `dangling-problem-gid`), while `make deposit-uncovered` refuses to run without an emitted Blueprint mirror (`PLAYBOOK_INVALID missing Blueprint mirror`). Resolve this cycle deterministically: (a) write the Scribe document WITHOUT the resolution claim and hold the dossier out of the tree (e.g. `git stash`-free: just do not create it yet); `make emit` (claim-free mirror) → EXIT=0; (b) `make deposit-uncovered GID=… BASE=origin/dev` → EXIT=0 (this writes the Freeze event + state pin); (c) add the `OpenProblemResolutionClaim` back and create the dossier; `make emit` twice (second run 0 changed; marker present); `scribe-content-checks` red=0. STOP only on `LEDGER_ALIGN_FAILED`, or on a `PLAYBOOK_INVALID` whose reason is NOT the missing pre-freeze mirror. `PLAYBOOK_INVALID missing Blueprint mirror` before step (a) is the expected state, not a stop condition.




**OUTPUT DISCIPLINE (2026-09-14 incident: a seat was killed by the carrier's upstream prompt filter while echoing the `make preflight` OPEN/OBSERVE stream):** every long door command (`make preflight`, `make lean-report`, `make deposit-uncovered`, `make emit`, `scribe-content-checks.sh`, `serial-lean.sh`, `dotnet test`) MUST be run as `<cmd> > $OP_SCRATCH/<name>-op-a000040.log 2>&1; echo <NAME>_EXIT=$?` and you echo ONLY the exit line plus `tail -5` of the log; find verdicts with `grep` in the file (`^RED`, `DESCRIBE_STATUS`, `PLAYBOOK_`, `LEDGER_ALIGN`). Never print a door's full stream.

## Worktree / state
`<repo>-op-a000040`, branch `lane/math/op-a000040`. The probe seat (attempt 2) landed and pushed `D5/S0/Certificates/DetlefsFibonacciFermatPrimeCharacterizationRefutation.lean` at head `__HEAD__`; `git fetch origin && git merge origin/dev` (merge-only) first, `git status --short` empty. Public declarations EXACTLY (five): `fibTest (n : ℕ) : Prop := Nat.fib n % n = 1 ∨ Nat.fib n % n = n - 1`, `fermatTest (n : ℕ) : Prop := 2 ^ (n - 1) % n = 1`, `inDetlefsSet (n : ℕ) : Prop := n = 5 ∨ (n ≠ 5 ∧ fibTest n ∧ fermatTest n)`, `claim : Prop := ∀ n : ℕ, 0 < n → (Nat.Prime n ↔ inDetlefsSet n)`, `theorem result : ¬ claim`. NO private declarations (the n = 219781 certificate is inline: `Nat.fastFib 219781 % 219781 = 1` by `decide` transported through `Nat.fastFib_eq`; `(2 : ZMod 219781)^219780 = 1` by `reduce_mod_char` transported to `2 ^ 219780 % 219781 = 1`; `¬ Nat.Prime 219781` by `norm_num` (271 · 811); then `(hclaim 219781 _).mpr` yields `Nat.Prime 219781`, contradiction). `#print axioms` all five std3; probe `lake env lean` EXIT=0; kernel cost __KERNEL__ (report yours). Toolchain: `eval "$(sed -n '/^export PATH=/p' tools/scripts/local-harness-gate.sh)"`, then `make -C tools dotnet` once. Build only with `bash tools/scripts/agent/serial-lean.sh <repo>-op-a000040`.

**Step 0 (BEFORE any Scribe/deposit work; module not frozen yet):** re-run the per-import deletion experiment (probe-reported anchors: __ANCHORS__); keep `anchors:` = exactly the EXIT=1 imports in `mathlib/module/...` form (never add a transitively-available module as a direct import). Keep the REFUTATION utility line VERBATIM: `utility: kind=certified-instance; basis=refutes=gid:D5/S0/Certificates/DetlefsFibonacciFermatPrimeCharacterizationRefutation.claim; result=D5/S0/Certificates/DetlefsFibonacciFermatPrimeCharacterizationRefutation.result; claim=D5/S0/Certificates/DetlefsFibonacciFermatPrimeCharacterizationRefutation.claim`. Keep `generality: I`, `mirror-E: none(waiver:kernel-checked-refutation)`. Keep the five public declarations byte-identical. Re-measure kernel cost once and record wall / type checking / max RSS in the PR body. Commit an anchors fix as its own commit before Stage B only if the header needed editing; else skip.

**HARD RULE: after `make deposit-uncovered` has written the Freeze event and state pin, NEVER rewrite history** — merge-only; the Freeze commit must remain an ancestor of the PR head.

**REUSE RULE (user directive 2026-09-15):** any helper that is a classical characterization or a general-purpose definition/lemma (σ/τ parity, residue classifications, next-prime, modular fast doubling, 2-adic ultrametric sums, …) must be PUBLIC — with a Scribe node and a real consumer in this module — so later lanes can `import` it as a frozen prerequisite; keep private only one-off certificate steps (concrete `decide` facts). Before making it public, `git grep` origin/dev for an existing public declaration of the same statement (reuse it instead).

## Authority
Preregistration issue **#7792** and its two comments (attempt-1 report + the orchestrator's route re-preregistration and proof-shape ruling: the direct `decide` certificate is **form (2) content**, exactly the shape of the landed 3/3-approved refutations A049591 #7703, A103585 #7717, A129598 #7791; do NOT write "bind-only" anywhere). Refuted statement: `claim` = Detlefs's May 25 2014 %F conjecture on A000040 "Conjecture: Sequence = {5 and n <> 5| ( Fibonacci(n) mod n = 1 or Fibonacci(n) mod n = n - 1) and 2^(n-1) mod n = 1}", formalized as ∀ n ≥ 1, Prime n ↔ (n = 5 ∨ (n ≠ 5 ∧ fibTest n ∧ fermatTest n)). Witness n = 219781 = 271·811: in the set (both tests pass) but composite. Do NOT change the five public statements; keep std3, no `native_decide`. NOT claimed: the May 28 2014 variant (`2^(3n) mod 3n = 8`; only DISCLOSE that 219781 also satisfies it), Detlefs's Sep 10 2010 Wilson-type characterizations in the same %F block, any theory or novelty of Fibonacci/Fermat pseudoprimes (219781 is a KNOWN Fibonacci-type pseudoprime listed in A094401, A093372, A212424 — attribute as prior art). DISCLOSE everywhere (note/dossier/body): the literal statement ALSO fails at the degenerate boundary n = 2 (2^1 mod 2 = 0, so the prime 2 is outside the set); the witness used is the substantive composite 219781; 1..10⁶ contains exactly seven such composites (219781, 252601, 399001, 512461, 722261, 741751, 852841), 219781 least.

## Deliverables (all in ONE PR)
1. **Header** (7 lines, `header-check.sh` EXIT=0): certified-instance/refutes `utility:` line verbatim; `generality: I`; `anchors:` = required consumed imports; digest ≤ 99 chars. Every PUBLIC declaration (all five) gets a Scribe node.
2. **Library note** `Library/Arith/sloane2014a000040.md` — the Arith DOMAIN bucket. Copy the frontmatter + `## Verified locator` shape and split-authorship shape of the landed `Library/Arith/laboselemer2002a049591.md` (#7703): bibkey `sloane2014a000040` (the %A sequence author FIRST), `authors: N. J. A. Sloane; Gary Detlefs` (%A author Sloane FIRST, then the %F conjecture author Detlefs), `year: 2014` (the %A line is undated; 2014 is the datable conjecture year — state this in the body), title `"OEIS A000040, The prime numbers"`, `doi: null`, `url: https://oeis.org/A000040`, `claim:` = the verbatim %N (`The prime numbers.`) and the verbatim May 25 2014 %F conjecture line with its attribution `- _Gary Detlefs_, May 25 2014` (quote the May 28 2014 variant too, marked as disclosed-not-claimed), `strata_touched` = the module GID, `license: citation-only`, `triage: anchor`; the locator quotes %N, both %F lines, notes in two sentences that only the May 25 line is refuted (witness 219781 = 271·811 passes both tests; the literal statement also excludes the prime 2 at the degenerate boundary), and attributes 219781's pseudoprime status to A094401 / A093372 / A212424. `Library/Arith` is __ARITH__/96 on fresh dev (count fresh; limit 96). LibraryNoteRef `D5/L/Arith/sloane2014a000040`.
3. **Problems dossier** `Problems/oeis-a000040-detlefs-fibonacci-fermat-prime-characterization-refutation.md`: copy the structure of the landed `Problems/oeis-a049591-prime-gap-divisor-characterization-refutation.md` (front matter `slug` / `bibkey: sloane2014a000040` / `doi: null` / `url: https://oeis.org/A000040` / `triage: theorem` / `motivation_gids:` BLOCK list with the module GID; sections Problem (verbatim %N + both %F lines; the literal refuted statement with fibTest/fermatTest/inDetlefsSet spelled out; the May 28 variant and the 2010 Wilson-type lines named as NOT claimed) / Motivation / Gap (dated surfaces: A000040 current text still labels both 2014 lines Conjecture with no refutation note, newest 10 revisions checked, older history anonymous-locked (ASSUMED-UNVERIFIED); arXiv/OpenAlex/MathOverflow/GitHub as checked by the probe; OEIS `seq:219781,252601,399001` no results; prior art A094401/A093372/A212424; no priority claim) / Route (the direct certificate: fastFib decide, reduce_mod_char, norm_num compositeness) / Falsifier / Evidence (module, `result` std3, kernel readings; orchestrator + probe numerics: seven composites ≤ 10⁶, prime 2 the only prime outside, two independent Fibonacci residue computations) / Triage (`theorem`) / ASSUMED-UNVERIFIED (older OEIS history; bounded scans; literature completeness). Check the front-matter key set against the landed A049591 dossier (a Falsifier section is required by the live contract).
4. **Scribe** `Blueprint/D5/S0/Certificates/DetlefsFibonacciFermatPrimeCharacterizationRefutation.scribe.cs`: copy the document shape of `Blueprint/D5/S0/Certificates/CloitrePrimeGapDivisorCharacterizationRefutation.scribe.cs` (#7703). Describe nodes for EVERY public declaration (verify the five from the report): `fibTest` (Definition, `FromLiterature(LibraryNoteRef.Create("D5/L/Arith/sloane2014a000040"))`, `F(n) mod n = 1 ∨ F(n) mod n = n − 1` — render `Nat.fib` as the Fibonacci function and `%` with the corpus's existing `mod` rendering (grep the landed corpus for how `%` is rendered, e.g. in KarttunenGcdTotientPositionRefutation or other Certificates), natural subtraction faithfully), `fermatTest` (Definition, FromLiterature, `2^{n−1} mod n = 1`), `inDetlefsSet` (Definition, FromLiterature, the disjunction with `n ≠ 5`), `claim` (Definition, FromLiterature, `∀ n ∈ ℕ, n > 0 → (Prime(n) ⟺ inDetlefsSet(n))`), `result` (Theorem, FromRepo, `¬claim`, prose naming 219781 = 271·811, both residues, the n = 2 boundary and the prior-art attribution; `new OpenProblemResolutionClaim(ProblemSlugRef.Create("oeis-a000040-detlefs-fibonacci-fermat-prime-characterization-refutation"), ResolutionKind.Refuted)`). Visible grouping via `Parenthesized`, never `Grp`; `And`/`Or` right-associative; no relation node inside `LatexGroup.Items`; no `Formula.Fraction`; NO `Formula.SetBuilder`; no governance vocabulary (SL-032). Then the two Scribe test subsets (FormulaCorpusInventoryTests, DocumentDiscoveryTests; SERIALLY; output to files) EXIT=0; `make lean-report` only if missing/stale (one lean-report per machine; if another lane's report is running, wait); read the emitted md value by value.
5. **Freeze-last door (two phases)**: phase A — Scribe WITHOUT the `OpenProblemResolutionClaim` node and WITHOUT the dossier → `make emit` EXIT=0 → commit → `make deposit-uncovered GID=D5/S0/Certificates/DetlefsFibonacciFermatPrimeCharacterizationRefutation.result BASE=origin/dev` EXIT=0 (STOP only on `LEDGER_ALIGN_FAILED`, or a `PLAYBOOK_INVALID` other than "missing Blueprint mirror"; on `DEPOSIT_HEADER_CHECK_INVALID protected base must be an ancestor of HEAD` merge origin/dev and retry; no hand edits under `Golden/`). Phase B — restore the claim node and the dossier → `make emit` twice (second `0 changed`; marker `refuted`) → `scribe-content-checks.sh` on the merge-base → `grep '^RED'` empty → header-check EXIT=0. Verify the accepted event's non-`_private` declarations equal the Lean report's public set (expected the five) and one Scribe truth anchor per public declaration — report both counts.
6. ONE builder commit for freeze + Scribe + note + dossier (subject `refute: OEIS A000040 — composite 219781 = 271·811 breaks Detlefs's 2014 Fibonacci–Fermat prime characterization`); tree clean; late dedupe on fresh `origin/dev` (`A000040`, `DetlefsFibonacciFermat`, `sloane2014a000040`, `219781`, `inDetlefsSet`); `make preflight BASE=$(git merge-base HEAD origin/dev)` once, output to a file (host noise does not stop you; only a content-level red naming this module does).
7. `git push`; PR message file `$OP_SCRATCH/pr-op-a000040.md` (line 1 = title `refute: OEIS A000040 — composite 219781 = 271·811 breaks Detlefs's 2014 Fibonacci–Fermat prime characterization`; body = 产地三项【skill 上下文 / 载体与分工 / 混合方式】 as facts about THIS implementation — skill-context line MUST contain the literal `consensus-rnd:sshx`（会话 <session>，目标 #7333）; 载体: **orchestrator local OEIS full-text scan** (the GPT Pro search seat was unavailable that day — pool outage; state this plainly) → orchestrator (verbatim %N/%F; independent Python by two methods: seven composites ≤ 10⁶ pass both tests, 219781 least, prime 2 the only prime outside; OEIS current text and revision surface; prior-art search; dedupe) → prereg #7792 (before the probe) → codex probe attempt 1 (scratch proof; self-judged bind-only) → orchestrator ruling + route re-preregistration on #7792 → codex probe attempt 2 (module landed) → this codex Stage B seat; 混合方式 must end with the literal sentence 「三席盲评（architecture / quality / tests，均为 codex-cli，各自独立 worktree，互不见对方判词）由 orchestrator 派出并逐项亲验；**判卷面无异模型共识**：本候选由 orchestrator（claude 主循环）本地 OEIS 扫描得出、无 GPT Pro 搜题席参与（池当日不可用），探针、实施与评审均为 codex 模型族——判卷面单一模型族，不冒充多样性共识。未开启 auto-merge。」 then 「评审席判词见本正文『评审判词账』小节。」 + 目标与来源 (#7333, #7792, verbatim %N + both %F lines; the n = 2 boundary; the prior art) + 回声表 (`Fibonacci(n) mod n = 1 or … = n - 1` ↔ `fibTest`; `2^(n-1) mod n = 1` ↔ `fermatTest`; `{5 and n <> 5 | …}` ↔ `inDetlefsSet`; `Sequence = …` ↔ `claim`; `result : ¬ claim` via 219781; May 28 variant / 2010 lines not claimed) + 检索留痕 + 「逃逸内容」(`result` content; escape witness **form (2)** = the inline finite certificate at n = 219781 (compositeness 271·811 by norm_num; `2^219780 % 219781 = 1` via reduce_mod_char; `Nat.fib 219781 % 219781 = 1` via `Nat.fastFib_eq` + decide) — no private lemma; direct frozen deps none; admission_basis: escape-witness; `utility: kind=certified-instance; basis=refutes` — four defs + one certified finite counterexample refuting the named ∀-iff (cf. A049591 #7703, A103585 #7717, A129598 #7791); `question_answered:` = is {5} ∪ {n≠5 : fibTest ∧ fermatTest} exactly the set of primes? no — 219781 is in it and composite (and 2 is prime but outside); `dominating_theorem_search:` = not-found-in-searched-scope; `build_seconds:` = null with the kernel profile) + 读数 (HEAD SHA; exit codes; `#print axioms`; kernel-cost; Scribe test counts; freeze `event_hash` + `statement_id`; the emitted marker; per-import EXIT table; public-declaration count = truth-anchor count = 5; bucket counts at HEAD with `/96` measured by DIRECT file count `git ls-tree --name-only HEAD -- <dir>/` (NOT recursive — `D5/S0/Certificates` has sub-buckets): `D5/S0/Certificates` (fresh dev __CERT__ + 1), `Blueprint/D5/S0/Certificates` policy-counted `.scribe.cs` (__BCERT__ + 1), `Library/Arith` (__ARITH__ + 1)) + 「未做」(no claim on the May 28 variant or the 2010 Wilson-type lines; no pseudoprime theory/novelty claim; no arXiv-complete claim; no priority claim; no auto-merge). **No pending-tense sentence anywhere; no session URL (§2.10).** Then `make pr-open HEAD=lane/math/op-a000040 MESSAGE=$OP_SCRATCH/pr-op-a000040.md` (NO AUTO_MERGE; output to a file); report `PR_WATCH_RESULT` and the PR number; do not push after the PR opens.

## Result envelope
`conclusion` = {"verdict":"success|open|already-landed","pr_number":n,"branch":"lane/math/op-a000040","head":"<sha>","module_path":"...","theorem_names":[...],"public_declarations":[...],"private_declarations":[...],"scribe_truth_anchors":n,"print_axioms":{...},"kernel_cost":{"wall_s":x,"type_checking_ms":x,"max_rss_bytes":x},"exit_codes":{"serial_lean":n,"header_check":n,"scribe_tests":n,"make_lean_report":n,"make_emit":n,"scribe_content_checks":n,"deposit_uncovered":n,"preflight":n,"push":n,"pr_open":n},"imports_table":{"<import>":"EXIT when deleted"},"utility_kind":"certified-instance","dossier_path":"...","library_note_path":"...","resolution_marker_present":true|false,"freeze_event_hash":"...","statement_id":"...","search_trace":[...],"plan_deviations":[...],"assumed_unverified":[...],"open_reasons":[...],"visible_inputs":["GoalArtifact(complete)","prereg #7792 + two comments","probe module @ __HEAD__","repo-prior-exposed"]}; `log_ref` = a path.
