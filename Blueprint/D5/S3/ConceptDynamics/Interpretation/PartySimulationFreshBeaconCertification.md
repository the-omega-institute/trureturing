# Party Simulation and Fresh-Beacon Certification

## Abstract

Party-simulable certificates have bounded approval, while a fresh beacon gives a product-law guarantee with an explicit total-variation charge.

**Theorem 1.1 (Simulation necessity and fresh-beacon sufficiency).**

$$\begin{gathered}\forall Seed, VerifierCoin, Input, Output, Certificate, Anchor, Task: \operatorname{Type},\\{}[\operatorname{Finite}(Seed)], [\operatorname{MeasurableSpace}(Seed)], [\operatorname{MeasurableSingletonClass}(Seed)],\\{}[\operatorname{Finite}(VerifierCoin)], [\operatorname{MeasurableSpace}(VerifierCoin)], [\operatorname{MeasurableSingletonClass}(VerifierCoin)],\\{}[\operatorname{MeasurableSpace}(Input)], [\operatorname{MeasurableSpace}(Output)], [\operatorname{MeasurableEq}(Output)],\\{}[\operatorname{MeasurableSpace}(Anchor)], [\operatorname{MeasurableSpace}(Task)], deployment: \operatorname{Measure}\left(Input\right), [\operatorname{IsProbabilityMeasure}(deployment)],\\{}expected: Input \to Output, \operatorname{Measurable}\left(expected\right),\\{}m: \mathbb{N}, epsilon, delta: \mathbb{R},\\{}seedLaw: \operatorname{PMF}\left(Seed\right), verifierCoinLaw: \operatorname{PMF}\left(VerifierCoin\right),\\{}partySuite: Seed \to \operatorname{Fin}\left(m\right) \to Input, certificate: Seed \to Certificate,\\{}verifier: (\operatorname{Fin}\left(m\right) \to (Input) \times (Output)) \times (Certificate) \to VerifierCoin \to Bool, coSelected: Seed \to Input \to Output,\\{}taskLaw: \operatorname{Measure}\left(Task\right), [\operatorname{IsProbabilityMeasure}(taskLaw)],\\{}anchorLaw: \operatorname{Measure}\left(Anchor\right), [\operatorname{IsProbabilityMeasure}(anchorLaw)],\\{}suiteMap: Anchor \to \operatorname{Fin}\left(m\right) \to Input, \operatorname{Measurable}\left(suiteMap\right),\\{}implementation: Task \to Input \to Output, \operatorname{Measurable}\left(\operatorname{uncurry}\left(implementation\right)\right),\\{}\operatorname{let}(L: (Input \to Output) \to \mathbb{R} = (P \mapsto \operatorname{real}\left(deployment, \left\{P(x) \neq expected(x) \mid x \in Input\right\}\right)),\\{}nuParty: \operatorname{Measure}\left((Seed) \times (VerifierCoin)\right) = \operatorname{prod}\left(\operatorname{toMeasure}\left(seedLaw\right), \operatorname{toMeasure}\left(verifierCoinLaw\right)\right),\\{}nuBeacon: \operatorname{Measure}\left((Task) \times (Anchor)\right) = \operatorname{prod}\left(taskLaw, anchorLaw\right),\\{}partyRecord: (Seed \to Input \to Output) \to Seed \to \operatorname{Fin}\left(m\right) \to (Input) \times (Output) = (strategy \mapsto (s \mapsto (i \mapsto (partySuite(s, i), strategy(s, partySuite(s, i)))))),\\{}honestRecord: Seed \to \operatorname{Fin}\left(m\right) \to (Input) \times (Output) = (s \mapsto (i \mapsto (partySuite(s, i), expected(partySuite(s, i))))),\\{}muInduced: \operatorname{Measure}\left(\operatorname{Fin}\left(m\right) \to Input\right) = \operatorname{map}\left(suiteMap, anchorLaw\right),\\{}muIdeal: \operatorname{Measure}\left(\operatorname{Fin}\left(m\right) \to Input\right) = \operatorname{pi}\left((i \mapsto deployment)\right));\\{}((\forall strategy: Seed \to Input \to Output, \operatorname{real}\left(nuParty, \left\{verifier((partyRecord(strategy, omega.1), certificate(omega.1)), omega.2) = true \land epsilon < L(strategy(omega.1)) \mid omega \in (Seed) \times (VerifierCoin)\right\}\right) \leq delta) \Rightarrow\\{}(\forall s: Seed, i: \operatorname{Fin}\left(m\right), coSelected(s, partySuite(s, i)) = expected(partySuite(s, i))) \Rightarrow\\{}(\forall s: Seed, epsilon < L(coSelected(s))) \Rightarrow\\{}\operatorname{real}\left(nuParty, \left\{verifier((honestRecord(omega.1), certificate(omega.1)), omega.2) = true \mid omega \in (Seed) \times (VerifierCoin)\right\}\right) \leq delta \land 1 - delta \leq \operatorname{real}\left(nuParty, \left\{verifier((honestRecord(omega.1), certificate(omega.1)), omega.2) = false \mid omega \in (Seed) \times (VerifierCoin)\right\}\right)) \land \\{}(0 \leq epsilon \Rightarrow\\{}epsilon \leq 1 \Rightarrow\\{}(\forall t: Task, epsilon < L(implementation(t))) \Rightarrow\\{}nuBeacon(\left\{\forall i: \operatorname{Fin}\left(m\right), implementation(z.1, suiteMap(z.2, i)) = expected(suiteMap(z.2, i)) \mid z \in (Task) \times (Anchor)\right\}) \leq \operatorname{ofReal}\left((1 - epsilon)^{m}\right) + \operatorname{measurableTotalVariation}\left(muInduced, muIdeal\right)).\end{gathered}$$

*Proof.* Machine-checked in Lean as `D5/S3/ConceptDynamics/Interpretation/PartySimulationFreshBeaconCertification.party_simulation_and_fresh_beacon_certification` (`✓ std3`). ∎

*Source.* Repository-derived.

*Commentary.*

The first implication quantifies over every seed-indexed implementation. The certificate depends only on the party seed, and the co-selected bad implementation agrees with expected behavior on that same suite.

Reliability therefore applies to a verifier input identical to the honest all-green input. Approval of the fixed nontrivial tier has probability at most delta, and total Boolean output makes the trivial tier its probability complement.

The second implication lets each task select its committed implementation before the anchor. Task and anchor are governed by their product measure, the public suite map pushes the anchor law forward, and the ideal suite is the deployment product measure on the unrestricted input carrier.

For every fixed task, the independent all-pass bound is transported to the induced suite law through measurable-event total variation, then integrated over the task law. Every measurability, probability-law, map, predicate, and threshold premise is displayed explicitly in the proposition.

## References

- Truth anchor: `D5/S3/ConceptDynamics/Interpretation/PartySimulationFreshBeaconCertification.party_simulation_and_fresh_beacon_certification`
- Dependency: [D5/S3/Estimation/DataProcessing/MeasurablePostprocessingDefectContraction](../../Estimation/DataProcessing/MeasurablePostprocessingDefectContraction.md)
