# Waiting Value from Information and Preserved Options

## Abstract

Waiting preserves immediate value, while genuine exposure can reverse it.

**Theorem 1.1 (Waiting value comes from information and preserved options).**

$$\begin{aligned}(\forall X \in \operatorname{Type}, E \in \operatorname{Type}, U \in \operatorname{Type}, M \in \operatorname{Concept}\left(X \to \mathbb{R}, \mathbb{R}\right), q \in \operatorname{Concept}\left(X, E\right), T \in E \to X \to X, V \in \operatorname{Concept}\left(X, U \to \mathbb{R}\right), A0 \in \operatorname{Set}\left(U\right), A1 \in E \to \operatorname{Set}\left(U\right), P \in \operatorname{Set}\left(E \to U\right),\; \left(\left(\forall e \in E, x \in X,\; T(e, x) = x\right) \land \left(\left(\forall u \in U,\; u \in A0 \Rightarrow ((e: E) \mapsto u) \in P\right) \land \left(\forall e \in E,\; A0 \subseteq A1(e)\right)\right)\right) \Rightarrow \left(\forall u \in U,\; u \in A0 \Rightarrow \left(((e: E) \mapsto u) \in \operatorname{admissiblePolicies}\left(P, A1\right) \land \operatorname{informedExpectedValue}\left(M, q, T, V, 0, ((e: E) \mapsto u)\right) = \operatorname{uninformedExpectedValue}\left(M, V, u\right)\right)\right)) \land\\{}(\forall X \in \operatorname{Type}, E \in \operatorname{Type}, U \in \operatorname{Type}, M \in \operatorname{Concept}\left(X \to \mathbb{R}, \mathbb{R}\right), q \in \operatorname{Concept}\left(X, E\right), T \in E \to X \to X, V \in \operatorname{Concept}\left(X, U \to \mathbb{R}\right), c \in \mathbb{R}, A0 \in \operatorname{Set}\left(U\right), A1 \in E \to \operatorname{Set}\left(U\right), P \in \operatorname{Set}\left(E \to U\right), W0 \in \mathbb{R}, W1 \in \mathbb{R}, h0 \in \operatorname{IsGreatest}\left(\operatorname{image}\left(\operatorname{uninformedExpectedValue}\left(M, V\right), A0\right), W0\right), h1 \in \operatorname{IsGreatest}\left(\operatorname{image}\left(\operatorname{informedExpectedValue}\left(M, q, T, V, c\right), \operatorname{admissiblePolicies}\left(P, A1\right)\right), W1\right),\; \left(c = 0 \land \left(\left(\forall e \in E, x \in X,\; T(e, x) = x\right) \land \left(\left(\forall u \in U,\; u \in A0 \Rightarrow ((e: E) \mapsto u) \in P\right) \land \left(\forall e \in E,\; A0 \subseteq A1(e)\right)\right)\right)\right) \Rightarrow W0 \leq W1) \land\\{}(\exists M2 \in \operatorname{Concept}\left(Unit \to \mathbb{R}, \mathbb{R}\right), q2 \in \operatorname{Concept}\left(Unit, Unit\right), T2 \in Unit \to Unit \to Unit, V2 \in \operatorname{Concept}\left(Unit, Bool \to \mathbb{R}\right), A20 \in \operatorname{Set}\left(Bool\right), A21 \in Unit \to \operatorname{Set}\left(Bool\right), P2 \in \operatorname{Set}\left(Unit \to Bool\right), W20 \in \mathbb{R}, W21 \in \mathbb{R},\; \left(\forall e2 \in Unit, x2 \in Unit,\; T2(e2, x2) = x2\right) \land \left(\left(\forall u2 \in Bool,\; u2 \in A20 \Rightarrow ((e2: Unit) \mapsto u2) \in P2\right) \land \left(\left(\exists e2 \in Unit, u2 \in Bool,\; u2 \in A20 \land \neg(u2 \in A21(e2))\right) \land \left(\operatorname{IsGreatest}\left(\operatorname{image}\left(\operatorname{uninformedExpectedValue}\left(M2, V2\right), A20\right), W20\right) \land \left(\operatorname{IsGreatest}\left(\operatorname{image}\left(\operatorname{informedExpectedValue}\left(M2, q2, T2, V2, 0\right), \operatorname{admissiblePolicies}\left(P2, A21\right)\right), W21\right) \land W21 < W20\right)\right)\right)\right)) \land\\{}(\exists M3 \in \operatorname{Concept}\left(Unit \to \mathbb{R}, \mathbb{R}\right), q3 \in \operatorname{Concept}\left(Unit, Unit\right), T3 \in Unit \to Unit \to Unit, V3 \in \operatorname{Concept}\left(Unit, Unit \to \mathbb{R}\right), A30 \in \operatorname{Set}\left(Unit\right), A31 \in Unit \to \operatorname{Set}\left(Unit\right), P3 \in \operatorname{Set}\left(Unit \to Unit\right), c3 \in \mathbb{R}, W30 \in \mathbb{R}, W31 \in \mathbb{R},\; 0 < c3 \land \left(\left(\forall e3 \in Unit, x3 \in Unit,\; T3(e3, x3) = x3\right) \land \left(\left(\forall u3 \in Unit,\; u3 \in A30 \Rightarrow ((e3: Unit) \mapsto u3) \in P3\right) \land \left(\left(\forall e3 \in Unit,\; A31(e3) = A30\right) \land \left(\operatorname{IsGreatest}\left(\operatorname{image}\left(\operatorname{uninformedExpectedValue}\left(M3, V3\right), A30\right), W30\right) \land \left(\operatorname{IsGreatest}\left(\operatorname{image}\left(\operatorname{informedExpectedValue}\left(M3, q3, T3, V3, c3\right), \operatorname{admissiblePolicies}\left(P3, A31\right)\right), W31\right) \land W31 < W30\right)\right)\right)\right)\right)) \land\\{}(\exists M4 \in \operatorname{Concept}\left(Bool \to \mathbb{R}, \mathbb{R}\right), q4 \in \operatorname{Concept}\left(Bool, Unit\right), T4 \in Unit \to Bool \to Bool, V4 \in \operatorname{Concept}\left(Bool, Unit \to \mathbb{R}\right), A40 \in \operatorname{Set}\left(Unit\right), A41 \in Unit \to \operatorname{Set}\left(Unit\right), P4 \in \operatorname{Set}\left(Unit \to Unit\right), W40 \in \mathbb{R}, W41 \in \mathbb{R},\; \left(\exists e4 \in Unit, x4 \in Bool,\; T4(e4, x4) \neq x4\right) \land \left(\left(\forall u4 \in Unit,\; u4 \in A40 \Rightarrow ((e4: Unit) \mapsto u4) \in P4\right) \land \left(\left(\forall e4 \in Unit,\; A40 \subseteq A41(e4)\right) \land \left(\operatorname{IsGreatest}\left(\operatorname{image}\left(\operatorname{uninformedExpectedValue}\left(M4, V4\right), A40\right), W40\right) \land \left(\operatorname{IsGreatest}\left(\operatorname{image}\left(\operatorname{informedExpectedValue}\left(M4, q4, T4, V4, 0\right), \operatorname{admissiblePolicies}\left(P4, A41\right)\right), W41\right) \land W41 < W40\right)\right)\right)\right)) \land\\{}(\exists M5 \in \operatorname{Concept}\left(Bool \to \mathbb{R}, \mathbb{R}\right), q5 \in \operatorname{Concept}\left(Bool, Bool\right), T5 \in Bool \to Bool \to Bool, V5 \in \operatorname{Concept}\left(Bool, Unit \to \mathbb{R}\right), d5 \in \operatorname{Concept}\left(Bool, Bool\right), c5 \in Bool \to \mathbb{R}, A50 \in \operatorname{Set}\left(Unit\right), A51 \in Bool \to \operatorname{Set}\left(Unit\right), P5 \in \operatorname{Set}\left(Bool \to Unit\right), W50 \in \mathbb{R}, W51 \in \mathbb{R}, W52 \in \mathbb{R},\; \left(\forall e5 \in Bool, x5 \in Bool,\; T5(e5, x5) = x5\right) \land \left(\left(\forall u5 \in Unit,\; u5 \in A50 \Rightarrow ((e5: Bool) \mapsto u5) \in P5\right) \land \left(\left(\forall e5 \in Bool,\; A50 \subseteq A51(e5)\right) \land \left(\left(\exists a \in Bool, b \in Bool,\; q5(a) \neq q5(b) \land \left(d5(q5(a)) \neq d5(q5(b)) \land \left(c5(d5(q5(a))) = 0 \land 0 < c5(d5(q5(b)))\right)\right)\right) \land \left(\operatorname{IsGreatest}\left(\operatorname{image}\left(\operatorname{uninformedExpectedValue}\left(M5, V5\right), A50\right), W50\right) \land \left(\operatorname{IsGreatest}\left(\operatorname{image}\left(\operatorname{informedExpectedValue}\left(M5, q5, T5, V5, 0\right), \operatorname{admissiblePolicies}\left(P5, A51\right)\right), W51\right) \land \left(W50 \leq W51 \land \left(\operatorname{IsGreatest}\left(\operatorname{image}\left((p5: Bool \to Unit) \mapsto M5((x5: Bool) \mapsto V5(T5(q5(x5), x5), p5(q5(x5))) - c5(d5(q5(x5)))), \operatorname{admissiblePolicies}\left(P5, A51\right)\right), W52\right) \land W52 < W50\right)\right)\right)\right)\right)\right)\right)) \land\\{}(\exists M6 \in \operatorname{Concept}\left(Bool \to \mathbb{R}, \mathbb{R}\right), q6 \in \operatorname{Concept}\left(Bool, Unit\right), T6 \in Unit \to Bool \to Bool, R6 \in Unit \to Bool \to Bool, V6 \in \operatorname{Concept}\left(Bool, Unit \to \mathbb{R}\right), A60 \in \operatorname{Set}\left(Unit\right), A61 \in Unit \to \operatorname{Set}\left(Unit\right), P6 \in \operatorname{Set}\left(Unit \to Unit\right), W60 \in \mathbb{R}, W61 \in \mathbb{R},\; \left(\forall e6 \in Unit, x6 \in Bool,\; T6(e6, x6) = x6\right) \land \left(\left(\exists e6 \in Unit, x6 \in Bool,\; R6(e6, T6(e6, x6)) \neq T6(e6, x6)\right) \land \left(\left(\forall u6 \in Unit,\; u6 \in A60 \Rightarrow ((e6: Unit) \mapsto u6) \in P6\right) \land \left(\left(\forall e6 \in Unit,\; A60 \subseteq A61(e6)\right) \land \left(\operatorname{IsGreatest}\left(\operatorname{image}\left(\operatorname{uninformedExpectedValue}\left(M6, V6\right), A60\right), W60\right) \land \left(\operatorname{IsGreatest}\left(\operatorname{image}\left(\operatorname{informedExpectedValue}\left(M6, q6, (e6: Unit, x6: Bool) \mapsto R6(e6, T6(e6, x6)), V6, 0\right), \operatorname{admissiblePolicies}\left(P6, A61\right)\right), W61\right) \land W61 < W60\right)\right)\right)\right)\right)).\end{aligned}$$

*Proof.* Machine-checked in Lean as `D5/S3/ConceptDynamics/Reversibility/WaitingValueReversibility.waiting_value_from_information_and_option_preservation` (`✓ std3`). ∎

*Source.* Repository-derived.

*Commentary.*

When every immediate action remains available through an admissible constant policy, waiting attains exactly the immediate action's value. Free, world-preserving observation then lets the frozen decision-value theorem compare the two optima.

The public statement also carries five unconditional same-model breakdown witnesses. The first three reuse the frozen opportunity-loss, positive-cost, and world-change countermodels.

The disclosure witness uses two distinguishable states, observations, and public signals, with zero penalty on one signal and positive penalty on the other. Its penalty-free informed optimum is at least the uninformed optimum, while exposure strictly reverses the comparison. The response witness separately models a triggered third-party state transition.

Repository, pinned-Mathlib, and installed-package searches found no exact whole theorem. The extension imports every existing family owner and adds only the two absent carriers.

## References

- Truth anchor: `D5/S3/ConceptDynamics/Reversibility/WaitingValueReversibility.waiting_value_from_information_and_option_preservation`
- Dependency: [D5/S3/ConceptDynamics/Reversibility/OpportunityLossValueReversal](OpportunityLossValueReversal.md)
- Dependency: [D5/S3/ConceptDynamics/Reversibility/WaitingCostValueReversal](WaitingCostValueReversal.md)
- Dependency: [D5/S3/ConceptDynamics/Reversibility/WorldChangeValueReversal](WorldChangeValueReversal.md)
