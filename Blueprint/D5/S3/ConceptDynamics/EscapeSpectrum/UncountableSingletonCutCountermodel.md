# Uncountable Singleton Cut Countermodel

## Abstract

Uncountably many singleton cuts cover every defect edge, while each finite selection leaves Lebesgue charge one and escape spectrum one.

Let J be the actual closed unit interval in the real numbers, with its Lebesgue probability measure mu, and let X be J times Bool. The base readout reads the interval coordinate and the target reads the Boolean coordinate. Every t in J supplies the definition d_t(s,i), equal to i when s=t and false otherwise. All definitions have cost one.

$\begin{aligned}\operatorname{unitInterval}\left(\right) = \operatorname{Icc}\left(0, 1\right)\\X = \operatorname{unitInterval}\left(\right) \times \operatorname{Bool}\left(\right)\\\Gamma = \operatorname{univ}\left(\operatorname{unitInterval}\left(\right)\right)\\\mu = \operatorname{volume}\left(\operatorname{unitInterval}\left(\right)\right)\\q = \operatorname{fst}\left(\right)\\T = \operatorname{snd}\left(\right)\\\forall t: \operatorname{unitInterval}\left(\right), \forall s: \operatorname{unitInterval}\left(\right), \forall i: \operatorname{Bool}\left(\right), d(t)((s,i)) = \operatorname{ite}\left(s = t, i, \operatorname{false}\left(\right)\right)\\\forall t: \operatorname{unitInterval}\left(\right), c(t) = 1\\\forall b: \operatorname{Bool}\left(\right), \forall t: \operatorname{unitInterval}\left(\right), e(b)(t) = ((t,b),(t,\operatorname{BoolNot}\left(b\right)))\\E = \operatorname{defectRelation}\left(q, T\right)\\\forall i: \operatorname{Subtype}\left(\Gamma\right), U(i) = \operatorname{inter}\left(E, \operatorname{compl}\left(\operatorname{conceptKernel}\left(i: \operatorname{Subtype}\left(\Gamma\right) \mapsto d(\operatorname{val}\left(i\right)), i\right)\right)\right)\\B = \operatorname{inter}\left(E, \operatorname{jointKernel}\left(i: \operatorname{Subtype}\left(\Gamma\right) \mapsto d(\operatorname{val}\left(i\right))\right)\right)\\\forall S: \operatorname{Finset}\left(\operatorname{Subtype}\left(\Gamma\right)\right), R(S) = \operatorname{defectRelation}\left(\operatorname{conceptJoin}\left(q, \operatorname{finiteSelectionSupplement}\left(\Gamma, d, S\right)\right), T\right)\\\forall S: \operatorname{Finset}\left(\operatorname{Subtype}\left(\Gamma\right)\right), P(S) = \operatorname{image}\left(\operatorname{val}\left(\right), \operatorname{coeSet}\left(S\right)\right)\\A = \operatorname{setOf}\left(C: \operatorname{Set}\left(X \times X\right), (C \subseteq E \land \forall b: \operatorname{Bool}\left(\right), \operatorname{NullMeasurableSet}\left(\operatorname{preimage}\left(e(b), C\right), \mu\right))\right)\\\nu: \operatorname{AddContent}\left(\operatorname{NNReal}\left(\right), A\right)\\\forall C: \operatorname{Set}\left(X \times X\right), \nu(C) = \frac{1}{2} \operatorname{toNNReal}\left(\mu(\operatorname{preimage}\left(e(\operatorname{false}\left(\right)), C\right))\right) + \frac{1}{2} \operatorname{toNNReal}\left(\mu(\operatorname{preimage}\left(e(\operatorname{true}\left(\right)), C\right))\right)\\w: \operatorname{EscapeWeight}\left(X \times X\right)\\\forall C: \operatorname{Set}\left(X \times X\right), \operatorname{mass}\left(w, C\right) = \operatorname{coeReal}\left(\nu(C)\right)\end{aligned}$

The supported algebra contains exactly the subsets of E whose two interval sections are NullMeasurableSet for mu. This predicate is measurability for completed Lebesgue measure. Under the interval inclusion, it is equivalent to Lebesgue measurability of the real image, with the same measure on every interval set. The half-sum formula defines nu on every ambient set of pairs. Interval probability makes both section measures finite, so their toNNReal conversions preserve their values. The real weight w is the coercion of this same AddContent.

**Theorem 1.1 (Every finite residual has mass one).**

$$\forall S: \operatorname{Finset}\left(\operatorname{Subtype}\left(\Gamma\right)\right), \operatorname{finiteResidualMass}\left(\Gamma, d, q, T, w, S\right) = 1$$

*Proof.* Machine-checked in Lean as `D5/S3/ConceptDynamics/EscapeSpectrum/UncountableSingletonCutCountermodel.finite_residual_mass_eq_one` (`✓ std3`). ∎

*Source.* Repository-derived.

*Commentary.*

For every finite selection S from the full language, the actual Option-valued finiteSelectionSupplement leaves each directed edge precisely when its basepoint is absent from P(S). The finite image P(S) has Lebesgue measure zero. Both residual sections therefore have measure one, and their half-sum is one.

**Theorem 1.2 (The full uncountable cover and all finite budget spectra).**

$$\begin{aligned}(\neg \operatorname{Countable}\left(\Gamma\right)) \land\\(\operatorname{Nonempty}\left(\Gamma\right)) \land\\(\operatorname{Injective}\left(i: \operatorname{Subtype}\left(\Gamma\right) \mapsto d(\operatorname{val}\left(i\right))\right)) \land\\(\neg \operatorname{Countable}\left(\operatorname{range}\left(i: \operatorname{Subtype}\left(\Gamma\right) \mapsto d(\operatorname{val}\left(i\right))\right)\right)) \land\\(E = \operatorname{range}\left(e(\operatorname{false}\left(\right))\right) \cup \operatorname{range}\left(e(\operatorname{true}\left(\right))\right)) \land\\(\forall a: \operatorname{Set}\left(\operatorname{unitInterval}\left(\right)\right), (\operatorname{NullMeasurableSet}\left(a, \mu\right) \Leftrightarrow \operatorname{NullMeasurableSet}\left(\operatorname{image}\left(\operatorname{SubtypeVal}\left(\operatorname{unitInterval}\left(\right)\right), a\right), \operatorname{volume}\left(\mathbb{R}\right)\right) \land \mu(a) = \operatorname{volume}\left(\mathbb{R}\right)(\operatorname{image}\left(\operatorname{SubtypeVal}\left(\operatorname{unitInterval}\left(\right)\right), a\right)))) \land\\(\forall C: \operatorname{Set}\left(X \times X\right), C \in A \Leftrightarrow (C \subseteq E \land \forall b: \operatorname{Bool}\left(\right), \operatorname{NullMeasurableSet}\left(\operatorname{image}\left(\operatorname{SubtypeVal}\left(\operatorname{unitInterval}\left(\right)\right), \operatorname{preimage}\left(e(b), C\right)\right), \operatorname{volume}\left(\mathbb{R}\right)\right))) \land\\(\forall D: \operatorname{Set}\left(\operatorname{Subtype}\left(E\right)\right), \operatorname{preimage}\left(\operatorname{SubtypeVal}\left(\operatorname{Subtype}\left(E\right)\right), \operatorname{image}\left(\operatorname{SubtypeVal}\left(\operatorname{Subtype}\left(E\right)\right), D\right)\right) = D) \land\\(\forall C: \operatorname{Set}\left(X \times X\right), (C \subseteq E) \Rightarrow \operatorname{image}\left(\operatorname{SubtypeVal}\left(\operatorname{Subtype}\left(E\right)\right), \operatorname{preimage}\left(\operatorname{SubtypeVal}\left(\operatorname{Subtype}\left(E\right)\right), C\right)\right) = C) \land\\(\operatorname{IsSetRing}\left(A\right)) \land\\(\emptyset \in A) \land\\(E \in A) \land\\(\forall C: \operatorname{Set}\left(X \times X\right), (C \in A) \Rightarrow E \setminus C \in A) \land\\(\forall C: \operatorname{Set}\left(X \times X\right), \nu(C) = \frac{1}{2} \operatorname{toNNReal}\left(\mu(\operatorname{preimage}\left(e(\operatorname{false}\left(\right)), C\right))\right) + \frac{1}{2} \operatorname{toNNReal}\left(\mu(\operatorname{preimage}\left(e(\operatorname{true}\left(\right)), C\right))\right)) \land\\(\forall C: \operatorname{Set}\left(X \times X\right), \operatorname{mass}\left(w, C\right) = \operatorname{coeReal}\left(\nu(C)\right)) \land\\(\nu(\emptyset) = 0) \land\\(\nu(E) = 1) \land\\(\forall C: \operatorname{Set}\left(X \times X\right), 0 \leq \operatorname{mass}\left(w, C\right)) \land\\(\operatorname{Monotone}\left(\nu\right)) \land\\(\operatorname{Monotone}\left(\operatorname{mass}\left(w\right)\right)) \land\\(\forall C: \operatorname{Set}\left(X \times X\right), \forall D: \operatorname{Set}\left(X \times X\right), ((C \in A \land (D \in A \land \operatorname{Disjoint}\left(C, D\right)))) \Rightarrow \nu(C \cup D) = \nu(C) + \nu(D)) \land\\(\forall C: \operatorname{Set}\left(X \times X\right), \nu(C) = \nu(\operatorname{inter}\left(C, E\right))) \land\\(0 < \operatorname{mass}\left(w, E\right)) \land\\(\operatorname{coeENNReal}\left(\nu(E)\right) < \infty) \land\\(\forall i: \operatorname{Subtype}\left(\Gamma\right), (U(i) = \{e(\operatorname{false}\left(\right))(\operatorname{val}\left(i\right)),e(\operatorname{true}\left(\right))(\operatorname{val}\left(i\right))\} \land U(i) \in A)) \land\\(\operatorname{iUnion}\left(i: \operatorname{Subtype}\left(\Gamma\right), U(i)\right) = E) \land\\(B = E \setminus \operatorname{iUnion}\left(i: \operatorname{Subtype}\left(\Gamma\right), U(i)\right)) \land\\(B = \emptyset) \land\\(B \in A) \land\\(\forall S: \operatorname{Finset}\left(\operatorname{Subtype}\left(\Gamma\right)\right), \operatorname{Finite}\left(P(S)\right)) \land\\(\forall S: \operatorname{Finset}\left(\operatorname{Subtype}\left(\Gamma\right)\right), \mu(P(S)) = 0) \land\\(\forall S: \operatorname{Finset}\left(\operatorname{Subtype}\left(\Gamma\right)\right), R(S) = E \setminus \operatorname{iUnion}\left(i \in S, U(i)\right)) \land\\(\forall S: \operatorname{Finset}\left(\operatorname{Subtype}\left(\Gamma\right)\right), \forall b: \operatorname{Bool}\left(\right), \operatorname{preimage}\left(e(b), R(S)\right) = \operatorname{compl}\left(P(S)\right)) \land\\(\forall S: \operatorname{Finset}\left(\operatorname{Subtype}\left(\Gamma\right)\right), \operatorname{iUnion}\left(i \in S, U(i)\right) \in A) \land\\(\forall S: \operatorname{Finset}\left(\operatorname{Subtype}\left(\Gamma\right)\right), R(S) \in A) \land\\(\forall S: \operatorname{Finset}\left(\operatorname{Subtype}\left(\Gamma\right)\right), R(S) \setminus B \in A) \land\\(\forall S: \operatorname{Finset}\left(\operatorname{Subtype}\left(\Gamma\right)\right), \nu(\operatorname{iUnion}\left(i \in S, U(i)\right)) = 0) \land\\(\forall S: \operatorname{Finset}\left(\operatorname{Subtype}\left(\Gamma\right)\right), B \subseteq R(S)) \land\\(\forall S: \operatorname{Finset}\left(\operatorname{Subtype}\left(\Gamma\right)\right), \nu(R(S)) = 1) \land\\(\forall S: \operatorname{Finset}\left(\operatorname{Subtype}\left(\Gamma\right)\right), \nu(R(S)) = \nu(B) + \nu(R(S) \setminus B)) \land\\(\forall S: \operatorname{Finset}\left(\operatorname{Subtype}\left(\Gamma\right)\right), \operatorname{finiteResidualMass}\left(\Gamma, d, q, T, w, S\right) = 1) \land\\(\forall t: \operatorname{unitInterval}\left(\right), (t \in \Gamma) \Rightarrow 0 < c(t)) \land\\(\forall S: \operatorname{Finset}\left(\operatorname{Subtype}\left(\Gamma\right)\right), \operatorname{finiteSelectionCost}\left(\Gamma, c, S\right) = \operatorname{coeReal}\left(\operatorname{card}\left(S\right)\right)) \land\\(\forall L: \operatorname{NNReal}\left(\right), \operatorname{finiteSelectionCost}\left(\Gamma, c, \emptyset\right) \leq \operatorname{coeReal}\left(L\right)) \land\\(\forall L: \operatorname{NNReal}\left(\right), \operatorname{finiteBudgetMassValues}\left(\Gamma, d, q, T, c, w, L\right) = \{1\}) \land\\(\forall L: \operatorname{NNReal}\left(\right), \operatorname{finiteEscapeSpectrum}\left(\Gamma, d, q, T, c, w, L\right) = 1) \land\\(\operatorname{allFiniteResidualInfimum}\left(\Gamma, d, q, T, w\right) = 1) \land\\(\operatorname{Tendsto}\left(\operatorname{finiteEscapeSpectrum}\left(\Gamma, d, q, T, c, w\right), \operatorname{atTop}\left(\right), \operatorname{nhds}\left(1\right)\right)) \land\\(\frac{\operatorname{mass}\left(w, B\right)}{\operatorname{mass}\left(w, E\right)} = 0) \land\\1 \ne \frac{\operatorname{mass}\left(w, B\right)}{\operatorname{mass}\left(w, E\right)}\end{aligned}$$

*Proof.* Machine-checked in Lean as `D5/S3/ConceptDynamics/EscapeSpectrum/UncountableSingletonCutCountermodel.uncountable_singleton_cut_countermodel` (`✓ std3`). ∎

*Source.* Repository-derived.

*Commentary.*

The interval language is nonempty and uncountable. Evaluation at (t,true) distinguishes its definitions, so their range is uncountable as well. Each singleton cut contains exactly the two directed edges over its basepoint. Every edge is cut by its own definition; this proves the full cover pointwise and makes the common blind residual empty. The algebra is a ring with complements relative to E, and the subtype carrier correspondence is exact in both directions. The charge is supported on E, has total mass one, is globally monotone and nonnegative, and is additive for disjoint algebra members. Finite cut unions have mass zero; each residual and its difference from B belong to the algebra and satisfy the common blind decomposition. A finite selection costs its cardinality. The empty selection is affordable for every NNReal budget, including zero. The finite residual equality gives the affordable mass set {1}, hence canonical spectrum one at every budget. The positive baseline and global monotonicity identify its limit with the all-finite infimum one. The blind ratio is zero and differs strictly from that limit. The full uncountable cover is a pointwise identity; each budget continues to permit only finite selections.

## References

- Truth anchor: `D5/S3/ConceptDynamics/EscapeSpectrum/UncountableSingletonCutCountermodel.finite_residual_mass_eq_one`
- Truth anchor: `D5/S3/ConceptDynamics/EscapeSpectrum/UncountableSingletonCutCountermodel.uncountable_singleton_cut_countermodel`
- Dependency: [D5/S3/ConceptDynamics/EscapeSpectrum/BudgetEnvelopeCompletion](BudgetEnvelopeCompletion.md)
