using static StrataLint.Scribe.DefinitionDsl;
using static StrataLint.Scribe.FormulaDsl;
using F = StrataLint.Scribe.FormulaDsl;

namespace StrataLint.Scribe.Blueprint.D5.S3.Constants.Cyclotomy;

internal sealed class RealSubfieldDocument : IScribeDocumentDefinition
{
    public DocumentDefinition Create() => DocumentDefinition.Create(ScribeNode.Create(
        "The real fifth-cyclotomic generator and root five generate the same field.",
        H("Real Fifth-Cyclotomic Subfield"),
        Blocks(
            Describe.Lean(
                DescribeId.Create("real-cyclotomic-generator-adjoin-equality"),
                DeclarationHandle.Create(
                    "D5/S3/Constants/Cyclotomy/RealSubfield."
                    + "real_cyclotomic_generator_adjoin_eq_sqrt_five"),
                H("The real fifth-cyclotomic field is generated by root five"),
                StatementSource.FromAuthor(Disp(Seq(
                    Mathbb, Grp(F.Id("Q")), Langle,
                    D(2), Sp, Operatorname, Grp(F.Id("cos")), Open,
                    D(2), Pi, Slash, D(5), Close, Rangle, Sp, Eq, Sp,
                    Mathbb, Grp(F.Id("Q")), Langle, Sqrt, Grp(D(5)), Rangle, Dot))),
                AssessedProvenance.FromRepo(),
                Blocks(
                    Paragraph(Text(
                        "The existing pentagon identity identifies the real generator "
                        + "2 cos(2 pi / 5) with (sqrt(5) - 1) / 2. Field closure therefore "
                        + "places this generator in Q(sqrt(5)); conversely, rearranging the "
                        + "identity as sqrt(5) = 2 times the generator plus 1 places sqrt(5) "
                        + "in the field generated by the cosine. The two inclusions give equality.")),
                    Paragraph(Text(
                        "This closes only the sentence Q(zeta_5)^+ = Q(sqrt(5)) from clause 2, "
                        + "using its standard real-generator presentation by 2 cos(2 pi / 5). "
                        + "It does not claim closure of the clause's other three welding assertions."))),
                DescribeRole.Theorem))));
}
