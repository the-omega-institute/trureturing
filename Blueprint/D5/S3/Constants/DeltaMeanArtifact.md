# Delta Mean Artifact

## Abstract

The corrected exchange-loss mean has zero absolute value.

**Theorem 1.1 (The absolute corrected exchange-loss mean is zero).**

$$\left|deltaMean\right|=0.$$

*Proof.* Machine-checked in Lean as `D5/S3/Constants/DeltaMeanArtifact.abs_delta_mean_zero` (`✓ std3`). ∎

*Source.* Repository-derived.

*Commentary.*

The artifact defines the corrected exchange-loss mean as zero. Its absolute value therefore vanishes by direct simplification.

## References

- Truth anchor: `D5/S3/Constants/DeltaMeanArtifact.abs_delta_mean_zero`
