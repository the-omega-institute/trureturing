# Finite Weyl Budget Bounds

## Abstract

Finite parity pencils converge monotonically to the full generalized Rayleigh budget interval.

**Theorem 1.1 (Finite Hermitian pencils bound and approximate the full budget interval).**

$$\forall EvenTest \in Type, OddTest \in Type, evenDim \in \operatorname{Nat}\left(\right) \to \operatorname{Nat}\left(\right), oddDim \in \operatorname{Nat}\left(\right) \to \operatorname{Nat}\left(\right), evenBase \in \operatorname{DependentMap}\left(N: \operatorname{Nat}\left(\right), \operatorname{Matrix}\left(\operatorname{Fin}\left(\operatorname{apply}\left(evenDim, N\right)\right), \operatorname{Fin}\left(\operatorname{apply}\left(evenDim, N\right)\right), \operatorname{Complex}\left(\right)\right)\right), oddBase \in \operatorname{DependentMap}\left(N: \operatorname{Nat}\left(\right), \operatorname{Matrix}\left(\operatorname{Fin}\left(\operatorname{apply}\left(oddDim, N\right)\right), \operatorname{Fin}\left(\operatorname{apply}\left(oddDim, N\right)\right), \operatorname{Complex}\left(\right)\right)\right), evenBoundary \in \operatorname{DependentMap}\left(N: \operatorname{Nat}\left(\right), \operatorname{Fin}\left(\operatorname{apply}\left(evenDim, N\right)\right) \to \operatorname{Complex}\left(\right)\right), oddBoundary \in \operatorname{DependentMap}\left(N: \operatorname{Nat}\left(\right), \operatorname{Fin}\left(\operatorname{apply}\left(oddDim, N\right)\right) \to \operatorname{Complex}\left(\right)\right), fullEvenBase \in EvenTest \to \operatorname{Real}\left(\right), fullEvenBoundary \in EvenTest \to \operatorname{Complex}\left(\right), fullOddBase \in OddTest \to \operatorname{Real}\left(\right), fullOddBoundary \in OddTest \to \operatorname{Complex}\left(\right), referenceBudget \in \operatorname{Real}\left(\right),\; \operatorname{let} finiteEvenQuotients = N: \operatorname{Nat}\left(\right) \mapsto \left\{\exists x \in \operatorname{Fin}\left(\operatorname{apply}\left(evenDim, N\right)\right) \to \operatorname{Complex}\left(\right),\; \operatorname{dot}\left(\operatorname{star}\left(\operatorname{apply}\left(evenBoundary, N\right)\right), x\right) \ne 0 \land q = \operatorname{div}\left(\operatorname{neg}\left(\operatorname{Re}\left(\operatorname{dot}\left(\operatorname{star}\left(x\right), \operatorname{mulVec}\left(\operatorname{apply}\left(evenBase, N\right), x\right)\right)\right)\right), \operatorname{normSq}\left(\operatorname{dot}\left(\operatorname{star}\left(\operatorname{apply}\left(evenBoundary, N\right)\right), x\right)\right)\right) \mid q \in \operatorname{Real}\left(\right)\right\}, \operatorname{let} finiteOddQuotients = N: \operatorname{Nat}\left(\right) \mapsto \left\{\exists x \in \operatorname{Fin}\left(\operatorname{apply}\left(oddDim, N\right)\right) \to \operatorname{Complex}\left(\right),\; \operatorname{dot}\left(\operatorname{star}\left(\operatorname{apply}\left(oddBoundary, N\right)\right), x\right) \ne 0 \land q = \operatorname{div}\left(\operatorname{Re}\left(\operatorname{dot}\left(\operatorname{star}\left(x\right), \operatorname{mulVec}\left(\operatorname{apply}\left(oddBase, N\right), x\right)\right)\right), \operatorname{normSq}\left(\operatorname{dot}\left(\operatorname{star}\left(\operatorname{apply}\left(oddBoundary, N\right)\right), x\right)\right)\right) \mid q \in \operatorname{Real}\left(\right)\right\}, \operatorname{let} fullEvenQuotients = \left\{\exists x \in EvenTest,\; \operatorname{apply}\left(fullEvenBoundary, x\right) \ne 0 \land q = \operatorname{div}\left(\operatorname{neg}\left(\operatorname{apply}\left(fullEvenBase, x\right)\right), \operatorname{normSq}\left(\operatorname{apply}\left(fullEvenBoundary, x\right)\right)\right) \mid q \in \operatorname{Real}\left(\right)\right\}, \operatorname{let} fullOddQuotients = \left\{\exists x \in OddTest,\; \operatorname{apply}\left(fullOddBoundary, x\right) \ne 0 \land q = \operatorname{div}\left(\operatorname{apply}\left(fullOddBase, x\right), \operatorname{normSq}\left(\operatorname{apply}\left(fullOddBoundary, x\right)\right)\right) \mid q \in \operatorname{Real}\left(\right)\right\}, \operatorname{let} finiteLower = N: \operatorname{Nat}\left(\right) \mapsto \operatorname{add}\left(referenceBudget, \operatorname{sSup}\left(\operatorname{apply}\left(finiteEvenQuotients, N\right)\right)\right), \operatorname{let} finiteUpper = N: \operatorname{Nat}\left(\right) \mapsto \operatorname{add}\left(referenceBudget, \operatorname{sInf}\left(\operatorname{apply}\left(finiteOddQuotients, N\right)\right)\right), \operatorname{let} fullLower = \operatorname{add}\left(referenceBudget, \operatorname{sSup}\left(fullEvenQuotients\right)\right), \operatorname{let} fullUpper = \operatorname{add}\left(referenceBudget, \operatorname{sInf}\left(fullOddQuotients\right)\right), \operatorname{let} evenPencil = N: \operatorname{Nat}\left(\right), R: \operatorname{Real}\left(\right) \mapsto \operatorname{add}\left(\operatorname{apply}\left(evenBase, N\right), \operatorname{smul}\left(\operatorname{ofReal}\left(\operatorname{sub}\left(R, referenceBudget\right)\right), \operatorname{vecMulVec}\left(\operatorname{apply}\left(evenBoundary, N\right), \operatorname{star}\left(\operatorname{apply}\left(evenBoundary, N\right)\right)\right)\right)\right), \operatorname{let} oddPencil = N: \operatorname{Nat}\left(\right), R: \operatorname{Real}\left(\right) \mapsto \operatorname{sub}\left(\operatorname{apply}\left(oddBase, N\right), \operatorname{smul}\left(\operatorname{ofReal}\left(\operatorname{sub}\left(R, referenceBudget\right)\right), \operatorname{vecMulVec}\left(\operatorname{apply}\left(oddBoundary, N\right), \operatorname{star}\left(\operatorname{apply}\left(oddBoundary, N\right)\right)\right)\right)\right), \operatorname{let} feasible = N: \operatorname{Nat}\left(\right), R: \operatorname{Real}\left(\right) \mapsto \operatorname{PosSemidef}\left(\operatorname{applyTwo}\left(evenPencil, N, R\right)\right) \land \operatorname{PosSemidef}\left(\operatorname{applyTwo}\left(oddPencil, N, R\right)\right), \left(\left(\forall N \in \operatorname{Nat}\left(\right),\; \operatorname{IsHermitian}\left(\operatorname{apply}\left(evenBase, N\right)\right)\right) \land \left(\left(\forall N \in \operatorname{Nat}\left(\right),\; \operatorname{IsHermitian}\left(\operatorname{apply}\left(oddBase, N\right)\right)\right) \land \left(\left(\forall N \in \operatorname{Nat}\left(\right), x \in \operatorname{Fin}\left(\operatorname{apply}\left(evenDim, N\right)\right) \to \operatorname{Complex}\left(\right),\; \operatorname{dot}\left(\operatorname{star}\left(\operatorname{apply}\left(evenBoundary, N\right)\right), x\right) = 0 \Rightarrow 0 \le \operatorname{Re}\left(\operatorname{dot}\left(\operatorname{star}\left(x\right), \operatorname{mulVec}\left(\operatorname{apply}\left(evenBase, N\right), x\right)\right)\right)\right) \land \left(\left(\forall N \in \operatorname{Nat}\left(\right), x \in \operatorname{Fin}\left(\operatorname{apply}\left(oddDim, N\right)\right) \to \operatorname{Complex}\left(\right),\; \operatorname{dot}\left(\operatorname{star}\left(\operatorname{apply}\left(oddBoundary, N\right)\right), x\right) = 0 \Rightarrow 0 \le \operatorname{Re}\left(\operatorname{dot}\left(\operatorname{star}\left(x\right), \operatorname{mulVec}\left(\operatorname{apply}\left(oddBase, N\right), x\right)\right)\right)\right) \land \left(\left(\forall N \in \operatorname{Nat}\left(\right),\; \exists x \in \operatorname{Fin}\left(\operatorname{apply}\left(evenDim, N\right)\right) \to \operatorname{Complex}\left(\right),\; \operatorname{dot}\left(\operatorname{star}\left(\operatorname{apply}\left(evenBoundary, N\right)\right), x\right) \ne 0\right) \land \left(\left(\forall N \in \operatorname{Nat}\left(\right),\; \exists x \in \operatorname{Fin}\left(\operatorname{apply}\left(oddDim, N\right)\right) \to \operatorname{Complex}\left(\right),\; \operatorname{dot}\left(\operatorname{star}\left(\operatorname{apply}\left(oddBoundary, N\right)\right), x\right) \ne 0\right) \land \left(\left(\forall N \in \operatorname{Nat}\left(\right),\; \operatorname{BddAbove}\left(\operatorname{apply}\left(finiteEvenQuotients, N\right)\right)\right) \land \left(\left(\forall N \in \operatorname{Nat}\left(\right),\; \operatorname{BddBelow}\left(\operatorname{apply}\left(finiteOddQuotients, N\right)\right)\right) \land \left(\left(\exists x \in EvenTest,\; \operatorname{apply}\left(fullEvenBoundary, x\right) \ne 0\right) \land \left(\left(\exists x \in OddTest,\; \operatorname{apply}\left(fullOddBoundary, x\right) \ne 0\right) \land \left(\operatorname{BddAbove}\left(fullEvenQuotients\right) \land \left(\operatorname{BddBelow}\left(fullOddQuotients\right) \land \left(\left(\forall N \in \operatorname{Nat}\left(\right),\; \operatorname{Subset}\left(\operatorname{apply}\left(finiteEvenQuotients, N\right), \operatorname{apply}\left(finiteEvenQuotients, \operatorname{add}\left(N, 1\right)\right)\right)\right) \land \left(\left(\forall N \in \operatorname{Nat}\left(\right),\; \operatorname{Subset}\left(\operatorname{apply}\left(finiteOddQuotients, N\right), \operatorname{apply}\left(finiteOddQuotients, \operatorname{add}\left(N, 1\right)\right)\right)\right) \land \left(\left(\forall N \in \operatorname{Nat}\left(\right),\; \operatorname{Subset}\left(\operatorname{apply}\left(finiteEvenQuotients, N\right), fullEvenQuotients\right)\right) \land \left(\left(\forall N \in \operatorname{Nat}\left(\right),\; \operatorname{Subset}\left(\operatorname{apply}\left(finiteOddQuotients, N\right), fullOddQuotients\right)\right) \land \left(\left(\forall q \in \operatorname{Real}\left(\right), epsilon \in \operatorname{Real}\left(\right),\; \left(\operatorname{Mem}\left(q, fullEvenQuotients\right) \land 0 < epsilon\right) \Rightarrow \left(\exists N \in \operatorname{Nat}\left(\right), qN \in \operatorname{Real}\left(\right),\; \operatorname{Mem}\left(qN, \operatorname{apply}\left(finiteEvenQuotients, N\right)\right) \land \operatorname{sub}\left(q, epsilon\right) < qN\right)\right) \land \left(\forall q \in \operatorname{Real}\left(\right), epsilon \in \operatorname{Real}\left(\right),\; \left(\operatorname{Mem}\left(q, fullOddQuotients\right) \land 0 < epsilon\right) \Rightarrow \left(\exists N \in \operatorname{Nat}\left(\right), qN \in \operatorname{Real}\left(\right),\; \operatorname{Mem}\left(qN, \operatorname{apply}\left(finiteOddQuotients, N\right)\right) \land qN < \operatorname{add}\left(q, epsilon\right)\right)\right)\right)\right)\right)\right)\right)\right)\right)\right)\right)\right)\right)\right)\right)\right)\right)\right)\right) \Rightarrow \left(\left(\forall N \in \operatorname{Nat}\left(\right), R \in \operatorname{Real}\left(\right),\; \operatorname{IsHermitian}\left(\operatorname{applyTwo}\left(evenPencil, N, R\right)\right) \land \operatorname{IsHermitian}\left(\operatorname{applyTwo}\left(oddPencil, N, R\right)\right)\right) \land \left(\left(\forall N \in \operatorname{Nat}\left(\right), R \in \operatorname{Real}\left(\right),\; \operatorname{Iff}\left(\operatorname{PosSemidef}\left(\operatorname{applyTwo}\left(evenPencil, N, R\right)\right), \operatorname{apply}\left(finiteLower, N\right) \le R\right)\right) \land \left(\left(\forall N \in \operatorname{Nat}\left(\right), R \in \operatorname{Real}\left(\right),\; \operatorname{Iff}\left(\operatorname{PosSemidef}\left(\operatorname{applyTwo}\left(oddPencil, N, R\right)\right), R \le \operatorname{apply}\left(finiteUpper, N\right)\right)\right) \land \left(\operatorname{Monotone}\left(finiteLower\right) \land \left(\operatorname{Antitone}\left(finiteUpper\right) \land \left(\operatorname{Tendsto}\left(finiteLower, atTop, \operatorname{nhds}\left(fullLower\right)\right) \land \left(\operatorname{Tendsto}\left(finiteUpper, atTop, \operatorname{nhds}\left(fullUpper\right)\right) \land \left(\left(\forall N \in \operatorname{Nat}\left(\right), R \in \operatorname{Real}\left(\right),\; \operatorname{Iff}\left(\operatorname{applyTwo}\left(feasible, N, R\right), \operatorname{MemIcc}\left(R, \operatorname{apply}\left(finiteLower, N\right), \operatorname{apply}\left(finiteUpper, N\right)\right)\right)\right) \land \left(\forall N \in \operatorname{Nat}\left(\right),\; \operatorname{apply}\left(finiteUpper, N\right) < \operatorname{apply}\left(finiteLower, N\right) \Rightarrow \left(\left(\forall R \in \operatorname{Real}\left(\right),\; \operatorname{PosSemidef}\left(\operatorname{applyTwo}\left(evenPencil, N, R\right)\right) \Rightarrow \operatorname{apply}\left(finiteLower, N\right) \le R\right) \land \left(\left(\forall R \in \operatorname{Real}\left(\right),\; \operatorname{PosSemidef}\left(\operatorname{applyTwo}\left(oddPencil, N, R\right)\right) \Rightarrow R \le \operatorname{apply}\left(finiteUpper, N\right)\right) \land \operatorname{Not}\left(\exists R \in \operatorname{Real}\left(\right),\; \operatorname{applyTwo}\left(feasible, N, R\right)\right)\right)\right)\right)\right)\right)\right)\right)\right)\right)\right)\right)$$

*Proof.* Machine-checked in Lean as `D5/S3/Weil/Budget/FiniteWeylBudgetBounds.finite_weyl_budget_bounds` (`✓ std3`). ∎

*Source.* Repository-derived.

*Commentary.*

The displayed statement constructs both finite quotient families, their endpoints, the two rank-one pencils, and finite feasibility directly from the matrix and boundary data.

Kernel positivity handles zero boundary pairings. Nested finite quotient sets and one-sided approximation of every full quotient express the Galerkin density hypothesis without assuming endpoint convergence.

Each finite pencil is positive semidefinite exactly on its corresponding budget ray. The lower endpoints increase, the upper endpoints decrease, both converge to the full interval, and a crossed finite interval supplies incompatible lower and upper requirements.

## References

- Truth anchor: `D5/S3/Weil/Budget/FiniteWeylBudgetBounds.finite_weyl_budget_bounds`
- Dependency: [D5/S3/Weil/Budget/OddTestBudgetUpperBound](OddTestBudgetUpperBound.md)
