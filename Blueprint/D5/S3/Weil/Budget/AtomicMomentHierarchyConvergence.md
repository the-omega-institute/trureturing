# Atomic Moment Hierarchy Convergence

## Abstract

Nested finite-moment completion frontiers converge to the frontier determined by the full moment family.

**Theorem 1.1 (Finite atomic frontiers decrease to the full frontier).**

$$\forall C \in \operatorname{Real}\left(\right), cap \in \operatorname{Real}\left(\right), levelMatch \in \operatorname{Nat}\left(\right) \to \left(\operatorname{Measure}\left(\operatorname{Real}\left(\right)\right) \to Prop\right), fullMatch \in \operatorname{Measure}\left(\operatorname{Real}\left(\right)\right) \to Prop, resolventBudget \in \operatorname{Measure}\left(\operatorname{Real}\left(\right)\right) \to \operatorname{Real}\left(\right), whiteFloor \in \operatorname{Measure}\left(\operatorname{Real}\left(\right)\right) \to \operatorname{Real}\left(\right), optimizer \in \operatorname{Nat}\left(\right) \to \operatorname{Measure}\left(\operatorname{Real}\left(\right)\right), cluster \in \operatorname{Measure}\left(\operatorname{Real}\left(\right)\right), selection \in \operatorname{Nat}\left(\right) \to \operatorname{Nat}\left(\right),\; \left(\left(\forall nu \in \operatorname{Measure}\left(\operatorname{Real}\left(\right)\right),\; \operatorname{apply}\left(whiteFloor, nu\right) \le cap\right) \land \left(\left(\forall N \in \operatorname{Nat}\left(\right), nu \in \operatorname{Measure}\left(\operatorname{Real}\left(\right)\right),\; \operatorname{applyTwo}\left(levelMatch, \operatorname{add}\left(N, 1\right), nu\right) \Rightarrow \operatorname{applyTwo}\left(levelMatch, N, nu\right)\right) \land \left(\left(\forall nu \in \operatorname{Measure}\left(\operatorname{Real}\left(\right)\right),\; \operatorname{apply}\left(fullMatch, nu\right) \Rightarrow \left(\forall N \in \operatorname{Nat}\left(\right),\; \operatorname{applyTwo}\left(levelMatch, N, nu\right)\right)\right) \land \left(\left(\forall nu \in \operatorname{Measure}\left(\operatorname{Real}\left(\right)\right),\; \left(\forall N \in \operatorname{Nat}\left(\right),\; \operatorname{applyTwo}\left(levelMatch, N, nu\right)\right) \Rightarrow \operatorname{apply}\left(fullMatch, nu\right)\right) \land \left(\left(\forall N \in \operatorname{Nat}\left(\right),\; \operatorname{applyTwo}\left(levelMatch, N, \operatorname{apply}\left(optimizer, N\right)\right)\right) \land \left(\left(\forall N \in \operatorname{Nat}\left(\right),\; \operatorname{apply}\left(resolventBudget, \operatorname{apply}\left(optimizer, N\right)\right) \le C\right) \land \left(\left(\forall N \in \operatorname{Nat}\left(\right),\; \operatorname{apply}\left(whiteFloor, \operatorname{apply}\left(optimizer, N\right)\right) = \operatorname{sSup}\left(\left\{\exists nu \in \operatorname{Measure}\left(\operatorname{Real}\left(\right)\right),\; \operatorname{applyTwo}\left(levelMatch, N, nu\right) \land \left(\operatorname{apply}\left(resolventBudget, nu\right) \le C \land r = \operatorname{apply}\left(whiteFloor, nu\right)\right) \mid r \in \operatorname{Real}\left(\right)\right\}\right)\right) \land \left(\left(\forall N \in \operatorname{Nat}\left(\right),\; \operatorname{applyTwo}\left(levelMatch, N, cluster\right)\right) \land \left(\operatorname{apply}\left(resolventBudget, cluster\right) \le C \land \left(\operatorname{StrictMono}\left(selection\right) \land \operatorname{Tendsto}\left((k: \operatorname{Nat}\left(\right) \mapsto \operatorname{apply}\left(whiteFloor, \operatorname{apply}\left(optimizer, \operatorname{apply}\left(selection, k\right)\right)\right)), atTop, \operatorname{nhds}\left(\operatorname{apply}\left(whiteFloor, cluster\right)\right)\right)\right)\right)\right)\right)\right)\right)\right)\right)\right)\right) \Rightarrow \operatorname{let} levelValues = N: \operatorname{Nat}\left(\right) \mapsto \left\{\exists nu \in \operatorname{Measure}\left(\operatorname{Real}\left(\right)\right),\; \operatorname{applyTwo}\left(levelMatch, N, nu\right) \land \left(\operatorname{apply}\left(resolventBudget, nu\right) \le C \land r = \operatorname{apply}\left(whiteFloor, nu\right)\right) \mid r \in \operatorname{Real}\left(\right)\right\}, \operatorname{let} fullValues = \left\{\exists nu \in \operatorname{Measure}\left(\operatorname{Real}\left(\right)\right),\; \operatorname{apply}\left(fullMatch, nu\right) \land \left(\operatorname{apply}\left(resolventBudget, nu\right) \le C \land r = \operatorname{apply}\left(whiteFloor, nu\right)\right) \mid r \in \operatorname{Real}\left(\right)\right\}, \operatorname{let} hierarchy = N: \operatorname{Nat}\left(\right) \mapsto \operatorname{sSup}\left(\operatorname{apply}\left(levelValues, N\right)\right), \operatorname{let} fullFrontier = \operatorname{sSup}\left(fullValues\right), \operatorname{Antitone}\left(hierarchy\right) \land \operatorname{Tendsto}\left(hierarchy, atTop, \operatorname{nhds}\left(fullFrontier\right)\right)$$

*Proof.* Machine-checked in Lean as `D5/S3/Weil/Budget/AtomicMomentHierarchyConvergence.atomic_moment_hierarchy_converges` (`✓ std3`). ∎

*Source.* Repository-derived.

*Commentary.*

Nested moment constraints make the finite frontiers antitone. A strict optimizer subsequence whose cluster satisfies every determining constraint identifies its order limit with the full frontier.

## References

- Truth anchor: `D5/S3/Weil/Budget/AtomicMomentHierarchyConvergence.atomic_moment_hierarchy_converges`
- Dependency: [D5/S3/Weil/Budget/ResolventFrontierGeometry](ResolventFrontierGeometry.md)
