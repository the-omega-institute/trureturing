# Labeled Zeta Vectors

## Theorem: The labeled vector never vanishes

Provenance: `literature-attested` via `D5/L/hedenmalm1997hilbert` (`lit/hedenmalm1997hilbert`)

Statement: `D5/S3/Weil/LabeledZeta.labeled_zeta_vector_ne_zero` `✓ std3`

The coordinate product needs no summability claim. Its empty-ledger coordinate is one, so the kernel-checked function cannot equal the zero vector.
