# Exact Weil Observable Range

Status: Candidate Lean source and author projection. Pinned compilation, transitive axiom audit and repository admission are separate obligations.

For a fixed ZeroData presentation and symmetric window, the scalar index evaluation reaches every reflection-even vector. The proof extends a window assignment by zero and applies the existing finite reflection-compatible interpolation theorem.

In the expanded coordinate space, a vector is reachable exactly when it is constant on every multiplicity fiber and its collapsed index vector is reflection even. Positive analytic multiplicity supplies an actual representative copy. Expansion and collapse are inverse on this subspace.

The original state space remains all WeilTestFunction values. For any two tests, equality of expanded evaluations is equivalent to equality of index evaluations. The joint observer which adds the expanded evaluation has the same kernel. Thus replication supplies no strict fiber-separating witness. This result uses the semantic kernel formulation of the intrinsic-information specification, without inventing finite collision probabilities on an infinite complex state space.

The mixed truncated zero sum of convolve(g, involution(h)) equals the existing multiplicity-weighted mirror form evaluated on the two reduced readouts. This identifies off-diagonal Gram entries on the now-proved exact image, with no duplicate multiplicity weight.

Main declarations: finiteWeilIndexEvaluation_range_iff; finiteWeilReducedEvaluation_surjective; finiteWeilCoordinateEvaluation_range_iff; finiteWeilEvaluation_readout_kernel_eq; no_intrinsic_kernel_escape_from_multiplicity_replication; truncatedZeroSum_mixed_eq_reducedMirrorForm.
