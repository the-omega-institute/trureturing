# Unique Finite-Atomic Residual

## Abstract

A represented truncated moment vector has an attained greatest Haar floor. Its singular residual has one rank-atomic measure and one maximal weighted completion.

**Theorem 1.1 (Unique finite-atomic residual).**

$$\forall N \in \mathbb{N}, m \in \mathbb{Z} \to \mathbb{C}, R \in \mathbb{R}, v \in \operatorname{Fin}\left(N + 1\right) \to \mathbb{C}, D \in \operatorname{ContinuousMap}\left(\operatorname{Circle}\left(\right), \mathbb{C}\right),\; \left(\left(\left(\left(\left(\left(\left(\left(\left(\left(\left(\forall ell \in \mathbb{Z},\; m\left(\operatorname{neg}\left(ell\right)\right) = \operatorname{star}\left(m\left(ell\right)\right)\right) \land m\left(0\right) = R\right) \land 0 < R\right) \land \left(\exists mu \in \operatorname{FiniteMeasure}\left(\operatorname{Circle}\left(\right)\right),\; \forall ell \in \mathbb{Z},\; \operatorname{natAbs}\left(ell\right) \le N \Rightarrow \operatorname{integral}\left(z, \operatorname{Circle}\left(\right), \operatorname{zpow}\left(z, \operatorname{neg}\left(ell\right)\right), mu\right) = m\left(ell\right)\right)\right) \land \operatorname{let} alphaStar = \operatorname{sSup}\left(\operatorname{setOf}\left((alpha \mapsto \exists mu \in \operatorname{FiniteMeasure}\left(\operatorname{Circle}\left(\right)\right),\; \left(\forall ell \in \mathbb{Z},\; \operatorname{natAbs}\left(ell\right) \le N \Rightarrow \operatorname{integral}\left(z, \operatorname{Circle}\left(\right), \operatorname{zpow}\left(z, \operatorname{neg}\left(ell\right)\right), mu\right) = m\left(ell\right)\right) \land \operatorname{toMeasure}\left(\operatorname{smul}\left(alpha, \operatorname{normalizedCircleHaar}\left(\right)\right)\right) \le \operatorname{toMeasure}\left(mu\right))\right)\right)\right) \land \operatorname{let} mStar = (ell \mapsto m\left(ell\right) - \operatorname{ite}\left(ell = 0, alphaStar, 0\right))\right) \land \operatorname{let} Tstar = \operatorname{Matrix}\left((j,k\in\operatorname{Fin}\left(N + 1\right) \mapsto mStar\left(\operatorname{toInt}\left(j\right) - \operatorname{toInt}\left(k\right)\right))\right)\right) \land \left(\exists mu \in \operatorname{FiniteMeasure}\left(\operatorname{Circle}\left(\right)\right),\; \left(\forall ell \in \mathbb{Z},\; \operatorname{natAbs}\left(ell\right) \le N \Rightarrow \operatorname{integral}\left(z, \operatorname{Circle}\left(\right), \operatorname{zpow}\left(z, \operatorname{neg}\left(ell\right)\right), mu\right) = m\left(ell\right)\right) \land \operatorname{toMeasure}\left(\operatorname{smul}\left(alphaStar, \operatorname{normalizedCircleHaar}\left(\right)\right)\right) \le \operatorname{toMeasure}\left(mu\right)\right)\right) \land \operatorname{PosSemidef}\left(\operatorname{Matrix}\left((j,k\in\operatorname{Fin}\left(N + 1\right) \mapsto mStar\left(\operatorname{toInt}\left(j\right) - \operatorname{toInt}\left(k\right)\right))\right)\right)\right) \land \operatorname{dotProduct}\left(\operatorname{star}\left(v\right), v\right) = 1\right) \land \operatorname{mulVec}\left(\operatorname{Matrix}\left((j,k\in\operatorname{Fin}\left(N + 1\right) \mapsto mStar\left(\operatorname{toInt}\left(j\right) - \operatorname{toInt}\left(k\right)\right))\right), v\right) = 0\right) \Rightarrow \left(\left(\left(\left(\exists mu \in \operatorname{FiniteMeasure}\left(\operatorname{Circle}\left(\right)\right),\; \left(\forall ell \in \mathbb{Z},\; \operatorname{natAbs}\left(ell\right) \le N \Rightarrow \operatorname{integral}\left(z, \operatorname{Circle}\left(\right), \operatorname{zpow}\left(z, \operatorname{neg}\left(ell\right)\right), mu\right) = m\left(ell\right)\right) \land \operatorname{toMeasure}\left(\operatorname{smul}\left(alphaStar, \operatorname{normalizedCircleHaar}\left(\right)\right)\right) \le \operatorname{toMeasure}\left(mu\right)\right) \land \left(\forall beta \in \operatorname{NonnegativeReal}\left(\right),\; \left(\exists mu \in \operatorname{FiniteMeasure}\left(\operatorname{Circle}\left(\right)\right),\; \left(\forall ell \in \mathbb{Z},\; \operatorname{natAbs}\left(ell\right) \le N \Rightarrow \operatorname{integral}\left(z, \operatorname{Circle}\left(\right), \operatorname{zpow}\left(z, \operatorname{neg}\left(ell\right)\right), mu\right) = m\left(ell\right)\right) \land \operatorname{toMeasure}\left(\operatorname{smul}\left(beta, \operatorname{normalizedCircleHaar}\left(\right)\right)\right) \le \operatorname{toMeasure}\left(mu\right)\right) \Rightarrow beta \le alphaStar\right)\right) \land alphaStar = \operatorname{smallestEigenvalue}\left(\operatorname{Matrix}\left((j,k\in\operatorname{Fin}\left(N + 1\right) \mapsto m\left(\operatorname{toInt}\left(j\right) - \operatorname{toInt}\left(k\right)\right))\right)\right)\right) \land \left(\exists tau \in \operatorname{FiniteMeasure}\left(\operatorname{Circle}\left(\right)\right),\; \left(\left(\forall ell \in \mathbb{Z},\; \operatorname{natAbs}\left(ell\right) \le N \Rightarrow \operatorname{integral}\left(z, \operatorname{Circle}\left(\right), \operatorname{zpow}\left(z, \operatorname{neg}\left(ell\right)\right), tau\right) = mStar\left(ell\right)\right) \land \left(\forall rho \in \operatorname{FiniteMeasure}\left(\operatorname{Circle}\left(\right)\right),\; \left(\forall ell \in \mathbb{Z},\; \operatorname{natAbs}\left(ell\right) \le N \Rightarrow \operatorname{integral}\left(z, \operatorname{Circle}\left(\right), \operatorname{zpow}\left(z, \operatorname{neg}\left(ell\right)\right), rho\right) = mStar\left(ell\right)\right) \Rightarrow rho = tau\right)\right) \land \left(\exists point \in \operatorname{Fin}\left(\operatorname{rank}\left(Tstar\right)\right) \to \operatorname{Circle}\left(\right), weight \in \operatorname{Fin}\left(\operatorname{rank}\left(Tstar\right)\right) \to \operatorname{NonnegativeReal}\left(\right),\; \left(\left(\operatorname{Injective}\left(point\right) \land \left(\forall a \in \operatorname{Fin}\left(\operatorname{rank}\left(Tstar\right)\right),\; 0 < weight\left(a\right)\right)\right) \land \operatorname{toMeasure}\left(tau\right) = \operatorname{sum}\left(a, \operatorname{Fin}\left(\operatorname{rank}\left(Tstar\right)\right), \operatorname{smul}\left(weight\left(a\right), \operatorname{dirac}\left(point\left(a\right)\right)\right)\right)\right) \land \left(\exists muStar \in \operatorname{FiniteMeasure}\left(\operatorname{Circle}\left(\right)\right),\; \left(\left(\exists tau \in \operatorname{FiniteMeasure}\left(\operatorname{Circle}\left(\right)\right),\; \left(\forall ell \in \mathbb{Z},\; \operatorname{natAbs}\left(ell\right) \le N \Rightarrow \operatorname{integral}\left(z, \operatorname{Circle}\left(\right), \operatorname{zpow}\left(z, \operatorname{neg}\left(ell\right)\right), tau\right) = mStar\left(ell\right)\right) \land \operatorname{toMeasure}\left(muStar\right) = \operatorname{toMeasure}\left(\operatorname{smul}\left(alphaStar, \operatorname{normalizedCircleHaar}\left(\right)\right)\right) + \operatorname{withDensity}\left(\operatorname{toMeasure}\left(tau\right), (z \mapsto \operatorname{ofReal}\left(\operatorname{normSq}\left(D\left(z\right)\right)\right))\right)\right) \land \left(\forall nu \in \operatorname{FiniteMeasure}\left(\operatorname{Circle}\left(\right)\right),\; \left(\exists tau \in \operatorname{FiniteMeasure}\left(\operatorname{Circle}\left(\right)\right),\; \left(\forall ell \in \mathbb{Z},\; \operatorname{natAbs}\left(ell\right) \le N \Rightarrow \operatorname{integral}\left(z, \operatorname{Circle}\left(\right), \operatorname{zpow}\left(z, \operatorname{neg}\left(ell\right)\right), tau\right) = mStar\left(ell\right)\right) \land \operatorname{toMeasure}\left(nu\right) = \operatorname{toMeasure}\left(\operatorname{smul}\left(alphaStar, \operatorname{normalizedCircleHaar}\left(\right)\right)\right) + \operatorname{withDensity}\left(\operatorname{toMeasure}\left(tau\right), (z \mapsto \operatorname{ofReal}\left(\operatorname{normSq}\left(D\left(z\right)\right)\right))\right)\right) \Rightarrow nu = muStar\right)\right) \land \operatorname{toMeasure}\left(muStar\right) = \operatorname{toMeasure}\left(\operatorname{smul}\left(alphaStar, \operatorname{normalizedCircleHaar}\left(\right)\right)\right) + \operatorname{sum}\left(a, \operatorname{Fin}\left(\operatorname{rank}\left(Tstar\right)\right), \operatorname{smul}\left(weight\left(a\right) \cdot \operatorname{ofReal}\left(\operatorname{normSq}\left(D\left(point\left(a\right)\right)\right)\right), \operatorname{dirac}\left(point\left(a\right)\right)\right)\right)\right)\right)\right)\right)$$

*Proof.* Machine-checked in Lean as `D5/S3/Weil/TestFunctions/UniqueFiniteAtomicResidual.unique_finite_atomic_residual` (`✓ std3`). ∎

*Source.* Repository-derived.

*Commentary.*

The feasible floor coefficients are those dominated by a representing measure, and the distinguished coefficient is their supremum. The exact floor theorem identifies it with the least Toeplitz eigenvalue.

After subtracting that attained greatest coefficient, the residual measure is constructed from the positive semidefinite truncated moment matrix. A unit kernel vector confines its support to the contact roots.

Lagrange interpolation makes every representing measure agree on each root mass, proving uniqueness. Removing zero masses leaves positive distinct atoms, while a weighted Vandermonde Gram factorization identifies their number with the Toeplitz rank.

Multiplication by the denominator norm-square density constructs the maximal completion at the distinguished coefficient. Residual uniqueness gives completion uniqueness, and Dirac density gives the weighted atomic sum.

## References

- Truth anchor: `D5/S3/Weil/TestFunctions/UniqueFiniteAtomicResidual.unique_finite_atomic_residual`
- Dependency: [D5/S3/Weil/TestFunctions/ExactTruncatedHaarFloor](ExactTruncatedHaarFloor.md)
