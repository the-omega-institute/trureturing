using static StrataLint.Scribe.DefinitionDsl;
using static StrataLint.Scribe.FormulaDsl;
using F = StrataLint.Scribe.FormulaDsl;

namespace StrataLint.Scribe.Blueprint.D5.S3.Arith.PrimeIdeals;

internal sealed class FinitelyBlindPrimeIdealsDocument : IScribeDocumentDefinition
{
    public DocumentDefinition Create() => DocumentDefinition.Create(ScribeNode.Create(
        "Dedekind reductions have finite blind sets and a coarse-property counterexample.",
        H("Finitely Blind Prime Ideals"),
        Blocks(
            Describe.Lean(
                DescribeId.Create("reduction-equality-is-divisibility-of-the-difference"),
                DeclarationHandle.Create(
                    "D5/S3/Arith/PrimeIdeals/FinitelyBlindPrimeIdeals."
                        + "reduction_eq_iff_dvd_difference"),
                H("Reduction equality is divisibility of the difference"),
                StatementSource.FromAuthor(Disp(Seq(
                    Forall, Sp, F.Id("v"), Sp, Comma, Sp, F.Id("a"), Sp, Comma, Sp,
                    F.Id("b"), Sp, Rightarrow, Sp,
                    Call("primeIdealReduction", F.Id("v"), F.Id("a")), Sp, Eq, Sp,
                    Call("primeIdealReduction", F.Id("v"), F.Id("b")), Sp, Iff, Sp,
                    Call("IdealDvdSpan", F.Id("v"), Subtract(F.Id("a"), F.Id("b"))), Dot))),
                AssessedProvenance.FromRepo(),
                Blocks(Paragraph(Text(
                    "The canonical ideal quotient map identifies two elements exactly when "
                        + "their difference lies in the prime ideal. In a Dedekind domain, "
                        + "membership is the same as divisibility of its principal ideal."))),
                DescribeRole.Theorem),
            Describe.Lean(
                DescribeId.Create("distinct-elements-have-finitely-many-blind-prime-ideals"),
                DeclarationHandle.Create(
                    "D5/S3/Arith/PrimeIdeals/FinitelyBlindPrimeIdeals."
                        + "finite_blind_prime_ideals"),
                H("Distinct elements have finitely many blind prime ideals"),
                StatementSource.FromAuthor(Disp(Seq(
                    Forall, Sp, F.Id("a"), Sp, Comma, Sp, F.Id("b"), Sp, Comma, Sp,
                    F.Id("a"), Sp, Neq, Sp, F.Id("b"), Sp, Rightarrow, Sp,
                    Operatorname, Grp(F.Id("Finite")), Open,
                    Call("blindPrimeIdeals", F.Id("a"), F.Id("b")), Close, Dot))),
                AssessedProvenance.FromRepo(),
                Blocks(Paragraph(Text(
                    "The nonzero principal ideal generated by a - b has only finitely many "
                        + "height-one prime divisors. The equality theorem transports that "
                        + "finite support to the set of prime ideals where reduction is blind."))),
                DescribeRole.Theorem),
            Describe.Lean(
                DescribeId.Create("unit-difference-has-no-blind-prime-ideals"),
                DeclarationHandle.Create(
                    "D5/S3/Arith/PrimeIdeals/FinitelyBlindPrimeIdeals."
                        + "unit_difference_has_no_blind_prime_ideals"),
                H("A unit difference has no blind prime ideal"),
                StatementSource.FromAuthor(Disp(Seq(
                    Call("IsUnit", Subtract(F.Id("a"), F.Id("b"))), Sp, Rightarrow, Sp,
                    Call("blindPrimeIdeals", F.Id("a"), F.Id("b")), Sp, Eq, Sp,
                    Emptyset, Dot))),
                AssessedProvenance.FromRepo(),
                Blocks(Paragraph(Text(
                    "When a - b is a unit, no proper prime ideal can contain it. Thus the "
                        + "exceptional blind set is empty, explicitly covering the unit case."))),
                DescribeRole.Theorem),
            Describe.Lean(
                DescribeId.Create("coarse-zero-observation-merges-one-and-negative-one"),
                DeclarationHandle.Create(
                    "D5/S3/Arith/PrimeIdeals/FinitelyBlindPrimeIdeals."
                        + "coarse_zero_observation_counterexample"),
                H("The zero-reduction property merges one and negative one everywhere"),
                StatementSource.FromAuthor(Disp(Seq(
                    D(1), Sp, Neq, Sp, Minus, D(1), Sp, Land, Sp,
                    Forall, Sp, F.Id("v"), Sp, InMacro, Sp, Mathbb, Grp(F.Id("Z")), Comma, Sp,
                    Call("zeroReductionObservation", F.Id("v"), D(1)), Sp, Iff, Sp,
                    Call("zeroReductionObservation", F.Id("v"), Seq(Minus, D(1))), Dot))),
                AssessedProvenance.FromRepo(),
                Blocks(Paragraph(Text(
                    "In the concrete integer ring, 1 and -1 are distinct global elements, "
                        + "but both are units. Their reduction is therefore nonzero at every "
                        + "prime ideal, so the coarse zero/nonzero property agrees everywhere."))),
                DescribeRole.Theorem),
            Describe.Lean(
                DescribeId.Create("distinctness-is-necessary-for-separation"),
                DeclarationHandle.Create(
                    "D5/S3/Arith/PrimeIdeals/FinitelyBlindPrimeIdeals."
                        + "distinctness_is_necessary_for_separation"),
                H("Distinctness is necessary for separation"),
                StatementSource.FromAuthor(Disp(Seq(
                    Neg, Exists, Sp, F.Id("v"), Sp, InMacro, Sp, Mathbb, Grp(F.Id("Z")), Comma,
                    Sp, Call("primeIdealReduction", F.Id("v"), D(0)), Sp, Neq, Sp,
                    Call("primeIdealReduction", F.Id("v"), D(0)), Dot))),
                AssessedProvenance.FromRepo(),
                Blocks(Paragraph(Text(
                    "For the concrete equal pair 0, 0, every prime ideal gives the same "
                        + "reduction. This records why the distinctness premise is required "
                        + "for a separation conclusion."))),
                DescribeRole.Theorem))));
}
