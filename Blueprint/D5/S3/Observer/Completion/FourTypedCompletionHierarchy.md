# Four Typed Completion Modes

## Abstract

Target, state-family, algebra, and uniform completion remain distinct typed claims.

**Theorem 1.1 (The four completion modes form a strict typed hierarchy).**

$$\begin{gathered}\forall K, H: Type,\\{}\operatorname{RCLike}\left(K\right) \land \operatorname{NormedAddCommGroup}\left(H\right) \land \operatorname{InnerProductSpace}\left(K, H\right),\\{}V: \mathbb{N} \to \operatorname{Submodule}\left(K, H\right), (\forall n\in \mathbb{N}, \operatorname{HasOrthogonalProjection}\left(V(n)\right)),\\{}T: \operatorname{Set}\left(H\right), x: H,\\{}((\operatorname{UniformBall}\left(V\right) \Rightarrow \operatorname{StateFamily}\left(V, T\right)) \land (x\in T \Rightarrow \operatorname{StateFamily}\left(V, T\right) \Rightarrow \operatorname{Target}\left(V, x\right))) \land\\{}(\text{let} V0: \mathbb{N} \to \operatorname{Submodule}\left(\mathbb{R}, \mathbb{R}\right) := n \mapsto bot; T0: \operatorname{Set}\left(\mathbb{R}\right) := \{0, 1\}; ((\operatorname{Target}\left(V0, 0\right)) \land (\neg \operatorname{StateFamily}\left(V0, T0\right)))) \land\\{}(\text{let} e0: \operatorname{EuclideanSpace}\left(\mathbb{C}, \operatorname{Fin}\left(2\right)\right) := \operatorname{toLp2}\left(1, 0\right); W: \operatorname{Submodule}\left(\mathbb{C}, \operatorname{EuclideanSpace}\left(\mathbb{C}, \operatorname{Fin}\left(2\right)\right)\right) := \mathbb{C} \cdot e0; V1: \mathbb{N} \to \operatorname{Submodule}\left(\mathbb{C}, \operatorname{EuclideanSpace}\left(\mathbb{C}, \operatorname{Fin}\left(2\right)\right)\right) := n \mapsto W; T1: \operatorname{Set}\left(\operatorname{EuclideanSpace}\left(\mathbb{C}, \operatorname{Fin}\left(2\right)\right)\right) := \{e0\}; ((\operatorname{StateFamily}\left(V1, T1\right)) \land (\neg \operatorname{UniformBall}\left(V1\right)))) \land\\{}(\text{let} A2: \operatorname{Matrix}\left(\operatorname{ZMod}\left(2\right), \operatorname{ZMod}\left(2\right), \mathbb{C}\right) := \operatorname{single}\left(0, 1, 1\right); s2: \operatorname{EuclideanSpace}\left(\mathbb{C}, \operatorname{Fin}\left(2\right)\right) := \operatorname{toLp2}\left(\operatorname{entry}\left(A2, 0, 0\right), \operatorname{entry}\left(A2, 0, 1\right)\right); V2: \mathbb{N} \to \operatorname{Submodule}\left(\mathbb{C}, \operatorname{EuclideanSpace}\left(\mathbb{C}, \operatorname{Fin}\left(2\right)\right)\right) := n \mapsto bot; T2: \operatorname{Set}\left(\operatorname{EuclideanSpace}\left(\mathbb{C}, \operatorname{Fin}\left(2\right)\right)\right) := \{s2\}; ((A2 \in \operatorname{windowGeneratedAlgebra}\left(2\right)) \land (\operatorname{windowGeneratedAlgebra}\left(2\right) = top) \land (s2 = \operatorname{toLp2}\left(0, 1\right)) \land (\neg \operatorname{Target}\left(V2, s2\right)) \land (\neg \operatorname{StateFamily}\left(V2, T2\right)) \land (\neg \operatorname{UniformBall}\left(V2\right)))) \land\\{}(\text{let} A3: \operatorname{Matrix}\left(\operatorname{Fin}\left(2\right), \operatorname{Fin}\left(2\right), \mathbb{C}\right) := \operatorname{single}\left(1, 0, 1\right); s3: \operatorname{EuclideanSpace}\left(\mathbb{C}, \operatorname{Fin}\left(2\right)\right) := \operatorname{toLp2}\left(\operatorname{entry}\left(A3, 1, 0\right), \operatorname{entry}\left(A3, 1, 1\right)\right); V3: \mathbb{N} \to \operatorname{Submodule}\left(\mathbb{C}, \operatorname{EuclideanSpace}\left(\mathbb{C}, \operatorname{Fin}\left(2\right)\right)\right) := n \mapsto top; T3: \operatorname{Set}\left(\operatorname{EuclideanSpace}\left(\mathbb{C}, \operatorname{Fin}\left(2\right)\right)\right) := \{s3\}; ((\operatorname{UniformBall}\left(V3\right)) \land (\operatorname{StateFamily}\left(V3, T3\right)) \land (\operatorname{Target}\left(V3, s3\right)) \land (\operatorname{primeDiagonalAlgebra}\left(2, \emptyset\right) \neq top) \land (\neg (A3 \in \operatorname{primeDiagonalAlgebra}\left(2, \emptyset\right))))).\\{}\text{where} \operatorname{Target}\left(V, x\right) := \operatorname{lim}\left(n, \infty, \left\lVert \operatorname{P}\left(V(n)^{\perp}\right)(x) \right\rVert\right) = 0; \operatorname{StateFamily}\left(V, T\right) := \operatorname{lim}\left(n, \infty, \operatorname{sup}_{y\in T} \left\lVert \operatorname{P}\left(V(n)^{\perp}\right)(y) \right\rVert\right) = 0; \operatorname{UniformBall}\left(V\right) := \operatorname{lim}\left(n, \infty, \left\lVert I - \operatorname{P}\left(V(n)\right) \right\rVert\right) = 0\end{gathered}$$

*Proof.* Machine-checked in Lean as `D5/S3/Observer/Completion/FourTypedCompletionHierarchy.four_typed_completion_hierarchy` (`✓ std3`). ∎

*Source.* Repository-derived.

*Commentary.*

For arbitrary scalar and Hilbert carriers K and H, with RCLike K, a normed additive commutative group H, an inner-product structure, orthogonally complemented stages V, a state family T, and a target x, uniform projection convergence implies convergence of the canonical family residual. If x belongs to T, family convergence then implies target convergence.

A constant zero tower on the real line resolves target zero but not the two-point family. A constant one-dimensional complex subspace resolves its displayed nonzero member family while every stage remains proper, so uniform convergence fails.

For algebra completion, the two-address clock-and-shift algebra is the full matrix algebra and contains the displayed off-diagonal observable. The state constructed from that observable nevertheless fails target, singleton-family, and uniform projection convergence for the constant zero tower.

Conversely, the constant top tower resolves the displayed matrix-derived state, its singleton family, and the uniform ball. The finite prime-diagonal operational algebra is still proper and omits the same constructed off-diagonal observable. Thus every completion claim in the statement remains attached to its target, family, operator algebra, or uniform-ball object.

## References

- Truth anchor: `D5/S3/Observer/Completion/FourTypedCompletionHierarchy.four_typed_completion_hierarchy`
- Dependency: [D5/S3/Observer/Completion/HilbertResolutionHierarchy](HilbertResolutionHierarchy.md)
- Dependency: [D5/S3/Observer/WindowAlgebra/WindowGeneration](../WindowAlgebra/WindowGeneration.md)
- Dependency: [D5/S3/Quantum/FixedAlgebra/PrimeDiagonalSaturation](../../Quantum/FixedAlgebra/PrimeDiagonalSaturation.md)
