# Operational Completeness and Classical Separation

## Abstract

Full finite-window operations and state tomography do not yield a multiplicative classical state.

**Theorem 1.1 (Operational completeness does not imply classical completeness).**

$$\begin{gathered}\forall M \in \mathbb{N}_{>1},\\{}\operatorname{Generated}\left(\operatorname{clock}\left(M\right), \operatorname{shift}\left(M\right)\right) = \operatorname{Matrix}\left(M\right) \land \\{}\operatorname{centralizer}\left(\operatorname{Generated}\left(\operatorname{clock}\left(M\right), \operatorname{shift}\left(M\right)\right)\right) = \operatorname{Scalars}\left(M\right) \land \operatorname{Char}\left(\operatorname{Generated}\left(\operatorname{clock}\left(M\right), \operatorname{shift}\left(M\right)\right)\right) = \varnothing \land \\{}(\forall \rho, \sigma, \operatorname{Density}\left(\rho, M\right) \land \operatorname{Density}\left(\sigma, M\right) \land (\forall A, \operatorname{omega}\left(\rho, A\right) = \operatorname{omega}\left(\sigma, A\right)) \Rightarrow \rho = \sigma) \land \\{}(\forall \rho, \operatorname{Density}\left(\rho, M\right) \Rightarrow \operatorname{Linear}\left(\operatorname{omega}\left(\rho\right)\right) \land \operatorname{Positive}\left(\operatorname{omega}\left(\rho\right)\right) \land \operatorname{omega}\left(\rho, I\right) = 1) \land \\{}\exists \rho, A, B, \operatorname{Density}\left(\rho, M\right) \land \operatorname{omega}\left(\rho, A\right) = 0 \land \operatorname{omega}\left(\rho, B\right) = 0 \land \operatorname{omega}\left(\rho, AB\right) = 1.\end{gathered}$$

*Proof.* Machine-checked in Lean as `D5/S3/Observer/WindowAlgebra/OperationalClassicalSeparation.operational_complete_not_classically_complete` (`✓ std3`). ∎

*Source.* Repository-derived.

*Commentary.*

For every finite cyclic window with more than one address, the algebra generated by the canonical clock and shift is the full matrix algebra. Its canonical centralizer is exactly the scalar matrices, and the generated algebra has no unital complex character.

The trace functional is imported from the matrix-state family. Trace expectations against all matrices separate positive trace-one density matrices, while each such density gives a normalized complex-linear functional that is nonnegative on adjoint squares.

The final clause constructs the basis density at address zero and the opposite matrix units from zero to one and one to zero. Their individual expectations vanish, but the expectation of their product is one, so the state is explicitly not multiplicative.

The proof applies the existing window generation, scalar commutant, character exclusion, and matrix-state cone results together with Mathlib's trace-pairing separation and matrix-unit identities. No existing declaration combined all six public clauses.

## References

- Truth anchor: `D5/S3/Observer/WindowAlgebra/OperationalClassicalSeparation.operational_complete_not_classically_complete`
- Dependency: [D5/S3/Observer/WindowAlgebra/WindowGeneration](WindowGeneration.md)
- Dependency: [D5/S3/Observer/WindowCharacter](../WindowCharacter.md)
- Dependency: [D5/S3/QuantumStates/GNSStateCone](../../QuantumStates/GNSStateCone.md)
- Dependency: [D5/S3/QuantumStates/GNSZeroPropagation](../../QuantumStates/GNSZeroPropagation.md)
