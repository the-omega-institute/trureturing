using static StrataLint.Scribe.DefinitionDsl;
using static StrataLint.Scribe.FormulaDsl;
using F = StrataLint.Scribe.FormulaDsl;

namespace StrataLint.Scribe.Blueprint.D5.S3.QuantumContext;

internal sealed class QuarticContextWitnessDocument : IScribeDocumentDefinition
{
    public DocumentDefinition Create() => DocumentDefinition.Create(ScribeDocument.Create(
        Header(
            "D5/S3/QuantumContext/QuarticContextWitness",
            "Two exact qutrit contexts give distinct quartic pricing totals for one pure state."),
        H("An Exact Qutrit Quartic-Context Witness"),
        Blocks(
            DocumentBlock.Describe.Theorem(
                DescribeId.Create("quartic-pricing-is-context-dependent-on-an-exact-qutrit-witness"),
                H("Quartic pricing is context-dependent on an exact qutrit witness"),
                LeanTheorem(
                    "D5/S3/QuantumContext/QuarticContextWitness.quartic_pricing_context_counterexample"),
                Disp(Seq(
                    Mathcal, Grp(F.Id("Q")), Underscore, Grp(D(4)),
                    Open, Rho, Comma, Sp, F.Id("C"), Underscore,
                    Grp(Mathrm, Grp(F.Id("std"))), Close,
                    Sp, Eq, Sp, Frac, Grp(D(1)), Grp(D(3)),
                    Sp, Lt, Sp, D(1), Sp, Eq, Sp,
                    Mathcal, Grp(F.Id("Q")), Underscore, Grp(D(4)),
                    Open, Rho, Comma, Sp, F.Id("C"), Underscore,
                    Grp(Mathrm, Grp(F.Id("aligned"))), Close, Dot)),
                DescribeProvenance.RepoDerived(),
                Blocks(
                    Paragraph(Text(
                        "Let rho be the rank-one qutrit density matrix whose nine entries are " +
                        "all one third. The declaration uniform_density_is_state proves that rho " +
                        "is positive semidefinite and has trace one. The standard context consists " +
                        "of the three coordinate projections.")),
                    Paragraph(Text(
                        "The aligned context is generated by the three integer rays (1,1,1), " +
                        "(1,-1,0), and (1,1,-2). Dividing their outer products by 3, 2, and 6 " +
                        "gives the displayed rational projection matrices. The declarations " +
                        "standard_projection_from_ray and aligned_projection_from_ray certify " +
                        "these vector formulas, while the two projective-context certificates " +
                        "prove self-adjointness, idempotence, and exact resolution of the identity.")),
                    Paragraph(Text(
                        "Quartic price is defined as the square of the real Born weight of a " +
                        "projection. In the standard context the three weights are all one third, " +
                        "so the quartic total is exactly one third. In the aligned context the " +
                        "weights are one, zero, and zero, so the quartic total is exactly one. " +
                        "The strict inequality is part of the Lean theorem and cannot be weakened " +
                        "to a non-strict comparison.")),
                    Paragraph(Text(
                        "The companion born_control_totals certificate gives total Born weight " +
                        "one in both contexts. Thus the mismatch belongs to the fourth-power " +
                        "pricing rule, not to an incomplete context or an unnormalized state. " +
                        "The module proves this concrete counterexample only; it asserts no " +
                        "Gleason representation theorem, random-basis statistic, or general " +
                        "extremal classification.")))))));
}
