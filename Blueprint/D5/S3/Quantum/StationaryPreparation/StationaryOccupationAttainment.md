# Stationary Occupation Attainment

## Abstract

Last-tail padding realizes the exact occupation Gram blocks and one fixed preparation isometry.

**Theorem 1.1 (General stationary attainment).**

$$\forall sigma: Type, (((\operatorname{Fintype}\left(sigma\right)) \land (\operatorname{Nonempty}\left(sigma\right)))) \implies (\forall a: \operatorname{Multiset}\left(sigma\right), head: sigma, (\operatorname{count}\left(a, head\right) = \operatorname{sup}\left(\operatorname{count}\left(a\right)\right)) \implies (((\forall r: \operatorname{Multiset}\left(sigma\right), \operatorname{M}\left(r\right) = \operatorname{NatDiv}\left(\operatorname{factorial}\left(\operatorname{card}\left(r\right)\right), \operatorname{prod}\left(\operatorname{fun}\left(i, \operatorname{factorial}\left(\operatorname{count}\left(r, i\right)\right)\right)\right)\right)) \land (\operatorname{z}\left(head, 0\right) = 1) \land (\forall h: Nat, (((\operatorname{le}\left(1, h\right)) \land (\operatorname{le}\left(h, \operatorname{count}\left(a, head\right)\right)))) \implies (\operatorname{z}\left(head, \operatorname{replicate}\left(h, head\right)\right) = 1)) \land (\forall r: \operatorname{Multiset}\left(sigma\right), (((\operatorname{le}\left(r, a\right)) \land (\operatorname{tailOcc}\left(head, r\right) \neq 0))) \implies (\operatorname{z}\left(head, r\right) = 0)) \land (\operatorname{Bijective}\left(\operatorname{fun}\left(r: \operatorname{Box}\left(a\right), \operatorname{pair}\left(\operatorname{tailIndex}\left(a, head, r\right), \operatorname{apply}\left(r, head\right)\right)\right)\right)) \land (\forall b: \operatorname{Tail}\left(a, head\right), ((\operatorname{block}\left(a, head, b\right) = \operatorname{mul}\left(\operatorname{mul}\left(\operatorname{L}\left(\operatorname{count}\left(a, head\right)\right), \operatorname{diagonal}\left(\operatorname{fun}\left(j, \operatorname{delta}\left(a, head, b, \operatorname{val}\left(j\right)\right)\right)\right)\right), \operatorname{transpose}\left(\operatorname{L}\left(\operatorname{count}\left(a, head\right)\right)\right)\right)) \land (\operatorname{rank}\left(\operatorname{block}\left(a, head, b\right)\right) = \operatorname{if}\left(b = 0, 1, \operatorname{add}\left(\operatorname{count}\left(a, head\right), 1\right)\right)) \land ((b = 0) \implies (((\operatorname{m}\left(a, head, b, 0\right) = 1) \land (\forall j: Nat, \operatorname{delta}\left(a, head, b, j\right) = \operatorname{if}\left(j = 0, 1, 0\right))))) \land ((b \neq 0) \implies (((\forall j: \operatorname{Fin}\left(\operatorname{add}\left(\operatorname{count}\left(a, head\right), 1\right)\right), \operatorname{lt}\left(0, \operatorname{delta}\left(a, head, b, \operatorname{val}\left(j\right)\right)\right)) \land (\forall j: \operatorname{Fin}\left(\operatorname{add}\left(\operatorname{count}\left(a, head\right), 1\right)\right), \operatorname{lastTail}\left(head, \operatorname{slice}\left(a, head, b, \operatorname{val}\left(j\right)\right)\right) = \operatorname{delta}\left(a, head, b, \operatorname{val}\left(j\right)\right))))) \land (\forall j: Nat, (((\operatorname{le}\left(1, j\right)) \land (\operatorname{le}\left(j, \operatorname{count}\left(a, head\right)\right)))) \implies (((\operatorname{div}\left(\operatorname{m}\left(a, head, b, j\right), \operatorname{m}\left(a, head, b, \operatorname{NatSub}\left(j, 1\right)\right)\right) = \operatorname{div}\left(\operatorname{add}\left(j, \operatorname{card}\left(\operatorname{tailWord}\left(a, head, b\right)\right)\right), j\right)) \land ((b \neq 0) \implies (\operatorname{lt}\left(1, \operatorname{div}\left(\operatorname{m}\left(a, head, b, j\right), \operatorname{m}\left(a, head, b, \operatorname{NatSub}\left(j, 1\right)\right)\right)\right)))))))) \land (\operatorname{card}\left(\operatorname{K}\left(a, head\right)\right) = \operatorname{N}\left(a\right)) \land (\forall r: \operatorname{Multiset}\left(sigma\right), (\operatorname{le}\left(r, a\right)) \implies (\operatorname{norm}\left(\operatorname{phi}\left(a, head, r\right)\right) = 1)) \land (\operatorname{gram}\left(\operatorname{fun}\left(r: \operatorname{Box}\left(a\right), \operatorname{phi}\left(a, head, \operatorname{occ}\left(a, r\right)\right)\right)\right) = \operatorname{G}\left(a, head\right)) \land (\forall r: \operatorname{Multiset}\left(sigma\right), (\operatorname{le}\left(r, a\right)) \implies (\operatorname{inner}\left(\operatorname{phi}\left(a, head, r\right), \operatorname{phi}\left(a, head, 0\right)\right) = \operatorname{z}\left(head, r\right))) \land (\operatorname{PosSemidef}\left(\operatorname{G}\left(a, head\right)\right)) \land (\operatorname{PosSemidef}\left(\operatorname{B}\left(a, head\right)\right)) \land (\operatorname{mul}\left(\operatorname{mul}\left(\operatorname{D}\left(a\right), \operatorname{G}\left(a, head\right)\right), \operatorname{D}\left(a\right)\right) = \operatorname{B}\left(a, head\right)) \land (\operatorname{entry}\left(\operatorname{B}\left(a, head\right), 0, 0\right) = 1) \land (\operatorname{rank}\left(\operatorname{G}\left(a, head\right)\right) = \operatorname{N}\left(a\right)) \land (\operatorname{rank}\left(\operatorname{B}\left(a, head\right)\right) = \operatorname{N}\left(a\right)) \land (\operatorname{rank}\left(\operatorname{B}\left(a, head\right)\right) = \operatorname{add}\left(1, \operatorname{mul}\left(\operatorname{NatSub}\left(\operatorname{card}\left(\operatorname{Tail}\left(a, head\right)\right), 1\right), \operatorname{add}\left(\operatorname{count}\left(a, head\right), 1\right)\right)\right)) \land (\forall r: \operatorname{Box}\left(a\right), s: \operatorname{Box}\left(a\right), (((\operatorname{occ}\left(a, r\right) \neq 0) \land (\operatorname{occ}\left(a, s\right) \neq 0))) \implies (\operatorname{entry}\left(\operatorname{B}\left(a, head\right), r, s\right) = \operatorname{sum}\left(\operatorname{fun}\left(i, \operatorname{if}\left(((\operatorname{lt}\left(0, \operatorname{val}\left(\operatorname{apply}\left(r, i\right)\right)\right)) \land (\operatorname{lt}\left(0, \operatorname{val}\left(\operatorname{apply}\left(s, i\right)\right)\right))), \operatorname{entry}\left(\operatorname{B}\left(a, head\right), \operatorname{lower}\left(r, i\right), \operatorname{lower}\left(s, i\right)\right), 0\right)\right)\right))) \land (\forall r: \operatorname{Multiset}\left(sigma\right), s: \operatorname{Multiset}\left(sigma\right), (((\operatorname{le}\left(r, a\right)) \land (\operatorname{le}\left(s, a\right)))) \implies (\operatorname{inner}\left(\operatorname{image}\left(a, head, r\right), \operatorname{image}\left(a, head, s\right)\right) = \operatorname{inner}\left(\operatorname{phi}\left(a, head, r\right), \operatorname{phi}\left(a, head, s\right)\right))) \land (\forall J: Type, (\operatorname{Fintype}\left(J\right)) \implies (\forall rho: J \to \operatorname{Multiset}\left(sigma\right), c: J \to Complex, (((\forall j: J, \operatorname{le}\left(\operatorname{apply}\left(rho, j\right), a\right)) \land (\operatorname{sum}\left(\operatorname{fun}\left(j, \operatorname{smul}\left(\operatorname{apply}\left(c, j\right), \operatorname{phi}\left(a, head, \operatorname{apply}\left(rho, j\right)\right)\right)\right)\right) = 0))) \implies (\operatorname{sum}\left(\operatorname{fun}\left(j, \operatorname{smul}\left(\operatorname{apply}\left(c, j\right), \operatorname{image}\left(a, head, \operatorname{apply}\left(rho, j\right)\right)\right)\right)\right) = 0))) \land ((\operatorname{lt}\left(0, \operatorname{count}\left(a, head\right)\right)) \implies (((\operatorname{phi}\left(a, head, 0\right) = \operatorname{phi}\left(a, head, \operatorname{replicate}\left(1, head\right)\right)) \land (\operatorname{span}\left(Complex, \operatorname{setOf}\left(x: \operatorname{Space}\left(\operatorname{K}\left(a, head\right)\right), \exists r: \operatorname{Multiset}\left(sigma\right), ((\operatorname{le}\left(r, a\right)) \land (r \neq 0) \land (x = \operatorname{phi}\left(a, head, r\right)))\right)\right) = top)))) \land ((a = 0) \implies (((\operatorname{N}\left(a\right) = 1) \land (\operatorname{Nonempty}\left(\operatorname{LinearIsometryEquiv}\left(Complex, \operatorname{Space}\left(\operatorname{K}\left(a, head\right)\right), Complex\right)\right))))) \land (\exists e: \operatorname{Equiv}\left(\operatorname{K}\left(a, head\right), \operatorname{Fin}\left(\operatorname{N}\left(a\right)\right)\right), \exists V: \operatorname{LinearIsometry}\left(Complex, \operatorname{Space}\left(\operatorname{Fin}\left(\operatorname{N}\left(a\right)\right)\right), \operatorname{TensorProduct}\left(Complex, \operatorname{Space}\left(sigma\right), \operatorname{Space}\left(\operatorname{Fin}\left(\operatorname{N}\left(a\right)\right)\right)\right)\right), \exists U: \operatorname{Unitary}\left(\operatorname{Product}\left(sigma, \operatorname{Fin}\left(\operatorname{N}\left(a\right)\right)\right)\right), ((V = \operatorname{comp}\left(\operatorname{tensorCoordinates}\left(\operatorname{N}\left(a\right)\right), \operatorname{emissionCoordinates}\left(a, head, e\right)\right)) \land (\operatorname{gram}\left(\operatorname{fun}\left(r: \operatorname{Box}\left(a\right), \operatorname{phiFin}\left(a, head, e, \operatorname{occ}\left(a, r\right)\right)\right)\right) = \operatorname{G}\left(a, head\right)) \land ((\operatorname{lt}\left(0, \operatorname{count}\left(a, head\right)\right)) \implies (((\operatorname{phiFin}\left(a, head, e, 0\right) = \operatorname{phiFin}\left(a, head, e, \operatorname{replicate}\left(1, head\right)\right)) \land (\operatorname{span}\left(Complex, \operatorname{setOf}\left(x: \operatorname{Space}\left(\operatorname{Fin}\left(\operatorname{N}\left(a\right)\right)\right), \exists r: \operatorname{Multiset}\left(sigma\right), ((\operatorname{le}\left(r, a\right)) \land (r \neq 0) \land (x = \operatorname{phiFin}\left(a, head, e, r\right)))\right)\right) = top)))) \land (\forall x: \operatorname{Space}\left(\operatorname{Fin}\left(\operatorname{N}\left(a\right)\right)\right), \operatorname{apply}\left(V, x\right) = \operatorname{apply}\left(\operatorname{tensorCoordinates}\left(\operatorname{N}\left(a\right)\right), \operatorname{apply}\left(U, \operatorname{apply}\left(\operatorname{blankEmbed}\left(\operatorname{N}\left(a\right), head\right), x\right)\right)\right)) \land (\forall r: \operatorname{Multiset}\left(sigma\right), (((\operatorname{le}\left(r, a\right)) \land (r \neq 0))) \implies (\operatorname{apply}\left(V, \operatorname{phiFin}\left(a, head, e, r\right)\right) = \operatorname{sum}\left(\operatorname{fun}\left(i, \operatorname{smul}\left(\operatorname{sqrt}\left(\operatorname{div}\left(\operatorname{count}\left(r, i\right), \operatorname{card}\left(r\right)\right)\right), \operatorname{tmul}\left(\operatorname{basis}\left(i\right), \operatorname{phiFin}\left(a, head, e, \operatorname{erase}\left(r, i\right)\right)\right)\right)\right)\right))) \land (\operatorname{apply}\left(V, \operatorname{phiFin}\left(a, head, e, 0\right)\right) = \operatorname{tmul}\left(\operatorname{basis}\left(head\right), \operatorname{phiFin}\left(a, head, e, 0\right)\right)) \land (\operatorname{norm}\left(\operatorname{phiFin}\left(a, head, e, a\right)\right) = 1) \land (\operatorname{norm}\left(\operatorname{phiFin}\left(a, head, e, 0\right)\right) = 1) \land (\forall w: \operatorname{Fin}\left(\operatorname{card}\left(a\right)\right) \to sigma, k: \operatorname{Fin}\left(\operatorname{N}\left(a\right)\right), \operatorname{circuit}\left(\operatorname{const}\left(U\right), \operatorname{card}\left(a\right), 0, \operatorname{initialized}\left(head, \operatorname{card}\left(a\right), \operatorname{phiFin}\left(a, head, e, a\right)\right), \operatorname{pair}\left(w, k\right)\right) = \operatorname{mul}\left(\operatorname{if}\left(\operatorname{occupation}\left(w\right) = a, \operatorname{inv}\left(\operatorname{sqrt}\left(\operatorname{M}\left(a\right)\right)\right), 0\right), \operatorname{coordinate}\left(\operatorname{phiFin}\left(a, head, e, 0\right), k\right)\right)))))))$$

*Proof.* Machine-checked in Lean as `D5/S3/Quantum/StationaryPreparation/StationaryOccupationAttainment.stationary_attainment_full` (`✓ std3`). ∎

*Source.* Repository-derived.

*Commentary.*

Let sigma be any finite nonempty alphabet, let a be any multiset on sigma, and choose head with a.count(head) equal to the supremum of the counts. Write A for this maximum. Arbitrary natural capacities c are represented by the sum of c(i) copies of each i: this multiset has count c(i) at i and cardinality equal to the sum of c(i). No capacity is required to be positive. Box(a) consists of bounded counts r(i) in Fin(a.count(i)+1), and occ(r) is their multiset. M(r) counts the words with occupation r and equals r.card! divided by the product of r.count(i)! over the alphabet.

Tail removes every copy of head. Set z(r)=1 when the tail of r is empty and z(r)=0 otherwise. Thus z(0)=1, z(h copies of head)=1 for 1<=h<=A, and every legal residual with nonempty tail has z(r)=0. The map from a bounded residual to its tail counts and head count is bijective. For a fixed tail b, put m(j)=M(j copies of head plus b) and delta(0)=m(0), delta(j)=m(j)-m(j-1) for j>0. The block B_b(j,k) is m(min(j,k)). Entries between distinct tail blocks are zero.

Let L have ones on and below its diagonal and zeros elsewhere. Each block is exactly L diag(delta(0),...,delta(A)) L transpose. For the zero tail, m(0)=1 and the increments are 1 at zero and 0 at every positive index, so the block has rank 1. For every nonzero tail all increments are positive and the block has full rank A+1. For 1<=j<=A, the real ratio m(j)/m(j-1) equals (j+card(b))/j, and exceeds 1 when b is nonzero.

The memory coordinates are one sink together with pairs (b,j), where b is a nonzero bounded tail and j lies in Fin(A+1). For a legal residual r with empty tail, padding(r) is the sink basis vector. For a nonempty tail b, its (b,j) coordinate is the square root of lastTail(j head+b) when j<=r.count(head), and all other coordinates vanish. Here lastTail(s) is card(tail(s))*M(s)/card(s), with real division. These weights equal delta(j) for every nonzero tail. Hence the actual padding vectors have exactly the displayed blocks, including their off-diagonal entries.

Normalize padding(r) by 1/sqrt(M(r)) to obtain phi(r). Every legal phi(r) has norm 1. Its Gram matrix is G: if s<=r, the entry is sqrt(M(s)*M(r-s)/M(r))*z(r-s); if r<=s instead, use the complex conjugate of the reversed expression; incomparable pairs have entry zero. The inner product of phi(r) with phi(0) equals z(r). With D diagonal and D(r,r)=sqrt(M(r)), the exact congruence D G D=B holds. Both matrices are positive semidefinite, B(0,0)=1, and their common rank is the product of a.count(i)+1 minus A. This equals the memory cardinality and also 1+(card(Tail)-1)*(A+1).

For two nonzero residuals r and s, B(r,s) is the sum over letters i of B(r-e(i),s-e(i)), retaining a term only when both counts at i are positive. Here lower(r,i) subtracts one at coordinate i and leaves other coordinates unchanged, using natural truncated subtraction within the same box. Prescribe image(0)=head tensor phi(0), and for nonzero r prescribe image(r) as the sum of sqrt(r.count(i)/r.card) times i tensor phi(r.erase(i)). For every pair of legal residuals the prescribed images preserve their inner product. Consequently, for every finite index type J, every complex coefficient family c, and every legal residual family r, a zero sum of c(j)*phi(r(j)) gives a zero sum of c(j)*image(r(j)). This establishes preservation of all linear relations before constructing V.

The padding transition emits head from the sink with probability one. For a state (b,h) with tail mass R>=2, its head probability is h/(h+R-1), and its probability for tail letter i is b(i)*(R-1)/(R*(h+R-1)). A head emission lowers h, and a tail emission lowers b(i). At R=1, positive h emits head with probability one; at h=0 the sole tail letter leads to the sink. Absent letters have probability zero. Under nonzero support, the emitted letter and successor recover the entire predecessor. The probabilities sum to one, so the weighted transition matrix W satisfies W conjugate-transpose times W=1. Its action on padding(r) in letter i is padding(r.erase(i)) for present i, and zero for absent i.

If A>0 then phi(0)=phi(one copy of head), and the nonterminal residuals already span the whole memory. Choose a coordinate equivalence with Fin(N), where N is the rank above. There exist one isometry V from this memory to alphabet tensor the same memory, and one unitary U on the alphabet-memory register. The identity V=tensorCoordinates composed with emissionCoordinates(a,head,e) fixes V to the coordinate transport of W. The action of U on blank head input agrees with V under the tensor coordinate isometry. V obeys the prescribed transition at every legal nonzero residual and sends phi(0) to head tensor phi(0). The coordinate transport preserves G and the nonterminal span.

Initialize the memory at phi(a), blank all output registers with head, and use this same U at every step. For every word of length a.card and every final memory coordinate k, the circuit coefficient is phi(0)(k)/sqrt(M(a)) when the occupation is a and is zero otherwise. Both the initial vector and common final vector phi(0) are unit vectors. There is no additional memory or time-dependent control. When a=0, N=1 and the memory is linearly isometric to the complex numbers; the output statement includes the zero-length circuit.

## References

- Truth anchor: `D5/S3/Quantum/StationaryPreparation/StationaryOccupationAttainment.stationary_attainment_full`
- Dependency: [D5/S3/Quantum/Algebra/StationaryGramRank](../Algebra/StationaryGramRank.md)
- Dependency: [D5/S3/Quantum/StationaryPreparation/PaddingResidualAction](PaddingResidualAction.md)
- Dependency: [D5/S3/Quantum/StationaryPreparation/PaddingResidualGram](PaddingResidualGram.md)
- Dependency: [D5/S3/Quantum/StationaryPreparation/PaddingTransition](PaddingTransition.md)
