using static StrataLint.Scribe.DefinitionDsl;
using static StrataLint.Scribe.FormulaDsl;
using F = StrataLint.Scribe.FormulaDsl;

namespace StrataLint.Scribe.Blueprint.D5.S3.PrimeForms.PellFamilies;

internal sealed class CrossingPellFamilyDocument : IScribeDocumentDefinition
{
    public DocumentDefinition Create() => DocumentDefinition.Create(ScribeNode.Create(
        "The crossing recurrence is an explicit family on the Pell conic of discriminant 48.",
        H("The Crossing Pell Family"),
        Blocks(
            Describe.Lean(
                DescribeId.Create("crossing-family-pell-invariant"),
                DeclarationHandle.Create(
                    "D5/S3/PrimeForms/PellFamilies/CrossingPellFamily.crossing_pell_invariant"),
                H("The crossing recurrence preserves its Pell invariant"),
                StatementSource.FromAuthor(Disp(Seq(
                    Forall, Sp, F.Id("n"), Sp, InMacro, Sp, Mathbb, Grp(F.Id("N")), Comma, Sp,
                    Open, D(2), F.Id("A"), Underscore, F.Id("n"), Plus, D(1), Close,
                    Caret, D(2), Minus, D(4), D(8),
                    F.Id("j"), Underscore, F.Id("n"), Caret, D(2), Eq, D(1)))),
                AssessedProvenance.FromRepo(),
                Blocks(
                    Paragraph(Text(
                        "Let A 0 = 3 and j 0 = 1. Recursively set A (n+1) = 7 A n + 24 j n + 3 "
                        + "and j (n+1) = 2 A n + 7 j n + 1. The resulting pairs begin "
                        + "(3,1), (48,14), and (675,195). Every pair satisfies "
                        + "(2 A n + 1)^2 - 48 (j n)^2 = 1.")),
                    Paragraph(Text(
                        "Pinned Mathlib already defines the Pell sequence generated by seven and "
                        + "proves its universal Pell equation as Pell.pell_eqz. The Lean proof "
                        + "identifies these crossing coordinates with that sequence and applies "
                        + "the "
                        + "library theorem directly; it does not reprove the Pell invariant.")),
                    Paragraph(Text(
                        "This closes only the displayed Pell-family clause of appendix E.56. "
                        + "It does not claim the constant word, trace formula, norm realization, "
                        + "crossing-form identities, no-smaller-leg lemma, or spectral "
                        + "accumulation "
                        + "limit from the "
                        + "rest of the source atom."))),
                DescribeRole.Theorem))));
}
