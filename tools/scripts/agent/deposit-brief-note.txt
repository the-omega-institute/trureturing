- **本 note 不含任何题目信息** —— 题目**只由上面的 `ATOM_ID` 指定**,
  你必须用 `make show-atom` 读正文,**以正文为唯一真源**。
  (2026-09-01 教训:orchestrator 批量派题时复用了别的题的 note,
   导致 brief 描述与 atom 正文不符,席位正确 blocked。故本 note 一律不写题目内容。)
  **若你发现 brief 任何一处描述与 `show-atom` 正文不符,以正文为准并在 conclusion 里写明分歧。**

- **查重(责任在你,走完 cover 查重五路)**:
  · 账目侧:用 `make show-atom ATOM_ID=<atom>` 读取当前 coverage GID 派生视图;
  · Lean 侧:按正文的数学对象取关键词搜 `D5/**/*.lean`;
  · **同名 0 命中不等于没被覆盖** —— 本轮多次实测异名覆盖
    (`Nerode` 0 命中却被 `ReachableBehaviorClassSurjectivity` 覆盖;
     `bulk-source` 被 `InteriorCurvatureCriterion` 覆盖)。
  · **先读落点目录下的既有模块全文**再动手。
  · 若判定已被覆盖,**blocked 并报告**,不要制造第二真源(第 6 条)。

- **落点必须用已有域**:先 `git ls-tree origin/dev --name-only D5/<S层>/` 看有哪些域,
  **严禁新建 `D5/<S层>/<新域>/`** —— 新域须注册进 `Meta/domains.yaml`,
  而该文件字节被 `ValidatedPolicy` 钉死(`CanonicalSnapshot.cs:65-70`),
  候选侧一改就 `INFRASTRUCTURE_FAILURE`。本轮已有一条 lane 因此整条作废。

- **自足性判断权在你**:通读全文后,
  ①把不依赖 ξ/RH/mathlib 缺失大定理的部分作为**主定理,完整证出**;
  ②外部依赖**参数化**并列进 `added_hypotheses`,说明为何是忠实抽象;
  ③若整条都不自足(关键量无定义、或只是叙述无可证断言),
  **blocked 并精确列出缺哪些定义** —— 这是合法且有价值的结果,本轮已有多条如此正确入账。
  **不要硬凑,不要弱化陈述冒充完成。**

- **静默错误陷阱(本仓反复付过学费,逐条自查)**:
  Lean 的全函数化会把数学错误变成**编译绿的假命题**——
  · 除以 0 得 0;`Real.log` 对非正数返回 0;`Real.sqrt` 对负数返回 0;`sInf ∅ = 0`;
  · 故**凡出现分母、对数、开方、下确界,必须显式假设正性/非空**,否则命题在退化点悄悄变平凡真;
  · 下标偏移(0-based vs 1-based)、严格/非严格配对(`<` vs `≤`)、
    量词方向(`∀T` vs `∃T`)——**写错照样编译**,先用具体数值手算验证再写一般证明。

- **非空见证(必须给,两侧)**:
  一个具体实例使结论成立、一个使前提被破坏而结论不成立;
  **两侧都要真算出数值**。只给单侧无法排除「命题空洞成立」。

- **Scribe 硬要求(现役门)**:`DescribeRole.{Theorem,Proposition,Lemma}` 节点
  **必须用 `StatementSource.FromAuthor(<Formula>)`**,不得用 `WithoutFormula()`
  (判据 `DescribeMigrationTests.cs:123-130`;本轮已因此污染 dev 三次)。
  公式须忠实反映该 Lean 定理的**结论**。

- **不许 sorry、不许私 axiom、不许弱化陈述冒充完成**(第 4 条:账上没有的不冒领)。

- **面集合封闭(deposit 只许动这四类,别的一个文件都不要新增)**:
  `D5/<GID 路径>.lean` · `Blueprint/<同一 GID 路径>.scribe.cs` ·
  `Blueprint/<同一 GID 路径>.md` · `Golden/Frozen/{accepted,state}/**`
  (cover 另加同一 atom 的 `Meta/Digestion/backfill/**` 条目;
   文献注 `Library/<已注册域>/*.md` 与 `Problems/*.md` 若正文需要则可带。)
  **实施报告写进 conclusion 或你自己的 run 目录,不要入库。**
  (2026-09-10 教训:一个席位把实施报告写进 `docs/reports/`,该目录当时已有
   **72 个文件**,而 SL-003 的**准入**上限是 48(仓库容忍 96)——目录早已处在
   「容忍但不可准入」的灰区,**任何 lane 只要碰它一下就判红**。判词逐字:
   `SL-003 docs/reports: directory contains 49 files (admission limit 48,
   repository tolerance 96; split per CLAUDE.md 8)`。
   该文件同时越出上列面集合,是双重越界;剔除后三门即绿。)

- **新增文件前先量它所在目录**:`git ls-tree --name-only origin/dev <dir>/ | wc -l`,
  与 SL-003 的**准入上限**比(上限由属主派生,别抄常数:
  `grep -oE 'DirectoryFileLimit[[:space:]]*=[[:space:]]*[0-9]+' \
   tools/StrataLint.Engine/Rules/RepositoryRules.Structure.cs`)。
  **接近或超过就不要往那儿加**,报告里说明并另择已有余量的目录;
  **不要自行裂别人的桶** —— 那是独立的一层改造,不搭在 deposit lane 上。
