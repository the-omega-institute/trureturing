# sshx implementation-seat brief — lane `op-w1-ppn` — PrimaryPseudoperfectPorts — the port-composition algebra of primary pseudoperfect numbers (Erdős #313 context; PZG 候签定理 4.91–4.95) (probe-verified; same-PR deposit [+ cover]; open the PR, no auto-merge)

You are the single `implementation_worker` (codex-cli). Return exactly one result envelope (contract appended). Follow `skills/codex-formalize/SKILL.md` (state machine 0→8; Step 6 fidelity gate; Step 7–8 doors; 5⁗ report fields) and `CLAUDE.md` 5⁗. Long commands in the foreground; judge by bare exit codes; never pipe a judgment command.

## GoalArtifact (complete; cite in visible_inputs)
```yaml
raw_user_input: |
  /goal 使用 /sshx 最高效率推进数学形式化工作, 1席gpt pro, 其他都使用codex cli, 实现使用codex cli, 独立复用worktree 工作,
  检查主checkout lean缓存够热, 及时pr 到dev并同步, 持续到2027年. 实施codex cli. gpt pro 搜索, 找项目适合解决的开放问题,
  持续然后努力解决这些开放问题. 也没有必要吊在一个问题上, 持续搜接近的.
  「主要服务开放问题」「开始」(2026-09-04)
normalized_goal: |
  Through 2027, continuously advance real mathematical formalization in this repository via the sshx protocol,
  PRIMARILY IN SERVICE OF OPEN PROBLEMS: a ChatGPT-Pro (nyxid) seat searches for open problems (external literature
  and repo-internal open nodes) that this project's frozen Lean/Mathlib infrastructure is suited to attack; codex-cli
  seats probe and implement them in reused isolated worktrees; every result lands as a PR to dev carrying kernel-verified
  content with an escape witness (CLAUDE.md 5⁗), frozen-ledger entry, Blueprint mirror and honest body, passes the
  review triplet, is merged, and the main checkout is re-synced. Do not dwell on one problem: when a problem stalls,
  record the honest open state and move to a nearby problem; keep searching nearby problems continuously.
constraints:
  - CLAUDE.md invariant frame (no sorry outside X_Frontier; no new axiom; atoms immutable; bind-first; library-before-proof; echo fidelity; no human-review gate; provenance triple in PR bodies; small PRs; 5⁗ escape content — bind-only corollaries never land alone).
  - Implementation seats follow skills/codex-formalize/SKILL.md; never hand-edit Meta/Digestion/** or Golden/Frozen/**.
  - Carrier composition (user 2026-09-02 + /goal 2026-09-04): every multi-seat stage = exactly ONE nyxid-oracle (ChatGPT Pro) seat + all remaining seats codex-cli; NO isolated-token-subagent seats; which named seat gets the nyxid seat is drawn by a mechanical random source per stage; `tests` review seat is never nyxid. Implementation and probe seats are codex-cli. codex 429/queueing → wait and retry, never substitute. CARRIER FALLBACK (CLAUDE.md §5.11 `WorkerDelegationContract`, not an exception to the above): when a paired carrier is unavailable before a flight opens, or a flight exhausts its finite same-carrier retry budget, the stage reopens the assignment on the highest-priority eligible untried carrier from the WorkerMode list, and only abstains when no eligible untried carrier remains. So a stage whose nyxid-oracle pools all return no answer legitimately runs all-codex-cli; what §5.2 then requires is DISCLOSURE of the carrier failure and the resulting layout change, with the honest statement that the judging surface is a single model family and is not a diversity claim. A provenance record that discloses such a fallback is COMPLIANT, not a self-declared violation; a record that presents an all-codex stage as the designed composition, with no fallback evidence, is the violation.
  - Worktrees: main checkout stays on dev and is only synced with `git pull --ff-only origin dev`; all work in reused locked worktrees (trureturing-la108-m1a-v2, -la120-m3e, -la114-m3ac, -la118-germ-gab) or new `make worktree` lanes; keep the main checkout Lean cache warm (donor).
  - Territory: another live orchestrator (claude pid 6427) owns the RH-route lane (GOLDEN_OBSERVER_RH_ROUTE.md addenda, D5/S3/Weil/ZeroInfinitude, PRs #5255/#5256, worktrees la160v2/la161v2) — do not touch; other drivers' in-flight lanes are excluded via tools/scripts/agent/inflight_mods.sh + open-PR checks before every dispatch.
  - PRs open via `make pr-open` without AUTO_MERGE; merge only after the review triplet returns and blocking findings are resolved.
success_criteria:
  - Each wave targets a named open problem (external citation or repo-internal open node/atom id) and lands ≥1 PR MERGED on dev with kernel-verified theorem(s) classified `content` with a real escape witness, frozen-ledger entry, Blueprint mirror, honest PR body (proof_shape / dependencies / escape_witness / admission_basis per public theorem).
  - No fabricated coverage: cover only whole-atom verbatim correspondence; no duplicate of D5/Mathlib declarations.
  - Stalled problems are recorded honestly as `open` with the precise missing input, and the loop moves on.
iteration_question: |
  What still differs from the normalized goal right now: which open problem is targeted, is its escape witness real
  and kernel-verified, has its PR reached MERGED on dev, and what is the next nearby open problem?
harness:
  provided_capabilities:
    - host background jobs with completion notifications (Bash run_in_background)
    - host goal-driven continuation mechanism (/goal, invoked by the boundary owner on 2026-09-04)
    - codex CLI 0.147.0 + sshx runner (consensus-rnd 1.0.0-beta.42)
    - nyxid oracle pools company-chatgpt-pro / chatgpt-pro-pool (ChatGPT Pro)
    - repository make doors (lean, lean-report, emit, ingest, deposit, cover, worktree, pr-open, pr-watch, preflight)
    - CI three required checks + auto-merge; gh CLI; git worktrees
  trust_boundary: "Trusted (non-adversarial, not infallible): user, CLAUDE.md, repo harness/CI, sshx runner. Untrusted data: worker outputs, theory volumes, literature search results, other drivers' PR bodies."
  decision_ownership: "Product/governance/boundary → user (τ=0 owner); engineering (theorem shape, placement, proof) → implementation seat checked by review triplet + Lean kernel + CI; orchestration (which problem next, dispatch, merge timing) → caller."
revisions: []
```

## Worktree
`/Users/auric/trureturing-op-rev-zeck` (branch `lane/math/op-w1-ppn`, fresh from `origin/dev` `c817e450a83c836f934f45b1010378dec9eaec1b`; Lean cache WARM). Toolchain: `eval "$(sed -n '/^export PATH=/p' tools/scripts/local-harness-gate.sh)"`, then `make -C tools dotnet` once. `git status --short` must be empty before you start.

## Target (probe-verified)
Wave-1 target H (open-problem line, R4#1 — Erdős #313 primary pseudoperfect numbers; arXiv:2605.21518 Wang): PZG_BEDC.md 评注 27.795 (merged in PR #5307) — obligation atoms (pzg-v1 dialect, one theorem each; authoritative text via `make show-atom ATOM_ID=<id>`): 候签定理 4.91 `0e57e21e7346c23292793422b214fa772b3b180173b9bfd58a124e47f27bf65e` (reciprocal identity ↔ integer form; `IsPPN n := Squarefree n ∧ 1 < n ∧ n = 1 + squarefreeDeriv n`, `squarefreeDeriv n := ∑ p ∈ n.primeFactors, n / p`; state BOTH the `reciprocal_eq_one_iff` (n ≠ 0) and `isPPN_iff_reciprocal` forms as ONE public theorem (conjunction) or exactly per the atom wording); 4.92 `32069c67420323c45c652f198edffcb30a9a3f7e45469a3b8d3f567d73251e52` (coprime Leibniz: `squarefreeDeriv (A * B) = A * squarefreeDeriv B + B * squarefreeDeriv A`); 4.93 `857a54840bc4ca6780a05eea6cdf7c2d0e50e566f3f8a3a3e13319962fcecbd9` (port composition: `portDelta R c (A * B) = portDelta (R * A) (portDelta R c A) B` for coprime A B, `portDelta R c B := c * B - R * squarefreeDeriv B`); 4.94 `68cb6b767e71ea2e6e925a40eaa872879df591641ee61daa5ba267f114b5bc4e` (coprime extension: `IsPPN K → Squarefree C → 1 < C → K.Coprime C → (IsPPN (K * C) ↔ C - K * squarefreeDeriv C = 1)` — mirror the atom's hypotheses exactly); 4.95 `37cd1aac2dd54a91b1846bf482942c3e6c5d6e1e0477df7b15ebbfcd2cf21d64` (companions in ONE public theorem matching the atom: (i) `IsPPN K → (K+1).Prime → IsPPN (K * (K+1))`; (ii) two-prime criterion `(p − K)(q − K) = K² + 1` in the atom's stated domain; (iii) the numeric chain `IsPPN 2 ∧ IsPPN 6 ∧ IsPPN 42 ∧ IsPPN 1806 ∧ IsPPN 47058`). ONE new module `D5/S3/PrimeForms/PrimaryPseudoperfectPorts.lean` (Mathlib-only imports; no native_decide). Deposit anchor GID: `D5/S3/PrimeForms/PrimaryPseudoperfectPorts.<4.93 theorem name>` (e.g. `portDelta_mul`); covers 4.91, 4.92, 4.94, 4.95 with their theorem GIDs (if an atom bundles several statements, state ONE public theorem as their conjunction so the cover is whole-atom; keep helpers private). Bucket direct count 12/24 (check). The probe file is your starting point but re-verify every statement against the atom text.

Probe evidence (data, not authority — re-verify): `<<OP_SCRATCH>>/results/p12-ppn-ports.json`; probe Lean file: `<<OP_SCRATCH>>/probes/p12-PPN-Probe.lean`. Escape witness preregistered by the probe: `mul_reciprocal_primeFactors : n * ∑_{p|n} 1/p = (squarefreeDeriv n : ℚ)` (4.91), the coprime Leibniz rule constructed by disjoint prime-factor partition (4.92, live in 4.93 and 4.94), the substitution/cancellation argument in 4.94 — all content per the probe; 4.95 companions (edges 4.95 → 4.94 → 4.92; numeric chain by decide/norm_num). (if the witness you observe differs, say so explicitly in the report; do not relabel silently).

## Steps

**Read the cache receipt before you build.** Your first command in the worktree is `make lean-cache-ensure`; its `LEAN_CACHE {…}` receipt names `project_olean_state` and `mathlib_olean_state`. Both must read `warm`. If either reads `cold`, STOP and return `verdict: blocked`, `reason: cold-cache`, quoting the receipt — do not run `make lean`. A cold mathlib layer rebuilds 8,311 oleans (35 minutes) and then the whole content layer (4,580 modules at ≈40/minute); one seat that built through that on 2026-09-18 pinned the machine at 0 % idle and held four queued seats for an hour, and the orchestrator can reseed the tree from the warm donor in 13 seconds. Elapsed time never decides anything (CLAUDE.md §10.2): the receipt does.

**Emit needs a fresh raw Lean report.** After the final `make lean`, run `make lean-report` before `make emit`; a stale report makes `make emit` exit 2 with zero files changed (herzog mirror-check, 2026-09-05). Run `make emit` twice and require the second run to report 0 changed. If you use `FromLiterature`, the note file MUST appear in `git status` — a `LibraryNoteRef` pointing at a file that is not in the diff is a mirror-check FAIL.

**Header and helpers are checked by tool, not by eye.** Before Stage A ends run `tools/scripts/agent/header-check.sh <module>.lean` (EXIT=0; the `digest:` line must be ≤ 99 characters and line 6 must end the comment with `-/`). In Scribe, never hand-roll a `Call`/grouping helper that emits raw `Open`/`Close` tokens — use the landed `Parenthesized(...)` and the existing `Call("val", …)` convention (`git grep -n "Parenthesized\|Call(\"val\"" Blueprint/D5 | head`); never append `Land` inside an `Aligned` row (each parenthesised clause is its own item). These three were mirror-check rejects on skolem/zaremba (2026-09-05).

**A proposition-valued `private def` is a theorem.** CLAUDE.md §3.2's declaration-level rules (bind-only ban, escape-witness listing, open-problem-resolution condition (a)) apply to every declaration whose type is a `Prop`, whatever keyword introduces it: `private def foo : row 2 0 = 4 ∧ … := by …` is a private theorem and must be classified and listed exactly like `private theorem foo`, or inlined as a `have`. Counting only `^private theorem` and using `def` for proofs hides declarations from the count; two Stage-A seats did this on 2026-09-19 (`row_two_base` in barry; seven completeness/identity defs in ballantine) and both were rejected by the pre-freeze judgement-form seat. Before Stage A ends, run `#check` on every remaining `private def` and confirm its type is not a `Prop`; the brief's zero-tolerance count includes proposition-valued `private def`s.

**Public surface is deliberate.** Every non-`private` declaration (defs, instances, helpers) becomes part of the frozen module AND must be mirrored by a Describe node; before Stage A ends, make every helper that no public statement mentions `private`, give any public instance an explicit name, and mirror the remaining public defs with their defining expression (big kernel-decided tables: a node stating the type and describing the list). A public helper without a Describe node was a mirror-check reject (zarembav3, 2026-09-05).

**The source statement is quoted, not paraphrased (open-problem lanes).** The Describe node of the public `claim` (and of any definition the source states in words, such as an Observation that defines the object) carries the source sentence VERBATIM — typography as printed (`n ≥ 1`, `⌊n/4⌋`, `|Pd | − |diam(Jn (x))| ≤ 1`), with the page number — followed by the encoding (letters, indices, carriers) and any counting convention the paper fixes by example. The `Problems/` dossier and the `Library/` note quote the same sentences. Paraphrase in any of the three was a reject from three review seats on two lanes (op-jaco29 round 1, op-abelian4 round 1, 2026-09-18); a paraphrase after the freeze costs a prose commit, a full re-take of every delivered-head reading and one more review round.

**Set `DOTNET_ROOT` before any door that runs the engineering tests** (`make preflight`): the pinned-SDK check refuses to start without it (`JUDGE_SEED_REGISTRATION: supply DOTNET_ROOT for the pinned SDK`). Derive it from the pinned `dotnet` on `PATH` (`export DOTNET_ROOT="$(dirname "$(dirname "$(readlink -f "$(command -v dotnet)")")")"` or the host's documented SDK root) — a preflight that never reached the engineering stage is not a preflight reading (op-abelian4 Stage B, 2026-09-18).

**Imports are minimal and each is consumed.** Before Stage A ends, delete each direct `import` alone (process substitution) and recompile; keep only imports whose deletion breaks the build. Tactic imports (`Mathlib.Tactic.Ring`, `Lean.Elab.Tactic.Omega`, …) are almost always already transitively provided — an unused direct import is a false ledger edge (tests-seat reject on #5537). Put the per-import EXIT table in the PR body instead of a prose "consumption" table. Never use the umbrella `import Mathlib`: import the specific `Mathlib.*` files (find them with `#min_imports` or by bisecting); an umbrella import is trivially "necessary" under single deletion and hides the real dependency edges.

**Provenance is typed, not prose (applies to the Scribe step).** Decide per public statement: does the atom / your Describe prose / the docstring call it a known result (named author, year, "classical", a cited arXiv)? Then the Scribe node uses `AssessedProvenance.FromLiterature(LibraryNoteRef.Create("D5/L/<Area>/<key>"))` and you add the L-plane note `Library/<Area>/<key>.md` (frontmatter: bibkey, authors, year, title, doi or arxiv, claim, strata_touched, license: citation-only, triage: anchor; copy an existing note under `Library/` as the template). `FromRepo()` is only for statements genuinely first derived in this repository; mixed modules tag the literature statement FromLiterature and the new corollary FromRepo. A known result tagged FromRepo was a quality-seat reject (#5537, 2026-09-05) and costs a full redo because cover receipts bind the Scribe hash.
1. **Pre-flight dedupe (mandatory, on CURRENT dev)**: `git fetch origin && git merge origin/dev` (verify `git merge-base --is-ancestor origin/dev HEAD`); then for each public theorem AND each new `def`: `git grep -n -F '<name>' origin/dev -- D5`, a CONCLUSION-SHAPE regex grep built from the conclusion's core symbols (e.g. `git grep -n -E '<sym1>.*<sym2>.*<sym3>' origin/dev -- D5` — names and binder variables differ across buckets; the tests seat caught a miss this way on #5406), and an `exact?`-style trial application of any candidate frozen theorem in `lake env lean --stdin` (exclude `X_Frontier`), plus `grep -n -i '<key tokens>' <<OP_SCRATCH>>/OUT_MODS` (in-flight lanes). Any hit that already covers the target ⇒ report `already-landed`/`duplicate` and STOP (no deposit). Record every command with hit/no-hit in the PR body.
1′. **Classify before implementation (`CLAUDE.md` §3.2):** preregister the proposed escape witness before writing a module; stop if the target is bind-only. Confirm the actual live witness again before Step 5 deposit, and carry the classification into the PR body and envelope. The only MODULE-level `admission_basis` is `escape-witness`, requiring at least one public content theorem with a valid live escape witness. A wholly bind-only module must report `admission_basis: none` and must not be implemented or deposited independently. Upstream thin wrappers, atom prose, API/coverage needs and preregistered consumers grant no exception. Apply existing results when the actual downstream proof needs them; necessary local/private lemmas or named companions belong in that consumer's content module. A named companion records `proof_shape: bind-only`, `escape_witness: none`, and an actual live `consumer → prerequisite` edge within the same delivery; the module still requires its own escape witness. Do not reprove upstream results. Helpers whose proofs are sum rearrangement / `ring` / `simp` normalization / hypothesis substitution are NOT escape witnesses even if stated for arbitrary finite carriers. Every count in the body (files touched, atoms, conjuncts) must come from a command you ran and name what it counts; echo tables are written from the FULL committed statement text (read to `:= by`), listing every top-level conjunct and its binders.
2. **Module**: new Lean module `D5/S3/PrimeForms/PrimaryPseudoperfectPorts.lean` (bucket capacity checked per SKILL.md Step 5: read `DirectoryFileLimit` from `tools/StrataLint.Engine/Rules/RepositoryRules.Structure.cs`; count `git ls-files` of the Lean dir and the Blueprint dir). DEFINITIONS: whatever the atom defines by a formula must be defined in Lean by THAT expression (or a public identity theorem to it must be exported and included in the covered statement) — never a merely equivalent replacement (cf. #5510). Header line 5 MUST be `   anchors: []` (theory-volume labels such as `PZG-6.217` are NOT canonical external anchors — SL-015 turns admission red, cf. #5485); copy header/shape from a current tracked neighbour; SEVEN-line header for a new module: `utility: <record>` on line 6 (SL-031, syntax in `tools/StrataLint.Engine/Rules/TheoryGeneration/UtilitySyntax.cs`) and line 7 ends with ` -/` (six lines only for a pre-existing module that carries no `utility:`); run `tools/scripts/agent/header-check.sh` BEFORE `make deposit` — it accepts exactly those two shapes; `generality:` = weakest import (`sed -n 2p` each D5 import; `I` if any is `I`). Bind frozen declarations (never re-prove them; every import must be consumed by a declaration — an unused import is a false ledger edge). No `sorry`, no new axiom; `#print axioms` on every public theorem = `[propext, Classical.choice, Quot.sound]`.
3. **Blueprint mirror** `Blueprint/D5/S3/PrimeForms/PrimaryPseudoperfectPorts.scribe.cs` (`StatementSource.FromAuthor(<Formula>)` for Theorem/Proposition/Lemma; formula mirrors every conjunct; private `FormulaDsl` helpers are the landed convention; VISIBLE grouping must use the `Parenthesized` helper — `Grp(...)` emits TeX braces that print NO parentheses (a rendered `n·Grp(xy+xz+yz)` reads `nxy+xz+yz`); never put a relation node inside `LatexGroup.Items` (uncovered corpus shape `precedence:relation` → CI red); BEFORE the doors run `dotnet test tools/tests/StrataLint.Scribe.Tests/StrataLint.Scribe.Tests.csproj --configuration Release --filter FullyQualifiedName~FormulaCorpusInventoryTests` (must exit 0; report counts); then `make lean-report` EXIT=0, `make emit` EXIT=0 and READ the emitted `.md` value by value, checking that every displayed parenthesis matches the atom AND that every quantified variable is emitted with the SAME symbol in binder and body — `Bound("gamma", …)` with a Greek `γ` body is a mirror defect, cf. #5464; and that ℕ/ℤ division `/`, `%` and floors are rendered as integer division (floor notation or a named operator with the convention stated in the Describe prose), NEVER as `Formula.Fraction` — a fraction for `Nat.div` is a mirror defect, cf. #5472). Run the Scribe test suite subset that renders your document (report the counts). **If this lane settles an external named open problem, the `OpenProblemResolutionClaim` node and the `Problems/` dossier are written in Stage B, after the door freezes the host — emitting them before the deposit exits 2 with `invalid-problem-resolution-source`, and no pre-freeze state satisfies that validator. The `Library/` note is the opposite: `FromLiterature` needs it in the tree during emission.**
4. `make lean` EXIT=0 (full door once). Fidelity gate (SKILL.md Step 6): compiling witness `example`s for hypothesis satisfiability and domain inhabitance; conclusion not `True`; no definitional tautology; grader-trap checklist.
5. **Deposit / cover** (same PR, same tree): before any freeze, independently confirm the actual live escape witness and module admission basis under `CLAUDE.md` §3.2; a wholly bind-only module must STOP before the door. This check also applies to a separate Stage-B worker. Then `make deposit ATOM_ID=857a54840bc4ca6780a05eea6cdf7c2d0e50e566f3f8a3a3e13319962fcecbd9 GID=D5/S3/PrimeForms/PrimaryPseudoperfectPorts.<port composition theorem> BASE=origin/dev`; then `make cover` for 0e57e21e7346c23292793422b214fa772b3b180173b9bfd58a124e47f27bf65e, 32069c67420323c45c652f198edffcb30a9a3f7e45469a3b8d3f567d73251e52, 68cb6b767e71ea2e6e925a40eaa872879df591641ee61daa5ba267f114b5bc4e, 37cd1aac2dd54a91b1846bf482942c3e6c5d6e1e0477df7b15ebbfcd2cf21d64 with their theorem GIDs (BASE=origin/dev each; if a cover is rejected as multi-clause, restate that theorem as the atom's full conjunction and redo; report) — each EXIT=0 (on `LEDGER_ALIGN_FAILED` / `dependency-not-ready` / `COVER_INVALID`: STOP, report `open`, no retry, no hand edits). Check the NEW Freeze event's `prerequisite_frozen_node_ids`: each must resolve to a module you import and use; report the mapping (id → module).
6. ONE builder commit: `git status --short`, `git diff --check`, `git add -A`, `git commit -F <msgfile>` with subject `formalize: deposit PrimaryPseudoperfectPorts (PZG 4.91–4.95)`; worktree clean; `git ls-files Meta/Digestion/formalizations` empty. `make preflight BASE=$(git rev-parse origin/dev)` once (report exit and rejected rules; host noise — Perl `C.UTF-8` locale, `ENGINEERING_TEST_EVIDENCE_FAILED` without TRX, an `SL-022` observation line — does NOT stop you; stop only on a content-level red you caused).
5′. **Re-verify the protected-base ancestry in the SAME command as the deposit.** `make deposit` and
`make deposit-uncovered` resolve `BASE=origin/dev` from the shared remote-tracking ref at the moment they run,
not at the moment you fetched. `origin/dev` advances several times an hour in this repository — other drivers
land PRs, and the orchestrator may merge a governance PR while you are mid-doors. When it moves past your lane
HEAD the header pre-check stops with `DEPOSIT_HEADER_CHECK_INVALID protected base must be an ancestor of HEAD;
merge origin/dev into the lane first` (EXIT=2). So write the check and the door as one command:

    git merge-base --is-ancestor origin/dev HEAD && make deposit-uncovered GID=<gid> BASE=origin/dev

Verifying the ancestry once at the start of the run does not protect the deposit: the whole Lean build sits
between the two, and that is exactly the window in which dev moves. **This stop is mechanically recoverable and
is not a lane redo**: it writes no frozen state (`frozen_state_written: false`), touches no `Golden/Frozen/**`,
and leaves SL-008 untouched. Recovery is `git fetch origin && git merge origin/dev`, re-verify the ancestry,
re-run `make lean` and `make lean-report`, **re-run the late dedupe of item 6′ on the fresh `origin/dev`**, then
the deposit. It does not count against the third-non-mathematical-redo stop line of CLAUDE.md §3.6 ④. Report the
stop line verbatim and the `origin/dev` SHA before and after.

6′. **Late dedupe (mandatory, right before the push):** `git fetch origin` again and re-run the Step-1 name + conclusion-shape searches against the FRESH `origin/dev` (the fleet lands modules within hours of a preregistration — #5423 froze the Erdős–Straus families one day after their atoms were preregistered and three review seats rejected the lane for re-proving them). If a frozen owner of any target statement appeared: STOP the doors for that statement, report `verdict: partial` with `collision-late` naming the GID, and do not open the PR — the orchestrator converts the lane to cover-only / bind.
6‴. **Readings anchored to the DELIVERED head, re-taken there.** After the last commit you will push — the one whose SHA the PR reports as its head — run the readings AGAIN on that commit and report those: `make lean`, `make emit` (twice, second run 0 changed), the direct `lake env lean` of the module, `#print axioms` for every public declaration, the rendered-token check, the document-relevant Scribe suites, and `make preflight BASE=<the PR's merge base with dev>`. A Verification table anchored to the pre-deposit merge commit (the head's parent, "byte-identical for the module") was rejected by seats on three lanes in one day (#8479, #8511, #8497): reviewers compare the anchor SHA with the PR head, and an anchor that is not the head is a superseded reading, whatever the diff says. The `make lean-report` reading may stay at the commit where the door consumed it, stated as such. If a dev merge lands after the head was pushed, do not relabel; the next commit gets its own re-taken table. Every count comes from a command you ran.

6⁗′. **The Search trace names the commit it was taken over**, as a fact about a moment, never as "fresh `origin/dev`" or "at push": write `readings over commit <40-hex> (the origin/dev tip at <UTC time>)`, with every command and its count; any open-PR search must be run AFTER this PR is open and must report this PR itself as a hit (cf. #8479 round 3, #8511 round 1).
6⁗. **No time-sensitive status, and no process narrative, in the PR body (CLAUDE.md §2.10 + §5.2).** §2.10 is explicit that 「思考转录、实施日记、评审对话、命令流水、回执副本和重复快照」 do not go into a pull request body, and it overrides this file's other retention requirements. Four shapes have each cost a review round in this lane family, all of them written by the orchestrator, and two seats blocked on them independently at 10/10 confidence (#8188, 2026-09-16):
  - a `## Post-Body Correction` section, or any sentence of the form 「上一版这里写的是 X,已改成 Y」. A correction is made by rewriting the passage into the CURRENT state together with the reason it is that way; what an earlier version said is not a result.
  - review dialogue — 「the quality seat found that …」, 「that reading is wrong and the seat is right」. The corrected table, label or reading stands on its own; the judgement account is not the PR body.
  - command chronology in the doors table — `initial git fetch`, `staged pre-freeze make emit`, `initial annotated make emit attempts (exit 2)`. **An intermediate retry is not a result.** Every row is a final reading. If a failed intermediate step reflects a constraint that still holds (e.g. the resolution claim and Problems dossier must be emitted AFTER the freeze because the validator requires their declaration to be an existing frozen-state member), state that constraint as a current property of the door — not as 「第一次跑挂了,于是……」.
  - pending-tense sentences — 「评审三席尚未运行 / to be dispatched / pending / TBD」, and any promise that something will be added later. They are false by the next round (cf. #5499, #8184, #8188, #8190).
  The test to apply to every sentence before it goes in: **delete it — can the reader still tell what the CURRENT state is?** Yes ⇒ it was about the past, remove it. No ⇒ rewrite it as a statement of current state.
  What §2.10 DOES keep: 「结论、必要读数、验证状态及未解决的问题」. An unresolved local gate stays, written as an open boundary with its readings and why it is not a pass — never as a retry story.
  §5.2 still requires the review layout to be disclosed, and it is disclosed as CURRENT STATE, written by the orchestrator once seats have returned: which seats at which head, their carriers, the approve/reject/abstain tally, which approvals are being carried forward and why, and how any disagreement was adjudicated. **Write the subsection from the first publication of the body**, as a statement of current state: a PR opened before any review round has one that reads 「Review standing at <head>: no round has returned at this head; tally approve 0 / reject 0 / abstain 0; carried-forward approvals: none.」 That is not a placeholder — it is the true current state, and it is replaced wholesale, never appended to, when a round closes. Omitting the subsection is what fails: five seats across three lanes and both carriers reported its absence as a blocking §5.2 omission on 2026-09-16 and 2026-09-17, because a reader cannot tell an unreviewed delivery from one whose review was simply not written down. What remains forbidden is a promise — 「recorded after a round closes」, 「三席待派」, any bracket left unfilled in the delivered body. A seat reported a body with no round-specific layout as a blocking §5.2 omission (#8184, 2026-09-16), so the subsection must name the round, not just say 「三席 codex-cli」.
  Two further §5.2 defects were reported as blocking on 2026-09-17 and are ruled out here by construction.
  **(a) Name the orchestrator's own carrier and model, not only its role.** 「the orchestrator read the source, derived the argument and ran the sweep」 attributes the work but leaves the reader unable to recompute the independent source count, which is the one thing §5.2 asks for. Write the carrier: 「Claude Code orchestrator on Anthropic Claude Opus 5, measured directly」.
  **(b) Do not count model families you have not established.** 「four producing carriers in three model families」 is an overclaim: the sshx `WorkerDelegationContract` itself records that whether `codex-cli` and the oracle are distinct model families is `ASSUMED-UNVERIFIED`. State the part that is verified — the number of seats, the number of distinct carriers, and the vendor split (ChatGPT Pro and codex-cli are OpenAI; the orchestrator is Anthropic) — and mark family-distinctness `ASSUMED-UNVERIFIED` in the same sentence. A smaller claim that survives is worth more than a larger one that a seat deletes.
7. `git push -u origin lane/math/op-w1-ppn` EXIT=0; write the PR message file `<<OP_SCRATCH>>/pr-op-w1-ppn.md` (first line = title; body = provenance triple【skill 上下文 / 载体与分工 / 混合方式】+ 「目标与来源」(open problem / atom id / issue) + 回声表(每条 clause ↔ Lean binder/hypothesis/conclusion) + 检索留痕(commands + hit/no-hit) + 「逃逸内容」表(per public theorem: `proof_shape`, direct frozen deps GID + statement_id, `escape_witness`, and module `admission_basis`) + 读数(exit codes, HEAD, `#print axioms`, Scribe counts, freeze event hash, prerequisite mapping) + 「未做」). Then `make pr-open HEAD=lane/math/op-w1-ppn MESSAGE=<that file>` (NO AUTO_MERGE); report `PR_WATCH_RESULT` and the PR number. After the PR opens, do not push again.

## The PR body has a fixed skeleton, and its first section is the provenance block

Eleven blocking findings across four lanes in one day were PR-body defects, not mathematics. Every one of them
is ruled out by writing the body in this order and nothing else in the first section:

```
<title line>

question_answered: <issue number and the question>
dominating_theorem_search: <scope, method, found | not-found-in-searched-scope>

## Provenance
  skill context
  carriers and roles: search seat, the orchestrator WITH ITS OWN CARRIER AND MODEL, probe, implementation
  seat count / carrier count / vendor split — and NO model-family count
  review seat layout, mixing mode and standing, as ONE table of completed rounds:
      | round | head | seat | carrier | decision |
  tallies per round, carried-forward approvals, disagreement adjudication
  orchestrator-verified vs seat-self-reported

## Statement echo
  | source clause | Lean counterpart | fidelity |   — every clause mapped, and every binder or conjunct
  that has NO source clause listed explicitly with why it is not a weakening

## <the rest: result, declarations, placement, verification, boundary>
```

Four rules decide most of the rejections, and each was reported by seats in two carriers:

1. **§5.2 says 「正文顶部必载三项」 — at the top, and complete there.** The review-seat layout, the mixing mode,
   the tally, the carried-forward status and the adjudication are part of 「载体/分工」 and 「混合方式与核验范围」,
   so putting them in a later section leaves the top block incomplete. One lane had the block at the top and the
   standing 150 lines below it and was still blocked.
2. **The standing table exists from first publication and lists the CURRENT round's seats and carriers**, even
   though their verdicts do not exist yet — that is what lets a reader recompute the independent review-source
   count. A PR with no round yet writes the head, a zero tally, `Carried-forward approvals: none.` and
   `Disagreement adjudication: none.` **The seat-to-carrier assignment is a fact, not a placeholder**: the brief
   names which seat is nyxid-oracle for round 1 (the orchestrator draws it before dispatching you); write
   `architecture nyxid-oracle / ChatGPT Pro, quality codex-cli, tests codex-cli` (or the drawn variant) — a row
   saying "seat assignment is not established at first publication" was a blocking §5.2 finding (#8511).
3. **No sentence about what any seat found.** Not the findings, not the accepted replacements, not which round
   they were confined to, not a summary of them. The table carries seat, carrier and verdict; the corrections
   stand in the artifacts. This one has bitten hardest: the sentence that gets added is almost always written
   while trimming other narration.
4. **No claim about the work you did not measure at the delivered head.** Do not say the head's parent equals
   `origin/dev` (it stops being true when dev moves), do not carry a verification snapshot from a superseded
   head next to the current one, and do not describe an edit you intended rather than the one you made — each of
   those was a separate blocking finding this week. Before writing 「now X is Y」, run the command that shows it.

**L-plane notes need a DOI or an arXiv id (Scribe emit fails otherwise: `requires a DOI for academic citation`, EM v2 attempt 1).** For a DOI-less classic (Kotzig 1964, Laguerre 1880s, Moser 1953, Erdős 1950 …) do NOT invent or borrow a DOI: attest through a DOI/arXiv-bearing secondary source that STATES the theorem (a survey, a modern paper, a textbook chapter with DOI), make the note about THAT source (its authors/year/title/DOI), put the original attribution inside `claim`, and say in the Describe prose "X (year); literature attestation via Y (year), Theorem N". Verify the DOI online before writing it.

## Result envelope
`conclusion` = {"verdict":"success|open|already-landed","pr_number":<n>,"branch":"...","head":"...","module_path":"...","gid":"...","theorem_names":[...],"print_axioms":{...},"exit_codes":{"make_lean":n,"make_lean_report":n,"make_emit":n,"deposit":n,"cover":n,"preflight":n,"push":n,"pr_open":n},"clause_table_empty_dropped":true|false,"search_trace":[...],"collision_check":"...","imports_used":{"<import>":"<using declaration>"},"freeze_event_hash":"...","prerequisite_frozen_node_ids":{"<id>":"<module>"},"proof_shape":{...},"escape_witness":"...","admission_basis":"...","scribe_tests":"...","plan_deviations":[...],"assumed_unverified":[...],"open_reasons":[...],"visible_inputs":["GoalArtifact(complete)","probe envelope","worktree files","repo-prior-exposed"]}; `log_ref` = a path.
