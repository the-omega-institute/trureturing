namespace StrataLint.RepositoryContract.Tests;

// Collection isolation applies within this assembly; selected projects run sequentially.
[CollectionDefinition("Lean cache environment", DisableParallelization = true)]
public sealed class ProcessBoundaryCollection;
