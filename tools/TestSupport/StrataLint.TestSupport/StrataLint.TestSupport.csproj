<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <IsPackable>false</IsPackable>
    <IsTestProject>false</IsTestProject>
    <RootNamespace>StrataLint.TestSupport</RootNamespace>
    <AssemblyName>StrataLint.TestSupport</AssemblyName>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.CodeAnalysis.BannedApiAnalyzers" PrivateAssets="all" />
    <!-- TestScratchFramework 派生自 XunitTestFramework / XunitTestFrameworkExecutor,
         二者在 xunit 元包的执行层(xunit.extensibility.execution),不在 Xunit.SkippableFact 内
         —— 迁入时由 22 个 CS0246 暴露。 -->
    <PackageReference Include="xunit" />
    <PackageReference Include="Xunit.SkippableFact" />
    <AdditionalFiles Include="$(MSBuildThisFileDirectory)../../Architecture/BannedSymbols.Determinism.txt" />
  </ItemGroup>
</Project>
