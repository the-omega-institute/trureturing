# Batch 17 Statement Echo

## Selection (worth argmax)

`origin/dev@16a33ef` contains PR #218 (`3cb3462`, GICT v3.30), batch-13 (`59318e6`), and the #272/#275 gate repair and ceremony renewal. I selected four E.24 claims that deepen the W3/three-walk frontier without repeating batch-14's conditional BHK-to-W3 rearrangement: the exact concatenation and reversal laws for the alternating continued-fraction walk, the integrality of the endpoint correction under an explicit divisibility witness, and the covariance of the W3 expression under an integral endpoint translation. This cluster has the highest available kernel-receipt potential because it isolates reusable algebraic structure behind the orientation and endpoint clauses while leaving BHK itself, the finite 1755/1755 and 440/440 certificates, the empirical orientation ledger, the actual word/column/Dedekind identifications, and the translation asymptotic unresolved. E.23, E.25, and E.26 alternatives overlap the unmerged batch-14--16 claims; old batch-12 and observer residuals have lower dependency readiness. The canonical route is `D5/S1/Phase/WalkFormula.lean`; the Phase bucket is 3/12 Lean files before admission and 4/12 after it, and the planned module remains below 400 lines.

## Claim B17-C1

- Task code: B17-C1
- Target GID: `D5/S1/Phase/WalkFormula.alternating_walk_append`
- Exact statement: for integer lists `xs` and `ys`, the alternating walk satisfies `alternatingWalk (xs ++ ys) = alternatingWalk xs + (-1) ^ xs.length * alternatingWalk ys`.
- Explicit assumptions: the walk is the recursive integer expression `a_1 - a_2 + a_3 - ...`; no continued-fraction normalization, coprimality, or orbit interpretation is assumed.
- Dependency GIDs: `D5/S1/Phase/WalkFormula.alternatingWalk`.
- Source anchors: GICT `appendix/E.24`, `cas_ref: sha256:78d3ec6ff0e52dbab3d825e82d8565844bbc3690644bc96e69e48e5f9a9ccb26`; PZG `remark/27.372-27.373`, `cas_ref: sha256:04e42dd025e34c9de8be32dae1b46277cf20cd5d19dc6178ff0d318a80bc22bb`.
- Meaning preserved: yes, as the exact compositional law for the displayed alternating column walk.
- Ambiguities requiring a new case: identifying a canonical continued fraction or word extractor with this list remains residual.
- Remark-closure guard: neither source atom is fully migrated; no finite certificate or measured clause is closed.

## Claim B17-C2

- Task code: B17-C2
- Target GID: `D5/S1/Phase/WalkFormula.alternating_walk_reverse`
- Exact statement: for every integer list `xs`, `alternatingWalk xs.reverse = (-1) ^ (xs.length + 1) * alternatingWalk xs`.
- Explicit assumptions: reversal is literal list reversal; the claim does not assert that the narrative fixed-point branch or inverse orbit is represented by that reversal.
- Dependency GIDs: B17-C1.
- Source anchors: the same GICT/PZG E.24 CAS pair as B17-C1.
- Meaning preserved: yes, as the parity-sensitive sign law needed before an orientation branch can be identified.
- Ambiguities requiring a new case: the actual orientation bit and its dependence on the fixed-point branch are not available as formal premises.
- Remark-closure guard: `orientation-branch-clause` and `w3-sign-reversal-postmortem` remain unresolved.

## Claim B17-C3

- Task code: B17-C3
- Target GID: `D5/S1/Phase/WalkFormula.endpoint_correction_is_integer`
- Exact statement: for integers `endpoint`, `endpoint'`, nonzero `c`, and integer `translation`, if `endpoint - endpoint' = c * translation`, then the rational correction `(endpoint - endpoint') / c` equals `translation`.
- Explicit assumptions: divisibility is supplied by the displayed equality and `c != 0`; it is not inferred from matrix, continued-fraction, or orbit data.
- Dependency GIDs: none beyond standard integer/rational arithmetic.
- Source anchors: the same GICT/PZG E.24 CAS pair as B17-C1.
- Meaning preserved: yes; this is exactly the endpoint-translation integrality conclusion under its disclosed structural premise.
- Ambiguities requiring a new case: proving the divisibility premise for canonical W3 endpoints remains part of the typed identification problem.
- Remark-closure guard: the asymptotic `translation approximately floor(T/gamma_0)` is measurable and remains unresolved.

## Claim B17-C4

- Task code: B17-C4
- Target GID: `D5/S1/Phase/WalkFormula.w3_walk_endpoint_translation`
- Exact statement: for rational `alt`, endpoints `endpoint endpoint'`, nonzero `c`, and integer `translation`, replacing `endpoint` by `endpoint + c * translation` increases `3 + alt + (endpoint - endpoint') / c` by exactly `translation`.
- Explicit assumptions: this is a rational algebraic covariance law; the input expression is not asserted to equal an actual word, column, or Dedekind walk.
- Dependency GIDs: none beyond standard rational arithmetic; B17-C3 supplies the integer interpretation when endpoints are integral.
- Source anchors: the same GICT/PZG E.24 CAS pair as B17-C1.
- Meaning preserved: yes, as the exact meaning of the endpoint "change" term in the W3 formula.
- Ambiguities requiring a new case: BHK, the Rademacher relation, and all three semantic walk identifications remain explicit residuals.
- Remark-closure guard: no source atom is deletable and no observation is used as proof.

## Sentence-by-Sentence CAS Echo

- GICT sentence: the Barkan/Hickerson/Knuth identity is stated with an odd-length normalized continued fraction and a modular inverse condition. No selected claim proves this theorem or those normalization premises; `bhk-continued-fraction-theorem` remains residual.
- GICT sentence: the BHK formula was checked 1755/1755. This is a finite observation and receives no Lean or remark closure; `bhk-1755-enumeration-certificate` remains residual.
- GICT sentence: the displayed W3 formula contains the alternating sum and endpoint correction. B17-C1--C4 formalize only the alternating-list algebra and endpoint correction algebra. The typed BHK-to-W3 implication is already selected by unmerged batch-14 and is not duplicated here.
- GICT sentence: the W3 formula was checked 440/440, and earlier signs were reversed. Neither historical finite certificate nor postmortem is a theorem proved here; both residuals remain.
- GICT sentence: the full-vote rule requires complete enumeration before accepting an identity. This is a governance/observation clause and remains residual.
- GICT sentence: word, column continued-fraction, and Dedekind walks coincide. The selected claims expose necessary algebra but do not provide any semantic bridge; `three-walk-identification` remains residual.
- GICT sentence: the family split and canonical Dedekind orientation depend on the fixed-point branch. B17-C2 proves only literal list-reversal parity; both semantic clauses remain residual.
- GICT sentence: endpoint change is integral. B17-C3 proves this exactly under the explicit divisibility witness; the witness for canonical endpoints is not smuggled into the theorem.
- GICT sentence: translation is asymptotic to a floor expression. This measurable statement receives no closure and remains residual.
- PZG repeats the BHK anchor, both finite counts, W3 sign history, three-walk unity, endpoint change, and orientation lesson. It omits the GICT-only family-split identity and translation asymptotic; its residual set is correspondingly smaller.

## Baseline Machine Ledger

Canonical report: `.lake/build/stratalint/raw-lean-report.json`, `sha256:4e79c43f40238f867188fe218a9e82aa84d180ca069dd9d45997fa05893dde7e`. Canonical baseline `digest-status --json --base origin/dev` has schema `stratalint-digest-status-v1`, `entries_total=1035`, and `deletable_now=0`.

- GICT E.24 `78d3ec...`: machine state `seen / residual / open / deletable=false`; its sole gap is `coverage-gid-missing`.
- PZG 27.372-27.373 `04e42d...`: machine state `seen / residual / open / deletable=false`; its sole gap is `coverage-gid-missing`.

## Planned SL-019 Residual Accounting

The parent residual `endpoint-translation-integrality` is restored to `residual / open`: B17-C3 is only a conditional implication under an explicit divisibility witness and does not discharge that parent residual. The following lists are the planned `unresolved_subitems`; final publication requires their counts and strings to equal the `unresolved-subitem` gaps emitted by `digest-status --json` atom by atom.

- GICT E.24: 11 residuals: `bhk-continued-fraction-theorem`, `bhk-1755-enumeration-certificate`, `w3-440-enumeration-certificate`, `w3-sign-reversal-postmortem`, `full-vote-enforcement-clause`, `three-walk-identification`, `dedekind-family-split-identity`, `orientation-branch-clause`, `dedekind-orientation-canonical-ledger-clause`, `endpoint-translation-integrality`, `translation-asymptotic`.
- PZG 27.372-27.373: 9 residuals: `bhk-continued-fraction-theorem`, `bhk-1755-enumeration-certificate`, `w3-440-enumeration-certificate`, `w3-sign-reversal-postmortem`, `full-vote-enforcement-clause`, `three-walk-identification`, `orientation-branch-clause`, `dedekind-orientation-canonical-ledger-clause`, `endpoint-translation-integrality`.

## Failure / Postmortem Log

- The first `Meta/StrataLint digest-status --help` attempt treated a directory as an executable and failed with `permission denied`. The canonical CLI was then located from source and built with `make dotnet`; the Release DLL produced the baseline JSON above.
- The first `route` invocation used an absolute `/tmp` manifest and failed closed because route manifests must be repository-relative. A temporary root-relative manifest was routed to `D5/S1/Phase/WalkFormula.lean` and then deleted.
- The first post-implementation RED replay still saw the baseline `Trureturing.olean`; `lake build Trureturing` rebuilt the root projection, after which the unchanged probe resolved all four GIDs.
- The first `ledger-append` attempt used a report that also contained the uncommitted root import and failed closed with `Active module Trureturing.lean changed identity`. The root import was temporarily removed, a canonical module-only report was regenerated, and the writer appended exactly one freeze. The root import was then committed and a new report drove exactly one `ledger-reattest` event.
- The first BACKFILL patch used a repeated YAML shape without atom-id context and landed on the first observer coarse atom. Diff review caught the error before digest or commit. That atom was restored byte-for-byte, and both E.24 patches were reapplied under full `atom_id`, `ast_path`, and `cas_ref` context. This repeats the batch-12 lesson: every ledger mutation must be audited by atom identity, not indentation shape.
- `origin/dev` advanced after the first complete digest: PR #271 merged batch-14, then PR #269 migrated the golden digestion corpus to CAS-backed fixtures. The stale preflight was interrupted before completion. Both revisions were merged without rebasing so frozen references remain reachable. The E.24 conflict was resolved as a monotone union: batch-14's BHK coverage plus the four batch-17 GIDs. The parent `endpoint-translation-integrality` residual is restored to `residual / open`; B17-C3 is only a conditional implication under an explicit divisibility witness and does not discharge that parent residual. The frozen suffix was relinearized from the new dev sequence 90 by the canonical writers; generated Scribe bytes were re-emitted rather than hand-merged.

## Final Kernel and Scribe Receipts

Canonical candidate report after both dev merges: `.lake/build/stratalint/raw-lean-report.json`, `sha256:3640bf7e3c389b3059dd966688bcd6331dafae78965d9dd2e7a696ebebd15c17`. The module source hash is `sha256:3e5dc47ec0e8b70372ef32e4c7783fb95ba43679edf1342cc991d8a5bebd7e2f`. `alternating_walk_append` and `alternating_walk_reverse` use only `Quot.sound` and `propext`; `endpoint_correction_is_integer` and `w3_walk_endpoint_translation` use only `Classical.choice`, `Quot.sound`, and `propext`. There is no `sorry` or extra axiom in the selected module.

The Scribe definition hash is `sha256:192029b6759bfe432fb1611b3bc9bd9aa66cc84471422f5f76e9e213e97098a2`; the emitted Markdown hash is `sha256:45c924cd5aad4e5913e506045106241a5bc34ea838aac0dcde5999ca3b0d13a3`. Both atoms carry four typed Scribe receipts. These attest the repo-derived projection and remove `scribe-receipt-missing`; they do not close any residual item.

After relinearization on batch-14's sequence 90, the canonical frozen-ledger writer appended sequence 91 for `D5/S1/Phase/WalkFormula.lean`; the canonical reattestation writer appended sequence 92 for the combined `Trureturing.lean`. No status or frozen event was written by hand.

## Final Digest-Status Reconciliation

Fresh `digest-status --json --base origin/dev` reports schema `stratalint-digest-status-v1`, `entries_total=1035`, and `deletable_now=0`.

- GICT E.24 `78d3ec...`: `seen / partial / closed / deletable=false`; machine unresolved count 11; narrative count 11; other gaps 0. Exact machine set: `bhk-1755-enumeration-certificate`, `bhk-continued-fraction-theorem`, `dedekind-family-split-identity`, `dedekind-orientation-canonical-ledger-clause`, `endpoint-translation-integrality`, `full-vote-enforcement-clause`, `orientation-branch-clause`, `three-walk-identification`, `translation-asymptotic`, `w3-440-enumeration-certificate`, `w3-sign-reversal-postmortem`.
- PZG 27.372-27.373 `04e42d...`: `seen / partial / closed / deletable=false`; machine unresolved count 9; narrative count 9; other gaps 0. Exact machine set: `bhk-1755-enumeration-certificate`, `bhk-continued-fraction-theorem`, `dedekind-orientation-canonical-ledger-clause`, `endpoint-translation-integrality`, `full-vote-enforcement-clause`, `orientation-branch-clause`, `three-walk-identification`, `w3-440-enumeration-certificate`, `w3-sign-reversal-postmortem`.

## Final Preflight

`BASE=origin/dev make preflight` ran against `origin/dev@f9d8190` and exited 2 after 1145 seconds. Engineering passed: proof restore 1s; .NET build 6s with 0 warnings and 0 errors; tests passed `798/798`, `173/173`, and `124/124` (1095/1095 total); selftest passed in 4s; both compile-fail proofs behaved as required in 1s. Gate Lean reports passed in 764s, `emit-check` passed in 16s, judge build/selftest passed, admission passed in 68s with only the expected five-path `PROTECTED_SURFACE_CHANGE`, and candidate build passed in 2s. Conservative replay alone failed after 287s with infrastructure exit 2 because its dotnet worker exceeded the 180-second process timeout. This is the batch-instructed negative-load replay case to hand to CI, not a candidate rule rejection. No PR is opened.
