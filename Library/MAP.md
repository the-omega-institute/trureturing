# Library Map

## Split history

- 2026-07-18 (SL-003): `notes/` reached 14 files when batch-10 added the
  `coffey2007theta` and `jaiswar2021identity` notes. Those two pressure-causing
  notes opened the controlled `Zeros/` bucket; all 12 paths already present
  before the overflow remain in `notes/`.
- 2026-08-06 (SL-003): `notes/` reached 13 files when the diagonal escape-count
  deposit added the `lawvere1969diagonal` note. That pressure-causing note
  opened the controlled `Diagonal/` bucket; all 12 paths already present
  before the overflow remain in `notes/`.
- 2026-08-07 (SL-003): `notes/` reached 13 files when the channel fixed-state
  deposit added the `watrous2018theory` note. That pressure-causing note opened
  the controlled `Quantum/` bucket; all 12 paths already present before the
  overflow remain in `notes/`.
- 2026-08-11 (SL-003): `notes/` was at its 12-file limit when the two-squares
  classification deposit added the `grosswald1985representations` note. That
  pressure-causing note opened the controlled `Arith/` bucket; all 12 paths
  already present before the overflow remain in `notes/`.
- 2026-08-13 (SL-003): `notes/` was at its 12-file limit when the generic
  Morse-Hedlund deposit added the `morsehedlund1940symbolic` note. That
  pressure-causing note opened the controlled `Words/` bucket; all 12 paths
  already present before the overflow remain in `notes/`.

## Buckets

- `notes/`: the original flat literature-note bucket.
- `Zeros/`: literature specific to completed zeta and zero geometry.
- `Diagonal/`: literature specific to diagonal arguments and escape counts.
- `Quantum/`: literature specific to finite-dimensional quantum information.
- `Arith/`: literature specific to classical arithmetic and quadratic
  representations.
- `Words/`: literature specific to symbolic dynamics and word combinatorics.
