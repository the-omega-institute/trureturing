---
bibkey: bala2022a000071
authors: Peter Bala
year: 2022
title: "OEIS A000071: Bala's strong divisibility conjecture for Fibonacci minus one at odd-base powers"
doi: null
url: https://oeis.org/A000071
claim: "Conjecture: for k a positive odd integer, the sequence {a(k^n): n >= 1} is a strong divisibility sequence; that is, for n, m >= 1, gcd(a(k^n), a(k^m)) = a(k^gcd(n,m))."
strata_touched:
  - D5/S1/Recurrence/BalaFibonacciPowerStrongDivisibility
license: citation-only
triage: anchor
---

# A000071: odd-base strong divisibility

Source: https://oeis.org/A000071, COMMENTS, Peter Bala, Dec 05 2022.
The entry's NAME is `a(n) = Fibonacci(n) - 1.` Its offset field is `1,4`,
so the first sequence index is 1.
The conjecture is quoted verbatim in `claim` above. The author field
attributes the conjecture to Bala, not the entire sequence entry.

The native implementation fetched the live OEIS JSON on 2026-09-21
(Asia/Singapore) and read NAME, the complete conjecture comment, offset
`1,4`, revision 493, and modification time `2026-05-30T04:28:26-04:00`.

The full formal target has natural k, n, m, Odd k, n>0 and m>0.
It includes k=1, all index parities, and the primes 2 and 5. Natural
subtraction is faithful because positive Fibonacci indices have value
at least one. No offset change or additional hypothesis is imposed.

The bounded prior-resolution search supplied with issue #9152 inspected
arXiv:1701.06722v2, 2402.19301v1, 2408.14632v2 and, in the caller's brief,
2503.11055v1 without finding this exact settlement in the inspected passages.
Those paper readings are caller-supplied and were not repeated by this
implementation. The live source still labels the clause Conjecture.
This is not an exhaustive literature search or a worldwide-priority claim.

The caller's external Lean search found only a sequence definition in
sequencelib and a shifted golden-orbit result in rwst/lean-code.
The native implementation verified the actual frozen Lucas recurrence,
companion-power shape and determinant suppliers and the pinned Mathlib
periodic-point and prime-power divisibility contracts. The resulting proof
is a repository derivation of the cited statement, not a proof attributed
to Bala. The scalar-polynomial alternative mentioned in the caller's design
input has an unverified formal bridge and no demonstrated lower cost; it
is not used by this proof.
