---
bibkey: ratajczak2024a375007
authors: Lechoslaw Ratajczak
year: 2024
title: A375007 — isolated quotient-remainder values
doi: null
url: https://oeis.org/A375007
claim: The entry conjectures that every term after the sixth has prime successor; it reports verification below 10^8.
strata_touched:
  - D5/S3/Arith/IsolatedQuotientRemainder
license: citation-only
triage: anchor
---

# A375007

## Verified locator

Canonical entry URL: https://oeis.org/A375007

Revision 9 (2024-08-18), read in full on 2026-09-10. There is no DOI: OEIS
entries are not assigned one, so the frontmatter records `doi: null` and this
URL is the canonical locator. The complete text response was read at
https://oeis.org/search?q=id:A375007&fmt=text and the internal view at
https://oeis.org/A375007/internal, both HTTP 200.

The sequence consists of positive integers t for which
t mod k = floor((t-k)/k) mod k, with 1<=k<=t, holds only at k=1 and k=t.
Revision 9 (2024-08-18), read in full on 2026-09-10, conjectures that
a(n)+1 is prime for n>6 and reports verification for terms below 10^8.
The first six terms are 1,2,3,4,8,24. No proof of this conjecture appears
in the complete entry. Its separate Fortunate-type conjecture is not addressed.

## Search receipt

- Read https://oeis.org/A375007/internal and the complete text response at
  https://oeis.org/search?q=id:A375007&fmt=text (both HTTP 200).
- The pinned Mathlib db584cd6d46c92f209a44c0f1c829460d327499d has no A375007
  match. Its Nat.minFac_prime, Nat.minFac_dvd and Nat.minFac_le_div supply
  the standard ordered factorization of a composite number.
- Authenticated GitHub code search for A375007 language:Lean returned zero.
  The relevant non-Lean hit, Maxima-enthusiast/OEISSnippets/A375/A375007.wxm,
  was opened: it contains only the finite enumeration code from OEIS.
- arXiv API all:A375007 returned zero results. Bing gave unrelated results,
  Google a redirect shell, and DuckDuckGo a challenge; these are not negative evidence.
- The four direct OEIS cross-references were downloaded and their names and
  conjecture lines screened, but not read in full. Their external links were
  not opened and remain ASSUMED-UNVERIFIED.

No complete proof was found in the searched scope. Publication priority
and the absence of unindexed or differently worded proofs are not established.
The two explicit factor constructions were supplied in the implementation
brief; their Lean proof is a repository derivation, not a proof attributed
to this OEIS entry. Detailed receipts: docs/reports/a375007-implementation-0910.md.

## Gate receipt for this note's own shape

`DescribeContentGovernance.HeaderAnchorCanonicality` 与 `...:160-199` 的 locator 检查
是两道**只在 CI 暴露**的门:`make lean` / `make lean-report` / `make emit` / `make deposit`
全 EXIT=0 也照样可能红。本 note 满足的形状是:
`## Verified locator` 标题存在,其正文(到下一个二级标题为止)非空,
且**逐字含 frontmatter 的 `url`**(`doi: null` 时该项自动满足)。
本地可在开 PR 前自查:
`bash tools/scripts/workflow/scribe-content-checks.sh <candidate-lean-report.json> "" <base-40位sha>`。
