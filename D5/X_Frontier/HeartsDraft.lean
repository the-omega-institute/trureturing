/- GID: D5/X_Frontier/HeartsDraft
   generality: E
   mirror-B: none(waiver:human-gated-draft-in-result)
   mirror-E: none(waiver:no-experiment-before-statement-approval)
   anchors: []
   digest: Retain the permanent O-5/O-6 ticket autopsies; the monument is in Hearts and draft history is in git. -/

/- TASK D5-T0001
    Candidate B is frozen in D5/X_Frontier/Hearts with MeromorphicOn plus AnalyticAt zero localization and no pole-free-band clause.
    曾试过并失败:M0 draft parameterized arbitrary Zqc/Witt data and let a structuralSingularities set absorb every nonanalytic point; rejected as a dictionary statement rather than the independent engine. R2 first candidates also asserted a pole-free band, contradicted by the zeta(phi^3 s) structural pole in 6.19; that clause was removed. Cross-model adversarial review judged candidate B faithful and it was adopted; O-6 was separated because its draft left the Weil functional free. -/

/- TASK D5-T0018
    The audited O-6 proposition is monumented in D5/X_Frontier/Hearts without a proof assertion; its truth remains open.
    曾试过并失败:XM-1 rejected O6WeilPositivity with a free functional parameter as vacuous; repository and pinned mathlib inspection found no canonical Weil explicit-formula functional to bind in D5-T0001. D5-T0018 A-F bound the concrete dependency chain, and the audited G proposition moved to Hearts after the 2026-07-16 owner authorization; the transferred draft and patch now live only in git history. The authorized replay first exposed a stale raw Lean report after the dev merge, then an SL-008 drift because the new analytic import changed two frozen O-5 instance paths; regenerating the report and declaration-locally excluding the new CStar/RCLike instances restored the exact statement IDs. The next preflight fetched a concurrently advanced dev and failed the protected-base ancestry check before rule evaluation; an ordinary merge restored ancestry without changing Lean semantics. A pinned-base preflight then reached the expected SL-022-only admission result but conservative replay refused the dirty tracked tree; committing the candidate is therefore required before the final replay. Recording that failure changed HeartsDraft after report generation, so the next engineering run correctly rejected the stale report source hash; the final report must be regenerated only after the autopsy commit. Clean replays then reached SL-022-only admission but hit the fixed 180-second conservative worker timeout under the default macOS temporary path. An exact pinned-base verifier replay with TMPDIR=/private/tmp emitted a finding-free CORPUS_CONSERVATIVE certificate, preserving all 37 baseline-admit cases; the final preflight must use that non-indexed temporary path. -/
