/- GID: D5/S3/ObserverMemory/Trajectories/CurrentReadoutRecovery
   generality: G
   mirror-B: D5/B/S3/ObserverMemory/Trajectories/CurrentReadoutRecovery
   mirror-E: none(waiver:evidence-not-specified-by-formal-manifest)
   anchors: []
   digest: Evaluation at time zero recovers the current readout from its complete itinerary. -/

import D5.S3.ObserverMemory.Prediction.ItineraryCompletion

/- Library-search audit trail (2026-08-23):
   * Repository search found the exact family trajectory constructor
     `completeItinerary`; it is imported and used directly below.
   * Repository searches for head, evaluation at zero, complete itineraries,
     and recovery by composition found no existing theorem stating this raw
     current-readout equality.
   * The repository's `completionReadout` acts on a quotient completion and is
     not the source map, so it is not a duplicate of this statement.
   * Pinned Mathlib supplies function composition and the definitional
     zero-iterate computation; no separately packaged recovery theorem was
     found.
-/

namespace D5.S3.ObserverMemory.Trajectories.CurrentReadoutRecovery

open D5.S3.ObserverMemory.Prediction.ItineraryCompletion

set_option autoImplicit false
set_option relaxedAutoImplicit false

/-- Read the time-zero value of an itinerary. -/
def itineraryHead {B : Type*} (u : Nat -> B) : B := u 0

/-- Evaluation at time zero recovers the current readout from the complete
itinerary generated by the state update and readout map. -/
theorem recover_current_readout {X B : Type*}
    (F : X -> X) (q : X -> B) :
    q = itineraryHead ∘ completeItinerary F q := by
  funext x
  rfl

-- Unit data witnesses that the quantified domain is inhabited.
example (F : Unit -> Unit) (q : Unit -> Unit) :
    q = itineraryHead ∘ completeItinerary F q := by
  exact recover_current_readout F q

#print axioms itineraryHead
#print axioms recover_current_readout

end D5.S3.ObserverMemory.Trajectories.CurrentReadoutRecovery
