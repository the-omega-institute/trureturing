/- GID: D5/S3/ConceptDynamics/Closure/SourceClosureThreeLaws
   generality: G
   mirror-B: D5/B/S3/ConceptDynamics/Closure/SourceClosureThreeLaws
   mirror-E: none(waiver:evidence-not-specified-by-formal-manifest)
   anchors: []
   digest: Source-semantic closure is extensive, monotone, and idempotent. -/

import D5.S3.ConceptDynamics.DefinitionEscape.DefinitionKernelGalois

/- Library-search audit trail (2026-08-27):
   * Body-shape searches found the canonical source-semantic closure
     `DefinitionKernelGalois.DefinitionClosure`, constructed from the family's
     canonical `jointKernel`; it is imported rather than redeclared.
   * Exact frozen D5 lemmas `definitionClosure_extensive`,
     `definitionClosure_mono`, and `definitionClosure_idempotent` supply the
     three source clauses. No single frozen theorem or Scribe statement packages
     all three clauses, so this public conjunction applies those owners directly.
   * The adjacent `Closure.SourceClosureLaws` theorem packages only extensivity
     and monotonicity for an assumed abstract closure operator and is therefore
     not an exact hit for the constructed source closure. -/

set_option autoImplicit false
set_option relaxedAutoImplicit false

namespace D5.S3.ConceptDynamics.Closure.SourceClosureThreeLaws

open D5.S3.ConceptDynamics.ConceptFiberDecomposition
open D5.S3.ConceptDynamics.DefinitionEscape.DefinitionKernelGalois

/-- The closure generated by a concept family contains its generators, is
monotone in those generators, and is unchanged by closing twice. -/
theorem source_closure_three_laws
    {X Output : Type*}
    (sources larger : Set (Concept X Output)) :
    sources ⊆ DefinitionClosure sources ∧
      (sources ⊆ larger ->
        DefinitionClosure sources ⊆ DefinitionClosure larger) ∧
      DefinitionClosure (DefinitionClosure sources) =
        DefinitionClosure sources := by
  exact ⟨definitionClosure_extensive sources,
    fun subset => definitionClosure_mono subset,
    definitionClosure_idempotent sources⟩

#print axioms source_closure_three_laws

end D5.S3.ConceptDynamics.Closure.SourceClosureThreeLaws
