# S22 composed predecessor evidence

The original S22 [source prefix](../../../develop/theory/ARITHMETIC_BOUNDARY_QUANTIZATION.md), bytes `[0,414248)`, is exactly the complete 394734-byte merged S21 prefix followed immediately by the complete 19514-byte I30 S22 pure tail: 414248 bytes, 8150 LF, SHA256 `709b3de5f38ef393313f2c9c29e2f1afa3435fef7d7455fb8e5202bb01a31122`. No mathematical byte was edited. [composition.json](composition.json) declares the original owners and every current interval.

## Original version ownership

| Resource | Exact meaning | Representation |
| --- | --- | --- |
| source-composed-I50 | Exact original S22 source, 414248 bytes | Checked file_span `[0,414248)` |
| source-S21-merged / source-composed-I48 | Original merged S21, 394734 bytes | Checked file_span `[0,394734)` |
| tail-S22-I30 | Complete original 19514-byte pure tail | Checked file_span `[394734,414248)` |
| source-I30-current | Original I30 output, 363007 bytes | [Exact shared-prefix composition](source-I30-current.json) |
| source-original-pre-I30 / source-I30-original / original-pre-I30 | Original pre-I30 source, 366276 bytes | Original 56-segment recovery |
| report-I30-current | Entire original I30 report, 67218 bytes / 903 LF | Two exact raw parts |
| report-original-pre-I30 / report-I30-original | Original pre-I30 report, 47873 bytes | Remove exact `../` from adjusted prefix |
| I30-original / I30 | Original map, 403516 bytes / 9729 LF | Thirteen exact raw parts |

The supplied pre-I30 source/report came from the original stored Git blobs. Their reconstruction here checks the original mapping; it does not reproduce any historical mathematical computation. `source-I30-current` always names the original I30 output, never today's composition. Original source spans and original identities belong to `source-original-pre-I30`; current spans, additions and all 208 historical current numbered bindings belong to `source-I30-current`. Coverage destinations labelled `source` and `report` address `source-I30-current` and `report-I30-current`. Neither old source offsets nor original map JSON bytes are rewritten. The original I30 source is exactly inherited `s21:source-S21-C48` bytes `[0,343492)`, the original single-LF separator `[343492,343493)`, and the complete S22 tail. The separator has its own raw descriptor and exact original origin; this recovers all 363007 bytes without duplicating the historical prefix.

`report-I30-adjusted-prefix` is report bytes `[0,47876)`. Removing exactly `[2608,2611)` recovers the old report. Its `[0,405)` is the exact caller note; `[405,47873)` is the complete original I17 report. `inert-I17-certificate` preserves the original 5081-byte executable as text only. Old recipes, verdicts, damaged carriers, diagnostics and opaque references remain historical. No certificate or historical checker was run.

## Public consumption

[Original report entry](../actual-5040-density-order-0910.md) restores every original rendered heading fragment and supplies full-section intervals and all intersecting raw-part links. [Original map entry](../theory-body-migration-s22-0910.json) declares `s22-logical-entry-v1`; `resource('I30')` recovers the original `theory-body-migration-s22-v1` JSON bytes. Concatenate all raw parts in ordinal order without separators before parsing. Each part is at most 750 canonical LF lines. [navigation.json](navigation.json) retains full report section ownership; raw `.txt` parts do not acquire new relative-link meanings.

`at('I30', '/numbered_correspondence/0')` consumes the original schema. `resolve({'document':'I30','json_pointer':'/numbered_correspondence/0'})` verifies the declared immutable map identity. `span('source-I30-current', row['current_span'], row['current_identity'])` reads the original current unit; `span('source-original-pre-I30', row['original_span'], row['original_identity'])` reads its original predecessor. `recover()` reconstructs the complete original pre-I30 source from all 56 original coverage spans. `section(anchor)` reconstructs the entire original report section, including all child headings until the next equal or higher heading. `resource('s21:I37')`, `resource('s20:I29')` and `resource('s19:I42')` expose the unchanged parent readers with explicit stage ownership.

The source path used by both original versions is `docs/develop/theory/ARITHMETIC_BOUNDARY_QUANTIZATION.md`. Original I30 report relative links belong to `docs/reports/quantized-gh/actual-5040-density-order-0910.md`; original pre-I30 report links belong to `docs/reports/actual-5040-density-order-0910.md`. Links inside archived source excerpts retain their original source context and are inert historical text. Absolute runtime paths and opaque references are not current navigation and must not be followed. The named versions provide original bytes even when an old live pathname now contains a logical entry.

## Whole units, structural regions and inherited readers

[current-units.json](current-units.json) carries all 304 inherited records unchanged and all 19 S22 units. Each S22 unit retains its whole canonical result and translates only its original tail offset by 51241 bytes. The chapter 33 header `[394734,394823)` is separate from unit 32.24. The inherited structural regions `[337429,337498)` and `[364059,364127)` remain separate. Unit 32.24 retains its entire sixteen-row table and 992-byte proof.

[own-addresses.json](own-addresses.json) links all 22 original numbered identities to the original map. Numbers 33.20, 33.21 and 33.22 remain permanently reserved outside the active body. Their exact operational archives and citation rebindings remain in the original report/map, including all 17 archive fragments and five tables. These addresses must not be reused.

[canonical-pairs.json](canonical-pairs.json) references the unchanged S21 catalog and adds exactly the 42 original S22 pairs in [imports.json](imports.json). Current 323 whole-unit roots reach 450 records; the retained catalog has 494 records, including 44 historical records outside current-root closure. S21's original `current_reachable_pairs=452` name and value remain: its 304 roots reach 431, and its other 21 records are exact original I35 history. The S22 increment adds 19 roots and 23 historical records. All inherited 53 ordered parent chains remain. No historical residual-open `40e660de...yaml` alias is restored. Zero ingest is required: moved offsets do not create new atoms.

The [S21 API](../s21-evidence/README.md) now selects its fixed original source prefix using its existing `file_span` mechanism; ordinary `file` continues to mean an entire file. The [S20](../s20-evidence/README.md) and [S19](../s19-evidence/README.md) original version spans and identities remain. [parent-contract-preservation.json](parent-contract-preservation.json) binds exact pre-I50 bytes of both changed S21 contracts. All I49 identity, alias, origin and logical-entry repairs and their original before archives are retained.

## Scope and retained limits

This implements the exact ordered composition required by C163. The fixed prime labels `(2,3,5,7)`, exponents `(4,2,1,1)`, every real-capacity and finite-height quantifier, strict density gaps, all price/vector endpoints, negative-capacity infeasibility, mass-four distinction, uniform moment series, explicit compressed-token corrections, second-moment reversal and fixed `r=log2` witness are unchanged. `z=0` remains a formal/scaled endpoint, never a finite-height lattice datum. No GH definition, RH result, arbitrary-prime claim or slab-corner inference is added.

The original 614 representation checks, source EOF diagnostic, four report trailing-space diagnostics, 19 I30 CAS EOF diagnostics and six I30 failures retain their original attribution and bytes. Historical verdicts approve only their original scope. The prior S21 actual merge receipt is supplied historical evidence; this implementation makes no fresh CI or MERGED claim. Full independent review of the complete current source/evidence, caller sealing, required gates and ordered S22 delivery remain pending. The standing research goal remains active.

## Fixed reconstruction API

Copy the complete current S19, S20 and S21 README Python APIs to external scratch as described by those contracts. Copy the block below beside them as `s22_evidence.py`, use Python `-B`, and set `s22_evidence.ROOT` to the checkout root. This extends the existing fixed-byte API and executes no archived code, mathematical procedure or canonical ingestion. `verify()` checks original versions, aliases, composition, whole-unit/structural coverage, original/current addresses and report sections. Canonical closure and capacity evidence is reported separately by the implementation's fixed representation audit.

```python
"""Fixed S22 evidence consumption; uses the existing S19/S20/S21 byte API."""
from pathlib import Path
from s19_evidence import strict_json, pointer, Evidence
from s20_evidence import identity, interval, S20Evidence
import s21_evidence as parent

ROOT = Path('.')
BASE = 'docs/reports/quantized-gh/s22-evidence/'

def file(path):
    p = Path(path)
    if p.is_absolute() or '..' in p.parts:
        raise ValueError('repository-relative physical path required')
    return (ROOT / p).read_bytes()

def read_json(path):
    return strict_json(file(path))

def catalog():
    return read_json(BASE + 'composition.json')

def check(data, expected):
    # I30 uses byte_count/lf_count; preserve the original schema and bytes.
    e = dict(expected)
    for old, new in [('byte_count', 'bytes'), ('lf_count', 'lf')]:
        if old in e:
            if new in e and e[new] != e[old]:
                raise ValueError('conflicting identity fields')
            e[new] = e[old]
    return parent.check(data, e)

def read_parts(name):
    entry = catalog()['versions'][name]
    d = read_json(entry['descriptor'])
    if d['schema'] != 's22-evidence-entry-v1' or d['resource'] != name:
        raise ValueError('wrong resource descriptor')
    if d['original'] != entry['identity']:
        raise ValueError('descriptor/catalog identity differs')
    chunks, cursor, line = [], 0, 1
    for ordinal, part in enumerate(d['parts'], 1):
        b = check(file(part['path']), part)
        if not b or part['ordinal'] != ordinal or part['lines'] > 750:
            raise ValueError('empty, unordered or oversized part')
        if part['byte_span'] != [cursor, cursor + len(b)]:
            raise ValueError('part byte coverage differs')
        if part['line_span'] != [line, line + part['lines'] - 1]:
            raise ValueError('part line coverage differs')
        if ordinal < len(d['parts']) and not b.endswith(b'\n'):
            raise ValueError('interior part must end on LF')
        chunks.append(b)
        cursor += len(b)
        line += part['lf']
    return check(b''.join(chunks), d['original'])

def resource(name):
    c = catalog()
    name = c['aliases'].get(name, name)
    if name.startswith('s21:'):
        parent.ROOT = ROOT
        return parent.resource(name[4:])
    if name.startswith('s20:'):
        return S20Evidence(ROOT, Evidence(ROOT)).raw(name[4:])
    if name.startswith('s19:'):
        return Evidence(ROOT).raw(name[4:])
    entry = c['versions'][name]
    kind = entry['kind']
    if kind == 'parts':
        data = read_parts(name)
    elif kind == 'file':
        data = file(entry['path'])
    elif kind == 'file_span':
        data = interval(file(entry['path']), entry['byte_span'])
    elif kind == 'slices':
        data = b''.join(interval(resource(s['version']), s['byte_span']) for s in entry['slices'])
    elif kind == 'i30_recovery':
        data = recover()
    elif kind == 'remove':
        original = resource(entry['version'])
        chunks, cursor = [], 0
        for removal in entry['removals']:
            start, end = removal['byte_span']
            if start < cursor or interval(original, [start, end]) != removal['literal'].encode('utf-8'):
                raise ValueError('historical inverse literal or order differs')
            chunks.append(original[cursor:start])
            cursor = end
        data = b''.join(chunks) + original[cursor:]
    else:
        raise ValueError('unknown S22 representation kind')
    return check(data, entry['identity'])

def at(name, json_pointer=''):
    return pointer(strict_json(resource(name)), json_pointer)

def resolve(reference, owner='I50'):
    if owner == 'I50':
        binding = catalog()['immutable_documents'][reference['document']]
        return pointer(strict_json(check(resource(binding['path']), binding)), reference['json_pointer'])
    if owner.startswith('s21:'):
        parent.ROOT = ROOT
        return parent.resolve(reference, owner[4:])
    raise ValueError('explicit declared document owner required')

def span(version, byte_span, expected=None):
    data = interval(resource(version), byte_span)
    return check(data, expected) if expected is not None else data

def recover():
    m = at('I30')
    owners = catalog()['map_versions']['I30']
    data = {'source': resource(owners['current_source']), 'report': resource(owners['current_report'])}
    chunks, cursor = [], 0
    for row in m['original_source_coverage']:
        start, end = row['original_span']
        b = check(interval(data[row['destination']], row['destination_span']), row)
        if [start, end] != [cursor, cursor + len(b)]:
            raise ValueError('original source partition gap, overlap or order differs')
        chunks.append(b)
        cursor = end
    return check(b''.join(chunks), m['original_source_identity'])

def section(anchor):
    nav = read_json(BASE + 'navigation.json')
    row = next(s for s in nav['report_sections'] if anchor == s['anchor'] or anchor in s['aliases'])
    return span(nav['report_version'], row['byte_span'], row['identity'])

def verify():
    c = catalog()
    for name in c['versions']:
        resource(name)
    for alias, name in c['aliases'].items():
        if resource(alias) != resource(name):
            raise ValueError('alias differs')
    source = resource('source-composed-I50')
    if source != resource('source-S21-merged') + resource('tail-S22-I30'):
        raise ValueError('source composition differs')
    if resource('source-S21-merged') != resource('s21:source-composed-I48'):
        raise ValueError('parent source version differs')
    m = at('I30')
    for field, name in c['map_versions']['I30'].items():
        check(resource(name), m[field + '_identity'])
    units = read_json(c['current_units'])['units']
    regions = [(u['span']['byte_start'], u['span']['byte_end_exclusive'], u['span']) for u in units]
    regions += [(r['byte_span'][0], r['byte_span'][1], r['identity']) for r in c['structural_regions']]
    cursor = 0
    for start, end, expected in sorted(regions):
        if start != cursor:
            raise ValueError('whole-unit/structural coverage differs')
        check(interval(source, [start, end]), expected)
        cursor = end
    if cursor != len(source):
        raise ValueError('source suffix uncovered')
    for row in read_json(c['own_addresses'])['addresses']:
        original = resolve(row['binding_ref'])
        span(row['historical_version'], row['historical_span'], original['original_identity'])
        if not row['reserved']:
            a = span(row['current_version'], row['current_span'], original['current_identity'])
            if a != span('source-I30-current', original['current_span'], original['current_identity']):
                raise ValueError('moved unit differs')
    for row in read_json(c['navigation'])['report_sections']:
        section(row['anchor'])
    return dict(versions=len(c['versions']), aliases=len(c['aliases']), whole_units=len(units),
                structural_regions=len(c['structural_regions']), source=identity(source))
```
