Attempts: 1 (infrastructure retries 0/3)
先记账：#6298 的评论链本轮仍未取到。公开 issue 页到 Activity 后直接进入登录评论入口，没有给出可读评论正文；因此下文没有拿主帖反推前四轮判词，只使用你在本轮 prompt 中给出的已冻结结算，以及固定 SHA cf869c83cd805096333e5ac824e4d601f2f8b669 的公开源码。GitHub
我读了固定 SHA 的源码；没有跑 Lean build。
Q1 —— §19 三条的落地形状
结论
项判形真逃逸？1. 精确隐藏场 Schur 响应内容项有：hidden_field_solve_eq2a. c_eff²-c₀² 恒等式bind-only无2b. min ≤ c_eff² ≤ max仍属 bind-only 伴随结果无；只是通用凸组合序关系3. p=0 ↔ ω²=c_eff²k²纯 bind-only无
所以按你第 5⁴ 条的判形：这个模块除了第 1 项，没有第二个真逃逸内容。 2、3 作具名伴随结果落地是准确包装；不要把它们算成新的内容 theorem。
候选查重口径：卷内已经明确写有 (19.2)、(19.4)、推论 19.1 和主符号；所查 Lean 仓内来源未找到对应的 §19 (ω,k) 标量场声明。我没有把这句话升级成“仓内不存在”，原因见最后的未核实部分。卷里的原始公式在固定 SHA 的 §19.1：双场 Lagrangian 与稳定性条件见 1432–1460，精确 D_eff 见 1463–1476，低频系数见 1479–1529。GitHub
1. 真正该承载内容的是 hidden_field_solve_eq
建议不要直接把 (19.2) 当定义然后证明 rfl。先冻结频域第二条 Euler–Lagrange 方程的消元：
leandef hiddenDen
    (Ωh ch kSq ω : ℝ) : ℝ :=
  Ωh ^ 2 + ch ^ 2 * kSq - ω ^ 2

theorem hidden_field_solve_eq
    {Ωh ch kSq ω λ q r : ℝ}
    (hH : hiddenDen Ωh ch kSq ω ≠ 0)
    (hr :
      λ * q + hiddenDen Ωh ch kSq ω * r = 0) :
    r = -(λ / hiddenDen Ωh ch kSq ω) * q := by
  ...
再由两条频域方程得到真正的 Schur 等价，例如：
leantheorem two_field_system_iff_schur
    {A H λ q r : ℝ}
    (hH : H ≠ 0) :
    (A * q + λ * r = 0 ∧ λ * q + H * r = 0) ↔
    ((A - λ^2 / H) * q = 0 ∧
      r = -(λ / H) * q) := by
  ...
源码确实先列出这两条频域方程，然后只写一句“消去 r 得式 (19.2)”；这一步正是模型方程到 Schur 响应之间目前值得具名的桥。GitHub
逃逸见证：hidden_field_solve_eq，或者若模块以更强包装为主，则以 two_field_system_iff_schur 为主 theorem、前者为 witness。
**它不是哪条冻结定理的平凡推论：**不是 D5/S3/Arith/GoldenResource/ChainSchurResponse 的实例改名。后者的 GID 明确是 chain matrix family，处理“exact linear coefficients and a quadratic remainder”，并建立在 ChainBlockPencil 与矩阵 Schur complement 上；它没有把 §19 的 Fourier Euler–Lagrange 双场方程绑定到 (ω,k) 的标量分母。GitHub
**mathlib 支撑面：**这里只需要 ℝ 上域运算、field_simp、ring_nf/ring，必要时 linarith。如果只做到每个 Fourier mode 的标量 statement，不需要 PDE 库、distribution theory 或 characteristic variety 基础设施。
H ≠ 0 之外还要排什么退化？
对于单纯的 hidden_field_solve_eq 和上述双方程 ↔ Schur 方程等价，答案是：
没有额外退化条件。H ≠ 0 足够。
具体说，不需要额外假设：


λ ≠ 0；


q ≠ 0；


A ≠ 0；


D_eff ≠ 0。


D_eff ≠ 0 只有在你进一步要写“外源驱动后的响应是 1 / D_eff”时才需要；那已经是下一层逆传播函数求逆，不是 Schur 消元本身。
这里还有一个 Lean 特别值得防的坑：ℝ 在 Lean 里的除法是 totalized，x / 0 有定义。因此若不显式带 hH : H ≠ 0，证明出一个形式上有 / H 的式子，不等价于物理正文通常所指的 Schur pivot 有效域。
§19 对 H=0 怎么处理？
**正文没有单独处理。**定理 19.1 把 (19.2) 称为“精确逆传播函数”，分母就是
H=Ωh2​+ch2​∣k∣2−ω2,
但 1463–1476 没写 H≠0；证明 1533–1542 列出双场方程后直接说消去 r。GitHub+1
不过随后用于低频展开的窗口
∣ch2​∣k∣2−ω2∣<Ωh2​
自动排除了 H=0：令 s=ch2​∣k∣2−ω2，则 |s| < Ωh² 与 Ωh>0 给 s>−Ωh2​，所以 H=Ωh2​+s>0。源码也正是在这个窗口内使用含 Ωh²+s 分母的精确余项展开。GitHub+1
因此建议顺手落一个伴随域守卫：
leantheorem hiddenDen_pos_of_lowFrequency
    (hΩ : 0 < Ωh)
    (hwin : |ch^2 * kSq - ω^2| < Ωh^2) :
    0 < hiddenDen Ωh ch kSq ω := by
  ...
它有用，但仍是 bind-only，不算第二个逃逸。
为了把 H=0 的数学边界说死，可以做方程级 case analysis：


额外分支 λ ≠ 0（明标：这不是假设 19.1 推出的参数特化）：若 H=0，第二式给 λq=0，故 q=0；第一式继而给 λr=0，故 r=0。完整 2×2 系统此处行列式为 −λ2，所以这个“裸隐藏场共振”并不自动成为耦合系统的 pole。


λ=0 时，H=0 使隐藏方程退化成 0=0，r 无法从它消去；这正说明 Schur pivot 不可用。


这两个 case 是对原双场方程的代数分析，不应作为“观察者假设推出 λ≠0”。
2. c_eff² 两条：是 bind-only
定义
leandef alpha (λ Ωh : ℝ) : ℝ := λ^2 / Ωh^4

def cEffSq (c0 ch λ Ωh : ℝ) : ℝ :=
  (c0^2 + alpha λ Ωh * ch^2) /
    (1 + alpha λ Ωh)
则
leancEffSq c0 ch λ Ωh - c0^2
  =
alpha λ Ωh / (1 + alpha λ Ωh) *
  (ch^2 - c0^2)
在 Ωh > 0 下，确实就是分母非零以后 field_simp + ring 一类的规范化。
区间
min(c02​,ch2​)≤ceff2​≤max(c02​,ch2​)
比恒等式多一个序关系步骤：证明
0≤1+αα​≤1,
再认出 c_eff² 是 c₀²,ch² 的凸组合。不过这只是 sq_nonneg、正分母、case split + linarith 的 ordered-field bookkeeping；按你的“真逃逸”标准仍是 bind-only。卷本身已经把这个结论列为推论 19.1。GitHub+1
逃逸见证：无。
**与冻结定理关系：**它既不是 ChainSchurResponse 的推论，也不需要消费任何 chain 冻结件；它是 §19 定义的局部代数伴随结果。
mathlib：sq_nonneg、pow_pos/positivity、有序域除法引理、field_simp、ring、linarith。
3. 主符号零集：按你拟的 statement 完全 bind-only
若定义就是
leandef principalSymbol (cEffSq ω kSq : ℝ) : ℝ :=
  -ω^2 + cEffSq * kSq
那么
leantheorem principalSymbol_eq_zero_iff :
    principalSymbol cEffSq ω kSq = 0 ↔
      ω^2 = cEffSq * kSq := by
  simp [principalSymbol]
  -- 或 unfold + linarith
没有新的中间命题。
逃逸见证：无。
**不是冻结 theorem 的平凡推论：**更准确地说，它根本不消费冻结 theorem；它只是定义的零点重排。
mathlib：simp/ring/linarith。
这里要防止包装膨胀：卷真正较强的语义句是“该主部允许洛伦兹型特征几何解释”，并明确同时否定“所有物质共享同一速度”和“因此满足 Einstein 方程”。你当前拟的 p=0 ↔ ... 远弱于形式化 characteristic geometry；不要用这个零集恒等式声称已经形式化了后者。GitHub

Q2 —— 最接近可判定的是“微观 gap → 可观测 gap 的下敏感度公理”
我的技术选择是右栏的下敏感度桥。
有一个表面上更短的答案：直接假设“所有探针共享同一 c_eff”，然后结论就是共享 c_eff。但那是纯 assumption rebinding，而且当前没有 §19 Lean 层可消费，所以不符合你这里“⊢ 部分离已冻结件最近”的判据。
下敏感度桥不同：它一旦被明写为假设，下游可以立即消费已经冻结的整数 selector gap。
候选查重口径：**所查来源未找到这条 assume 形条件定理。**相反，仓内已经有一条精确 guardrail：ChainResponseSelectorGap 明说 chain Schur 响应的 selector loss 可任意小，这反驳“统一正整数 gap 自动穿过 Schur 消元”的推断，但不反驳整数矩阵上的 integer_log_unique_maximum。GitHub
建议的 Lean 假设形
先把整数层相对 scalar optimum 的损失具名：
leandef integerSelectorLoss
    {n : Type*} [Fintype n]
    (k : ℕ) (p : ℝ) (T : Matrix n n ℤ) : ℝ :=
  (Fintype.card n : ℝ) *
      (Real.log (k : ℝ) - p * k)
    -
  (Real.log (T.det : ℝ) - p * (T.trace : ℝ))
然后把真正缺的物理桥老老实实放在假设位：
lean(hsens :
  ∀ T : Matrix n n ℤ,
    (T.map fun z => (z : ℝ)).PosDef →
    T ≠ (k : ℤ) • (1 : Matrix n n ℤ) →
    κ * integerSelectorLoss k p T ≤ observableLoss T)
再带
lean(hκ : 0 < κ)
这里 observableLoss 必须由将来真正的响应/实验 readout 模型定义；不能为了证明 theorem 临时把它定义成 κ * integerSelectorLoss，否则整个目标就退化成 bind-only。
这条 hsens 正是“下敏感度公理”的 Lean 形：微观 objective separation 至少以比例 κ>0 传到 observable loss。
条件定理形
lean/--
CONDITIONAL theorem.
Does not prove lower sensitivity; `hsens` is an explicit extra axiom.
-/
theorem observable_gap_of_lower_sensitivity
    {n : Type*} [Fintype n] [DecidableEq n]
    {k : ℕ} (hk : 2 ≤ k)
    {p κ : ℝ}
    (hlo : Real.log (((k + 1 : ℕ) : ℝ) / k) < p)
    (hhi : p < Real.log ((k : ℝ) / (k - 1 : ℕ)))
    (hκ : 0 < κ)
    (observableLoss : Matrix n n ℤ → ℝ)
    (hsens :
      ∀ T : Matrix n n ℤ,
        (T.map fun z => (z : ℝ)).PosDef →
        T ≠ (k : ℤ) • (1 : Matrix n n ℤ) →
        κ * integerSelectorLoss k p T ≤ observableLoss T)
    (T : Matrix n n ℤ)
    (hT : (T.map fun z => (z : ℝ)).PosDef)
    (hne : T ≠ (k : ℤ) • (1 : Matrix n n ℤ)) :
    κ * IntegerFischerSelector.selectorGap k p ≤ observableLoss T ∧
    0 < observableLoss T := by
  ...
证明的 ⊢ 部分只有：


调 integer_log_unique_maximum 得
0<selectorGap
和非 scalar 时
selectorGap≤integerSelectorLoss;


乘 κ>0；


与 hsens 传递；


得 observable positive gap。


integer_log_unique_maximum 的源码正给出 dimension-independent positive selectorGap，以及 T ≠ kI 时比 scalar optimum 至少低一个 selectorGap。GitHub+1
逃逸见证：无。这是刻意的：它是 CONDITIONAL / assume theorem。真正困难的逃逸内容已经被完整暴露为 hsens，没有藏在证明里。若非要给 downstream composition 起名字，可以叫 selector_gap_to_observable_gap，但它不能报成研究逃逸见证。
它不是哪条冻结定理的平凡推论：hsens 不是 ChainSchurResponse 或 ChainResponseSelectorGap 的推论；后一条事实上告诉你，对已经冻结的 chain family，若 κ 要取一个与链长无关的正常数，这类自动传递正是被 no-go 阻断的。源码给出
lossn​≤21​(1/81)n+1,
所以响应 loss 可以趋零。GitHub+1
实际消费的冻结 GID
严格区分“证明依赖”和“边界 guardrail”：


证明直接消费：D5/S3/Arith/GoldenResource/IntegerFischerSelector，核心声明 integer_log_unique_maximum。GitHub+1


不作为正向证明依赖、但必须挂在 theorem 文档上的反推 guardrail：D5/S3/Arith/GoldenResource/ChainResponseSelectorGap。它明确冻结“uniform integer selector gap 不自动经 Schur 消元传到 real response”。GitHub


ChainResponseSelectorGap 自身 import D5/S3/Arith/GoldenResource/ChainSchurResponse；但上面这个抽象条件 theorem 无需直接消费 ChainSchurResponse。GitHub+1


**mathlib 支撑面：**现有 matrix/PosDef 表面、Real.log、线性序与乘法单调性，证明层可用 linarith/nlinarith。新难点不在 mathlib，而完全在 hsens 的物理语义。
因此这条若落地，正确结算语句只能是：

在显式 lower-sensitivity 假设下，冻结的整数 selector gap 条件地推出 observable gap。

不能写成“解决了微观离散如何传到观察层”，更不能写成“19.1 推出了 observable gap”。

Q3 —— §17 / 注 19.1 接口：本轮没有找到卷内矛盾
这里需要把“失效”拆得很精确。
1. 真正失效的是 (17.6) 作为统一正下界
定理 17.3 明设所有当前可见模式
νa​≥ν∗​>0,
然后给两条：
Vβ​≥β−1χ(0)(17.5)
和
Vβ​≥2ℏν∗​​χ(0).(17.6)
证明里也明确分开来源：第一条来自 xcothx≥1，第二条来自 cothx≥1 再使用 νa​≥ν∗​。GitHub
所以若有一列有限模型
ν∗​(n)→0,
则：


每个有限 n 若仍有 ν_*(n)>0，(17.6) 对该 n 仍然成立；


但系数 ℏν∗​(n)/2→0，所以不存在由 (17.6) 给出的、独立于 n 的统一正下界。


这正是源码推论 17.1 自己写出的边界：无隙极限中 (17.6) 不能保留为统一正下界。GitHub
因此不能把 Q3 结算成“17.3 在每个有限 n 变假了”。那会越界。坏掉的是uniform-in-n positive gap conclusion。
2. (17.5) 不能跟着一起判死
这是本轮最值得保留的精化。
(17.5) 的系数是 β⁻¹，没有 ν_*。从源码给出的证明也看得出来，第一条 lower bound 不使用具体 gap 尺度，只使用正频率下的 xcothx≥1。GitHub
所以仅凭 visible gap → 0：
不能判 (17.5) 的有限温固定 β 下界也随之塌零。
当然，若真正取到无限体积/连续谱的无隙极限，还需要另外证明有限和、协方差、χ(0) 等能合法过极限；§17 的有限谐振 proof 本身没有提供那个 continuum-limit theorem。故准确说法是：


(17.6)：uniform positive lower bound 明确失效；


(17.5)：没有被 ν_*→0 这一事实单独反驳，但卷也没有因此获得一个自动的无隙 continuum limit theorem。


3. 推论 17.1 失去的也是“固定正谱隙”那部分定量保证
推论 17.1 的前提明确写的是平衡、有限谐振、固定正谱隙，并紧接着自己警告 ν_*→0 时 (17.6) 不能作统一正下界。GitHub
所以无隙序列中不能继续引用它来声称一个由 fixed-gap quantum bound 给出的 uniform O(1) 噪声下界。
但在固定有限温 β 且某方向 χ(0)>0 的单个有限模型里，(17.5) 仍足以给那个方向正涨落。因此也不能把推论 17.1 粗暴改写成“gap→0 后非零响应和零涨落可以任意并存”。
4. 卷后来有没有把失效的 (17.6) 当作仍统一成立？
所查源码未找到。
三个最相关的后续接口我逐一核了：


推论 18.1只说“在第 17 节条件下”短时二次项满足相应响应下界；它没有在去掉 §17 条件后无条件复用 (17.6)。所以这是一条显式条件引用，不构成矛盾。GitHub


注 19.1反而正面写出：hidden 高频 gap 可保留、visible 低能 gap 可趋零，此时不能把依赖“全部可见模式统一正下界”的 §17 涨落界原样带进无隙极限。它与上述判形一致。GitHub+1


定理 20.1在重建涨落谱时调用的是定理 17.1 的精确涨落谱关系，不是定理 17.3 的 fixed-gap lower bound；而且 20.1 仍显式限定有限正定谐振模式以及 §17–18 的平衡/初态条件。因此我没有找到它偷用 gapless 下失效的 (17.6)。GitHub+1


因此本轮 Q3 的结算应是：
没有发现“卷内的错”。
更精确的接口图是
visible gap→0⟹(17.6) 失去 uniform positive content​
而不是
visible gap→0⟹§17 全部涨落关系失效​.
后一条太强；特别是 (17.5) 和定理 17.1 不能这样一起抹掉。
也因此，本轮没有可提供的“矛盾双方原文 + GID”：**因为靶向源码审计没有发现那一对矛盾陈述。**源码中的注 19.1 实际上已经正确封住了这个接口。
我没核实的部分


**#6298 评论链没核实到。**公开 issue 页面本轮仍未提供评论正文，只显示 Activity 后的登录评论入口；所以我没有核实前四轮评论原文，也没有从主帖反推它们。GitHub


**没有跑 Lean build。**上面的 Lean 都是“可直译形状”与 proof-surface 判断，不是“已编译通过”的声明；尤其 tactic 的最终一两行可能需要按实际 imports 调整。


没有完成 authenticated GitHub 全仓 code-search。公开精确检索对 hidden_field_solve_eq、cEffSq 等没有返回结果，我也检查了当前已知 chain 侧文件；因此口径只能是“所查来源未找到对应 §19 Lean 声明”，不能说“仓内不存在”。


Q3 不是全卷机械扫描。我定向读了 §17.1–17.3、推论 17.1、§18 与短时推论、§19 注 19.1，以及 §20 对 §17 的调用；所以结论严格是“所查这些接口未找到继续把 (17.6) 当 uniform gapless bound 使用的地方”，不是全卷不存在任何遗漏。


本轮没有重新做外部文献的系统查重。Q1 的三个 statement 已经直接存在于卷内；Q2 是明确的新增 assume 条件封装，其研究难点没有被声称为文献结果或观察者假设的逻辑后果。
