Attempts: 1 (infrastructure retries 0/3)
取证状态
本轮我按你指定的源码钉在 0b1f5805cc68c42a5cac3f2f8f10699d0efb4a90，只读公开 URL，没有跑 Lean、没有 clone/build、没有读取任何未推送内容。
/issues/6298 的评论链这次仍然没取到正文。GitHub 搜索结果显示该 issue 有 5 条评论，但匿名 issue 页面到 Activity 后直接要求登录，没有展开评论内容。故下面不从主帖反推前四轮判词，只把你在本轮 brief 中列出的冻结结论作为已给定输入。GitHub+1
另外，卷自身写明其中新增物理解释和综合命题在它所引用的旧项目快照上“尚未进行 Lean 编译”；这不能证明当前 pinned commit 全仓仍没有对应 Lean。匿名 GitHub code search 又要求登录，所以凡涉及“仓内没有 §19 Lean 声明”的地方，我只能报“所查来源未找到”，不能报全库不存在。GitHub+1

Q1 —— §19 三条的落地形状
判词
我的判形是：
项判形真逃逸见证1. D_eff = A - λ²/H有内容，但内容主要在“何时允许消元”hidden_field_solve_eq；更值得保留的是 hidden_denom_pos_of_low_frequency_window2a. c_eff²-c₀² = α/(1+α)(c_h²-c₀²)bind-only无2b. min ≤ c_eff² ≤ max不是纯 bind-only，但逃逸很小ceff_weight_mem_Icc3. p=0 ↔ ω²=c_eff²k²bind-only无
所以，对你问的“除了第 1 项还有没有别的真逃逸内容”：有且只有一个很小的：第 2 项区间结论所需的 convex-weight/order lemma。 第 2 项的恒等式本身和第 3 项都不应伪装成内容。卷里本身也是把区间作为 Cor. 19.1，而主符号解释则直接从 -ω²+c_eff²|k|² 得出。GitHub+1
文献/仓内有没有这条陈述
所查来源未找到现成的对应 Lean 声明。 卷内则明确已有 (19.2)、(19.4)、Cor. 19.1 和 Prop. 19.1；因此这里是在把卷内公式形式化，不应报成新的物理推导。GitHub+1
1. 精确 Schur 消元：真正该冻结的是定义域
建议 Lean 形状如下；这是形状草案，未编译：
leandef hiddenDenom (Ωh ch ω kSq : ℝ) : ℝ :=
  Ωh^2 + ch^2 * kSq - ω^2

def visibleDiag (Ω0 c0 ω kSq : ℝ) : ℝ :=
  Ω0^2 + c0^2 * kSq - ω^2

theorem hidden_field_solve_eq
    (hH : hiddenDenom Ωh ch ω kSq ≠ 0)
    (hr :
      λ * q + hiddenDenom Ωh ch ω kSq * r = 0) :
    r = -(λ * q) / hiddenDenom Ωh ch ω kSq := by
  ...

theorem schur_effective_equation
    (hH : hiddenDenom Ωh ch ω kSq ≠ 0)
    (hq :
      visibleDiag Ω0 c0 ω kSq * q + λ * r = 0)
    (hr :
      λ * q + hiddenDenom Ωh ch ω kSq * r = 0) :
    (visibleDiag Ω0 c0 ω kSq
      - λ^2 / hiddenDenom Ωh ch ω kSq) * q = 0 := by
  ...
逃逸见证：hidden_field_solve_eq。 它不是冻结的 ChainSchurResponse 的平凡重命名：后者是有限链/块 pencil 一侧；这里必须建立 Fourier 标量分母 H(ω,k) 的定义域见证，才能合法进行该具体消元。mathlib 支撑面只是 eq_div_iff / div_eq_iff、field_simp、ring_nf、linarith。
但我更建议再冻结这一条，因为它把卷里“低频窗口”与 H ≠ 0 真正接起来：
leantheorem hidden_denom_pos_of_low_frequency_window
    (hwin :
      |ch^2 * kSq - ω^2| < Ωh^2) :
    0 < hiddenDenom Ωh ch ω kSq := by
  ...
这里由 abs_lt.mp hwin 的左半边直接得到
−Ωh2​<ch2​k2−ω2,
所以 H>0。逃逸见证就是 hidden_denom_pos_of_low_frequency_window 本身的 strict-positivity bridge；它同样不是 UniformResolventRemainder 或链 Schur 结果的推论。mathlib 面是 abs_lt.mp + linarith。
这条其实比再次证明标量 Schur 代数更有落地价值。
H ≠ 0 之外还要防什么退化？
对 hidden_field_solve_eq 本身：没有。H ≠ 0 足够；λ=0、q=0、visible diagonal 为零都不妨碍这条代数消元。
但模块其余部分还分别需要：


定义 (19.3)(19.4) 时要有 Ω_h ≠ 0；19.1 已给 Ω_h>0，所以可自动取得。GitHub


若真正要把 D_eff 再取逆称为 visible response，还要另外假设 D_eff ≠ 0；“隐藏块可消元”与“完整 visible response 不在极点”是两件事。


使用卷里的精确 reciprocal expansion 时，需要 Ω_h ≠ 0 和 H ≠ 0。低频窗口同时保证后者。卷的展开及窗口就在 1479–1558 行。GitHub


卷怎么处理 H=0？
§19 没有单独处理这个共振点。 它先直接写 (19.2)，证明只说消去 r；随后才进入 |c_h²|k|²-ω²|<Ω_h² 的低频窗口。该窗口事实上排除了 H=0。GitHub
但更早的定理 14.1 已明确限定动态 Schur 消去只在“相应逆算子存在的频率上”进行，并在证明中真的使用 hidden block inverse。因此 §19 的 Lean 化应把 hH : H ≠ 0 显式恢复出来，而不是利用 Lean 中除法的 totalization。GitHub
这里还有一个值得写进注释的精确边界：


H=0, λ≠0 时，完整 Fourier 矩阵是
(Aλ​λ0​),det=−λ2=0.
所以裸 hidden resonance 并不等于耦合系统的本征共振；齐次方程反而强迫 q=0。


H=0, λ=0 时 hidden sector 完全解耦，才保留真正的 hidden-sector resonance。


Lean 里 0 / 0 = 0 的 totalized 运算可能在后一种情况下把 A - λ²/H 化成 A。代数值碰巧“看起来合理”，但它不是合法 Schur inversion 的证明。因此 hH 不应删。


2. c_eff²：恒等式 bind-only，区间有一个小逃逸
建议定义：
leandef alpha (λ Ωh : ℝ) : ℝ :=
  λ^2 / Ωh^4

def ceffSq (c0 ch λ Ωh : ℝ) : ℝ :=
  (c0^2 + alpha λ Ωh * ch^2) /
    (1 + alpha λ Ωh)
差值：
leantheorem ceffSq_sub_c0_sq
    (hΩ : Ωh ≠ 0) :
    ceffSq c0 ch λ Ωh - c0^2 =
      alpha λ Ωh / (1 + alpha λ Ωh) *
        (ch^2 - c0^2) := by
  ...
判 bind-only。逃逸见证：无。 hΩ 后 α≥0、1+α≠0，主体就是 field_simp/ring。它不消费任何冻结链定理。
区间则最好先冻结：
leandef ceffWeight (λ Ωh : ℝ) : ℝ :=
  alpha λ Ωh / (1 + alpha λ Ωh)

theorem ceff_weight_mem_Icc
    (hΩ : Ωh ≠ 0) :
    ceffWeight λ Ωh ∈ Set.Icc (0 : ℝ) 1 := by
  ...
再推出：
leantheorem ceffSq_mem_interval
    (hΩ : Ωh ≠ 0) :
    min (c0^2) (ch^2) ≤ ceffSq c0 ch λ Ωh ∧
    ceffSq c0 ch λ Ωh ≤ max (c0^2) (ch^2) := by
  ...
逃逸见证：ceff_weight_mem_Icc。 非平凡点不是代数规范化，而是由平方非负得到 0≤α/(1+α)≤1，然后把 c_eff² 识别成端点的 convex combination。它不是任何冻结 chain GID 的推论；mathlib 面是 Set.Icc、sq_nonneg/positivity、除法序性质、le_total、linarith。
这个逃逸是真的，但很小；我不会因此把第 2 项抬成模块主内容。
3. 主符号：bind-only
leandef principalSymbol
    (ceffSq ω kSq : ℝ) : ℝ :=
  -ω^2 + ceffSq * kSq

theorem principalSymbol_eq_zero_iff :
    principalSymbol ceffSq ω kSq = 0 ↔
      ω^2 = ceffSq * kSq := by
  ...
逃逸见证：无。判 bind-only。 simp [principalSymbol] 加 linarith 足够这一层；它不消费冻结定理。卷自己也是一句“由主符号直接得到”。GitHub
**因此 Q1 的落地建议很明确：**一个模块可以落，但主内容应记为 exact Schur/domain guard；c_eff 恒等式和 characteristic equation 是具名伴随结果；interval lemma 可算一个小的真实 escape。

Q2 —— 最接近可判定的是“微观 gap → 可观测 gap 的下敏感度”
这里我会选右栏的 lower-sensitivity bridge，不是 cross-species universality。
原因很机械：如果把 cross-species 公理直接写成 ∀ i, cEff i = c，下游“所有 probe 有共同速度”几乎就是把假设重复一遍，不消费任何冻结 GID。这正是你说的“把难点塞到假设位”。observer→objective 和 Einstein 更远；无隙 §17 replacement estimate 本身就是缺失的分析估计。
而 lower-sensitivity 的好处是：L3 已经把它的两端都冻结了——一侧有整数离散 gap，另一侧有 Schur selector loss 指数趋零。所以条件定理非常短，而且还能立刻判定“一条 universal 版本的这种公理不可能覆盖 L3 family”。
文献/仓内有没有这条陈述
所查来源未找到下面这个显式 Lean 条件定理。 它是我根据你已冻结的 L3 边界写出的 assume 形态，不应报成既有物理定理，也不应报成解决了 observer→physics 的开放桥。
公理的 Lean 形
未编译形状：
leandef UniformLowerSensitivity
    (κ : ℝ)
    (microGap observableGap : ℕ → ℝ) : Prop :=
  0 < κ ∧
  ∀ n, κ * microGap n ≤ observableGap n
这精确表达的是：

存在一个不随链长变化的正系数 κ，使微观 gap 的固定比例一定传到可观测 gap。

注意这里不能偷偷允许 κ=κ_n→0；否则它根本不是你右栏缺失的“下敏感度公理”。
最短的条件定理
leantheorem observable_uniform_gap_of_lower_sensitivity
    (κ : ℝ)
    (microGap observableGap : ℕ → ℝ)
    (hMicro : ∀ n, 1 ≤ microGap n)
    (hSens :
      UniformLowerSensitivity κ microGap observableGap) :
    ∀ n, κ ≤ observableGap n := by
  ...
这是条件定理，不推进开放问题。它只是：
assume uniform lower sensitivity⟹uniform observable gap.
这一条的 ⊢ 部分几乎只有序算术，因此确实是五项里离冻结件最近的。
逃逸见证：lower_sensitivity_floor，即从 1≤microGap n 与 κ>0 得 κ≤κ·microGap n。它本身很小。不是 integer_log_unique_maximum 的平凡推论，因为后者只给微观离散侧；也不是 UniformResolventRemainder 的推论，因为后者只控制可观测实值侧。mathlib 面：mul_le_mul_of_nonneg_left、linarith。
但 L3 随即把 universal 版本打掉
更有结算价值的是紧跟一条：
leantheorem l3_refutes_uniform_lower_sensitivity
    (κ : ℝ)
    (microGap observableGap : ℕ → ℝ)
    (hMicro : ∀ n, 1 ≤ microGap n)
    (hCollapse :
      ∀ n,
        observableGap n ≤
          (1 / 2 : ℝ) * (1 / 81 : ℝ)^(n + 1)) :
    ¬ UniformLowerSensitivity κ microGap observableGap := by
  ...
这里真正的逃逸见证可叫：
leanselector_loss_eventually_lt_kappa
内容是对任意 κ>0，存在 n 使
21​(1/81)n+1<κ.
然后 lower sensitivity 给 κ≤observableGap n，L3 给 observableGap n<κ，矛盾。
这不是 IntegerFischerSelector 单独的平凡推论，也不是 UniformResolventRemainder 单独的平凡推论；它消费“固定离散 gap + 实值响应指数塌零”两侧，再加显式 assume 的下敏感度桥。mathlib 支撑面是几何序列 Tendsto/tendsto_pow_atTop_nhds_zero_of_lt_one 一类结果，再加实数序关系与 linarith；具体 API 名我未编译确认。
会消费的冻结 GID
按你本轮给出的冻结名， specialization 会消费：


IntegerFischerSelector 一侧的整数离散/integer_log_unique_maximum，用于得到固定微观 gap；


ChainSchurResponse，把被比较量落实为 Schur response/selector loss；


UniformResolventRemainder 的 remainder_bound，以及 L3 已冻结的指数上界；


若实现沿你已冻结的递推证明走，还会触及 chain_endpoint_sq_le。


L3 最终封装本身的精确 GID 我不能填写：它在你说的评论链中，而本轮我仍未取得评论正文。这里若编一个名字就违反你的纪律。
技术上的最终判断是：

lower-sensitivity 是最值得写成显式 assume 条件定理的那一项；但 L3 已证明它不能作为覆盖整个 19.1 类的 universal axiom。

这条如果落，只能报“假设这个额外 bridge，则……；而 frozen L3 family 不满足 universal 版本”。不能报“观察者假设推出了可观测 gap”。

Q3 —— §17 涨落界与 Note 19.1 的接口
结论：这次所查来源没有找到卷内矛盾
但这里有一个必须纠正得非常精确的边界：
不是 §17 的两条涨落下界都在 visible gap→0 时失效。真正失去 uniform 正下界的是 (17.6)；(17.5) 不能因为 gap closing 就一起判死。
1. 哪个假设首先失效？
Theorem 17.3 统一假设所有 visible modes 有一个共同的
νa​≥ν∗​>0.
对有限体积序列若最小 visible gap ν∗,n​→0，就不存在一个与 n 无关的正 ν_* 可用于整个序列。GitHub
2. (17.6) 的精确失效边界
Vβ​≥2ℏν∗​​χ(0).
它不是每个有限 n 都变假。如果每个有限系统仍有 ν_{*,n}>0，那么可以逐 n 用
Vβ,n​≥2ℏν∗,n​​χn​(0).
失去的是一个 n-independent positive floor，因为 ν_{*,n}→0 时右端系数一起趋零。卷自己的 Cor. 17.1 已明确指出无隙极限下 (17.6) 不能保留为统一正下界。GitHub
因此 Note 19.1 在 1638–1642 行说 hidden 高频 gap 可保持、visible 低能 gap 可趋零，并因此不能原样搬运“依赖全部 visible modes 统一正下界”的 §17 bound，和 §17 自己的量词边界是一致的。GitHub
3. (17.5) 没有因 ν_*→0 自动失效
Vβ​≥β−1χ(0).
这个结论的右端没有 ν_*。更关键的是证明：它来自
xcothx≥1,
而 (17.6) 才使用 coth x≥1 再配合 ν_a≥ν_*。因此在固定有限 β、仍可合法定义各有限系统热平衡谱的情形，gap closing 本身不摧毁 (17.5)。GitHub
甚至可以说 Theorem 17.3 的包装略强：它把 (17.5) 和 (17.6) 放在同一个 hgap 假设下面，但证明显示 (17.5) 可以单独抽出来，在更弱条件下证明。
这不是“卷内错”；只是 theorem factoring 可以更精确。
4. 17.1 / 17.2 也不能说“无隙时已被反驳”
Theorem 17.1 的有限模型公式含 1/ν_a 和 coth(βħν_a/2)，17.2 的静态谱和规则含 1/ν_a²。对每一个仍有正频率的有限 n，这些公式仍在其声明域内。真正形成 ν=0 的极限对象则可能需要单独的收敛/红外分析；当前定理没有自动给这个极限。GitHub
所以这里不能从“ν_{*,n}→0”跳成“FDT 17.1 已失效”。
后文有没有仍按 (17.6) 的 uniform gapless 版本使用？
所查来源未找到。
我检查到的关键后续接口反而都守住了边界：


Cor. 18.1 只说“在第 17 节条件下”短时二次项有相应 response lower bound；这是显式条件句，没有把它提升成无隙序列的 uniform statement。GitHub


Note 19.1 正是在提醒不能把该 uniform-gap bound 原样带入无隙极限。GitHub


Theorem 20.1 重新固定“有限个正定谐振结构模式”以及 §17–18 的平衡条件；证明涨落部分引用的是 Theorem 17.1，不是 Theorem 17.3/(17.6)。GitHub


因此这次没有一组“卷内错误原文 ↔ 已冻结 Lean GID”可以报。硬凑一组反而会把 (17.5) 和 (17.6) 混在一起，制造一个不存在的冲突。
这轮 Q3 真正结算出的精确接口是：
visible gap→0⟹失去的是 (17.6) 的 uniform positive floor， 不是固定 β 下的 (17.5).​
而 Note 19.1 与这个边界相容。

本轮结算
Q1 可以落一个 §19 Fourier-response 模块，但内容账应记成：Schur 消元的定义域/低频窗口保证 invertibility 是主逃逸；c_eff 差值恒等式和主符号 iff 是 bind-only；interval 只有一个小的 order-theoretic escape ceff_weight_mem_Icc。
Q2 技术上最近的是 micro-gap → observable-gap lower sensitivity。写成显式 assume 后，下游 uniform-gap theorem 极短；更重要的是，冻结 L3 已能证明这种 bridge 不能 universal 地覆盖该 19.1 counterfamily。这是一条条件/no-go 结论，不是开放问题突破。
Q3 所查来源未找到卷内错。§17↔§19 的接口目前是自洽的；必须保留的细分是 (17.6) uniform floor 塌掉，而 (17.5) 在固定有限温度下不因 gap closing 自动塌掉。
我没核实的部分


/issues/6298 搜索页显示有 5 条评论，但匿名 GitHub 页面没有给我评论正文，因此前四轮评论链仍未核实；我没有从主帖反推。


没有跑 Lean/build。以上 Lean 全是可直译形状判断，不是编译证据；具体 mathlib theorem 名可能需要实现席调整。


GitHub 匿名 code search 被登录墙挡住，因此“当前 pinned commit 全仓没有 §19 Fourier-side Lean 模块”我没有独立完成全库证明；口径只能是“所查来源未找到”。GitHub


IntegerFischerSelector、ChainSchurResponse、UniformResolventRemainder、chain_endpoint_sq_le、remainder_bound 等冻结 GID/声明名在本答复中依据你给出的本轮冻结清单；评论链取不到，所以 L3 最终 wrapper 的精确 GID ASSUMED-UNVERIFIED，我没有编造。


Q3 做了 pinned 卷内相关 theorem/note 及后续引用的定向反查，但由于没有本地 checkout/grep，不能把“所查来源未找到后文误用”升级为“机械穷尽证明全卷绝无任何误用”。


本轮没有另做外部文献穷尽检索；因此所有候选的 novelty 口径都只是“所查来源未找到”，不是“文献中不存在”。
