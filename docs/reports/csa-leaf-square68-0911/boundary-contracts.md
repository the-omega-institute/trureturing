# Prospective boundary contracts

Origin: consensus-rnd:sshx, sole codex-cli implementation worker, repo-prior-exposed. No same-round peer outputs, delegation, or independent review. Registered before local proof implementation; caller preregistration-v2 supplies the two bridge obligations and named consumer.

Fixed base: e492280a34dc1f09ac42d23749ad6a90bd223584. Mathematical source: df31b816bf570ddb71664473d1f7e33e8fcae6f0, SHA256 98506d9461a2bf48f586c68a7987d839a8be1d0c3d9e503febbd7bccc2f884bc.

The general theorem must quantify over every actual BalancedRich 3. Source filtering uses arbitrary fixed Set SourceTree, changes only Selection, and retains the identical full Context by construction. Classical decidability is internal. The fixed equal-leaf set is the range of j ↦ mul (of j) (of j).

Focused warm Lean probes will check these independently specified contracts:

1. Empty archive, empty current region with retained archive, and empty selection: filtered product readout is zero, without Nonempty assumptions. Filtering retains the full Context definitionally.
2. A selected +1 and -1 occurrence with the same leaf: the charge coefficient is zero, but all four ordered generated occurrences remain selected. Their contributions are +1, -1, -1, +1. The readout is zero. Occurring-source membership must not be identified with nonzero support.
3. Two distinct selected positive occurrences of the same leaf (with unselected negative balancing events): four ordered selected pairs and readout four. Event-identity diagonal would incorrectly give two.
4. Mixed leaf and nonleaf selected sources: only equal leaf pairs survive. Equal nonleaf sources and unequal leaf pairs are excluded. A nonleaf nonzero coefficient must not enter the leaf square sum or its reindexing.

These are validation probes only, not independent admission content. Type checks alone do not discharge these behavioral contracts. The general proof must group complete occurrence fibers before eliminating zero totals, consume source_charge_apply and leaf_product_fiber_readout on its live path, and apply Finset.sum_mul_sum.

Only the source-charge definition, its finite support, the literal source filter, and the P68.1 readout equation are owned here. No syntax membership, exact input-leaf count, P68.2/3, full CSA, or ZFC conservativity/model/consistency result is asserted. The strengthened source atoms remain uncovered.
