# A279609 qualification D

Pinned mathematical baseline: `3f6ffb946bd074546424ad2298f9b79d375bfc98`.

## Result

One combined implementation lane is qualified at the proposed GID
`D5/S3/Arith/GoldenResource/A279609DoublingRefutation`. It must carry the closed
OEIS claim and its negation in one module. W-2 through W-5 are related live proof
steps, not independent first-freeze modules.

The planned utility header is:

`utility: kind=certified-instance; basis=refutes=atom:f1e442dc23de02562417cb024f7fc6712689168f5b16fee22b387bf678d18ca5; result=D5/S3/Arith/GoldenResource/A279609DoublingRefutation.result; claim=D5/S3/Arith/GoldenResource/A279609DoublingRefutation.claim`

W-2, W-3, W-4, and W-6 produced faithful Mathlib-only target statements whose
cheap probes returned `status=open` in 46, 21, 22, and 20 seconds respectively.
The exact positive atom and standalone W-5 target each exhausted the one-repair
allowance with `probe-error`; neither receives an independent lane. The two
whole-section historical atoms are non-propositional and have no Lean target.

## Escape Content

W-5 retains the original genuine witness: outward rational enclosures place
the two real Lagarias expressions in unique integer floor cells. Those concrete
cell enclosures are not supplied by the frozen generic harmonic, gamma, log, or
exponential bounds; they are not aliases of the final integer equalities; and
both are consumed live by floor uniqueness.

W-4 requires the newly preregistered `critical_price_optimizer_trichotomy`:
comparison of an arbitrary supporting price with the certified common boundary
must force the optimizer below `m`, above `n`, or into the single tied-layer
interval. A common window endpoint by itself was not treated as a proof of
adjacency.

Under the pinned current `CLAUDE.md` section 3.2, `refutes` does not admit a
bind-only theorem. W-6 is content only because W-4 and W-5 remain on its live
path after same-batch helpers are inlined.

## Evidence Boundary

The current OEIS A279609 entry still describes `F(n)/2^n` as appearing monotone
and records no proof or counterexample. Its b-file stops at index 23; A004490's
lines 24 and 25 are `395622702669701707200` and `791245405339403414400`.
Exact A279609 searches returned zero arXiv and Crossref records. Repository
prior-art search exited 3 only for the source volume, adjacent frozen price
infrastructure, an unrelated Li-criterion PR, and two already-merged branch
names. No D5 or Blueprint theorem contains A279609, either witness integer, or
the claimed gap values.

An independent 120-decimal calculation reproduced both divisor sums, both gap
values, and `2*D(m)-D(n)=718636135679194643`. This is a numerical check, not a
formal proof.

## Not Claimed

This report does not prove the refutation, promise completion within one seat,
claim an exhaustive literature search, claim the first-failure-among-30 result,
or claim any implication for RH. No full Lean build or repository mutation gate
was run.
