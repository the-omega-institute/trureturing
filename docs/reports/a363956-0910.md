# A363956 implementation record

## Origin and scope

Skill: `lean4`. Implementation: Codex primary agent, single source, no independent
review claimed. User supplied the conjecture and proposed finite-prime escape.
Tier 1; goal is surjectivity of the actual least-unused-positive-multiple recursion.
No surjectivity assumption may enter the definition. The 200000-term observations
are user reports only and do not verify the conjecture. Omega with multiplicity
and A363504/A351495 are outside implementation scope.

Base: `d59adb46d4703e7fdc7ef7569c5c0919247cc87a` (`origin/dev` at start).
Branch: `lane/math/a363956`. Lean 4.33.0; Mathlib
`db584cd6d46c92f209a44c0f1c829460d327499d` verified in tracked pins.
CLAUDE.md read in full in segmented reads, including initially truncated portions.

## Preregistered proof route

Question answered: does every positive integer occur in A363956?
Proposed escape witness: finitely many activations of queue 2 would imply finitely
many bare-prime outputs. A queue whose prime has never appeared outputs that prime
at its first activation. The tail would therefore use a finite set of queues.
An infinitely activated queue exhausts all its multiples; their distinct prime
factor counts are unbounded, contradicting the finite set of generated primes.
Then powers of 2 times finite products of other primes force every prime queue.
These are planned proof obligations, not verified results yet.
Stop if the finite-prime contradiction or least-multiple step fails; retain an
honest note with an actual Lean attempt and its precise remaining goal.

## Search receipts

1. Repository D5, `rg -n -i 'A363956|A363504|A351495|least.unused|greedy.*multiple|queue.*fair|cardDistinctFactors'`:
   no sequence or queue-fairness hit. Hits include PrimeFactorCountMoments,
   PrimeFactorCountGeneratingFunction, DistinctPrimeFactorCountBound and
   DivisorCountRadicalCoincidence. Broader greedy/least-unused search identified
   SumInConcatenation and several unrelated greedy constructions; their public
   declarations are being inspected before deciding reuse.
2. Read DistinctPrimeFactorCountBound public theorem: lower radical-product bound
   and upper logarithmic bound for omega, not queue fairness. It identifies omega
   with the prime-factor finset cardinality using existing Mathlib lemmas.
3. Mathlib and external search: pending. No search-complete or priority claim.

## Verification and delivery

Initial `.lake` absent. No bare Lake command run. `make lean-cache-ensure` is next.
`make lean`, report, emit, freeze and Scribe content checks: pending.
Public theorem proof_shape/dependencies/escape_witness/admission_basis: pending
until declarations are implemented; no frozen declaration delivered at this point.

## Unclaimed

No proof of permutation, no counterexample, no new finite range claimed.
External pages and independently checked lemmas mentioned by the user remain
ASSUMED-UNVERIFIED until explicitly opened or checked here. No theory volume or
atom will be authored to manufacture a coverage target.

## Search and cache checkpoint

Pinned Mathlib searches found `Nat.nth_mem_of_infinite`, `Nat.nth_injective`,
`Nat.nth_count`, `Nat.nth_prime_zero_eq_two`, all named cardDistinctFactors
lemmas in the brief, `Nat.primeFactors_mono`, `Nat.primeFactors_pow`,
`Nat.primeFactors_prod`, `Set.Finite.of_finite_fibers`, and
`Set.Finite.of_injOn`. The whole Mathlib textual search for
`least.unused|queue.*fair|greedy.*multiple|A363956` had no hits.
SumInConcatenation's full public surface is specific to decimal legality;
PrimeFactorCountMoments/GeneratingFunction public statements concern zeta-law
moments, pointwise support and Euler products; DivisorCountRadicalCoincidence
concerns divisor-cardinality/radical divisibility. None provides queue fairness.
The generic greedy_residue_classes theorem concerns residue-class covering,
not least-unused multiplicative queues. No exact reusable D5 theorem identified.

OEIS `/A363956/internal`, `/A363504/internal`, `/A351495/internal` actually fetched
and read on 2026-09-10. All three explicitly call permutation a conjecture.
A363956 gives a(210667)=17 as a recorded example, not a proof. Main entry revision
is #10, Jul 01 2023. These pages have programs, data/plot links and cross-references;
no proof is printed. The one-hop fetch via Python urllib first returned HTTP 403;
curl subsequently succeeded for both, so this is resolved, not a search gap.
GitHub `gh search code A363956 --language Lean --limit 30 --json path,repository,url`
returned `[]`; arXiv API `all:A363956` returned totalResults=0. These bounded
searches establish only `not-found-in-searched-scope`, not exhaustive absence.
Snapshots are `/tmp/a363956-entry.html`, `/tmp/A363504-entry.html`,
`/tmp/A351495-entry.html`. Linked data/plots and revision histories were not read.

`make lean-cache-ensure` EXIT=0: status=seeded, method=clonefile,
donor=/Users/chronoai/trureturing, clonefile_attempts=1, stamp_miss=null,
mathlib_missing_olean_files=0, project_olean_state=warm, mathlib_olean_state=warm,
archive_skip_reason="project olean state is warm". Incremental Lean checks may now run.
Capacity: D5/S3/Arith has 33 direct files (118 including descendants);
Library/Arith has 48 files and is full. No new note will be put in that bucket.

## First Lean proof checkpoint

Warm-tree `lake env lean /tmp/A363956.lean` EXIT=0. The file contains the
actual finite-history recursion starting at (2,{1,2}), then seq(1)=1 and
seq(n)=state(n-2).first for n>=2. Proved candidate existence, minimum membership,
all tail terms >=2, exact stored history, tail injectivity, and queue_exhausts:
an infinitely activated prime queue outputs every positive multiple somewhere.
`#print axioms queue_exhausts` reports only propext, Classical.choice, Quot.sound.
The initial attempts exposed id normalization, range_add_one's implicit index,
and conjunction binder errors; repaired without changing the mathematical claim.
This is an unbounded intermediate lemma, not a finite enumeration result.
The finite-prime contradiction and final surjectivity remain to be proved.

## Finite-prime contradiction checkpoint

`lake env lean D5/S3/Arith/OmegaGreedyPermutation.lean` EXIT=0;
`#print axioms two_queue_infinite` = [propext, Classical.choice, Quot.sound].
Proved q_appears: each selected prime either already belongs to the actual
history or is the very next output, by minimality and divisibility. Hence a
finite queue-2 index set contains all prime-output indices and makes the whole
queue range finite. Injectivity of the prime enumeration makes the omega range
finite. Set.Finite.of_finite_fibers supplies an infinite queue, whose multiples
have arbitrarily large omega (finite products of distinct primes), contradiction.
No monotonicity or numerical growth estimate of the prime enumeration is used
in this argument; only primality and injectivity are consumed.
Remaining step: fixed-cardinality even prime supports force each prime queue.

Canonical route EXIT=0 for G/S3/Arith/OmegaGreedyPermutation. Initial route
calls rejected an absolute manifest path and then empty artifact; corrected to
repository-relative .lake/a363956-manifest.json and artifact=lean. No rule changed.

## Complete Lean proof checkpoint

Warm incremental check EXIT=0 after narrowing tactic imports. All five public
theorems print exactly [propext, Classical.choice, Quot.sound]: sequence_initial,
sequence_positive, sequence_injective, sequence_greedy, a363956_surjective.
For each r, the products 2^(t+1) times the product of the first r+1 primes are
injectively indexed by t and have exactly r+1 distinct prime factors. Their
prime support already includes 2; the power does not enlarge that support.
Queue 2 exhausts these even numbers, forcing infinitely many activations of
prime r. Every m>=2 has a prime divisor, whose queue exhausts m; 1 is the seed.
The definition of seq contains only the greedy recursion, not fairness or
surjectivity. sequence_greedy proves its entire prior-index forbidden set.
The final small repair was the order of the two-element seed finset, closed
with Finset.pair_comm; no finite experiment is delivered as a result.

## Declaration audit

All public GIDs have prefix `D5/S3/Arith/OmegaGreedyPermutation.`.
Direct pre-existing frozen dependencies for every theorem: `[]` (no D5 imports,
therefore no omitted GID/statement_id pair). Only pinned Mathlib is imported.
The module admission_basis is `escape-witness` and utility is `none`.

| Public theorem | proof_shape | escape_witness | admission_basis / obligation |
| --- | --- | --- | --- |
| sequence_initial | bind-only | null | companion: prescribed seeds, consumed by a363956_surjective |
| sequence_positive | content | next_spec | escape-witness: total positive greedy recursion |
| sequence_injective | content | b_injective | escape-witness: positive-index injectivity |
| sequence_greedy | content | used_seq | escape-witness: original minimum and entire forbidden history |
| a363956_surjective | content | two_queue_infinite | escape-witness: requested positive-integer surjectivity |

Companion edge direction is consumer -> prerequisite:
a363956_surjective -> sequence_initial. Positivity, injectivity and greedy
characterization are the user's explicit obligations about this same seq.
They are not separate deposits. No finite positive instance is a public result
apart from the defining seeds, which are not an independent computational claim.
All delivered content is unbounded symbolic recursion, set finiteness or number
theory; no checker, bounded enumeration, numeric reduction or certified finite
instance is delivered. G records independence from a quadratic discriminant;
the particular omega-indexed rule is fixed by the requested question.

CLAUDE 3.2 four-part audit (symbol-level closure measurement follows the build):

1. Closure: next_spec lies on the positivity path through step_spec/b_ge_two;
   b_injective is applied by sequence_injective; used_seq rewrites the exact
   set defining sequence_greedy; two_queue_infinite is consumed through
   all_queues_infinite, tail_surjective and queue_exhausts by surjectivity.
2. Not a frozen projection: next_spec constructs an unused multiple above an
   arbitrary finite history. b_injective uses the new history invariant and
   unused minimum. used_seq is an induction on that history. two_queue_infinite
   is the finite-prime-output contradiction; no searched library supplies it.
3. Not definitionally equivalent: candidate membership is not positivity of
   every seq term; injectivity of the tail b is not positive-index injectivity
   including the separate seed; a finite-set identity is not the infimum
   equation; infinite activation of queue 2 is not surjectivity of all integers.
4. Live path: positivity uses nonempty candidate membership, injectivity
   cancels b-values, the greedy equality rewrites its forbidden set, and
   surjectivity actually uses queue 2 to cover the even witnesses forcing all
   queues. Each cited fact remains after discarding unused conjunction fields;
   none is an unrelated constant inserted merely to decorate a dependency list.

Library/Arith was full (48); the already registered Factorization bucket had
1 file and now holds the source note. docs/reports had 45 files before this
report, now 46. The note includes the required Verified locator with the
literal doi and url lines. Scribe uses repo-derived provenance acknowledging
that note, not literature-attested proof. The external conjecture dossier is
an input record, not a theory volume, atom or self-coverage target.

## Canonical build and symbol audit

First `make lean` EXIT=0, 66.621 s, 12832 jobs; target newly built in 5.2 s.
Other builds were the cache's bounded delta to this pinned tree, not target
proof dependencies. First `make lean-report` EXIT=0, 63.185 s; delta plan
changed=0, added=1, removed=1, recheck=1 against the donor report.
Both LEAN_CACHE receipts: status=present, method=none, stamp_miss=null,
project_olean_state=warm, mathlib_olean_state=warm, zero missing Mathlib oleans.
These are macOS ARM warm-tree observations, not CI timing claims.

The Lean environment audit `/tmp/A363956Audit.lean` traverses elaborated
constant types and proof values with getUsedConstants, restricted to this
namespace. It returned EXIT=0 and confirmed all four witnesses:
sequence_positive -> next_spec (16 namespace constants),
sequence_injective -> b_injective (24), sequence_greedy -> used_seq (16),
a363956_surjective -> two_queue_infinite (52).
The first audit attempt used ConstantInfo.value?'s default allowOpaque=false,
which omits theorem proof values and reported a missing witness. Reading Lean's
Declaration.lean identified this API setting; enabling allowOpaque=true resolved
it. This was an audit-program error, not a missing proof dependency.
The query proves reachability; live-path and non-projection classifications
remain the explicit semantic assessment above, not automatic classifications.

Before freezing, shortened the digest and made the seed-history simplification
terminal to remove the target's two build-linter warnings. Final build/report
receipts follow. The initial source and audit failure logs remain in the runner
attempt directory alongside per-stage command/exit/seconds JSON receipts.

Final `make lean` EXIT=0, 14.752 s (target 5.0 s), 12832 jobs; no target warnings.
Final symbol audit EXIT=0, 7.502 s, same four confirmed dependency paths.
Pre-PR fresh dev = e1128e488f6d7585f7425d4f022749799c3657f6;
`git grep -n -P 'A363956|a363956_surjective|OmegaGreedyPermutation' origin/dev -- D5`
returned no hits. `git merge-tree --write-tree HEAD origin/dev` EXIT=0,
hypothetical union tree aebab08e182607c7ce9e97d281461a5015a125c1, no conflicts.
The original immutable base remains the validation input; no base code executed.

## Canonical declaration identities

Final `make lean-report` EXIT=0, 54.948 s, recheck=1.
The report's 74 declaration records (including generated implementation records)
all have standard axioms. The five explicitly authored public theorems are:

| Selector | statement_id |
| --- | --- |
| sequence_greedy | sha256:402e649370f63b2e77e08a892d3bee3756f9e3e807a31c96ea7025b7cde1f769 |
| sequence_initial | sha256:51809f6d9a3b0071634e0cc1bd3206596cb9d18e5583e72e64b123618847b6a6 |
| sequence_positive | sha256:efc7b06460aa0798f7c43e64d4baaec07baa6db6c1b350b26ceaf3204ff47ee9 |
| a363956_surjective | sha256:674af55eebfd133c5ecdcc9b336bba9e9256f0005964c170962e2a848f8478b3 |
| sequence_injective | sha256:69afeaf79a14df4867842fa82c0d1db20edd9dc7035bc2e254ee1b18c4879a5b |

Final Lean source hash: sha256:de1ddab8fa3f03c64201a1e5801c83351c67fdc9b0b889bfbda8fc9a7f892058.

Initial make emit EXIT=2, 26.713 s: invalid-problem-resolution-source and
 dangling-problem-gid correctly reject a problem resolution before its theorem
is frozen. Resolution binding and its problem dossier are temporarily staged
outside emission, then restored after the canonical no-atom freeze. No Lean
statement, axiom, gate, atom or truth criterion is changed.

Freeze-preparation `make emit` EXIT=0, 66.311 s, one changed Blueprint.
The source proof is final; pending operations only freeze it and attach the
external problem resolution. This checkout has a canonical `make deposit-uncovered`
entry (unlike the older report used as a reference). It invokes header validation
and `ledger-align --add` with no atom, so that is the chosen make entry.

## Freeze receipt

`make deposit-uncovered` EXIT=0, 84.560 s, against the original immutable base.
Header check passed; ledger-align --add: selectors_considered=3946,
changed=0, added=1, unchanged=3945, conflicts=0. No atom or coverage edge.
Freeze event: sha256:a3af4a918a9b33cef98b8276b94cbe3323f951d686dfd75c18274382a245756b.
State pin: sha256:3ce2463e1ef5b6f35b21461a1f962785595b0e75428b56ecba964154db28cb6e.
Frozen prerequisite node IDs: [].
The final problem dossier and resolution binding are restored for final emission.

## Final Scribe checks

Final `make emit` EXIT=0, 61.519 s, with the restored problem resolution and one
changed Blueprint. The emitted document states the five verified theorems and
the finite-prime contradiction, acknowledging the OEIS conjecture as its source.

`bash tools/scripts/workflow/scribe-content-checks.sh
.lake/build/stratalint/raw-lean-report.json ""
d59adb46d4703e7fdc7ef7569c5c0919247cc87a` EXIT=0, 23.570 s.
The script selected `describe-report --check` and `markdown-check`; it did not
select `projections --check`, because this patch changes no projection rule or
producer input. Describe: nodes=10403, red=0, observe=4813. Markdown: judged=1,
formula(s)=5, red=0, using the repository's real KaTeX checker. Describe also
emits OPEN projection observations, including this module; its EXIT=0 is not a
claim that the global projection registry has no OPEN observations. The final
emitted theorem document marks all five statements as verified with std3.

Delivery status at this checkpoint: theorem proved and frozen, local required
task checks passed; PR creation and independent review are still pending.
Earlier pending and unclaimed entries above are chronological checkpoints,
superseded by these verified results. No claim is made about the Omega variant,
growth, fixed points, exhaustive literature priority, or the user's numerical
experiment. Linked OEIS data/plots and revision histories remain
ASSUMED-UNVERIFIED. No new theory volume, atom, or finite-example deposit exists.

## Implementation handoff

PR: https://github.com/the-omega-institute/trureturing/pull/6736
(`lane/math/a363956` -> `dev`), opened via `make pr-open` after the local Scribe
check and push. Creation head: d6145eeb516388e253cebeb46d9a8103aaf8bdd3.
Readback: OPEN, autoMergeRequest=null; engineering and canonical Lean-report
checks IN_PROGRESS at the creation receipt. Required CI has not yet been
reported green here. The PR is not merged and no independent review is claimed.

Implementation result under this task's explicit criterion: 成 — the requested
unconditional surjectivity theorem is proved, `make lean` EXIT=0, no sorry or
private axioms, frozen artifact published, and PR opened. This is an
implementation-stage result, not a claim of completed repository merge or
independent consensus. The outer runner owns subsequent review and termination.

All runner-owned delivery paths are under the attempt-1 directory supplied in
the task. The worker publishes a complete result.json object followed by
completion.sentinel using temporary-file atomic renames; final CI/head
readbacks and individual command receipts are attached there.
