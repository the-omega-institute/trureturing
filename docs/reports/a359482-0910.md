# A359482 implementation record (2026-09-10)

## Origin and scope

Lean skill (`/Users/chronoai/.codex/skills/lean4/SKILL.md`); Codex implementation
seat, single-agent work and self-checks. The external orchestrator supplied the
brief and earlier probes; those are attributed evidence, not this seat's runs.
Independent review remains the orchestrator's responsibility.

Branch: `lane/math/a359482`; immutable base:
`48e95107100e503cdc52aa44cc15165b65fc3213`.
Tier 1. Question: is OEIS A359482 a permutation of the positive integers?
Target: `Legal x y := D(x+y).IsInfix (D(x) ++ D(y))`, with most-significant
digit first and no leading zero; prove no positive single-digit successor,
then the tail bound and omission of 2 for the original least-unused greedy rule.

## Preregistered proof and stop conditions

Proposed witness (from the brief, not yet Lean-verified): an occurrence of an
L-digit sum in the L+1-digit concatenation is a prefix or suffix. The suffix
forces `D * 10^(L-1) = 9*r`; modulo 9 and the leading-digit/remainder bounds
contradict this. The full-length occurrence forces x=0. Positive controls
`(1,10)`, `(10,99)`, `(99,889)` must pass before the general proof is attempted.
No enumeration is a public result. No self-authored theory volume or atom.
If substring/leading-zero interfaces or L=1 cannot be closed, report blocked
with the sharp remaining lemma rather than claiming finite partial progress.

## Search receipts

1. Fully read `CLAUDE.md` (779 lines), `agents/CONTEXT.md`, and spec A5/A5.1.
2. `rg -n -i 'a359482|no_small_successor|digits.*infix|infix.*digits' D5 Library Blueprint`:
   no D5 proof; only `Library/Words/oeis2026triage0910.md` triage record.
   Broader `digits|Infix|concatenat` scan found digit infrastructure, not this theorem.
3. OEIS internal page fetched successfully on 2026-09-10:
   `https://oeis.org/A359482/internal`. It defines the correct substring direction,
   asks the permutation question, and records Hasler's July 3, 2023 conjecture
   that 1 is the only single-digit term. It links Angelini's blog and Havermann's
   term file; no proof is present in the entry. Snapshot: `/tmp/a359482-oeis.html`.
4. Mathlib commit independently read as
   `db584cd6d46c92f209a44c0f1c829460d327499d`. Searches in
   `Data/Nat/Digits/{Defs,Lemmas,Div}.lean`, `Data/List/Infix.lean`, and Lean's
   `Init/Data/List/Sublist.lean` found `le_length_digits_le`, `ofDigits_append`,
   `ofDigits_lt_base_pow_length`, `digits_append_digits`, `IsInfix.reverse`,
   `infix_cons_iff`, `IsInfix.eq_of_length_le`; none is the target theorem.
   One initial search named a nonexistent `Digits/Basic.lean`; corrected to `Defs.lean`.
5. GitHub code search `A359482 language:Lean` returned `[]`; repository search
   `A359482` returned total_count=0, incomplete_results=false. arXiv API query
   `all:A359482` returned totalResults=0. These bounded identifier searches do
   not establish exhaustive absence. Network search capability was exercised.
6. Angelini blog fetched and F section plus Hans/Hasler corrections read:
   `https://cinquantesignes.blogspot.com/2023/07/echecs-et-maths.html`.
   The original 899 is corrected to 889, followed by prefixes and PARI code;
   no single-digit exclusion proof. Snapshot `/tmp/a359482-blog.html`.
   No foreign program was executed. Search outcome: not-found-in-searched-scope.

## Verification

`make lean-cache-ensure` EXIT=0 before any lake invocation. LEAN_CACHE:
status=seeded, method=clonefile, donor=/Users/chronoai/trureturing,
clonefile_attempts=1, stamp_miss=null, mathlib_missing_olean_files=0,
project_olean_state=warm, mathlib_olean_state=warm,
archive_skip_reason="project olean state is warm".
At the initial checkpoint no Lean theorem had been verified.

The first Lean check of the definitions and `legal_controls` exited 0: all
three required positive controls passed using `decide`. General proof work
started only afterwards. The first general-proof draft failed on simplifier
normalization of singleton/empty appends and ofDigits, and on a list lemma
name. Those failures are not mathematical counterexamples.

The corrected general proof exited 0. `#print axioms no_small_successor`
reports exactly `[propext, Classical.choice, Quot.sound]`. Its remainder list
may be empty, so L=1 is included. Leading-zero exclusion is not assumed in
the arithmetic contradiction: the putative leading digit 0 forces x=0.

## Sequence totality preregistration

Before implementing the greedy sequence, register its required totality
argument: put P=10^(length D(x)), R(0)=x, R(k+1)=x+P*R(k). The decimal word
of R(k) repeats D(x), so y=P*R(k) is a legal successor: x+y has k+2 copies
of D(x), and the concatenation has those copies followed by length D(x)
zeros. These successors are unbounded, hence one avoids every finite history.
This is an additional necessary construction, not an assumption of no small
digits. Then take the natural infimum of positive unused legal successors.

The complete sequence implementation now passes `lake env lean` on the warm
tree (EXIT=0). Six public theorems all report exactly the standard three
axioms. `sequence_greedy` explicitly identifies the infimum's forbidden set
with all earlier indexed terms; `sequence_pos` and `sequence_legal` discharge
the recursion's existence obligations. The tail bound and missing-2 proof
are unconditional. Two initial totality proof errors were API use only:
`positivity` does not solve `1 ≤ P` directly, and `sInf_mem` needed its set
argument specified. Both were corrected, with no axiom or sorry added.

After removing an unused private helper and narrowing the tactic import,
`make lean` EXIT=0 in 14.843 s; 12820 jobs, only the target module newly Built
(6.0 s). This is macOS ARM warm-tree evidence, not a CI timing claim.
`make lean-report` EXIT=0 in 75.862 s. Its delta rechecked 12 modules against
the donor report. Both LEAN_CACHE receipts show status=present, method=none,
stamp_miss=null, project and Mathlib warm, zero missing Mathlib oleans.
Logs and JSON timing receipts live under the runner attempt directory.

Capacity measured before additions: D5/S3/Arith 32 direct files;
Blueprint mirror 27 Scribe definitions; docs/reports 40 direct files.
Library/Arith already has 48 files, so the decimal-word source note uses the
registered Words domain (24 direct files before the addition). No domain
registration or existing frozen module is changed.

## Declaration audit

The module's admission_basis is `escape-witness`. Its direct pre-existing
frozen dependencies are `[]` for every public theorem (hence no omitted
GID/statement_id pair): canonical report imports are only Init and Mathlib.
Exact public GIDs use the prefix `D5/S3/Arith/SumInConcatenation.`; the identity
table below gives the complete statement_id for each selector. Same-module
companion edges are explicitly listed here, always consumer -> prerequisite.

| Public theorem | proof_shape | escape_witness | admission_basis / obligation |
| --- | --- | --- | --- |
| no_small_successor | content | shifted_digits_impossible | escape-witness; requested universal exclusion |
| sequence_greedy | content | state_used | escape-witness; faithful least-unused definition |
| sequence_pos | content | arbitrarily_large_successor | escape-witness; total positive recursion |
| sequence_legal | content | arbitrarily_large_successor | escape-witness; actual adjacent rule |
| sequence_tail_ge_ten | bind-only | null | companion under escape-witness; requested tail bound |
| not_positive_permutation | bind-only | null | companion under escape-witness; requested non-permutation |

The last two classifications conservatively treat this module's new public
prerequisites as available: `sequence_tail_ge_ten -> no_small_successor,
sequence_pos, sequence_legal`; `not_positive_permutation -> sequence_tail_ge_ten`.
They are not independent deposits. Relative to the protected base alone,
their expanded closures also contain the new exclusion and totality proofs.
`sequence_greedy` accompanies the same seq definition used by the tail theorem;
its obligation is the brief's explicit prohibition on defining seq by the result.

Four-part witness audit (CLAUDE 3.2):

1. **Closure:** a Lean environment query in `/tmp/A359482DependencyAudit.lean`
   traversed elaborated type/value constants using `getUsedConstants`, within
   the target namespace. EXIT=0. It found shifted_digits_impossible for
   no_small_successor, state_used for sequence_greedy, and
   arbitrarily_large_successor for both sequence_pos and sequence_legal.
2. **Not a frozen projection:** no pre-existing D5 theorem is imported.
   The first witness combines the two positional evaluations with the mod-9
   obstruction; state_used is a new induction identifying a recursive set;
   totality constructs repeated decimal words of unbounded size. The searched
   Mathlib APIs supply arithmetic and list operations, not these statements.
3. **Not definitionally equivalent:** shifted_digits_impossible concerns a
   particular digit-list decomposition, not arbitrary substring exclusion;
   state_used concerns a finite set, not the infimum equation; unbounded legal
   successors concern arbitrary positive x and bounds, not the greedy seq.
4. **Live path:** the suffix branch ends by applying shifted_digits_impossible
   to the extracted lists. The greedy equation rewrites its forbidden set by
   state_used. Both positivity and legality obtain candidate membership from
   Nat.sInf_mem, whose nonempty-set argument is the constructed unbounded
   successor outside used. These uses survive removing unrelated conjuncts;
   no unused witness is inserted merely to decorate a dependency closure.
   The namespace query establishes reachability, not a machine classification
   of all four semantic conditions; the latter are this seat's stated review.

Computational classification is `none`: every public result is unbounded
symbolic arithmetic, recursive-set reasoning or its direct corollary, with
no bounded enumeration, reusable checker, numeric reduction, or certified
finite instance as delivered content. The sole finite probe is private
legal_controls; it is not a public theorem or independent deposit.
`generality: G` records independence from the quadratic discriminant and
unbounded inputs; decimal radix 10 is fixed by the user's question.

## Not claimed

No claim of exhaustive literature coverage or priority; no proof of the
stronger assertion about the unique two-digit multiple of ten; no finite
search promoted to universal proof. Angelini's blog and third-party search
were subsequently opened by this seat as recorded above. Havermann's external
100000-term file and the OEIS revision history remain unopened (ASSUMED-UNVERIFIED).

## Canonical public declaration identities

All six public theorems have only `Classical.choice`, `Quot.sound`, `propext`.

| Declaration (prefix D5/S3/Arith/SumInConcatenation.) | statement_id |
| --- | --- |
| sequence_pos | sha256:31cd11f5a62fc5e9dc9a95ce41a78482062b9bbcb76ede05c9c30aee7ee8d8dd |
| sequence_legal | sha256:2991b094b1049c718172df3ba15b45fdcede2ca5d73832fa0f9f0211644b95c1 |
| sequence_greedy | sha256:a3f01af42e983c27daffd539d5a01f871f157a68b054f4c7326fea58f74c770f |
| no_small_successor | sha256:1bbbbccf15b24c1fe2a6628b6e53423d2afb941acbb42339ecdd8b96c14ff757 |
| sequence_tail_ge_ten | sha256:39a0e54edfbb0268f949028b6a051c5de74566b55be1163059a033151e918961 |
| not_positive_permutation | sha256:0a3e598af387aeb1e97a6ed8ccfa1c4465a6c5153fb5bfe9c9102b84890f841c |

## Freeze and route receipts

Canonical route for the typed manifest returned the chosen S3/Arith path and
G header. `make emit` EXIT=0 in 64.721 s.
`deposit-header-check` EXIT=0 in 9.985 s against the immutable base;
`ledger-align --add D5/S3/Arith/SumInConcatenation.lean --candidate-lean-report .lake/build/stratalint/raw-lean-report.json`
EXIT=0 in 10.231 s: selectors_considered=3934, changed=0, added=1,
unchanged=3933, conflicts=0. This is the authorized no-atom deposit route;
the make wrapper would require an atom and cover it, so it was not invoked
with a fabricated atom. No theory volume, atom, or coverage edge was created.

After freezing, added the canonical external problem dossier and its Scribe
resolution binding (`refuted` for the permutation question). Re-emission
EXIT=0 in 62.879 s. This adds a reference to the already verified theorem,
without changing Lean or manufacturing an input theory volume.

Pre-PR duplicate scan: freshly fetched dev
`dc99e08f407dffa7c507a1daf43e8da5872faa24`; `git grep -n -P
'A359482|no_small_successor|SumInConcatenation' origin/dev -- D5` has no hits.
`git merge-tree --write-tree HEAD origin/dev` EXIT=0, no conflicts, hypothetical
union tree `17c6b8348053b1acd1bf0a6ec6854468232c15e2`. No base code was executed;
the original immutable protected base remains the validation input.

## Scribe content checks

After the final emission, ran exactly:
`bash tools/scripts/workflow/scribe-content-checks.sh .lake/build/stratalint/raw-lean-report.json "" 48e95107100e503cdc52aa44cc15165b65fc3213`.
EXIT=0 in 25.309 s. The canonical selector ran describe-report and the real
KaTeX markdown check for the changed Blueprint. The projection-rule inputs
were unchanged, so the script did not select projections --check. No RED
finding was present. The source note contains the mandatory Verified locator
section and verbatim `doi: null` and `url: https://oeis.org/A359482` lines.
No preflight was run. Final source hash recorded by the canonical report:
`sha256:a8794aa4a25f773b88ea3875ebdb63a1d6d8b28a927afbea5f14065a8de006f7`.

## Delivery boundary

The requested implementation criterion is a verified, frozen change with an
opened PR. PR [#6710](https://github.com/the-omega-institute/trureturing/pull/6710)
was opened by `make pr-open HEAD=lane/math/a359482 MESSAGE=/tmp/a359482-pr.md`.
No merge is requested or claimed; autoMergeRequest is null. Independent review
belongs to the outer orchestrator. At the first GitHub readback, engineering
and canonical Lean report checks were IN_PROGRESS; this is not a CI-green
claim. The final observed CI result is recorded in the runner result envelope.

Freeze event: `sha256:d60a58e4f1c8a43fb3e84e15f797758c56005a9ac5b2eed90f62e078d77e0452`.
State pin: `sha256:7007c75fa76bdc89fa6b379b7bd9a73a3e7f4bd39aa02c1f5758bd2f0cb17277`.
