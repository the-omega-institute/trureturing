# 5040 mean/variance envelope: bounded Arb evidence

Date: 2026-09-08. Scope: the single specified input
`((2,4),(3,2),(5,1),(7,1))`, with no candidate generation or range scan.
This supports sections 15-17 of
[ARITHMETIC_BOUNDARY_QUANTIZATION.md](../develop/theory/ARITHMETIC_BOUNDARY_QUANTIZATION.md).
It is numerical-library evidence, not a Lean certificate or an RH result.
5040 is outside Robin's strict `n > 5040` domain.

Provenance: caller-supplied actual GPT PRO task
`14803801-6a68-4b5a-8a76-f6615d838e88`, returned model `chatgpt-5.5-pro`,
completed `2026-09-08T13:17:09.406+00:00`, supplied the primary paper argument.
The caller also supplied a 256-bit check. This `consensus-rnd:sshx`
implementation worker independently recomputed this input with the code below;
it did not act as an independent reviewer. Codex is repo-prior-exposed;
no sterile-prior or model-family diversity claim is made.
Source base: `0b8b5592d56d601d92db36b5f7ef331ef3ed33f1`.

Exact arithmetic gives `n=5040`, `sigma=19344`, `sigma/n=403/105`, `E_S=35/8`.
Natural logs, `k=4`, and `x_p=log(p^(a_p+1))=(a_p+1)log(p)` are used.
`mu=sum(x_p)/k`, `V=sum((x_p-mu)^2)` is the sum of squared deviations,
not the averaged variance. Put `B=exp(mu)`, `r=sqrt(V/(k*(k-1)))`,
`L=mu-r`, `H=mu+(k-1)*r`. The compared quantities are

```text
exact     = 403/105
Jensen    = E_S * (1 - 1/B)^k = exp(J)
quadratic = Jensen * exp(-B*V/(2*k*(B-1)^2))
sharp     = E_S * (1 - exp(-H)) * (1 - exp(-L))^(k-1)
```

The executed environment was Python 3.12.13, Python-FLINT 0.8.0, Arb precision
256 bits. The following table gives strict rational enclosures `a/d < z < (a+1)/d`,
where `d=10^50`. Each endpoint was certified by Arb comparison, without parsing
the human-readable ball display. Integer extraction used `floor().unique_fmpz()`.

| z | Lower numerator a |
|---|---:|
| mu | 346806722294572074517103011784929867316133612824257 |
| V | 27133175249332569671634728430586873423713121031745 |
| L | 331769774673979719717219590163822109430996798911538 |
| H | 391917565156349138916753276648253140971544054562411 |
| exact | 383809523809523809523809523809523809523809523809523 |
| sharp | 383865586689570166735893845850881054340607342920482 |
| quadratic | 385004796618310708532303878695076948942695007873789 |
| Jensen | 385438777199267676938187661368470637018253489457890 |

Arb directly certified `exact < sharp < quadratic < Jensen` and `L > 0`.
The independently formed rational endpoints also strictly separate the four
values. This agrees with the supplied caller check. The code's exit status was 0.
Trust includes Python, Python-FLINT and FLINT/Arb directed ball arithmetic;
the finite result does not certify the general paper theorem or any Robin range.

Run this complete bounded calculation from any directory with `uv` available:

```sh
uv run --no-project --python 3.12 --with python-flint==0.8.0 python - <<'PY'
from fractions import Fraction
from importlib.metadata import version
from math import prod
import platform
from flint import arb, ctx

assert version('python-flint') == '0.8.0'
ctx.prec = 256
pa = ((2, 4), (3, 2), (5, 1), (7, 1))
n = prod(p**a for p, a in pa)
sigma = prod((p**(a + 1) - 1) // (p - 1) for p, a in pa)
ratio = Fraction(sigma, n)
assert n == 5040 and sigma == 19344 and ratio == Fraction(403, 105)
k = len(pa)
x = [arb(p**(a + 1)).log() for p, a in pa]
mu = sum(x, arb(0)) / k
V = sum(((t - mu)**2 for t in x), arb(0))
B = mu.exp()
r = (V / (k * (k - 1))).sqrt()
L, H = mu - r, mu + (k - 1) * r
assert V > 0 and L > 0
E = prod((Fraction(p, p - 1) for p, a in pa), start=Fraction(1))
Eball = arb(E.numerator) / E.denominator
exact = arb(ratio.numerator) / ratio.denominator
jensen = Eball * (1 - 1 / B)**k
quadratic = jensen * (-B * V / (2 * k * (B - 1)**2)).exp()
sharp = Eball * (1 - (-H).exp()) * (1 - (-L).exp())**(k - 1)
assert exact < sharp and sharp < quadratic and quadratic < jensen
values = {'mu': mu, 'V': V, 'L': L, 'H': H, 'exact': exact,
          'sharp': sharp, 'quadratic': quadratic, 'jensen': jensen}
d = 10**50
bounds = {}
for name, value in values.items():
    a = (value * d).floor().unique_fmpz()
    assert a is not None
    a = int(a)
    assert arb(a) / d < value and value < arb(a + 1) / d
    bounds[name] = (Fraction(a, d), Fraction(a + 1, d))
    print(name, a, a + 1)
assert ratio < bounds['sharp'][0]
assert bounds['sharp'][1] < bounds['quadratic'][0]
assert bounds['quadratic'][1] < bounds['jensen'][0]
print('n=', n, 'sigma=', sigma, 'sigma/n=', ratio, 'E_S=', E)
print('python=', platform.python_version(), 'python-flint=', version('python-flint'),
      'precision_bits=', ctx.prec, 'denominator=', d)
print('PASS: exact < sharp < quadratic < Jensen; Arb and rational endpoint checks')
PY
```

The distance certificate's practical advantage over the optimal separable
discrete Lagrange dual remains OPEN. The supplied follow-up tasks
`949095a4-d3a7-4179-b842-f255db211159` and
`db06ed96-26f3-47ff-90eb-db97ec4de9d6` returned only carrier failures
(`prompt_delivery_uncertain`; `page.goto Page crashed`), no mathematical result.
This report records no GPU run or search prediction as verified.
