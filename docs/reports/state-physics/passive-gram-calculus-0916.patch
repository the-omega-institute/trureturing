diff --git a/docs/develop/theory/CONTEXTUAL_SPACETIME_ARITHMETIC_ML.md b/docs/develop/theory/CONTEXTUAL_SPACETIME_ARITHMETIC_ML.md
--- a/docs/develop/theory/CONTEXTUAL_SPACETIME_ARITHMETIC_ML.md
+++ b/docs/develop/theory/CONTEXTUAL_SPACETIME_ARITHMETIC_ML.md
@@ -372,5 +372,236 @@
 证明。对三因子逐项求导即得。定理15.2已经给不能消去的具体见证。继续加入新导数可以形成候选闭包塔，但终止还需要函数代数的不变性或实际纤维条件；不能从“再加一层导数”推断任意网络有限闭合。证毕。
 
+## 15A. 微分流、有限步算法与固定标签下的被动 Gram 识别
+
+本增补接第12节的实际两层 Gram 更新与第15节的切线观察边界。固定参考快照为 `e67699dc905b37ef05859ed0075831fd01b1e1ce`，本卷 blob 为 `fb5b3308f198ffd9c7511edefd732cdc69342e99`。本节为普通数学证明，未取得 Lean 内核验证。原 #8029 于2026-09-15关闭且未合并，其候选代码不作为本节的已证前置，也不在本次交付中复制。续推登记见该 PR 评论5684076786；登记晚于初步普通推导与一个示例，早于本轮完整诊断。
+
+### 15A.1 微积分的三个不同接口
+
+完整状态的局部 Lipschitz 向量场与给定初态，在相应存在区间确定唯一积分曲线。一个状态处的导数、经过非单射观察后的导数，以及整个向量场的数据量不同。另一个独立区别是：给定微分方程的精确流与其数值步进通常不同。
+
+例如对足够光滑的矩阵轨道，乘积的 Taylor 公式为
+
+$$
+V(t+h)U(t+h)=VU+h(\dot VU+V\dot U)
++\frac{h^2}{2}(\ddot VU+2\dot V\dot U+V\ddot U)+O(h^3).
+$$
+
+同步参数 Euler 步给出的乘积则精确为
+
+$$
+(V+h\dot V)(U+h\dot U)=VU+h(\dot VU+V\dot U)+h^2\dot V\dot U.
+$$
+
+这些等式没有断言微积分遗漏信息；它们明确了所保留的对象。若真实任务就是离散梯度下降，有限步项属于任务本身。若真实任务是连续物理方程，则数值步进产生的可见差异还需通过离散化误差控制，才能解释为物理信号。
+
+有限步长可以破坏梯度流中的守恒律，已有 Kunin 等的 Neural Mechanics 及 Barrett–Dherin 的 backward error analysis；这不是本节的新颖性主张。本节求解固定训练任务中的具体观测纤维。
+
+### 15A.2 固定零标签的实际学习状态
+
+固定平方外部维数 q≥1，隐藏宽度 m≥1，U∈R^(m×q)、V∈R^(q×m)。采用同一个训练集 X=I_q、Y=0，同一个未平均损失
+
+$$\mathcal L(U,V)=\frac12\|VU\|_F^2.$$
+
+所有训练步都使用这一个损失；没有重置、改标签或自由选择梯度。步长 h_n>0 已知。读数为完整输出矩阵 W_n=V_nU_n，可由固定基向量输入取得；矩阵读数与单个标量读数的费用应分别计算。
+
+令 A=U^TU、B=VV^T、W=VU，Γ=[[A,W^T],[W,B]]。实际初态域恰为
+
+$$\Gamma\succeq0,\qquad \operatorname{rank}\Gamma\le m.$$
+
+反向由半正定矩阵因子分解 Γ=Z^TZ、Z=[U,V^T] 得到真实参数，必要时补零行。第12节的更新取 G=W，给出
+
+$$
+\begin{aligned}
+A^+&=A-2hW^TW+h^2W^TBW,\\
+B^+&=B-2hWW^T+h^2WAW^T,\\
+W^+&=W-h(BW+WA)+h^2WW^TW.
+\end{aligned}\tag{15A.1}
+$$
+
+定义 H(W)=[[0,W^T],[W,0]]、C_h(W)=I-hH(W)，则同步参数更新的同一个 Gram 满足 Γ^+=C_h(W)ΓC_h(W)^T。这一合同式和实际像条件均为已有方法。本节不把它们另立成新的 Lean 包装。
+
+### 15A.3 固定损失中的四次可见信号
+
+**命题15A.1。** 两个实际状态有相同 W，且 A_2=A_1+cI、B_2=B_1-cI。设
+
+$$L=B_1W+WA_1,\qquad \mathcal C=WW^TL-LW^TW.$$
+
+先以步长η更新，再以步长τ更新，且两步均使用固定零标签损失，则
+
+$$\boxed{W_2^{(2)}-W_2^{(1)}=c\eta^3\tau\mathcal C.}\tag{15A.2}$$
+
+**证明。** 第一步 W_1 相同，因为标量平移在 B W+W A 中相消。两个后继对角块之差为 c(I-η²W^TW) 及 -c(I-η²WW^T)。第二步的梯度就是共同 W_1，故由(15A.1)直接相减，得到
+
+$$\Delta W_2=c\eta^2\tau(W_1W^TW-WW^TW_1).$$
+
+代入 W_1=W-ηL+η²WW^TW，零次与二次项分别抵消，括号等于η𝒞。证毕。
+
+等步长 h 时信号为 c h^4𝒞。这里的四次是离散算法对步长的依赖，不是连续输出轨道的第四阶时间导数。该公式包含𝒞=0、c=0和所有退化参数。
+
+作为对照，J=diag(I,-I) 满足 H(W)J+JH(W)=0。连续 Gram 方程 Γdot=-H(W)Γ-ΓH(W) 对 Γ→Γ+cJ 不变，且该平移不改变 W。实际梯度流由多项式向量场给出局部唯一解；共同存在区间上平移关系保持，完整连续输出相同。因此不能把离散可识别性反推成连续流的可识别性。
+
+**条件风险。** 若 W,A_1,B_1 已知，只有 c 未知且真实可行 c 域恰为长度ℓ的闭区间，第二步输出具有 Frobenius 噪声预算ε，则当𝒞≠0时，估计 c 的精确最坏风险为
+
+$$\min\left\{\ell/2,\frac{\epsilon}{|\eta|^3|\tau|\|\mathcal C\|_F}\right\}.$$
+
+证明：把去基线后的输出投影到𝒞，得到标量有界噪声，取相容区间中点达到上界；用两个合法 c 的输出中点作为共同数据达到下界。𝒞=0时风险为ℓ/2。该结论不覆盖其他 Gram 坐标也未知的端到端估计。等步长的逆放大为 h^-4，步长越小并不自动使识别更稳。
+
+### 15A.4 三个被动训练步的精确识别定理
+
+以下不预先知道 A、B 或完整瞬时切核。只观察 W_0,W_1,W_2,W_3，使用已知非零步长η_0,η_1,η_2。
+
+固定 W_0 的一个奇异值分解 QΣP^T，将所有 W_n 同时变换为 Q^TW_nP，将 A、B 变换为 P^TAP、Q^TBQ。以下公式均在这一共同坐标中。假设Σ=diag(σ_i)，所有σ_i>0，σ_i²两两不同。令
+
+$$d_i=1-\eta_0^2\sigma_i^2,\qquad d_i\ne0.$$
+
+在{1,...,q}上定义无向图：i≠j间有边，当且仅当 (W_1)_ij 或 (W_1)_ji 非零。记其连通分量数为r。
+
+**定理15A.2（前缀的完整相容方向）。** 在相同 W_0 的对称 Gram 对角块上：
+
+1. 相同 W_1 当且仅当初始差为 ΔA=diag(x_i)、ΔB=-diag(x_i)。
+2. 再要求相同 W_2，当且仅当 d_i x_i 在该图的每个连通分量上为常数。因此线性相容方向的维数恰为r。
+3. 图连通时，前 W_0,W_1,W_2 的全部实际竞争初态恰为
+
+$$A_c=A_*+cK,\qquad B_c=B_*-cK,\qquad K=\operatorname{diag}(d_i^{-1}),\tag{15A.3}$$
+
+其中 c 还须满足 Γ_c 半正定及其原宽度秩约束。定义完全可由读数计算的
+
+$$S=W_2W_1^TW_1-W_1W_1^TW_2.$$
+
+这条纤维上，第三步输出差精确为
+
+$$\boxed{W_3(c)-W_3(0)=c\eta_1^2\eta_2 S.}\tag{15A.4}$$
+
+特别地，图连通且 S≠0 时，四次输出唯一确定初始 A、B。随后全部固定任务的输出未来由(15A.1)决定。
+
+**证明。** 首步相同等价于 ΔBΣ+ΣΔA=0。对 i≠j，利用两矩阵对称，两个条目方程给
+
+$$\sigma_j\Delta B_{ij}+\sigma_i\Delta A_{ij}=0,
+\qquad\sigma_i\Delta B_{ij}+\sigma_j\Delta A_{ij}=0.$$
+
+系数行列式σ_j²-σ_i²非零，故两个非对角元均为零。对角方程给ΔB_ii=-ΔA_ii。反向直接代入，所以第一项是充要条件。
+
+首步之后对角块差为 D=diag(d_i x_i) 和 -D。第二步相同等价于 DW_1-W_1D=0，即对每条边要求 d_i x_i=d_j x_j。该条件恰在各连通分量上常值。d_i非零使每个分量对应独立初始方向，得到第二项。
+
+连通时D=cI，初始差恰为(15A.3)。此时两个系统在时刻1具有标量差 cJ，时刻2读数相同。再次用(15A.1)相减，时刻2对角差为 c(I-η_1²W_1^TW_1) 和 -c(I-η_1²W_1W_1^T)。时刻3差即(15A.4)。S≠0迫使 c=0，完成识别。证毕。
+
+**最小步数。** 若 m≥2q 且基准Γ_*正定，沿(15A.3)足够小的正负 c 都仍正定并具有真实参数因子。它们共享 W_0,W_1,W_2，所以两个被动训练步不足；在定理的连通性及S条件下，三个步足够。这里最小的是该实际模型族的训练深度，不是黑盒标量查询次数。
+
+本定理的条件不应删除。重复或零初始奇异值、首步共振使上面消元失效，但不因此自动推出永不可识别。断连时每个分量有独立残余，后续还需分别检查。S=0仅说明该剩余方向在第三步尚未出现，不能推成所有未来不可见。在半正定边界或较窄宽度上，实际可行方向也可能少于线性相容方向。
+
+### 15A.5 完整保留不可识别情形
+
+若W_0=0，固定零标签损失的参数梯度全为零，任意合法 A、B 都产生恒零输出。这是真实被动实验的永久障碍。
+
+若在共同奇异坐标中 A、B、W 全对角，更新始终逐坐标解耦。每个坐标的 w、s=a+b 满足
+
+$$w^+=(1+h^2w^2)w-hws,\qquad s^+=(1+h^2w^2)s-4hw^2.$$
+
+所以每个初始 a-b 都可能在同一 (w,s) 纤维内永久不可见。连通图条件没有掩盖这些情形，它说明为什么信息必须通过非对角耦合才能从各分量传回来。精确非零性不提供统一稳定下界；任意小的耦合可以使实际逆问题病态。
+
+### 15A.6 任意谱与秩上的精确轨迹可行集
+
+对任意给定的精确候选轨迹 W_0,...,W_N 和已知步长 h_0,...,h_(N-1)，令
+
+$$P_0=I,\quad P_{n+1}=C_{h_n}(W_n)P_n,\qquad
+\mathcal O_W(X)=\big((P_nXP_n^T)_{21}\big)_{n=0}^N.$$
+
+下标21指q×q的左下块。𝒪_W在线性空间Sym_(2q)上为一个完全由读数定义的线性算子。
+
+**定理15A.3。** 在隐藏宽度m下，恰好产生该精确轨迹的所有真实初始Gram为
+
+$$\boxed{\{X\succeq0:\operatorname{rank}X\le m,
+\quad\mathcal O_W(X)=(W_0,...,W_N)\}.}\tag{15A.5}$$
+
+**证明。** 正向由实际合同更新逐步得到。反向，将任意这样的X因子化成真实U_0,V_0。假设到时刻n实际输出等于给定W_n，那么下一实际更新使用的梯度和C矩阵就与构造P时相同；线性约束给下一输出也相同。n=0由约束直接成立，归纳完成。证毕。
+
+此式适用于重复谱、零谱、共振、断连和边界秩，不用另加耦合非零假设。当m≥2q时秩约束冗余，剩下半正定仿射可行集；小宽度的秩约束一般非凸。该刻画不等于任意规模下已经实现了高效算法。
+
+### 15A.7 含传播矩阵误差的观测后证书
+
+真实读数误差满足 ||Y_n-W_n||_F≤ε_n，初态Γ_0半正定且TrΓ_0≤R，其中R为已知先验。构造
+
+$$\widehat C_n=I-h_nH(Y_n),\quad
+\widehat P_0=I,\quad\widehat P_{n+1}=\widehat C_n\widehat P_n,
+\quad\widehat{\mathcal O}(X)=\big((\widehat P_nX\widehat P_n^T)_{21}\big)_{n=0}^N.$$
+
+选择可计算的 a_n≥||Ĉ_n||_op，例如有理数据下可用 a_n=1+|h_n|∑_ij|Y_n,ij|。设 b_n=|h_n|ε_n，
+
+$$p_n=\prod_{t<n}(a_t+b_t),\qquad\widehat p_n=\prod_{t<n}a_t,
+\qquad e_n=\epsilon_n+R(p_n^2-\widehat p_n^2),\quad
+E=\left(\sum_{n=0}^Ne_n^2\right)^{1/2}.$$
+
+**定理15A.4。** 真实初态对这个带噪声构造的算子满足
+
+$$\|Y_n-(\widehat P_n\Gamma_0\widehat P_n^T)_{21}\|_F\le e_n.$$
+
+**证明。** ||C_n-Ĉ_n||_op≤b_n。有限乘积递推给 ||P_n||≤p_n、||P̂_n||≤p̂_n 和 ||P_n-P̂_n||≤p_n-p̂_n；第三项也可直接由乘积展开得到。PSD和迹界给||Γ_0||_F≤R，故
+
+$$\|P_n\Gamma_0P_n^T-\widehat P_n\Gamma_0\widehat P_n^T\|_F
+\le R(p_n-\widehat p_n)(p_n+\widehat p_n).$$
+
+取输出块，再加原始测量误差ε_n。证毕。
+
+在紧集合 X PSD、TrX≤R、rankX≤m 上最小化 ||𝒪̂(X)-Y||，任一全局最小解记Γ̂。真实Γ_0可行，所以最小值存在且≤E。三角不等式给
+
+$$\|\widehat{\mathcal O}(\widehat\Gamma-\Gamma_0)\|\le2E.$$
+
+若有从观察算子认证的常数s_obs>0，使每个实对称X都满足 ||𝒪̂(X)||≥s_obs||X||_F，则
+
+$$\boxed{\|\widehat\Gamma-\Gamma_0\|_F\le2E/s_{\rm obs}.}\tag{15A.6}$$
+
+这是观测后条件，不是未知真实耦合或奇异值的正下界假设。证书为零或很小时，应拒绝声称精确或稳定全状态恢复。浮点SVD数值本身不是认证下界。
+
+有理数据可构造纯有理证书：在对称整数基 E_ii、E_ij+E_ji 中形成矩阵O，选满列秩方形行子块，并构造左逆L验证LO=I。若M=∑_ij|L_ij|，则对坐标x有 ||X||_F≤√2||x||_2≤2M||Ox||_2，故可取s_obs=1/(2M)。这提供可复验的有理证书，未主张其常数最优。若M=0则非零维度下LO=I不可能，故分母有定义。
+
+**目标版本。** 对线性目标𝒯_T(X)=〈T,X〉_F和任意读数空间向量λ，即使𝒪̂不单射也有
+
+$$\boxed{|\mathcal T_T(\widehat\Gamma)-\mathcal T_T(\Gamma_0)|
+\le2\|\lambda\|E+2R\|T-\widehat{\mathcal O}^{*}\lambda\|_F.}\tag{15A.7}$$
+
+证明：将T写成𝒪̂*λ与其余项，第一项应用2E，第二项应用||Γ̂-Γ_0||_F≤2R。证毕。特别地，若目标方向属于𝒪̂*的像，隐藏状态仍有残余也不妨碍该目标被误差控制。此处未将该普通范数推论重新包装为新形式化声明。
+
+全局最小化的存在性与观测证书均已证明，但尚无端到端求解器、秩约束优化复杂度或有限机器精度求解验证。证书中的误差可以保守，不被称为全模型类的精确极小极大常数。
+
+**可用迹先验。** 在真实零标签任务中，若初态迹≤R且0≤h_n≤4/R，则迹始终≤R。由(15A.1)，
+
+$$\operatorname{Tr}\Gamma^+\le\operatorname{Tr}\Gamma-h(4-h\operatorname{Tr}\Gamma)\|W\|_F^2.$$
+
+这里用Tr(W^TBW)+Tr(WAW^T)≤(TrA+TrB)||W||_F²，归纳即可。上面的传播误差证书只要求初始迹先验，不依赖这个可选步长限制。
+
+### 15A.8 一个正定且损失下降的三步校准
+
+取q=2、m≥4、W_0=diag(1,2)、A_0=[[4,1],[1,4]]、B_0=4I，三步步长均为1/16。实际Γ_0严格对角占优且正定。首步
+
+$$W_1=\begin{pmatrix}129/256&-1/16\\-1/8&33/32\end{pmatrix}.$$
+
+连通图为一条边，d=(255/256,63/64)。取K=diag(256/255,64/63)、c=1/4，则A_c=A_0+cK、B_c=B_0-cK仍给严格正定Gram。两者的W_0,W_1,W_2精确相同。由实际递推算得
+
+$$S=\begin{pmatrix}
+-3422577/1073741824&-91001403/4294967296\\
+1612017279/34359738368&12799425/2147483648
+\end{pmatrix}\ne0.$$
+
+第三输出差恰为(c/16^3)S。基准轨迹三个训练步的损失都严格下降。这只是一个实际例子和定理的非空性校准，不推成一般训练收敛、泛化或量子加速结果。
+
+### 15A.9 物理、量子与任务边界
+
+本节的“状态”是足以预测指定学习算法未来输出的Gram。原始参数的共同隐藏行正交变换不会改变Γ，因此此识别不恢复每个参数坐标。连续梯度流的同一投影纤维可以永久闭合，有限步算法则可能使它分裂；这是精确演化律的区别。
+
+若将Γ归一化为量子密度矩阵，原合同步骤仍需成功滤波、失败标志、制备来源与整体尺度，才能保留上一轮的操作对应。当前固定损失梯度取决于未知W，实施该反馈需要测量与经典控制器，不能把本节无扰动的数学矩阵读取等同于未知单份量子态上的非扰动测量。本节不从此证明任何量子过程的diamond误差或物理实现。
+
+这里用微积分与有限差分检验具体的状态对应；没有把所有状态机、ML或量子系统宣布为同一种物理系统。
+
+### 15A.10 文献与实际验证范围
+
+既有前置为本卷第12–15节和 Library/Dynamics/holzmueller2020training.md。Holzmüller–Steinwart的附录C定位来自该已读Library记录；本轮另核对arXiv:2002.04861v3题录与摘要，没有声称重读全文。Kunin等的 Neural Mechanics（arXiv:2012.04728v2）与Barrett–Dherin的 Implicit Gradient Regularization（arXiv:2009.11162v3）题录、摘要已读取，用于有限步/流差异的既有背景。
+
+Lindsey–Menon，Regularization implies balancedness in the Deep Linear Network（arXiv:2511.01137v2，2026-03-23），本轮读取HTML摘要和前部设定，其几何理论中的满秩范围不自动覆盖本节退化类。以上文献没有被归因于本节特定的图连通性、三步读数与噪声证书结论；有限检索不建立全球优先权。
+
+本轮verify_passive_gram.py实际执行91项精确符号/有理断言，全部通过。包括独立2×2符号恒等式、真实U,V更新、矩形及低秩参数、正定竞争模型、相容核维数、真实三步差、传播误差包络和有理左逆身份。条件核维数的四组实际结果为：q=2连通(2,1,0)，q=3连通(3,1,0)，q=3断连(3,2,1)，q=2对角(2,2,2)。噪声检查验证包络和线性算子，未部署半正定或低秩拟合求解器。
+
+有限检查不代替全称证明。新Lean/Scribe未提交，旧关闭PR的源未复用为已验证前置，未执行Lean/Lake、独立评审、物理实验或训练基准。没有新增外部具名开放问题解决声明。真正新增的数学目标是固定数据被动观测的完整纤维分类；Gram更新、Taylor展开和范数传递保留其已有工具地位。
+
 ## 16. 自回归状态、注意力与上下文适应
 
 **定义 16.1（受控自回归状态）。** 一个具体自回归数学模型的完整状态取为能决定下一步条件规律的元组，例如固定权重 $\theta$、当前可用上下文 $c$、缓存 $k$、位置变量 $j$、解码控制 $d$、伪随机发生器状态 $r$，训练时再加优化器状态 $v$。外部输入属于动作；若环境也被称作自治系统的一部分，则其相关状态一并进入载体。所列字段是待证明的候选，不能以名称代替其充分性。来源记录是否需要保留，取决于必需查询是否读取它。
