[Index](../../marked_head_profile.md) · [Actual J source](219-one-late-source-split-controls-complete-saturated-j-heads.md) · [Shared selected source and survivor](244-three-complete-j-heads-share-raw-survivor-and-marked-deletion.md) · [Retained135/125](251-two-retained-original-tests-sharpen-the-complete-j-heads.md) · [Two seven depths](256-a-second-seven-depth-sharpens-the-complete-retained-j-heads.md)

# Seven retained states and two seven depths control complete J heads

On both entire actual saturated J faces, the same unnormalized survivor
mu of mass3/20 satisfies the following complete independently labelled
original357 test bounds.

| Original target | Complete upper | Previous256 upper |
| --- | ---: | ---: |
| AP13 | 0.194906849629635927257675556... | 0.194906849629635927257675556... |
| 0 | 5.554675534497605086132310227... | 5.614947195860340363802219458... |
| 16 | 4.438652982211784493825880431... | 4.486692197855258203297416668... |

The exact complete upper bounds, in the same order, are

    5415097003260174967/27783000000000000000,
    1747623922413605093240428858165191004949/314622143374512991758741000000000000000,
    161295578477781331243120458561733435993927/36338857559756250548134585500000000000000.

AP13 retains the256 bound. The exact improvements for original
costs0 and16 are, respectively,

    702325899358762726571651641416929597/11652671976833814509583000000000000000,
    64655193131503901467031001064707781273/1345883613324305575856836500000000000000.

The two heavy costs retain their original definitions and independent
original tests. AP13 is the integral of(A-4)_+. The complete mean bound
16/25 remains available. Each target covers62,500,000 original containing
choices, the entire common late-split interval and every exponent tail.

The added constraint is the original225=9*5^2 test retained jointly with
135 and125. Their seven nonempty membership states keep intersections
and the actual complement. The same retained load enters the complete
arbitrary-residue seven bridge with independent21,35,63,105 projections
at depth one and147,245 at depth two. This combines structural constraints
before optimization, not numerical savings from separately optimized
bounds.

These are ordinary rational complete-head bounds on the J domain. No
actual-source attainment, off-face extension, complete52-cost comparison,
Lean verification or unrestricted Erdos7 resolution is asserted.

## The same actual raw source, survivor and deletion remain

Use244's actual raw35 source Lambda, of mass1/4, and its actual survivor
mu, of mass3/20. Its common late coordinate is

    theta=1/135+x/270, 0<=x<=1.

The876-variable base model is retained verbatim. In each of the25
cell-slot rectangles, its16 old masks m record the original selected
events25,27,75,81. The raw and survivor masses are X_(i,m),Y_(i,m).
The source caps, exact mass constraints, old selected profiles,
intersection caps and complete marked-deletion inequalities all remain.
In particular Y<=wX uses the same J retained density w.

As in251, denote219's normalized deep-three table by p(c,s), and its
absolute descendant-five table by E(c,s). They are different source
objects and are not interchangeable. The new model does not import K
source bounds or a K forced27 condition. Relabelling source and tests
together covers both J orientations and their source-cell permutations.

## Seven actual membership states and the complement

Index the new original events by

    J0=I135, J1=I125, J2=I225.

For s=1,...,7, let S_s be the event whose three membership bits are the
binary digits of s. Inside each old rectangle-mask coordinate k=(i,m),
define

    U_(s,k)=Lambda(k intersect S_s),
    V_(s,k)=mu(k intersect S_s).

The states are disjoint and their zero-bit complement is represented
implicitly. Retain all necessary inequalities

    sum_s U_(s,k)<=X_k, sum_s V_(s,k)<=Y_k,
    V_(s,k)<=w_i*U_(s,k),
    Y_k-sum_s V_(s,k)<=w_i*(X_k-sum_s U_(s,k)).       (TR1)

The last line prevents the unmarked complement from acquiring a
survivor/raw ratio exceeding the same actual retained density. The
individual states include all pair-only and triple intersections; no
new labels are required to be nested or to share a residue.

## Independent original profiles, including225

Use separate profile simplexes lambda135(c,s), lambda125(s),
lambda225(c,s), of sizes25,5,25. Their respective sums are1. For each
rectangle(c,s), the raw marked totals obey

    sum_(old masks; new bit135 set) U
                       <=lambda135(c,s)*p(c,s)/27,
    sum_(old masks; new bit125 set) U
                       <=lambda125(s)*E(c,s)/125,
    sum_(old masks; new bit225 set) U
                       <=lambda225(c,s)*E(c,s)/25. (TR2)

The first two profiles are251's J profiles. The last holds because
225 fixes its own ternary depth-two cell and its own first-five slot;
the remaining five depth is measured by the same absolute descendant
table. It is a cell-slot profile, not a pure-five slot shared across
all ternary cells.

An actual positive-source original event selects its own point profile.
Absent or source-null events have zero marked mass and may choose any
point profile; for225, the(cell0,P) profile has zero coefficient. Allowing
mixtures enlarges the set of actual configurations, without asserting
that arbitrary mixtures are realizable. All three simplexes remain
independent.

The complete survivor caps are

    mu(J0)<=1/135, mu(J1)<=13/3750, mu(J2)<=1/225.    (TR3)

The middle cap is130's J pure-five bound(13/30)*5^-3. The other two
follow from raw Haar domination. The maximum225 coefficient in(TR2)
is also1/225.

## Original LCM intersections and every source column

For compatible independently labelled original cylinders of moduli a,b,
the raw intersection is at most1/lcm(a,b); incompatible cylinders have
empty intersection. The new/old raw intersection denominators are

| New original label |25|27|75|81|
| --- | ---: | ---: | ---: | ---: |
|135|675|135|675|405|
|125|125|3375|375|10125|
|225|225|675|225|2025|

Sum U over the corresponding new membership bit and old mask bit.
For new/new intersections, sum all states containing both bits and use

    Lambda(J0 intersect J1)<=1/3375,
    Lambda(J0 intersect J2)<=1/675,
    Lambda(J1 intersect J2)<=1/1125.                 (TR4)

The triple intersection is included in each relevant pair sum. No
independent pair mass is invented outside the seven joint states.

There are876 original coordinates,2800 new raw masses,2800 new survivor
masses and55 new profile weights, giving6531 variables. The exact
constraint inventory is

    inequalities=587+75+4000+3+12+3+6531=11211,
    equalities=16+3=19.                            (TR5)

The4000 terms are the ten state/complement links per old rectangle-mask
coordinate. Independent unit upper rows cover every variable, including
the source coordinate and profile weights. They are valid on the actual
model and permit exact repair of numerical dual proposals.

## One complete retained load enters both seven depths

The shallow head B contains1,3,9,5,15,45. At an old selected mask m,
put v=B+popcount(m). A new state s adds n_s=popcount(s), so the complete
retained load is at most13. Every original selected label is retained
exactly once.

Let m1 be the number of matching21,35,63,105 old-coordinate projections
at the current source point, and let m2 count matching147,245 projections.
The general arbitrary-residue bridge reused by256 is

    q=(t-v)_+, a=1+m1, b=1+m2,
    g_t(v;m1,m2)=(6/35)*(a-q)_+
        +(6/245)*(b-(q-a)_+)_+
        +1/[5*7^(2+(q-a-b)_+)].                    (TR6)

The first two terms retain depth one and depth two; the final geometric
term includes every remaining unit-seven depth. Its derivation orders
seven-cylinder caps and removes the first q available contributions.
It does not identify independently chosen seven residues or replace
one source by a different source for each label.

For every threshold t>=2, the raw base coefficient on X is g_t(v), and
the survivor base coefficient on Y is(v-t)_+. On each new state the
exact additional coefficients are

    U_(s,k):g_t(v+n_s)-g_t(v),
    V_(s,k):(v+n_s-t)_+-(v-t)_+.                    (TR7)

Thus a three-bit state receives the actual three-indicator increment,
not three copies of a one-indicator increment. At threshold1 the raw
and survivor base coefficients use B, and(TR7) is omitted, as in244
and251's first-hinge decomposition. The helper checks that w*(v-t)_++g_t(v) has increasing convex
transitions and its exact affine continuation; no load or exponent
truncation is used.

## All omitted-label payments are complete and disjoint

The three newly retained old labels are distinct assigned summands of
219's complete remainder R4=2471/81000. Their removal leaves

    Rtriple=2471/81000-1/135-13/3750-1/225
           =6151/405000>0.                         (TR8)

The original147 and245 assigned caps removed from the positive-seven
remainder are J-specific:

    (6/245)*(1/8)=3/980,
    (6/245)*(1/10)=3/1225.

Consequently the complete positive-seven complement is

    Z6=1/28-3/980-3/1225=37/1225.                  (TR9)

These subtractions operate on explicitly assigned nonnegative cap
series. They do not subtract estimates from unknown actual masses.
All other old and positive-seven labels at every depth retain their
assigned caps. For positive hinge coefficients a_t, the external
complete payment is

    a1*(53/600+37/1225)
       +sum_(t>=2)a_t*(6151/405000+37/1225).        (TR10)

The exact finite LP objective is the sum of the coefficients in(TR7)
and the base raw/survivor terms, weighted by a_t. Equations(TR8)--(TR10)
retain the whole tail after the new labels are accounted for in the LP.
Monotone convergence and the summable original cap series extend the
bounds from arbitrary finite original tests to complete tests, uniformly
in independently chosen residues.

## Complete enumeration and exact rational upper certificates

Every target keeps12500 shallow layouts, ten21/35 choices,50 independent
63/105 choices and ten independent147/245 choices:

    12500*10*50*10=62500000.                        (TR11)

The additional135/125/225 profiles are optimized within their separate
simplexes in each branch. The source LP retains the complete interval
0<=x<=1 as an actual variable.

Pruning first uses219's two-projection complete bound, then the common
late-coordinate minimum of its two- and four-projection bounds. Exact
251 certificates may cover a whole group of ten second-depth choices.
For each remaining choice, the three complete affine bounds, including
256's six-projection expression, are compared at both endpoints and
every strict pairwise intersection. Only this full finite set yields
the maximum of their lower envelope; endpoint-only substitution is
not used.

A256 whole-objective certificate is reusable only when its original
layout, seven projections and regenerated objective hash agree. It is
rechecked against its original3306-variable source model. Such a dual
is not inserted into the6531-variable model. Remaining branches use
the new exact6531-column dual. Each pruning bound already includes
its own entire tail payment; pieces from different decompositions are
never mixed.

For nonnegative primal variables z with Az<=b, Ez=e, the retained
rational dual satisfies

    y>=0, A^T y+E^T h>=objective,
    objective*z<=b*y+e*h.                          (TR12)

Every inequality column, equality contribution and objective value in
(TR12) is reconstructed exactly. Floating-point optimization proposes
y,h only; exact unit-row repair and full-column checks certify the
bound. The final certificate contains only consumed rational duals,
in a lossless dictionary/run-length encoding.

The completed scans retain134 distinct6531-column duals, giving
875154 exact new-model column checks. They cover187,500,000
containing choices across all three targets. The numbers of new-model
branches and reused256 branches after the preceding pruning stages are:

| Target | New-model branches | Reused256 branches | Distinct new-model duals |
| --- | ---: | ---: | ---: |
| AP13 | 0 | 26 | 10 |
| 0 | 52 | 294 | 62 |
| 16 | 52 | 296 | 62 |

The two heavy-cost certificate controllers have layout

    (1,4,2,1,2,4,2),
    projection(21,35,63,105,147,245)=(1,2,4,1,2,1,2).

At these branches the preceding256 certificates remain available;
the triple-state model reduces the other previously controlling
branches. Thus the final gain is a uniform combination of complete
branch bounds, not a claim that every branch improves.

The checker accounts for every containing choice through a complete
branch or a uniform containing bound. The terminal maximizing branch
is reconstructed independently, including its common late-coordinate
affine values. This identifies the maximum of the reported certificate
bounds, not an actual family attaining all source inequalities.

Artifacts: [exact checker](../../frontier/j-source/j_face_triple_second_depth_heads.py),
[dual proposer](../../frontier/propose_j_face_triple_second_depth_heads.py), and
[certificate](../../certificates/source_norms/j-source/j_face_triple_second_depth_heads.json).
