[Index](../../marked_head_profile.md) · [Original AP11 blocks](266-two-original-ap11-blocks-use-the-complete-second-depth-interface.md) · [Complete375 model](288-retaining375-strengthens-the-complete-j-survival-hinge.md)

# Retaining375 strengthens the complete J AP11 head

On both entire actual saturated J faces, the original AP11-0 function
and actual survivor measure mu satisfy

    integral AP11_0(A) dmu
      <=12119695377815344789/70619948437500000000
       ≈0.17161858151937771710441147261705687931.                 (RA1)

The complete improvement over266 is

    209824630093844118421/130747790250000000000000
      ≈0.00160480440772760301714544655564456088.                 (RA2)

All3,125,000,000 independent original containing choices are covered.
The original AP11-0 target, marked deletions, common late coordinate and
every exponent/cofactor tail remain. The AP11-1 bound is not updated.

## The original target and the existing complete375 model

Write H_t(n)=(n-t)_+. The original266 identity is

    AP11_0(n)=(1355/263538) H1(n)+(20425/263538) H2(n)
                 +(25/363) H3(n)+(28/33) H5(n).                 (RA3)

Its value at1 is zero. Every coefficient is positive and their sum is1,
so the complete eventual slope is1. This is the same function on every
positive integer, including unbounded loads.

The source model is exactly288's12941-variable375 extension, imported
through its existing constructor. All6531 original264 variables,
11211 inequalities and19 equalities remain. The375 intersections and
their complements add3200 raw variables,3200 survivor variables and10
independent root-slot profile coordinates, giving30454 inequalities and
20 equalities. The inherited actual embedding, density constraints,
profile E(c,s)/125, survivor cap1/375 and every original CRT cap are
those in288. No original residue is identified with another label.

## Every hinge retains every selected old state

For a fixed shallow layout and all eight positive-seven projections,
let g_t(v) be the complete two-depth raw kernel of288(RS5), with
q=(t-v)_+. At an old cell/mask and marked state h, set
v=B+popcount(mask)+popcount(h). If a_h,b_h are the actual raw and
survivor intersections with375, the new selected contribution is

    sum_t alpha_t [g_t(v+1)-g_t(v)] a_h
      +sum_t alpha_t [H_t(v+1)-H_t(v)] b_h.                    (RA4)

The seven pre-existing nonempty marked states retain their analogous
increments. Formula(RA4) applies to all eight states, including their
implicit complement; the density constraints use the same actual source.

H1 is included. Since every original shallow load B is at least1,

    H1(B+R+T)=B+R-1+T                                       (RA5)

for nonnegative integer indicator sums R,T. Its selected increments
are therefore exact. No selected label is dropped from H1 and charged
again through a different tail convention.

For each t, the raw kernel is nondecreasing through its finite
transitions and constant for every v>=t, while H_t continues linearly
with slope1. The inherited weighted bridge is integer-convex and its
complete affine continuation is checked. Nonnegative combination with
(RA3) preserves these properties. The four-label affine bounds use
the original CRT telescoping corrections for every hinge, including H1.
Their independent rational endpoint calculations agree with the integer
compiler. This supplies whole-load bounds, without an exponent cutoff.

Only375's assigned old tail payment leaves the source:

    6151/405000-1/375=5071/405000.

The complete positive-seven remainder is13/490. Multiplication by the
unchanged full slope1 gives the total source constant

    5071/405000+13/490=774979/19845000.                         (RA6)

Every affine competitor retains its own prefix and full complementary
tail. The different labels375*7^j, j>=1, remain in the positive-seven
tail.

## Complete original-domain coverage

Take266's published AP11-0 controlling layout and six projections as
an explicit seed. Check all50 independent441/735 completions with the
extended375 model. Their maximum fixes the benchmark before pruning;
no seed optimality is assumed.

The exact two/four/six/seven/eight projection partition is

| Stage | Number bounded at this stage |
| --- | ---: |
| Two projections |124983|
| Four projections |846|
| Six projections |37|
| Seven projections |0|
| Eight projections by affine bounds |18|
| Eight projections by seed certificates |50|
| Remaining eight-projection leaves |82|

The full count identity is

    25000*124983+500*846+50*37+18+50+82=3125000000.             (RA7)

Each of the82 residual leaves receives an exact12941-column dual.
The adopted bound at that leaf is the minimum of its complete new
bound and its strongest available complete old bound. The final result is

    max(fixed benchmark, max_(residual leaves) adopted bound).  (RA8)

Here it equals the benchmark in(RA1). Consequently every previously
pruned choice and every residual choice is covered by the same result.

The132 distinct seed and residual duals check1,708,212 columns, each also
checked directly with rational arithmetic. The prefix proof rechecks17
available266 duals,626 independent affine checks and5200 finite bridge
transitions with their complete algebraic continuations. The complete
branch digest and residual list are recomputed, and every supplied dual
is used. The existing rational-table/run-length codec is unchanged.

The [helper](../../frontier/j-source/j_face_retained375_ap11_heads.py) and
[certificate](../../certificates/source_norms/j-source/j_face_retained375_ap11_heads.json)
retain71 mathematical source pins and replay without research-directory
inputs:

```sh
python3 -I -O docs/reports/erdos7-odd-covering/frontier/j-source/j_face_retained375_ap11_heads.py --check
```

A separate consumer must recompute all52 original costs and the full
survival denominator. Actual-source attainment, off-face extension,
global joining, Lean verification and unrestricted Erdos7 are not
conclusions of this complete source result.
