[Index](../../marked_head_profile.md) · [Complete J hinge interface](219-one-late-source-split-controls-complete-saturated-j-heads.md) · [Whole actual J geometry](130-the-whole-j-face-forces-source-anti-alignment.md)

# The complete J survival and linear heads retain one late split

All four original AP11 block tests and eleven original affine-tail
costs have complete bounds on both entire saturated actual J faces.
Each of the fifteen independent objectives retains all6,250,000
containing head/projection choices, every original exponent label
and one common late-source parameter

    1/135 <= theta <= 1/90.                            (JS1)

The actual raw and survivor masses remain1/4 and3/20. These are J
bounds obtained from219's J source, density, deletion marginals and
complete tails. No K-source cost or K forced27 condition is used.
The existing219 AP13 and heavy0/heavy16 results remain available;
this result adds the other fifteen complete head interfaces.

The four new AP11 bounds, together with the existing J AP13 bound
and complete J first moment, also give a positive complete survival
denominator. A complete52-cost numerator, source-neighborhood
transport and an exhaustive global comparison remain separate.
These are ordinary source inequalities and exact rational checks,
with no Lean verification or actual-family attainment assertion.

## 1. The same full J source for every independent test

Use219's canonical ordered distinct root1 cells(L,M,N)=(2,3,4).
The mandatory late masses are theta and1/90-theta in cells M,N.
The absolute cap table and normalized selected-cylinder table remain
distinct. For nonnegative25-entry coefficients z, the exact
containing raw-source LP is

    U_theta(z)=sum_(c,s) cap_theta(c,s)*z(c,s)
                  -(1/120)*min_((c,s) in Sroot) z(c,s). (JS2)

Here Sroot is the fixed eight-entry positive root1 support.
Each of its caps is at least1/90, so removing1/120 from a
least-priced entry produces a feasible full-budget optimizer.
The removed coefficient does not depend on theta. Thus(JS2) is
affine in theta throughout(JS1), with the same root budgets

    (1/24,1/12,1/8).

The J density is

    w(c,s)=1-[1_(c<2)+1_(c=1)+1_(s=H)
                              +1_(c>=2,s=H)]/5.

For every nonnegative head H,219's two distinct mandatory deletion
families give

    integral H dmu <= U_theta(wH)-C3(H)-C5(H),

    C3(H)=sum_s q_s*min(H(0,s),H(1,s))/90,
    C5(H)=sum_c eta_c*(1+1_(c>=2))*min_s H(c,s)/100.    (JS3)

No claim that forbidden27 lies in a prescribed cell is required.
Both corrections are evaluated on the same original six-label
head B=1+I3+I9+I5+I15+I45. The independent original selected labels
25,27,75,81 use their normalized J operators and the valid mixed
intersection bounds1/675,1/2025.

The complete zero-seven remainders are

    R=(53/600,71/1000,1367/27000,1007/27000,2471/81000),

and the two/four-projection positive-seven remainders are

    Z2=43/700, Z4=1/28.                               (JS4)

They are remainders of proved nonnegative assigned-cap series.
Every selected label is removed exactly once from its own series;
all remaining exponent heights are summed geometrically.

## 2. Fifteen original functions, with all-load identities

The eleven cost indices are

    1,2,7,10,17,18,23,26,32,33,36.

For each original complete function f_i define

    a1=f_i(2)-f_i(1),
    at=f_i(t+1)-2f_i(t)+f_i(t-1), 2<=t<=8.

All coefficients are nonnegative, and the exact identity is

    f_i(v)=f_i(1)+sum_(t=1..8) at*(v-t)_+, v>=1.       (JS5)

The original function evaluator and metadata supply every finite
transition and the eventual affine tail. The checker compares the
finite values and verifies

    sum_t at=tail_slope,
    f_i(1)-sum_t t*at=tail_constant.

These two identities establish the entire continuation beyond the
original entrance. No tail truncation or comparison with a K-face
cost is made. The separate constant term is f_i(1)*3/20.

The AP11 count law is the original

    p1=28/33, pn=50/(3*11^n), n>=2.

For n=1,2,3,4 the exact positive-integer expansions of(nv-5)_+
are, respectively,

    H5, H2+H3, H1+2H2, 3H1+H2,
    Ht(v)=(v-t)_+.

Let a_(n,t) denote these coefficients. The original block e=0..3
uses the positive hinge vector

    b_(e,t)=sum_(n=e+1..4) pn*a_(n,t)/n
                             +T0*1_(t=1),
    T0=sum_(n>=5)pn=5/43923.                          (JS6)

Every finite transition and the exact infinite affine continuation
are checked. These coefficient identities concern the original
cost functions and count law; their validity does not depend on
choosing J or K as a source domain. Their bounds here use only J.

## 3. Maximize the minimum at one common theta

For each positive hinge vector a from(JS5) or(JS6),219 constructs
two complete affine bounds on the same original test:

    F2(theta), F4(theta),

including(JS2), both corrections(JS3), every selected increment
and the complete tails(JS4). The valid uniform comparison is

    max_(layout,projections) max_(theta in[1/135,1/90])
                                  min(F2(theta),F4(theta)). (JS7)

The inner maximum is evaluated at both endpoints and at the exact
intersection when it lies strictly inside the interval. Parallel
and identical lines need only endpoints. Maximizing each line
separately before taking their minimum would discard their common
source parameter; that replacement is not used.

There are12,500 original six-label layouts,10 independent21/35
projection pairs and50 independent63/105 projection triples. Thus
every objective has6,250,000 containing branches. The original
scanner expands a two-projection branch unless

    max_theta F2(theta) <= current comparison maximum.

For a skipped branch this already bounds all50 further projections
uniformly in theta. If n_exp and n_pruned are its counts, the exact
partition satisfies

    n_exp+n_pruned=125000,
    50*n_exp+50*n_pruned=6250000.                      (JS8)

The500 evaluations at the initial seed help establish the current
maximum but are not an additional set of original leaves. All
candidate values use exact common integer scaling, with no floating
point tolerances. For every objective eight independent fractional
branch evaluations and two maximizing-branch evaluations check the
compiled result. The fractional raw support also has an independently
matching feasible primal/dual LP.

The actual source normalizations, both J orientations and all
ordered distinct(L,M) pairs are those already handled by219's
source/test permutations. Reusing its entire interface preserves
all original independent residues; it does not identify the
maximizers of different cost functions.

## 4. The complete J mean is another valid alternative

130/219's complete mean bound is

    integral A dmu <=16/25,
    integral(A-1)_+ dmu <=16/25-3/20=49/100.           (JS9)

For every t>=1, Ht(v)<=H1(v) on positive integer loads. Hence each
positive hinge combination also has the complete bound

    sum_t at*49/100.

The adopted upper for a cost or block is the minimum of its full
scan upper and this valid J mean alternative, adding the separate
constant f_i(1)*3/20 where applicable. Both are uniform on the
same complete J domain. No source-local or face-incompatible bound
enters this minimum.

## 5. Complete count-tail and survival denominator

The remaining AP11 count law has first moment

    T1=sum_(n>=5)n*pn=17/29282.

The checker verifies total probability1, first moment7/6 and the
exact complete-tail coefficients

    T1-4T0=1/7986, T1-5T0=1/87846.

Because the actual J mass is exactly3/20, the entire remaining
count-tail payment is

    Tcount=(49/100)/7986+(3/20)/87846.                 (JS10)

This includes every n>=5, together with all original exponent
heights already included in(JS9). No newly estimated square or
factorial moment is needed here.

Let B0,...,B3 be the four adopted bounds from(JS6). The independent
AP13 result from219 is

    U4=29483/147000.

Consequently the complete J survival denominator obeys

    E >=3/20-U4/6-(B0+B1+B2+B3+Tcount)/7.              (JS11)

The four blocks and AP13 keep independent original test labels.
A common upper theorem permits their positive weighted sum without
requiring a simultaneous maximizing layout. The positive value of
(JS11) is checked using exact rational arithmetic.

## Complete values

Every new scan improves strictly on its complete J mean-only
alternative. The displayed decimals are rounded; the certificate
retains each exact fraction.

| Original objective | Adopted complete upper | Expanded two-projection branches |
| --- | ---: | ---: |
|AP11-0|0.1774415262010518|30|
|AP11-1|0.04893430691747389|34|
|AP11-2|0.005682577085657476|10|
|AP11-3|0.0005784066922308845|12|
|linear-1|1.542995133900929|20|
|linear-2|0.1668560908799678|31|
|linear-7|1.75900207041156|20|
|linear-10|0.2251586523942572|31|
|linear-17|1.233480327986078|20|
|linear-18|0.133414145831781|31|
|linear-23|1.406148035467731|20|
|linear-26|0.1800298340261988|31|
|linear-32|0.1978467161309685|32|
|linear-33|0.04524731387378799|30|
|linear-36|0.051753677515288|30|

The complete count-tail payment is exactly277/4392300. Substituting
the four AP11 entries into(JS11) gives

    E >=67792212611/813541806000
      =0.08332972210035387... .

Across the fifteen independent scans,382 of the1,875,000 original
two-projection branches expand. Including the500 seed projections
per objective, there are26,600 expanded four-projection evaluations.
The evaluated branches contain no strict interior crossings. This
does not remove the exact crossing rule from(JS7), nor assert that
every skipped branch has no crossing; each skipped branch is already
bounded uniformly over the entire theta interval.

## Exact certificate

The [helper](../../frontier/j-source/j_face_complete_survival_linear_heads.py)
and [certificate](../../certificates/source_norms/j-source/j_face_complete_survival_linear_heads.json)
retain all fifteen original coefficient vectors, cost tags and
all-load identities; each complete branch digest, pruning partition,
late-coordinate maximizing witness, both full candidate tails,
independent fractional checks, valid J mean minimum and exact final
upper. The full count law and complete denominator are retained as
separate consumer inputs.

The sum of the per-objective containing-choice counts is93,750,000;
this counts each independent objective's comparison branches, not
93,750,000 simultaneously realizable sources. The150 independent
fractional checks test the exact integer compiler. The continuum
late-parameter claim follows from the affine source formula and
exact crossing rule, and the unbounded exponent claim follows from
the complete geometric series and monotone-convergence interface.
Neither follows merely from the finite number of checks.

```sh
python3 -I -O docs/reports/erdos7-odd-covering/frontier/j-source/j_face_complete_survival_linear_heads.py --check --workers 2
```

The theorem remains restricted to the saturated actual J faces.
It supplies the remaining AP11 and affine-head inputs for a future
complete J52-cost consumer, not that consumer or a global join.
