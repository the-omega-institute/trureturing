[Index](../marked_head_profile.md) · [Whole J face](j-source/130-the-whole-j-face-forces-source-anti-alignment.md) · [Surplus transport](j-source/132-the-whole-j-face-has-a-quantitative-surplus-neighborhood.md) · [Source packing](116-signed-face-duals-transport-one-shared-finite-source.md) · [Whole-face concentration](71-global-j-k-control-faces-and-exact-escape-gaps.md)

# A whole J source neighborhood has a complete labelwise bound

Let qJ be71's whole-J-zero-set weight. Suppose

    qJ>=1-delta, rho=S-S0, t=delta+rho<=1/1000,
    S0=sum_c pi_c D_c, delta>=0.                 (JT1)

The quantities refer to one actual original congruence family, with
its actual common shallow carrier law and survivor measure. Both J
orientations and the entire beta-times-late simplex are retained.
Define the complete independent-label error

    B1(e)=sum_(a,b>=0,a+b>0)min(e,3^-a*5^-b).

Every complete independently labelled original357 identity test obeys

    integral A dmu
       <=S+49/100+119*delta/360+B1(100*t),
    6*S-integral A dmu
       >=13/50+5*rho-239*delta/60-B1(100*t).      (JT2)

In particular the replacement reserve over the actual carrier-averaged
old49 direction40 is at least

    79/1944-54*delta-B1(100*(delta+rho)).          (JT3)

On the nonzero source-and-surplus rectangle

    delta<=1/100000000, rho<=1/100000000,         (JT4)

this reserve exceeds1/25. Thus this result extends132 from exact source
faces to a neighborhood in all source parameters. The constants are
conservative. It is an ordinary continuous-domain proof with exact
rational arithmetic checks, not Lean verification or a new global K.
It still requires integration with the current marked inventory and
all other parameter regions.

## 1. Concentration and one enlarged capacity budget

Choose71's J orientation, with distinguished carrier(root0,cell1).
Write h=sum eta, h0=eta0+eta1 and h1=eta2+eta3+eta4. Concentration gives

    deficit0>=(1-delta)/2, alpha1>=(1-delta)/4,
    beta2+beta3+beta4>=(1-delta)/4,
    late2+late3+late4>=(1-delta)/72,
    3/4<=z<=3/4+delta/4, kappa=1-pi_A<=delta.

All total capacity constraints remain in force. Consequently

    1/2<=h<=1/2+delta/18,
    1/6<=h0<=1/6+delta/18,
    1/3-delta/18<=h1<=1/3,
    eta_l>=1/9-delta/18 (l=1,2,3,4),
    h1>h0, h1/(h1-h0)<=3.                       (JT5)

Let dmax=max_l(z-alpha_ROOT(l)-beta_l). Since alpha0 and each beta
coordinate in root0 are at most delta/4,

    0<=z-dmax<=delta/2.

Use132's six nonnegative capacity defects, with the same definitions
for E5,E15,E5d,E15d and omega, and define

    E3=z/90-V_(pure3,a>=3)(1).

For arbitrary original ternary cylinders J and five events F,
Lambda(J times F)<=3^-a*q(F), where q is the actual pure5 complement.
Thus the positive five-projection defect Xi3=q/90-V_pure3deep^5 has
mass E3. Raising the old deep3 cap from dmax/90 to z/90 costs at most
delta/180. All other unused cofactor capacities remain nonnegative.
The single complete capacity/union decomposition therefore gives

    R:=E5+E15+E5d+E15d+E3+omega
      <=S-D+(z-dmax)/90
      <=rho+7*delta/36<=t.                       (JT6)

Here71 gives0<=S0-D<=17*delta/90. Every positive seven depth, absent
label and union/cap loss is included once in(JT6).

## 2. Four source slots and the incomplete late allocation

Put p=z-3/4, a=1/4-alpha1 and b=1/4-sum_(l>=2)beta_l, so
Delta=p+a+b<=3*delta/4. These are actual additional source budgets.
A missing first source5,15 or45 wastes at least1/5 of its respective
pure5, root1 alpha or root1 beta capacity. Hence all are present,
15 and45 have their specified root1 location, and their first-five
slots P,A,B are distinct. Otherwise the latter label spends zero
additional first-depth capacity.

The original source25 is present: its absence or containment in P
wastes1/25 from the pure5 budget. Let Q be its first-five parent.
If Q=A, source25 makes the first alpha label lose1/25; if Q=B it
makes the first beta label lose1/25. Each alternative exceeds
delta/4. Therefore P,A,B,Q are distinct. Let H be the fifth slot,
and L the cell of the original source45, in{2,3,4}.

Set ell=h/5-Lambda(H). Cofactor5 in P,A,B,Q loses at least
h/5,h1/5,eta_L/5,h/25, respectively, each at least1/50 under(JT5).
Missing labels lose their entire cap. Summing all seven depths gives

    E5>=min(ell,1/50)/5.

Since E5<=t<=1/1000<1/250,

    ell<=5*E5<=5*t<1/50.                        (JT7)

Thus H is the actual best slot and satisfies116's packing guard.
Cofactor15 outside(root1,H) loses at least1/100: use h1/25 for Q,
(h1-h0)/5 for another root and the analogous first-label losses for
the other slots. The complete wrong or missing carrier weights are
therefore at most50*E5 and100*E15. Replacing their absent ideal H
sections costs at most10*(E5+E15) on any event; indeed the target
section masses are at most1/9 and1/15 respectively.

Let lambda=1/72-sum_(l>=2)late_l<=delta/72. Assign the actual
additional root1 late deletion disjointly to its original labels.
The sum of their unused raw budgets, including absent labels and
assignments outside root1, is lambda. The complete b=1 raw budget is
1/90. Such a label cannot contribute in P or A, nor in B at cell L.
If its slot is Q, source25 removes at least one fifth of its raw
rectangle; its assigned remainder is at most four times its unused
budget. Writing L_B,L_H for assigned b=1 mass in B,H, this proves

    L_B+L_H>=1/90-5*lambda,
    L_H<=ell,
    L_B>=1/90-5*lambda-ell.                     (JT8)

This argument allows all beta and late allocations in the other
root1 cells; it imposes no concentration at a simplex vertex.

## 3. A uniform bound for each first-five test

Use the ideal four-family multiplier

    w=1-[1_root0+1_cell1+1_H+1_(root1 times H)]/5.

The error from the actual shallow3/9 law on any event is at most
kappa*(h0+eta1)/5<=delta. The cofactor5/15 replacement error is at
most10*(E5+E15), and the actual V-to-deletion error contributes omega.
The extra pure3 projection subtraction contributes Xi3 when used.

Let K_lj be130's source table before the compulsory1/90 late B
subtraction, divided by eta_l*. Here eta*=(1/18,1/9,1/9,1/9,1/9).
The root-wide packing proof of116 bounds the actual source before
late removal by

    eta_l*(K_lj+1_(j=Q)*(Delta+4*ell)).          (JT9)

Indeed ell/h<=2*ell and ell/h1<=4*ell in116(S4). Only eta0 can
exceed eta0*, by at most delta/18; the other eta_l are at most eta_l*.
Since0<=w<=1 and K_lj<=1/5, changing eta costs at most delta/90
on any column or root-column. The added Q term costs at most

    (5/9)*(3*delta/4+4*ell)<=delta/2+3*ell.

The ideal multiplier is1 on root1/B. By(JT8), replacing its actual
assigned b=1 subtraction by the face subtraction costs at most
5*lambda+ell<=delta+ell. For all full columns and root-columns,
the total positive source-table error is consequently at most

    2*delta+4*ell.                              (JT10)

This comparison uses only the total B allocation: both full columns
and root-columns are independent of its split among the two allowed
cells.130's six tables therefore supply their existing face caps.

For test5 one also subtracts the actual q/90. Write p_j for pure5
deletion in slot j. The original alpha and beta first labels give
p_A<=a,p_B<=b, and ell>=h*p_H gives p_H<=2*ell. Conservation gives

    q_P=0, q_A=1/5-p_A, q_B=1/5-p_B,
    q_Q=3/20+p+p_A+p_B+p_H, q_H=1/5-p_H.

Thus the positive error in replacing -q/90 by the face vector is
at most(delta/4+2*ell)/90<=delta+ell. No false equality of source
marginals or source total variation is assumed. Combining the
preceding bounds with(JT7) gives the individual test inequalities

    mu(test5)<=4/75+4*delta+35*R,
    mu(test15)<=1/25+3*delta+30*R.               (JT11)

In particular each excess is at most39*t, below100*t.

## 4. Every remaining original zero-seven label

The complete pure5 family has ideal ternary projection eta/20 and
positive deficiency of mass E5+E5d. For the3*5 family the ideal
projection is eta restricted to root1 divided by20. Correct-root
labels cost at most their capacity deficiency. A wrong-root label's
desired mass is at most h1*5^-b, whereas its cap loss is at least
(h1-h0)*5^-b. By(JT5), ratio3 suffices. These comparisons and the
same Omega produce a ternary projection error of total mass at most

    E5+E5d+3*(E15+E15d)+omega<=3*R.             (JT12)

Project the source factors to the whole J face as in71. Its bounds
are ||n-n*||_1<=delta/2, ||eta-eta*||_1<=delta/9 and
||d-d*||_infinity<=5*delta/4. The root/cell expression

    Lambda(T)-[Lambda(T intersect root0)+Lambda(T intersect cell1)]/5
       -[eta(T)+eta(T intersect root1)]/20

changes by at most delta on root or cell events; the carrier-law
error is at most delta. Using130's face caps and(JT12),

    mu(test3)<=11/120+2*delta+3*R,
    mu(test9)<=2/45+2*delta+3*R.                 (JT13)

For an original cylinder T of depth a>=3 in cell l, ignore favorable
late deletion and use Lambda(T)<=d_l*eta(T). The coefficient of
eta(T) in this comparison is

    [1-(1-kappa)*(1_(l<2)+1_(l=1))/5]*d_l
                                      -(1+1_(l>=2))/20.

It is nonnegative since d_l>=1/4, and it exceeds its face coefficient
by at most3*delta. The face maximum is11/20. Since eta(T)<=3^-a,

    mu(test3^a)<=(11/20)*3^-a+3*delta+3*R.      (JT14)

For a pure5 cylinder F of depth b>=2, keep the good shallow carrier
and Xi3. Its raw q(F) coefficient is

    h-(1-kappa)*(h0+eta1)/5-1/90
       <=13/30+2*delta/15.

It is positive, q(F)<=5^-b, and E3+omega<=R. Hence

    mu(test5^b)<=(13/30)*5^-b+delta+R.          (JT15)

The other mixed labels already have the unchanged raw face caps
(1/3)*5^-b for3*5^b,b>=2; (1/9)*5^-b for9*5^b,b>=1; and
3^-a*5^-b for a>=3,b>=1. Thus every original zero7 nonunit label
satisfies, with its own independent residue,

    mu(C_(a,b))<=face_cap_(a,b)+100*t.

Also mu(C_(a,b))<=3^-a*5^-b. Taking the better bound, then summing,
costs B1(100*t). Reusing one uniform scalar in this clipped sum is
not assigning a fresh residual budget to each label: the exact
quantity paid is the single complete function B1 of the shared t.
No common positive reference measure with all face caps is needed.

## 5. Complete tails and the old49 comparison

The nonunit zero7 face caps sum to17/50. The positive7 labels retain
their complete raw cap(s+C)/5. The projection bounds imply

    |s-1/4|<=delta/2, |C-1/2|<=83*delta/72,
    (s+C)/5<=3/20+119*delta/360,
    D>=3/20-263*delta/360.

For C, the root and cell maxima cost2||n-n*||_1, the deep3 maximum
costs||d-d*||_infinity/18, and the three eta terms cost at most
3||eta-eta*||_1/4. Adding the unit S proves the first(JT2); using
S>=D+rho proves the second.

For old49 use130's same fixed layouts on every face point. Their
face margin is10661/48600.66's uniform fixed-layout continuity bound
is12*tau, with tau=||n-n*||_1+||eta-eta*||_1+||d-d*||_1.
Here ||d-d*||_1<=13*delta/4, so tau<=139*delta/36. Changing the
carrier law costs at most kappa/2. Consequently the actual old
margin is at most10661/48600+47*delta, hence also with50*delta.
Subtracting it from(JT2), dropping the favorable5*rho and rounding
239/60+50 up to54 gives(JT3).

For e>0 the complete tail has the exact finite correction formula

    B1(e)=7/8-sum_(a+b>0,3^-a*5^-b>=e)(3^-a*5^-b-e).

At e=1/500000, corresponding to(JT4)'s largest100*t, its value is
296704261/2214337500000. Thus the reserve throughout(JT4) is at least

    358752149987/8857350000000
      =0.0405033277432866...>1/25.

Monotonicity of B1 and the error terms proves the entire rectangle,
not just its corner. The complete finite original families from50
eventually enter it; the companion checker evaluates explicit such
heights with all absent exponent tails still charged. A new global
comparison is a separate consumer obligation.
