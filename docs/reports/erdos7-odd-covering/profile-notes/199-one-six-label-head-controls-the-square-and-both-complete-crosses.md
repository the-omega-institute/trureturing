[Index](../marked_head_profile.md) · [Previous complete comparison](191-the-two-prime-paths-retain-the-same-compulsory-deletion.md) · [Original complete pair partition](128-the-complete-factorial-tail-retains-its-head-off-the-face.md) · [Same factorial head](112-the-factorial-tail-retains-one-compatible-original-head.md)

# One six-label head controls the square and both complete crosses

On both complete saturated actual K faces, every independently labelled
complete original357 test obeys

    integral A² dmu <=8201/1800=4.556111111111... .       (SH1)

The bound improves191's13109/2700 by323/1080. It replaces the entire
square; the earlier pure-axis saving is not added again. With every191
cost retained and the existing all-load majorants applied once, the
complete face comparison becomes

    K<=902033522738934218547984261300578265995197609588916431
       /1984289303770415314553198889862083292500000000000000
      =454.5877060491884... .                          (SH2)

The improvement over191 is4.008899054984877.... The new shared
structure is the same original six-label head in its own square,
its entire old-tail cross, and its entire positive-seven cross.
No maximizing original residue is identified between distinct labels.

The result concerns both actual saturated K faces with r=rho=0 and
mass53/360. It is an ordinary continuous-source theorem evaluated by
exact rational source LPs, without an actual-attainment claim, Lean
verification, off-face extension, new global K or unrestricted Erdos7
resolution.

## 1. The exact partition keeps the two crosses distinct

For one actual test write

    A=B+O+Z,
    B=1+I3+I9+I5+I15+I45.                            (SH3)

O contains every zero-seven label outside{0,1,2} times{0,1}; Z
contains every positive-seven label, including those with old cofactor1.
The exact square expansion is

    A²=B²+2BO+2BZ+O²+2OZ+Z².                         (SH4)

Every integral below uses this same actual survivor measure. Functions
of the old coordinates use its old-coordinate marginal. The raw
old-coordinate source is Lambda. On the stated face128 gives
mu<=w*Lambda;75 additionally gives the distinct complete forbidden27
measure supported in cell1, with five marginal q/135.

Use the actual tables pre, descendant, w and raw from112, and the
same three raw-source group budgets

    {cell0}:1/36, {cell1}:1/12, {cells2,3,4}:5/36.

For a nonnegative function z constant on the25 cell/slot rectangles,
write L(z) for the capacity LP with these budgets and individual
rectangle capacities raw(c,s). Every actual Lambda is feasible, so
integral z dLambda<=L(z). Each LP used in the computation has an
independently checked feasible primal and matching dual. This is an
upper relaxation of actual sources, not an existence assertion.

## 2. One head-square and complete old-tail operator

The actual head layout is

    ell=(r3,c9,s5,r15,s15,c45,s45).

It determines B on every rectangle. Since B² is nonnegative and
constant there, retain the complete27 deletion after the four distinct
families already in w:

    integral B² dmu
      <=L(w*B²)-sum_s B(1,s)²*q_s/135=:H(ell),
    q=(0,1/5,1/5,3/20,1/5).                         (SH5)

The sum is the integral of B² against that one deletion measure.
There is no additional deduction of the same27 measure from(SH5).

For nonnegative z let the five operators of128 be

    C3(z)=max_c sum_s pre(c,s)*z(c,s),
    C5(z)=max_s sum_c descendant(c,s)*z(c,s),
    C15(z)=max_(r,s) sum_(ROOT(c)=r) descendant(c,s)*z(c,s),
    C45(z)=max_(c,s) descendant(c,s)*z(c,s),
    Cmix(z)=max_(c,s) z(c,s).

Their complete omitted-label sum is

    T(z)=C3(z)/18+[C5(z)+C15(z)+C45(z)]/20
                                      +Cmix(z)/72.   (SH6)

This partitions pure3 depths at least3, the three five-descendant
families of depth at least2, and mixed depths a>=3,b>=1. The factors
are complete geometric sums. Applying the pointwise domination to
each original omitted cylinder proves

    integral BO dmu<=T(w*B).                         (SH7)

All original labels in O remain independent. Unlike maximizing B
first, this formula keeps its actual head-dependent values in every
weighted cylinder operator.

## 3. All positive-seven labels see the same head

Define a complete raw old-label row at the same z by

    R(z)=L(z)
       +max_r L(z*I_root r)+max_c L(z*I_cell c)
       +max_s L(z*I_slot s)
       +max_(r,s) L(z*I_(root r times slot s))
       +max_(c,s) L(z*I_(cell c times slot s))
       +T(z).                                      (SH8)

The first six terms account for the old exponent rectangle
{0,1,2} times{0,1}, including its unit. The remaining term accounts
for every old exponent outside it. For any independently chosen
old-coordinate cylinder of that label, its weighted raw intersection
with z is bounded by its corresponding term in(SH8).

At positive seven depth e the raw conditional pure-seven cap is
u_e=6/(5*7^e). Before mixed deletion the measure is the raw source
times the pure-seven law. Therefore each original positive-seven
test has weighted survivor integral at most u_e times its weighted
raw old-cylinder integral. Summing all old cofactors and then all
e>=1 proves

    integral BZ dmu <= R(B)*sum_(e>=1)u_e=R(B)/5.     (SH9)

This uses the actual B from(SH3) throughout. It neither replaces
the actual survivor by a product measure nor identifies the
positive-seven residues with head residues. Each independently
chosen positive-seven old cylinder is bounded separately within
the same weighted row. The raw caps supply the needed inequality
even if the survivor correlates the old and seven coordinates.

## 4. The entire tail square remains in the account

Reuse128's disjoint nonnegative LCM cap-series partition, reconstructed
by108's original helper. Its three ordered contributions are

    integral O² dmu <=107/300,
    integral OZ dmu <=121/720,
    integral Z² dmu <=173/180.

Thus

    integral(O²+2OZ+Z²)dmu<=2977/1800.              (SH10)

The diagonal terms of O² and Z² are included. The positive-seven
square factor is the complete4/15, not the different factor2/3
which includes zero-seven/positive-seven crosses. No tail pair
appears in both(SH9) and(SH10): B and O are disjoint label lists.

Combining(SH4)--(SH10), before maximizing the original head, gives

    integral A² dmu
      <=max_ell[H(ell)+2T(w*B_ell)+2R(B_ell)/5]
                                                +2977/1800. (SH11)

For finite families every inequality holds with omitted labels filled
by their nonnegative capacities. All complete cap sums converge.
For a fixed source, monotone convergence therefore gives the full
test. For varying finite sources approaching the saturated face,
the original three-prime raw LCM bounds dominate all pairs uniformly:
their multiplicities are products of linear depth factors and their
caps decay geometrically. The tails outside any exponent box tend
uniformly to zero. Stabilize each finite set of original residues,
pass the finite integrals to the face, and then remove the box. This
is the inherited actual-face limiting argument, with no fixed-height
assumption.

## 5. Exact finite evaluation and complete comparison

There are2*5*5*2*5*5*5=12500 independent original heads. Empty or
source-null choices can be replaced by one of the displayed surviving
choices for an upper bound, since A² is increasing in every indicator.
The exact common maximum in(SH11) is653/225=2.902222.... Its unique
maximizing head is

    ell=(0,1,2,0,2,1,2).

At that head the three contributions are

    H=1249/900, 2T(w*B)=46/75, 2R(B)/5=811/900.

They sum to653/225, and adding2977/1800 gives8201/1800. The other
K orientation exchanges the two root0 cells everywhere, including
the forced27 measure; first-beta changes permute the three root1
cells. The full head inventory and common source bounds are preserved,
so the same result holds on both whole actual beta faces.

The scan evaluates612500 rational source LPs. Its retained certificate
contains the complete head digest, the exact maximizing components,
all source tables and budgets, and the original full tail partition.
The computation evaluates(SH11); its source and infinite-label proof
is the ordinary argument above.

Starting from191's52 cost bounds, replace the complete square by(SH1)
and use the existing all-load majorants once. Original indices
41,42,43,44,45,49,50,51 improve. The numerator gains are

    direct square:       0.03973887050534394...,
    majorant propagation:0.2802827022341361... .

Every prior factorial and cost gain remains in the input vector. The
earlier pure-axis square improvement is not subtracted from(SH1).
The resulting numerator is34.56492263283733..., and the complete
denominator remains50511415637/632754738000>0, including all AP11
blocks, the AP13 loss and the infinite count tail. These give(SH2).

The [helper](../frontier/shared_six_head_square_comparison.py) and
[certificate](../certificates/source_norms/shared_six_head_square_comparison.json)
retain the exact result and complete consumer.

```sh
python3 -I -O docs/reports/erdos7-odd-covering/frontier/shared_six_head_square_comparison.py --check
```
