[Index](../../marked_head_profile.md) · [Fixed actual chain](333-variable-full-haar-thresholds-retain-more-survivor-mass.md) · [Original-label comparison](../001-064/06-a-common-matrix-bound-for-all-actual-rectangles.md#retaining-an-original-exponent-label-across-the-full-auxiliary-law)

# Original AP blocks sharpen the fixed full-Haar account

Keep exactly333's finite source guard

    qJ>=1-1/4000, rho>=1/10,

its actual supported AP11/T4--AP13/T5 law, and its full-Haar thresholds
(6,6,8,12,12,16,18,24,24) at(17,19,23,29,31,37,41,43,47).
The following stronger complete hinge account changes no kernel,
source probability, original forbidden residue or conditioning rule.
It preserves each original AP11/AP13 exponent block across all
auxiliary AP count outcomes before taking its raw source bound.

The resulting surviving-mass lower through43 is approximately

    0.0582630339314176,

and the47 lower is approximately

    -0.0116878079531338.

Both improve333 on the same actual physical chain. The exact fractions
in the certificate govern these readings. A further correction strictly
larger than the latter deficit is still required to establish survival
through47. No optimality or arbitrary-prime conclusion is asserted.

## Original AP labels remain fixed before the auxiliary count

For a single complete through13 own test, let L_(a,b) be its original357
cofactor load for exponent pair(a,b) at11 and13. These loads have their
own original residues and are fixed before auxiliary counts are drawn.
The established conditional comparison uses independent AUXILIARY
N11,N13 with caps5/3,12/7 and sets N=N11*N13. Their mean is

    mN=E N=(7/6)(8/7)=4/3.

For an increasing convex h, the centered original-label Jensen bound is

    h(sum_(a<N11,b<N13) L_(a,b))
      <=h(N)+sum_(a<N11,b<N13)[h(N L_(a,b))-h(N)]/N. (A1)

This is06(FL1)'s existing order of collecting a fixed original test
before taking its source maximum. Distinct exponent pairs still use
distinct own tests. Missing original pairs can be completed in advance;
every added term is nonnegative and every original finite height remains.

For h(v)=(v-r)_+, r>1, define

    g_r=E(N-r)_+,
    Q_(a,b),r(v)=E[1_(a<N11,b<N13)
                         ((Nv-r)_+-(N-r)_+)/N].       (A2)

Each Q is nonnegative, increasing and convex on v>=1, and Q(1)=0.
Thus it is a legitimate centered input to329's source comparison.
The exact raw AP bound at a whole-J vertex theta is

    R_theta(r)=(1/4)g_r+sum_(a,b) B_theta(Q_(a,b),r), (A3)

where B_theta is the established complete raw357 operator. This is
the existing raw_ap_hinge/ap_original_blocks_raw interface; no new
source optimizer or independent actual-law assumption is introduced.

The original-pair sum is complete. Put k=ceil(r) and
p_(a,b)(n)=Pr(a<N11,b<N13,N=n),
w_(a,b)=Pr(a<N11,b<N13). For n<k, n<r, and

    Q_(a,b),r(v)
      =sum_(n<k)p_(a,b)(n)(v-r/n)_+
         +[w_(a,b)-sum_(n<k)p_(a,b)(n)](v-1).       (A4)

All coefficients are nonnegative. Only pairs with(a+1)(b+1)<k can
have a finite n<k contribution. Every other pair contributes exactly
w_(a,b)(v-1). Since sum_(a,b)w_(a,b)=E N, their entire omitted
coefficient is

    mN-sum_((a+1)(b+1)<k)w_(a,b)>=0.              (A5)

The source mean for v-1 at every vertex is13/20. Equations(A4)--(A5)
evaluate all count and original-exponent tails exactly.

## The outer post13 count and actual common variables

At the incoming target threshold t, let Z be the complete product of
333's PRECEDING post13 comparison counts. These counts index the
original post13 exponent tuples. Given an outcome Z=z, the Jensen
comparison is an average of z complete through13 own tests at the
cost(v-t/z)_+, multiplied by z.

For each such own test, its AP pair(a,b) and residue data stay fixed
while averaging over N11,N13 in(A2). The upper may select different
own tests for different outer z values. It asserts no equality of
tests belonging to different post13 tuples or outcomes, and no
independence of the actual physical coordinates. The shared objects
are precisely the original AP pair within its own test, the actual
source parameters, and the fixed physical chain of333.

Write q_z=Pr(Z=z) for z<t, U0=Pr(Z>=t), U1=E[Z;Z>=t], and

    f=E(Z-t)_+=U1-t*U0,
    c=E(NZ-t)_+.

Retain333's lambda=1+7/4000, actual source mass S, source mean excess
upper M, and unchanged raw13 denominator E(S)=eS-Ddelta.
For z<t put r=t/z>1. Equation(A3)'s centered part

    R_theta(r)-(1/4)g_r=sum_(a,b)B_theta(Q_(a,b),r)>=0

is transported with factor lambda by329. Its constant part g_r is
transported as g_r*S, not multiplied by lambda. This is valid also
when the actual mixed7 measure has mass S smaller than its raw source:
only nonnegative centered costs are enlarged to that raw source.

For z>=t, the hinge of the z complete through13 loads is affine because
every load includes its unit. Its source-centered contribution is at
most z*mN*M; its constant term is(z*mN-t)S. Thus define

    C_theta=lambda*sum_(z<t)q_z*z
                      [R_theta(t/z)-(1/4)g_(t/z)]
              +U1*mN*M,
    C=max_theta C_theta.                         (A6)

The nine-vertex maximum covers the same containing source face as329:
the centered cost functions in(A2) are nonnegative and convex, and the
raw operators are separately convex in the source parameters. The
AP internal tail is already fully paid by(A4)--(A5), while U1 pays
every outer z>=t. No finite or infinite exponent layer is discarded.

## Center before the sole13 normalization

The post13 comparison functional is pointwise at least f at every
through13 input: all its own loads are at least one. The raw physical
11/13 law has mass S. If its actual surviving13 part has mass z13,
then z13>=E(S)>0. Subtracting the proven nonnegative floor from the
part removed at11/13 gives

    integral_(surviving13) functional
      <= C+cS-f(S-z13).

Consequently the actual incoming physical hinge is bounded by

    H_<p,t<=f+[C+(c-f)S]/E(S).                  (A7)

As in330/333, only Z defines the conditioning floor f; the full product
NZ cannot supply a pointwise floor at a through13 input. All future
clipped kernels have the same normalized rule on every full prefix,
so this comparison can also be evaluated on the13-removed inputs.
No later killed mass replaces the sole13 denominator.

The helper verifies c>=f>=0, a nonnegative shifted numerator, and
e*C+(c-f)Ddelta>0. Therefore(A7) decreases with actual S, and333's
Smin=1/4-(263/360)/4000 gives its uniform value on the stated guard.

## Same-chain cumulative result

Use333's unchanged assigned-charge factors1/(p-1-t_p) and sum the
nonnegative upper payments from(A7). Every row is strictly smaller
than its corresponding333 hinge bound. The improved43 survival
and remaining47 deficit stated above follow from the same union
bound on the same actual physical chain. Any additional same-chain
intersection correction must exceed the NEW exact47 deficit; no
intersection lower bound is asserted here.

The [portable exact helper](../../frontier/cover-geometry/high_rho_original_ap_blocks.py)
reconstructs each original AP pair's finite probabilities, complete
active tail and omitted-pair mass, checks the existing centered source
API term by term, and retains the complete outer product tails.
The [canonical certificate](../../certificates/source_norms/cover-geometry/high_rho_original_ap_blocks.json)
contains the full rational account and binds its current sources,
ordinary proof and producer. This is an ordinary mathematical result
with exact arithmetic verification, not a new Lean formalization.

```sh
python3 -I -S -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/high_rho_original_ap_blocks.py --check
```
