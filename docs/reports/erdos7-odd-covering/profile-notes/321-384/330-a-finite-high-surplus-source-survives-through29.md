[Index](../../marked_head_profile.md) · [Finite whole-J source guard](329-a-finite-whole-j-neighborhood-covers-high-surplus.md) · [Two-step criterion](327-actual-two-prime-survival-needs-a-masked-moment.md)

# A finite high-surplus source survives through29

Fix one finite actual original forbidden family whose357 source is in
the effective9 chart and satisfies

    qJ>=1-1/4000, rho=S-S0>=1/20.                   (P1)

No alignment of source45 and135 is required. Construct the SAME
supported AP11/T4--AP13/T5 law used in329 and its original52-cost
comparison. Apply its prescribed pure-base17/T8 and19/T8 physical
kernels, retaining the actual killed parts without intermediate
conditioning. After19, apply the full-uniform23 and29 clipped kernels
with delta23=delta29=1/2, including every actual current-ending class.
The resulting actual killed29 mass, measured relative to the supported
13 probability, is at least

    s29>=0.0305420818309208542506395...>0.          (P2)

The exact rational in the certificate governs this bound. All original
prime-power heights are retained, including earlier powers appearing
in later-ending moduli. Thus every family with these primes through29
and condition(P1) has a surviving residue. This is a restricted source
result through29; no assertion about31 or arbitrary later primes is
made. No source LP, new test optimizer, or Lean declaration is used.

## The same finite source and the positive13 denominator

Use329's existing full nine-vertex raw account. Write its constants as
P,D,n0,e and C0, so its exact-face centered comparison is

    N<=n0*S+P, E>=e*S-D.

For delta=1/4000 and lambda=1+7delta,329 gives, for every original
nonnegative increasing convex cost, domination of its centered raw
source operator by lambda times the corresponding exact-face operator.
It also gives

    S>=Smin=1/5-(263/360)delta,
    V_mean(mu357)-S<=13/20+(91/60)delta=:M,
    Pdelta=lambda*P,
    Ddelta=lambda*D+(1/7986)(91/420)delta.          (P3)

Here mu357 is the actual mixed7 survivor measure and has mass S.
The last denominator remains the original complete AP11/AP13
denominator; no later mass has been substituted for it.
The complete comparison therefore has

    E(S)=e*S-Ddelta,
    U(S)=C0+[n0*S+Pdelta]/E(S).

At Smin the exact readings are

    E>=0.124468399045035294192894...,
    U<=325.987222330708106525843...,
    d:=403-U>=77.0127776692918934741575... .        (P4)

E increases and U decreases with S. Using the largest allowed delta
enlarges both raw constants and lowers Smin, so(P4) holds throughout
(P1). The supported13 probability exists because E>0.

## Complete physical product-count comparison

The actual AP prefix cylinder caps are

    (p,c)=(11,5/3),(13,12/7),(17,2),(19,9/5).

The first two are the AP11/T4--AP13/T5 construction; the last two are
the prescribed17/T8 and19/T8 physical kernels. At each step the cap is
uniform conditional on its entire physical history. The existing
original-label comparison therefore bounds an increasing convex test
by independent AUXILIARY counts N_p with

    Pr(N_p=1)=1-c/p,
    Pr(N_p=n)=c(p-1)/p^n, n>=2,
    E N_p=1+c/(p-1).                              (P5)

This independence belongs to the comparison counts. It is not an
independence assumption on the actual masks or kernels. At every
auxiliary rectangular outcome, each original exponent tuple retains
its own original357 load. Distinct tuples may use different residues;
Jensen is applied before the uniform own-test source bounds.

Consider a hinge threshold t and a post13 list Q, either empty,
{17}, or{17,19}. Let W be the product of all counts in{11,13} union Q.
Write p_n=Pr(W=n) for n<t and

    T0=Pr(W>=t), T1=E[W 1_(W>=t)], c=T1-t*T0.

For each of the nine full-J source vertices theta define

    A_theta=sum_(n<t) p_n*n*B_theta((v-t/n)_+),
    A*=max_theta A_theta,
    Cdelta=lambda*A*+T1*M.                       (P6)

B_theta is the complete raw357 operator already used in328/329.
Since t/n>1, each hinge in the finite sum vanishes at one.329's
centered domination thus applies directly, without a mass correction.
For every n>=t, the compared load is a sum of n original loads, all
at least one; hence its hinge equals their centered sum plus n-t.
Using(P3) for each own load gives the complete unnormalized physical
prefix upper

    integral physical compared hinge <= Cdelta+c*S. (P7)

T0 and T1 are the exact complete product-law tails. No count or exponent
tail is discarded. Separate convexity of the raw operators extends
the nine bounds across the containing source face and329 transports
them to the actual finite source.

## Center before the sole13 conditioning

Let Z be the product of the POST13 auxiliary counts only, and put

    f=E(Z-t)_+.

For each actual through13 point, its post-count comparison functional
is at least f because every original through13 test load contains its
unit term. Thus the part removed at11/13 has at least this floor.
If the actual unnormalized killed13 mass is z>=E(S), the upper after
normalization is

    f+[Cdelta+(c-f)S]/z
      <=f+[Cdelta+(c-f)S]/E(S).                    (P8)

The floor is computed from Z, not from the full product W: it must
hold pointwise before the13 conditioning. The script verifies
c>=f>=0 and the shifted numerator is nonnegative, so the displayed
denominator substitution has the correct direction.

For fixed error budget, the last expression decreases with S because
its derivative numerator is

    -e*Cdelta-(c-f)Ddelta<0.

It increases with the source error budgets. Therefore the same Smin
and delta used in(P4) give uniform bounds for the complete tests:

| Observation | Post13 factors | Uniform upper |
|---|---|---:|
| H13,8 | none | 1.7372557403287556... |
| H17,8 on the normalized physical17 input | 17 | 2.457531792571307... |
| H19,11 on the physical17/19 law | 17,19 | 2.284050038829753... |

Each is a maximum over all its own complete original tests. The
nonnegative H19,11 hinge on the actual killed19 law is bounded by its
physical counterpart. No signed objective is transferred by this
domination.

## Actual killed masses and the established23-to29 criterion

Let eta19=nu13 K17^- K19^- and m=eta19(1). The existing complete
assigned-charge estimates for pure-base T8 give

    b17<=H13,8/8, b19<=H17,8/10,
    m>=1-b17-b19>=0.537089853201774827684922... .   (P9)

Here b19 is evaluated on the normalized physical17 source. Positivity
of the union bound does not assume independence of actual bad events.
The exact killed composition can only retain additional overlap mass.

The SAME original52-cost comparison includes T13(81). Thus22(KC13),
as used in327(HC1), gives for every complete original through19 test A

    integral(A^2-484)deta19<=-d,
    Gamma19(eta19)<=484m-d.                       (P10)

This implication requires only U<403 and the same source and kernels;
the aligned numerical guard of327 is replaced by(P1)--(P4). J itself
is not inserted as a Gamma bound. No new frozen statement or abstract
continuation theorem is being asserted.

Now reuse327(HC4)--(HC8), with its full23 and29 geometric coefficients.
The actual final killed mass obeys

    s29>=(41/196)m+(155/94864)d-H19,11/11.          (P11)

Both positive coefficients use the valid lower bounds(P4),(P9); the
negative hinge term uses its valid nonnegative physical upper(P8).
The exact positive slack is

    (451/196)m_lower+(155/8624)d_lower-H19,11_upper
       =0.335962900140129396757034...>0.

Dividing it by11 gives(P2). The23 and29 kernels are normalized even
on fully forbidden fibres, so the calculation does not condition on
an unproved positive intermediate mass. Final positivity supplies
actual survival through29, while later primes remain outside scope.

The [portable helper](../../frontier/moments-survival/high_rho_survival_through29.py) reconstructs every
product-law low atom and exact tail, all27 raw hinge vertex accounts,
the same source/mass inequalities and the final rational slack. It
consumes artifacts through certificate_io and records the input and
proof hashes. Its write/check modes use exact fractions and no solver.

Run the [exact certificate](../../certificates/source_norms/moments-survival/high_rho_survival_through29.json) replay with:

```sh
python3 -I -S -O docs/reports/erdos7-odd-covering/frontier/moments-survival/high_rho_survival_through29.py --check
```
