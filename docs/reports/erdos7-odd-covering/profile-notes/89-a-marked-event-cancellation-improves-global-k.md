[Index](../marked_head_profile.md) · [Original event payments](47-six-linear-tests-share-the-survival-carrier.md) · [True conditional functions](49-full-linear-and-quadratic-carriers-refine-the-frontier.md) · [Global decomposition](74-a-strict-global-k-gain-from-the-controlling-faces.md) · [Broad source packing](85-a-broad-five-slot-source-deletion-tradeoff.md)

# A marked-event cancellation improves global K

Retaining the original zero-seven modulus5 test event, while
cancelling its already-paid shallow deletion against the same
actual virtual measure, gives the complete global bound

    K<=33908684762398767967218824063093944440044324845639
          /66558143848442620628509895712956736352500000000
      =509.4595912952610054691277... .                    (MG1)

This improves profile74 by0.0010955301886802600775171... . It
holds throughout the inherited source split, including all eight
fallback branches. Every original test keeps independent residues,
all exponent tails remain complete, and the signed comparison uses
one actual surviving mass. The terminal threshold403 is not reached;
both complete-core gaps and unrestricted Erdos #7 remain open.
This is an ordinary proof and exact rational certificate, not Lean
verification or an extremal-family construction.

## 1. Remove exactly one event from the old identity floor

Direction40 has cost f(v)=v, fixed barrier C=6, and selected source
cost psi(v)=v-1. Keep the actual zero-five and first-positive-five
ternary layouts b,c5 of profiles47 and49. Put

    k_l=6-b_l, t_l=c5_l.

The three selected zero-seven events are I5,I15,I45. The marked
event I5 has the full ternary carrier and its own independent first
five slot F_J. Write h=sum_l eta_l and

    L_J=h/5-mu(F_J),                                 (MG2)

where mu is the actual surviving old marginal. The old fixed-layout
source bound U retains the three source payments separately.
If a selected test label is absent, pad that test load by an arbitrary
residue of the missing label before bounding it. This only increases
its nonnegative identity cost on the same survivor; it does not add a
forbidden deletion or alter the actual source, V, delta or pi. A lower
bound for the padded deficit therefore also bounds the original deficit.
Because the identity increment and the psi increment equal1, the
payment for I5 is exactly h/5-Lambda(F_J). Subtracting I5 from the
raw test load therefore changes its source upper bound from

    U-[h/5-Lambda(F_J)]-(other two payments)

to U-h/5-(other two payments). Restoring the marked event after
survival adds its exact mass mu(F_J).

The deletion floor must change at the same time. Originally

    (6-A)_+<=k_l-I5-I15-I45,

where the right side is nonnegative. For the remaining load A-I5,

    (6-A+I5)_+<=k_l-I15-I45.                        (MG3)

This right side is also nonnegative. Thus the exact old proof of
the other two event payments applies with correction t_l-1 in
place of t_l. No I15 or I45 payment is reused. The positive-seven
source complement and all unselected deletion terms stay intact.

Let W_c(k,t) be the old conditional cofactor expression before its
outside complete-seven factor1/5. For c=(r,j), allowing empty root
or cell components, its selected shallow and deep terms are

    A_c(k*n-t*eta/5)+(13/243)*max_l(k_l*d_l-t_l/5).

The other terms do not involve t. On replacing t by t-1, every
deep entry increases by the same1/5, so its maximum increases
by1/5 as well. Consequently, after the outside1/5,

    [W_c(k,t-1)-W_c(k,t)]/5=P_c,
    P_c=[1_(r present)*eta(root r)
                  +1_(j present)*eta_j+13/243]/25.    (MG4)

This is an exact identity, independent of b,c5. The coefficient
13/243 is the full sum of3^-a for a=3,4,5. The unselected pure3
tail a>=6 remains1/486; together they remain1/18. Positive-five
cofactor terms have not changed.

Minimizing over the same independent original layouts and then
averaging with the actual carrier mixture pi gives

    d40_actual>=m40_old+L_J-Pbar,
    Pbar=sum_c pi_c*P_c,
    m40_old=sum_c pi_c*m40(theta;c).                 (MG5)

Here m40 is the true globally defined profile49 function, including
where a stored table uses a weaker predecessor bound. Since the
unchanged old bound also holds,

    d40_actual>=m40_old+[L_J-Pbar]_+.                (MG6)

As a coarse independent check, P_c<=121/6075 for every carrier:
eta(root)<=1/3, eta_j<=1/9, and
(1/3+1/9+13/243)/25=121/6075. The stronger argument below retains
the exact shallow part of Pbar instead of discarding its relation
to the actual deletion.

## 2. Cancel the old shallow payment on the same actual source

Let V be the full virtual mixed7 deletion measure and delta its
actual projected union. Then

    mu=Lambda-delta, V-delta>=0, omega=(V-delta)(1).

Write V3=v3*Lambda and V9=v9*Lambda for the complete original
cofactor3 and9 virtual families. The densities v3,v9 depend only
on the ternary coordinate. Each is at most sum_e6/(5*7^e)=1/5,
including missing labels by zero. The carrier mixture pi uses
exactly these original seven weights and includes empty carriers.
Thus(MG4) gives

    Pbar=Psh+Pdeep,
    Psh=(1/5)*integral(v3+v9)deta,
    Pdeep=13/6075.                                 (MG7)

Define the raw product measure and its actual source hole by

    M=eta tensor Haar5, W=M-Lambda>=0,
    x_J=W(F_J)=h/5-Lambda(F_J).

W includes every original35 source deletion, including the late
ones. It is a measure difference, not an independently chosen law.
Since F_J has Haar5 mass1/5 and v3+v9 depends only on ternary
coordinates,

    (V3+V9)(F_J)=Psh-integral_(F_J)(v3+v9)dW
                 >=Psh-(2/5)*x_J.                  (MG8)

This holds for every independent choice of the marked test slot J.
The full virtual cofactor5 and15 measures V5,V15 are disjoint
label subfamilies of V. Using the one error measure V-delta,

    delta(F_J)>=(V3+V9+V5+V15)(F_J)-omega.

Because L_J=x_J+delta(F_J), equations(MG7)--(MG8) imply

    L_J-Pbar>=(3/5)*x_J+V5(F_J)+V15(F_J)
                                  -omega-Pdeep.    (MG9)

The shallow payment Psh has cancelled exactly. It is not added
again as a new gain. Only the full selected deep payment remains
on the right, with its negative sign.

## 3. The broad slab supplies a positive marked-event surplus

Use profile85's notation

    Delta=(z-3/4)+(1/4-alpha1)+(1/4-beta2-beta3-beta4),
    r=min_J x_J, H in argmin_J x_J, m=Lambda(F_H),
    rho=S-S0, S0=sum_c pi_c D_c,
    E5=h/25-V5(1), E5+omega<=rho.

On the entire slab Delta<=1/18, its small-loss branch has

    r<r0=1/12000,
    x_J>=r+G for J!=H, G>=g0=397/36000,
    m/5>=1199/60000,
    max(1,m/G)<=c0=4000/397.                        (MG10)

The bad-carrier weight q5 for the complete original cofactor5
family, including absent labels, satisfies

    E5>=r/5+G*q5,
    V5(F_H)=m*(1/5-q5)
             >=m/5-(m/G)*(E5-r/5).                 (MG11)

If J!=H, discard the nonnegative V5,V15 terms in(MG9). Since
omega<=rho and c0>=1,

    L_J-Pbar>=3*g0/5-c0*rho-Pdeep.                 (MG12)

If J=H, keep only V5 and apply(MG11). Both E5-r/5 and omega
are nonnegative, their sum is at most rho-r/5, and m/G<=c0.
Therefore

    L_H-Pbar>=(3/5)*r+m/5-c0*(rho-r/5)-Pdeep
             >=3*g0/5-c0*rho-Pdeep,               (MG13)

using1199/60000>3*g0/5. This pays the union/cap loss once. V15
is not needed for this uniform lower bound and was dropped in
the direction valid for it.

Combining(MG6), (MG12) and(MG13), every original identity test on
the whole small-r slab satisfies

    d40_actual>=m40_old+[a_mark-c0*rho]_+,
    a_mark=3*g0/5-Pdeep=21757/4860000>0.             (MG14)

The same original modulus5 label and its exact carrier payment
are used throughout. No arbitrary slot gain is added to an old
comparison without removing its prior payment.

## 4. Globalize using the old escape and actual mass terms

Let K0 be profile53's target and K74 the preceding global target.
Profiles71 and74 give, for the old sufficient signed comparison,

    Phi_K0_old>=B+A0*rho,
    B>=gamma_K*(1-zeta), zeta=q(Z_K),
    A0>=Acur>0,                                   (MG15)

where Acur is the smaller signed mass coefficient at K74. All
terms use the same actual S, source parameters and pi. The exact
positive comparison weight of direction40 is

    beta40=94212612766226/1174116234095805.

The whole-slab bound(MG14) is already positive after the mass
penalty is absorbed. The global comparison improves further by
using the carrier information supplied by the same concentration
that controls its source. Fix

    delta_star=13/500, r_star=1/8000.

If zeta>=1-delta_star, profile71 selects one entire K zero face
whose distinguished carrier has pi-weight at least1-delta_star.
That carrier is(root1,cell0) or(root1,cell1); its root and cell
are disjoint. Every other carrier has pointwise multiplicity at
most2. Since the actual mixture satisfies

    v3+v9=(1/5)*sum_c pi_c*(1_(root c)+1_(cell c)),

we get the stronger pointwise estimate

    v3+v9<=(1+delta_star)/5,
    w_star=1-(1+delta_star)/5=1987/2500.             (MG16)

Thus(MG9) holds with w_star in place of3/5. This uses the actual
pi already present in Pbar; it introduces no independent carrier
distribution.

The same zero-box concentration gives

    Delta<=3*delta_star/4<1/18,
    eta_star>=1/9-delta_star/18,
    h1>=1/3-delta_star/6.

For the first inequality each common alpha, beta and z factor
has weight at least1-delta_star. For the other two, the supported
deficit lies in one of the root0 cells with barycentric weight
at least1-delta_star, so each root1 deficit is at most
delta_star/2. The displayed h1 bound adds these three valid
coordinate bounds; it need not be sharp.

If r<r_star, all hypotheses of85's general packing lemma hold.
In particular r<min(h/5,h1/5,eta_star/5), and the first source
slots are distinct because Delta<1/18. The four entries of its
general gap G are bounded below by

    799/8000, 2627/40000, 2617/120000, 52469/2000000.

Consequently

    G>=g_star=2617/120000,
    max(1,m/G)<=c_star=40000/7851,
    m/5>=1/50-r_star/5>w_star*g_star.               (MG17)

This uses85's general gap and payment formulas, not its special
r0 cutoff. Repeating the two cases J=H and J!=H from section3
with w_star,g_star,c_star gives

    d40_actual>=m40_old+[a_star-c_star*rho]_+,
    a_star=w_star*g_star-Pdeep
          =369198299/24300000000.                  (MG18)

The exact mass coefficient satisfies Acur-beta40*c_star>0. Thus

    Acur*rho+beta40*[a_star-c_star*rho]_+
      >=beta40*a_star+(Acur-beta40*c_star)*rho
      >=R,
    R=1023033422871656583811/839147779074354750000000
      =0.0012191338026302613793843... .              (MG19)

Here[x]_+>=x handles every actual rho. If instead r>=r_star,
the always-valid E5>=r/5 gives rho>=1/40000. If
zeta<1-delta_star, the old escape term is at least
gamma_K*delta_star. The exact checks give

    gamma_K*delta_star>R,
    Acur/40000>R.

These cases cover every actual effective9 source. They use the
old globally defined functions in(MG15), followed by a pointwise
choice of the better valid identity bound. They do not assert
concavity of a table with improved entries.

## 5. Lower the target and retain every fallback branch

The comparison denominator E from74 satisfies

    0<rho53*S<=E<=S<=s<=5/9.

Its positivity is inherited from the complete old survival
comparison before division. Changing the target by h changes the
sufficient signed expression by exactly-hE. Take

    h=9*R/10
     =1023033422871656583811/932386421193727500000000,
    Knew=K0-h.

Then

    Phi_Knew_new>=R-(5/9)*h=R/2>0,                 (MG20)

which proves(MG1). The smaller Acur was used only as a conservative
lower bound in(MG15); h is subtracted from K0 directly. There is
no second charge of the preceding74 decrement.

The checker verifies Knew>K_offset and positivity of the new
signed mass coefficient. Each of the eight inherited complete
fallback bounds stays below Knew. The separate J, Gamma13,
T13(81), survival and complete-core errors retain their preceding
values; both terminal gaps remain positive after this improvement.

## 6. Completeness and reproduction

The selected event source payments are retained before any cap or
layout maximum. The exact cap shift(MG4) leaves all positive-five
and unselected pure3 terms unchanged. The seven weights in every
virtual measure sum over all depths. The nonnegative measure
identities and complete geometric domination justify limits of
finite families and changing independently chosen residues.

The [checker](../frontier/broad_marked_identity_global.py) pins the
old true conditional operator, the broad source theorem, and the
global signed comparison. It checks all100 finite original
layout floors, the identity source tail, exact carrier-payment
coefficients including empty carriers, complete selected and
unselected tails, the shared residual arithmetic, all three global
cases, target signs, eight fallbacks and both complete-core gaps.
The ordinary proof above supplies the measure cancellation and
continuous-domain quantifiers; finite floor checks do not stand
in for those arguments. The
[certificate](../certificates/source_norms/broad_marked_identity_global.json)
retains the exact rational target, reserve and dependencies.

```sh
python3 -I -O docs/reports/erdos7-odd-covering/frontier/broad_marked_identity_global.py --check
```
