# Whole comparison fields cover every row0 column2 leaf5 layout

For square7 roles(row0,column2,leaf5), a library of ten complete priority95 fields pays the uniform head gate193/100000 for every independent assignment of the ten labelled live9qs roles. One field is selected for an entire layout and used for every weak corner and every support query. The fields may have different regular-leaf symmetries; the selected field is transported together with its layout and source.

The low-pure stabilizer transports this base result to precisely

    {0} x {2,3} x {4,5}.                           (H1)

Together with [Report672](672-a-constant-field-releases-both-regular-leaf-column0-roles.md), the certified live domain is

    ({0,1} x {0,1,2,3} x {4,5})
      union ({0,1} x {0} x {0,1,2}).               (H2)

This is twenty-two of the forty live triples and eight of the twelve role orbits. The remaining eighteen triples are exactly{0,1} x {1,2,3} x {0,1,2}, in four role orbits. No regular-leaf role is added byH1.

The fixed low pure phases2mod3,1mod9,4mod5,1mod25, central15 phase0, literal head primes, forty linear-star and ten qs incidence conditions when present, globally fixed original phases, and complete ordinary/private interfaces remain. Arbitrary finite higher-pure and admitted outside phases retain their previous scope. Each [Report666](666-four-parent-owners-return-on-the-complete-leaf-pair-head.md) policy retains its own stated density: the principal cutoffs137/191 give greater than1/(2000000 Q_off), whereas its common cutoff149 gives greater than1/(10000000 Q_off). Report672's stronger four-parent-from61 bound greater than1/(200000 Q_off) still belongs only to its six regular-leaf column0 triples. It is not extended toH1. This is an ordinary mathematical result with exact finite certificates, not new Lean verification or unrestricted Erdős #7.

## 1. One fixed source and its complete responses

Use the80 live cells(l,m),95 weak corners(i,j), head primes7,11,13,17,19 and ten lexicographically labelled pair edges of671. The unary comparison masses are

    Z7(l,m)=5/6-[1_(floor(l/3)=0)+1_(floor(m/5)=2)+1_(l=5)]/35,
    Zq=(q-2)/(q-1)-2/[q(q-2)] for q>7.             (H3)

The row0 and leaf5 deletions cannot coincide, so at most two terms inH3 are active. A single globally fixed tuple alpha of pair leaves determines

    beta_e(l,m)=b_e+d_e 1_(l=alpha_e).              (H4)

All matching responses, queried supports and selector maxima use this same tuple. The complete512 source charges, four guarded9q² additions, projection2673/110656 and full continuation budgets remain unchanged. Local response tables are computed for all decrement counts0 through3, all1024 activated-edge subsets and all32 queried supports; no source tail or selector is omitted.

For every field, both priority inequalities hold over all95 corners and80 live cells. The original559 literal selectors across the sixteen central query modes reduce only by exact componentwise domination between nonnegative profile vectors. A retained selector is always an original selector. This preserves each maximum for every nonnegative response vector; it is not a numerical guess about the maximizing query.

## 2. Different field symmetries do not change the legal layout quotient

The square7 leaf5 and row0 are fixed by permutations of regular leaves0,1,2. Canonical pair layouts use a restricted-growth string: at each numerical edge visit4,5, then the previously introduced regular labels followed by the next new label, up to2. Numerical edges and prime identities are never permuted. This gives

    (5^10+3*3^10+2*2^10)/6=1,657,470 representatives,
    5^10=9,765,625 actual labelled layouts.         (H5)

The witness attached to a representative need not be regular-S3 invariant. For a noncanonical layout, choose a regular-leaf permutation taking it to its representative, apply the representative's entire chosen field, and transport every field cell, weak corner, query and original phase back. The two priority inequalities and complete gate are preserved under this bijection. There is no requirement that the transported field already occur as a separate member of the finite canonical library.

Explicitly, if pi sends alpha to its canonical representative r and theta_r is its chosen whole family, set

    theta_alpha^(i,j)(l,m)=theta_r^(pi(i),j)(pi(l),m). (H8)

The first priority follows by comparing theta_r^(pi(l),j)(pi(l),m) with theta_r^(pi(i),j)(pi(l),m); the second follows by comparing theta_r^(pi(i),m)(pi(l),m) with theta_r^(pi(i),j)(pi(l),m). Both use the original priorities at the transported cell. The matching responses obey H_(pi alpha),T(pi(l),m)=H_alpha,T(l,m), and the literal prefix selector menus are permuted bijectively. Consequently the complete gate at(i,j) for alpha equals the gate at(pi(i),j) for r. The permutation and whole field are fixed once per original family. The argument does not require the chosen295-key field itself to be S3 invariant.

Inside a fixed layout, all five weak ternary markers remain separate. Only permutations within a fixed quinary column reduce95 corners to20 representatives with ternary markers0,1,2,4,5 and quinary markers0,6,10,15. Column2 remains distinguished in this calculation. The between-role exchange of columns2 and3 is applied only after completing the fixed-column theorem and moves the source and entire field with it.

Eight library fields use180 keys under regular-S3. Two use295 keys under the subgroup fixing0 and exchanging1,2. For the latter fields, the key for ternary corner i and source leaf l uses the lexicographically smaller of(i,l) and its simultaneous1/2 swap; it also retains both quinary column indices and the equality of the two quinary markers. The exact key list and numerator list for each field are retained separately. No per-corner field choice is made.

## 3. Finite full-domain certificate

The [preparer](../../frontier/cover-geometry/row0_column2_full_layout_prepare.py), [integer evaluator](../../frontier/cover-geometry/row0_column2_full_layout_checker.cpp), [reproduction wrapper](../../frontier/cover-geometry/row0_column2_full_layout_certificate.py) and [result](../../frontier/cover-geometry/row0_column2_full_layout_certificate.json) reconstruct the exact rational gate from pinned640/658 inputs. All nonconstant field denominators are2^20. Response intervals have mesh2^-32, source coefficients round downward, nonnegative charges round upward, and the complete lower gate rounds downward to2^-40. Integer accumulator bounds are checked before evaluation.

The acceptance threshold is2122057442, the ceiling of2^40 times193/100000. The family order is fixed in the certificate. The unit field is first; the remaining fields are six rounded convex mixtures, one regular-S3 field and two fixed0 fields. Each mixture with weight k/8 is explicitly stored as

    floor(((8-k)*2^20+k*n_u)/8)/2^20,             (H6)

coordinatewise, where n_u is the indicated anchor numerator. Every final stored field, including these rounded mixtures, is checked directly; no positivity is inferred solely from convexity or an unrounded LP output. The field library is not claimed minimal.

Both complete scans pass all1,657,470 canonical layouts, representing9,765,625 labelled layouts. The producer's complete minimum is

    2122075175/2^40 > 193/100000.                  (H7)

It occurs at layout(0,0,5,5,0,5,1,5,5,4), where the constant field is selected. The producer's RGS index is1,120,564. The separately ordered independent enumeration reaches the same actual labelled layout at rank191,946; ranks from the two orderings are not interchangeable.

| Whole field | Selected canonical layouts |
| --- | ---: |
| constant_one |1,607,035|
| B_mix_1_of_8 |25,540|
| B_mix_2_of_8 |11,851|
| B_mix_3_of_8 |6,076|
| C_mix_5_of_8 |6,392|
| C_mix_6_of_8 |331|
| C_mix_7_of_8 |143|
| C |1|
| D |24|
| E |77|

The preparer checks678,065 exact conditions and the final summary checks26 further conditions. The producer records33,345,702 attempted corner gates and17,038,197,879 nonzero charged screens, including rejected attempts before the winning whole field is found.

The independently authored [numeric verifier](../../frontier/cover-geometry/square7_heterogeneous_full_layout_independent_driver.py), reusing the unchanged [671 evaluator](../../frontier/cover-geometry/square7_role_full_layout_independent.cpp), reads only the pinned640/658 inputs and the exact final witness fields. It reconstructs its own matching responses, keys, selector menus and layout enumeration without reading or importing the producer code. Its [result](../../frontier/cover-geometry/square7_heterogeneous_full_layout_independent_driver.json) records1,006,923 passing checks; the per-field counts andH7 agree exactly. It records33,149,400 accepted corner gates and16,972,492,800 complete selector maxima including zero-fee supports. These independent counts have a different denominator of work from the producer's attempted-gate and nonzero-fee counts. Both implementations use the same agent/model family; this is not a claim of different-model consensus or Lean verification.

```sh
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/row0_column2_full_layout_certificate.py --threads 8
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/square7_heterogeneous_full_layout_independent_driver.py --threads 8
```

Both canonical entry points run without temporary-source imports or an optimizer. The independent entry reads the fixed numerical fields from the producer result, and reconstructs all arithmetic itself.

The direct network arithmetic in this layout certificate uses658's original three-parent budget. The public gate193/100000 then supplies the existing666 policies through their already established actual-law and full-cap interfaces; it does not replace those interfaces.

## 4. Actual-source transport and remaining obligations

For every fixed original family, choose one canonical representative and one whole field. Apply the existing two-priority comparison to the actual higher-pure source, preserving the original low pure phases and central15 mask. Missing or source-null pair originals admit the established per-original fixed-leaf padding and cap-ratio comparison. None of these choices may vary independently by weak corner, source row or query.

The selected field stays at most one. Moving the square7 role changes the unary deletion profile but does not enlarge the pure comparison laws or omitted-coordinate upper masses. The owner kernels retain their actual forbidden union on that same law, with the inherited h values, full-Haar caps and Euler factors. The new source lower bound is not used to renormalize an inherited cap. Complete exponent tails, ordinary TypeI and arbitrary-parent charges and private/CRT reconstruction retain their original hypotheses.

Finally exchange quinary columns2 and3, or exchange leaves4 and5, carrying the square7 role, pair markers, field and source jointly. This gives exactlyH1 and henceH2. Releasing the four remaining regular-leaf orbits, the fixed low-pure source geometry, all head-incidence restrictions and unrestricted parent counts below the final switches remains outside this result.
