# Three whole-field libraries complete all live square7 roles

Under the fixed low-pure geometry and inherited incidence and continuation hypotheses, the remaining base roles(row0,column1,leaf0), (row1,column1,leaf0) and(row1,column2,leaf0) each have a whole priority95 field library paying the gate193/100000 for every independent assignment of the ten labelled live9qs roles. The first two roles share a three-field numerical library; the third has nine fields. The source responses are recomputed for each role, so sharing a field library does not identify the three source laws.

Whole-data regular-leaf relabelling and the quinary column2/3 exchange add precisely

    ({0} x {1} x {0,1,2})
      union ({1} x {1} x {0,1,2})
      union ({1} x {2,3} x {0,1,2}).               (R1)

These twelve triples are disjoint from the twenty-eight retained by [674](674-product-stabilizer-fields-release-row0-regular-leaf-columns23.md). The combined live domain is exactly

    {0,1} x {0,1,2,3} x {0,1,2,4,5}.              (R2)

Thus all forty live triples and all twelve role orbits of this fixed low-pure geometry are covered. The ten pair markers may independently occupy any live leaf in each case.

The fixed low pure phases2mod3,1mod9,4mod5,1mod25, nulls, central15 phase0, literal head primes, fifty head-incidence conditions when present, globally fixed original phases and complete ordinary/private interfaces remain. The result removes the live square7 role restriction within that conditional class. It does not remove these source conditions, does not allow unrestricted owner parent sets below the stated switches, and does not settle unrestricted Erdős #7. Missing and source-null originals retain only the separately established global per-original padding and source-null interfaces. No new Lean verification is asserted.

The original666 policies retain their stated densities on the newly admitted roles. Its principal cutoffs137/191 give greater than1/(2000000 Q_off), and its common cutoff149 has the separate1/(10000000 Q_off) bound. The stronger four-parent-from61 bound1/(200000 Q_off) from672 still belongs only to the six regular-leaf column0 triples.

## 1. Role-dependent responses on one fixed source

For each specified(r,s,0), the square7 unary response is

    Z7(l,m)=5/6-[1_(floor(l/3)=r)+1_(floor(m/5)=s)+1_(l=0)]/35. (R3)

The maximum live deletion count is3 for(0,1,0), and2 for each row1 role. The shared exact response construction retains all counts0 through3 and the uniform lower bound157/210. The other unary masses, globally fixed pair activations beta_e=b_e+d_e1_(l=alpha_e), complete512 fees, four guarded9q² additions, projection2673/110656 and complete selector menus are unchanged.

A role, layout and selected field are fixed together throughout every weak corner, queried support and continuation use. The same original phases determine every pair activation. No maximizer, phase or field is selected independently for different corners.

## 2. Product-group layout reduction and complete corner obligations

For fixed leaf0, the commuting swaps(1 2) and(4 5) preserveR3, the nulls, low pure deletions, central15 mask,80 live cells and95 weak corners. They act on whole rooted cylinders, all original phases, the entire layout, source, field and selectors. Numerical edge positions and prime labels remain fixed.

There are

    (5^10+2*3^10+1)/4=2,470,931 canonical layouts  (R4)

per base role. The orbit counts for sizes1,2,4 are1,59,048,2,411,882; the weighted actual count is9,765,625. The first occurrence from{1,2} is named1 and the first from{4,5} is named4. Both the C++ evaluator and a separate Python traversal check this full census for every branch.

A field paying a canonical layout is transported in its entirety to each orbit mate, together with source cells and weak markers. The field library need not be closed under the group. The layout quotient never removes a ternary weak marker inside a fixed mixed layout. All five ternary weak markers remain, and only the within-column quinary symmetry gives20 representative corners for all95.

## 3. The finite whole-field witnesses

The common column1 library comprises the unit field, the whole4/5 transport ofB_mix_1_of_8 from673, and the transported strong670 field. Both role branches validate that same exact field data against their respectiveR3 responses.

The row1 column2 library comprises the unit field; B_mix_1_of_8; the transportedB_mix_1_of_8,B_mix_2_of_8,B_mix_3_of_8,C_mix_5_of_8,C_mix_6_of_8; and two explicitly retained transported fixed0 fields. The last two use295 keys under the1/2 swap; all other fields use180 regular-S3 keys. All nonconstant denominators are2^20. The exact numerator and key arrays are independently retained per branch.

Every final stored field is checked directly for0<=theta<=1 and both priority inequalities on all95 corners and80 cells. All559 original literal selectors are accounted for. Compression of a selector menu removes only an original nonnegative profile vector componentwise dominated by another retained original vector, so every maximum is preserved for nonnegative responses.

## 4. Complete finite verification

The shared [preparer](../../frontier/cover-geometry/regular_leaf_remaining_roles_prepare.py) requires explicit--row and--column and admits only the three stated roles. The binary header carries row, column and leaf, and the [checker](../../frontier/cover-geometry/regular_leaf_remaining_roles_checker.cpp) rejects every other role. The [wrapper](../../frontier/cover-geometry/regular_leaf_remaining_roles_certificate.py) constructs three separate inputs and runs the complete product-group domain on each. Each branch of the [result](../../frontier/cover-geometry/regular_leaf_remaining_roles_certificate.json) carries its own role, field data, hashes, selected-family census and compressed complete selection.

The response mesh is2^-32, source coefficients round downward, nonnegative charges upward, and the full gate lower bound rounds down to2^-40. Bounded integer accumulator checks precede evaluation. Acceptance requires at least2122057442, the ceiling of2^40 times193/100000. The implementation's early rejection is valid because all omitted charges are nonnegative.

Each independently reconstructed branch passes all2,470,931 canonical layouts, representing9,765,625 actual labelled layouts. The independent complete lower numerators at2^-40 are:

| Base role | Complete lower numerator | Selected family counts |
| --- | ---: | --- |
| (0,1,0) |2,123,626,650|2,470,885;31;15|
| (1,1,0) |2,122,242,590|2,470,288;351;292|
| (1,2,0) |2,122,069,660|2,457,875;192;8,458;3,191;983;53;1;171;7|

The family counts follow the exact order in each result branch. All three lower bounds exceed193/100000. Their respective minimum actual layouts are(4,4,4,4,4,4,1,0,4,4), (4,4,4,4,0,4,4,4,5,0), and(4,0,4,4,4,4,1,1,0,5), each paid by the unit field.

The independently authored [verifier](../../frontier/cover-geometry/regular_leaf_remaining_roles_independent_driver.py) reuses674's unchanged independent engine and records its three branches in a [combined result](../../frontier/cover-geometry/regular_leaf_remaining_roles_independent_driver.json). It pins the full numerical witness container and each derived branch input, then reconstructs the matching responses, complete selector menus and product-group domain directly from the pinned640/658 inputs and exact field data. It does not read or import the producer implementation. Its577,636,577,636 and945,505 explicit checks pass. Each branch evaluates49,418,620 accepted corner gates and25,302,333,440 accepted complete selector maxima, including zero-fee supports.

The final producer also passes all three complete domains after discarding every unused field. Its lower bounds, actual minimum layouts and selected-family counts agree exactly with the independent table. The two column1 preparations each check478,599 exact conditions; the row1 column2 preparation checks649,478, for1,606,676 preparation checks in total. The three summaries add81 checks.

In branch order, the producer records49,418,772,49,421,074 and49,507,849 attempted corner gates, with25,252,991,739,25,254,156,727 and25,298,046,823 nonzero charged screens. These counts include rejected whole-field attempts and use a different screen convention from the independent accepted-count totals.

A separate ordinary-mathematical review checksR1–R4, actual-source transport and the inherited density scope. Its229,983 exact checks concern field keys, both priorities and role/layout transport contracts; they do not constitute another full-layout run. The implementations and reviews use the same agent/model family, not different-model consensus or Lean verification.

```sh
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/regular_leaf_remaining_roles_certificate.py --threads 2
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/regular_leaf_remaining_roles_independent_driver.py --threads 2
```

The argument to--threads is the thread count per branch; the wrapper runs three branches concurrently. Direct arithmetic inside each branch uses658's complete three-parent budget, and its uniform gate supplies666's separately proved continuation policies through the same actual-law interfaces.

## 5. Actual-source transport and the exact completed domain

Apply the established actual higher-pure comparison to each canonical whole field and transport it back once per original family. The comparison field is at most one, so the pure-source domination and omitted-coordinate bounds required by the continuation remain. Owner kernels keep their actual forbidden union, inherited h, full-Haar caps and Euler factors. The new lower source mass is not used to renormalize an old cap. All exponent tails, ordinary TypeI fees, arbitrary-parent tails and private/CRT hypotheses remain attached to that actual common law.

Between distinct distinguished-leaf problems, a regular-S3 relabelling sends0 to1 or2 while moving every alpha marker and source datum. This is different from enlarging the within-role product group while leaf0 remains fixed. The two column1 roles therefore supply three triples each. Exchanging quinary columns2 and3 for the row1 column2 role, again with the entire source and field, supplies six more. These are exactlyR1. Their union with674 isR2.

Complete live-role coverage closes this particular finite role restriction. The fixed low-pure geometry, retained head-incidence inventory and complete continuation conditions remain the next boundaries of the argument.
