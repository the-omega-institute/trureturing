# Sharper private domains admit five parents from sixty-seven

Keep the complete actual head and ordinary/private interfaces of [Report655](655-five-parent-owners-begin-at-seventy-one.md). The following network has a survivor:

| Owner prime | Size of its fixed union of earlier declared head/network parents |
| --- | --- |
| `37 <= v < 67` | at most four |
| `67 <= v < 2^46` | at most five |
| `v >= 2^46` | arbitrary finite size |

Its full survivor density is strictly greater than

    1/(100000 Q_off).

An elementary alternative permits five parents from67 up to2^68 and arbitrary finite parent unions at everyv>=2^68, with the same density bound. The first policy uses Rosser--Schoenfeld1962 Theorem8; the alternative uses the existing elementary prime-product estimate. Each is one complete policy on one actual source.

The head remains655's literal ten primes, central conventions, all fifteen square-star and twenty square-pair originals with arbitrary globally fixed full phases, and the remaining forty linear-star and twenty `qs/9qs` incidences on one fixed live-root table. All ordinary domains, private-interior separation and numerical-label assignments remain unchanged. Every numerical modulus is odd, greater than one, occurs at most once and has one globally fixed residue. An owner has one fixed union of smaller declared parents; private interiors do not become undeclared parents. All network sizes, widths, depths and original exponent heights remain arbitrary finite quantities.

The gain comes from retaining the private-descendant budget already present in [Report599](599-the-ten-prime-head-admits-arbitrary-twelve-vertex-attachments.md). Later network estimates had relaxed this budget to a larger ordinary-domain loss. Recovering the original bound, and calibrating the finite rows to their declared full-Haar caps, supplies almost one extra unit in each finite hinge threshold. This is an application of existing domain and kernel results, not a uniform overlap theorem, new Lean verification, or unrestricted Erdős #7 result.

## 1. The actual ordinary domains retain more mass

At ownerv let `V_v^0` be the actual complete owner words admitting an avoiding extension through its ordinary private block tree. It includes pure-v originals and excludes mixed network originals. Write

    a_v=H_v(V_v^0),
    e_v=sum_(r in actual strict private descendants) 2^(-(r-1)/2).

Report599 HB2–HB3 gives

    a_v>=1-1/(v-1)-2e_v/(v-1).                    (PD1)

[Report600 TP3](600-two-head-coordinates-admit-unbounded-large-prime-entries.md) explicitly retains this bound for an entry's private domain. Reports614 and616 retain the same Report599 ordinary trees, and [Report619](619-one-global-three-parent-network-under-the-root-one-head.md) retains their domains and private interfaces. Reports625,647 and655 change the network rows without adding further exclusions to these ordinary domains.

Every strict private descendant is an actual prime at least37. Their original primes are distinct, and the relevant private interiors are disjoint. Hence

    e_v<=sum_(r>=37 prime)2^(-(r-1)/2)
       <=sum_(n>=18)2^(-n)=2^(-17).

The second bound is the same convergent majorant used in599 HB13. Therefore

    a_v>=D_v^*/(v-1),
    D_v^*=v-2-2^(-16).                            (PD2)

This improves the working bound `(v-3)/(v-1)` by `(1-2^(-16))/(v-1)`. It requires neither alignment nor overlap of mixed owner phases.

There is no omitted extra owner charge in PD2. Pure-v originals enter PD1 once. Mixed owner originals are excluded from the definition of `V_v^0` and are treated next. Ordinary Type I blockers depend on a head coordinate and remain separate head bad events with total charge at most1/65536, as in619 §5 and625 §5. They are not another exclusion from each owner domain. Separate ordinary components and the disjoint private interiors retain the existing final gluing rule.

## 2. A finite row using its declared full-Haar cap

For every finite ownerv with37<=v<1253, choose one integer

    10<=h<D_v^*,
    c_v=(v-1)/h<v/10,                             (PD3)

before sampling any history. At a complete earlier history, letB be the actual mixed forbidden union inside `V=V_v^0`, and put

    a=H_v(V), f=H_v(B), g=H_v(V\B)=a-f.

Use647's normalized relative-domain row with

    theta_actual=1-1/(c_v a).

This is well-defined because `c_v a>=D_v^*/h>1`. Equivalently, set

    r=min(f,a-1/c_v).

Forf=0 the row is Haar conditioned onV. Forf>0 its density relative to full owner Haar is

    k_v=1_V [1-(r/f)1_B]/(a-r).                  (PD4)

The numerator integrates toa-r, the density is nonnegative, and `1/(a-r)<=c_v`. Thus every history has a normalized row supported on its actual ordinary domain and obeying PD3. If the entire ordinary domain is forbidden, f=a and PD4 simplifies to `1_V/a`; it remains normalized with loss1. No surviving fibre has been assumed.

The exact current mixed loss is

    ell_v=(f-r)/(a-r)=(1-c_v g)_+.               (PD5)

This is the existing capped-allocation formula. In655 the fixed relative threshold `1-h/(v-3)` uses an effective full-Haar cap `(v-3)/(ha)`, which can be strictly smaller than its declared cap `(v-1)/h`. PD4 uses the declared cap where a positive mixed loss requires it. It changes the finite row, rather than attributing a new estimate to an unchanged row.

Only finite owners37..1249 use PD4. Every actual ownerv>=1253 keeps655's original N=0 relative half-threshold row, with

    c_v=2(v-1)/(v-3).                             (PD6)

The tail's actual row and threshold are unchanged.

## 3. Keep each original phase through the complete load comparison

For a nonunit parent-exponent patternx and owner heightj, let `I_(x,j)` be its actual earlier-coordinate indicator, or zero if that numerical original is absent. Its phase is the one fixed for that original. Define the weighted actual load

    L_v=sum_x sum_(j>=1)(v-1)v^(-j) I_(x,j).

The original family is finite. The actual owner union satisfies

    f<=L_v/(v-1).

Together with PD2 this gives

    (v-1)g>=D_v^*-L_v,
    ell_v<=(L_v-(D_v^*-h))_+/h.                  (PD7)

The last hinge is not clipped at one before convex comparison.

Sample655's same head submeasure first and then every owner in increasing order, using PD4 or PD6 as prescribed. All rows are normalized at every complete history. Each finite row satisfies the same `c_v/v<1/10` invariant used in655. The head marginal, product domination before restrictions, omitted-coordinate bounds, and backwards conditional cylinder-cap argument therefore apply on this one new law.

Use655 P1–P8, whose branch domination holds for every nonnegative real hinge threshold. They give

    Pi(owner-v violation)
       <=zeta_r E(C_r-(D_v^*-h))_+/h,            (PD8)

where

    T4={3,5,7,11}, zeta4=D13 D17 D19,
    T5={3,5,7,11,13}, zeta5=D17 D19,
    C_r=product_(p in T_r)(L_p+1)-1,
    EC4=23/9, EC5=95/33.

The auxiliary laws and `D13,D17,D19` are exactly655's. Actual parents are not asserted independent. Each actual phase remains in its rectangle indicator during the backwards comparison. Only afterward do equal exponent patterns coincide on the auxiliary side; numerical-label uniqueness and `sum_(j>=1)(v-1)v^(-j)=1` bound their completed coefficient by one. Thus PD8 covers every allowed finite original height.

## 4. Complete finite fees and the unchanged half-threshold tail

For each finite owner choose theh minimizing the rigorous upper bound in PD8 over the declared integer menu in PD3. The selected policy has7 four-parent rows at37..61 and186 five-parent rows at67..1249. Every row is fixed before its history is sampled.

For an integer thresholdt, use655's complete-mean identity

    E(C-t)_+=EC-t+sum_(C<t)Pr(C)(t-C).

Only the negative part has finite support. For a rational threshold `t=n+u`, with integern and0<=u<1, integer-valuedC gives exactly

    E(C-t)_+=(1-u)E(C-n)_+ +u E(C-(n+1))_+.

Here `u=1-2^(-16)`. Directed integer arithmetic at scale10^75 encloses the needed multiplicative convolutions through1237 and interpolates the adjacent complete hinge bounds. It uses the exact full mean, so no original or auxiliary height is truncated. Every adopted row interval has width less than10^-60. The complete finite fee is

    W_finite=0.009248380121987546... .             (PD9)

At every ownerv>=1253 retain the actual half-threshold row PD6. The generic five-role seventh-moment proof in655 P13–P15 therefore remains applicable, with exactly the same complete bound

    W5=0.0000008202932024191104... .              (PD10)

As in655, this conservatively pays five-parent owners to infinity; actual arbitrary-parent owners above the final switch are sampled once and paid separately. It introduces no second law or second sampling of an owner.

## 5. Recompute the same-law Euler product

The finite caps have changed with the newly selectedh values, so655's old numerical Euler product is not reused. Set

    T_p(c)=1+c[3/(p-1)+2/(p-1)^2],
    B0=3^7=2187.

Use655's ten head caps, the193 actual finite caps from PD3, and the123 half-threshold caps from PD6 up toB0. Directed160-bit integer arithmetic gives

    M0=product_(odd p<=B0)T_p(c_p)
       <=M0_plus=1858433.023232708... .           (PD11)

The producer also encloses `P_odd(B0)=product_(3<=p<=B0)p/(p-1)`. The unchanged tail identity and product estimate in655 P18–P19 give

    C_tail=2187/2186,
    M(v)<=M0_plus C_tail[P(v)/P(B0)]^6.

HereP includes prime2, which cancels in the ratio. The full-history second-moment comparison uses caps of this same actual policy; the larger newM0 is explicitly paid.

For the Rosser--Schoenfeld policy keep655's switchV=2^46 and its P20–P21 formula. WithK46,

    E_RS(K)=M0_plus C_tail (99/97)^6 (V/(V-3))^2
      *sum_(j=0)^6[6!/(6-j)!](7K/10)^(6-j)
      /[2(V-1)(1841/240)^6].

The complete arbitrary-parent fee is

    E_RS(46)=0.00009960440899640427... .           (PD12)

For the elementary policy keepV=2^68. With `P0_minus` the positive lower enclosure of `P_odd(B0)` and `A6=M0_plus C_tail/P0_minus^6`, use655 P23:

    E_elem(K)=2A6[4(K+2)]^6 2^(-K)
       /[1-(1/2)((K+3)/(K+2))^6].

This gives

    E_elem(68)=0.00012727501008444667... .        (PD13)

The analytic and elementary prime-product premises, and the all-prime infinite-tail arguments, are exactly those documented in655. No smaller finite switch or new asymptotic estimate is assumed.

## 6. One complete positive budget and actual gluing

Keep655's head constants

    gamma=203129722400814193208791597
              /20692505911553620784640000000,
    alpha=2673/110656.

The Type I head fee remains1/65536. Under each chosen complete policy, the raw common survivor mass is at least

    gamma-W_finite-W5-1/65536-E.

The two certified raw lower bounds are

    RS46:         0.0004525203843131231...,
    elementary68: 0.0004248497832250807... .

The exact rational comparisons give

    alpha*(gamma-W_finite-W5-1/65536-E_RS(46))>1/100000,
    alpha*(gamma-W_finite-W5-1/65536-E_elem(68))>1/100000. (PD14)

Every quantity in a chosen line concerns the same actual source and its specified rows. Normalization preserves earlier marginals; deleting all paid violations leaves positive common mass. Every resulting owner value lies in its actual ordinary extension domain. The unchanged disjoint private filling, Type I witnesses and separate-component gluing therefore supply an actual full outside extension. Full head Haar projection and CRT give the stated density after dividing by the outside resolving productQ_off.

## Exact evidence and remaining boundary

The [portable producer](../../frontier/cover-geometry/ordinary_domain_five_parent_certificate.py) and [exact data](../../frontier/cover-geometry/ordinary_domain_five_parent_certificate.json) retain the one67-entry policy, all193 rational row thresholds and caps, complete hinge bounds, the full seventh moment, all326 Euler factors, and both final rational comparisons. It reads655's unchanged sibling651 source coefficient certificate and verifies its pinned hash and head constants. Its13037 explicit checks remain active with assertions disabled:

    python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/ordinary_domain_five_parent_certificate.py

The [independent verifier](../../frontier/cover-geometry/ordinary_domain_five_parent_independent.py) and [its exact output](../../frontier/cover-geometry/ordinary_domain_five_parent_independent.json) reconstruct geometric atoms with384-bit directed intervals, evaluate the adopted rational thresholds by adjacent-integer hinge interpolation, derive the complete seventh moment from Stirling numbers, and multiply all326 Euler factors in reverse order using exact fractions. Its22810 checks validate every adopted finite row, both complete budgets and the1/100000 projection comparisons. It does not import or read the producer source; its narrower intervals lie inside the producer's intervals. Reproduce from the repository root:

    python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/ordinary_domain_five_parent_independent.py

The domain inheritance, normalized-row construction, all-threshold branch comparison and infinite-tail arguments above supply the universal quantifiers. The finite coefficient calculation does not replace those arguments or constitute Lean verification.

The remaining hypotheses include655's central conventions and sixty head incidences, the declared ordinary/private interfaces, the four-parent restriction below67, and the five-parent restriction below the selected final switch. Recovering the inherited domain reserve strengthens this specified network theorem; it does not establish uniform positive owner overlap or remove those remaining hypotheses.
