# A product stabilizer releases row0 regular-leaf columns2 and3

For square7 role(row0,column2,leaf0), five whole priority95 comparison fields pay the gate193/100000 for every independent assignment of the ten labelled live9qs roles. The within-role layout group fixes0 and independently exchanges1/2 and4/5. It reduces the complete calculation to2,470,931 representatives while still representing all9,765,625 actual labelled layouts.

The whole-data exchange of quinary columns2 and3 and regular-leaf relabelling give exactly the six new triples

    {0} x {2,3} x {0,1,2}.                       (P1)

Combined with [Report673](673-heterogeneous-fields-complete-the-live-leaf45-role-domain.md), the live domain is

    ({0,1} x {0,1,2,3} x {4,5})
      union ({0,1} x {0} x {0,1,2})
      union ({0} x {2,3} x {0,1,2}).               (P2)

There are twenty-eight of the forty live triples and nine of the twelve role orbits. The remaining twelve triples are({0} x {1} x {0,1,2}) union({1} x {1,2,3} x {0,1,2}), in three role orbits.

The fixed low-pure phases, nulls, central15 phase, literal head primes, fifty retained incidence conditions, actual common source, globally fixed original phases and complete ordinary/private interfaces remain. This releases a role restriction within that conditional theorem; it does not settle unrestricted Erdős #7. No new Lean verification is asserted. Each666 continuation policy keeps its own stated density; the six new triples receive no four-parent-from61 or1/(200000 Q_off) upgrade from672.

## 1. The source and exact layout quotient

For the fixed base role the only changed unary response is

    Z7(l,m)=5/6-[1_(floor(l/3)=0)+1_(floor(m/5)=2)+1_(l=0)]/35. (P3)

Three deletions can coincide, so the inherited uniform strict response calculation retains all decrement counts0 through3. No stronger two-deletion bound is used. The other unary responses, pair activations beta_e=b_e+d_e1_(l=alpha_e), complete512 fees, four guarded9q² charges, projection2673/110656 and literal query menus are the previously paid ones. All ten alpha_e values stay fixed on a single original family across every corner and query.

Let tau=(1 2) and sigma=(4 5) act on the live leaves. They preserve both first ternary roots, leaf0, null leaf3, central15 mask and all quinary data. They therefore preserveP3, the80 live cells and the95 weak corners. Their action on higher prefixes carries each entire rooted cylinder and its deeper digits together. The numerical edges and their prime labels remain in place.

On ten labelled positions the identity fixes5^10 tuples, tau and sigma each fix3^10, and tau sigma fixes only the all0 tuple. Thus

    N=(5^10+2*3^10+1)/4=2,470,931.                 (P4)

There is one orbit of size1,59,048 orbits of size2 and2,411,882 orbits of size4. Their weighted size is9,765,625. The earlier4,912,337-representative regular-S2 quotient remains valid; it simply retains pairs of representatives related by sigma.

Canonical tuples name the first occurrence from{1,2} by1 and the first from{4,5} by4. At each position the depth-first order is0,1, then2 if the first pair has appeared,4, then5 if the second pair has appeared. Two Boolean flags determine this recursion. Both the C++ enumeration and a separate Python traversal verify the full count, orbit census, weighted count and selected minimum index.

## 2. The whole-field library and legal transport

The five fields are the constant-one field and the whole4/5 transports ofB_mix_1_of_8,B_mix_2_of_8,B_mix_3_of_8,C_mix_5_of_8 from673. Every final numerator is retained explicitly; the nonconstant denominator is2^20. Range bounds and both marker priorities are checked over all95 weak corners and80 live cells.

For each canonical alpha choose one entire field that pays every corner and query. If pi sends a noncanonical alpha to its representative, transport the field by

    theta_alpha^(i,j)(l,m)=theta_rep^(pi(i),j)(pi(l),m). (P5)

The source, all original phase prefixes, weak marker, cell and selector are moved by the same pi. Both priorities and each complete gate are preserved. The library need not contain every transported field and need not be closed under the group.

The product group reduces layout problems, not ternary corners within a fixed layout. All five weak ternary markers are still checked. Within-column quinary permutations alone reduce95 corners to20 representatives(i,j), with i in{0,1,2,4,5} and j in{0,6,10,15}. Each chosen field retains that symmetry.

## 3. Complete finite certificate

The [preparer](../../frontier/cover-geometry/regular_leaf_row0_column2_prepare.py), [integer checker](../../frontier/cover-geometry/regular_leaf_row0_column2_checker.cpp), [reproduction wrapper](../../frontier/cover-geometry/regular_leaf_row0_column2_certificate.py) and [result](../../frontier/cover-geometry/regular_leaf_row0_column2_certificate.json) retain the full finite certificate. They pin640/658, reconstruct every rational matching response and original selector maximum, and use directed intervals of mesh2^-32 with a final complete lower gate at mesh2^-40. Nonnegative componentwise selector domination retains an original maximizing candidate and is checked for every removed selector. Source coefficients round down and nonnegative fees round up.

The producer and independent full runs both pass all2,470,931 representatives and obtain the same conservative complete lower bound

    2122155717/2^40 > 193/100000.                  (P6)

The minimum layout is(4,4,4,4,4,0,1,1,0,0), at canonical index2,220,738 in both traversals. The unit field pays it. The identical selected counts are:

| Whole field | Canonical layouts |
| --- | ---: |
| constant_one |2,468,936|
| B_mix_1_of_8_swap45 |1,596|
| B_mix_2_of_8_swap45 |344|
| B_mix_3_of_8_swap45 |49|
| C_mix_5_of_8_swap45 |6|

The preparer checks535,411 exact conditions and the final summary checks27 further conditions. The producer computes49,426,538 attempted corner gates and25,256,931,442 nonzero charged screens.

The independently authored [driver](../../frontier/cover-geometry/square7_ell0_product_full_layout_independent_driver.py), [product-group evaluator](../../frontier/cover-geometry/square7_ell0_product_full_layout_independent.cpp) and [result](../../frontier/cover-geometry/square7_ell0_product_full_layout_independent_driver.json) use only the pinned640/658 inputs and exact final field data. It does not read or import producer code. Its700,112 checks pass, including its separately reconstructed complete layout census and response/selector calculations. It counts49,418,620 accepted corner gates and25,302,333,440 accepted complete selector maxima including zero-fee supports. The two operation counts use different conventions; the complete lower bound, actual minimum layout and selected family counts agree exactly. Both implementations use the same agent/model family and do not constitute different-model consensus or Lean verification.

A separate ordinary-mathematical review approvesP1–P5, the product-group count and whole-source transport. Its107,311 exact field checks concern the five families and both priority inequalities; they are additional contract checks, not another full-layout scan.

```sh
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/regular_leaf_row0_column2_certificate.py --threads 8
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/square7_ell0_product_full_layout_independent_driver.py --threads 8
```

The stored layout network arithmetic uses658's three-parent budget. Its uniform floor193/100000 supplies666's already established policy interfaces. The principal first-four-parent cutoffs137/191 retain density greater than1/(2000000 Q_off); the common cutoff149 retains its separate1/(10000000 Q_off) bound. No stronger continuation bound is inferred from the role count.

## 4. Actual-source composition and exactly six new triples

The existing actual higher-pure comparison transports one whole canonical witness back to the actual original family. Per-original padding of missing or source-null labels is fixed globally, as in673. Fields remain at most one, so the existing domination of omitted-coordinate pure responses is preserved. Owner kernels keep their actual forbidden union, inherited h, Haar caps and Euler factors; the source mass is not renormalized to alter those caps. Full exponent tails, TypeI charge, arbitrary-parent tail and private/CRT interfaces keep their original hypotheses.

Regular-S3 is used between distinguished-leaf problems to move leaf0 to1 or2, carrying the entire alpha tuple and all source data. It is not used as a within-role quotient while leaf0 is fixed. Similarly columns2/3 are exchanged only as a transport between distinct role problems. These two operations giveP1 and no additional row role. The roles inP2 therefore total exactly28, and the remaining twelve retain their stated open status.
