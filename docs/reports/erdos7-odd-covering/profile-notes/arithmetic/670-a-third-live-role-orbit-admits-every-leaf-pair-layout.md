# A third live role orbit admits every leaf-pair layout

For square7 roles (row0,column1,leaf5), two simultaneous priority-field families give complete head gate greater than193/100000 for every independently assigned live9qs layout. One family is constant one; the other is an explicit180-entry dyadic family. The whole family is selected once for each globally fixed layout and then used at every weak corner and for all full-height queries.

This extends the full-layout theorem to the row0 column1 geometry. Transporting the entire configuration by the live-leaf swap4/5 also proves square7 roles(0,1,4). Combined with [Report669](669-two-common-fields-release-every-column1-leaf-pair-layout.md), the certified live triples are exactly the following union:

    {(1,s,ell): s in{1,2,3}, ell in{4,5}}
       union {(0,1,ell): ell in{4,5}}.              (R1)

There are eight triples across three live role orbits. The result does not assert the twelve-triple Cartesian product{0,1} x {1,2,3} x {4,5}; in particular row0 columns2/3 are not supplied by this transport.

The original low pure phases2mod3,1mod9,4mod5,1mod25, null geometry, central15 phase0, literal head primes, incidence conditions on the forty linear-star plus ten qs slots when present, and complete ordinary/private interfaces remain. All permitted outside phases are fixed once globally; the established arbitrary finite higher-pure freedom is retained. The principal policies of [Report666](666-four-parent-owners-return-on-the-complete-leaf-pair-head.md) retain density greater than1/(2000000 Q_off), and its other policies retain their stated bounds. This ordinary-mathematical extension and its exact arithmetic certificates are not new Lean verification or unrestricted Erdős #7.

## 1. Moving the ternary square7 role changes the common unary source

Keep the live-cell set, all95 weak corners and labelled edge order from669. For each fixed alpha in{0,1,2,4,5}^10, retain

    beta_e(l,m)=b_e+d_e 1_(l=alpha_e).

The changed square7 mass is

    Z7(l,m)=5/6-[1_(floor(l/3)=0)+1_(floor(m/5)=1)+1_(l=5)]/35. (R2)

The row0 and leaf5 indicators cannot both hold, so the decrement count is at most2. The previously available conservative boundZ7>=157/210 still holds. All other unary masses, complete512 coefficients, four guarded9q² additions, selector menus, projection and continuation fees are unchanged.

The responses H_T are the same signed matching sums from669, recomputed usingR2 and the one fixed alpha at every corner. The established strict induced region and actual-source comparison remain applicable. The finite positive-gate certificate supplies the additional obligation needed to use those interfaces; moving the source alone would not prove a positive bound.

## 2. A simultaneous boundary family

The nonconstant family uses the same180 orbit-key storage format as669. Every numerator is between0 and2^20. Both ternary and quinary priority inequalities are checked at all95 corners and80 live cells. The constant family satisfies the same inequalities.

The format's regularS3 equivariance does not assert that an arbitrary mixed alpha is itself symmetric. It permits transport of the entire layout and its selected family under regular-leaf relabelling. The row0 indicator inR2 is invariant under permutations within leaves0,1,2. Therefore the same1,657,470 restricted-growth representatives cover all9,765,625 actual labelled layouts.

Within any one fixed layout only the quinary within-column symmetry is used to reduce the corner calculation: all five ternary markers and the four quinary representatives0,6,10,15 are retained. Every original selector maximum is preserved by the exact nonnegative-profile domination check; no charged menu is dropped.

## 3. Complete finite verification

The finite programs use the same2^-32 response intervals, downward mass rounding, upward fee rounding, exact bounded integer accumulation and2^-40 final lower rounding as669. Acceptance requires integer lower bound at least2122057442, which is strictly above193/100000 after scaling.

| Quantity | Certified result |
| --- | ---: |
| Canonical layouts |1,657,470|
| Actual labelled layouts |9,765,625|
| Uncertified layouts |0|
| Dyadic family selected |200,703|
| Constant-one family selected |1,456,767|
| Prepared exact checks |450,127|
| Summary consistency checks |25|

The minimum complete lower bound is

    1061031375/549755813888
      = 0.00193000482795469... >193/100000.           (R3)

Its canonical index is617110 and its layout is

    (5,0,1,5,0,1,1,5,1,2),                          (R4)

where the dyadic family is selected.

The producer's selected family applies to all20 representative corners, hence all95 actual corners. Its minimum is a conservative interval lower bound, not a claimed optimized exact gate. The artifact retains only the two final fields, source pins, exact preparation checks and compressed per-layout selection.

The [preparer](../../frontier/cover-geometry/row0_column1_full_leaf_pair_library_interval_prepare.py), [integer evaluator](../../frontier/cover-geometry/row0_column1_full_leaf_pair_library_interval_checker.cpp), [reproduction wrapper](../../frontier/cover-geometry/row0_column1_full_leaf_pair_library_interval_certificate.py) and [result](../../frontier/cover-geometry/row0_column1_full_leaf_pair_library_interval_certificate.json) reconstruct and certify the complete domain. Reproduce with

```sh
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/row0_column1_full_leaf_pair_library_interval_certificate.py --threads 8
```

The stored direct policy checks use658's three-parent budget. The later666 policies follow through the same normalized row/source/query interfaces and the common certified193/100000 gate. The stored658 budget checks do not directly recompute every666 policy.

The [independent driver](../../frontier/cover-geometry/row0_column1_full_layout_independent_driver.py), [independent evaluator](../../frontier/cover-geometry/row0_column1_full_layout_independent.cpp) and [result](../../frontier/cover-geometry/row0_column1_full_layout_independent_driver.json) reconstruct the local profiles directly fromR2 and read only the pinned640/658 data and proposed numerical fields. They do not import or read the producer implementation. With the opposite selection order, all1,657,470 representatives pass; the constant family is selected1,649,499 times and the new dyadic family7,971 times. All33,149,400 accepted corner gates and16,972,492,800 accepted screen maxima are completed. Its minimum selected lower integer2122106543 exceeds the target2122057442. All495,753 independent checks pass.

```sh
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/row0_column1_full_layout_independent_driver.py
```

Each selected family is a sufficient witness; different selection orders need not give identical minima or family counts. Both implementations establish the common gate193/100000 for the entire domain.

Both retained entry points were rerun from repository sources with exit code0 and reproduced their JSON byte for byte. The source, symmetry, padding, exact eight-triple union and continuation received a separate ordinary-mathematical review. These independent implementations and review use the same agent/model family, not different model families or Lean verification.

## 4. Transport and the unresolved roles

For any actual fixed family in scope, map alpha to its canonical representative, choose the certified entire field family, transport it and all original phases/queries back together, and apply645's two-priority comparison to the actual finite higher-pure source. The omitted-coordinate factors, row and full-Haar caps, Euler factors, continuation charges and final private/CRT construction retain their existing hypotheses.

Inactive, missing or source-null9qs originals can be completed at a globally chosen live leaf and compared by the existing edge-cap ratio thinning. The padding choice is per numerical original, never per weak corner or query. Analogous missing/source-null square7 padding stays within a certified role triple.

A simultaneous swap of live leaves4/5 carriesR2 and every alpha marker to the leaf4 problem. Since alpha already ranges over the whole labelled domain, the transported domain is again the whole domain. This adds the two row0 column1 triples inR1.

The remaining live square7 role orbits and the original fixed-low-pure, incidence and continuation assumptions remain substantive. Neither a new field nor a full finite layout enumeration removes them automatically.
