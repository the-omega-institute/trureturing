# The remaining33 require a rebuilt common-source interface

The remaining33 labels of [Report635](635-a-thirty-label-conditional-core-admits370-missing-originals.md) admit two exact source-update interfaces. Neither interface yet has a uniform positive gate. A15-original counterexample below also proves that a central fibre carrying positive mass before these additions can become genuinely empty while the whole family still has a survivor. Keep the literal primes and fixed631
first-root/central15/STAR geometry, arbitrary allowed pure phases and finite
heights, and one globally fixed residue for every numerical original.
These are ordinary source deductions and exact finite diagnostics; no new Lean verification or unrestricted Erdős #7 conclusion is asserted.

The missing inventory has an exact functional decomposition:

| Role | Numerical originals | Count |
| --- | --- | ---: |
| Central masks |45,75,225|3|
| One outside square |9q², q in Q|5|
| One outside first root |45q,75q,225q, q in Q|15|
| Outside first-root pair |9qs, q<s in Q|10|

Here Q={7,11,13,17,19}. The first three rows change the central support and
five star factors. The last row can be paid on the resulting source or put
inside its edge-avoidance events. These two routes share the same interface.

## 1. Every numerical original has its own one-time global role

Use six live ternary reference leaves l=0,...,5 and twenty quinary reference
leaves m=0,...,19, before deleting the actual square-null leaves. Their literal
residues are

    a3(l)=3(l mod3)+floor(l/3),
    a5(m)=5(m mod5)+floor(m/5).

Let x(c) be their common CRT residue modulo225, c=(l,m). For every original
N with its globally fixed residue a_N, set

    d_N=gcd(N,225),
    xi_N=a_N mod d_N,
    I_N(c)=1_(x(c) congruent xi_N mod d_N).

A role whose projection is already excluded by a first pure root is simply
inactive on this reference domain. Null square leaves are removed later by
the actual central source. Roles may be represented by the literal residue,
not by free independent choices at different cells.

In particular, xi_45, xi_(45q) and xi_(45s) are three DIFFERENT original
parameters. Divisibility of their moduli does not make their residues equal.
The same warning applies to75,225,9q² and9qs. A missing original can have zero
activation; if full-slot padding is used, its auxiliary role is fixed once
for the whole layout and transported with that layout.

The central mask is

    M_lambda(c)=1_(x(c) not congruent0 mod15)
               (1-I_45(c))(1-I_75(c))(1-I_225(c)).

Apply this mask without normalizing the remaining central probability law.
It neither changes the old pure-source density caps nor grants a fresh source.

## 2. The complete star update

Let r_q=1/(q-1), a_q=1/[q(q-2)], and let Z_q^0(c) be631's fixed seven-star
full-slot lower mass:

    Z_q^0=1-(r_q+a_q)(1_(l//3=0)+1_(m//5=2))
             -r_q(1_((l//3,m//5)=(0,2))+1_(l=0)+1_(m=10)).

The new full-slot factor is exactly

    Z_q^lambda(c)=Z_q^0(c)
      -a_q I_(9q²)(c)
      -r_q[I_(45q)(c)+I_(75q)(c)+I_(225q)(c)].       (I1)

The roles in I1 are independent across numerical originals, but each is
fixed globally. The inequality underlying I1 is a union upper bound on
actual deletions from the SAME outside pure-survivor law rho_q. If Z_q>0,
its actual survivor has mass at least Z_q and can be uniformly thinned to
that mass, giving xi_q(c)<=rho_q. All inherited outside prefix caps continue
to hold. Neither different phases nor overlap patterns are declared disjoint.

If Z_q<=0, this formula does not provide a positive source or permit division
by Z_q. Such a cell may be assigned zero mass in every response. Actual
survivor mass could exceed this lower bound; exploiting that would require
additional joint information about actual outside roots/lifts.

There is no replacement uniform positive Z box inherited from635. At a
cell where three old linear slots, two old square slots and all four new
slots are active, I1 gives1-6r_q-3a_q, equal to-3/35 at q=7.

## 3. Two conditional-source routes

For e={q,s}, write kappa_e=r_qr_s+a_qr_s+r_qa_s. Each edge carries the three
old originals qs,q²s,qs² with no central factor.

Route A conditions only on these30 old originals, using

    beta_e^A(c)=kappa_e.

The ten new9qs originals are then paid on this same conditional source.
Their actual fixed ternary leaf is retained in that debit; a worst-leaf
maximum is optional and weaker.

Route B puts9qs into the same edge event whenever its central role is active:

    beta_e^B(c)=kappa_e+r_qr_s I_(9qs)(c).           (I2)

The actual event remains a union of prefix rectangles depending ONLY on q,s.
Thus L(K5) is still a valid ordinary dependency graph under the product of
the normalized actual star survivors. There is no shared-endpoint assumption.
Route B must not pay9qs again after conditioning.

For either route define u_e(c)=beta_e(c)/(Z_q(c)Z_s(c)) only where every Z is
positive. Let

    S_A(u)=1-sum_(e in A)u_e
             +sum_(e<f in A, e disjoint f)u_e u_f.

Choose one fixed common cell set Gamma_lambda satisfying

    M_lambda=1, every Z_q>0, and S_A(u)>0 for ALL1024 edge subsets A. (I3)

For example Gamma may be the complete strict-good set defined by I3. It is
fixed by this globally coherent role layout, before screen maxima and before
central source weights. A smaller common set is also allowed. Cells excluded
from Gamma have zero mass and zero in all32 query responses.

On Gamma, Reports623 §4 and629 §§1–2 apply directly to these actual union
events. With nu_c=product_q xi_q/Z_q and F_c their actual avoidance event,

    zeta_c=theta(c) product_q Z_q S_all(u) nu_c(.|F_c)

is ONE submeasure dominated by product rho_q. Set it to zero outside Gamma.
Each unordered pair of disjoint edges is counted once. The same zeta_c supplies every support query. Its response is

    H_T(c)=theta(c)1_Gamma(c) [
      product_(q not in T) Z_q
      -sum_(e disjoint T) beta_e product_(q not in T union e) Z_q
      +sum_(e<f, e disjoint f, (e union f) disjoint T)
             beta_e beta_f product_(q not in T union e union f) Z_q ]. (I4)

Here0<=theta<=1 is shared by all32 T. There are only matchings of sizes0,1,2
because Q has five coordinates. The inherited arbitrary-height query bound is

    zeta_c(A_query)<=product_(q in T)cap_q(A_q) H_T(c).

The quotient factors cancel as in623; no extra leaf mass is attached to the
deep ternary delta or(4/5) times quinary delta. Dependence on the central
coordinates remains only through their mod9/mod25 leaf, so all16 central
selector types are still valid.

## 4. The existing370 must be paid again on the new responses

Let P90 be635's three central-decorated pair coefficients, modes1,4,5 only.
Let D370 be its complete added370 coefficients, and L,W the original full512
remaining-loss and weighted-query coefficients. Keep the same vector

    C_j=g(L_j+P90_j+D370_j)+c W_j,
    c=1084133/201247200, g=1-c.

Every coefficient remains nonnegative. Its previous numerical screen value
is NOT retained: all512 screens must be recomputed on the new H of I4.
The source used to prove635's old number is not silently substituted.

For central weights w_l,v_m let S_j(H) be the complete central menu maximum.
Route B's sufficient gate is

    G_B=g sum_(l,m)w_l v_m H_empty(l,m)-sum_j C_j S_j(H). (I5)

Route A instead has

    G_A=g sum_(l,m)w_l v_m H_empty(l,m)-sum_j C_j S_j(H)
          -g sum_(e={q,s})r_qr_s
               sum_(l,m)w_l v_m I_(9qs)(l,m) H_e(l,m).   (I6)

The final term uses each original's one global ternary leaf. It can be
upper-bounded by the mode(2,0) maximum to remove explicit roles, but those
maxima must still be taken on this SAME H. The explicit-role version in I6
is sharper and is linear in the relevant source weights.

These inequalities follow by restricting the constructed source outside the
originals not already enforced by it. The unit query is the resulting actual
retained mass, so a deletion costs g times its mass bound. All nonunit query
bounds survive restriction. The3 central masks and20 new stars are already
in the source; Route B also already enforces the ten leaf pairs. Nothing is
omitted or charged twice. Conditional avoidance cannot enlarge the source
beyond rho because the deliberate S_all scaling is retained.

A positive G_A or G_B for actual source weights gives the same complete
finite-height continuation criterion, with the same Haar conversion factor
alpha=2673/110656. Reconstruct the23/29/31 continuation on THIS source.
Old density or continuation kernels are not reused after an unnoticed
renormalization.

## 5. Precisely which comparisons survive

For one fixed layout lambda, fixed Gamma and fixed theta, all H are independent
of the central weights. At fixed null leaves(z,z'), the existing convex
corner decomposition of the actual central pure laws still applies. Mass and
Route A's explicit leaf-pair debit are bilinear in w,v; each screen is a
maximum of bilinear forms with nonnegative costs. Successive Jensen in w and v
therefore remains valid for one common theta over those corners.

This supplies a finite sufficient test: for each fixed null pair, one may
choose theta_(lambda,z,z') and certify its complete gate at all5 times19=95
weak comparison corners. The SAME theta must be used at those95 corners.
Allowing every corner to choose its own theta and then quoting Jensen does
not prove the claim for intermediate actual weights. An actual-weight-dependent
construction needs its own direct certificate or additional comparison theorem.

The old128 representatives are no longer automatic. They relied on the
stabilizer of the old whole role field. The33 new global roles generally break
those symmetries. Either verify a new stabilizer preserving every declared
role and every response, or use all11400 ordered pure-source comparison pairs.
Only the central weight comparison is asserted here; these corners remain
comparison laws, not a claim that every corner is physically attained by a
pure-tail inventory.

Across different layouts lambda, the strict mask can change because Z and beta
change. Report629 §7 already explains why strict-good truncation need not
preserve the old star-factor concavity. Do not apply a star-layout Jensen with
different masks at its comparison points. A valid route must hold the mask
fixed on the compared domain and certify strictness throughout it, or prove a
new comparison controlling the mask change. Report611's distinction between
layout-dependent and universal theta also remains in force.

Thus the unresolved uniform goal is still a quantifier obligation: handle
EVERY allowed jointly assigned33-role layout and every admissible actual
source, using a valid common-source construction. Writing I1–I6 does not
supply its positive lower bound.

## 6. A genuinely empty old positive fibre

The [empty-fibre checker](../../frontier/cover-geometry/remaining33_actual_dead_fibre.py) with its [data](../../frontier/cover-geometry/remaining33_actual_dead_fibre.json) gives15 distinct odd
originals. At central x=57mod225, corresponding to(l,m)=(1,11), all central
pure/15/45/75/225 deletions are avoided and old631 theta equals1. The six linear
stars are

| Modulus | Full residue | Root modulo7 |
| ---: | ---: | ---: |
|21|15|1|
|35|2|2|
|105|87|3|
|315|102|4|
|525|432|5|
|1575|1182|6|

Use pure phases2mod3,4mod5,0mod7,1mod9,1mod25, and phase0 for15,45,75,225.
Each displayed star has the required central projection of57, and the first
three respect631's fixed STAR roles. The seven points of the central fibre
modulo1575 are covered exactly once: the six live7 roots by these six stars,
and root0 by pure7. The old actual uniform central law gives this leaf mass
1/95. Its actual new outside fibre mass is exactly0.

Integer3 survives all15 originals, so this is a local support obstruction,
not a global covering system. It refutes requiring positive source at every
old positive cell. It does not require changing theta numerically: the new
Gamma or base source may vanish there even with theta=1. The full-slot
lower budget-3/35 is distinct from the actual mass0.

The direct checker reports32 exact checks. An independent CRT reconstruction
reports33 checks and confirms the old leaf mass, actual empty fibre and global
witness. This reuses629's local-obstruction principle in the newly relevant
fixed-STAR/remaining33 interface; it is not a second Lean theorem.

## 7. Bounded executable criterion and measured limits

The [bounded interface checker](../../frontier/cover-geometry/remaining33_source_interface_probe.py) accepts one list of exactly33 full
original residues. It verifies distinct numerical identities, computes each
central projection once, builds both strict masks by all1024 polynomials,
recomputes all32 response grids and all512 screens, and returns I5/I6 for ONE
explicit source corner using old631 theta. It performs no optimization,
layout scan or universal source-corner certification.

    python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/remaining33_source_interface_probe.py --demo regression
    python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/remaining33_source_interface_probe.py --demo stress
    python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/remaining33_source_interface_probe.py --layout <json> --source 3 4 5 6

The regression puts all new central residues at0; their actions vanish on old
theta's positive support. Both routes exactly reproduce635's gate at its
recorded worst comparison corner. This checks the interface's reduction to
existing work; it is not an additional general admission theorem.
The stress layout keeps central-only roles at0 but gives the20 star and10
leaf-pair originals central projections of57. It detects the predicted
nonpositive star budget at(1,11). Its gate readings are separate outcomes,
not implied by a successful interface execution. Output status is explicitly
COMPLETED_BOUNDED_DIAGNOSTIC, with a separate positive-gate flag for each route.
The input layout and all cell-level exclusions are retained for replay.

At supplied comparison corner(3,4,5,6), the stress input gives

    G_A=-7129400927201127801962224033487
          /876388485665800409702400000000000,
    G_B=-105670801742711025794230663465039
          /14898604256318606964940800000000000.

Both are negative with old theta. Each route has101 strict-good reference
cells before null-leaf exclusion, with one nonpositive star-budget cell and
three additional strict-bad cells, besides the15 central-masked cells. Thus
the mere existence of many good cells does not pay the complete query load.
The regression and stress executions respectively performed6696 and6504
explicit exact checks. Their successful execution confirms the calculations,
not the missing universal positive gate. No search for a new theta was run.

The [regression data](../../frontier/cover-geometry/remaining33_interface_regression.json) and [stress data](../../frontier/cover-geometry/remaining33_interface_stress.json) include the33 complete numerical original residues, both strict masks, all32 response grids and every reported gate. The exact coefficient source is the pinned actual_pair_activation_certificate.json used by Report635. These programs are finite diagnostics, and a completed run is not a positive-gate certificate.
