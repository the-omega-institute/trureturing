# Sparse cubes lower the uniform arbitrary-parent threshold

On the complete actual head of [Report648](648-induced-pair-responses-release-all-square-pairs.md), arbitrary finite parent unions are now allowed at every outside owner prime at least **2^47**, with full survivor density strictly greater than

    1/(140000 Q_off).

Every smaller outside owner still has at most three fixed declared parents. The head retains all fifteen freely phased square stars and all twenty freely phased square pairs, its remaining forty linear-star and twenty pair incidences, and its central conventions. The ordinary/private interfaces remain those of [Report625](625-arbitrary-pure-heads-support-sharper-common-networks.md) and [Report647](647-order-matched-moments-strengthen-complete-networks.md).

The new actual intermediate rows have conditional density caps tending to2. This permits a sixth-power Euler estimate, replacing the twelfth-power bound used at the former threshold2^115. All finite heights and the entire infinite prime tail are included. No optimal threshold is asserted.

The following are four independently sufficient choices of ONE complete row policy:

| Finite-row order | Arbitrary-parent threshold | Full density lower bound | Infinite prime-product input |
| --- | ---: | ---: | --- |
|3|2^47|1/(140000 Q_off)|Rosser--Schoenfeld1962 Theorem8|
|4|2^48|1/(68000 Q_off)|Rosser--Schoenfeld1962 Theorem8|
|3|2^69|1/(21000000 Q_off)|Report621's elementary bound|
|4|2^70|1/(159000 Q_off)|Report621's elementary bound|

These are ordinary mathematical deductions and exact rational certificates, not new Lean verification. They do not remove the remaining small-owner, head or private-interface restrictions and do not resolve unrestricted Erdős #7. They are separate from [Report650](650-four-parent-owners-start-at-eighty-nine.md), whose four-parent rows and Euler factors cannot be substituted into this proof.

## 1. One source and one row at each actual owner

Keep647 §1's ten literal head primes and actual joint-prefix interface. In particular, use the SAME actual submeasure mu supported on the actual head-good set, with

    mu(total)>=gamma,
    mu(joint positive prefix cylinders)<=product_p c_p p^(-j_p),
    mu<=alpha^(-1) Haar_P, alpha=2673/110656,

and head caps

    (c3,c5,c7,c11,c13,c17,c19,c23,c29,c31)
      =(2,4/3,7/5,11/9,13/11,17/15,19/17,5/3,20/11,2).

For the stated application take648's

    gamma=203129722400814193208791597
              /20692505911553620784640000000.             (SC1)

Every actual numerical modulus occurs once, has one globally fixed full residue, and is assigned once to a head, owner or ordinary inventory. The parent union at each owner is fixed before sampling, consists of smaller declared head/network coordinates and merges all syntactic tuples of that owner. Ordinary private interiors are not additional undeclared parents. All finite network sizes and resolving exponent heights are allowed.

Choose a finite-row order r in{3,4} and one threshold V from the opening table. Sample the head and then the actual outside owners in increasing prime order:

- At37<=v<971 use exactly647's rth-order finite row and its actual cap.
- At971<=v<V use the sparse three-parent cube row below.
- Atv>=V use625's N=0 half-threshold row with any fixed finite parent union.

Each normalized row is defined on every complete prior history, including a wholly forbidden remaining fibre. No row conditions on eventual survival. Subsequent normalized rows preserve the head marginal and earlier event masses. Every estimate below concerns this one joint submeasure Pi.

## 2. Sparse cubes preserve the joint parent invariant

For v>=971 define

    n(v)=min{n>=7:v<=3^n},
    N=n(v)^3-1,
    D=v-n(v)^3-2.

At a three-parent owner select the nonunit patterns in[0,n)^3. The inherited ordinary domain pays2/(v-1); all positive heights belonging to each selected pattern cost at most1/(v-1). The remaining Haar mass is therefore at least D/(v-1). This estimate retains numerical distinctness and each original phase.

Apply the normalized half-threshold row from647 §2 to that domain. Its conditional density and violation probability satisfy

    c_v=2(v-1)/D=2+2(n^3+1)/D,
    K_v(forbidden)=(2t-1)_+<=t^2.                    (SC2)

The initial band is971<=v<=3^7. At its left endpoint c_v<4. For n>=8,

    n^3+2<=3^(n-1)/2<v/2.

The base n=8 is immediate. Induction follows from

    3(n^3+2)-((n+1)^3+2)=2n^3-3n^2-3n+3>0,

whose coefficients after n=8+t are811,333,45,2. Consequently every sparse row has

    2<c_v<4<v/5.                                   (SC3)

The large-owner N=0 row has cap2(v-1)/(v-3), which is no larger than SC2. All finite647 rows already have cap<v/5. Thus the entire actual law preserves625's ordered three-role comparison against

    (p_i,d_i)=((3,2),(5,4/3),(7,7/5)).

Reverse integration of queried actual rows, followed by the joint head bound, gives the same maximum-depth kernel as625 ST5. This proves joint estimates even for shared or dependent parents. Auxiliary independent variables may evaluate the comparison kernel; actual parents are not declared independent.

The all-height argument of625 ST6 and647 OM5--OM6 now applies to the sparse cube. The complete cube second moment obeys625 ST8:

    Pi(owner-v violation)<24*3^(-n)/(v-n^3-2)^2.      (SC4)

All positive owner-height weights sum to one; every cross term retains its actual original phase. Missing parents embed with zero exponents. No height truncation is used.

## 3. A complete smaller three-parent tail

For n=7 put A=971,B=3^7; for n>=8 put A=3^(n-1)+1,B=3^n. Set c=n^3+2. Padding primes by all integers and integrating the decreasing positive function gives

    sum_(A<=v<=B, prime) 1/(v-c)^2
      <=1/(A-c-1)-1/(B-c).                         (SC5)

Sum24*3^(-n) times SC5 exactly for7<=n<=20. For n>=21 the induction above gives A-c-1>=3^(n-1)/2. Dropping the negative term in SC5 bounds the whole nth band by144*9^(-n). Hence the ENTIRE remaining tail is at most162*9^(-21).

The resulting rational upper bound R_sparse satisfies

    R_sparse=0.000013343567374910049...,
    R_sparse<512/9529569.                           (SC6)

Combining it with each unchanged647 finite fee gives complete three-parent upper bounds

    W3=0.00858981507235306...,
    W4=0.007896721505062414....                     (SC7)

Exact fractions, not the displayed decimals, are used below. Paying SC7 over all owner primes conservatively overcounts the actual three-parent subset below V. Actual large owners use only their N=0 row and are charged separately; no owner is sampled twice.

## 4. A sixth-power Euler bound on the same new law

For each possible parent coordinate use its actual head/finite cap below971 and the SC2 comparison cap at every larger prime, even if that prime is absent or uses the smaller N=0 cap. Define

    T_p(c)=1+c[3/(p-1)+2/(p-1)^2].

The full cofactor second moment at an arbitrary-parent owner v is bounded, exactly as in621 UT6--UT10, by

    M(v)<=product_(odd primes p<v)T_p(c_p),
    Pi(owner-v violation)<=M(v)/(v-3)^2.             (SC8)

This pads missing parents and the zero cofactor by positive terms. It preserves every actual cross term and includes all finite heights. It does not assume that different optimized caps come from separately chosen laws.

Put B0=3^10=59049. Compute the complete finite product

    M0=product_(odd primes p<=B0)T_p(c_p).

There are5967 factors. Directed integer arithmetic with denominator2^160 gives certified bounds M0<=M0_plus, with readings

    order3: M0_plus=215672207.61642894...,
    order4: M0_plus=539682250.662324....             (SC9)

Every actual finite cap is retained. Reverse multiplication at192 bits supplies a contained independent rounding interval.

For p>B0, its sparse depth n is at least11. The same cubic induction, now starting from5(11^3+2)<=3^10, gives n^3+2<=p/5. Therefore

    c_p-2<=5(n^3+1)/(2p),
    3/(p-1)+2/(p-1)^2<=16/(5p).

The second inequality follows from p^2-27p+16>=0 for p>=27. Since T_p(2)>=1 and log(1+u)<=u,

    log(T_p(c_p)/T_p(2))<=8(n^3+1)/p^2.             (SC10)

In the nth band, padding by integers gives sum p^(-2)<=2/3^n. The complete logarithmic excess is thus at most

    Gamma=16 sum_(n>=11)(n^3+1)/3^n
         =0.20989347829768498...<1,
    exp(Gamma)<=1/(1-Gamma)=C_tail.                 (SC11)

The sum is evaluated as an exact infinite geometric-polynomial sum, not a finite prime search. The exponential bound follows termwise from1/j!<=1. Finally, with u=1/(p-1),

    T_p(2)=1+6u+4u^2<=(1+u)^6.

Combining these estimates on the SAME actual source gives, for v>=V>B0,

    M(v)<=M0_plus C_tail
            [product_(B0<p<=v, prime)(1-1/p)^(-1)]^6. (SC12)

Allowing the endpoint prime v in the product only increases the upper bound. The sixth power follows from the changed actual row caps; it is not a replacement of the twelfth power while retaining the old law.

## 5. Explicit analytic tail and the thresholds2^47 and2^48

Use the existing [Rosser--Schoenfeld citation](../../../../../Library/Arith/rosser1962approximate.md). For P(x)=product_(p<=x)p/(p-1), its1962 Theorem8 gives

    P(x)>exp(gamma_E) logx(1-1/(2log^2x)), x>1,
    P(x)<exp(gamma_E) logx(1+1/(2log^2x)), x>=286.

Here gamma_E is Euler's constant, unrelated to the head mass. At B0=3^10, dividing the upper and lower bounds yields

    P(v)/P(B0)<=(201/199)logv/logB0, v>=B0.         (SC13)

The complete factor at2 cancels. The positive series for log3 gives

    logB0>10(1+1/12+1/80)=263/24.

Also log2<7/10. Both are elementary rational bounds; the certificate checks their series constants.

Write V=2^K, which is not an odd prime. For x>=V-1 the function f(x)=log^6(x)/x^2 decreases, since f'(x)=x^(-3)log^5(x)(6-2logx)<0. Every prime at least V is an odd integer V+1+2j. Thus

    sum_(v>=V, prime) f(v)
      <=sum_(j>=0)f(V+1+2j)
      <=(1/2) integral_(V-1 to infinity) f(x) dx.  (SC14)

Each term is bounded by the integral over its preceding interval of length2. The lower endpoint V-1, including its denominator, is retained. Also v/(v-3)<=V/(V-3). Six integrations by parts give the explicit integral, and log(V-1)<(7/10)K yields the rational bound

    E_K=M0_plus C_tail (201/199)^6 (V/(V-3))^2
         *[sum_(j=0)^6 (6!/(6-j)!)(7K/10)^(6-j)]
         /[2(V-1)(263/24)^6].                     (SC15)

This bounds the complete arbitrary-parent prime tail in SC8. Its relevant readings are

    order3, K47: E_K=0.0009153264737002598...,
    order4, K48: E_K=0.0012938817884846781....       (SC16)

The argument uses the stated classical analytic theorem. The original scan's SHA-256 agrees with the existing citation, and printed page70 states the cited ranges. The rational certificate does not prove that external analytic theorem or claim Lean verification of it.

## 6. An elementary alternative

An external prime-product theorem is unnecessary for the larger thresholds in the opening table. Let P_odd(B0) be the exact odd-prime reciprocal product, and let P0_minus>0 be its downward-rounded rational bound. Set

    A6=M0_plus C_tail/P0_minus^6.

By SC12, M(v)<=A6 P_odd(v)^6. Reuse the elementary proof in [Report621](621-one-uniform-owner-threshold-removes-the-parent-count-bound.md), UT15--UT18:

    P_odd(2^k)<=4(k+1), k>=2.

In2^k<=v<2^(k+1), bound v-3>=2^(k-1) and count at most2^(k-1) odd integers. The band fee is at most

    B_k=2A6[4(k+2)]^6 2^(-k).

Its successive ratio is(1/2)((k+3)/(k+2))^6, decreasing in k. At either selected K it is less than one. Consequently the complete tail is bounded by

    E_elem(K)=2A6[4(K+2)]^6 2^(-K)
                    /[1-(1/2)((K+3)/(K+2))^6].    (SC17)

Order3 at K69 and order4 at K70 both leave positive full648 margins. The small positive order3 margin is checked using exact rational inequalities; a rounded decimal comparison is not its certificate.

## 7. Simultaneous survivor, evidence and remaining restrictions

The ordinary Type I charge remains1/65536. Owner ordinary domains have already been paid inside D. For each policy the same joint law therefore has good mass at least

    gamma-Wr-1/65536-E,

where E is its own SC15 or SC17 bound. Projecting with the head Haar domination and filling the declared private interiors simultaneously gives

    head extendible Haar mass>=alpha*(gamma-Wr-1/65536-E). (SC18)

Each of the four exact comparisons in the opening table is strict. At least one actual outside assignment extends each surviving head assignment; dividing by Q_off and applying CRT gives the stated full densities. The original labels and their full phases never change.

The [portable certificate](../../frontier/cover-geometry/sparse_cube_euler_six_certificate.py) and [data](../../frontier/cover-geometry/sparse_cube_euler_six_certificate.json) contain the pinned647/648 input hashes, all fourteen integer-band fees, the complete infinite remainders, actual finite-product intervals and the four final rational comparisons. Its47134 explicit checks remain active with Python optimization enabled:

    python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/sparse_cube_euler_six_certificate.py

The [independent verifier](../../frontier/cover-geometry/sparse_cube_euler_six_independent_verify.py) and [data](../../frontier/cover-geometry/sparse_cube_euler_six_independent_verify.json) reconstruct all primes by trial division, all finite fees from their row parameters, the logarithmic excess by a cubic telescoper and the products by256-bit reverse rounding. Its54647 explicit checks confirm all four final comparisons and containment in the published product intervals.

The universally quantified source, induction and integration arguments are SC2--SC17; finite checking does not replace them. The principal remaining gaps are the at-most-three-parent restriction below the chosen V, the sixty head incidences and central conventions, and the ordinary/private interfaces. Reducing V leaves all these restrictions visible.
