# Both ternary rows at column0 admit every leaf-pair layout

For square7 roles(row0,column0,leaf5) and(row1,column0,leaf5), two existing whole95 priority-field families certify every independent assignment of the ten live9qs roles. Both roles use the same fixed low pure/null geometry and the same central15 mask. One family is constant one; the other is Report669's dyadic family. A family is chosen once for the globally fixed layout and used at every weak corner and query.

The principal Report666 policies consequently retain survivor density strictly greater than1/(2000000 Q_off). Its other policies retain their individually stated density bounds. Swapping live leaves4 and5 through the whole configuration gives the same arbitrary-layout result for square7 leaf4. Combined with670, the admitted live triples are

    {(1,s,ell): s in{0,1,2,3}, ell in{4,5}}
       union {(0,s,ell): s in{0,1}, ell in{4,5}}.     (B1)

These twelve triples represent five live role orbits of the fixed low-pure stabilizer. The row0 column2/3 cases and all remaining live role orbits do not follow fromB1.

The low pure phases2mod3,1mod9,4mod5,1mod25, central15 phase0, literal head primes, incidence conditions on the forty linear-star and ten qs slots when present (including retained225q originals), and the complete ordinary/private interfaces remain required. Arbitrary finite higher-pure phases and the admitted outside-phase choices retain their existing scope. Every phase is fixed once globally. The result is ordinary mathematics supported by exact finite arithmetic, not new Lean verification or unrestricted Erdős #7.

## 1. The column0 source keeps the central mask

Use the same80 live cells(l,m),95 weak corners(i,j), five head primes and ten labelled edges as669. The original mask still removes(l<3,m<5); moving a square7 role does not redefine this mask. For row parameterr in{0,1}, the square7 unary comparison mass is

    Z7(l,m)=5/6-[1_(floor(l/3)=r)+1_(floor(m/5)=0)+1_(l=5)]/35. (B2)

The second indicator is retained on the live leaves4/5 even though its regular-row cells are masked. Forr=0 the row and column indicators can both hold only in the masked region, and row0 and leaf5 cannot coincide. Thus the decrement count onLIVE is at most2. Forr=1, all three indicators can coincide at leaf5 in column0, giving the same worst decrement3 already included in the uniform strict bound.

Each fixed layout alpha uses

    beta_e(l,m)=b_e+d_e 1_(l=alpha_e).                (B3)

All other unary masses, matching responses, complete512 coefficients, four guarded9q² additions and literal selector menus remain unchanged. The all-induced strict condition still follows fromZ7>=157/210 andbeta_e<=b_e+d_e. This establishes the existing actual-source construction; the positive complete gate is checked separately below.

## 2. The same two whole-family witnesses serve both rows

Take the constant-one family and the180-entry denominator2^20 family from669. The preparation program independently verifies every value bound and both priority inequalities on all95 corners and80 live cells. Their regularS3-equivariant storage format does not impose symmetry on a mixed actual layout.

For each globally fixed alpha, one whole family must satisfy all corners and all charged menus. No field may be changed in response to a particular corner, support or literal selector. Both row roles permit simultaneous regular-leaf relabelling of the entire layout, field and queries, so1,657,470 restricted-growth representatives cover all5^10=9,765,625 labelled layouts per row.

At a fixed layout the corner reduction uses only the within-column quinary permutations. It retains all five ternary markers and the four quinary representatives0,6,10,15. Thus all20 representatives are evaluated, covering the95 original weak corners. The exact selector-domination check preserves the maximum over every original menu.

## 3. Shared preparation and two complete role evaluations

One preparer constructs the shared column0 response profiles and both field families. A single parameterized integer checker then runs separately for row0 and row1. It explicitly computes the decrement inB2; it does not reuse a row1-only formula for row0.

The same conservative interval scheme as669/670 is used: exact rational preparation at scale2^-32, downward mass rounding, upward nonnegative fee rounding, bounded integer products, and a final complete gate lower bound at scale2^-40. The fixed acceptance threshold is2122057442, the ceiling of2^40 times193/100000. The shared witness-selection order is constant one followed by the dyadic family.

| Quantity | Row0 | Row1 |
| --- | ---: | ---: |
| Canonical layouts |1,657,470|1,657,470|
| Actual labelled layouts |9,765,625|9,765,625|
| Uncertified layouts |0|0|
| Constant-one family selected |1,657,470|1,657,455|
| Dyadic family selected |0|15|
| Minimum complete lower numerator at scale2^-40 |2,144,903,195|2,139,507,919|

Both minima exceed193/100000. The row0 minimum is

    2144903195/1099511627776
      =0.001950778091668326...,

at canonical index422004, the all-leaf5 layout. The row1 minimum is

    2139507919/1099511627776
      =0.0019458711167317233...,

at canonical index422203, layout(5,5,5,5,5,0,5,5,5,1). In both minimum cases the constant-one family is selected. The preparation makes449,285 exact checks, and the two branch summaries make50 consistency checks.

The selected family is valid simultaneously at all95 corners for its layout. These reported minima are interval lower bounds; they are not claimed to be exact optimized minima. Row0's success with the constant family is a verified property of this complete finite domain, not a general claim that field optimization is unnecessary.

The final source package uses one [preparer](../../frontier/cover-geometry/column0_leaf5_full_layout_prepare.py), one [parameterized checker](../../frontier/cover-geometry/column0_leaf5_full_layout_checker.cpp), one [reproduction wrapper](../../frontier/cover-geometry/column0_leaf5_full_layout_certificate.py), and one [certificate](../../frontier/cover-geometry/column0_leaf5_full_layout_certificate.json) with two role branches. Reproduce both branches with

```sh
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/column0_leaf5_full_layout_certificate.py --threads 8
```

The artifact pins the actual numerical inputs and includes both whole-family witnesses, the exact preparation checks, compressed per-layout selections and separately stated branch minima. Its directly stored policy checks concern658's three-parent budget. The666 policies follow through their existing interface and the common193/100000 gate, with each policy retaining its own stated density bound.

The separately authored [independent driver](../../frontier/cover-geometry/column0_common_role_independent_driver.py), [parameterized evaluator](../../frontier/cover-geometry/square7_role_full_layout_independent.cpp), and [result](../../frontier/cover-geometry/column0_common_role_independent_driver.json) reconstruct the rational responses and complete selector menus from the pinned640/658 inputs and the proposed numerical fields. They do not read or import the producer implementation. The binary input records the row and column explicitly, and each row receives its own complete enumeration. All494,561 checks pass. Across the two branches, the independent evaluator completes66,298,800 accepted corner gates and33,944,985,600 accepted screen maxima. Both branch minima and field-selection counts agree with the producer table above.

```sh
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/column0_common_role_independent_driver.py
```

Both retained entry points were rerun from repository sources with exit code0 and reproduced their stored JSON byte for byte.

A separate ordinary-mathematical review checks the actual column0 source, fixed central15 mask, simultaneous priorities, lawful layout/corner transports, padding, and exact twelve-triple union. The implementations and review come from the same agent/model family; they do not constitute different-model consensus or Lean verification.

## 4. Transport to actual families and the remaining frontier

For any actual original family in scope, transport its complete alpha tuple to a canonical representative, select the certified whole family, and transport the original phases, fields and queries back together. The two priorities permit645's actual finite higher-pure source comparison. All omitted-coordinate factors, normalized rows, row/full-Haar caps, Euler factors, continuation charges and private/CRT reconstruction retain their prior hypotheses.

Missing or source-null9qs originals can be completed at one fixed live leaf per label and handled by the established edge-cap ratio thinning. Such padding is global per original, not independently chosen per row or query. Missing/source-null square7 roles retain the same conservative padding within an admitted triple.

Finally the swap of leaves4 and5 moves both the square7 leaf role and every pair marker. Because all labelled layouts are already admitted, this transports both new row roles to leaf4 without adding a new layout restriction. It gives the four new triples{0,1} x {0} x {4,5}, yielding preciselyB1 when combined with670.

The fixed-low-pure, common-source, incidence and continuation assumptions remain substantive. The newly verified column0 geometries do not remove other role orbits or establish the unrestricted covering statement.
