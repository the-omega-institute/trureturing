# Five-parent owners begin at seventy-one

Keep the complete actual head and private interfaces of [Report651](651-an-unqueried-head-coordinate-admits-four-parents-from-thirty-seven.md). The following network has a survivor:

| Owner prime | Size of its fixed union of earlier declared head/network parents |
| --- | --- |
| `37 <= v < 71` | at most four |
| `71 <= v < 2^46` | at most five |
| `v >= 2^46` | arbitrary finite size |

The complete survivor density is strictly greater than

    1/(1250000 Q_off).

An elementary alternative allows five parents from71 up to2^68, arbitrary finite parent unions at everyv>=2^68, and gives density strictly greater than

    1/(6000000 Q_off).

The first statement uses Rosser--Schoenfeld1962 Theorem8. The elementary alternative uses Report621's elementary prime-product bound. These are two independently sufficient complete policies. Each has one actual source, one fixed kernel at each owner, and its own final arbitrary-parent switching point.

Keep651/648's literal ten head primes, central conventions, all fifteen square-star and twenty square-pair originals with arbitrary globally fixed full phases, and the remaining forty linear-star and twenty `qs/9qs` incidences on one fixed live-root table. Keep the ordinary domains, disjoint private interiors and numerical-label assignments of647/625/619. Every original numerical modulus is odd, greater than one, occurs at most once and has one globally fixed full residue. Each owner has one fixed union of smaller declared parents; merge repeated syntactic descriptions and numerical duplicates before selecting its row. Undeclared private interiors cannot be parents. Parent identities do not depend on the sampled history. All network sizes, widths, depths and exponent heights remain arbitrary finite quantities.

The improvement uses the product of ALL omitted head-coordinate masses and an analytic comparison of the remaining parent branches. In the far tail, high moments bound the loss of the actual half-threshold row; they do not change that row's threshold or conditional density. This is ordinary mathematics with exact rational certificates, not new Lean verification or a resolution of unrestricted Erdős #7.

## 1. Preserve every unused head coordinate on one source

Use651/648's same actual head submeasure with

    gamma=203129722400814193208791597
             /20692505911553620784640000000,
    alpha=2673/110656,
    mu(total)>=gamma,
    mu<=alpha^(-1) Haar_head.

Its support, actual unary source representation, joint-prefix bounds and Haar domination all concern this same measure.

Put `Q={7,11,13,17,19}` and

    D7=5/6,
    Dq=(q-2)/(q-1)-2/[q(q-2)] for q=11,13,17,19.

Thus

    (D11,D13,D17,D19)
      =(871/990,1549/1716,3793/4080,5455/5814).

Before the later deletions, the actual core is dominated by

    rho3 rho5 product_(q in Q) xi_(q,c),
    xi_(q,c)<=rho_q, integral xi_(q,c)<=Dq,

uniformly in the actual central3/5 cellc. Pair, guarded and head-good restrictions only decrease this source.

For a fixed parent's head subsetT, integrate the normalized later kernels backwards. Queried later head/outside coordinates are compared using their cylinder caps conditional on EACH complete preceding history; unqueried normalized rows integrate to one. After this replacement, the nonnegative payoff is independent of every unqueried core coordinate. Integrating all `q in Q\T` therefore gives

    zeta(T)=product_(q in Q\T)Dq.                    (P1)

Drop the remaining restrictions and apply the pure-core convex comparison only after this integration. Hence a nonnegative increasing convex load payoff obeys

    integral phi(actual load) dPi
       <=zeta(T) E phi(C_(T,l)).                     (P2)

This is651's comparison with every available omitted-coordinate mass retained. It does not normalize a surviving fibre, multiply an unrelated probability by the head mass, or choose a different source for each query. Later normalized kernels preserve all earlier marginals.

## 2. Actual envelopes and a complete reduction of the parent branches

Write `X=L+1`. The head auxiliaries have survival functions

    Pr(L3>=e)=2*3^(-e),
    Pr(L5>=e)=(4/3)*5^(-e), e>=1;

    Pr(Lq>=1)=1/(q-1),
    Pr(Lq>=e)=1/[(q-2)q^(e-1)], e>=2, q in Q.

In particular `E X3=2`, `E X5=4/3` and `E Xq=(q-1)/(q-2)`. The later head caps are5/3,20/11,2 at23,29,31.

Every actual outside row below will satisfy `c_u/u<1/10`. If its queried outside parents are ordered, thejth is at least thejth prime `r_j` at least37. Its full-history query cap is bounded by the decreasing auxiliary tail

    Pr(L_out,j>=e)=(1/10)r_j^(1-e), e>=1,
    r_j=(37,41,43,47,53).                            (P3)

Independence is used only for these comparison auxiliaries. An actual owner with fewer thanr parents can be padded to exactlyr, forr4 or5, by unused head coordinates: all ten heads precede every owner, the product of auxiliaries increases, and adding aQ coordinate removes a factorDq<1 from(P1). Thus the nonnegative hinge bound cannot decrease. There are386 four-parent and638 five-parent head-subset/outside-count types, restricted at small owners by the number of earlier outside primes.

Their maximum can be found without extending that finite branch census to every threshold. For positive integer valued variables, the sufficient criterion

    D E X>=E Y,
    D Pr(X>u)>=Pr(Y>u) for u>=1

implies `D E(X-u)_+>=E(Y-u)_+` for everyu>=0. Integrate the tails foru>=1; for0<=u<=1 the difference is `D EX-EY+(1-D)u>=0`.

The core laws therefore satisfy

    Dq E(X3-u)_+>=E(Xq-u)_+ for q>=7,
    Dq E(X5-u)_+>=E(Xq-u)_+ for q>=11.             (P4)

For3, use `Dq EX3>=5/3>=EXq` and check depths1,2 atq7; for5, use `Dq EX5>=10/9>=EXq` atq>=11 and check depths1,2 atq11. The relevant endpoint inequalities are

    (5/6)(2/3)>=1/6, (5/6)(2/9)>=1/35,
    (5/6)(4/15)>=1/10, (5/6)(4/75)>=1/99.

Higher depths propagate by the prime ratios. Replacing a selectedq by3 or5 multiplies the omitted-coordinate factor byDq. Conditional on the independent productA of all other auxiliaries, apply(P4) at `u=(t+1)/A` and multiply byA. Thus the whole weighted hinge increases. The variablesX3 andX5 also stochastically dominate later head and outside envelopes. One can insert3 and then5 while retaining3: if the remaining coordinates are all inQ, at least three of them remain, including oneq>=11.

SmallerQ coordinates increase both their tails and the omitted-coordinate factor. AlsoX7 andX11 stochastically dominate all later head and outside envelopes. Insert7 and then11, removing the largest outside role whenever one is replaced. This proves that the four-parent branch maximum is

    T4={3,5,7,11}, zeta4=D13 D17 D19.              (P5)

For five parents, any remainingQ or later-head coordinate can be replaced by13. The only apparent exception is the outside37 envelope, leaving

    A={3,5,7,11,13}, zetaA=D17 D19,
    B={3,5,7,11,outside37}, zetaB=D13 D17 D19.

The common3 coordinate resolves this last comparison. PutD=D13, and letXo have the outside37 envelope. The signed law

    mu(m)=Pr(X13=m)-D Pr(Xo=m)

has

    mu(1)=1789/17160>0,
    mu(2)=-b, b=3647/317460,
    mu(m)>0 for all m>=3,
    R=sum_(m>=3)mu(m)=2891/634920,
    3R-b=1379/634920>0.                             (P6)

The atom laws are `Pr(X13=1)=11/12`, `Pr(X13=2)=131/1716`, `Pr(X13=m)=12/[11*13^(m-1)]` form>=3; and `Pr(Xo=1)=9/10`, `Pr(Xo=m)=18/[5*37^(m-1)]` form>=2. Positivity beyondm3 follows by the ratio37/13. The complete positive-tail massR is a difference of exact depth-two tails.

For `tau(x)=Pr(X3>x)` and integerT>=6,

    tau(T/3)>=3 tau(T/2).

This follows from `floor(T/2)-floor(T/3)>=1` and the exact geometricX3 tail. Therefore

    Pr(X3 X13>T)-D Pr(X3 Xo>T)
      >=-b tau(T/2)+R tau(T/3)>0.                    (P7)

AtT0 through5, direct finite-complement evaluation gives, respectively,

    167/1716, 3221/51480, 114641/5714280,
    26853779/8245706040,
    36528847721/11898553815720,
    23037391749659/17169613156083960,

all positive. Integer support extends the tail inequality to every real nonnegative threshold. Integrating tails, then conditioning on the independent common productX5X7X11, proves thatA dominatesB for every original hinge thresholdt>=0. Thus

    T5={3,5,7,11,13}, zeta5=D17 D19                (P8)

controls ALL five-parent branches. These are maxima of comparison fees, not assertions that actual phases attain the fees. Every actual original phase remains fixed through(P2).

## 3. The actual finite policy and its full-height hinge fees

For each prime37<=v<1253 put `D=v-3`. Use one fixed integer `10<=h<D` and the actual normalized threshold row

    theta=1-h/D,
    c_v=(v-1)/h<v/10.

The inherited ordinary domain pays pure owner powers and has Haar mass at leastD/(v-1). If its remaining forbidden fraction ist, the row's violation is `(t-theta)_+/(1-theta)`. Its unbounded load-hinge upper bound is `(L-(D-h))_+/h`. By(P5)--(P8), every allowed fixed parent union therefore has fee

    B_r(v,h)=zeta_r E(C_r-(D-h))_+/h,
    C_r=product_(p in T_r)(L_p+1)-1,                (P9)

withr4 below71 andr5 from71. Both actual and comparison families allow arbitrary finite original heights. Completing the positive comparison sum uses numerical-label uniqueness and the full geometric owner-height sum. The hinge is not clipped before convex comparison.

The exact complete means are

    E C4=23/9, E C5=95/33.

For each required integert, evaluate the entire hinge by

    E(C-t)_+=EC-t+sum_(C<t)Pr(C)(t-C).              (P10)

Only the negative part has finite support. A multiplicative convolution of the coordinate atom laws computes its product distribution through1236. Every atom and every convolution is enclosed between directed integer multiples of10^-75. The exact full mean supplies the complete infinite tail; no allowed height is truncated.

Choose theh minimizing the rigorous upper fee in the declared finite integer menu, independently at each owner and before sampling any history. Optimality outside that menu is neither asserted nor needed. The finite policy has8 four-parent rows at37..67 and185 five-parent rows at71..1249. Each of its193 adopted rows has an enclosing fee interval of width less than10^-60. The resulting complete finite charge is

    W_finite=0.009669528498092422... .               (P11)

The data retain every actualh andcap. This is a new policy, not the old651 finite fee with a different prefactor inserted afterwards.

## 4. High moments bound the actual half-threshold tail

The switching boundary1253=7*179 is composite; the first actual half-threshold owner is1259. For every actual ownerv>=1253, use the SAME `N=0` half-threshold row, regardless of whether its declared parent count is at most five or arbitrary. Its cap is

    c_v=2(v-1)/(v-3)<v/10.                          (P12)

It is normalized on all complete previous histories, including a wholly forbidden fibre. The ordinary domain still pays the unit/pure pattern. A higher moment below changes only the loss estimate, not(P12), the actual threshold or the source.

A common five-role comparison requiring no omitted-coordinate factor is

    p=(3,5,7,11,13),
    k(1)=(2/3,4/15,1/6,1/10,1/10),
    d=(2,4/3,7/5,11/9,13/10),
    k_i(e)=d_i p_i^(-e), e>=2.

The certificate checks each eligible head role at depths1,2. Higher depths follow by the ordered prime ratios. An outside parent has shallow cap at most1/10 and depth-two cap at most1/370, below every corresponding deep reference cap; the same prime-ratio argument handles later depths. Dropping all unused source restrictions and using the same backwards conditional comparison bounds the actual load moments by those of

    C=product_(i=1)^5(L_i+1)-1.

For every integer orderr>1 andx>=0,

    (2x-1)_+<=A_r x^r,
    A_r=2^r(r-1)^(r-1)/r^r.                        (P13)

Forx>1/2 the derivative of `(2x-1)/x^r` has the sign of `r-2(r-1)x`; its maximum is attained at `x=r/[2(r-1)]`. The statement is zero on the other half-line.

Take r7. The complete momentM7=E(C^7) is calculated exactly from the five geometric laws:

    M7=2576699667659.711...,
    A7=2^7*6^6/7^7.

One direct formula, withq=1/p, is `S_0=1/(1-q)` and `S_j=q/(1-q) sum_(i<j)binomial(j,i)S_i`, where `S_j=sum_(m>=0)m^j q^m`. With `E X^0=1`, the positive-order formula for `r>=1` gives

    E X^r=1+k(1)(2^r-1)
      +d[sum_(j<r)binomial(r,j)S_j-1-q(2^r-1)],
    M7=sum_(j=0)^7(-1)^(7-j)binomial(7,j)
                      product_i E(X_i^j).          (P14)

Every actual five-parent owner has violation at most `A7 M7/(v-3)^7`. Padding by ALL integersv>=1253 and integrating the preceding unit intervals gives the complete five-parent tail

    W5=A7 M7/[6*(1253-4)^6]
      =0.0000008202932024191104... .                 (P15)

This sum runs to infinity, conservatively paying all actual five-parent owners below the final switching point. Actual arbitrary-parent owners above that point are sampled only once and charged separately. Neither independent actual parents nor a finite-height cutoff was introduced.

## 5. The Euler product of these same actual rows

Forcapc let `T_p(c)=1+c[3/(p-1)+2/(p-1)^2]`. The same-law cofactor second-moment argument of621/649 gives, for every arbitrary-parent ownerv,

    M(v)<=product_(odd p<v)T_p(c_p),
    Pi(owner-v violation)<=M(v)/(v-3)^2.             (P16)

Use the ten actual head caps, the193 adopted finitecaps, and(P12) at every larger prime. The product baseline is now only

    B0=3^7=2187.

It has exactly326 odd-prime factors:10head,193finite and123half-threshold rows. Directed160-bit integer arithmetic supplies

    M0=product_(odd p<=B0)T_p(c_p)
       <=M0_plus=1807126.4709222636... .             (P17)

Every factor belongs to the specified actual policy. The data also enclose `P_odd(B0)=product_(3<=p<=B0)p/(p-1)` for the elementary alternative.

At every larger prime, the actual half-row factor simplifies to

    T_p(2(p-1)/(p-3))=(p+1)^2/[(p-1)(p-3)].

Forp>=11,

    T_p(2(p-1)/(p-3))/(p/(p-1))^6<=1+2/p^2.        (P18)

After clearing positive denominators, the difference is

    p^4(p-11)+5p^3+p^2-3p+1>=0.

SinceB0 is odd, padding the primesp>B0 by the odd integersB0+2,B0+4,... and integrating gives

    sum_(p>B0)2/p^2<=1/B0.

Consequently the ENTIRE residual Euler factor is bounded by

    C_tail=1/[1-1/B0]=2187/2186,
    M(v)<=M0_plus C_tail [P(v)/P(B0)]^6,            (P19)

whereP includes the prime2, which cancels in the ratio. This argument uses the actualN=0 row itself; no sparse cube, separately optimized density cap or old Euler correction is substituted.

## 6. Complete arbitrary-parent tails and final density

The [existing Rosser--Schoenfeld citation](../../../../../Library/Arith/rosser1962approximate.md) records1962 Theorem8, printed page70, equations(3.28)--(3.29):

    P(x)>exp(gamma_E)logx[1-1/(2log^2x)], x>1,
    P(x)<exp(gamma_E)logx[1+1/(2log^2x)], x>=286.

Its inspected scan has SHA-256 `8e37b06f82e09421bceb2502578c47b61469141f0287e6acedb70e01765ab556`. The coefficient certificate uses this stated analytic theorem and does not claim to prove it or add a Lean formalization.

For the NEW baselineB0=3^7, use `logB0>7` to obtain

    P(v)/P(B0)<=(99/97)logv/logB0, v>=B0.           (P20)

The ratio99/97 is `(1+1/(2*7^2))/(1-1/(2*7^2))`; it is matched to this baseline. Also `logB0>7*(263/240)=1841/240` and `log2<7/10`, by the elementary series estimates used in649.

ForV=2^K, the function `log^6(x)/x^2` decreases onx>=V-1. Padding the prime tail by odd integers and integrating preceding intervals of length2, then integrating by parts six times, gives

    E_RS(K)=M0_plus C_tail (99/97)^6 (V/(V-3))^2
      *sum_(j=0)^6 [6!/(6-j)!](7K/10)^(6-j)
      /[2(V-1)(1841/240)^6].                       (P21)

AtK46 the complete arbitrary-parent fee is

    E_RS(46)=0.00009685458763795921... .             (P22)

For the elementary alternative, letP0_minus be the positive lower enclosure ofP_odd(B0) and `A6=M0_plus C_tail/P0_minus^6`. Report621's elementary bound `P_odd(2^k)<=4(k+1)` gives

    E_elem(K)=2A6[4(K+2)]^6 2^(-K)
      /[1-(1/2)((K+3)/(K+2))^6].                  (P23)

The denominator bounds the complete geometric band tail. AtK68,

    E_elem(68)=0.00012376127465192022... .            (P24)

The Type I charge remains1/65536. On the one actual law for each chosenV, all original violations are therefore paid by

    W_finite+W5+1/65536+E.

The exact remaining raw masses are bounded below by

    V=2^46: 0.00003412182956669203...,
    V=2^68: 0.000007215142552731013... .

The retained rational comparisons are

    alpha*(gamma-W_finite-W5-1/65536-E_RS(46))
       >1/1250000,
    alpha*(gamma-W_finite-W5-1/65536-E_elem(68))
       >1/6000000.                                 (P25)

Normalized rows preserve earlier event masses; deleting the actual union of all paid violations leaves positive common mass. The unchanged simultaneous private filling and full Haar projection then extend each surviving head point by an actual outside assignment. Dividing by the actual outside resolving productQ_off and applying CRT proves the stated complete densities.

## Exact evidence and scope

The [portable producer](../../frontier/cover-geometry/five_parent_seventy_one_halfrow_certificate.py) reads the sibling651 coefficient certificate for its unchanged source constants. It reconstructs the full finite hinge intervals, all193 fixed rowcaps, the complete seventh moment, all326 actual Euler factors, the infinite-tail constants and both selected budgets. Its12256 explicit checks remain active with Python assertions disabled:

    python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/five_parent_seventy_one_halfrow_certificate.py

The [corresponding exact data](../../frontier/cover-geometry/five_parent_seventy_one_halfrow_certificate.json) retain exact fractions, input and producer hashes, branch laws, coordinate moments, adopted row parameters, finite-product intervals, both final comparisons and check categories. It retains only the two stated policies, not an optimization scan. The ordinary branch, source and infinite-tail proofs above supply the universal quantifiers; the finite program does not replace them or constitute Lean verification.

The [independent verifier](../../frontier/cover-geometry/five_parent_seventy_one_halfrow_independent.py) and its [exact result](../../frontier/cover-geometry/five_parent_seventy_one_halfrow_independent.json) reconstruct geometric atom distributions with 384-bit directed bounds, derive seventh moments using Stirling numbers and falling-factorial geometric sums, and multiply all 326 Euler factors in reverse order as exact fractions. Its 22593 checks independently verify all 193 adopted row fees, source caps, complete tails, both final density margins and the six small signed-law thresholds in (P7). Its narrower intervals lie inside the producer's intervals. The verifier does not import or read the producer source and does not need to reoptimize the adopted row parameters. Reproduce from the repository root:

    python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/five_parent_seventy_one_halfrow_independent.py

The remaining unrestricted gaps include the sixty head incidences and central conventions, the declared ordinary/private interfaces, five parents at owners below71, and more than five parents below the chosen final threshold. The result does not prove those hypotheses unnecessary or give an arithmetic covering counterexample. Its reusable gain is a stronger actual-source comparison together with an estimate of the half-threshold row that leaves the row's small conditional cap intact.
