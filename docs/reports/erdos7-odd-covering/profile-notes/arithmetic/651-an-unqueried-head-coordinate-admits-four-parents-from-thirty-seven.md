# An unqueried head coordinate admits four parents from thirty-seven

Keep the complete actual head of [Report648](648-induced-pair-responses-release-all-square-pairs.md): its literal ten primes, central conventions, arbitrary globally fixed phases for all fifteen square stars and twenty square pairs, and remaining forty linear-star and twenty `qs/9qs` incidences on one fixed live-root table. Keep the ordinary domains, disjoint private interiors and numerical-label assignment of [647](647-order-matched-moments-strengthen-complete-networks.md), [625](625-arbitrary-pure-heads-support-sharper-common-networks.md) and [619](619-one-global-three-parent-network-under-the-root-one-head.md).

The following whole network has a survivor:

| Owner prime | Size of its fixed union of smaller declared head/network parents |
| --- | ---: |
| `37 <= v < 2^115` | at most four |
| `v >= 2^115` | arbitrary finite size |

All network widths, depths, sizes and exponent heights are arbitrary finite quantities. Every original numerical modulus is odd, greater than one, occurs at most once and has one globally fixed full residue. Parent identities do not depend on the sampled history. Repeated syntactic descriptions are merged before assigning originals or choosing rows. Undeclared private-interior coordinates cannot be parents.

The complete survivor density is strictly greater than

    1/(190000 Q_off),

where `Q_off` is the product of the actual outside resolving prime powers, with the same meaning as647/648. This removes650's at-most-three-parent restriction at37..83. The head incidence conditions and ordinary/private interfaces remain substantive hypotheses. This is ordinary mathematics with exact rational certificates, not new Lean verification or a resolution of unrestricted Erdős #7.

The improvement uses the actual deletion already present at a head coordinate absent from the owner's parent union. It also applies the full increasing-convex comparison directly to an unbounded hinge. It does not infer convex comparison from joint cylinder bounds alone.

## 1. Preserve the unused part of the actual source

Put `Q={7,11,13,17,19}`. Let `rho` be648's normalized seven-prime pure product law on3,5 andQ. Before its pair and guarded deletions, its actual core is dominated by

    rho_3 rho_5 product_(q in Q) xi_(q,c),

where `c` is the actual central3/5 cell and `xi_(q,c)<=rho_q`. The coordinate masses are

    Z_7(c)=5/6-n(c)/35<=5/6,
    Z_q=(q-2)/(q-1)-2/[q(q-2)]  for q=11,13,17,19.

Thus, uniformly in every actual cell and admitted layout,

    Z_q(c)<=zeta=5455/5814<1.                         (U1)

The maximum is `Z_19`. Pair deletion, its scalar thinning, the central15 mask, guarded deletion and later head-good restrictions only decrease the actual measure.

Append the SAME normalized kernels at23,29,31 and at outside primes as in the actual construction. Every cylinder cap holds conditional on each complete previous history. If a head kernel was specified only on the actual incoming support, complete it by Haar elsewhere; cap1 is below its declared cap. This leaves the actual source unchanged and defines the required comparison on the larger support. Outside rows are already defined on all histories, including dead fibres.

Let an owner have a fixed parent union of size at most four. Its positive rectangle load depends only on these parents. For a nonnegative increasing convex payoff, first integrate normalized later rows backwards. At a queried later coordinate use the full-history conditional cylinder bounds to replace its indicators by independent nested auxiliaries. At an unqueried coordinate its kernel integrates to one.

After all queried coordinates outside the seven-prime core have been replaced, fix the auxiliary values. The payoff is now a nonnegative function of only the queried core coordinates. No auxiliary value has been passed into an actual kernel: those later kernels have already been integrated. Dependence of an original later kernel on an unqueried head coordinate is therefore harmless.

At least one `q in Q` is absent from the owner's parent union. Integrate its actual `xi_(q,c)` first. Since the payoff is independent of this coordinate, (U1) supplies a factor at most `zeta`. Drop the other deletion restrictions using `xi<=rho`, and integrate any other unqueried core coordinates. The remaining upper integral is `zeta` times the appropriate marginal of the normalized pure product law. Apply the core-coordinate convex comparison there.

Consequently the complete source comparison is

    integral phi(L) dPi <= zeta E_ref phi(C),          (U2)

for the loads and completed reference count specified below. This proof uses the actual unary source representation, rather than only the scalar head reserve or joint-prefix bounds. It neither normalizes the restricted head nor multiplies an independently obtained event probability by its total mass.

Different owners may use different omitted coordinates in this inequality. All inequalities hold for the SAME actual joint measure `Pi`; no omitted coordinate is deleted again, and no original phase changes. A larger parent union might query all ofQ, so (U2) is not applied to the arbitrary-parent tail.

## 2. The conditional convex comparison and its all-height envelope

The comparison mechanism is already available in [problem detail32](../../problem-details/32-conditional-root-measures-and-unrestricted-prime-tails.md) and [55](../../problem-details/55-adaptive-read-once-head-orders-with-unrestricted-tail-comparison.md), with depth-dependent versions in [538](538-near-maximal-mixed-packing-preserves-the-anchor-query-hinge.md). Its application here retains the unqueried-coordinate factor from(U1).

For completeness, if Bernoulli indicators `B_j` have marginal upper bounds `q_j`, nonnegative coefficients `a_j` and a nonnegative increasing convex function `phi`, then

    E phi(sum_j a_j B_j)
      <= E_U phi(sum_j a_j 1_(U<=q_j)).                 (U3)

Order theq_j decreasingly and put `I_j={1,...,j}`. The increasing supermodular set function `F(S)=phi(sum_(j in S)a_j)` satisfies

    F(S)<=F(empty)+sum_(j in S)[F(I_j)-F(I_(j-1))].

Take expectations and use the ordered marginal bounds; the right side is the nested-indicator expectation. A fixed nonnegative additive term insidephi is allowed. Conditional use of this inequality backwards through the actual kernel chain gives the comparison above, because after each replacement the remaining load is still a nonnegative sum of earlier rectangle indicators. This does not assert independence of actual parent coordinates. [Report388](388-saturated-chain-blockers-do-not-supply-conditional-caps.md) shows why joint-prefix caps alone cannot replace this conditional argument.

Use ordered reference primes and depth envelopes

    p=(3,5,7,11), d=(2,4/3,7/5,11/9),
    k_i(0)=1,
    (k_1(1),...,k_4(1))=(2/3,4/15,1/6,1/10),
    k_i(e)=d_i p_i^(-e) for e>=2.                    (U4)

The sharper first-depth bounds at7 and11 use the root-balanced pure source onQ. The exact head caps at23,29,31 are5/3,20/11,2. All actual finite rows below have `c_u/u<1/10`; the cubical and arbitrary-parent rows retain that inequality too. An outside parentu is at least37, hence its deeper query cap is at most

    (1/10)u^(1-e).

At depth two this is at most1/370, below every reference deep cap `d_i/p_i^2`. Increasing depth preserves the comparison since the ordered actual prime is at least its reference prime. The certificate checks every eligible explicit head role at depth one and two; the same monotonic ratio argument covers every greater depth. Only those conditional caps actually needed before the core comparison are used.

Let independent auxiliary depths satisfy `Pr(L_i>=e)=k_i(e)`. No nonunit cofactor patterns are preselected in the finite policy. The unit pattern, corresponding to pure owner powers, is already removed by the inherited ordinary domain. The complete reference count is

    C=product_(i=1)^4(L_i+1)-1.                       (U5)

For each actual cofactor exponent pattern and owner heighth, retain its own globally fixed phase during(U3). Its nonnegative weight is `(v-1)v^(-h)`. Only after comparison, labels with the same exponent pattern become the same auxiliary event. Numerical-label uniqueness and the full geometric sum bound their total coefficient by one. Completing absent patterns and heights increases the reference count to(U5). Fewer than four actual parents are padded only on this upper-comparison side.

The actual family remains finite; nonnegative completion and monotone convergence justify the infinite reference sum. Its first moment is exactly

    EC=product_i[1+k_i(1)+d_i/(p_i(p_i-1))]-1
      =23/9.                                         (U6)

Thus all hinges below are integrable. Finite reference calculations do not truncate the allowed original heights.

## 3. Fixed finite rows and their complete hinge fees

For each prime37<=v<1253 put `D=v-3`. Choose an integer `10<=h<D` before sampling any history and use the inherited normalized threshold row with

    theta=1-h/D,
    c_v=(v-1)/h<v/10.                               (U7)

The ordinary-domain mass is at least `D/(v-1)`. If its remaining forbidden fraction ist, the row's exact conditional violation is

    (t-theta)_+/(1-theta).

The actual loadL gives `t<=L/D`, so this is bounded by the UNBOUNDED increasing convex hinge `(L-(D-h))_+/h`. Combining with(U2) gives

    Pi(owner-v violation)
       <= zeta E_ref(C-(D-h))_+/h.                   (U8)

The hinge is not clipped at one inside the convex comparison. No integer moment order is required, and no adaptive choice ofh or parent identity is made by history.

The [producer](../../frontier/cover-geometry/unqueried_head_four_parent_certificate.py) and [data](../../frontier/cover-geometry/unqueried_head_four_parent_certificate.json) select one fixedh for all193 primes in this finite window. Selection minimizes a rigorous upper bound among10<=h<D; optimality beyond that declared finite search is not claimed or needed.

The producer sums the positive hinge on the whole box `0<=L_i<28`. Each coordinate atom is enclosed by integer multiples of`2^-128`, with products rounded by multiplying their nonnegative enclosing endpoints. The ENTIRE omitted region has the common bound

    E[product_i(L_i+1) 1_(some L_i>=28)]
      <= sum_i d_i p_i^(-28)[29+1/(p_i-1)]
                  product_(j!=i) E(L_j+1).           (U9)

Divide this byh for each row. All arithmetic and row comparisons are rational. The raw complete finite hinge sum obeys

    W_hinge <10137/1000000,
    W_finite=zeta W_hinge<zeta*(10137/1000000).       (U10)

An [independent verifier](../../frontier/cover-geometry/unqueried_head_four_parent_independent.py) and [data](../../frontier/cover-geometry/unqueried_head_four_parent_independent.json) use a different all-height identity:

    E(C-t)_+=EC-t+sum_(C<t)Pr(L)*(t-C).              (U11)

SinceC=product(L_i+1)-1, the negative part has finite hyperbolic-cross support. Exact(U6) includes the full infinite first moment; directed probability arithmetic on the finite negative part encloses each full hinge. The verifier checks every adopted row against the producer's independent positive-box enclosure. It also reconstructs all203 actual finite Euler factors and the complete final comparison.

## 4. Same-law continuation, Euler correction and density

For1253<=v<2^115 retain650's four-dimensional cubical rows. Their cap is at most4, hence strictly belowv/10. The larger comparator in650, whose fourth coefficient is11/5, still bounds every query here. Keep its already certified complete prime sum, including its analytic infinite remainder:

    W_cube=0.000029747933205988...<3/100000.

No factorzeta is needed or applied to this inherited upper charge. Paying the complete cube tail overcharges primes above the actual switching point, where only the arbitrary-parent construction is used.

Forv>=2^115 keep the arbitrary finite-parent row with cap `2(v-1)/(v-3)<4`. Recompute the Euler correction from the ten actual head caps and the193 new finite caps(U7):

    A=product_(odd p<1253)
        [1+c_p(3/(p-1)+2/(p-1)^2)]((p-1)/p)^12
      =0.0017826624511777095...<3/1000.               (U12)

All later caps are at most4. The same exponent12 elementary prime-product and dyadic tail argument from621/625/650 therefore supplies

    E115=5*(3/1000)*(4*117)^12/2^115
        =19740202146111572828188083
           /495176015714152109959649689600.          (U13)

The ordinary Type I charge remains1/65536; neither it norE115 is multiplied byzeta. On the one actual sequential source, the full network debit is bounded by

    zeta*(10137/1000000)+3/100000+E115+1/65536
       < gamma20,

    gamma20=203129722400814193208791597
                 /20692505911553620784640000000.    (U14)

The producer's more precise full upper sum is0.00959532015510848..., leaving raw common mass at least0.00022126384245351133.... These decimals display exact rational bounds retained in the data.

With the inherited projection constant `alpha=2673/110656`, the exact simpler comparison is

    alpha[gamma20-zeta*(10137/1000000)-3/100000
                        -E115-1/65536]>1/190000.     (U15)

Normalized rows preserve earlier marginals; deleting the actual union of all paid violations leaves positive mass by(U14). The simultaneous private filling and CRT construction from647/648 then divide the projected head mass byQ_off, giving the stated complete density. No private witness from a different history, independently optimized phase, or separately attainable marginal has been combined.

The producer passes1990 explicit checks; the independent verifier passes15380, with126371 cells in its finite hyperbolic cross. Both use Python's standard library and keep checks active under`-O`:

    python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/unqueried_head_four_parent_certificate.py
    python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/unqueried_head_four_parent_independent.py

Their results concern these coefficient and policy computations. The source comparison and infinite-tail arguments are ordinary mathematical proofs; no new Lean build or formalization is claimed.

## Remaining unrestricted boundary

The sixty retained head incidences, central phase conventions, declared network domains and private-parent restrictions remain. Four parents is still a substantive restriction below2^115. Allowing five or more can query all five coordinates ofQ, so the uniform unused-coordinate proof requires a different argument there. Report649's lower arbitrary-parent thresholds use a different actual policy and Euler correction; they have not been combined with this one.

The reusable improvement is to retain actual unqueried deletion mass through backwards conditional comparison. It preserves a source/query relationship discarded by a scalar head reserve and a separate unrestricted owner fee. Any extension to more parent roles or freer head phases must establish its own same-source response and complete budget.
