# Actual leaf capacities support a complete mixed-inventory continuation

A fixed101-original phase pattern supports every remaining mixed numerical label at arbitrary finite heights, together with [Report657](657-sharper-private-domains-admit-five-parents-from-sixty-seven.md)'s declared ordinary/private owner network. Use at most four earlier parents at37<=v<67, at most five at67<=v<2^46, and any fixed finite parent union above2^46. The elementary alternative uses2^68 as the final switch. Under the source and interface hypotheses below, both policies have full survivor density greater than1/(50000 Q_off).

The101 labels may be padded when absent; every present one keeps its listed phase. No further live pure original at3,5,7,11,13,17,19 is admitted. Remaining mixed phases, including all four formerly guarded9q² labels, are arbitrary and fixed globally. The literal head primes and657's ordinary/private geometry, numerical-label assignment and parent limits remain assumptions. This is a conditional complete-inventory class, not unrestricted Erdős7.

The source no longer requires active linear stars to share a common outside root. Its finite calculation identifies why: at the same actual source and corner, the generic central deep-query envelope cannot pay the complete gate193/100000 for ANY cell-constant retention field. Keeping actual within-leaf density instead permits a denominator12 field to pay the gate. After replacing the four guarded fees by full9q² fees and restoring the omitted-coordinate mass interface, the head gate is0.01038810099670533..., enough for657's unchanged policies.

All deductions are ordinary mathematics with exact arithmetic, not new Lean verification. The finite one-source result does not establish a95-corner family or an arbitrary higher-pure transfer; section6 gives an explicit obstruction to that latter shortcut.

## 1. One fixed finite family and one actual source

The fixture contains101 distinct odd numerical moduli with one fixed full residue each:91 anchor mixed originals, the five outside pure roots0 modulo q for Q={7,11,13,17,19}, and the central originals2mod3,1mod9,4mod5,1mod25,0mod15. No other pure originals are present in the fixture. The following table specifies its mixed phases without decoding the numerical CRT file:

| Original role | Central component | Outside component |
| --- | --- | --- |
| cq, c in(3,5,15,9,25,45,75,225), q=7 |81modc|(1,2,3,4,5,6,1,2)mod7 in that order|
| Same eight cq, q>=11 |81modc|(1,2,3,4,5,6,7,8)modq in that order|
|3q²,5q² for everyq; also9·7²|0mod3,1mod5; also0mod9|1modq²|
|qs,9qs,q²s,qs² forq<s|0mod9 for9qs; no central condition otherwise|1 at both outside coordinates, to the indicated heights|

The fixed central originals are2mod3,1mod9,4mod5,1mod25,0mod15; each outside pure original is0modq. These are101 distinct numerical moduli:91 anchor mixed, five outside pure and five central; the central group includes15. Put P0={3,5,7,11,13,17,19}. Missing members may be inserted as auxiliary deletions with exactly these phases. Any additional P0 pure original must be absent or already null on the stated raw pure-product support; this explicitly excludes further live higher-pure deletions.

For each outside q use Haar conditioned on its nonzero first root; each live first root has mass r_q=1/(q−1), and each specified second-level cylinder has mass1/[q(q−1)]. These are actual normalized laws, with all finite higher digits Haar, satisfying the inherited caps r_q at depth1 and1/[(q−2)q^(e−1)] at depth e>=2.

Use root-major central indices l=0,...,5 and m=0,...,19, representing the literal residues

    x3(l)=floor(l/3)+3(l mod3) modulo9,
    x5(m)=floor(m/5)+5(m mod5) modulo25.

The square-pure nulls are l=3 and m=5. The15 deletion additionally removes l<3,m<5. Thus the live index set has80 cells, with l in{0,1,2,4,5} and m in{0,...,19}\{5} outside that rectangle.

At central cell c=(l,m), impose all active cq root deletions. Let R_q(c) be their actual union. All ten qs originals forbid root1 simultaneously at their two endpoints. Their joint complement therefore allows at most one outside coordinate at root1. The ten9qs and twenty square pairs are subsets of these already forbidden pair events. Each3q² event, and9·7², is source-null wherever active because3q already deletes root1. Only5q² can additionally delete the special second-level cylinder1modq².

Define A_q(c) as the actual surviving mass at root1 and B_q(c) as that at all other roots. If root1 is deleted, A_q=0. If root1 survives and the5q² event is active, A_q=1/q; otherwise A_q=r_q. The other mass is

    B_q(c)=[q−1−|R_q(c)|−1_(1 notin R_q(c))]/(q−1).

The exact conditional outside survivor mass is

    F(U;c)=product_(q in U)B_q(c)
             +sum_(p in U)A_p(c) product_(q in U\{p})B_q(c),
    zeta_c(1)=F(Q;c).

This is a disjoint sum over zero or exactly one surviving root1 coordinate. It uses the actual restriction of the conditional Haar product. No matching thinning, union subtraction, or positive-fibre division defines this source. The cell(l,m)=(0,6), with central residue81mod225, is dead and gives zero for every query as well as for mass.

## 2. One global queried root tuple and all finite query heights

Fix the queried support T and first-root values d_T once for the entire central sum. A deleted queried root gives zero. Two queried root1 coordinates also give zero. If no queried root equals1, the normalized first-root response is F(Q\T;c). If just p in T has queried root1, it is

    [A_p(c)/r_p] product_(q notin T)B_q(c).

Root9 is globally undeleted at every q>=11 and is different from1. It therefore dominates every other choice at that queried coordinate simultaneously in all cells: changing a killed root to9 restores support, and changing root1 to9 removes the at-most-one restriction and its factor at most one. At7, roots5 and6 suffice jointly; root6 dominates roots1,2,3,4 in every cell. The numeric source retains all six roots and checks this domination exactly. Thus maximization over roots5/6 is lawful only after one fixed root has been used across the whole central sum.

For a queried root other than1, each actual depth-e cylinder has its Haar conditional mass; its ratio to the inherited depth-e cap, for e>=2, is(q−2)/(q−1)<=1. At root1, a deleted special child gives zero; every other child has the same deep ratio. When root1 survives, its first-root normalized factor is at least(q−1)/q, and

    (q−1)/q > (q−2)/(q−1).

Hence the normalized first-root response with the same fixed root dominates all deeper queries. The result is one actual source with simultaneous full-height bounds, not a separate source selected for each query.

## 3. Actual central deep selectors

Fix the central weights at weak corner(i,j)=(4,10): w_i=1/9, other nonnull w_l=2/9; v_j=3/75, other nonnull v_m=4/75. Let each central law be uniform within its surviving mod9 or mod25 leaf. This is possible for the stated finite family because no higher central pure originals are present.

For any cell-constant retention field0<=theta(l,m)<=1, define one submeasure by multiplying the central product law and zeta_c by theta(c). Neither the outside event predicates nor theta depend on higher central digits. Therefore for a ternary depth-e cylinder contained in leaf l,

    rho3(cylinder)=w_l 3^(2−e), e>=3.

Relative to the inherited cap2·3^(−e), the exact multiplier is9w_l/2: one half on the weak leaf and one elsewhere. The quinary multiplier relative to(4/3)5^(−f) is75v_m/4: three quarters on the weak leaf and one elsewhere. Two deep queries acquire the product of those multipliers. The whole/root/leaf selectors retain their actual weights. These formulas remain valid after theta because it is constant on each mod9×mod25 cell.

Thus the corrected deep menus are

    at3: (9w_l/2) delta_l;
    at5: (4/5)(75v_m/4) delta_m.

The512 coefficients and all four guarded9q² additions remain unchanged. One maximizes each full selector against a single globally fixed outside root choice. Checking a numerical field is then exact rational arithmetic on this same law.

The supplied80-entry field has denominator12 and was independently checked against all559 literal selectors and all six7-root choices. Its complete gate is

    2341914815312423739601756723277
      /119188834050548855719526400000000
    =0.019648776951028825... >193/100000.

This is one actual source at one chosen central weight vector, with a cell-constant field. It does not need a95-corner mixing theorem. It is not an arbitrary-phase result for the fixed91 mixed labels. Its interpretation assumes the stated absence of higher pure originals. The generic-cap ceiling concerns the corresponding conservative response/selector certificate; it is not a ceiling on all possible survivor laws or actual cover geometry.

## 4. The generic envelope has an exact field-universal obstruction

Write the complete gate at the chosen corner as

    G(theta)=sum_c p_c theta_c
        -sum_j C_j max_(d in original menu j) sum_c a_(j,d,c) theta_c,
    0<=theta_c<=1.

Here p_c=g w_l v_m zeta_c(1), every C_j is the unchanged nonnegative charge, and each d specifies a legal central selector and ONE globally fixed outside queried root. The generic and actual gates differ only in the deep central entries a_(j,d,c). The source coefficient remains g=200163067/201247200.

For each charged group j, the numerical witness supplies nonnegative integers n_(j,d) summing to2^32. Put lambda_(j,d)=C_j n_(j,d)/2^32. Then, for every field in the full cube,

    G_generic(theta)
      <=sum_c [p_c-sum_(j,d)lambda_(j,d)a_(j,d,c)] theta_c
      <=sum_c max(0,p_c-sum_(j,d)lambda_(j,d)a_(j,d,c)).

Every term of this dual mixture is an original selector at a globally fixed root. No synthetic query, independently chosen source, field priority or optimizer correctness is assumed. The exact last value is

    U_generic=2257007011560668856122899
      /411282767102882161859179315200000000
      =5.487725701369053e-12... <193/100000.

Thus a larger library of fields, a different optimization algorithm, or a priority constraint cannot repair this particular generic response envelope. The obstruction allows all80 field entries to vary independently in[0,1]. It is not an upper bound on every possible survivor law or on the original arithmetic survivor set.

For the SAME source and complete charges, the denominator12 field evaluated with the actual deep selectors gives the positive rational value in section3. Its generic-selector value is negative:

    -71797489255198563303508567559
      /1146046481255277458841600000000.

The constant-one field has the same negative value under both selector conventions,

    -96670790422594502298224800043
      /1862325532039825870617600000000.

The gain therefore requires the actual deep-cap relation together with a nonconstant retention field. Source mass before retention is unchanged:

    305684996597/646498195200.

## 5. Uniform factor thinning restores the omitted-coordinate interface

The exact source factors used above have mass at most one, whereas655's continuation comparison uses smaller specified mass bounds. The following source operation restores that interface without choosing a new phase or a query-specific law.

For Q={7,11,13,17,19}, let

    (D7,D11,D13,D17,D19)
      =(5/6,871/990,1549/1716,3793/4080,5455/5814).

Before the exact pair restriction, replace every actual unary factor sigma_(q,c) by Dq sigma_(q,c). Since sigma_(q,c)<=rho_q and its mass is at most one, the thinned factor is dominated by rho_q and has mass at most Dq. Every inherited full-height prefix cap is preserved as an upper bound. Pair restriction and the cell field only decrease the resulting source.

The entire joint source, and every actual prefix-query measure of it, are multiplied by the SAME constant

    D_all=product_q Dq=429470970629/743970230784.

This is a homogeneous source operation, not renormalization. Consequently the positive complete gate becomes

    D_all G_actual
      =1005784428862661895329308405859299747631233
       /88672944375462709711523174183180697600000000
      =0.011342630336080047... >193/100000.

The product domination and omitted-coordinate mass premise of655 are thereby available on this same thinned source. The omitted-coordinate premise is only one interface. The remaining complete-inventory and continuation steps are as follows.

### The four full9q² originals must be paid

The frozen diagnostic uses four added g a_q charges at mode9=(2,1), forq=11,13,17,19. In646/648 these pay only guarded portions of9q² lying inside one extra quinary root. Our fixed101 source does not provide that guard for an arbitrary new9q² phase. The network extension therefore REPLACES those four added coefficients by g a_q at mode8=(2,0), which pays the whole original with its actual central mod9 phase and outside modq² phase.

The original640 coefficient vector contains none of those four restricted labels; replacement does not remove an independently paid45q² charge or any other original. With exactly the same denominator12 field, the additional full-versus-guarded debit is

    913878108228781515431/552684452765855256000000.

The complete mixed-inventory gate before factor thinning is now

    G_full=2144832980209210799160393676877
      /119188834050548855719526400000000
      =0.017995250958655836... .

After the common factor thinning,

    gamma680=921143501847540509401483050662119383445633
      /88672944375462709711523174183180697600000000
      =0.01038810099670533... .

For every field, the full mode8 screen dominates the corresponding guarded mode9 screen. The generic-cap upper obstruction therefore also applies to this strengthened complete-inventory gate. The positive value uses actual deep selectors and the same source throughout.

Of640's95 previously source-null mixed labels,91 are restricted exactly by the fixture and the remaining four are now paid in full. Every other P0 mixed numerical label is included by the unchanged complete L,P90,D370,B inventory. The special classes55,40,90,370,3 together with15 contain559 distinct labels. Their union with the1759-vector old low-height region is exactly all2172 mixed exponent vectors in{0,1,2}^7; their146-label intersection is counted only through the existing numerical-label accounting. Maximum exponent at least3 remains covered by the complete analytic L series. PureP0 originals remain subject to the explicit source-domain restriction above; they are not mixed labels paid by these vectors.

### Rebuild the later head and apply one complete657 policy

Let eta be the scaled actual core source restricted off the remaining actual mixed originals. Original deletion decreases every query. The complete gate just proved gives the same relation eta(1)-c Gamma(eta)>=gamma680 with c=1084133/201247200 used in640. Rebuild the actual normalized head-only kernels at23,29,31 on this eta. They exist at every complete history, including dead fibres, and keep their conditional full-Haar caps5/3,20/11,2. Their phases and finite heights are arbitrary within the existing head inventory.

The core is dominated by the product of the stated central laws and the uniformly thinned actual unary factors. Later restrictions only decrease it. Its simultaneous prefix caps therefore follow by reverse integration of the normalized head rows. The old full-Haar head bound1/alpha, alpha=2673/110656, remains valid; even the raw new bound

    195409291636195/13432923795456

is no larger than1/alpha. The same-source product domination, full-height prefix caps and omitted-coordinate factorsDq are precisely the inputs of655's convex-load comparison reused by657. Actual owner phases remain in their rectangle indicators until that comparison; no independently chosen auxiliary law replaces them.

Append657's unchanged actual owner rows. Retain all its ordinary extension domains, strict private-descendant assumptions, disjoint private interiors, single-head TypeI blocks, one-time numerical-label assignment and fixed earlier-parent unions. The193 finite rows,326 Euler factors, complete five-parent tail, TypeI fee and chosen full arbitrary-parent tail are unchanged. The only substituted quantity is the larger head lower bound gamma680.

The exact complete projected margins are

    RS46:        0.00002473658839989808...,
    elementary68:0.000024068178944393297...,

both strictly greater than1/50000. As in657, simultaneous private filling and CRT give full density greater than1/(50000 Q_off). There is no bound on the finite network size, width, depth or exponent heights within those declared interfaces. The two final switches are alternative complete policies; their fees are not combined.

This class lies beyond the old common-root head hypothesis. At central residue36mod225, the active3q and5q originals require two different outside roots1 and2. This cell has positive exact outside mass and positive retained field. Thus a single common live root cannot absorb even those two originals there; the new proof uses their actual joint avoidance instead.

## 6. What changes when higher pure originals return

Let S_l be the actual surviving part of one central leaf, with Haar mass t_l, and suppose mass w_l is assigned uniformly on S_l. Its density is w_l/t_l. Against a reference density bound D, every deeper query has normalized factor at most

    gamma_l=w_l/(D t_l),

provided w_l<=D t_l. The former weak-leaf factors1/2 and3/4 used the full capacities t_l=1/9 and1/25. They do not follow merely from the weak masses.

For the ternary weak leaf, w_l=1/9 and D=2. The whole higher-pure budget1/18 can be concentrated in this leaf. At its limiting remaining capacity t_l=1/18, gamma_l reaches one. For the quinary weak leaf, w_l=1/25 and D=4/3; concentrating the higher-pure budget1/100 leaves t_l=3/100, again giving gamma_l=1.

These limits are approached by actual finite families with one original at each distinct pure modulus, not incompatible independent minima. Within the chosen p² leaf, delete the mutually disjoint cylinders with successive higher-digit words0,10,110,...,1^(n−1)0. Their relative Haar masses are p^(−1),...,p^(−n). Both central coordinates can use these constructions simultaneously. They give

    t3_n=(1+3^(−n))/18,
    gamma3_n=1/(1+3^(−n));
    t5_n=(3+5^(−n))/100,
    gamma5_n=3/(3+5^(−n)).

Both normalized factors tend to one. Thus no uniform improvement below the old density factor is available from weak mass alone over this family. The following single nested path realizes the stronger all-depth sharpness. Every deleted relative word begins0 or1, so child2 is wholly intact. For every e>=3 choose the nested query cylinder whose higher word is2 followed by e-3 zeros. It survives, has Haar massp^(-e), and attains the SAME normalized factor gamma_n at that depth. These queries form one coherent nested path, and the3/5 paths coexist in the same actual family. Thus this uniform-on-survivor source cannot obtain a uniform weighted-depth saving by treating the deep maxima as incompatible. This does not exclude other reweightings, other source constructions, or a bound exploiting the full joint mixed inventory.

A general extension must therefore retain actual leaf capacity together with assigned mass, or retain enough prefix geometry to pay depth-specific queries more sharply. The finite positive witness establishes a benefit from actual-prefix information. It does not discharge the arbitrary higher-pure or unrestricted head-phase problem.

The capacity calculation has the following parameterized form. For any p>2, use reference density D_p=(p−1)/(p−2) and weak mass w=1/p². The same finite prefix family leaves

    t_n=(p−2+p^(−n))/[p²(p−1)],
    w/(D_p t_n)=(p−2)/(p−2+p^(−n)) ->1.

Thus the ternary and quinary limits come from one actual mass/capacity identity, rather than separate incompatible optimizations.

## 7. Portable exact evidence

The101 numerical originals live in [clustered_global_phase_fixture.json](../../frontier/cover-geometry/clustered_global_phase_fixture.json). The exact source producer [clustered_exact_actual_source.py](../../frontier/cover-geometry/clustered_exact_actual_source.py) reads that fixture alone and writes [clustered_exact_actual_source.json](../../frontier/cover-geometry/clustered_exact_actual_source.json). Its44,133 checks include the actual local q² atoms, all pair predicates, all8,960 root-query slices, and the dead-cell zero response. Redundant9qs and square-pair originals remain in the fixture and are verified as source-null rather than silently dropped.

The numerical field and selector-dual weights live in [clustered_actual_boundary_gate_witness.json](../../frontier/cover-geometry/clustered_actual_boundary_gate_witness.json). The source and coefficient hashes are pinned there. The coefficient input is the unchanged `remaining33_global_root_exclusion_certificate.json`, SHA256 `dd00b0e9f6a93713d437394c368a4ef81407cabd5ecb931ae7753926d9d8c3e4`. The exact gate producer [clustered_actual_boundary_gate_certificate.py](../../frontier/cover-geometry/clustered_actual_boundary_gate_certificate.py) uses only standard-library rational arithmetic and performs72,910 explicit checks, including62,607 complete charged literal query screens. The generic dual has564 nonzero terms. These checks remain active when Python assertions are disabled. Its result is [clustered_actual_boundary_gate_certificate.json](../../frontier/cover-geometry/clustered_actual_boundary_gate_certificate.json).

The separately written [clustered680_independent_check.py](../../frontier/cover-geometry/clustered680_independent_check.py) reconstructs the local source from all101 original phases and verifies the same field and dual without reading the gate producer. Its158,626 checks include all8,960 joint query slices and every positive selector with all six7-root choices. The result [clustered680_independent_check.json](../../frontier/cover-geometry/clustered680_independent_check.json) gives the same positive gate and generic ceiling. The independent extension also verifies factor thinning, all query-slice domination bounds, the four full-event fee replacements, and both complete657 budgets from the pinned row and tail data. These are independent implementations within the same model family, not different model families or Lean proofs.

The companion [clustered_actual_boundary_continuation_certificate.py](../../frontier/cover-geometry/clustered_actual_boundary_continuation_certificate.py) and its result retain the full mode8 repair and657 policy reuse. Its45 explicit checks pin the same numerical witnesses and657's exact coefficient data, recompute all four full-query screens, and compare both complete budgets with1/50000. It selects no new row, cap or Euler product. The coefficient input `ordinary_domain_five_parent_certificate.json` has SHA256 `e46183469f280d262da37a9c33005e724690fc20b0106202c42c9913ed8d8ec0`.

The fixed numerical witnesses suffice for reproduction; an optimizer is not required to verify either inequality. The universal field quantifier is the displayed dual inequality. The all-height query statement is the prefix calculation in sections2–3. The finite script is not being used as a cutoff on query depth.

All four retained entry points were rerun with exit code0 and reproduced their result JSON byte for byte. From the artifact directory:

```sh
python3 -I -S -B -O clustered_exact_actual_source.py
python3 -I -S -B -O clustered_actual_boundary_gate_certificate.py
python3 -I -S -B -O clustered_actual_boundary_continuation_certificate.py
python3 -I -S -B -O clustered680_independent_check.py
```
