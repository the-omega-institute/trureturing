# Growing parent sets preserve a common survivor

Keep the fixed reference head and complete actual pure-source and phase
hypotheses of [Report617](617-all-central-star-phases-admit-root-one-continuation.md),
as used by [Report619](619-one-global-three-parent-network-under-the-root-one-head.md).
This includes Report617's relational-root variant: the head primes stay
fixed, the central pure source stays fixed, and the prescribed equalities
and inequalities between outside roots replace their literal digit names.
That extension has the same actual A/B responses, source domination and
query caps; no new head phase class is inferred from the network argument.

An outside owner may now have a fixed parent set of unbounded size, subject
to explicit growth conditions:

| Number r of distinct parents | Required owner prime v |
| --- | --- |
| At most 3 | v>=37 |
| 4 | v>=1253 |
| r>=5 | v>=2(3r)^r+3 |

Several syntactic parent tuples at one owner are allowed when their union
is contained in this ONE fixed parent set. Their actual numerical labels
are combined and deduplicated before one common row is chosen. All other
private-domain restrictions of Report619 remain. The proportion of complete
head configurations admitting a common avoiding extension exceeds1/26000.

There is no uniform bound on parent-set size across the admitted families.
There is still a growth restriction relating that size to the owner's
numerical prime. The head remains the precise Report617 class. The result
is an ordinary proof with exact rational checks, not new Lean verification
or a resolution of unrestricted Erdős #7.

## 1. Actual inventories and the inherited head interface

For each outside owner v, choose one fixed set

    A_v={a_1<...<a_r<v}

from the head primes and the earlier outside network entries. Its actual
mixed inventory is any finite subset of

    a_1^(e_1)...a_r^(e_r)v^h,
    e_i>=0, sum e_i>0, h>=1.                       (GP1)

There is one globally fixed residue at each actual numerical modulus,
and all moduli in the complete family are distinct. If the inventory was
presented through several smaller tuples, GP1 refers to their fixed union;
there is no independent law or optimization for each tuple. A numerical
label appearing in several descriptions remains a single actual label.
The largest prime in GP1 is its unique owner. Pure owner powers enter its
ordinary private domain once and are not part of GP1's nonunit cofactors.
All original heights remain arbitrary finite quantities.

The set A_v is fixed across all histories. Parent values can vary under
the joint law, but neither their identities nor the phases of their
originals are selected separately in different branches. The threshold
uses this declared parent-set size. For r<=3 use Report619's zero-exponent
embedding when necessary.

Ordinary private interiors are disjoint from the network entries and from
each other away from their declared roots. Ordinary Type I head branches,
allowed blocks and separate ordinary components retain Report619's
conditions. Every original belongs once to the head, one owner inventory
or an ordinary block. Additional undeclared private crossings are not
allowed. Network size, depth, width and all exponent heights are unbounded
subject to each actual instance being finite.

Report619's completed source supplies ONE core submeasure eta and full
head law mu with

    eta<=rho, rho a normalized product pure-survivor law,
    eta(1)-c Gamma(eta)>=gamma,
    gamma=60482197234393807/2054767329987788800
          >147/5000,
    mu<=C H_P, C=138320/2673, alpha=1/C,             (GP2)

and head-bad charge at most c Gamma(eta). For every queried head subset J,

    mu_J<=2^|J| H_J.                                (GP3)

These are joint-source statements: eta is dominated by one product rho,
and the normalized head-only kernels have conditional caps at most2.
Report617's relational-root construction preserves these exact interfaces.
It does not relax the fixed central pure source, the common distinguished
pair root, or the reference head primes.

The exact entire three-parent fee from Report619 is

    W_3<531/20000.                                  (GP4)

This is a sharper rational reading of its retained exact certificate,
not a new three-parent calculation. The separate ordinary Type I fee
remains2^-16. The full head density C is used for the final projection.

## 2. One normalized row for the whole fixed parent set

Sample the full head first, then all outside owners in increasing numerical
order. Every parent is already available. The actual ordinary extension
domain of v satisfies

    H_v(V_v^0)>=1-2/(v-1).                           (GP5)

Choose a set of N nonunit parent-exponent patterns and delete every actual
owner cylinder attached to them. At one pattern, original numerical
distinctness permits at most one label at each owner height, so its entire
owner-height cost is at most1/(v-1). Absent labels cost zero. The selected
complement R_v therefore has

    H_v(R_v)>=D_v/(v-1), D_v=v-3-N.                 (GP6)

Use the existing normalized capped row at threshold1/2 on H_v(.|R_v).
If t is its remaining forbidden-fibre mass, the row has relative density
at most2 and remaining violation probability

    (t-1/2)_+/(1/2)<=t^2.                           (GP7)

This row exists and is normalized even when t=1; no surviving fibre is
assumed. The resulting original-Haar cap is2(v-1)/D_v.

Owners with r<=3 keep the actual choices of Report619. For r>=4 choose
an integer n in

    2n^r+3<=v<=2(n+1)^r+1                          (GP8)

and select the full cube[0,n)^r except the unit. Set

    N=n^r-1, D_v=v-n^r-2>=n^r+1.

Then

    2(v-1)/D_v<=4<=v/4.                            (GP9)

For r=4 the threshold1253 gives n>=5. For r>=5 the threshold2(3r)^r+3
gives n>=3r. The old r<=3 rows also have original-Haar cap at most v/4.
Thus every row in this one mixed construction satisfies the SAME uniform
conditional interface at every complete prior history. All rows are
normalized. Their product with mu defines one unnormalized law Pi;
the head marginal and all earlier event masses are preserved.

In particular the old three-parent estimates remain valid under this
new Pi. Their proofs require the inherited head interface and the
conditional bound v/4, not the literal old sampling law. No earlier fee
is combined with an independently favorable choice of later parent law.

## 3. A joint response for arbitrarily many parent roles

Let p_1<p_2<... be the odd primes beginning3,5,7,11,... . Compare r sorted
actual parents to the first r such primes, and define

    c_i=max(2,p_i/4).

The i-th actual parent is at least p_i. A head parent costs at most2 per
positive prefix by GP3. For an outside parent u and positive depth j,
its conditional cap gives

    (u/4)u^(-j)=(1/4)u^(1-j)
       <=(p_i/4)p_i^(-j)<=c_i p_i^(-j).             (GP10)

At depth zero the cost is one. Eliminate queried outside parents in
reverse actual sampling order and then apply the joint head bound.
Unqueried normalized rows integrate out. For two parent patterns x,y,
including any of their actual fixed owner-height phases, the intersection
of the parent cylinders is either empty or has Pi mass at most

    k_r(x,y)=product_(i=1..r)
          [1 if max(x_i,y_i)=0;
           c_i p_i^(-max(x_i,y_i)) otherwise].       (GP11)

No independence of the actual parents is asserted. Repeated queries on
one coordinate pay its cap once at their maximum depth. The kernel
already includes all queried head costs.

For S_n=[0,n)^r let

    M_r(n)=sum_(x notin S_n,y notin S_n)k_r(x,y).    (GP12)

The remaining forbidden fraction t is at most1/D_v times a sum of parent
indicators averaged over actual owner heights with weights(v-1)v^(-h).
Their infinite extension has total weight one. Expanding the square under
this SAME Pi and using GP7/GP11 proves

    Pi(owner-v violation)<=M_r(n)/D_v^2.             (GP13)

This is one moment for the merged inventory, including cross terms between
different syntactic tuples. It is not a sum of separately optimized tuple
fees. Actual inventories are finite; the displayed infinite sums are
convergent nonnegative upper bounds retaining every finite height.

## 4. The complete four-parent tail from1253

For one reference coordinate (p,c), set

    T_p=1+c[3/(p-1)+2/(p-1)^2],
    A_p(n)=c p(p+1)p^(-n)/(p-1)^2,
    B_p(n)=c p^(1-n)[n+1+2/(p-1)]/(p-1).            (GP14)

These are respectively the sum of the coordinate kernel over all pairs,
over both indices at least n, and over one index at least n with the other
unrestricted. Their formulas follow by grouping equal maximum exponents
and summing geometric and arithmetic-geometric series.

Each pattern outside S_n has an exceeding coordinate. Summing same-coordinate
and different-coordinate choices for the two patterns gives

    M_r(n)<=U_r(n)
      :=sum_i A_i(n)product_(j!=i)T_j
          +2sum_(i<j)B_i(n)B_j(n)product_(k!=i,j)T_k. (GP15)

The cases need not be disjoint. Positivity makes the overcount a valid
upper bound.

For r=4 the actual references and caps are

    p=(3,5,7,11), c=(2,2,2,11/4),
    T=(5,11/4,19/9,47/25).

At n=5 exact evaluation gives

    3^5 U_4(5)=2103072391361/28533312500<74.          (GP16)

Each scaled same-coordinate term3^n A_i(n) has successive ratio3/p_i<=1.
For a scaled cross term the ratio for n>=5 is at most

    [3/(p_i p_j)]((n+2)/(n+1))^2
       <=(3/15)(7/6)^2=49/180<1.

Consequently

    M_4(n)<74*3^(-n) for every n>=5.                (GP17)

The interval GP8 contains exactly(n+1)^4-n^4=4n^3+6n^2+4n+1 odd integers.
Consecutive intervals omit only the even integer2(n+1)^4+2, so every prime
at least1253 belongs to one. Replacing primes by these odd integers and
using D_v>=n^4 gives

    W_4<74 sum_(n>=5)3^(-n)
                      [4/n^5+6/n^6+4/n^7+1/n^8]
       <=74*(671/5^8)sum_(n>=5)3^(-n)
        =24827/31640625<1/1250.                    (GP18)

This pays all primes above the threshold, not a finite prime window.
1253 being composite does not affect the bound. The producer also checks
the complete cube moment by the independent finite formula

    M_r(n)=product_i T_i-2 product_i(T_i-B_i(n))
                          +product_i(T_i-2B_i(n)+A_i(n))

at selected n. The universal estimate is established by GP14--GP18.

## 5. One bound for every larger parent-set size

For every odd reference prime p>=3 and c=max(2,p/4),

    c/(p-1)<=1, 2/(p-1)<=1.

It follows that T_p<=5. For n>=1, since3/p<=1,

    3^n A_p(n)
       <=3c(p+1)/(p-1)^2<=6.                       (GP19)

Also B_p(n)<=(n+2)p^(1-n). For distinct reference primes p,q their product
is at least15, so

    3^n B_p(n)B_q(n)
       <=3(n+2)^2[3/(pq)]^(n-1)
       <=3(n+2)^2/5^(n-1)<=27.                     (GP20)

The last sequence starts at27 for n=1, and its successive ratio is at
most(1/5)(4/3)^2=16/45<1. Substitution into GP15 yields, simultaneously
for every r>=5 and n>=1,

    M_r(n)<=C_r 3^(-n),
    C_r=6r5^(r-1)+54 binom(r,2)5^(r-2)
       =3r(9r+1)5^(r-2).                          (GP21)

Let n0=3r. The owner threshold in the opening table ensures n>=n0 in GP8.
There are at most2r(n+1)^(r-1) integers in that interval: use
(n+1)^r-n^r=sum_(j=0..r-1)(n+1)^(r-1-j)n^j. Missing endpoints between
successive intervals are even, so all odd primes are included. Thus

    W_r<=2r C_r sum_(n>=3r)
               (1+1/n)^(r-1)n^(-r-1)3^(-n).

Here(1+1/n)^(r-1)<=3. For example, its binomial expansion is bounded by
the geometric series with ratio(r-1)/n<=1/3, whose sum is at most3/2.
Replacing the remaining inverse power by its value at3r gives

    W_r<=t_r:=9r C_r/(3r)^(r+1)*3^(-3r).            (GP22)

This is a bound on the complete prime tail for each arity r, with all
owner heights already included.

To sum all arities, direct simplification gives, for r>=4,

    t_(r+1)/t_r
      =(5/81)*[(9r+10)/(9r+1)]
                  *(r/(r+1))^(r-1)/(r+1)
      <=(5/81)*(46/37)*(1/5)
       =46/2997<1/60.                              (GP23)

In particular

    t_5=46/1937102445,
    sum_(r>=5)W_r<=sum_(r>=5)t_r
       <=(60/59)t_5=184/7619269617<1/10^7.          (GP24)

No finite arity cutoff is used. Each actual finite family has only finitely
many owners and parent sizes; the infinite nonnegative series majorizes
all of them uniformly. The growth restriction is essential to the proved
series: it is not removed by summing over r.

## 6. One complete common-law budget and gluing

Partition the actual owners according to their one declared parent-set
size. Under the single Pi, their total violation mass is bounded by the
old complete W_3, plus W_4, plus the sum in GP24. Paying the entire positive
series for all three ranges is conservative. An actual owner still uses
only one row; no simultaneous realization of separate worst inventories
is asserted.

Subtract head-bad and the inherited Type I head blockers as well. By
GP2, GP4, GP18 and GP24,

    Pi(good)
      >147/5000-531/20000-1/65536-1/1250-1/10^7
       =10417363/5120000000>0.                    (GP25)

All actual mixed originals, including those presented in different tuples
at the same owner, are tested in this one joint assignment. Owner values
lie in their actual ordinary extension domains. The declared private
witnesses and separate ordinary components can be filled simultaneously
because their interfaces are disjoint. CRT supplies an integer avoiding
every original class.

The head marginal of Pi remains mu. Project good using the FULL head
constant C, obtaining

    H_P(U_ext)>alpha*10417363/5120000000
       =27845611299/708198400000000>1/26000.         (GP26)

For Q_off equal to the product of all outside resolving prime powers,
full survivor density exceeds1/(26000 Q_off). The uniform head fraction
does not give a full-density bound independent of outside heights.

The remaining restrictions are the complete Report617 head hypotheses,
the displayed growth thresholds, one fixed parent union at each owner,
smaller-parent order, and the inherited private-interior conditions.
Allowing an arbitrary changing union, unrestricted small-prime parent
sets or unrestricted head phases would require further estimates.

## Retained exact certificate

The [producer](../../frontier/cover-geometry/growing_parent_sets_forward_kernels.py)
and [data](../../frontier/cover-geometry/growing_parent_sets_forward_kernels.json)
check 28 named exact arithmetic conditions: the checked Report619 source
fingerprint and sharper readings of its existing constants, four-parent
cube constants and a separate complement computation, the complete
four-parent tail, the all-arity ratio and geometric constants, and the
positive final budget. The conditional-law proof and the universal
inequalities GP19--GP24 supply the unbounded quantifiers; finite arithmetic
checks do not replace them. No new Lean verification is claimed.

    python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/growing_parent_sets_forward_kernels.py
