# The complete gate budget requires layout-adapted leaf-pair fields

Within the declared matching-response and95-corner gate criterion, the commonleaf4 and commonleaf5 layouts each admit their complete network, but no single common95-field family pays that same complete budget for both. An exact dual upper bound separates the two quantifier orders. The obstruction concerns this conservative certificate criterion; it does not assert that the actual family lacks a survivor or that a layout-dependent construction is impossible.

All ten `9qs` originals may have central residue `4 mod9`, with arbitrary globally fixed outside endpoints, while retaining Report661's arbitrary finite higher central pure phases and complete declared network. A new jointly compatible family of95 fields gives full survivor density strictly greater than

    1/(170000 Q_off).

The square7 central roles remain `1 mod3`, `2 mod5`, `7 mod9`; the forty linear-star and ten `qs` root incidences remain. In particular the ten leaf-pair activations now lie atleaf4 while the square7 depth-two activation stays atleaf5. This is not a relabelling that moves both together. The first/square pure phases and null leaves, central15 phase0, original inventory and ordinary/private interfaces remain those of [Report661](661-priority-fields-restore-arbitrary-higher-central-pure-phases.md). Every `225q` incidence is still required. No arbitrary independently varying central `9qs` layout is asserted.

The original661 fields fail on this layout, even for a finite actual128-original family and every admitted central source under those fields. Their conservative gate is negative despite an explicit actual survivor. The new result replaces the fields and preserves the source, complete charges and actual-source transport. Thus a negative fixed-field gate does not establish failure of the whole compatible-field method.

This is ordinary mathematics with exact rational certificates. It is not new Lean verification or a resolution of unrestricted Erdős #7.

## 1. One changed activation, one common source

Use the root-major central coordinates

    x3(l)=l//3+3(l mod3) mod9, l=0,...,5,
    x5(m)=m//5+5(m mod5) mod25, m=0,...,19.

The nonnull leaf sets are `I={0,1,2,4,5}` and `J={0,...,19}\{5}`. The central15 mask deletes `l<3,m<5`. First/square pure originals retain `2 mod3`, `1 mod9`, `4 mod5`, `1 mod25`, or the same auxiliary deletions at absent slots. Higher central pure originals may have arbitrary globally fixed residues and arbitrary finite heights. Outside-core pure originals retain661's arbitrary phase and height scope.

For `Q={7,11,13,17,19}`, put

    r_q=1/(q-1), a_q=1/[q(q-2)],
    Z7(c)=5/6-[1_(l//3=1)+1_(m//5=2)+1_(l=5)]/35,
    Zq=(q-2)/(q-1)-2a_q, q>7,
    beta_qs(c)=a_q r_s+r_q a_s+r_q r_s 1_(l=4).       (D1)

The `Z7` rule still containsleaf5. Only the final activation inbeta changes from661. Every actual `9qs` central component is now4mod9, fixed once throughout that family's source, queries and continuation. All twenty `q²s/qs²` and ten `9qs` outside endpoints remain arbitrary globally fixed phases.

The conditional matching construction of [Report659](659-a-common-thinning-admits-ten-free-leaf-pair-endpoints.md) gives one outside submeasure at each cell, with exact empty response and simultaneous envelopes

    H_T(c)=sum_(matchings F in Q\T)(-1)^|F|
             product_(e in F)beta_e(c)
             product_(q notin T union vertices(F))Zq(c). (D2)

Apply the central mask separately. The actual outside source is independent of the numerical weak markers and deeper central digits. The uniform union bound

    1-sum_(q<s) beta_qs/(Zq Zs)
      >=754111121894423/876550251053789>0             (D3)

still holds: bound everybeta by `a_q r_s+r_q a_s+r_q r_s` and everyZ by its old minimum. On five vertices every induced matching polynomial has only constant, negative single-edge and positive disjoint-pair terms. D3 therefore places all induced polynomials in the strict region. This establishes the source interface, not a positive complete gate.

The complete512 coefficients are exactly661's pinned640 array with the same four `g a_q` additions at central mode(2,1), support{q}, q=11,13,17,19, where `g=200163067/201247200`. No charged original, full-height sum or guarded9q² fee is dropped. Pair-block originals are removed once in the actual source.

## 2. A realized failure of the old fields

Evaluate the unchanged661 fields on D1--D2. Among the95 numerical corners,14 gates are negative. The minimum, attained exactly at weak pairs(5,6),(5,7),(5,8),(5,9), is

    W=-1564824019544284842082124059
         /541767427502494798725120000000
      =-0.002888368587897579... .                    (D4)

All six live-cell response profiles still have every one of their1024 induced Shearer polynomials positive. D4 is not a zero-fibre or illegal-normalization artifact.

Here is one actual finite family realizing the issue. At the ten head primes use first pure phases2 at3,4 at5, and0 elsewhere; add1mod9,1mod25 and0mod15. For eachq inQ include all eight linear stars `3q,5q,15q,9q,25q,45q,75q,225q`, with central components0 and outside root1. Include the three square stars with respective component pairs `(1 mod3,2 modq²)`, `(2 mod5,3 modq²)`, `(7 mod9,4 modq²)`. For eachq<s include `qs` with roots(1,1), `9qs` with central4mod9 and outside roots(2,2), and `q²s,qs²` with both outside prime-power components5. These are108 distinct numerical moduli. CRT fixes one residue per original. The constant root table `t_q(c)=1` satisfies all fifty retained incidences.

Add, for everye=3,...,12, the twenty pure originals

    7+3^(e-1) mod3^e,
    6+5^(e-1) mod5^e.                               (D5)

Each coordinate's new cylinders are pairwise disjoint and lie within its designated weak live leaf. For the actual surviving setsS_l,S_m, the admissible normalized-source leaf capacities are `C3_l=2H3(S_l)`, `C5_m=(4/3)H5(S_m)`. At the weak leaves these equal

    C3_5=1/9+eps3, eps3=3^(-12),
    C5_6=3/75+eps5, eps5=1/(3*5^12).

All strong live leaves retain capacities2/9 and4/75. Thus every normalized feasible central source has leaf vectorsw,v satisfying

    ||w-w0||1<=2eps3, ||v-v0||1<=2eps5,
    w0=(2,2,2,0,2,1)/9,
    v0=(4,4,4,4,4,0,3,4,...,4)/75.                  (D6)

The cornerw0,v0 itself is realized by constant densities on the actual pure survivors, under the same caps2 and4/3. This is an actual source statement for this inventory; arbitrary numerical corners are not assumed actual for every inventory.

WriteG(w,v;theta) for the fixed D2 envelope functional with all512 fees. The old effective-field rule is, outsideleaf5 and the masks,

    theta_eff(w;c)=t_s+(t_w-t_s)[2/(9w_l)-1],

where `(t_w,t_s)` is `(19/20,11/12)` oncolumn2 and `(11/12,8/9)` otherwise. Leaf5 stays one. Its derivative inw_l has magnitude at most3/5 on `1/9<=w_l<=2/9`. EveryH_T and every selector total mass is at most one. ThereforeG is `(g+sum C_j)`-Lipschitz in each of the relevant L1 weight norms and the field sup norm. Combining these bounds with D6 gives

    G(w,v;theta_eff(w))
      <=W+2(g+sum C_j)[(8/5)eps3+eps5]
      =-832364737001229126640319362983505989571
          /292884748778639056731441750000000000000000
      =-0.0028419531589551172... <0.                 (D7)

D7 concerns every admitted normalized central product source of this128-original inventory, using this prescribed effective field and conservative envelope budget. It is not a bound on actual surviving mass, on exact probabilities in place of query envelopes, or on optimized fields.

Indeed this family has a survivor: use local full-depth residues `x3=0`, `x5=x7=x11=x13=x17=x19=6`, and `x23=x29=x31=1`. Their CRT combination is

    101642458635358409323242187506
       modulo280450252940623696839111328125.

The evidence checks that it avoids all128 original classes. The negative certificate and noncoverage therefore coexist explicitly.

## 3. Joint fields and a symmetry reduction that preserves the task

For each `(i,j) in I×J` use the same numerical comparison weights as661,

    w^i_l=(2-1_(i=l))/9, v^j_m=(4-1_(j=m))/75.

Choose an entire fieldtheta^ij, zero on null/masked cells, satisfying BOTH priority inequalities at every live cellc=(l,m):

    theta^lj_c>=theta^ij_c,
    theta^im_c>=theta^ij_c.                         (D8)

The witness has180 explicitly supplied integer numerators and denominator2^20. It expands to all95 fields and7600 live entries. The numerical data and producer define the field completely; they require no optimizer or external helper.

A useful exact reduction explains this size. Permute the three regular ternary leaves{0,1,2}, and independently permute the live quinary leaves within each of the four columns. These permutations preserve D1--D2, both nulls, the central mask and all four selector levels: whole weights, root/column restrictions, leaf restrictions, and deep selectorsdelta_l and(4/5)delta_m. They permute selector choices inside each menu, leaving all512 coefficients unchanged. No outside prime is permuted.

These are symmetries of the comparison tables. The actual original family and its chosen conditional outside laws are not assumed symmetric.

Act simultaneously on weak markers and cells. D8 and `0<=theta<=1` are preserved. Each corner gate is a linear mass minus a nonnegative sum of maxima of linear forms, hence concave in its field. Averaging an entire feasible95-field family over this finite symmetry group cannot reduce its minimum corner gate. There are12 corner orbits and180 field-entry orbits. This is a lossless restriction for this fixed layout's group, not for arbitrary layouts of independently phased edges.

The witness is certified directly on all95 corners, without relying on representative-only checks. If a proposed field requires repair, [Report645](645-weak-marker-priority-preserves-actual-corner-mixtures.md)'s greatest admissible minorant is

    theta#^ij_c=min(theta^ij_c,theta^lj_c,theta^im_c,theta^lm_c).

The retained dyadic witness already satisfies D8. Its complete gates use every one of the512 modes, all original selector choices and the same field for mass and every query. Their exact minimum is

    gamma4=559369963871230293301573748245583
             /260371980944558993614496071680000000
           >107/50000.                             (D9)

The finite certificate establishes a feasible jointly compatible family. No optimum or separate attainment of corner optima is needed.

## 4. Transport to arbitrary actual higher pure phases

For any one admitted actual family, construct the normalized central laws from661 and [Report624](624-arbitrary-central-pure-phases-admit-two-complete-boundary-gates.md). The tail bounds `sum_(e>=3)3^-e=1/18` and `sum_(e>=3)5^-e=1/100` give nonnull capacitiesC3_l,C5_m with sums at least one and upper bounds2/9,4/75. Choose probability vectorsalpha,beta satisfying

    alpha_l>=(2/9-C3_l)/(1/9),
    beta_m>=(4/75-C5_m)/(1/75).

Then `w=sum_i alpha_i w^i`, `v=sum_j beta_j v^j` are actual feasible leaf masses. Constant densities within actual surviving leaves realize laws bounded by2H3 and(4/3)H5. The same conditional outside submeasure from D2 is attached at every cell, independently of these numerical corner indices.

Set

    lambda_c=sum_(i,j)alpha_i beta_j w^i_l v^j_m theta^ij_c,
    theta_eff(c)=lambda_c/(w_l v_m)

on positive cells, and zero on nulls and the common mask. Report645 applies because D8 holds for one entire field family and the outside response is shared. It constructs ONE actual thinning with gate at least

    sum_(i,j)alpha_i beta_j G_ij(theta^ij)>=gamma4.   (D10)

This transport includes every finite query depth. The density caps bound deep single and paired cylinders, the priority inequalities bound their retained fields by the corresponding averaged corner fields, and a maximum of the average is bounded by the average of maxima. Shallow modes use the retained masses exactly. No corner source or network is assumed individually actual and then mixed; the actual source is constructed first from that family's capacities.

## 5. Complete continuation and scope

Changing the9qs central activation and multiplying by a field in[0,1] preserve domination by the same actual pure product and its unary submeasures. The normalized-row construction and all source-correct omitted-coordinate factors used in661 remain valid. The complete512 charges,23/29/31 continuation recipe,193 finite ordinary row parameters, generic five-role moment bound, all326 explicit Euler factors, Type I fee and arbitrary-parent tail budgets are unchanged. The actual continuation and network laws are instantiated on the new head submeasure; they are not copied as the oldleaf5 probability laws. D10 provides the new source-unit mass with which those simultaneous costs are paid.

The two network policies from [Report658](658-joint-square-pairs-and-deep-linear-stars-preserve-a-complete-network.md) therefore give the following lower reserves after the same projection factor `2673/110656`:

| Complete policy | Projected reserve |
|---|---:|
|At most three earlier declared parents below2^46; arbitrary finite parents thereafter|0.000006603490596535636...|
|At most three earlier declared parents below2^68; arbitrary finite parents thereafter|0.0000059350811410308526...|

Both are strictly greater than1/170000 by exact rational comparison. Ordinary rows remain normalized even on fully forbidden fibres and zero-support histories. Private interiors and original labels retain the same one-time assignment and interface restrictions. Simultaneous filling and CRT divide the displayed reserves by the actualQ_off. Finite network widths, depths, original heights and sizes remain arbitrary within these declared interfaces.

The old661 conclusion still applies to its original commonleaf5 class. The present result gives a distinct commonleaf4 class with a different compatible field family. Neither result permits all ten leaves to vary independently, frees the fifty remaining root incidences, combines the225q phase release, changes null faces, or removes the below-switch parent restriction.

## 6. A common95-field family cannot pay both complete budgets

LetP be the full polytope of95 fields satisfying the masks, `0<=theta<=1` and both D8 inequalities. A memberTheta ofP is held fixed across layouts. For `h in{4,5}`, define

    C_h(Theta)=min_(i,j) G_(h,i,j)(theta^ij),

using that layout's own matching response but the same complete512 coefficients. LetB_RS andB_elem be the respective complete source-unit network fees from658. Report661 and D9--D10 prove

    min_(h in{4,5}) max_(Theta in P) C_h(Theta)>B_elem.

An exact rational dual gives the opposite result in the other order:

    max_(Theta in P) min_(h in{4,5}) C_h(Theta)
      <=D=102102759580521744666143435821925141297
              /60671068953538198064069740635095040000000
      =0.001682890401333... <17/10000
      <B_RS<=B_elem.                                (D11)

The maximum in D11 ranges over every jointly compatible common family, not merely the two particular displayed witnesses.

Here is the certificate mechanism. The symmetry group of section3 also preserves the commonleaf5 response, because bothleaf4 andleaf5 are fixed. Averaging simultaneously across all190 layout/corner gates cannot decrease their minimum. Thus any full common family can be replaced, without loss for this objective, by180 orbit variablesx with `0<=x<=1` and the induced priorities `Ax<=0`. This step is needed to transfer the following upper bound from the reduced problem to all ofP.

For each of thirteen chosen layout/corner cases, select one legal selector for each of its512 modes and form the linear anchor payoff

    E_b(x)=g M_b(x)-sum_r C_r Q_(b,r,s_(b,r))(x)=f_b dot x.

Since every true menu maximum is at least its chosen member, that case's complete gate is at mostE_b. The supplied thirteen nonnegative rational weightsmu_b sum exactly to one. Let `v=sum_b mu_b f_b`. Therefore, for every feasiblex,

    min_(h,i,j) G_(h,i,j)(x)<=v dot x.

The witness also supplies thirteen nonnegative rational priority multiplierslambda. Each priority row is explicitly oriented `x_lower-x_upper<=0`, so

    v dot x=(v-A^T lambda) dot x+lambda dot Ax
            <=sum_k max(0,(v-A^T lambda)_k)=D.       (D12)

The checker reconstructs the complete selected-query coefficients from the two actual response formulas, including all orbit multiplicities. It verifies every selected index against its full labelled menu and verifies every priority row against D8. No floating dual status, numerical tolerance or unlisted selector is used in D12. A chosen selector need not maximize its menu; the upper-bound direction is valid without that assumption.

This is a limitation of a uniform-corner sufficient certificate with one field family independent of layout and the declared complete fees. It is not an upper bound on actual surviving mass, not an impossibility theorem for source-adapted weights with a different argument, and not a failure of `for every layout, there exists a compatible field family`. The two positive constructions explicitly realize the latter quantifier order on these two layouts. D11 rules out certifying even these two layouts by one common field family under this criterion, while allowing a common positive head gate below the continuation budget.

## Exact evidence and remaining problem

The [producer](../../frontier/cover-geometry/leaf_pair_adapted_priority_fields_certificate.py) and [exact data](../../frontier/cover-geometry/leaf_pair_adapted_priority_fields_certificate.json) read only the pinned640 and658 JSON. They rebuild120 cell responses, expand the180-entry dyadic witness, check both priority inequalities on all7600 live field entries, and evaluate48,640 complete screens with1,699,360 literal selector candidates. All68,207 explicit checks pass with assertions disabled. Both complete network comparisons are exact.

```sh
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/leaf_pair_adapted_priority_fields_certificate.py
```

The [old-field counterexample verifier](../../frontier/cover-geometry/leaf_pair_displaced_old_field_counterexample.py) and [its exact result](../../frontier/cover-geometry/leaf_pair_displaced_old_field_counterexample.json) independently reconstruct all95 old-field gates using only pinned640 data. Its89,867 explicit checks also include all induced polynomials,128 original CRT phases, actual unary survivors and prefix caps, the capacity and Lipschitz constants, and the displayed actual survivor. D7's all-source conclusion uses the proof above, not just a finite source sample.

```sh
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/leaf_pair_displaced_old_field_counterexample.py
```

The [independent adapted-field verifier](../../frontier/cover-geometry/leaf_pair_adapted_priority_fields_independent.py) and [its exact output](../../frontier/cover-geometry/leaf_pair_adapted_priority_fields_independent.json) read the pinned640/658 inputs and the candidate field JSON, without reading or importing the producer. Its85,875 explicit checks rebuild all95 gates and source masses, both sets of7600 priority comparisons, the six full induced-Shearer profiles, every complete screen and both network reserves. All reconstructed gates and source masses agree exactly with the candidate. The minimizing corners are precisely `i in{0,1,2}, j in{15,...,19}`, fifteen in total.

```sh
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/leaf_pair_adapted_priority_fields_independent.py
```

The [common-field dual checker](../../frontier/cover-geometry/leaf_pair_common_field_dual_certificate.py) and [its exact data](../../frontier/cover-geometry/leaf_pair_common_field_dual_certificate.json) retain all thirteen selector bundles and thirteen priority multipliers. They reconstruct both response tables and every signed orbit coefficient from pinned640/658 data. All14,655 explicit checks pass, including exact weight normalization,6,656 labelled selector memberships, priority directions, D11 and both complete budget gaps. The rational witness is embedded in the checker; no optimizer output or temporary input is needed.

The [independent dual verifier](../../frontier/cover-geometry/leaf_pair_common_field_dual_independent.py) and [its exact data](../../frontier/cover-geometry/leaf_pair_common_field_dual_independent.json) read that candidate JSON and the pinned640/658 files without reading or importing the checker. Its14,175 checks reconstruct the7600-variable action of seventeen group generators and obtain exactly180 genuine orbits. They also independently reconstruct every selected linear payoff, verify each priority multiplier and recover all180 residual coefficients and exactly the same D11 bound. This includes the full orbit multiplicities rather than assuming a reduced-coordinate inequality applies to the original field problem.

```sh
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/leaf_pair_common_field_dual_certificate.py
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/leaf_pair_common_field_dual_independent.py
```

All five programs accept `--directory` and `--output`; the adapted-field and dual independent verifiers also accept `--witness`. The retained defaults use sibling canonical inputs. The actual-source construction, all-depth inequalities, symmetry reduction and continuation arguments are the ordinary proofs above and in the cited reports. No finite computation here is a Lean proof.

The missing uniform obligation remains: for every globally fixed assignment of ten central leaves, construct ONE actual source and ONE jointly compatible95-field family whose complete gate pays its full continuation. The dual obstruction to a single common family does not refute this layout-dependent target. Independent corner optima still cannot be combined without the source-transport inequalities.
