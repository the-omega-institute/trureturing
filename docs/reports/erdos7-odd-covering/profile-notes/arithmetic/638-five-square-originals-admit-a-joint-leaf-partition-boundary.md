# Five9q² originals admit an exact joint-leaf partition boundary

This is an ordinary finite derivation on [Report635](635-a-thirty-label-conditional-core-admits370-missing-originals.md)'s fixed geometry, using its actual conditional-source interface. No new Lean theorem or arbitrary-phase positive extension is claimed here. The numerical probe below fixes source corner(3,4,5,6), the original635 common theta, all370 admitted additions and all512 complete costs.

## Global assignments are ordered partitions

Let Q={7,11,13,17,19}. The new original9q² has one globally fixed ternary square-leaf role lambda(q) in{0,...,5}; source-null or missing roles may be padded by an arbitrary fixed live role when seeking a sufficient bound. An outside square phase remains arbitrary under a_q=1/[q(q−2)]. Define

    A_l={q:lambda(q)=l}.

Then(A_0,...,A_5) is an ordered partition of Q, and this correspondence is bijective. In particular a coordinate q cannot be independently assigned to more than one leaf. There are exactly6^5 globally coherent assignments.

At leaf c=(l,m), restrict the actual old star submeasure by the active new9q² cylinder, then uniformly thin to

    Z_q^lambda(l,m)=Z_q(l,m)-a_q*1_(q in A_l).

This lower mass is valid even if the actual original is absent or already avoided. Keep the same unconditional30 pair unions with cap kappa_e. On a fixed support where all Z_q−a_q are positive and the entire induced-polynomial test at these smallest masses is strict, every coherent assignment has one valid conditional source and all32 response grids. On the original635 positive-theta support the exact uniform test has minimum

    414845761706/567846054963 >0.

Thus no assignment-dependent mask is used in this calculation. All old370 costs remain and the new five labels are enforced by the source, without duplicate original-loss fees.

## Every selected reading factors through this partition boundary

For fixed T, source weights and one legitimate central selector k, write

    Q_(T,k)(lambda)=sum_l F_(T,k,l)(A_l).

Here F is obtained by summing the selected central columns m of t(l,m)H_T^lambda(l,m), with the declared selector weights. The formula is literal: at fixed l the source depends only on the subset A_l, not on how the remaining q are assigned to other leaves. Deep selectors retain delta_l and(4/5)delta_m. The same property holds for a selector difference, a fixed linear combination of query readings, and the complete mass term.

For ANY six real-valued tables F_l:2^Q->R define

    D_0(empty)=0,
    D_0(S)=-infinity for S nonempty,
    D_(i+1)(S)=max_(A subset S)[D_i(S\A)+F_i(A)].

Induction on i shows that D_i(S) is exactly the maximum sum over ordered disjoint allocations of S to the first i leaves. Hence D_6(Q) is the exact maximum over all6^5 coherent global assignments. Replace max/-infinity by min/+infinity for minima. Since sum_(S subset Q)2^|S|=3^5, the six stages use at most6*3^5 transitions. If a leaf's table is identically zero, it may absorb all unused coordinates; optimizing over used subsets after the nonzero leaves is exactly equivalent.

This is the sufficient joint interface missing from an all-cell cap. The state S records which labelled q coordinates have already been assigned; it prevents charging the same9q² role on all six leaves.

## Complete anchor-and-regret bound

Fix a legitimate reference selector k_j^0 for each of all512 cost rows. Let d_j be the full nonnegative635 coefficient g(L_j+D370_j+P_cond,j)+cW_j, with g=1-c. Define

    E(lambda)=g M(lambda)-sum_j d_j Q_(j,k_j^0)(lambda),
    Delta_(j,k)(lambda)=Q_(j,k)(lambda)-Q_(j,k_j^0)(lambda).

Then the actual complete cap gate obeys the exact identity

    G(lambda)=E(lambda)-sum_j d_j max_k Delta_(j,k)(lambda).

The reference selector belongs to the menu, so every maximum is nonnegative. Partition DP gives exact extrema for each fixed selector potential; therefore

    G(lambda) >= min_lambda E(lambda)
                 -sum_j d_j max_(lambda,k)Delta_(j,k)(lambda).

Every selector is retained or eliminated only when a valid upper bound cannot exceed the current exact maximum. The different extremizers in this last bound need not be jointly realizable; their combination is a conservative lower bound, not an asserted actual family. A negative lower bound proves neither that all real gates are negative nor that every theta fails.

For a FIXED set of anchors the displayed lower-bound functional is concave in theta: the first term is a minimum of linear forms, and each subtracted regret is a maximum of linear forms. The minimizing partition and maximizing(selector,partition) choices supply exact supporting linear forms. This permits a finite cutting-plane common-theta search using the partition DP as a separation routine. Changing anchors changes the sufficient bound and must be stated; it cannot silently be used to claim the fixed-anchor optimization's optimality.

## Source weights and the valid symmetry reduction

For fixed lambda, a fixed mask and a fixed common theta, the complete gate is separately concave in the two central source-weight vectors. Thus a common theta whose gate is positive at every weight corner for that same lambda extends by the established fixed-null mixtures.

The old128 source representatives cannot automatically be reused while lambda is held fixed. For the uniform statement over ALL6^5 assignments, however, the original stabilizer acts simultaneously by

    (w,v,lambda) -> (g w,g v,g composed with lambda).

It preserves the fixed STAR/mask, every complete menu, and theta whenever theta is constant on the old ten categories. The global assignment domain is closed under this action. Consequently the worst gate over all lambda is invariant on the old source orbits. Therefore128 source representatives suffice for a bound uniform over all lambda AND all actual source weights, provided exactly the same theta is used throughout. Separately optimized theta at individual source corners cannot be combined by Jensen.

## The current exact outcome and its scope

The original635 theta fails the sufficient test requiring a positive complete gate at every comparison source corner and every five-label layout. On source corner(3,4,5,6), the DP reference minimum produces the coherent assignment

    lambda_(7,11,13,17,19)=(1,4,4,4,4).

A direct Fraction recomputation of its complete512-selector gate gives

    -934992879319436220663921430853 /
     1489860425631860696494080000000000 <0.

The five new numerical labels can explicitly be realized as

    246 mod441, 364 mod1089, 508 mod1521,
    868 mod2601, 1084 mod3249.

Their ternary square residues are3,4,4,4,4 and their outside square residues are all1. These choices are globally fixed and distinct. The CRT assignments verify that this is a permitted global phase pattern. They do not prove that the chosen comparison source corner is attained by any finite actual pure-tail family; that reachability is not established here.

This is an obstruction to THAT fixed-theta comparison-corner certificate. It is not a zero optimum over all thinnings, not an empty actual fibre and not an arithmetic covering counterexample. No statement about every actual source or every possible thinning follows from this comparison-corner obstruction.

The first full-domain anchor-regret lower bound is also negative:

    -8711574065190673896313336557113 /
     2483100709386434494156800000000000.

The scaled-integer probe uses4608 distinct selector potentials and292 exact DP extremum calls. A separate Fraction matrix/menu calculation confirms the coherent negative witness. The useful retained result is the complete partition interface and the exact fixed-strategy obstruction. An arbitrary-phase positive five-label extension remains unproved.

The [portable exact DP](../../frontier/cover-geometry/five9square_partition_dp.py) and [result data](../../frontier/cover-geometry/five9square_partition_dp.json) bind the complete Report635 source coefficients, preserve all original-label assignments and verify the reported rational values. Run

    python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/five9square_partition_dp.py

An independent Fraction reconstruction imports no producer. It verifies all1024 strict polynomials, reconstructs all512 costs from the original L/W arrays and the370-label census, and independently evaluates the complete gate at the displayed layout. Its4467 checks include the five full CRT residues and distinct numerical labels. The DP recurrence and joint symmetry argument above justify their stated quantifiers; the program does not enumerate7776 times11400 source-layout pairs.
