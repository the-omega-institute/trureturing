# Two common fields release every column1 leaf-pair layout

For square7 roles (row1,column1,leaf5), the ten labelled9qs central roles may now independently occupy any live leaf. Two exact whole95 priority-field families suffice: the constant-one family and one supplied180-entry dyadic family. For every layout one entire family has complete head gate strictly greater than193/100000, simultaneously at every weak corner and against every charged selector menu.

The main complete continuation policies of [Report666](666-four-parent-owners-return-on-the-complete-leaf-pair-head.md) consequently retain density strictly greater than1/(2000000 Q_off). Its other policies retain their individually stated bounds; in particular the common149 weaker-density variant retains its stated1/(10000000 Q_off) bound.

Swapping the two live leaves4 and5 through the entire configuration gives the same independent9qs freedom for square7 roles(1,1,4). Together with [Report667](667-a-new-square7-column-needs-a-different-common-field.md), the full arbitrary-layout conclusion therefore holds for the six square7 live triples

    (row,column,leaf) in {1} x {1,2,3} x {4,5}.       (C1)

Every original's phases are chosen once globally. The low pure deletions2mod3,1mod9,4mod5,1mod25, central15 phase0, literal head primes, incidence conditions on the forty linear-star and ten qs slots when present, and declared ordinary/private interfaces remain required. The full retained head inventory includes225q originals; such slots need not be present. The existing arbitrary finite higher-pure and outside-phase scope is unchanged. Missing or source-null originals admit the conservative padding described below. This is ordinary mathematics with exact arithmetic certificates, not new Lean verification or unrestricted Erdős #7.

## 1. One fixed layout controls the entire response table

Use the same root-major coordinates and live mask as665/668. Let

    Q=(7,11,13,17,19), I={0,1,2,4,5},
    J={0,...,19}\{5},
    LIVE={(l,m) in I x J: not(l<3 and m<5)}.

There are80 live cells and95 weak corners(i,j) in I x J. The ten pair labels are the lexicographically ordered two-element subsets ofQ. Fix one layout alpha=(alpha_e) in I^10. Define

    r_q=1/(q-1), a_q=1/[q(q-2)],
    b_qs=a_q r_s+r_q a_s, d_qs=r_q r_s,
    beta_e(l,m)=b_e+d_e 1_(l=alpha_e),
    Z7(l,m)=5/6-[1_(floor(l/3)=1)+1_(floor(m/5)=1)+1_(l=5)]/35,
    Zq(l,m)=(q-2)/(q-1)-2a_q, q>7.                  (C2)

The same alpha_e is used at every corner, query support and selector. No phases are optimized independently across cells.

For T subset Q the response H_T is the degree-at-most-two matching sum on the remaining coordinates:

    H_T = sum_(matchings M on Q\T) (-1)^|M|
            [product_(e in M) beta_e]
            [product_(q in Q\(T union vertices(M))) Zq]. (C3)

The uniform strict induced-region bound used in665 remains valid becauseZ7>=157/210 and0<=beta_e<=b_e+d_e. It constructs the same actual conditional source and query interface. It does not by itself establish the positive gate; that is supplied by the simultaneous field certificate.

The complete gate is the established linear mass term minus all512 charged menu maxima. Its coefficients are the pinned640 complete array with the same four guarded9q² additions. Every retained original and full-height query charge remains included.

## 2. Two simultaneous field families

The first family is theta^(i,j)(l,m)=1 onLIVE for all95 corners. The second uses the supplied180 integers with common denominator2^20. Its orbit key is

    (r if i<3 else i,
     eq/other if i,l<3 else r if l<3 else l,
     floor(j/5),floor(m/5),j=m).                      (C4)

The eq/other entry records whetheri=l. This key is merely an explicit storage format for the whole family; for each arbitrary fixed alpha no false regular-leaf symmetry is assumed for its response table.

The preparation program checks all field bounds and both inequalities, for every one of the95 corners and80 live cells:

    theta^(l,j)(l,m)>=theta^(i,j)(l,m),
    theta^(i,m)(l,m)>=theta^(i,j)(l,m).               (C5)

These are joint whole-family conditions. Choosing a different field at each corner or query is not allowed. The selected family depends only on the globally fixed layout, and is then used for every source row and every charged maximum associated with it.

## 3. The complete finite certificate

Simultaneously relabelling regular leaves0,1,2 acts on the entire alpha tuple, weak corners and field cells. Both stored families are equivariant for that action. This reduces the5^10 actual layouts to

    (5^10+3*3^10+2*2^10)/6 = 1,657,470               (C6)

canonical restricted-growth layouts. This is a transport between different layouts, not an assertion that an arbitrary fixed mixed layout is invariant under the regular S3.

Within each fixed layout, permutations internal to each quinary column reduce the95 corner calculations to the20 representatives

    i in{0,1,2,4,5}, j in{0,6,10,15}.                (C7)

All five ternary weak markers remain represented. Every one of the559 literal selectors per corner is either retained or exactly dominated by a retained selector on the same nonnegative response profile. Thus the reduced menus compute the original maxima, rather than a smaller experimental menu.

The rational preparer bounds local H_T responses between integer multiples of2^-32, rounds the mass coefficient down and each nonnegative fee coefficient up, and checks all accumulator bounds. The C++ evaluator uses the lower mass and upper fee terms, then rounds the resulting complete lower bound down to a multiple of2^-40. Consequently every accepted integer is a lower bound for the exact complete gate.

The acceptance threshold is

    ceil(2^40 *193/100000)=2122057442.                (C8)

The final selection tries the dyadic family first and the constant-one family second. The exhaustive result is:

| Quantity | Certified result |
| --- | ---: |
| Canonical layouts |1,657,470|
| Actual labelled layouts |9,765,625|
| Uncertified layouts |0|
| Dyadic family selected |1,639,240|
| Constant-one family selected |18,230|
| Prepared exact checks |450,147|
| Summary consistency checks |25|

The minimum accepted complete lower bound is

    2122076431/1099511627776
      =0.001930017270751705... >193/100000.           (C9)

It occurs at canonical index138107, layout

    (4,0,4,4,4,1,4,2,5,1),                          (C10)

where the dyadic family is selected. The certificate does not claim this interval lower bound is the minimum exact gate or an optimal achievable bound.

The [preparer](../../frontier/cover-geometry/column1_full_leaf_pair_library_interval_prepare.py), [integer checker](../../frontier/cover-geometry/column1_full_leaf_pair_library_interval_checker.cpp), [reproduction wrapper](../../frontier/cover-geometry/column1_full_leaf_pair_library_interval_certificate.py) and [complete certificate](../../frontier/cover-geometry/column1_full_leaf_pair_library_interval_certificate.json) retain the final witness, its exact input pins and the compressed per-layout family selection. Reproduce with

```sh
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/column1_full_leaf_pair_library_interval_certificate.py --threads 8
```

The wrapper reconstructs its binary input, recompiles the sibling checker, reruns every canonical layout and independently recounts the restricted-growth indices and orbit sizes. The directly stored network margins concern658's three-parent budget. The666 extension follows from the unchanged interface and the common193/100000 lower bound, not from claiming that those stored fields directly recompute every666 policy.

The separately authored [independent driver](../../frontier/cover-geometry/column1_full_layout_independent_driver.py), [independent evaluator](../../frontier/cover-geometry/column1_full_layout_independent.cpp) and [result](../../frontier/cover-geometry/column1_full_layout_independent_driver.json) read only the pinned640/658 inputs and the two numerical fields in the final certificate. They do not read or import the producer implementation. They reconstruct all131,072 rational responses and all22,360 literal selector vectors before exact domination checks, then use a separate prefix traversal and the opposite family-selection order.

All1,657,470 representatives pass independently. This traversal selects the constant field1,615,608 times and the dyadic field41,862 times, completing33,149,400 accepted corner gates and16,972,492,800 accepted screen maxima. Its minimum selected lower integer is2122059746, still above the target2122057442. Its495,773 checks pass. Different selections can give different minimum lower bounds; both runs certify the same common gate193/100000 for every layout.

```sh
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/column1_full_layout_independent_driver.py
```

Both final entry points were rerun from repository sources with exit code0. The producer result reproduced byte for byte; the independent default entry performs the full traversal and reproduced every mathematical result of its separate full run. A separate ordinary-mathematical review checked the source, symmetries, priorities, padding and continuation. These implementations and review use the same agent/model family; they are not a claim of different model families or new Lean verification.

## 4. Actual-source transport and the remaining boundary

For each globally fixed actual family, choose its canonical alpha, take the single selected whole95 family, and transport it back by the same regular-leaf permutation. All phases, fields and selectors move together. Apply the actual-source comparison and645's two-priority transport to the finite higher-pure survivor laws. The same omitted-coordinate factors, full-Haar caps, Euler factors, normalized row reconstruction and complete continuation charges then apply. The lower boundC9 supplies the nominal gate required by666.

If an original9qs label is absent or its central component is killed by the fixed low pure deletions, pad that one label by any fixed live leaf. The virtual edge caps dominate its actual zero activation, so the established one-cell ratio thinning transfers the certified gate. Padding is chosen once per original, not independently by source row or query. The same conservative treatment of missing or source-null square7 roles remains available within a certified triple.

Finally, swapping leaves4 and5 changes both the square7 leaf role and every alpha marker. Since the theorem now covers every labelled alpha, the transported domain is again allI^10. This proves the column1 leaf4 case and hence the expanded six-triple domainC1.

The other live square7 role orbits, alternative low pure/null geometry, arbitrary head incidence patterns, and unrestricted Erdős #7 remain outside this result. The new conclusion releases independent9qs leaf choices in the specified column1 geometry while preserving the common-source, whole-family and complete-network obligations.
