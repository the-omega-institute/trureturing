# A common thinning admits ten free leaf-pair endpoints

All ten originals9qs, q<s in Q={7,11,13,17,19}, may have arbitrary globally fixed outside endpoints together with all twenty square-pair freedoms of [Report648](648-induced-pair-responses-release-all-square-pairs.md), under the fixed central source and roles below. The declared three-parent ordinary/private network has full survivor density strictly greater than

    1/(420000 Q_off).

This statement uses the central pure deletions2mod3,1mod9,4mod5,1mod25 and no additional source-live central pure originals; every9qs has central residue7mod9; the three square stars at7 have the fixed central roles1mod3,2mod5,7mod9. Forty linear-star and tenqs incidences remain, including every225q incidence. Other outside phases and higher outside pure heights retain the scope specified below. These are substantive restrictions.

The first complete network permits at most three fixed earlier declared parents at each37<=v<2^46 and arbitrary fixed finite parent unions atv>=2^46. An elementary alternative uses the same restrictions with switch2^68. Both retain the declared ordinary domains, private-interior separation and one-time original assignments of [Report657](657-sharper-private-domains-admit-five-parents-from-sixty-seven.md). Neither result combines [Report658](658-joint-square-pairs-and-deep-linear-stars-preserve-a-complete-network.md)'s225q phase release.

The exact complete head gate is

    gamma*=1864487415907319442048626989
              /931162766019912935308800000000
           =0.0020023217035156316... >1/500.             (T1)

A small rational thinning repairs a fixed layout whose unthinned union and matching gates were negative. Its proof uses one explicitly realized actual central source and one common response table. It does not assert a uniform theorem for arbitrary central9qs roles or arbitrary higher central pure phases, and is not new Lean verification or unrestricted Erdős #7.

## 1. An explicit actual central source

Use the central pure originals

    2 mod3, 1 mod9; 4 mod5, 1 mod25,

with no additional source-live pure3 or pure5 originals. A missing one of these four can be imposed as the same auxiliary deletion. Further pure originals are allowed only if they are already null on this declared source; no arbitrary higher central pure phase is included.

In root-major leaves, l denotes literal residue l//3+3(l mod3) modulo9 and m denotes m//5+5(m mod5) modulo25. Set

    w=(2,2,2,0,1,2)/9,
    v_m=0 for m=5, 3/75 for m=6, 4/75 otherwise.

These sum to one. The ternary null leaf3 and quinary null leaf5 are exactly the literal pure-square residues1. Distribute the stated mass uniformly inside each surviving leaf. The resulting actual probability laws satisfy

    rho3<=2H3, rho5<=(4/3)H5,

avoid the four pure originals, and retain every finite-depth prefix cap used by the complete selector interface. This directly realizes corner(3,4,5,6). It needs no isomorphism to652 and no unsupported arbitrary-pure corner realization.

Keep central15 phase0 and its mask M=0 on l<3,m<5. Every other head original and full-height remaining-original fee stays present under648's numerical inventory, subject to the exact retained restrictions below.

## 2. Exact mixed-phase class and one outside source

Keep Q={7,11,13,17,19} and the same fixed cell-dependent live-root table as648. All forty linear stars, including225q, retain their table incidence. All tenqs retain at least one table endpoint. Thus fifty root-incidence requirements remain. This theorem does not combine658's225q release.

All twenty square-pair originals q²s,qs² have arbitrary globally fixed outside phases. All ten9qs have fixed central residue7mod9, equivalently leaf5, and arbitrary globally fixed outside endpoint roots. Their endpoint incidences are removed together. Absence is allowed with fixed auxiliary padding.

At7 fix the square-star central roles

    3*7²: residue1mod3,
    5*7²: residue2mod5,
    9*7²: residue7mod9.

Their outside square prefixes are arbitrary and fixed globally. Atq>7 the three square-star central roles and outside phases remain arbitrary. All outside-core pure originals retain648's arbitrary finite-height scope.

Construct646's actual coordinate submeasures. At7 delete all actual active square-star cylinders and thin to

    Z7(l,m)=5/6-[(l//3=1)+(m//5=2)+(l=5)]/35.

Atq>7 select the same at-most-two square prefixes and thin to

    Zq=(q-2)/(q-1)-2/[q(q-2)].

Keep the four guarded9q² residual fees unchanged. The forty linear stars and tenqs are null by the retained root table. Root-table values are fixed by the original family and do not depend on a query, corner or later continuation.

For each edgee={q,s}, combine its three actual events q²s,qs² and, on l=5,9qs. With r_q=1/(q-1), a_q=1/[q(q-2)], their unnormalized product cap is

    beta_e(l,m)=a_q r_s+r_q a_s+r_q r_s 1_(l=5).        (T2)

The edge events may overlap and their outside phases may differ. Their ordinary dependency graph is L(K5) under the normalized product of the actual coordinate submeasures. The exact uniform bound

    1-sum_e beta_e/(Z_q Z_s)
      >=754111121894423/876550251053789>0

puts every cell in the strict induced-Shearer region: every induced polynomial is at least1 minus the full edge sum, since its size-two matching terms are nonnegative.

Reuse [Report623](623-a-common-matching-source-admits-arbitrary-edge-endpoints.md), equations(ME8)--(ME9), in the general edge-union form of [Report629](629-free-pair-endpoints-need-a-common-source-with-dead-fibres.md) and [Report632](632-a-common-thinning-repairs-a-negative-free-endpoint-gate.md). It constructs one actual pair-avoiding submeasure zeta_c dominated by the coordinate product, with exact empty response and simultaneous full-height query upper responses

    H_T(c)=sum_(matchings J in Q\T)(-1)^|J|
              product_(e in J)beta_e(c)
              product_(q notin T union vertices(J))Zq(c). (T3)

The maximum matching size is two. This is one conditional scaling for the whole pair union, not a different source per query. It deletes all thirty square-pair/leaf-pair originals. The central dependence is only through c, so deep selectors remain those of624/648.

## 3. One four-level thinning across all queries

On the actual positive cells outside M, define theta by this table. Column2 means m//5=2.

| Ternary leaves | Column2 | Other columns |
| --- | ---: | ---: |
| l=0,1,2 | 11/12 | 8/9 |
| l=4 | 19/20 | 11/12 |
| l=5 | 1 | 1 |

Set theta=0 on either pure-null leaf and on the central15 mask. This is one deterministic cell field fixed for the declared actual family. It is used for source mass, every original-loss charge and all query screens.

Define the actual core source by mixing theta(c) zeta_c with rho3*rho5 and the mask. It remains dominated by the same actual pure product. Guarded residuals and all remaining core originals are then handled by the complete648 coefficients. The512-entry array is unchanged because9qs was previously excluded from the loss inventory by root absorption and is now excluded by actual pair avoidance; it is never omitted or charged twice.

Let C_j be that full array, g=200163067/201247200, mu_c=w_l v_m, and let s_jk(c) range over each complete central selector menu. Then the sufficient gate is exactly

    K(theta)=g sum_c mu_c theta(c) M(c)H_empty(c)
       -sum_j C_j max_k sum_c s_jk(c)theta(c)M(c)H_(Tj)(c). (T4)

The literal evaluator retains every120cell response and all512 screens. It gives actual pre-deletion source mass

    24963364098039109/62033988360960000,

and(T1). No optimization result is used in replay: the small rational table is substituted and every selector maximum is recomputed. An independently authored literal evaluator gives exactly the same fraction.

The mechanism is a joint balance, not a decrease in all terms separately. Relative to theta=1, the weighted mass cost and query-fee saving are

    mass cost=6761250336491948285528653
                 /212230829862088418304000000,
    fee saving=10154652838156781489246540287
                 /286511620313819364710400000000.

Their difference is strictly positive and larger than the old negative gate's magnitude. In particular the active9qs rowl=5 is retained fully; the thinning mostly suppresses other rows that sustain expensive query maxima. An initial derivative cannot substitute for evaluating the whole new gate because the maximizing selectors can change under thinning.

## 4. An exact sufficient inequality for extending this construction

Fix one actual response table and one actual source. For an allowed attenuation d_c in[0,1], put theta_c=1-d_c. Write

    R_jk=sum_c s_jk(c)M(c)H_(Tj)(c),
    D_jk(d)=sum_c s_jk(c)d_c M(c)H_(Tj)(c),
    R_j=max_k R_jk, gap_jk=R_j-R_jk>=0.

The full selector drop is exactly

    R_j-max_k[R_jk-D_jk(d)]
       =min_k[gap_jk+D_jk(d)].                       (T5)

Therefore a complete owner budgetW is paid precisely when the chosen d meets

    sum_j C_j min_k[gap_jk+D_jk(d)]
      > W-K(1)+g sum_c mu_c d_c M(c)H_empty(c).       (T6)

This uses the existing anchor-plus-regret/selector interface of [Report608](608-one-common-thinning-frees-every-central-pair-role.md); it is not a new general optimization theorem. It is a concrete sufficient target retaining selector switching. Bounding only the currently maximizing selectors can overstate the drop after another selector takes over. The table in section3 verifies(T6) for the fixed source and applicableW.

For a uniform arbitrary-central-pure result, more is required. At fixed nulls,624 decomposes actual weights into five times nineteen weak-leaf corners. [Report645](645-weak-marker-priority-preserves-actual-corner-mixtures.md) gives a no-clipping bridge if the95 fields theta^{ij}_c satisfy its weak-marker priority in both indices. One must produce all95 fields for each admissible fixed original layout, give a common positive gate exceeding the network budget, and verify that priority. The single theta table above neither supplies these fields nor proves the required inequalities. Alternatively, [Report644](644-actual-capacity-clipping-realizes-corner-dependent-thinnings.md) gives an explicit capacity-clipping loss that must be paid. Independent optimal fields at different corners cannot simply be averaged.

For arbitrary ten9qs central roles, the complete response(T2) must retain ten separately fixed leaves alpha_e, and(T6) or the coupled95field certificate must hold uniformly over those layouts. The existing strict Shearer bound provides a valid source, but not this gate positivity. No claim of that uniform extension is made here.

## 5. The complete declared network on this actual source

The source after the complete head treatment keeps the same simultaneous joint caps and the same omitted-coordinate products as657/658: its matching source is dominated by the coordinate product, and theta<=1 only restricts it. Therefore the three-parent upper comparisonT3={3,5,7} and657's fixed finite h/cap schedule apply on a freshly built joint law.

Use every actual finite row and every actual tail row from658's declared schedule, with at most three fixed parents below2^46 (or2^68) and arbitrary finite parents afterward. Keep all ordinary/private interfaces, one-time original assignments, Type I witnesses and globally fixed phases. The full cap table is unchanged, so its complete W5 and Euler-tail fees remain valid.

Using the pinned658 coefficient budget gives

    RS46 raw margin:       0.00012734159413485581...,
    elementary68 margin:  0.00009967099304681341...,
    RS46 projected:       0.000003076056256528969...,
    elementary projected: 0.000002407646801024185... .

Both projected margins exceed1/420000, before division by the actual outside resolving productQ_off. This is a direct same-source budget comparison using the established658 schedule and pinned exact coefficients. The actual-source and original-scope hypotheses in sections1--2, especially the restricted central pure inventory and fixed leaf5 roles, remain essential. No additional225q release is combined.

## Exact evidence and remaining boundary

The portable [producer](../../frontier/cover-geometry/leaf_pair_common_thinning_certificate.py) and its [exact data](../../frontier/cover-geometry/leaf_pair_common_thinning_certificate.json) pin the existing640 coefficient certificate and658 network certificate. They reconstruct the actual central weights, all120cell matching responses, the common rational thinning and all512 screens, evaluating17888 literal selector candidates. They verify every193 adopted finite cap and recompute both final budgets with the new head gate. Its4830 explicit checks pass with assertions disabled:

```sh
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/leaf_pair_common_thinning_certificate.py
```

The [independent verifier](../../frontier/cover-geometry/leaf_pair_common_thinning_independent.py) and its [exact result](../../frontier/cover-geometry/leaf_pair_common_thinning_independent.json) reconstruct the literal response grids, every complete selector and the final network comparisons without reading the producer. Its6933 checks pass with assertions disabled. It recovers the unthinned ten-leaf-pair union and matching gates, verifies the exact weighted mass cost and query-fee saving, then checks both complete raw/projected budgets and the common1/420000 bound. The source realization and all-height transport remain the ordinary proof obligations supplied above; these finite checks do not replace them.

```sh
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/leaf_pair_common_thinning_independent.py
```

Only sibling canonical certificates are inputs. The producer accepts `--directory`; the independent verifier accepts `--source640`, `--network658` and `--candidate` for explicit input paths. Both accept `--output` for the result path. No optimizer, phase scan, temporary helper or negative-gate probe is needed. The ordinary actual-source, full-height, common-query and network arguments above supply the universal quantifiers within this restricted phase class.

The result demonstrates that a negative unthinned sufficient gate need not obstruct a lawful common-source selection. It does not remove the fixed central pure inventory, the specified7-square-star roles, the common9qs ternary role, the fifty remaining root incidences, or the ordinary/private interfaces. Extending the source weights or original layouts requires the simultaneous selector inequalities(T6) and the actual-source transport described in section4; one positive field does not supply that uniform extension.
