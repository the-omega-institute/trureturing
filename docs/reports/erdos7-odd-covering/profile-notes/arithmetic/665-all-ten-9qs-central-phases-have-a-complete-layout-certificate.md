# All ten 9qs central phases have a complete layout certificate

The ten originals of the form 9qs, with q<s in Q={7,11,13,17,19}, can now have arbitrary globally fixed central and outside phases within the declared family. Their central mod9 components need not agree, occupy only leaves4/5, or contain at most one regular leaf. Optional missing 9qs originals are included by the comparison argument below. The same four rational priority-field families from [Report663](663-a-finite-field-library-covers-every-binary-leaf-layout.md) cover every five-live-leaf layout. The complete ordinary/private networks have survivor density strictly greater than

    1/(2000000 Q_off).

The first/square pure phases remain 2mod3, 1mod9, 4mod5, 1mod25; the fixed null leaves remain ternary3 and quinary5. The central15 phase remains0, and the three square7 central roles remain 1mod3, 2mod5, 7mod9, or (row1,column2,leaf5). For every present linear-star original cq with c in {3,5,15,9,25,45,75,225}, retain its common-table first-root incidence; for every present qs retain at least one common-table endpoint. These forty plus ten incidence slots remain unchanged. The separate ten 9qs slots have no endpoint-incidence requirement; section6 permits them to be inactive or absent. All twenty square-pair outside phases remain arbitrary. Arbitrary finite higher central pure phases retain the actual-source scope of661. The literal head primes, ordinary domains, private interiors and assignment rules remain unchanged. The two network policies allow at most three declared earlier parents below2^46 or2^68 respectively, and arbitrary fixed finite parent unions above the selected switch.

This is a proof in ordinary mathematics with exhaustive exact integer interval certificates. It is not a new Lean verification or a solution of unrestricted Erdős #7.

## 1. Every labelled live-leaf layout

Keep the root-major leaf coordinates

    x3(l)=l//3+3(l mod3) modulo9,
    x5(m)=m//5+5(m mod5) modulo25.

The five live ternary leaves are I={0,1,2,4,5}, representing residues0,3,6,4,7mod9. The fixed null leaf3 is residue1; the first pure deletion removes residues2,5,8. A full live layout is a map

    alpha:E(K5) -> I.

All ten edges are separately labelled by their actual numerical primes. For each live leaf l put A_l={e:alpha(e)=l}. These five disjoint subsets partition the same ten original edge labels. There are exactly5^10=9,765,625 layouts. No phase is selected independently within different weak corners or queries.

The common matching-source comparison retains

    r_q=1/(q-1),             a_q=1/[q(q-2)],
    b_{qs}=a_q r_s+r_q a_s,  d_{qs}=r_q r_s,
    Z7(l,m)=5/6-[1_(l//3=1)+1_(m//5=2)+1_(l=5)]/35,
    Zq=(q-2)/(q-1)-2a_q for q>7,
    beta_e(l,m)=b_e+d_e 1_(e in A_l).                 (F1)

Every beta_e is bounded by b_e+d_e. The inherited all-induced strict-region bound

    1-sum_e (b_e+d_e)/(Z_q Z_s)
       >=754111121894423/876550251053789>0            (F2)

supplies the same actual conditional matching source and positive matching responses for every admitted layout. This positivity alone is not the complete charged gate; sections3--5 verify that gate separately.

For T subset Q let

    H_T=sum_(matchings M on Q\T) (-1)^|M|
               product_(e in M) beta_e
               product_(q in Q\(T union vertices(M))) Zq.  (F3)

These are simultaneous full-height response envelopes from the one source, not separately attained optima. The matching degree is at most two, so the table has four possible Z7 decrements,1024 local edge subsets and32 supports:131,072 exact rational entries.

For a cell in leaf l, use its actual subset A_l and

    n(l,m)=1_(l//3=1)+1_(l=5)+1_(m//5=2).

Thus H_T(l,m)=H_T(n(l,m),A_l). Aggregation uses ten profiles, one for each pair (l,1_(m//5=2)). Different regular leaves may carry different subsets; they are never merged inside one layout.

## 2. A complete canonical cover and twenty corner representatives

A simultaneous permutation of regular leaves0,1,2 transports an entire admissible cylinder problem. On first root0 write x=3t+9u and send it to3sigma(t)+9u, with sigma a permutation of{0,1,2}; leave the other first roots unchanged. Carry every actual phase component, higher pure deletion, root table, source and query through this one bijection. It preserves cylinder heights, measure, the fixed low deletions, central15 mask, the specified square7 roles and the declared incidence relations. It does not permute outside primes or numerical edge costs.

Canonicalize regular labels by their order of first occurrence in the ten-edge lexicographic list. Recursively try labels4,5 and then regular labels0 through min(number already used,2). This restricted-growth enumeration has

    (5^10+3*3^10+2*2^10)/6=1,657,470                 (F4)

representatives. A representative using zero, one, two or three regular labels represents respectively1,3,6 or6 actual layouts. The exact sum is5^10. This is an exhaustive orbit cover, not a sample or an identification of different numerical edges.

Within an arbitrary fixed layout there need not be any nontrivial ternary symmetry. Accordingly all five weak ternary markers are retained. Only quinary permutations within each column are used to reduce weak-marker corners. Representatives are

    (i,j) in {0,1,2,4,5} × {0,6,10,15}.             (F5)

Their quinary multiplicities are5,4,5,5 for each i, giving all95 corners. The source tables, fixed mask, field library and every whole/root/leaf/deep selector transform together. Arbitrary actual root tables need not themselves be symmetric: this is a symmetry of the common numerical comparison interface.

The four supplied field families possess the stronger regular-label equivariance already checked in663. Their180-entry storage remains a valid representation of those specific witnesses. It is not asserted to parameterize every possible field for an arbitrary layout.

## 3. One whole family for every corner and continuation

The library is copied byte for byte from663's pinned exact JSON. Family0 has denominator180; the other families have denominator2^20. Each family contains all95 weak-corner fields, lies between zero and one, and satisfies both priority inequalities

    theta^(l,j)(l,m)>=theta^(i,j)(l,m),
    theta^(i,m)(l,m)>=theta^(i,j)(l,m).              (F6)

For each entire canonical layout choose one family from this fixed four-item library. The same choice is used for all twenty representative corners, every one of the512 charged query modes, and the actual-source transport. The precise target is

    for every alpha, there is s(alpha) in{0,1,2,3}
    such that for every weak corner(i,j),
       G_alpha(theta_s(alpha)^(i,j)) >193/100000.     (F7)

The gate retains the pinned640 complete coefficients, together with all four guarded9q² fees from658. Its mass coefficient is g=200163067/201247200. Every selector menu is maximized in full; no selector is frozen across different layouts or supports.

Each literal selector is aggregated exactly to the ten profiles above. The preparer removes duplicates and coefficientwise dominated original selectors, checking both that each retained selector is an original and that every original is dominated by a retained original. Nonnegative responses preserve every menu maximum. There are44,720 literal selector vectors across all four families and twenty corners;7,485 remain. This is an exact reduction of the given menu, with no added synthetic majorants.

The selection order3,0,1,2 is only a deterministic witness-search convention. The final selection is stored explicitly. A rejected candidate is not evidence that its true gate is negative or that another method fails.

## 4. Exact outward intervals and exhaustive finite verification

All input matching responses are reconstructed with rational arithmetic. Put A=2^32 and form

    L_T=floor(A H_T), U_T=ceil(A H_T),
    g_L=floor(A g), C_j^+=ceil(A C_j).               (F8)

Every bound is checked by exact integer comparison. For a selected field with denominator D, selector coefficients are nonnegative integers d_p with sum at most675D. Ternary selector scaling is9 and quinary scaling is75. At a corner evaluate

    m_-=sum_p d_mass,p L_empty,p,
    M_j^+=max_(d in original menu j) sum_p d_p U_T,p,
    N=g_L m_- - sum_j C_j^+ M_j^+.                 (F9)

Then the true gate is at least

    N/(675D 2^64)
       >= q/2^40,
    q=floor(N/(675D 2^24)).                         (F10)

The second floor is mathematical floor, including when N is negative. All fees are nonnegative. During a rejected family test, a partial numerator below the threshold can safely reject that candidate because unpaid fees can only lower the complete numerator. Every accepted family nevertheless completes all twenty corners and all their512 query modes.

The exact range bounds are

    sum_j C_j^+=28,820,733,047,
    max_j C_j^+=4,850,262,370,
    675D2^32<=3,039,929,748,475,084,800<2^63,
    |N| and every signed partial sum <2^97<2^127.    (F11)

Selector multiply-adds use64-bit integers; coefficient-screen products promote to signed128-bit before multiplication. The largest fee coefficient exceeds32-bit capacity. The finite checker validates input bounds, all selected family indices, exhaustive layout count and the fixed threshold

    ceil((193/100000)2^40)=2,122,057,442.             (F12)

The complete run covers all1,657,470 representatives with zero failures. The smallest certified lower value among its selected families is

    gamma_cert=2,122,060,321/1,099,511,627,776
                 >193/100000.                       (F13)

It occurs at canonical index1,387,528, with layout

    [0,1,5,0,1,1,4,0,5,1]

in lexicographic edge order and selected family0. This is a minimum of the reported outward lower bounds for the selected witnesses; it is not claimed to be the exact unrounded gate minimum or the optimum over all possible fields.

The selected-family counts are98,546;9,235;482,129;1,067,560. The run evaluates45,475,659 corner attempts and23,199,364,495 nonzero screen maxima, including candidate rejections. The rational preparer passes507,051 explicit checks. The result JSON stores the complete selection as1,657,470 one-byte family indices in lossless zlib/base64 form, with hashes and length; the compressed data is18,813 bytes. Binary input and executable files are temporary reproducible objects.

## 5. Actual higher-pure sources and complete networks

For a fixed actual family, first use624/661 to obtain the actual pure3 and pure5 survivor laws after its arbitrary finite higher pure deletions. They are supported on that family's surviving cylinders and dominated by2H3 and(4/3)H5. Decompose their numerical leaf weights into fixed-null weak-corner vectors with coefficients alpha_i,beta_j.

Attach the actual conditional matching submeasure fromF1 at each live cell, independently of numerical weak-marker choices and deeper central digits. For the one selected whole family define

    lambda(l,m)=sum_(i,j) alpha_i beta_j w_l^i v_m^j
                                      theta_s^(i,j)(l,m),
    theta_eff(l,m)=lambda(l,m)/(w_l v_m)

on positive cells, and zero elsewhere. Both prioritiesF6 are preserved. The645 transport gives, with every finite query depth present,

    G_actual(theta_eff)
       >=sum_(i,j)alpha_i beta_j G_alpha(theta_s^(i,j))
       >=gamma_cert.                                (F14)

No numerical extremal corner is treated as an actual higher-pure source. No field, source or original phase is changed in response to a later query.

The same domination preserves the inherited joint caps and omitted-coordinate estimates. Build the23/29/31 continuation and all actual normalized rows with657/658, paying all193 finite row bounds, the complete owner tail, TypeI fee and each selected arbitrary-parent tail. With projection2673/110656 the exact certified reserves are

    RS46:          0.0000013291205868309112...,
    elementary68: 0.0000006607111313261276...,

both strictly above1/2000000. Already the smaller rational gate193/100000 pays both policies. The exact public-integer thresholds for this density are2,084,321,048 and2,114,745,196, belowF12. All fee comparisons are rational; no rounded decimal is used to establish positivity.

Simultaneous private filling and CRT divide these bounds by the actualQ_off and give the density in the opening statement. The finite size, width, depth and resolving heights within the declared networks remain arbitrary.

## 6. Inactive and missing 9qs originals

The live-layout enumeration directly covers all five central residues that survive the fixed low pure deletions. A 9qs original whose central residue is1,2,5 or8mod9 is already absent from the actual source: residue1 is killed by the square pure deletion and residues2,5,8 by the first pure deletion. Its pair increment is zero on every live cell. A missing 9qs original also has zero increment.

For each such inactive or missing labelled edge, choose an auxiliary virtual live role, for exampleleaf5. Leave every active role unchanged. This makes one complete five-live-leaf layout already covered byF7. At each cell the resulting comparison caps satisfy

    beta_actual<=beta_virtual<=b+d

with the SAME unary massesZq. The uniform all-induced strict conditionF2 still holds. Apply the verified deletion-ratio comparison from663§7. If H_T and H_T^+ denote actual and virtual matching responses, set

    r(l,m)=H_empty^+(l,m)/H_empty(l,m).

Then

    0<r<=1,
    r H_empty=H_empty^+,
    r H_T<=H_T^+ for everyT.                         (F15)

Multiply the virtual layout's entire95-field family by this one cellwise factor. It is independent of both weak markers, so both priorities persist. Actual source mass equals the virtual comparison mass, and every nonnegative query expression, including each full menu maximum, is no larger. Hence, at every numerical corner,

    G_actual(r theta_virtual)>=G_virtual(theta_virtual)
                              >=gamma_cert.          (F16)

The actual conditional law remains the law of the one original family. The factor r is applied once through the field, then the same higher-pure transport and network continuation apply. The virtual layout is a conservative comparison table; it does not insert an actual new modulus, assign incompatible phases or combine separately optimized original families.

Thus the result removes the central-phase restriction on all ten9qs originals, including arbitrary mixtures of active, inactive and absent labels. It does not remove any of the other family restrictions stated above.

## Exact evidence and remaining boundary

The portable [wrapper](../../frontier/cover-geometry/full_leaf_pair_library_interval_certificate.py), [rational preparer](../../frontier/cover-geometry/full_leaf_pair_library_interval_prepare.py), [integer checker](../../frontier/cover-geometry/full_leaf_pair_library_interval_checker.cpp) and [result](../../frontier/cover-geometry/full_leaf_pair_library_interval_certificate.json) use pinned640,658 and663 JSON. Reproduce with a C++17 compiler and Python's standard library:

```sh
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/full_leaf_pair_library_interval_certificate.py
```

The wrapper accepts --directory, --output, --cxx and --threads. The temporary binary tables and executable are regenerated; the declared result contains source hashes, code hashes, fixed-point constants, explicit complete family selection, full gate minimum and both complete policy comparisons. The separately authored [independent driver](../../frontier/cover-geometry/full_layout_independent_driver.py), [independent evaluator](../../frontier/cover-geometry/full_layout_independent.cpp) and [result](../../frontier/cover-geometry/full_layout_independent_driver.json) rebuild the input from the three pinned canonical JSON sources without reading or importing the producer implementation. The driver uses direct matching sums, reconstructs the original menus and verifies the whole priority family. Its evaluator uses a separate prefix-recursive layout order, sparse selector products, the family order0,1,2,3 and direct numerator-threshold comparisons. It completes all1,657,470 representatives, including33,149,400 accepted complete corner gates and16,972,492,800 accepted complete selector maxima. The597,031 input/interface checks pass. Its selected-family counts719,577;15,535;922,352;6 differ from the producer's, but its minimum selected lower integer is also2,122,060,321. A successful selection is a witness, not an assertion of a uniquely best family.

Reproduce the independent result with:

```sh
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/full_layout_independent_driver.py
```

Both entry points were rerun from the retained sources with exit code0 and reproduced their result JSON byte for byte. The two separately generated input tables have identical bytes and SHA256 `75876eb7254d29c93a4d0a5170d263ccd6879e5e82b2b278610faa4f609055b6`. This is independent implementation within the same agent/model family, not a claim of independent model families or Lean verification.

The previous binary and one-regular-leaf results retain their stronger density bounds on those smaller domains. This result supplies an exhaustive bound for every9qs central phase in the current family. Its new freedom is not a release of the fixed low pure/null geometry, central15 phase, square7 roles, retained root incidences, literal head primes or ordinary/private interfaces. Those remain distinct proof obligations on the route to unrestricted Erdős #7.
