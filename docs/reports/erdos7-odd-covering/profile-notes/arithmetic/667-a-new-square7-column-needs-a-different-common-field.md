# A new square7 column needs a different common field

A new common priority-field family supports square7 roles (row1,column1,leaf5) when the ten live9qs roles all occupy leaf5. Its exact uniform head gate exceeds193/100000, so it supports every complete network policy of [Report666](666-four-parent-owners-return-on-the-complete-leaf-pair-head.md), including density strictly greater than1/(2000000 Q_off).

There is also a symmetry extension of665's full arbitrary9qs-layout theorem. Its fixed square7 triple(1,2,5) may be replaced by any triple in

    (1,{2,3},{4,5}).                                  (R1)

The entire source, original phases, root tables, fields and queries must be transported together. These four triples retain665's complete independent phase freedom for all ten9qs originals. The new column1 branch has the narrower same-leaf condition; the two results do not imply their Cartesian product.

| Certified square7 live roles | Permitted live9qs layout | Network consequence |
| --- | --- | --- |
| (1,s,ell), s in{2,3}, ell in{4,5} | Every independently assigned layout | All666 policies |
| (1,1,5) | Every active9qs central leaf is5 | All666 policies |
| (1,1,4) | Every active9qs central leaf is4 | The transported preceding branch |

Inactive or missing labels are handled by fixed auxiliary padding below. In root-major coordinates leaf4 is residue4mod9 and leaf5 is residue7mod9. Column1 means central residue1mod5; it is distinguished by the fixed quinary square-pure null.

The low pure deletions2mod3,1mod9,4mod5,1mod25, central15 phase0, literal head primes, actual40 linear-star and10 qs incidence slots, all other head inventory and ordinary/private interfaces remain those of665/666. The square-star roles at q>7 and the permitted outside phases retain their existing arbitrary finite scope. This is ordinary mathematics with exact arithmetic certificates, not new Lean verification or unrestricted Erdős #7.

## 1. The actual rooted symmetries leave twelve live role orbits

Use the root-major ternary coordinates l=0,...,8, with first root floor(l/3). The first pure deletion fixes root2 and the square pure deletion fixes leaf3 in root1. Therefore any permitted rooted bijection fixes roots0,1,2 individually. The surviving internal actions are the permutations of leaves0,1,2, the swap of leaves4,5, and permutations of the dead leaves6,7,8. Their leaf orbits are

    {0,1,2}, {3}, {4,5}, {6,7,8}.                     (R2)

On the quinary side the first pure deletion fixes root4, the square pure deletion fixes its root1, and the central15 mask distinguishes root0. Thus the only nontrivial root permutation swaps roots2 and3. The quinary role orbits are

    {0}, {1}, {2,3}, {4}.                             (R3)

Permutations within each allowed subtree must carry its complete deeper cylinders. They preserve the numerical modulus and its exponent, the low deletions and the central15 mask. The actual family is transformed by one bijection; original labels and outside numerical primes are never interchanged.

Consequently the135 full triples (r,s,ell) in3×5×9 have48 orbits. The48 padded triples used in646, with r in{0,1}, s in{0,1,2,3}, ell in{0,...,5}, have18 orbits. The40 fully live triples have12 orbits, represented by

    r in{0,1}, s in{0,1,2}, ell in{0,5}.              (R4)

The [independent orbit enumeration](../../frontier/cover-geometry/fixed_low_pure_square7_role_orbits_independent.py) and its [exact census](../../frontier/cover-geometry/fixed_low_pure_square7_role_orbits_independent.json) check these counts from explicit generators. The published triple(1,2,5) belongs to exactly the four-element live orbitR1. Symmetry alone therefore does not release the other eleven live orbits.

Because665 covers every labelled9qs layout, transporting such a layout along with the other original data stays within its quantified class. Transport the certified whole95-field family and its selectors back by the same bijection. Both priorities, every full-height query and every final budget are preserved. This provesR1 without repeating665's enumeration.

If a square7 original is absent or its central component is already killed by the fixed low pure deletions, its actual activation is zero on all source-live cells. It can be padded by a fixed allowed live role and the actual coordinate measure thinned to that conservative mass. For theR1 branch, the present slots may thus use3*7² central residues{1,2}mod3,5*7² residues{2,3,4}mod5, and9*7² residues{1,2,4,5,7,8}mod9. The additional residues beyondR1 are source-null, rather than further live orbits.

## 2. A variable-unary transport theorem

The comparison in663§7 fixed unary masses while increasing edge caps. The same argument permits smaller virtual unary masses, with an explicit query factor that cannot be omitted.

LetZ_q andZ_q^+ be positive actual and virtual unary comparison masses, and beta_e,beta_e^+ nonnegative edge caps satisfying

    Z_q^+<=Z_q, beta_e<=beta_e^+.

Write x_e=beta_e/(Z_q Z_s) and x_e^+=beta_e^+/(Z_q^+ Z_s^+). Assume every induced independence polynomial of the line graph at x^+ is strictly positive. Then x<=x^+, and the actual point is in the same strict region. For physical queried coordinates T, let E_T be the edges having neither endpoint in T. The matching responses obey

    H_T/H_empty
       = [product_(q in T) 1/Z_q]
          p_(E_T)(x)/p_E(x).                         (R5)

The already verified deletion-ratio monotonicity makes the polynomial ratio nondecreasing from x to x^+. The inverse-unary prefactor also increases becauseZ^+<=Z. Hence

    H_T/H_empty <= H_T^+/H_empty^+ for every T.       (R6)

The positive factors in H_empty=(product_q Z_q)p_E(x) both decrease when passing to the virtual table, so0<H_empty^+<=H_empty. Therefore

    r=H_empty^+/H_empty in(0,1],
    r H_empty=H_empty^+,
    r H_T<=H_T^+ simultaneously for everyT.          (R7)

At each central cell multiply the entire virtual95-field family by this one r. It is independent of the weak markers, so both priority inequalities are preserved. The mass term agrees with the virtual mass; every nonnegative query expression and its actual menu maximum is no larger. Thus the actual complete gate is at least the virtual gate. The same actual higher-pure source transport and network construction then apply.

This is a sufficient transport condition. It still requires a positive complete gate for the virtual table. Taking a pointwise minimum of several unary tables does not supply that gate.

## 3. The fixed unary table is not pointwise worst

Compare the published tripleB=(1,2,5) withA=(0,2,5). Keep one globally fixed9qs layout and every other numerical datum unchanged. At the live unmasked cells(l,m)=(0,6) and(4,6), the square7 masses satisfy

    Z7^A(0,6)=169/210 <5/6=Z7^B(0,6),
    Z7^A(4,6)=5/6 >169/210=Z7^B(4,6).             (R8)

The tables cross. No single pointwise direction permits replacingA byB throughR7. These cells compare the same two globally fixed triples; they do not choose a different phase independently in each row.

The same crossing can be seen in actual unary survivor submeasures before pair conditioning. Take the7 pure first root0, auxiliary root cut1 and no deeper pure deletion, and use distinct live49-prefixes5,6,12 for the three square7 originals. The root-cut law has mass5/6, and each such49-prefix has mass1/42. Changing only the3*7² central role from1 to0 removes one extra actual prefix at the first cell and restores it at the second. The actual unary masses are17/21 versus5/6 at the first cell and5/6 versus17/21 at the second; these are distinct from the conservative comparison masses169/210 and5/6 inR8. This supplies an actual pair behind the numerical crossing; it is not a claim of nonexistence of a complete covering survivor.

Crossing does not settle which triple has the worst optimized complete gate. It rules out this pointwise transport shortcut and the assertion that all roles follow from one rooted symmetry orbit.

## 4. Three exact failures of the original four-field library

Fix all ten9qs roles atleaf5 and the weak corner(i,j)=(4,15). Keep the complete512 coefficients, including the four guarded9q² additions, and the exact four field families from663. For each family evaluate every literal selector in every menu. The complete head gate is

    G=g sum_c w_l v_m theta(c)H_empty(c)
       -sum_(mode,T)C_(mode,T)
            max_(x,y in that menu)sum_c x_l y_m theta(c)H_T(c). (R9)

The square7 unary mass is

    Z7(l,m)=5/6-[1_(l//3=r)+1_(m//5=s)+1_(l=ell)]/35.

The three tests change the roles to(1,1,5), to(0,1,5), or replaceZ7 everywhere by157/210. All ten beta increments remain attached toleaf5 in each test. The other unary masses, the source mask, field values and complete fees are unchanged.

At this one fixed corner the largest gate among the four supplied fields is strictly negative in each test:

| Tested configuration | Maximum over the four fields at the fixed corner |
| --- | ---: |
| Roles(1,1,5) | -0.00022750755788589024... |
| Roles(0,1,5) | -0.000896233343045682... |
| UniformZ7=157/210 | -0.010568694592855275... |

The first value is exactly

    -28207901679337221607975838179801
       /123986657592647139816426700800000000.        (R10)

For every one of the four families its uniform minimum is no larger than its gate at this common corner. Therefore

    max_(four families) min_(corners) G
      <=max_(four families)G(4,15)<0.               (R11)

This refutes direct reuse of these four witnesses. It does not refute a new compatible field, another source, a different envelope, or the original arithmetic problem. In particular the next section constructs a new positive field for the first failing configuration.

The exact [small certificate](../../frontier/cover-geometry/square7_role_small_gate_certificate.py) and [data](../../frontier/cover-geometry/square7_role_small_gate_certificate.json) contain the16 rational values: three failures plus the baseline control, each with four fields. They account for all559 literal selectors and512 charged modes per field and pass9,495 explicit checks. The broader diagnostic scan is not needed to establishR11.

## 5. A new whole priority family repairs the column1 branch

For roles(1,1,5) with all live9qs roles atleaf5, take the supplied180 integer field coefficients with common denominator2^20. They define all95 weak-corner fields through the same orbit map as663. Each field is between zero and one. The exact certificate directly checks on every live cell, for all95 weak corners,

    theta^(l,j)(l,m)>=theta^(i,j)(l,m),
    theta^(i,m)(l,m)>=theta^(i,j)(l,m).              (R12)

It then evaluates the complete gate at every one of the95 corners, retaining all559 literal selectors and every512 coefficient. The exact minimum is

    236786401859826794742637401832211
      /108488325393566247339373363200000000
       =0.0021825979984558697... >193/100000.        (R13)

It is attained at weak corner(0,6). These are simultaneous fields from one fixed family, rather than separate optimizers for each gate or query.

The actual conditional source has the same matching form as665, with the new column1 unary table. The same uniform strict region applies because its decrement still lies between0 and3 and all edge caps remain bounded byb_e+d_e. Every original phase is fixed globally. The field is chosen once for that complete family. Both prioritiesR12 allow645 to transport the uniform bound to the actual pure3/pure5 survivor laws after arbitrary finite higher pure deletions. Every full-height query remains included.

Use the conservative public lower bound193/100000. All other actual-source domination, omitted-coordinate bounds, full-Haar caps and reconstruction interfaces are unchanged. Thus666's complete finite row policy, generic five-parent tail, TypeI charge, arbitrary-parent tail and final private/CRT gluing apply directly. No new network cost is omitted or borrowed from a different law.

The [new field certificate](../../frontier/cover-geometry/square7_column1_priority_field_certificate.py) and [exact data](../../frontier/cover-geometry/square7_column1_priority_field_certificate.json) retain only the final180-entry rational witness, its exact95 corner gates, input pins and complete-budget checks. They pass64,537 explicit checks. The stored policy margins concern658's three-parent budget; the666 extension uses the unchanged interface and the common lower bound193/100000. Reproduce both finite calculations with:

```sh
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/square7_role_small_gate_certificate.py
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/square7_column1_priority_field_certificate.py
```

The programs accept --directory and --output. The separately authored [small-case verifier](../../frontier/cover-geometry/square7_role_small_gate_independent.py) and [result](../../frontier/cover-geometry/square7_role_small_gate_independent.json) reconstruct all16 gates with matching deletion--contraction and pass90,781 checks. The [positive-field verifier](../../frontier/cover-geometry/square7_column1_priority_field_independent.py) and [result](../../frontier/cover-geometry/square7_column1_priority_field_independent.json) independently reconstruct all95 gates,53,105 literal selectors,48,640 complete screens and both priority inequalities. Its121,112 checks reproduceR13 exactly. Neither independent mathematical implementation reads or imports the corresponding producer implementation; they read the pinned canonical inputs and the proposed witness data.

```sh
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/square7_role_small_gate_independent.py
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/square7_column1_priority_field_independent.py
```

All five retained programs, including the orbit census, were rerun from repository sources with exit code0; the independent exact values agree with the producer results. A separate ordinary-mathematical review checked the role transport, variable-unary ratio, new actual-source construction and666 continuation. These are independent implementations and review within the same agent/model family, not a claim of independent model families or Lean verification.

If some9qs originals are inactive or absent, complete their virtual roles byleaf5 and use665's beta comparison orR7. If the9*7² role and every active9qs role are insteadleaf4, swap the two live leaves in root1 through the whole problem and transport back. Neither step introduces independent new choices on different corners.

The new column1 branch does not certify mixed live9qs layouts. Releasing the remaining role orbits while retaining665's entire arbitrary9qs domain still requires a compatible common field or another lawful response bound for those joint configurations. The finite countercases identify exactly why the old four-item library alone does not discharge that obligation.
