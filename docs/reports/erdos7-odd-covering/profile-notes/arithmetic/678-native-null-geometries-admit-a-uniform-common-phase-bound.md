# Native null geometries admit a uniform common-phase bound

For each of the three relative square-null geometries(0,5),(3,0),(0,0), the constant-one comparison field has a complete gate greater than1/100 for every live square7 role and every globally fixed assignment of the ten labelled9qs central leaves. The proof uses one analytic lower estimate over all5^10 layouts; it does not enumerate those layouts. The actual source, root selectors and central15 mask are rebuilt on the native nonnull leaves.

Together with677's complete40-role theorem for the remaining null geometry(3,5), this removes the prescribed pure3/pure5 residues and central15 residue from that conditional head theorem. All actual pure3 and pure5 originals may have arbitrary globally fixed phases at arbitrary finite heights. An actual15 event is used when its first-root endpoints are live; otherwise one auxiliary live rectangle is chosen once. After lawful rooted normalization, the four relative null geometries exhaust all cases. The uniform gate retained across all four is193/100000, since the old(3,5) geometry keeps its already certified lower bound.

The literal head primes and the fifty retained incidence slots remain: every active cq, c in{3,5,15,9,25,45,75,225}, has its q-root in the same chosen table, and every active qs has at least one endpoint in that table. These conditions must hold on every ACTUAL unmasked nonnull central cell, including the extra cells present in the new geometries. A table valid only on the old80-cell support is insufficient. The original residues are globally fixed and the table is independent of every comparison corner, query and continuation branch.

The other mixed-label, actual outside-source, ordinary-domain, private-interface, assignment and owner-parent hypotheses remain. This is not an unrestricted odd-covering theorem. No new Lean verification is asserted. No stronger owner cutoff or density is inferred from the larger gate in the three new cases.

## 1. Native sources and selectors

Use root-major labels l=0,...,5 at3 and m=0,...,19 at5, after deleting one actual or auxiliary first root in each coordinate. Let z3 and z5 be the actual or auxiliary square-null labels. Put

    I={0,...,5} minus{z3}, J={0,...,19} minus{z5},
    LIVE={(l,m) in I x J:not(l<3 and m<5)}.       (N1)

The actual root tests are l//3 and m//5. No crossing of first-root sets is hidden in a relabelled old query menu.

| Relative nulls | Nonnull ternary leaves | Actual unmasked cells |
| --- | --- | ---: |
| (0,5) |1,2,3,4,5|85|
| (3,0) |0,1,2,4,5|83|
| (0,0) |1,2,3,4,5|87|

For each weak marker i in I, the corner weights are1/9 at i and2/9 at every other nonnull ternary leaf. For j in J they are3/75 at j and4/75 elsewhere. A null leaf has weight zero.

The four ternary selectors are the whole weight vector, its restriction to each ACTUAL first root, its restriction to a nonnull singleton, and the constant deep-cap singleton delta_l. The four quinary selectors use the corresponding actual roots and the deep cap(4/5)delta_m. Their sixteen tensor-product menus contain559 literal selectors per corner. These deep selectors are not multiplied by the weak leaf's weight again.

The complete512 nonnegative charges are the pinned640 coefficients with the four guarded9q² additions from the current matching interface. The source coefficient remains g=200163067/201247200. No original-height or query-height term is truncated, and no fee is omitted when a new null location changes an old root screen.

## 2. A single common matching table for each actual family

Let Q={7,11,13,17,19} and let E be its ten numerically labelled pair edges. Set

    r_q=1/(q-1), a_q=1/[q(q-2)],
    b_qs=a_q r_s+r_q a_s, d_qs=r_q r_s.           (N2)

A live square7 role is(r,s,ell) in{0,1} x{0,1,2,3} x I. At a central cell,

    n(l,m)=1_(l//3=r)+1_(m//5=s)+1_(l=ell),
    Z7(n)=5/6-n/35,
    Zq=(q-2)/(q-1)-2a_q for q>7.                 (N3)

Every n is between0 and3. For one globally fixed layout alpha:E→I,

    beta_e(l,m)=b_e+d_e 1_(l=alpha_e).            (N4)

All original endpoints and higher residues remain fixed. The retained table must absorb the fifty constrained originals on the actual LIVE domain. The existing local conditional matching construction then supplies one actual outside submeasure at each cell and all32 simultaneous response envelopes. It applies because the inherited uniform all-induced strict region holds over all n in0..3 and beta_e<=b_e+d_e.

For queried support T, its matching response is

    H_T(n,beta)=sum_(matchings M on Q\T)(-1)^|M|
         product_(e in M)beta_e
         product_(q in Q\(T union vertices(M)))Zq. (N5)

The empty response is the selected source mass; other responses are simultaneous query upper envelopes. They are not separately chosen laws or separately attained optima. All grids vanish on the common15 mask.

## 3. Keep one leaf per original before bounding the source

The matching degree on five vertices is at most two. Write B(n)=H_empty(n,b). The exact source expansion is

    H_empty(n,beta)=B(n)
      +sum_e L_e(n)1_(l=alpha_e)
      +sum_(e<f, e and f disjoint)d_e d_f
          product_(q outside e union f)Zq
          1_(l=alpha_e=alpha_f),                 (N6)

where

    L_e(n)=d_e[-product_(q outside e)Zq
       +sum_(f disjoint e)b_f
           product_(q outside e union f)Zq].     (N7)

Every quadratic summand inN6 is nonnegative. For one weak corner(i,j), let w_l and v_m denote its weights and put

    S0=sum_LIVE w_l v_m B(n(l,m)),
    A_e(t)=sum_(m:(t,m)in LIVE)w_t v_m L_e(n(t,m)). (N8)

For every actual layout alpha,

    sourceMass(alpha)
      >=S0+sum_e A_e(alpha_e)
      >=S0+sum_e min_(t in I) A_e(t)=S_lower.     (N9)

The minimization is performed after summing all cells having the same leaf. Each numerical original still has one globally chosen alpha_e. Replacing every cell independently by an all-edges-active worst case would discard that relation and is not the estimateN9.

The ten minima may be recorded as an auxiliary minimizing tuple, but this does not change the original family's phases.N9 holds for EVERY fixed alpha. The minimizing tuple may differ with the weak corner because each displayed inequality is separately valid for every actual layout; no fictitious corner-dependent family is subsequently realized.

## 4. A simultaneous upper envelope for all query fees

For an induced edge e inside Q\T,

    derivative_(beta_e)H_T
      =-product_(q outside T union e)Zq
         [1-sum_(f disjoint e, f inside Q\T)
                    beta_f/(Z_f1 Z_f2)].         (N10)

The bracket is positive under the inherited all-induced strict-region bound. A queried edge has derivative zero. Consequently, throughout the allowed box,

    H_T(n,b+d*activation)<=H_T(n,b)               (N11)

for all32 supports at once. The upper envelope on the right uses the same actual n(l,m) and native mask, rather than an old response table with different nulls.

For every one of the512 fee modes, evaluate its complete actual selector maximum on this nonnegative baseline response. Call the resulting total F_upper. All coefficients are nonnegative, so the actual complete fee is at most F_upper. CombiningN9 andN11 gives

    G_alpha(unit)>=g S_lower-F_upper=G_lower      (N12)

for every fixed alpha. Neither bound requires the source minimum and fee maximum to be jointly attained. They are a lower and an upper comparison applying simultaneously to the same actual layout, so their subtraction has the stated lower-bound direction.

## 5. Complete finite checks of the uniform estimate

There are forty live roles for each native null geometry and95 weak corners for each role. The producer uses only lawful root-preserving symmetries: it keeps both ternary row roles, takes one representative square leaf per actual first root, and distinguishes the quinary null column from the central15 column. It checks12,8,8 role representatives for the three geometries. All five weak ternary markers remain separate; within each actual quinary column a nonnull representative gives20 corners with multiplicities summing to95.

The independent implementation directly checks all forty roles and twenty quinary-reduced corners in each geometry, avoiding the producer's role quotient. Numerical edge identities are never permuted or sorted.

The exact uniform lower bounds are:

| Nulls | Minimum lower comparison | Decimal shown only for orientation |
| --- | --- | ---: |
| (0,5) |4825545038369589069237127/268733843007189880320000000|0.017956595955204956|
| (3,0) |1047576905711276274236418259/103462529557768103923200000000|0.010125181649713714|
| (0,0) |43799151849208753837946671/1591731223965663137280000000|0.027516675673476383|

Each rational value is strictly greater than1/100. These are minima of the conservative analytic comparisonN12, not claimed exact or optimized minima of the actual complete gate.

The portable [producer](../../frontier/cover-geometry/new_null_uniform_linear_bound.py) and [result](../../frontier/cover-geometry/new_null_uniform_linear_bound.json) retain the complete exact comparison. The producer passes378,884 explicit checks, including all560 reduced corner lower bounds strictly above1/100. Its only numerical input is the pinned `remaining33_global_root_exclusion_certificate.json`; the SHA256 is `dd00b0e9f6a93713d437394c368a4ef81407cabd5ecb931ae7753926d9d8c3e4`.

The independently written [checker](../../frontier/cover-geometry/new_null_uniform_affine_bound_independent.py) and [result](../../frontier/cover-geometry/new_null_uniform_affine_bound_independent.json) pass1,514,356 checks. It directly covers all2,400 corners from40 roles x20 corners x3 geometries, checks1,341,600 literal selectors and1,228,800 support modes, and obtains the same three exact minima. The independent implementation receives the numerical contract and result rather than the producer implementation. Both canonical entrypoints reproduce their retained results byte for byte. These checks audit finite exact arithmetic; they do not supply a Lean proof.

```sh
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/new_null_uniform_linear_bound.py
python3 -I -S -B -O docs/reports/erdos7-odd-covering/frontier/cover-geometry/new_null_uniform_affine_bound_independent.py
```

The finite program checks the rational coefficient, response and selector arithmetic. The universal layout quantifier follows fromN6–N12; the unbounded finite-height query statement follows from the inherited actual-source density caps and complete coefficient sums. No finite layout scan is being used as a cutoff on original heights.

## 6. Actual pure phases and legal central15 normalization

First construct the actual pure sources as in624. Delete the actual pure3 root, or one auxiliary root if absent. Delete the actual pure9 leaf if live, or one auxiliary live leaf if it is absent or already killed by the first deletion. All higher pure3 originals together have Haar cost at most1/18, leaving at least1/2. The actual surviving leaves admit one normalized law rho3<=2H3 with nonnull leaf masses at most2/9. Similarly, the higher pure5 tail costs at most1/100; the actual pure5 survivor has mass at least3/4 and admits rho5<=(4/3)H5 with nonnull leaf masses at most4/75.

These are laws on the actual surviving higher cylinders, before numerical corners are introduced. Their leaf-mass vectors decompose into convex combinations of the five and nineteen weak corners. Numerical extreme corners need not themselves be realizable as actual higher-digit laws.

If the actual15 event has both endpoints in live first roots, retain that rectangle. If it is absent or already source-null, impose one fixed live auxiliary rectangle. Use rooted first-digit permutations to send this selected rectangle to(row0,column0) and the deleted first roots to2 and4. Permute leaves only within their current first roots. The ternary square null becomes0 or3 according to its root relative to the chosen rectangle; the quinary square null becomes0 or5. These four possibilities are exhaustive.

This uses rooted-tree bijections preserving all cylinder heights and numerical modulus identities. It does not use the cross-root square-leaf exchanges from675 as though they preserved first-root selectors. All original phases, root-incidence tests, source laws and queries are transported by the same bijection. A source-null original15 remains avoided; the auxiliary rectangle is a source restriction and is not counted as a second numerical original.

At the three new native geometries, theta=1 on LIVE satisfies both weak-marker priorities as equalities. Attach the same actual matching outside table to the actual pure source. The separate-concavity or645 priority argument transfers the uniform corner boundN12 to that one actual source for every finite query depth. At null geometry(3,5), use677's complete40-role whole-field certificate and its already checked priority transport.

If a pair or square7 original is absent or source-null, use the existing global per-original virtual-role padding and actual matching-ratio thinning. The comparison role is fixed once for the original, not chosen by later corners or queries. It does not assign a second phase to an actual numerical modulus. These comparisons retain the same all-induced strict domain and full-height query interfaces.

Thus arbitrary actual central pure phases and arbitrary central15 phase reduce to one of four native source cases, and every case has the common complete lower gate193/100000. The proof keeps the actual finite family and its joint law throughout.

## 7. What is released and what remains

The prescribed3/9/5/25 phases and central15 phase are no longer needed in this conditional class. Every actual central pure original still occurs at most once per numerical modulus and has one fixed phase; finite exponent heights have no common imposed maximum. The live square7 role and all ten9qs central leaves are unrestricted after normalization.

The fifty actual-domain incidence conditions, fixed head primes, complete remaining mixed-label inventory and ordinary/private interfaces remain. The source construction is still conditional on one table satisfying them throughout the actual support. The argument does not show that an arbitrary odd congruence family has such a table.

The inherited Haar domination and simultaneous query caps survive native rebuilding and rooted transport. They allow the already stated continuation policies to be applied at the unchanged uniform threshold, under all their existing assumptions. The larger separate lower bounds in the three new cases are not used here to claim new parent counts, earlier switches or stronger global density.
