[Index](../../marked_head_profile.md) · [Saturated source geometry](../72-a-linear-gap-on-the-entire-controlling-beta-face.md) · [Complete extra deletions](../109-the-mean-and-all-hinges-share-one-original-test.md) · [Common survivor](216-the-survivor-and-extra-deletions-share-the-complete-head.md) · [Complete52-cost comparison](218-the-retained-deletions-control-sixteen-complete-tests.md)

# Deep-five deletion retains the selected observation masks

On both complete actual saturated K faces, the extra deep-five
deletion and the actual survivor can be measured on the same four
selected-event masks. The deletion has exact ternary marginals
which its coarse cell totals alone do not express. Retaining those
marginals gives a1275-variable containing LP for every independent
head, with two exhaustive alternatives for the original27 and81
test residues.

The complete52-cost comparison is

    K<=618246942346120808640571404067136947898347196125297
       /1507060000923219778242080876538947936501946649680
      =410.2337942533043... .

It improves218's410.3347087952207... by0.1009145419164...,
on the same two saturated faces. The comparison remains above403.

The domain remains r=rho=0, raw mass1/4 and surviving mass53/360.
All infinite exponent tails and every original independent test
label remain. This is an ordinary source theorem with exact rational
dual certificates; it does not assert actual-family attainment,
a common optimizer across independent tests, an off-face or global
extension, Lean verification, or unrestricted Erdos7 resolution.

## 1. The complete deep-five deletion has a product factorization

Use72's canonical orientation, with thinned cell0 and

    eta=(1/18,1/9,1/9,1/9,1/9),
    ROOT=(0,0,1,1,1), slots=(P,A,B,Q,H).

Let E5 denote the complete projected virtual deletion of the
forbidden old-cofactor families5^b and3*5^b, b>=2, at every positive
seven depth. Saturation makes the actual deleted measure equal to
the entire virtual deleted measure. Every individual cofactor cap
is saturated: its nonnegative gap belongs to a convergent sum of
nonnegative gaps whose total is zero. Thus its gap is zero.

For a pure-five cylinder J of depth b>=2,

    Lambda<=eta tensor Haar5,
    Lambda(X3 times J)=eta(X3)*5^-b.

The nonnegative difference between these measures has zero mass on
X3 times J, so it vanishes there as a measure. Consequently

    Lambda restricted to(X3 times J)
      =eta tensor(Haar5 restricted to J).            (DM1)

The3*5^b family has the same conclusion on root1. That root is
forced by its larger ternary mass1/3>1/6. Both statements hold for
arbitrary further measurable observations, not only total masses.

These source-free five cylinders cannot lie in P,A or B. The source
first5 label removes P everywhere; source15 removes A on root1;
and source45 removes B in its first-beta cell L, whose eta mass is
positive. Each forbidden location loses positive product mass and
therefore fails the individual cap equality. The remaining first
slots are Q,H. This is a support condition; it does not assert that
every descendant of Q is source-free.

For the independently chosen original cylinders J_(b,e), sum(DM1)
with the full positive-seven weights u_e, where sum_(e>=1)u_e=1/5.
There are finite positive five-coordinate measures nu_p,nu_a such
that

    E5=eta tensor nu_p+(eta restricted to root1) tensor nu_a,
    nu_p(1)=nu_a(1)=(1/5)*sum_(b>=2)5^-b=1/100,
    support(nu_p),support(nu_a) subset Q union H.       (DM2)

The two families have distinct original labels. No common residue,
nesting, or disjointness of their projected five cylinders is
required. Positivity and the complete geometric sums justify all
countable additions and restrictions. Profile75's finite-prefix
and labelwise diagonal argument retains the same limits for
families approaching the saturated face.

## 2. One actual deletion measure shares the selected masks

Keep216's variables x_(c,s,M) and y_(c,s,M) for the raw and surviving
mass in cell c, slot s and membership mask M of the original tests
25,27,75,81. Add

    z_(c,s,M)=E5(cell c times slot s intersect mask M). (DM3)

The400 new variables are nonnegative, and their coarse sums are
the existing deep-five deletion variables:

    sum_M z_(c,s,M)=e5_(c,s).                         (DM4)

The density w already subtracts only the four forbidden cofactor
families3,9,5,15. E5 consists of other original labels. Equality of
the actual and virtual deleted measures at saturation therefore
implies, as measures,

    E5<=w*Lambda-mu,
    y_(c,s,M)+z_(c,s,M)<=w_(c,s)*x_(c,s,M).           (DM5)

The projected supports may overlap; (DM5) does not assume otherwise.
The coarse pure-three deletion constraints and216's common
e3+e5 residual inequalities remain. There is still only one actual
survivor and one actual E5 measure.

The source-free H geometry also gives five equalities

    sum_M x_(c,H,M)=eta_c/5,                         (DM6)

and(DM2) gives e5_(c,s)=0 for s=P,A,B. Neither adds a new deletion
credit to the objective.

## 3. Eight exact marginals retain the unthinned ternary structure

Write

    Mc=E5(cell c times X5)=eta_c*(1+ROOT(c))/100.

On cells c=1,2,3,4, the ternary measure eta is exactly Haar: all
pure-three source deficit is in cell0. A selected original27
cylinder with parent c has eta mass eta_c/3. A selected81 cylinder
with parent c has mass eta_c/9. Equation(DM2) gives

    E5(selected27 in parent c)=Mc/3,
    E5(selected81 in parent c)=Mc/9.                 (DM7)

The fine-five source restrictions cannot alter these ratios,
because(DM2) is a product-measure statement. No such Haar ratio
is asserted in the thinned cell0.

Retain the original independent projection-mixture coordinates
lambda27(c),lambda81(c) from209. For an actual event of positive
raw-source mass, these are point masses at its own parent cell. The eight new
linear equalities are

    sum_(s,M:27 in M) z_(c,s,M)=(Mc/3)*lambda27(c),
    sum_(s,M:81 in M) z_(c,s,M)=(Mc/9)*lambda81(c),
    c=1,2,3,4.                                     (DM8)

Allowing mixture coordinates enlarges the set of actual point-mass
configurations, as in209. It does not identify different original
residues or assume that arbitrary mixture points are realizable.

Absent and source-null events require care. Their marked raw,
surviving and E5 masses are all zero. Assign that event's projection
coordinate to cell0. All new equations(DM8) then have zero right
sides, and every original raw selected-profile constraint remains
valid because its left side is zero and its cap is nonnegative.
This convention does not change the actual event or force a new
positive selected mass. Actual events of positive raw-source mass
in cell0 likewise retain their original cap constraints, without
a new Haar equation.

## 4. Both original27/81 alternatives remain

Two ternary cylinders of depths3 and4 are either nested or disjoint.
For the original independently chosen tests, exactly the following
containing alternatives suffice:

    Nested: selected81 subset selected27;
    Disjoint: selected81 intersect selected27 is empty. (DM9)

In the nested branch, raw masks with bit81=1 and bit27=0 have zero
mass. In the disjoint branch, raw masks containing both have zero
mass. Each is one nonnegative zero-sum inequality across all25
rectangles. The survivor and E5 mask masses inherit these zeros
through(DM5). Empty events satisfy an appropriate alternative too.

The two branches are bounded separately and their upper bounds are
combined by a maximum. Choosing only the cheaper branch would not
bound every original configuration. No analogous equality is
imposed on the independent25 and75 residues in this LP.

Together with all216 constraints, each branch contains

    400 raw+400 survivor+25 projection+50 coarse deletion
       +400 E5 mask variables=1275 variables,
    581+15+400+1=997 inequalities,
    16+5+25+8=54 equalities.                         (DM10)

The second K orientation is transported by first relabelling its
thinned cell as0. All raw, survivor, deletion, projection and test
coordinates move together. Root1 permutations similarly move the
first-beta cell to the canonical L=2. Thus neither the second face
nor any beta or late allocation has been dropped.

## 5. Complete hinges and independent heavy-cost scans

Retain218's original head B and its selected-label policy

    v_1=B, v_t=B+|M| for2<=t<=8.

For a nonnegative coefficient vector a, the objective is unchanged:

    J_a=sum_(c,s,M)sum_t a_t*
        [y_(c,s,M)*(v_t-t)_+ +x_(c,s,M)*g_t(v_t)].    (DM11)

Here g_t is the complete two-depth seven bridge with the original
independent21,35,63,105,147,245 projections. The new deletion
variables constrain the actual y; no extra credit is subtracted
from(DM11), from its raw seven increment, or from an omitted tail.

If U_a bounds(DM11) in both branches, the unchanged complete tail
formula is

    integral_mu sum_t a_t*(A-t)_+
      <=U_a+a1*(163/1800+2669/88200)
              +sum_(t>=2)a_t*(19/648+2669/88200).     (DM12)

The original heavy functions at indices0 and16 have exact finite
hinge expansions and an eventual affine tail. Their original
f(1)*53/360 terms are added after(DM12). Finite transitions, total
slope and eventual constant are checked exactly, so no positive
integer load or original exponent tail is omitted.

For each heavy function the scan covers12,500 shallow heads,
10 first projection pairs,50 next projection combinations and10
second-depth pairs: all62,500,000 original containing choices.
The existing complete two-, four-, six-projection and209 candidates
remain valid pruning alternatives. Their one external deletion
payment uses218's full-hinge credit; no credit is subtracted from
the new retained-survivor LP.

Every expanded leaf uses the maximum of the nested and disjoint
rational dual bounds. Every omitted subtree has a complete upper
at most the final scan maximum. The exact branch-count identity
and deterministic decision digest cover all original choices,
including the separately accounted seed. Each heavy test takes
its own maximum; their maximizing residues need not agree.

## 6. Rational certification and complete consumption

For each branch write its containing system as Ax<=b, Ex=d, x>=0.
The certificate contains nonnegative inequality prices u and free
equality prices v satisfying all1275 column inequalities

    A^T u+E^T v>=objective.

Weak duality gives the exact signed upper b.u+d.v. The canonical
checker uses only the Python standard library. A common positive
matrix denominator and a common positive objective/price denominator
turn every check into an integer inequality. All997 inequality
prices,54 equality prices and1275 columns are retained; no numeric
tolerance or optimizer status substitutes for this check.

The numerical proposal program repairs coarse deletion columns,
then mask-link, survivor, projection and raw-cap columns in that
order. Mask-link corrections can reduce raw slack; the final raw
cap repair pays for this effect before every exact column is checked.

The consumer starts from218's entire52-cost vector and full survival
denominator. It substitutes the two complete heavy bounds, applies
the existing whole-load majorants and reconstructs the signed
numerator once. All218 AP11 blocks, the independent AP13 bound,
integer-identity improvements, actual negative mass coefficient,
complete square and full count tail remain. A local controller
value alone never enters this complete comparison.

The [helper](../../frontier/transport/selected_deletion_mask_heavy_comparison.py),
[proposal program](../../frontier/transport/propose_selected_deletion_mask_duals.py)
and [certificate](../../certificates/source_norms/selected_deletion_mask_heavy_comparison.json)
retain the exact branch systems, independent scans, rational prices
and the complete52-cost consumer.

```sh
python3 -I -O docs/reports/erdos7-odd-covering/frontier/transport/selected_deletion_mask_heavy_comparison.py --check
```

## 7. Exact complete results

The independent heavy scans give

    cost0<=7256382769550121973275789070012621819
           /1284172013773522415341800000000000000
          =5.650631451021376...;
    layout=(1,4,2,1,2,4,2),
    projections=(1,2,4,1,2,1,2).

    cost16<=1339584928485414107234572986321358144027
            /296643735181683677943955800000000000000
           =4.515803873845393...;
    layout=(1,3,2,1,2,3,2),
    projections=(1,2,3,1,2,1,2).

These tuples maximize the certified containing operators. They are
not asserted to be actual-family maximizers. Each result covers
every original head and projection choice, rather than just the
displayed tuple.

The scans use1,141,073 and1,158,043 exact old capacity LPs,
respectively. Each uses65 applications of the two-branch operator
and128 distinct branch duals. All256 stored rational duals are
checked in every1275 column. The total branch accounting is
62,500,000 choices for each independent cost, including all safely
pruned alternatives.

Only original indices0 and16 change in218's final52-cost vector.
The resulting numerator is

    N=59607500005355479473386126459840354042194762549
       /1798918804638173427134854520592000000000000000,

with decrease

    2410502422219569597289632288901673143
       /280163527671590140280402700000000000000.

The full218 denominator remains

    d=32368969848248957388587/379652842800000000000000.

The complete ratio uses the same offset185694867601/8599322160.
Its exact improvement over218 is

    9642009688878278389158529155606692572
       /95546286053315059514437920657885569307.

Both costs enter this one signed numerator. No improvement is then
subtracted a second time from the ratio or its denominator.
