[Index](../../marked_head_profile.md) · [One retained original135 test](223-the-original135-test-enters-the-complete-heavy-bridge.md) · [Complete survival denominator](224-the-retained135-test-strengthens-two-complete-survival-bounds.md) · [Actual deletion masks](222-deep-five-deletion-retains-the-selected-observation-masks.md)

# Two original tests share the complete retained bridge

The original135 and125 tests can be retained together on the
same raw-source and actual-survivor measure. Their four joint
membership states distinguish the shared intersection from the
two individual events. This preserves the full nonlinear hinge
and positive-seven increment when both indicators occur.

The complete52-cost comparison is

    K<=305066797349268271231540792232070420001120036055951
        /758117209355329672087641395540731792073789603280
       =402.400570234626356519...<403.                (P0)

This improves224's404.482723848015664131... by
2.082153613389307612... . The strict margin below403 is
0.599429765373643480... . These numbers concern the complete
face expression with its original offset and denominator.

The domain is both complete actual saturated K faces, r=rho=0,
with raw mass1/4 and surviving mass53/360. Each original test
keeps its own residue. All exponent tails,52 independent costs,
signed actual mass and224's complete survival denominator remain.
The containing LP need not be realizable point by point. No
off-face/global extension, Lean verification or unrestricted
Erdos7 resolution is asserted.

## 1. Four membership states retain the joint observation

Use222's old rectangle/four-selected-test masks C_(c,s,M), with
raw measure Lambda, actual survivor mu and retained density w.
Let J135 and J125 be the independently chosen original135 and125
test cylinders. The three explicit marked events are

    S0=J135 minus J125, S1=J125 minus J135,
    S2=J135 intersect J125.

The remaining event S3 is the complement of J135 union J125. For
i=0,1,2 define

    u_i(c,s,M)=Lambda(C_(c,s,M) intersect Si),
    v_i(c,s,M)=mu(C_(c,s,M) intersect Si).             (P1)

They are nonnegative. Since the Si are disjoint and mu<=w*Lambda,
the following constraints hold on each old mask:

    sum_i u_i<=x, sum_i v_i<=y,
    v_i<=w*u_i for i=0,1,2,
    y-sum_i v_i<=w*(x-sum_i u_i).                    (P2)

The last inequality retains the unmarked complement as well.
It does not follow by freely choosing three independent pairs
of marginal measures; here all six measures come from the same
actual partition. The original raw, survivor and E3/E5 mask
constraints remain. Not refining E3/E5 further by S_i enlarges
the containing model and imposes no extra deletion payment.

## 2. The two independent residue profiles have different caps

For135, use223's normalized first-slot table pre_(c,s). Its
raw cylinder cap is pre_(c,s)/27 in the actual coarse ternary
cell and first-five slot. Introduce a separate25-element profile
lambda135 and require

    sum_(c,s)lambda135_(c,s)=1,
    sum_M(u_0+u_2)(c,s,M)
      <=lambda135_(c,s)*pre_(c,s)/27.                (P3)

For125=5^3, the original descendant-five table d_(c,s) is eta_c
unless the entire slot is removed by the source5, source15 or
first-beta source45 label, in which case it is zero. It is a
ternary mass, so a depth3 five cylinder in slot s has raw cap
d_(c,s)/125. Introduce a separate five-element profile lambda125:

    sum_s lambda125_s=1,
    sum_M(u_1+u_2)(c,s,M)
      <=lambda125_s*d_(c,s)/125.                   (P4)

Thus lambda125 has one common first-five slot across all ternary
cells. It is not replaced by a separately chosen slot in each
cell. Neither(P3) nor(P4) identifies an original selected residue
with another. For actual positive-mass events choose their own
point profiles; absent/source-null events have zero marked
marginals and may choose any profile. Convexifying the profiles
only enlarges the set of actual configurations.

The surviving135 cap is1/135. Profile75(F27.9) gives the complete
pure-five cap(2/5)*5^-b for every original depth b>=2. At b=3,
the surviving125 cap is therefore2/625. Hence

    sum_(c,s,M)(v_0+v_2)<=1/135,
    sum_(c,s,M)(v_1+v_2)<=2/625.                    (P5)

These are valid constraints on the same actual survivor. They
do not add external mean/deletion credits to the objective.

## 3. The original CRT caps retain actual intersections

Raw domination by Haar3 times Haar5 gives, for independently
chosen original residue cylinders of moduli a and b,

    Lambda(J_a intersect J_b)<=1/lcm(a,b).

Incompatible residues have empty intersection. Applied to the
four original selected labels, the denominators are

| New label |25|27|75|81|
| ---: | ---: | ---: | ---: | ---: |
|135|675|135|675|405|
|125|125|3375|375|10125|

For the first row, sum u_0+u_2 over masks containing the relevant
old bit. For the second row, use u_1+u_2. The common new event
also satisfies

    sum_(c,s,M)u_2<=1/lcm(135,125)=1/3375.           (P6)

The same nested/disjoint alternatives for27/81 from222 remain
exhaustive. They are bounded separately, then combined by a
maximum. No additional residue compatibility is assumed for
125 or135. Every variable is at most1 because each measure
has total at most1 and every profile belongs to a simplex.
The independent unit-cap rows preserve the actual embedding.

Each branch has

    1275+1200 raw-state+1200 survivor-state+30 profile
      =3705 nonnegative variables,
    997+50+2400+2+8+1+3705=7163 inequalities,
    54+2=56 equalities.                             (P7)

## 4. The joint state contributes its true two-indicator increment

Keep the same original head B and six independent positive-seven
projections. At t>=2 set V=B+|M|; at t=1 keep the old unselected
head B. On the three explicit states, the newly retained count
is respectively

    n0=1, n1=1, n2=2.

For each t>=2 the exact additional retained integral is

    sum_(i=0..2)[u_i*(g_t(V+n_i)-g_t(V))
                 +v_i*((V+n_i-t)_+-(V-t)_+)].       (P8)

Here g_t is the original complete two-depth seven increment,
including all higher unit7 powers. Add(P8) to222's raw-increment
and actual-survivor hinge objective with the same nonnegative
heavy coefficients. The two-indicator term is evaluated directly;
it is not replaced by twice a one-indicator increment. No new
term is added at t=1. Profile and deletion coordinates retain
zero objective coefficients.

The maximum old load is now6+4+2=12. The compiler evaluates the
same exact g_t formula at12 and checks all loads1 through12.
For V>=t, g_t is constant while the survivor hinge is affine,
so no exponent cutoff or unchecked continuation is introduced.

## 5. Two distinct assigned summands leave the old tail

The old complete remainder19/648 contains the original135 cap
1/135 from the deep mixed category and the original125 cap
2/625 from the pure-five category. They are different original
labels. Moving both into the retained head at t>=2 leaves

    Rpair=19/648-1/135-2/625=7579/405000>0.          (P9)

The threshold1 remainder remains163/1800, and the complete
complementary positive-seven remainder remains2669/88200.
This subtracts the two designated summands from a nonnegative
upper-cap series, not upper estimates from unknown actual mass.
The constraints(P5) bound the now-retained contributions and
do not pay either tail credit a second time.

For each fixed head/projection choice, let U_nested,U_disjoint
be rational dual uppers for the retained objective. The complete
hinge combination satisfies

    integral_mu sum_t a_t*(A-t)_+
      <=max(U_nested,U_disjoint)
        +a1*(163/1800+2669/88200)
        +sum_(t>=2)a_t*(7579/405000+2669/88200).      (P10)

The new denominator625 need not divide the old integer compiler
grid. The checker therefore keeps the new tail constant as an
exact Fraction before applying the original scaling. It does
not round it or demand an invalid integral conversion.

All earlier two-, four-, six-projection and209 prefix pruning
expressions retain their original constants and218's full-hinge
deletion credit. Only the new candidate uses(P9). The new
actual-survivor objective receives no external deletion credit.

## 6. Complete scans, exact certificates and full consumption

Each independent original heavy cost retains all12,500 head
layouts and10*50*10 positive-seven projection combinations.
Every original125/135 profile is contained in(P3)--(P4), rather
than fixed to a preceding controller. A branch is evaluated or
bounded by an earlier complete candidate below the final scan
maximum. The inherited exact accounting covers62,500,000
original head/projection choices per heavy cost, separately
from the seed. At each expanded leaf both27/81 geometric
branches are checked and their uppers are combined by maximum.

The exact complete heavy bounds are

    Cost0<=84643573012651440685950791145819799129
           /15731107168725649587937050000000000000
          =5.380649442203772966...,
    Cost16<=249986519040059146643072918461822105447
            /58142172095610000877015336800000000000
           =4.299573098661965344... .                (P11)

The complete scan counts are:

| Cost | Two-projection evaluations | Four-projection evaluations | Six-projection evaluations | Two-branch dual uses, including seed | Distinct branch duals | Exact old source-capacity LPs |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
|0|125000|3942450|10260|1568|3134|4077713|
|16|125000|3990250|10270|1580|3158|4125523|

The two-, four- and six-projection pruning counts are respectively
(46151,3941424,8693) for cost0 and(45195,3989223,8691) for
cost16. The earlier prefix bound is available at seven joint
evaluations per cost, with the seed included in the counting
convention, and alone prunes no branch. Both full maxima
have head(1,4,2,1,2,4,2) and positive-seven projections
(1,2,4,1,2,1,2). These are maximizers of the containing upper
program; no actual configuration attaining the bounds is claimed.

The model uses223's parameterized standard-library integer
dual checker. It verifies all3705 column inequalities with
all7163 nonnegative inequality prices,56 free equality prices,
and the complete signed objective value. The same independent
unit-row repair is used only in the separate numerical proposal
program. No optimizer or floating tolerance is used by the
canonical checker.

The exact rational-table/run-length codec from223 receives the
new dimensions7163 and56. It restores every price and the
signed objective before verification. The writer additionally
compares the original and decoded dual banks entry by entry.
The finite encoding changes no measure, objective or constraint.

There are6292 distinct branch duals, each with7220 entries:
7163 inequality prices,56 equality prices and one signed value.
Their45428240 complete entries use713971 distinct canonical
rationals. The logical certificate contains160741138 bytes in
52 independently hash-checked semantic parts. Every decoded
dual is used and checked in a complete scan.

The consumer begins with224's full52-cost vector and improved
positive survival denominator. It replaces heavy indices0 and16
and applies the existing whole-load majorants. The prior h4
feedback at cost41 is retained. All other original labels,
signed mass, complete square, four independent AP11 blocks,
separate AP13 loss and complete infinite count tail remain.
The same original offset is used; no optimizing source is
shared across independently labelled test costs.

Only the two original heavy indices0 and16 improve under the
whole-load propagation. The complete numerator and denominator are

    N<=1836301120269463608077386122701475200034013223
        /56216212644942919597964203768500000000000000
       =32.664973926069937896...,
    d=146547090151856332806443/1708437792600000000000000
      =0.085778417444648334...>0.                    (P12)

Combining(P12) with the original offset185694867601/8599322160
gives(P0). In exact arithmetic the margin is

    403-K>=454438020929586619778690170844492204617174065889
           /758117209355329672087641395540731792073789603280
          >0.                                      (P13)

Transporting the source-coordinate permutations also transports
the new four-state partition and both independent profiles.
The second K face is normalized with its thinned ternary cell
at0. The original diagonal argument applies to the additional
fixed finite cylinder observations, with the same complete
geometric tail control. The result consequently has the same
endpoint limsup scope and supplies no quantitative off-face
neighborhood by itself.

The [canonical helper](../../frontier/transport/retained135125_heavy_comparison.py),
[certificate](../../certificates/source_norms/retained-transport/retained135125_heavy_comparison.json)
and [proposal generator](../../frontier/transport/propose_retained135125_duals.py)
contain the exact complete models, duals, scan and consumer.

```sh
python3 -I -O docs/reports/erdos7-odd-covering/frontier/transport/retained135125_heavy_comparison.py --check
```
