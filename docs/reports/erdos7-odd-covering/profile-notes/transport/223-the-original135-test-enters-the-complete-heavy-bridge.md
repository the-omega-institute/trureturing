[Index](../../marked_head_profile.md) · [Actual deletion masks](222-deep-five-deletion-retains-the-selected-observation-masks.md) · [Complete retained survivor](216-the-survivor-and-extra-deletions-share-the-complete-head.md) · [Whole-hinge pruning](218-the-retained-deletions-control-sixteen-complete-tests.md)

# The original135 test enters the complete heavy bridge

The independent original135=27*5 test can be retained inside the
same raw-source and actual-survivor bridge as the four selected
tests25,27,75,81. Its full contribution is represented by two
submeasures, with its own independent coarse projection. The
remaining zero-seven cap series loses exactly its original1/135
summand. Neither the original first-threshold remainder nor the
complete positive-seven remainder changes.

The complete52-cost comparison is

    K<=613259255229956541090108352534518566233825383292657
       /1507060000923219778242080876538947936501946649680
      =406.9242464495614166... .

It improves222's410.2337942533043... by3.309547803742935...
on the same two saturated faces. It remains3.924246449561417...
above the sufficient403 threshold.

The domain is both complete actual saturated K faces, r=rho=0,
with raw mass1/4 and surviving mass53/360. The model has2100
nonnegative variables and contains every actual configuration.
It does not assert that every feasible vector is realizable, that
independent tests share an optimizer, or that any finite exponent
cutoff suffices. All source symmetries and complete tails remain.
These are ordinary source inequalities and exact rational dual
certificates, without a Lean or unrestricted Erdos7 conclusion.

## 1. Two actual submeasures retain one independent original label

Keep222's old rectangles and four-selected-test masks C_(c,s,M),
with raw mass x, survivor mass y and complete extra deep-five
deletion mass z. Let J be the original independently chosen135
residue cylinder. Add

    u_(c,s,M)=Lambda(C_(c,s,M) intersect J),
    v_(c,s,M)=mu(C_(c,s,M) intersect J).                (R135.1)

The retained density w is constant on each old rectangle, and
mu<=w*Lambda. Restricting this inequality to J and its complement
gives all four new relations

    0<=u<=x, 0<=v<=y,
    v<=w*u, y-v<=w*(x-u).                            (R135.2)

The second inequality involving w retains the complement of J
as well as J itself. All variables come from the same actual
configuration. No independence of J and any selected mask event
is used. The existing E3/E5 deletion variables need not be split
by J: omitting that extra information enlarges a containing model.

The source table pre_(c,s) of72 and the original bridge satisfies

    raw_cap_(c,s)=eta_c*pre_(c,s),

with rows, in the slots(P,A,B,Q,H),

    c=0,1:  (0,1/5,1/5,3/20,1/5),
    c=L:    (0,0,0,1/10,1/5),
    other root1 cells: (0,0,1/5,1/10,1/5).            (R135.3)

Here L is the first-beta cell, canonically2. The entries pre are
five-coordinate masses before the additional deletions discarded
by this upper table; they have already been normalized by eta_c.
A135 test chooses a depth3 ternary cylinder and one first-five
slot. Its ternary eta mass is at most1/27, so for its actual
coarse projection(c,s),

    Lambda(J)<=pre_(c,s)/27.                        (R135.4)

There is no further factor eta_c in(R135.4). In the thinned cell0
only the inequality eta(J_3)<=1/27 is needed, not Haar equality.

Introduce a separate25-coordinate simplex lambda135_(c,s) and
require

    sum_(c,s)lambda135_(c,s)=1,
    sum_M u_(c,s,M)<=lambda135_(c,s)*pre_(c,s)/27.     (R135.5)

For an actual positive-mass event choose its own point projection.
An absent or raw-source-null event has u=v=0 and may choose any
profile. The new simplex is independent of the four original
selected projection simplexes. Allowing its convex mixtures is
only a relaxation, not a randomization assumption on the residue.

## 2. The original arithmetic intersections bound the submeasures

The raw source is dominated by Haar3 times Haar5. Consequently

    sum_(c,s,M)v_(c,s,M)<=1/135.                      (R135.6)

For the three original selected labels25,75,81, the generalized
CRT gives

    Lambda(J135 intersect J25)<=1/lcm(135,25)=1/675,
    Lambda(J135 intersect J75)<=1/lcm(135,75)=1/675,
    Lambda(J135 intersect J81)<=1/lcm(135,81)=1/405. (R135.7)

An incompatible pair has empty intersection, so these bounds need
no chosen compatibility relation. In the LP, each left side is
the sum of u over the corresponding selected bit. The indices
remain the original bits0,2,3 of25,75,81. No equality is imposed
between their independently chosen residues and the135 residue.

The two exhaustive nested/disjoint27/81 branches from222 remain
separate. Each is extended by(R135.1)--(R135.7). Thus every actual
configuration still belongs to at least one branch, including
the existing cell0 convention for source-null27 or81 events.

Every measure coordinate is at most1, and every profile coordinate
belongs to a probability simplex. Adding one unit-cap inequality
for each coordinate therefore preserves the actual embedding.
These rows allow exact dual repair without changing another column.
Each final branch has

    1275+400+400+25=2100 variables,
    997+25+1600+1+3+2100=4726 inequalities,
    54+1=55 equalities.                              (R135.8)

## 3. The added objective is the exact retained135 contribution

Keep the original head B=1+I3+I9+I5+I15+I45 and the six independent
positive-seven projections21,35,63,105,147,245. Let g_t be204's
complete two-depth seven increment, including all higher unit7
powers. For a four-selected mask M put

    V_1=B, V_t=B+|M| for t>=2.

The old objective for a nonnegative hinge combination is

    sum_(c,s,M)sum_t a_t*[y*(V_t-t)_+ +x*g_t(V_t)].   (R135.9)

At t>=2, the actual event J adds exactly

    u*[g_t(V_t+1)-g_t(V_t)]
      +v*[(V_t+1-t)_+-(V_t-t)_+]
    =u*[g_t(V_t+1)-g_t(V_t)]+v*1_(V_t>=t).          (R135.10)

This follows by splitting each actual integral into J and its
complement. It does not replace the survivor by the raw source.
Sum(R135.10) with the same coefficients a_t, excluding t=1,
and add it to(R135.9). Call the result V135. All deletion and
profile coordinates still have zero objective coefficient.

The new largest retained old load is6+4+1=11. The original exact
g_t formula applies to every integer load: once the load reaches
t, g_t is constant and the survivor hinge continues affinely.
The checker verifies the raw/survivor decomposition through11,
so the extra label creates no unchecked endpoint or truncation.

## 4. Exactly one original summand leaves the complete old tail

The omitted zero-seven series already includes the complete deep
mixed category

    sum_(a>=3,b>=1)3^-a*5^-b=(1/18)*(1/4)=1/72.

The original135 label is the(a,b)=(3,1) summand1/135 in this
nonnegative cap series. Retaining that label at t>=2 changes
the old remainder exactly to

    R135=19/648-1/135=71/3240.                       (R135.11)

This removes an assigned summand from an upper-cap series; it
does not subtract an upper bound from unknown actual mass.
Constraint(R135.6) controls the contribution now inside the LP
and supplies no additional subtraction. The unchanged threshold1
remainder is R0=163/1800. The unchanged complete complementary
positive-seven remainder is Z=2669/88200.

If U_nested and U_disjoint are exact feasible dual uppers for
V135, then the complete hinge combination satisfies

    integral_mu sum_t a_t*(A-t)_+
      <=max(U_nested,U_disjoint)
        +a1*(163/1800+2669/88200)
        +sum_(t>=2)a_t*(71/3240+2669/88200).          (R135.12)

The maximum is required because the actual27/81 geometry is
not prescribed. No external mean or deletion credit is subtracted
from this actual-survivor objective. For a complete heavy function,
its original f(1)*53/360 mass term is added after(R135.12).

## 5. Complete scanning retains all earlier pruning bounds

For each original heavy cost, all12,500 old head layouts and
their10*50*10 positive-seven projection combinations remain.
The135 projection is bounded inside its independent simplex;
it is not fixed by a previously maximizing old head.

At each branch, the new candidate(R135.12) is minimized with
the earlier complete two-, four-, six-projection and209 prefix
upper bounds. Those earlier expressions retain their own original
tails and218's full-hinge deletion credit. In particular, the
new value71/3240 is used only in the new candidate. It is not
inserted into a pruning expression that still omits135.

The scan's running maximum only increases. Any omitted subtree
has an earlier complete upper at most that final maximum.
The same exact branch accounting as218 therefore covers all
62,500,000 old choices per heavy cost, with both geometric
branches checked at every expanded leaf. The separately evaluated
seed is excluded from the original-choice count. Independent
costs retain independent maximizing test residues.

The exact complete heavy bounds are

    Cost0<=172580311957960636311568416958046909651
           /31462214337451299175874100000000000000
          =5.485319949413995...,
    Cost16<=15928644770458973404122805657714623280399
            /3633885755975625054813458550000000000000
           =4.383364211234663... .                    (R135.13)

The complete scan counts are:

| Cost | Two-projection evaluations | Four-projection evaluations | Six-projection evaluations | Two-branch dual uses, including seed | Distinct branch duals | Exact old source-capacity LPs |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
|0|125000|2580000|4130|514|1026|2709133|
|16|125000|2626000|4190|518|1034|2755193|

Both full maxima retain the head(1,3,2,1,2,3,2) and positive-seven
projections(1,2,3,1,2,1,2). These maximize the containing upper
program; no actual configuration attaining those bounds is claimed.

## 6. Exact dual checking and the complete52-cost consumer

Write each extended system as Ax<=b, Ex=d, x>=0. A stored
rational dual has u>=0, unrestricted v, and

    A^T*u+E^T*v>=objective

in all2100 columns. Its upper is the full signed value b.u+d.v.
The canonical checker multiplies the matrix, dual and objective
by positive common denominators and tests every inequality using
integers. It preserves all4726 inequality prices, all55 equality
prices and the complete objective, without numeric tolerances.

The separate proposal program uses numerical optimization only
to suggest prices. After rational quantization, it increases the
price of each coordinate's own unit-cap row by that column's
remaining positive deficit. Each such row touches only its own
column, so no later repair undoes an earlier one. The repaired
certificate is checked exactly before retention. A verified seed
bank can be reused without repeating numerical optimization.

The2060 branch duals are stored in an exact rational table with
run-length encoded complete price vectors. Each vector has4782
entries:4726 inequality prices,55 equality prices and its signed
objective value. The table contains162471 distinct canonical
rationals. Decoding restores every original price and value before
the same2100-column checker runs. The writer also checks exact
equality between the original and decoded banks. This encoding
reduces representation size without discarding a price or changing
the mathematical model. The complete logical certificate contains
32843878 bytes, stored in43 hash-checked semantic parts.

The consumer starts from222's complete52-cost vector, replaces
the original heavy indices0 and16, and applies the existing exact
whole-load majorants. It retains the signed actual mass term,
complete square, all independent AP11/AP13 survival losses and
their full count remainder. The denominator and original offset
are unchanged. No optimizer is shared across independent tests.

Only the two original heavy entries improve under this propagation.
The complete numerator and positive denominator are

    N<=59099899036887713592003768274147459510898017909
        /1798918804638173427134854520592000000000000000
       =32.8530108665881704...,
    d=32368969848248957388587/379652842800000000000000
      =0.08525939015634037... .                       (R135.14)

With the unchanged offset185694867601/8599322160, the resulting
complete expression is the comparison reported above.

All source-coordinate permutations transport x,y,z,u,v and every
independent profile together. In the second K orientation the
thinned ternary cell is first relabelled0. The original finite-head
and labelwise diagonal argument passes the finite clopen mask
restrictions and the uniformly controlled complete tails to
families approaching either saturated face. This supplies the
same endpoint limsup domain, without a quantitative neighborhood
or a new global comparison.

The [canonical helper](../../frontier/transport/retained135_heavy_comparison.py),
[certificate](../../certificates/source_norms/retained-transport/retained135_heavy_comparison.json)
and [proposal generator](../../frontier/transport/propose_retained135_duals.py)
retain the exact models, duals, source pins, complete scan and
full consumer.

```sh
python3 -I -O docs/reports/erdos7-odd-covering/frontier/transport/retained135_heavy_comparison.py --check
```
