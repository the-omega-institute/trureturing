[Index](../marked_head_profile.md) · [Seven retained states](264-seven-retained-states-and-two-seven-depths-control-complete-j-heads.md) · [Actual tail pairs](276-actual-retained-tail-pairs-strengthen-original-j-costs-and-square.md) · [Pure low factorials](278-two-pure-factorial-observations-strengthen-the-j-source.md)

# Seven retained old labels strengthen the complete J moments

On both entire actual saturated J faces, the same original functions satisfy:

| Target | Factorial threshold | Exact integral upper | Decimal prefix |
| --- | ---: | ---: | ---: |
|cost-48|3|2812612580248410797/793800000000000000|3.5432257246767583736457545981355505...|
|cost-49|2|15189105300089302871/3969000000000000000|3.8269350718290004714033761652809272...|
|square|1|392007731113171819/88200000000000000|4.4445320987887961337868480725623583...|
|factorial5|5|2120267967804001/2646000000000000|0.80131064542857180650037792894935752...|
|factorial2|2|643756572268746031/441000000000000000|1.4597654699971565328798185941043084...|
|factorial3|3|2264706575156562827/1984500000000000000|1.1411975687359852995716805240614764...|

Each target covers all62,500,000 original independent containing choices,
the entire late interval and every old and positive-seven cofactor depth.
The source model is exactly264's6531 variables,11211 inequalities and19
equalities. All2628 distinct rational duals are checked on all6531 columns,
for17,163,468 column inequalities and375,000,000 covered choices across the
six targets. These are source inequalities; a separate complete comparison
consumer must rederive its own envelopes and independent moment witnesses.

The added original old label225 extends276's actual pair-count objective
without changing the underlying raw source Lambda or survivor mu. The
positive-seven labels and276/278's full-function affine pruning remain
unchanged. No equality or independence of unrelated test residues is imposed.

The original positive-integer identities remain

    cost48(n)=5*(n-3)_+ +2*Phi3(n),
    cost49(n)=(31/16)*(n-2)_+ +(17/16)*(n-3)_+ +2*Phi2(n),
    n^2=1+(n-1)_+ +2*Phi1(n),
    Phik(n)=(n-k)_+*(n-k+1)_+/2.

The helper checks every finite transition and all coefficients of each
entire polynomial tail. Phi5, Phi2 and Phi3 keep an empty hinge sum;
none uses a synthetic hinge or adds a new cost index.

## Same actual state model

Use264's6531 variables,11211 inequalities and19 equalities, including all
876 original244 variables and the implicit complement. The old mask has
bits(25,27,75,81). The nonempty marked states have bits(135,125,225), in that
order, and counts

    n_s=popcount(s), s=1,...,7 = (1,1,2,1,2,2,3).

U starts at876 and V at3676, with400 coordinates per state. The independent
profiles begin at6476,6501,6506. The former3306-variable model's V offset2076
must not be reused inside the seven-state model.

X,Y,U,V are restrictions of the same actual raw source Lambda and survivor
mu. Retain all complement-domination, marked-deletion, common-theta, profile
and original CRT constraints. In particular225 uses its independent25-cell
profile E(c,s)/25, not a five-slot profile or a copied75/125 residue. Its
raw/survivor cap is1/225. No independence of selected events is imposed.

## Six added old-old payments

The retained old set expands from
R6={25,27,75,81,135,125} to R7=R6 union{225}. The complete276 POO series has
upper111/800. Each additional pair is an original distinct-label pair:

| Other label | LCM with225 | Assigned survivor payment |
| --- | ---: | ---: |
|25|225|1/225|
|27|675|1/675|
|75|225|1/225|
|81|2025|1/2025|
|135|675|1/675|
|125|1125|1/1125|

The payments sum to134/10125. They come from the original J survivor cap
function at the LCM, not from a product of marginal probabilities. Therefore

    selected_POO = 8857/202500 +134/10125
                 =11537/202500,
    remaining_POO=111/800-11537/202500
                 =132479/1620000 >0.

There are21 selected pairs. This removes only six new assigned summands:
pairs between225 and unselected old labels remain in the complete POO tail.

## Seven added old-positive-seven blocks

Keep exactly276's selected positive-seven set: every pure7^e for e>=1;
the cofactors3,5,9,15 at depth1; and cofactors3,5 at depth2. No other
positive-seven cofactor is newly retained.

The old raw cap of225 is1/225. Each selected cofactor divides225, so every
LCM with225 is225. The new assigned payments are

    cofactor1, all e>=1: (1/5)*(1/225)=1/1125,
    each of3,5,9,15 at e=1: (6/35)*(1/225)=2/2625,
    each of3,5 at e=2: (6/245)*(1/225)=2/18375.

The infinite cofactor1 payment is exact because
sum_(e>=1)6/(5*7^e)=1/5. Thus

    added_POZ=(1/225)*(1/5+4*6/35+2*6/245)
             =229/55125,
    selected_POZ=5402/91875+229/55125
                =17351/275625,
    remaining_POZ=121/720-17351/275625
                 =51501/490000 >0.

The original42 blocks become49. Every unselected old/positive-seven pair,
including the positive-seven labels with cofactor225, remains in the POZ
complement. The zero-seven label225 is a different original label from
225*7^e; their payments must not be conflated.

## Actual state identities and the generalized factorial

Let m be the four-bit old-mask count, n=n_s, and R the selected old count.
In each cell-mask coordinate, exact integration gives

    integral R dmu = m*Y +sum_s n_s*V_s,
    integral binom(R,2) dmu
       =binom(m,2)*Y +sum_s[m*n_s+binom(n_s,2)]*V_s. (P1)

The second formula is precisely all21 selected POO pairs; the zero-state
contribution is already in Y. It does not add seven copies of Y. The
helper checks every one of the128 mask/state configurations directly.

For the same rectangle define

    W=1/5 +(6/35)*first_count +(6/245)*second_count,

using276's independent original21/35/63/105/147/245 projections. The selected
POZ replacement is

    W*[m*X +sum_s n_s*U_s].                         (P2)

This uses raw U and X. An extra survivor factor w or V in(P2) would change
the measure and would not represent the assigned raw POZ payments.

For any integers k>=1, B>=0, T>=0, put

    Phi_k(x)=(x-k)_+*(x-k+1)_+/2,
    h=(B-k+1)_+.

Then

    Phi_k(B+T)<=Phi_k(B)+h*T+binom(T,2).             (P3)

If B>=k-1, expanding the two quadratics gives equality for every T>=0,
including B=k-1. Otherwise a=k-1-B>=1, Phi_k(B)=h=0, and
Phi_k(B+T)=binom((T-a)_+,2)<=binom(T,2). This is a proof for every integer
T, not an extrapolation from a finite range. The helper retains the exact threshold-specific all-tail split records.
The preceding two algebraic cases prove the inequality for all B,T; no
finite transition check is substituted for the infinite continuation.

Decompose T=O+Z. The identity

    binom(T,2)=binom(O,2)+O*Z+binom(Z,2)

separates POO, POZ and PZZ into disjoint pair classes. Equations(P1),(P2)
replace only their designated POO/POZ summands. The entire conditional PZZ
construction of276 is unchanged and remains on the same raw source and
common theta. If the original cost has factorial coefficient f_c, every
factorial head, cross, selected pair and pair payment carries that same f_c.
For the square f_c=2; for pure factorials f_c=1. The original hinge terms
and constant mass term retain their own coefficients.

## The h-times-old-tail partition

For z=w*h, use276's five coefficient families C(z): pure-three row,
pure-five slot, root-five, cell-five, and uniform mixed. The original full
weights are

    (1/18,1/20,1/20,1/20,1/72).

225 is exactly the cell-five family's b=2 term. The selected weights become

    (4/81,6/125,1/25,1/25,1/135),

and the complete remaining weights are

    (1/162,1/500,1/100,1/100,7/1080).               (P4)

Their sum is the original vector coordinate by coordinate. This replaces
only C_cell(z)/25 by the actual h*I225 survivor contribution through(P1).
All five residual weights are positive geometric-series complements.
The h-times-positive-seven cross is unchanged: in particular its
`old_tail(h)/5` term still contains every unselected positive-seven old
cofactor, including225. No new225 subtraction is licensed there.

For nonzero hinge terms with threshold at least2,264's old remainder is

    R4-cap135-cap125-cap225
      =2471/81000-1/135-13/3750-1/225
      =6151/405000.

A threshold1 term retains its original R0=53/600 convention. The positive-seven
hinge remainder is still37/1225. These hinge payments and(P4) belong to
separate algebraic summands of the original cost; neither is subtracted
from the POO or POZ series.

## All infinite pair remainders balance

Before the unchanged conditional PZZ replacement,

    selected_POO +remaining_POO
      +selected_POZ +remaining_POZ +89/240
        =4879/7200,

    remaining_POO+remaining_POZ+89/240
        =44271383/79380000.

Thus every original pair payment is selected or retained exactly once.
Replacing89/240 by276's two-endpoint conditional PZZ secant is the same
already-proved operation; adding225 does not license a second PZZ saving.
Infinite sums use the original nonnegative cap series and their exact
geometric complements. No finite exponent truncation, independence
assumption, common attainment assumption, or changed original cost label
enters this extension.


## Complete scanning and exact certification

The exact6531-column objective starts with264's unchanged complete hinge
objective. For factorial coefficient f_c and at-one constant a, it adds,
in cell i and four-bit mask count q,

    X: f_c*(h+q)*W,
    Y: f_c*(Phi_k(B)+h*q+binom(q,2)),
    U_s: f_c*n_s*W,
    V_s: f_c*(h*n_s+q*n_s+binom(n_s,2)).              (P5)

If c0,c1 are the complete unselected cross endpoints, and p0,p1 are276's
complete conditional PZZ endpoints, the shared theta coordinate receives
f_c*((c1-c0)+(p1-p0)). The outside constant receives

    a*(3/20)+f_c*(c0+4879/7200-89/240+p0-C_OO-C_OZ).  (P6)

The only potentially negative objective coefficient is this single common
theta secant coefficient. All actual X/Y/U/V coefficients are nonnegative.
The two endpoint secants bound the same complete function on the full late
interval. In particular neither the PZZ cap nor any selected pair payment
is subtracted twice.

Every threshold gets a fresh276 affine compiler. The original complete
old affine lines remain valid bounds on the same function after225 is
retained in the exact objective. Conditional pruning changes only their
one assigned PZZ payment. The pure targets use the complete276 Phi5 or278
Phi2/Phi3 scanner. Unpruned branches use the stronger seven-state objective;
all pruned branches keep their original full-tail bound. The checker
verifies the exact branch-count identities and the final maximizing
certificate branch. This maximum is an upper certificate, not an attained
original test family.

All75,000 target/layout pairs and150,000 corresponding cross endpoints
are recomputed and content-hashed. A numerical optimizer only proposes
dual prices; the published helper has no optimizer and checks the supplied
rational prices against every original model column. It uses the existing
11211-inequality/19-equality dual codec and canonical certificate IO.

The executable result is `frontier/j_face_triple_pair_factorial_heads.py`;
its exact data are `certificates/source_norms/j_face_triple_pair_factorial_heads.json`.
Run the helper with `--check` to reconstruct the pinned inputs, partitions,
all layouts, complete scans, dual certificates and every recorded field.

The statements concern the two entire original saturated J faces. They
assert no source attainment, off-face extension, global comparison join,
Lean verification or unrestricted resolution of Erdős problem7. Any later
stronger head formula is a separate source inequality and does not alter
these certified original functions or their completed scans.
