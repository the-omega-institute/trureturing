[Index](../marked_head_profile.md) · [Original complete heads](264-seven-retained-states-and-two-seven-depths-control-complete-j-heads.md) · [Original375 refinement](288-retaining375-strengthens-the-complete-j-survival-hinge.md)

# Retaining375 certifies the complete heavy cost

On both entire actual saturated J faces, the original heavy function

    h0(n) = sum_(t=1,...,8) a_t (n-t)_+

satisfies

    integral h0(A) dmu <= 543/100 = 5.43.

Here `A` is the original load for this functional and `mu` is its actual
survivor measure of mass `3/20`; the underlying actual raw measure has
mass `1/4`. The coefficients are exactly those of original cost0 in264:

| t | a_t |
| --- | --- |
| 1 | 298469622484874903/2311075142665519230 |
| 2 | 12386341040791428463/9244300570662076920 |
| 3 | 525174571/736900164 |
| 4 | 9522349/858858 |
| 5 | 1580/4719 |
| 6 | 3160/42471 |
| 7 | 395/3861 |
| 8 | 282583/7722 |

All coefficients are positive, `h0(1)=0`, and their sum is `403/8`.
The previous complete upper was

    1747623922413605093240428858165191004949
    /314622143374512991758741000000000000000
      = 5.554675534497605... .

The target function, its H1 component, every original independent label,
and all exponent and cofactor tails remain unchanged.

## The same actual-source model and complete target

Use288's original375 refinement of264. Its12941 nonnegative variables,
30454 inequalities and20 equalities keep all old variables and constraints.
The retained old labels are `25,27,75,81,135,125,225,375`; the independent
positive-seven labels are `21,35,63,105,147,245,441,735`. Intersections,
complements and separate original residue profiles have exactly the
actual-source interpretation established in288.

At a cell with old load `v`, first-extra count `m` and second-extra count
`e`, apply the complete conditional cap kernel of288 to each threshold:

    G(v,m,e) = sum_t a_t seven_increment(t,v,m,e),
    H(v)     = sum_t a_t (v-t)_+.

On the original marked states the objective uses `G` for raw mass and
`H` for survivor mass. Retaining375 contributes the exact differences
`G(v+1,m,e)-G(v,m,e)` and `H(v+1)-H(v)` on the corresponding actual375
intersections. These are the same full physical columns as288, now for
the original positive combination of eight hinges.

Each threshold's raw kernel is nondecreasing at every finite transition
and exactly constant for all `v>=t`. Its survivor hinge continues with
slope1. Thus the finite transition checks also establish the complete
infinite continuation; no remaining-load cutoff is introduced.

Only375's original assigned payment is removed from the old tail. The
complete tails and their heavy contribution are

    old tail             = 5071/405000,
    positive-seven tail  = 13/490,
    heavy tail constant  = (403/8)(5071/405000+13/490)
                         = 312316537/158760000.

## Complete original prefix coverage

The helper reconstructs the complete independent original prefix domain
using the existing exact affine, conditional-cap and source-dual APIs.
It compares the resulting residual table with the independent complete
reconstruction. The original writer's final artifact is unavailable;
no equality with that nonexistent artifact is asserted.

The fixed original prefix threshold is

    15751281119445414267484319072612603681
    /2913167994208453627395750000000000000
      = 5.406925090059987... < 5.43.

This threshold is a scheduling value supplied by50 checked original
complete-objective seed certificates, not an assumed universal bound.
Every pruned prefix has its own exact upper at most that value.

The complete independent-choice accounting is

    25000*58912 + 500*3303194 + 50*10214 + 10*5422
      +17601+50+20429 = 3125000000.

The20429 residual leaves retain their complete original layout, seven
projection entries, final `(c441,r735,s735)` labels, their own affine
upper and their own strongest original upper. The original5000-leaf
rule failed and remains failed. The new target is certified by the
following separate complete cover.

## Exact zero columns and envelopes preserve every original member

An induction through2645 original zero-right-side rows certifies4634
zero variables. In each step, all negatively signed variables have
already been proved zero; nonnegativity then forces the positively
signed variables to vanish. No other column is discarded.

For any selected group of original leaves, let `c_l` be its exact
physical objective vector after those certified zeros are removed.
Set `m_j=max_l (c_l)_j`. For every original feasible nonnegative `x`,

    c_l . x <= m . x.

This remains true when different coordinates maximize at different
leaves. It does not assert a common maximizing original source. An
exact rational dual for `m`, with the complete heavy tail added,
therefore covers every original leaf in that group.

The final cover is disjoint and exhaustive:

| Bound supplying the final certificate | Original residual leaves |
| --- | ---: |
| Each leaf's own original upper | 2768 |
| Direct domination by existing source duals | 305 |
| Fixed complete layout and seven-projection envelope | 13569 |
| Additionally fixed original c441 | 3611 |
| Additionally fixed original r735 | 161 |
| Final exact original-objective classes | 15 |
| Total | 20429 |

The final fifteen original leaves form twelve exactly equal-objective
classes after certified zero elimination. Their original branch labels
remain distinct. Seven classes reuse existing duals by checked direct
domination; only five require additional original-objective proposals.
Direct domination is not asserted to be equality with the source dual's
generating objective.

All1177 nonempty covering nodes use981 distinct exact dual vectors.
Only certificates used in this final cover are retained, together with
the50 prefix seeds. Earlier unsuccessful envelopes do not carry the
final result. The complete original leaf bounds are retained, and each
covered leaf adopts the smaller of its new complete certificate and
its own original upper.

For each supplied dual the helper checks every original column,
nonnegative inequality multiplier, unrestricted equality multiplier,
and the exact right-hand-side sum. A separate direct Fraction check
repeats the column inequalities. Both use the unchanged existing
rational-table/run-length codec. The final maximum is exactly `543/100`.

The [standalone helper](../frontier/j_face_retained375_heavy_heads.py)
and [certificate](../certificates/source_norms/j_face_retained375_heavy_heads.json)
reconstruct and check the complete result with standard-library arithmetic:

```sh
python3 -I docs/reports/erdos7-odd-covering/frontier/j_face_retained375_heavy_heads.py --check
```

This is a complete source bound on the two actual saturated J faces.
A separate consumer must recompute all52 costs and the survival
denominator. Different original cost loads are not identified by this
result. Actual-source attainment, off-face extension, global joining,
Lean verification and unrestricted Erdős7 are not conclusions here.
