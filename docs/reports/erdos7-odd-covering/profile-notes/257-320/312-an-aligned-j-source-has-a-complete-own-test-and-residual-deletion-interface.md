[Index](../../marked_head_profile.md) · [Common deleted measure](../001-064/57-common-deleted-measure-coupling.md) · [Sharp aligned surplus](311-imperfect-j-source-alignment-forces-a-sharp-sector-surplus.md)

# An aligned J source has a complete own-test and residual-deletion interface

In the actual countable-label closure of130/302, assume qJ=1, original45
and135 lie in the same surviving mod9 cell L, and rho=S-S0=2/675.
No concentration of the remaining beta or late masses is assumed.
For every complete original357 test A, with its own independently chosen
residue at every divisor label and including its unit term,

    integral A dmu <=293/450,
    integral (A-1)_+ dmu <=269/540,
    6S-integral A dmu >=4/15, S=413/2700.          (AE1)

The source135 hole stays its actual27-by-first5 rectangle; it is never
spread uniformly over a mod9 cell. These bounds concern the source J
interface, not Gamma19 or a403 consumer. The endpoint is a countable
actual-source statement and a uniform limiting statement for actual
finite families, not a claim that a finite family attains qJ=1.

## The modified complete deficit vanishes

Fix one130 orientation; the other exchanges cells0 and1. Put g=1/135.
The exact source data retain eta=(1/18,1/9,1/9,1/9,1/9), h=1/2,
h1=1/3, s=1/4, nominal old cap sum C=1/2, and D=S0=3/20.
The beta tail and late tail may be distributed throughout root1, subject
to actual-label compatibility. Their original complete budgets remain
1/4 and1/72.

By311, the cofactor5 and cofactor15 raw caps improve respectively from
h/5 and h1/5 to h/5-g and h1/5-g. These are uniform over THEIR OWN
independent old carriers. At every positive seven depth use the unchanged
u_e=6/(5*7^e), whose complete sum is1/5. All other original cofactor caps
are unchanged. The resulting complete mixed7 cap total is(C-2g)/5.

Write Dact for57's actual projected deletion, V for its virtual original
cofactor measure, and Omega=V-Dact>=0. There is an exact nonnegative
ledger

    S-D-2g/5
      =[(C-2g)/5-V(1)]+Omega(1).               (AE2)

The first bracket is itself the sum of all individual adjusted cofactor
capacity deficiencies, including every absent label and every omitted
exponent tail. Each term is nonnegative by311. At the present endpoint
the left side is zero. Thus every adjusted cap is attained at EVERY
original seven depth, Omega=0, and projected mu=Lambda-V as measures.
In the old unadjusted notation this says E5=E15=g/5=1/675; every other
cofactor deficiency is zero. The seven probability and overlap losses
inside Omega vanish as well. This is the complete ledger, not a finite
head inference or an assertion from the particular311 witness.

## The exact H hole and remaining source labels

Let P,A,B,Q be311's four distinct first-five slots, and H the fifth.
Original135 lies in H. Let I be its actual ternary27 cylinder inside L,
and let zeta be raw Haar on I times H; zeta has mass g and is entirely
removed by original135 after the earlier source stages.

Every other first-five slot has source loss strictly greater than g
relative to the cofactor5 nominal cap: the smallest alternative loss
is h/25=1/50. For cofactor15 the smallest competing loss is h1/25=1/75;
wrong-root carriers lose at least1/30. Therefore equality in the two
adjusted cap families forces every cofactor5 carrier to H and every
cofactor15 carrier to root1 times H. It also forces total source loss
on H to equal g, and likewise on root1 times H. Since this loss measure
dominates the original135 rectangle of that full mass,

    Lambda restricted to H=(eta tensor Haar_H)-zeta.   (AE3)

There is no other positive source hole in H, including outside L.

Complete source-budget equality makes all source exclusions contribute
their full additional raw masses. The deeper pure5 and alpha families
avoid P,A,B. They also avoid H: any such source cylinder in H would
intersect the full original135 rectangle, contradicting its full additional
budget. They are consequently both in Q, with complete five masses1/20
each. This is the same four-slot packing as130, with the actual zeta hole
now retained in H.

Every original b=1 late source label other than135 has to use B outside L.
P and A are already deleted in root1, Q intersects source25, B in L is
already deleted by45, and H has no remaining source-hole capacity by(AE3).
All late additional budgets are full and disjoint when assigned, so the
complete remaining b=1 mass is

    1/90-g=1/270.                                (AE4)

It may split arbitrarily between the other two root1 cells. No particular
split or dispersed-beta profile from311 is imposed.

## Ternary own-test caps with the literal hole

All deep pure3 forbidden caps are unchanged and attained. The argument
of130 therefore still forces their full raw root0 cylinders and gives
exactly the projected virtual deletion q(F)/90 for every five event F.
Here q is the actual pure5 complement; its first-slot masses are
(0,1/5,1/5,3/20,1/5) on(P,A,B,Q,H).

The full pure5 forbidden family has ternary projection

    eta/20-zeta^3/5,

and the full3*5 forbidden family has projection

    eta restricted to root1/20-zeta^3/5.

To verify these formulas, the b=1 carriers are fixed by(AE3). Every b>=2
cap is still full, so its old rectangle equals its product upper as a
measure; summing its full five tail and seven weights gives the unchanged
deep projection eta/100, respectively eta_root1/100. The b=1 projection
is eta/25-zeta^3/5, respectively eta_root1/25-zeta^3/5. Adding them proves
the displayed formulas with their complete tails.

Keeping also the fixed shallow3 and9 families gives for each ternary T

    mu(T)<=Lambda(T)
       -[Lambda(T intersect root0)+Lambda(T intersect cell1)]/5
       -[eta(T)+eta(T intersect root1)]/20
       +(2/5)zeta^3(T).                           (AE5)

Thus the two root caps are3/40 and11/120+2/675. The cell caps outside L
are unchanged from130. In L its beta and late masses obey beta_L>=1/5
and late_L>=g, giving

    mu(L)<=2/45-beta_L/9-late_L+2g/5<=4/225<2/45.

Every own test9 therefore still has cap2/45.
For a deep ternary cylinder T in L, Lambda(T) already contains the
negative term-zeta^3(T). Combining it with(AE5) leaves the favorable
term-(3/5)zeta^3(T). The root1 raw coefficient is at most2/5-beta_L;
the other root1 cells have coefficient at most2/5. The root0 coefficients
remain11/20 and2/5. Consequently every depth-a ternary test, a>=3, has
cap(11/20)3^-a, and its COMPLETE tail is11/360. The135 hole is not
replaced by its cell average in this argument.

## First-five and mixed first-slot tests

Keep the four distinct original forbidden families3,9,5,15 in V. Their
carrier choices just proved give the same multiplier as130,

    w=1-[1_root0+1_cell1+1_H+1_(root1 times H)]/5,
    mu<=w Lambda.

Before beta/late source losses the cell table divided by eta_l is

| Cell | P | A | B | Q | H |
|---|---:|---:|---:|---:|---:|
|0,1|0|1/5|1/5|3/20|1/5|
|L|0|0|0|1/10|1/5|
|Other root1 cells|0|0|1/5|1/10|1/5|

Subtract zeta's whole mass g from(L,H) and the whole remaining b=1
mass1/270 from B in the other root1 cells, with its arbitrary split.
All omitted source deletions have favorable sign. After applying w and
subtracting the DIFFERENT deep-pure3 family q/90, the column caps are

    (0,1/50,41/675,29/600,11/225).               (AE6)

Their maximum is41/675, the own test5 bound. Before subtracting q/90,
the root1 column caps are

    (0,0,11/270,1/30,8/225).

The root0 column caps are no larger than1/45. Hence every independently
labelled test15 has cap11/270. Checking the two extreme late splits for
each of the three choices of L suffices because these table expressions
are affine. This is an upper-table calculation, not a realization claim.

The higher pure5 argument only uses the unchanged shallow3/9 and deep3
projected deletions. It therefore retains cap(13/30)5^-b for b>=2 and
complete sum13/600. The remaining mixed categories keep their full raw
source caps.

## Complete mean and hinge inventory

| Own original test category | Complete upper |
|---|---:|
|3|11/120+2/675|
|9|2/45|
|3^a, a>=3|11/360|
|5|41/675|
|5^b, b>=2|13/600|
|15|11/270|
|3*5^b, b>=2|1/60|
|9*5^b, b>=1|1/36|
|3^a*5^b, a>=3,b>=1|1/72|

These nonunit zero7 bounds sum to79/225. For ALL positive7 tests,
including the pure7 unit-cofactor labels, apply their own raw source caps.
The cofactor5 and15 improvements are uniform over independently chosen
carriers and therefore apply to test carriers as well as forbidden ones.
The full positive7 upper is consequently

    (s+C-2g)/5=3/20-2/675=397/2700.             (AE7)

The unit contributes S=3/20+2/675=413/2700. Adding all disjoint categories
gives293/450. Since A>=1, its first hinge is exactly A-1, so its upper
is269/540 and its barrier6 reserve is4/15. Each load uses its own original
residues. Different tests may maximize different categories; their separate
upper bounds do not assume a common optimizer.

## Quantitative transfer around the aligned floor

For a finite actual aligned source under311, with qJ>=1-delta and
delta<=1/1000, put lambda=1/72-sum_root1 late and g_delta=1/135-lambda.
Use the SAME actual V,Dact and Omega from57. The new complete raw cap
bound is V(1)<=(C-2g_delta)/5. Therefore the exact nonnegative budget is

    epsilon_*=S-D-2g_delta/5
      =[(C-2g_delta)/5-V(1)]+Omega(1)>=0,
    epsilon_*<=rho-2/675+7delta/36.              (AE8)

The last inequality uses S0-D<=17delta/90 and lambda<=delta/72. All
unused adjusted cofactor capacities, missing tails, seven-cap losses and
union overlap are paid by epsilon_*; the unavoidable135 hole is not
charged again as an error.

For each bounded0<=phi<=R, direct reuse of57 gives

    integral_Dact phi>=integral_V phi-R epsilon_*.

For a finite collection of independently labelled old35 tests Z_i,
nonnegative weights w_i>=0, and centered nonnegative costs
f_i(v)<=a_i(v^(r_i)-1) with a_i>=0 and r_i in{1,2}, put
Phi=sum_i w_i f_i(Z_i),
alpha=sum_(r_i=1)w_i a_i and beta=sum_(r_i=2)w_i a_i. Its COMPLETE same-law
transfer is

    integral_Dact Phi>=integral_V Phi-sqrt(M epsilon_*),
    M=alpha^2*(2227/640)+2alpha beta*(13481/320)
                           +beta^2*(884579/1280).      (AE9)

The existing whole-label CRT moment bounds in57 apply without changing V
or any Z_i. Thus(AE9) covers the complete unbounded hinge and quadratic
cost tails; it does not arise from a finite cutoff. The rational penalty
R epsilon_*+M/(4R) may replace the square root for any R>0.

This is a transfer for the ACTUAL V of that imperfect source. It does not
by itself transport the exact table(AE6), or prove that its same constants
hold at nonzero delta or epsilon_*. Additional source-geometry transport
is required before using(AE1) on a numerical off-face box.

The nested actual finite source families constructed in311 have qJ tending
to1 and rho tending to2/675; their original labels stabilize on each finite
exponent box. Their completed limit therefore supplies a nonempty instance
of this endpoint domain. In particular the endpoint statement is not being
proved only for an impossible finite equality condition.

For arbitrary actual finite aligned families with qJ tending to1 and rho tending
to2/675, the complete label compactness and uniform first-moment tails
already established in130/302 give the uniform limiting(AE1). One first
normalizes a shallow orientation and takes subsequences of the source,
forbidden and each independent test labels. Equation(AE8) forces the
limiting adjusted ledger to vanish; the limiting source is precisely in
the domain proved above. Complete original CRT tails justify passage of
the integrals. A contrary limsup sequence would give a violating exact
limit. No explicit finite-source radius is asserted here.

## Arithmetic check

The [portable helper](../../frontier/j-geometry/j_aligned_own_test_interface.py) uses the canonical
certificate IO and writes only with --write. It supports --base and
--check; its default [certificate](../../certificates/source_norms/j-geometry/j_aligned_own_test_interface.json) records the exact interface. It pins57,130,311
and each of their certificates, including the actual311 endpoint sequence. It verifies all six affine table endpoints,
the root and cell comparisons, every complete category fraction, the
positive7 adjustment and all three(AE1) constants. It performs no LP or
source scan. The source-forcing and all-tail arguments are the ordinary
proofs above; the small table computation alone does not certify them.

```sh
python3 -I -S -O docs/reports/erdos7-odd-covering/frontier/j-geometry/j_aligned_own_test_interface.py --check
```
