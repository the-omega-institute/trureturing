[Index](../../marked_head_profile.md) · [Raw cross-depth method](../001-064/63-six-original-labels-strengthen-the-endpoint-square.md) · [Complete LCM partition](../065-128/128-the-complete-factorial-tail-retains-its-head-off-the-face.md) · [Shared J moments](../193-256/241-one-original-j-head-controls-complete-square-and-factorial-moments.md) · [Two quadratic costs](../193-256/247-two-original-j-quadratic-costs-share-hinges-and-factorial-head.md) · [Complete factorial](../193-256/248-the-complete-j-factorial-tail-retains-both-prime-paths.md) · [Complete square](../193-256/253-two-complete-prime-path-blocks-sharpen-the-j-square.md)

# Independent positive-seven depths share one complete raw source

On both entire actual saturated J faces, every independently labelled
complete original357 test A satisfies, for the same unnormalized
survivor mu of mass3/20,

    integral A^2 dmu <=49001/10800=4.537129629629...,
    integral Phi5(A) dmu <=18359/21600=0.849953703703...,
    Phi5(v)=(v-5)_+*(v-4)/2.                         (CP1)

The original quadratic costs47 and48 retain their separate head
optimizations and now satisfy

| Original cost | Complete upper |
| --- | ---: |
| 47 | 41477502721101203/12600000000000000 |
| 48 | 47898453555557813/12348000000000000 |

Their decimal values are3.29186529532549230158... and
3.87904547745042217363..., respectively. The complete mean bound
16/25 remains available. These are ordinary rational bounds on the
stated whole J domain; no source attainment, off-face extension,
full52-cost comparison, Lean verification or unrestricted Erdos7
resolution is asserted.

The common improvement replaces the complete distinct-pair block
whose two original labels both have positive seven depth:

    PZZ<=403/1080, replacing2/5, gain29/1080.          (CP2)

The raw cross-depth argument is already present in63 for six old
cofactors. Here it is applied to complete old-cofactor loads, including
every omitted original label, on the whole J source domain. The
source formulas and complete LCM multiplicities are reused from128
and241; no K numerical assumption enters this extension.

## One complete old-cofactor load on the actual J source

Let Lambda be the actual raw35 source of241, of mass1/4. Each original
old modulus3^a*5^b has its own chosen residue. Complete missing labels
arbitrarily when necessary; all loads and objectives used below are
nonnegative and increase under that completion.

Write an arbitrary complete old load as F=B+R, retaining

    B=I1+I3+I9+I5+I15+I45,
    H={0,1,2} times {0,1},
    R=sum_((a,b) outside H) I_(3^a*5^b).             (CP3)

The head has12500 independent layouts on the five ternary cells and
five first-five slots. Choices outside the surviving source support
can be replaced by supported choices, increasing the load. Neither
the head residues nor the omitted residues are assumed nested.

For a fixed head,241's exact raw source optimization is

    U_theta(z)=sum_i cap_theta(i)*z_i
                 -(1/120)*min_(positive root1 cells i) z_i,
    1/135<=theta<=1/90.                              (CP4)

The three root-group masses are1/24,1/12,1/8. Their raw capacity sums
are1/24,1/12,2/15. The eight positive entries in the third group have
fixed support and each has capacity at least1/90, so its1/120 excess
can be removed at a minimum-coefficient entry. Thus(CP4) bounds every
actual source in the interval and is affine in theta for each fixed z.

The complete raw head/omitted cross is bounded by128's operator

    O(B)=C3(B)/18+[C5(B)+C15(B)+C45(B)]/20
                                                    +max(B)/72, (CP5)
    C3(B)=max_c sum_s p(c,s)*B(c,s),
    C5(B)=max_s sum_c d(c,s)*B(c,s),
    C15(B)=max_(r,s)sum_(ROOT(c)=r)d(c,s)*B(c,s),
    C45(B)=max_(c,s)d(c,s)*B(c,s).

Here p and d are241's normalized deep-three and absolute
descendant-five tables. This is integral_Lambda(BR), so no survivor
density w is inserted. The five terms sum all original omitted
families: pure3 depth>=3; pure5,3*5^b,9*5^b at depth>=2; and
3^a*5^b at a>=3,b>=1. The tables and(CP5) are independent of theta.

## The complete raw omitted pairs and diagonal

The raw cylinder cap Cr(a,b) from128 and241 is

    Cr(0,0)=1/4, Cr(1,0)=1/8, Cr(2,0)=1/12,
    Cr(a,0)=(3/4)*3^-a, a>=3,
    Cr(a,b)=(1/2,1/3,1/9)_a*5^-b, a<=2,b>=1,
    Cr(a,b)=3^-a*5^-b, a>=3,b>=1.                   (CP6)

For O=H-complement, the number kOO(a,b) of ordered O-by-O pairs with
coordinatewise maximum(a,b) is

    (2a-5)(2b+1),                  a>=3,b<=1;
    (2a+1)(2b-3),                  a<=2,b>=2;
    (2a+1)(2b+1)-12,               a>=3,b>=2.       (CP7)

These follow by taking two finite differences of

    [(a+1)(b+1)-(min(a,2)+1)(min(b,1)+1)]^2.

Each omitted label contributes one diagonal. Consequently the
complete assigned distinct-pair and diagonal caps are

    P_R=(1/2)*sum_O [kOO(a,b)-1]*Cr(a,b)=209/1440,
    D_R=sum_O Cr(a,b)=37/360.                        (CP8)

These are identities of known nonnegative cap series. No upper
diagonal bound is subtracted from an unknown moment.

For explicit evaluation, put

    G(p,n,s,t)=sum_(k>=n)(s*k+t)*p^-k
      =p^-n*[(s*n+t)/(1-p^-1)+s*p^-1/(1-p^-1)^2],
    a0=G(3,3,0,1), aw=G(3,3,2,1),
    b1=G(5,1,0,1), b2=G(5,2,0,1), bw2=G(5,2,2,1).

Then

    2P_R=(3/4)G(3,3,2,-6)+(1/2)G(5,2,2,-4)
          +(1/3)G(5,2,6,-10)+(1/9)G(5,2,10,-16)
          +G(3,3,6,-16)/5+aw*bw2-13*a0*b2,
    D_R=(3/4)*a0+(1/2+1/3+1/9)*b2+a0*b1.           (CP9)

The raw pure coefficients in(CP9) are distinct from the survivor
coefficients used for the zero-seven tail in241. In particular,
neither its111/800 surviving pair cap nor its53/600 surviving
diagonal is used in(CP8).

## One head controls complete raw pairs and squares

Pointwise for each finite original load,

    binom(F,2)=binom(B,2)+BR+binom(R,2),
    F^2=B^2+2BR+2binom(R,2)+R.

Combining(CP4),(CP5),(CP8), before maximizing the common head, gives

    integral binom(F,2)dLambda
       <=U_theta(binom(B,2))+O(B)+209/1440,
    integral F^2 dLambda
       <=U_theta(B^2)+2O(B)+209/720+37/360.          (CP10)

The full12500-layout, two-endpoint rational scan gives

    integral binom(F,2)dLambda<=Praw=377/288,
    integral F^2 dLambda<=Qraw=481/144.              (CP11)

Both maxima of these stated bounds use

    ell=(1,4,4,1,4,4,4), theta=1/90,
    U_theta(binom(B,2))=91/120,
    U_theta(B^2)=769/360, O(B)=73/180.

This describes the optimizing certificate, not an actual source that
simultaneously attains every omitted-cylinder cap. Each expression
in(CP10) is affine in theta for each head; checking both endpoints
therefore covers the entire interval.

## Independent positive-seven depths need no shared residue

For every e>=1 let F_e be the complete old-cofactor load obtained by
forgetting only the seven coordinate of the original labels
3^a*5^b*7^e. All residues may vary independently with(a,b,e), including
the seven residue within one depth. In particular, the actual
depth-e block need not factor as one seven indicator times F_e.

Drop mixed-seven deletions. The actual survivor is dominated by the
same raw35 source Lambda times the pure-seven-normalized reference.
A depth-f seven cylinder has cap

    u_f=6/(5*7^f).

At equal depth e, each pair of distinct old labels has seven
intersection empty or one depth-e cylinder. Apply its cap term by
term, then sum:

    same-depth distinct-pair mass
       <=u_e*integral binom(F_e,2)dLambda<=u_e*Praw. (CP12)

At e<f, every old-cofactor choice at depth e pairs with every choice
at depth f. The seven intersection is empty or a depth-f cylinder.
The same termwise argument and the pointwise square inequality give

    cross-depth pair mass<=u_f*integral F_e*F_f dLambda
       <=(u_f/2)*integral(F_e^2+F_f^2)dLambda
       <=u_f*Qraw.                                  (CP13)

The F_e and F_f are different complete tests, integrated against
one actual Lambda. Equation(CP11) is uniform over their independent
layouts and tails. No common maximizing head or matching residue is
required.

The exact complete depth weights are

    sum_(e>=1)u_e=1/5,
    sum_(1<=e<f)u_f=1/30.

Thus(CP12),(CP13) prove

    PZZ<=Praw/5+Qraw/30=403/1080.                    (CP14)

All steps first apply to arbitrary finite original label sets.
The complete polynomial-geometric cap series in(CP6)--(CP9) are
summable. Increasing finite sets therefore gives(CP11)--(CP14) by
monotone convergence; their remainders also bound every independently
chosen finite truncation. No unresolved infinite tail is omitted.
Transporting the source and original test coordinates together gives
the other J orientation.

## Replace a disjoint complete block in each consumer

In241 the old PZZ cap is2/5, so the saving is29/1080. Its other pair
blocks and both diagonal payments remain unchanged:

    complete distinct tail=5089/7200-29/1080
                          =14687/21600,
    complete ordered square tail=5947/3600-29/540
                                 =17261/10800.      (CP15)

The248 factorial improvement uses only zero-seven pure-three and
pure-five tails. Its head bound is unchanged, and(CP15) yields

    6313/7200-29/1080=18359/21600.

The two complete path blocks changed by253 also contain only
zero-seven labels. Replace the PZZ part in its controller and in
every one of its12499 other layouts:

    controller:5509/1200-29/540=49001/10800,
    other layouts:16439/3600-29/540=48737/10800.

Their gap remains11/450. Updating only the controller would not
prove the displayed uniform square bound; both parts use(CP15).

For247's original costs47 and48, the complete omitted-pair charge
is exactly2*Ptail. Each independent whole-head LP and original cost
identity remains unchanged. Replace its outside constant by
2*(14687/21600), saving29/540 for each cost. This gives the two
fractions in(CP1)'s table without identifying their separate tests
or subtracting gains from unrelated optimized totals.

The accompanying exact checker reconstructs all25000 endpoint
records, the complete geometric sums, and every displayed consumer
replacement. Independent finite-prefix checks directly enumerate
omitted-label LCM counts and distinct pairs, then compare with the
closed finite geometric expressions and their complete remainders.
It also enumerates positive-seven pair prefixes to verify the
equal-depth/cross-depth partition. The finite checks support the
general formulas; the summable identities carry the infinite tails.

Artifacts: [exact checker](../../frontier/j-geometry/j_face_coherent_positive7_pairs.py)
and [certificate](../../certificates/source_norms/j-geometry/j_face_coherent_positive7_pairs.json).
