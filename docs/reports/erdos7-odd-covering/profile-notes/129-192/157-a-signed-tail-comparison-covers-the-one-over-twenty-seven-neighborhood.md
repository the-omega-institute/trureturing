[Index](../../marked_head_profile.md) · [Signed masses](151-shared-source-losses-and-signed-masses-strengthen-the-local-comparison.md) · [Independent shallow bounds](152-independent-shallow-indicators-sharpen-the-complete-mean-and-square.md) · [Common loss budget](153-the-six-source-losses-share-one-actual-concentration-budget.md)

# A signed tail comparison covers the one-over-twenty-seven neighborhood

The complete actual-source rectangle

    qK>=26/27, 0<=rho<=1/100000, r<=1/520

now satisfies the original52-cost comparison

    K<=485.1608401342302...<509.                 (WT1)

All52 original independent test labels, the five denominator
objectives, both K orientations, the whole valid first-beta face,
every infinite count and exponent tail, and the one actual residual
are retained. Without152's stronger shallow bounds, the same proof
already gives489.8804182271787. Thus the complete1/27 rectangle is
established by either column;151's unpaid three groups have all been
replaced by complete finite-head and tail bounds.

These are ordinary continuous-source results checked by exact
rational arithmetic. They do not supply a global comparison outside
this rectangle, a Lean theorem, or a resolution of unrestricted
Erdos7. The displayed K has the original comparison normalization.

The main improvement is an exact cancellation within the complete
positive-seven complement. Its numerical source price falls from
367delta/1260 to a bound using one actual concentration budget.
The same valid complement improves both the numerator costs and
the five denominator losses.

## 1. A tighter outer box from the same actual factor losses

Use153's oriented losses ua,uz,ub,ud,ul,up, each at most delta, and
put t=delta/(1-delta). Their sum is at most t. The original selected
source coordinates are

    deficit0=(1-ud)/2, late0=(1-ul)/72,
    z=3/4+uz/4, alpha1=(1-ua)/4,
    sum_(j>=2)beta_j=(1-ub)/4.

In134's notation Delta=p+a+b, so

    Delta=(uz+ua+ub)/4<=t/4.                    (WT2)

Consequently its shallow slot increments can be replaced by

    v0=min(1/20,t/4+2rbar),
    v1=min(1/10,t/4+rbar/h1min).                (WT3)

The original lower packing gap, h1min, the common q polygon, all
seven shifted residual coordinates, the source ratios and all other
134 parameters remain valid. In particular, no new residual copy
is introduced by these improved caps.

For the three source-row groups, first use the exact original
formula n_j=w_j*d_j/9-late_j. Since d0<=z,

    n0<=((1+ud)/2)*(3/4+uz/4)/9-(1-ul)/72,
    n0-1/36<=ud/24+uz/72+ud*uz/72+ul/72
            <=(3+delta)*t/72.                 (WT4)

The last inequality uses ud*uz<=delta*ud and the single loss budget.
For the other root0 cell,

    n1<=z/9<=1/12+delta/36.                    (WT5)

Outside cell0 the total deficit is at most ud/2, so every root1 width
is at least1-ud/2. Dropping nonnegative late mass gives

    sum_(j>=2)n_j
       <=3*(z-alpha1)/9-(1-ub)*(1-ud/2)/36
       <=5/36+ua/12+uz/12+ub/36+ud/72
       <=5/36+t/12.                            (WT6)

Thus the group-budget increments are

    ((3+delta)*t/72, delta/36, t/12).           (WT7)

All are bounded by the old134 increments on0<=delta<=2/27. The
new slot caps are also no larger than the old ones. Every actual
source still belongs to the new outer box by(WT2)--(WT7). These are
source-coordinate bounds, not a total-variation statement about
survivor measures. The original grouping and first-beta relabeling
are unchanged.

## 2. Cancel the common source term in the complete positive-seven tail

In125's complete omitted-tail theorem, set

    N3=max(sum(n0,n1),sum(n2,n3,n4)),
    N9=max_j n_j,
    D=max_j d_j, h=sum_j eta_j,
    h1=sum_(j>=2)eta_j, em=max_j eta_j.

Its original complete positive-seven complement is bounded by

    Zplus=C/5-(6/35)*(N3+h/5),
    C=N3+N9+D/18+(h+h1+em)/4+1/72.             (WT8)

This is the same complete count/exponent sum that enters134's
fixed tail support. Combine the coefficients before estimating:

    Zplus=N3/35+N9/5+D/90+11h/700
                         +h1/20+em/20+1/360.   (WT9)

All coefficients in(WT9) are positive. The old absolute source
transport paid for movement of N3 once inside C and again in its
negative subtraction. Equation(WT9) retains the exact difference.
It deletes no original label or tail and makes no independence
assumption.

For N3, the root0 upper obtained from(WT4),(WT5) is

    1/9+(3+delta)*t/72+delta/36.

It is at most5/36+t/12 because their difference is

    (1-delta)/36+(3-delta)*t/72>=0.

Together with(WT6), this proves N3<=5/36+t/12. For N9, the three
types of cell have upper bounds

    n0<=1/36+(3+delta)*t/72,
    n1<=1/12+delta/36,
    n_j<=1/18+t/36 for j>=2.

The second dominates the other two on0<=delta<=2/27. After
multiplying by the positive denominators, the needed inequalities
are4-5delta-3delta^2>=0 and1-delta-delta^2>=0; both remain positive
at2/27 and decrease on this interval. Hence N9<=1/12+delta/36.

The other original bounds are

    D<=3/4+delta/4, h<=1/2+delta/18,
    h1<=1/3, em<=1/9.

Substitution in(WT9) yields the complete new tail bound

    Zplus<=779/12600+t/420+29delta/3150.         (WT10)

At delta=1/27 this is137671/2211300, or0.06225794781350337.
Relative to134, its exact saving is

    DeltaZ=45797/4422600
          =0.010355220910776466... .             (WT11)

The improvement is independent of the finite head, common q vertex,
residual coordinate, selected original prefix and chosen fixed tail
cut. This uniformity is what permits reuse of existing denominator
head certificates without repeating their enumeration.

## 3. Exact transport of the five existing denominator certificates

For an original hinge combination f=sum_t a_t*(x-t)_+,134's fixed
tail constant contains precisely

    (sum_t a_t)*Zplus.

Replace only Zplus by(WT10). Each of the seven coordinate values at
every original q vertex decreases by(sum_t a_t)*DeltaZ. The existing
head maxima, mean correction, selected-source error, exponent
omissions, affine tail slopes and original cut remain unchanged.
Therefore this is exact constant transport of an already proved
bound, not a claim that an unexamined head maximum is unchanged
after arbitrary parameter changes.

The helper reconstructs the complete tail supports and checks that
every coordinate has exactly this difference. The weighted five-row
denominator cost decreases by

    0.003451555064765087... .                     (WT12)

All five original denominator objectives are retained; their
1,875,000 previously checked head evaluations are bound by the146
certificate and its complete source pins.

## 4. Complete the three formerly missing numerator groups

The tighter source caps and budgets change the head LPs of the
mean11, single-H2 and joint-quadratic9 groups. Their old certificates
store maxima and hashes, not every head's dual or objective slack.
They therefore do not by themselves identify the new exact maxima.

The helper evaluates precisely these21 missing original objectives,
each at the same three common polygon vertices. It retains all
12,500 original layouts and all10 positive-seven choices for every
objective and vertex:7,875,000 head evaluations, with252 independent
rational checks of the integer LP and selected-cylinder operators.
The five already certified denominator objectives are reused as in
section3. The computation neither narrows the source domain nor
substitutes a different test inventory.

Each mean correction remains on its own original head. The nine
quadratic rows retain113's same-head factorial correction and136's
complete positive pair tails. Original index47 retains the separate
raw square357(81/4) controller, and index46 retains raw square357(81).
All infinite polynomial and geometric count tails remain included.

The new Zplus is inserted in every complete support before taking
the common q/residual maximum. The residual budget is still one
simplex for all original weighted rows, as in137 and140. No cost
gets its own independently maximized q or residual coordinate.

## 5. Assemble the original signed comparison with both masses

Use151's original branch transport for the two heavy costs, with
153's improved N,D norms and the exact zero-seven raw cancellation.
Writing a=53/360, each cost satisfies

    Cost_i<=F_i+C_i*((S-a)-(s-1/4))+J_i,

where F_i is its original adopted face bound and J_i is the complete
branch error. The two global mass floors S>=a+rho and s>=1/4 are
retained until the signed comparison. No separate S-s payment is
subtracted again.

Original index40 has unit term S, and the complete square has one
unit-unit term S. Both139/143 and152 expose exactly the same bound
Supper=a+5delta/9+rho0 for that square term. The assembled endpoint
therefore uses

    square_endpoint=a+Qupper-Supper,            (WT13)

and the single-hinge mass terms are evaluated at a. This preserves
the actual S coefficient; it does not treat an endpoint as an
unconditional numerator upper.

Let CH=sum_(i=0,16)w_i*C_i and let cS,cQ,C0,w40 have their original
meanings. For a comparison value T set

    DS=(T-C0)*(1-1/614922)-cS-CH-w40-cQ.

The original full target margin has the bound

    (T-C0)*E-N_actual
       >=DS*(S-a)+CH*(s-1/4)+(T-C0)*d-Nendpoint,

where d includes all five denominator objectives and the complete
H1/55902 payment. Both columns below have d>0, CH>0 and DS>0.
Taking T=C0+Nendpoint/d closes the comparison exactly; the favorable
DS*rho term is discarded. The one original residual is still paid
inside the complete branch and tail bounds.

| Complete comparison | H1/Q from139/143 | H1/Q from152 |
| --- | ---: | ---: |
| Actual denominator lower bound d | 0.07847116309267557 | 0.07847592754298640 |
| Signed endpoint | 36.74696981647671 | 36.37882767477974 |
| Full comparison T | 489.8804182271787 | 485.1608401342302 |
| Margin below509 | 19.1195817728213 | 23.8391598657698 |

The second column binds152's complete API and source certificate at
exactly delta=1/27,rho0=1/100000. Its H1 and Q bounds are
0.516418822242547 and5.181021797971829. No rounded value is used in
the comparison. The first result is retained to exhibit a complete
wide-radius theorem independent of the additional152 improvement.

The [helper](../../frontier/comparison-bounds/wide_k_signed_tail_comparison.py) checks the
exact52-label partition, all source pins, every transported
denominator coordinate, all new head maxima, the original complete
raw controllers, the square unit term, and both positive signed
mass coefficients. The [certificate](../../certificates/source_norms/comparison-bounds/wide_k_signed_tail_comparison.json)
contains the exact rationals and full retained head/tail data.

```sh
python3 -I -O docs/reports/erdos7-odd-covering/frontier/comparison-bounds/wide_k_signed_tail_comparison.py --check
```
