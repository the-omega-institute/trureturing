[Index](../../marked_head_profile.md) · [Previous complete global comparison](163-the-complete-local-domain-removes-the-zero-escape-ceiling.md) · [Complete local shells](165-the-pure-five-joint-envelope-enters-both-complete-comparisons.md) · [Wider source domain](166-the-complete-local-source-domain-crosses-the-outer-strip.md)

# Two complete source neighborhoods remove the outer crossing

Joining165's complete local domain with166's wider source domain
gives the ordinary complete global comparison

    K<=509.3013429354616... .                    (UN1)

The exact rational is retained in the certificate. Every original
fallback and both terminal errors remain; the smaller sufficient
gap above403 is106.30191793577973... . The global comparison does
not resolve unrestricted Erdos7. No Lean or frozen-state claim is
made, and the canonical118 certificate is unchanged.

The outer marked/unmarked crossing of163 no longer controls. It
belongs to the new complete local domain when the actual residual
is small. When the residual is larger, its original unmarked mass
reserve suffices. The remaining endpoint is sigma=0,rho=1/2600
in the complement of the old slot-loss cutoff. This is an endpoint
of the retained inequality system; no actual finite family is
claimed to attain it.

## 1. Use each complete local comparison on its own source

The two complete actual-source domains are

    D0: sigma<=s0=1/27, rho<=R0=1/1000,
        r<=r0=1/520;
    D1: sigma<=s1=1/22, rho<=R1=1/15000.         (UN2)

Both K orientations are included. The same actual source variables
satisfy r<=5rho everywhere. In particular D1 implies
r<=1/3000, inside166's proved range.

165's two closed shells cover every residual in D0 and give the
complete bound508.9604579570802... .166 gives the complete bound
477.2648939507091... on D1. Both are strictly below(UN1).
Each includes all52 original independent costs, the same actual
mass, the complete denominator and every infinite tail.

Outside D0 union D1, split according to the actual sigma:

- If sigma<=s0, then rho>R0 or r>r0. Since r<=5rho,
  rho>=u0=min(R0,r0/5)=1/2600.
- If s0<=sigma<=s1, absence from D1 gives rho>=R1.
- If sigma>=s1, retain the original zero residual lower bound.

Weak boundaries are included in the adjacent valid comparisons.
No individual local gain is transferred to a different source.
The two residual thresholds come from the complement of complete
local domains, not from assigning independent values to r and rho.

## 2. Retain the marked reserve only where it is needed

Use147/155's original constants and46 fixed outer alternatives:

    a=53/360,
    B(sigma)=B0-B1*sigma+B2*sigma^2,
    e(sigma)=gamma2*sigma-(gamma2-gamma1)*sigma^2,
    A=54.27497997750398...,
    P=37.97187303103495...,
    Q=0.515666653007543...,
    L5=5L=13.762429399039852... .

The marked theorem holds through sigma<=21/500 and r<=3/1000.
It is used here only on the smaller interval sigma<=s0=1/27.
With rcut=3/1000 and target decrement h, its complete signed
comparison is

    Phi>=B(sigma)-Q*sigma+e(sigma)
        -h*(a+sigma/10)+(A-h-P-L5)*rho.         (UN3)

The slot penalty was paid once using r<=5rho. On the complement
of D0 it is legitimate to put rho=u0 if A-h-P-L5>=0.
The polynomial in sigma is concave; its endpoints0,s0 suffice.

For sigma<=s0 and r>=rcut, discard the marked credit together
with its penalties. Discarding the nonnegative escape reserve
as well gives the valid complete lower bound

    Phi>=(A-h)*rcut/5-h*(a+s0/10).             (UN4)

The two alternatives(UN3),(UN4) include every r on the low source
strip. All original cost labels and their complete tails remain
inside the inherited theorems.

## 3. Use the unmarked residual reserve across the new bridge

On s0<=sigma<=s1 outside D1, retain the original unmarked reserve
and the proved actual mass bound of154:

    Phi>=e(sigma)-h*(a+sigma/10)+(A-h)*rho
       >=e(sigma)-h*(a+sigma/10)+(A-h)*R1.      (UN5)

No marked cost losses P or L5 occur in this branch. Their credit
is also absent. The residual belongs to this same actual source.
Since(UN5) is concave in sigma, only s0,s1 need to be checked.

Above s1 the unchanged zero-floor branches are

    e(sigma)-h*(a+sigma/10),      s1<=sigma<=1/3;
    e(sigma)-h*(a+5sigma/9),      1/3<=sigma<=1/2;
    e(sigma)-h*[1/4+11sigma(1-sigma)/36],
                                  1/2<=sigma<=1.             (UN6)

The second expression at1/2 is its left-limit polynomial bound;
no theorem with a strict domain is applied to an actual boundary
point. Both sides of each mass-bound switch are retained.
Every polynomial in(UN6) has the required concavity at the chosen h.

## 4. The complete endpoint minimum and remaining boundary

Set

    h=[B0+(A-P-L5)/2600]/[a+1/2600]
     =0.15934558022178108... .                  (UN7)

The eleven exact endpoint decrement capacities are:

| Complete outside endpoint | Capacity |
| --- | ---: |
| Low source marked, sigma=0 |0.15934558022178108...|
| Low source marked, sigma=1/27 |0.1610786593586812...|
| Low source, large r |0.21491363796332725...|
| Residual bridge, sigma=1/27 |0.16176630646982543...|
| Residual bridge, sigma=1/22 |0.19066738014705247...|
| Early middle, sigma=1/22 |0.16690987844039319...|
| Early middle, sigma=1/3 |0.74470281249334932...|
| Old middle, right side at1/3 |0.40450431319276634...|
| Old middle, left limit at1/2 |0.36973323707382434...|
| Far, sigma=1/2 |0.48143987465783084...|
| Far, sigma=1 |0.18780374299724043...|

Only the first endpoint has equality at(UN7); all other signed
margins are positive. The one-residual coefficients A-h-P-L5 and
A-h are positive. All eight original fallback comparisons are
strictly below K0-h, and both terminal errors are unchanged.
The original positive survival factor justifies division.

Thus K<=K0-h gives(UN1). The comparison improves163 by
0.004794136403373943... . Improving constants strictly inside
D0 or D1 does not change the new controlling endpoint. Expanding
their applicable domain, or strengthening the same-source marked
reserve at that slot-loss boundary, is needed to change it.

The residual width needed just for the bridge's lower endpoint
at this target is exactly

    Rcrit=[h*(a+s0/10)-e(s0)]/(A-h)
         =0.00005991239433446405...<1/15000.    (UN8)

The other bridge endpoint and the zero-floor endpoint at s1 are
separately checked. In particular(UN8) alone is not asserted to
characterize all possible local domains or global partitions.
It explains why a wider source radius must also retain enough
residual width for this chosen proof.

The [helper](../../frontier/source-budgets/union_source_outer_comparison.py) and
[certificate](../../certificates/source_norms/source-budgets/union_source_outer_comparison.json)
pin the complete local inputs, reconstruct the previous global
partition, and check the new exhaustive complement, every signed
endpoint, all original fallbacks and both complete terminal errors.
No new original-head scan is needed for this scalar consumer.

```sh
python3 -I -O docs/reports/erdos7-odd-covering/frontier/source-budgets/union_source_outer_comparison.py --check
```
