[Index](../marked_head_profile.md) · [Complete radius](146-a-global-mass-floor-and-coupled-costs-expand-the-k-radius.md) · [Common factor budget](153-the-six-source-losses-share-one-actual-concentration-budget.md) · [Original heavy branches](142-both-heavy-vector-controllers-have-a-uniform-source-radius.md)

# Shared source losses and signed masses strengthen the local comparison

On the complete actual-source rectangle

    qK>=1-1/50, 0<=rho<=1/100000,

the unchanged original52-cost comparison satisfies

    K<=492.4130990598792... .                     (SM1)

This improves146's499.3946903946737 on the same rectangle. The
improvement uses153's one budget for six source losses and retains
the two actual masses S and s until the signed target comparison.
All52 original labels, the five denominator objectives, every
infinite tail and the one actual residual variable are retained.

At delta=1/27, the corresponding optimistic envelope is

    506.6820988739958... .                        (SM2)

This second value is not a complete bound: the mean11, single-H2
and joint-quadratic9 positive neighborhood increments remain unpaid.
It removes146's specific519.9054 obstruction, but does not prove
K<=509 at delta=1/27. No new global K or unrestricted Erdos7 result
is claimed. These are ordinary continuous-source proofs with exact
rational checks, not Lean theorems.

## 1. Transport each original heavy branch with one source budget

Use142's actual first-beta projection, which fills the missing
root1 beta mass into the actual first-beta cell. This retains the
whole admissible95 face and both original root0 orientations.
For0<=sigma<=delta<1/2,153 proves

    sum of the six actual factor losses<=delta/(1-delta).

Its original factorwise source estimates therefore give

    N=||n-n*||1<=delta/[6*(1-delta)],
    E=||eta-eta*||1<=delta/9,
    D=||d-d*||infinity<=delta/[4*(1-delta)].      (SM3)

The underlying concentration formula contains both original
orientation contributions. Their cubic cancellation, followed by
the common-factor product bound, supplies the shared budget. Thus
(SM3) is valid on the actual source domain; it does not assume the
six losses can all attain delta independently.

Fix i=0 or16 and one of its original100 branches. As in146, write
its original margin as

    M_i,b=C_i*s+G_i,b.

The complete raw difference in G is raw357(f)-raw35(f_zero).
Its zero-seven block is identical on both sides. Cancel it exactly
once, retaining the price vector

    L_remaining=L_full-L_zero.                  (SM4)

The helper checks the first complete seven block agrees with the
entire zero block in all three coordinates. The remaining complete
prices retain every finite block and the original infinite tail.

Now reuse142(HV4)--(HV14) with N,E,D from(SM3), removing C_i*N
because C_i*s has been kept outside G. Explicitly, for the branch
vectors psi, correction, derivative maximum v, barrier-deficit
maximum k, and positive-five correction maximum c, put

    Lscore=2delta*(k/4+c/10)+2*(k*N+c*E/5),
    Lrest=k*D/18+3k*E/4,
    LoldG=Lremaining.NED+||psi||infinity*N
         +||correction||infinity*E
         +Lzero,d*D+Pplus_zero*E+(Lscore+Lrest)/5.

Here Lremaining.NED denotes its scalar product with(N,E,D).
The non-H cap and budget increments, hplus, carrier-score change,
selected pure-coordinate reciprocal estimate and complete residual
coefficient remain exactly142's. Only the occurrences of N and D
use(SM3). Thus

    J_i,b=LoldG+v*max(LnonH,LH)+2*(13/1215)*k*D
                         +Pbar*rho0             (SM5)

is a uniform loss bound for G_i,b. The source-domain guards
rbar<1/2500 and D<=1/18 are checked at both positive radii.
The100 branches retain their original baseline and positive-five
labels. Set J_i=max_b J_i,b.

The95 face bound applies to every branch and all allowed beta
points. Consequently, writing F_i for its adopted face cost,

    Cost_i<=F_i+C_i*((S-a)-(s-1/4))+J_i,
    a=53/360.                                   (SM6)

This comes from C_i*S-min_b(C_i*s+G_i,b) directly. It does not
start from146's already-improved scalar cost and subtract its
shared-mass saving a second time. In particular, (SM6) keeps both
S and s and does not pay even the improved153 bound on S-s.

## 2. Retain the other exact unit-mass terms

In141, original index40 has precisely

    Cost40=S+H1.

In143's complete square, the unit-unit term is precisely S:

    Q<=S+Qrest_upper.

The bounded nonunit cylinders, all weighted LCM tails, complete
mixed pair series and positive-seven contribution stay unchanged.
The helper reconstructs the full square decomposition and checks
that index40 is the only single-hinge row with a unit-mass term.

Let cS be the signed mass coefficient, cQ the complete square
weight, w40 the original weight of index40, and

    CH=sum_(i=0,16) w_i*C_i.

After(SM6) and these two unit terms are inserted, the complete
original numerator satisfies the single inequality

    N_actual<=(cS+CH+w40+cQ)*S-CH*s+R.           (SM7)

R retains the other50 original costs, the heavy face constants and
the weighted J_i, the nonunit square terms, and the original52
label partition. It is an upper bound on the remaining expression
only; no positivity of each individual summand of R is required.

The same original denominator theorem139 gives

    E_actual>=cE*S-P,
    cE=1-1/614922,
    P=Pden_upper+H1_upper/55902.                (SM8)

All five original denominator objectives and their exact shared
finite-head maximization and tails are retained from146.

## 3. Both masses have lower bounds with favorable signed coefficients

The original raw-source caps give the global inequality

    s=[z*sum w-sum_r alpha_r*(root sum w)_r
                       -sum_j beta_j*w_j]/9-sum late
      >=[(3/4)*(9/2)-(1/4)*3-1/4]/9-1/72
      =1/4.                                    (SM9)

This is153's direct source-mass floor. Independently,146 proves
S0>=a across the whole source product and all18 carriers; hence
S=S0+rho>=a+rho. Neither floor is a lower bound for the final
survival denominator before its original losses are paid.

For a target T>C0, where C0 is the unchanged comparison offset,
set

    DS=(T-C0)*cE-cS-CH-w40-cQ,
    d=cE*a-P,
    Nendpoint=(cS+CH+w40+cQ)*a-CH/4+R.

Combining(SM7)--(SM9), with DS>=0 and CH>0, gives

    (T-C0)*E_actual-N_actual
       >=DS*(S-a)+CH*(s-1/4)+(T-C0)*d-Nendpoint
       >=DS*rho+(T-C0)*d-Nendpoint.             (SM10)

Therefore, if d>0 and T=C0+Nendpoint/d with DS>=0, the full target
inequality follows. The endpoint Nendpoint is not an unconditional
upper bound on the actual numerator: its validity is in the signed
comparison(SM10). The favorable mass coefficients are required.

For calculation from146's cached complete rectangle, this endpoint
can be evaluated exactly as

    Nendpoint=N146-old_heavy_excess
                  +sum_i w_i*J_i
                  -(w40+cQ)*(Supper146-a).      (SM11)

Equation(SM11) is only an algebraic evaluation of(SM10), after the
original signed derivation. The old heavy excess is removed once,
the new G error is added once, and the exact unit masses are retained
once. The single rho budget remains in(SM5); the favorable DS*rho
term is discarded, so no residual credit is spent twice.

## 4. Exact comparison results and the remaining unpaid terms

For delta=1/50 and rho0=1/100000, the retained data are

| Quantity | Exact-certificate decimal rendering |
| --- | ---: |
| Original146 denominator lower bound | 0.07719421042741008 |
| Original146 heavy excess | 1.0177460706768808 |
| New weighted G error | 0.48117770790770975 |
| Exact unit-mass saving in(SM11) | 0.00237006784713933 |
| Signed endpoint | 36.34449868782721 |
| Complete comparison T | 492.4130990598792 |
| Remaining coefficient DS | 401.9703361331724 |

The helper checks d>0, CH>0, DS>=0 and exact zero of
(T-C0)*d-Nendpoint. All52 costs are present at this radius, so
this is the complete theorem(SM1), with no head re-enumeration.

For delta=1/27, the input146 certificate was an obstruction
calculation, containing only selected positive increments above
the face bounds. It omitted mean11, H2_single and joint_quadratic9.
The same substitution lowers its optimistic value from519.9054
to506.6821, with DS=416.2393127427202. Since this lies below509,
that particular lower bound on the chosen envelopes no longer
obstructs the target. The missing positive groups must still be
evaluated and paid before any complete theorem at this radius.

In particular, the numerical improvement does not permit inferring
that one may widen the rectangle or replace a global fallback.
The global covering problem still requires the remaining source
regions and its original prime-tail interface.

The [helper](../frontier/shared_loss_signed_k_comparison.py) binds
the original146 certificates and153's budget API by exact hashes.
It reevaluates every original heavy branch, recovers the face bounds
at zero radius, and checks the G transport against the actual91
branch formulas at finite398 sources of heights12 and15. These400
finite comparisons supplement the ordinary proof; they do not replace
its continuous-domain argument. All validation uses explicit runtime
checks under Python optimization.

The [certificate](../certificates/source_norms/shared_loss_signed_k_comparison.json)
contains exact rationals, all heavy branch prices, the complete
source pins, the two different scopes, and the actual-source checks.

```sh
python3 -I -O docs/reports/erdos7-odd-covering/frontier/shared_loss_signed_k_comparison.py --check
```
