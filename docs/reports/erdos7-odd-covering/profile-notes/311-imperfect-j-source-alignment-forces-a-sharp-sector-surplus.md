[Index](../marked_head_profile.md) · [Whole J face](130-the-whole-j-face-forces-source-anti-alignment.md) · [Actual J neighborhood](309-an-explicit-actual-j-neighborhood-keeps-the-complete-comparison-below403.md)

# Imperfect J source alignment forces a macroscopic mass surplus

For the original effective9 actual source, let qJ be71's weight of the
two whole J zero faces. Write S0=sum_c pi_c D_c and rho=S-S0. Assume

    qJ>=1-delta, 0<=delta<=1/1000.

There is no hypothesis that rho is small. Fix the orientation supplied
by71 and let L be the surviving mod9 cell of original45. The original
135 is present and lies in a root1 cell M. If M=L, then

    S-D >= (2/5)*(1/135-lambda),
    rho >= 2/675-7delta/36 >= 299/108000,          (A)

where lambda=1/72-sum_root1 late<=delta/72. The last constant is
approximately0.0027685185185. All inequalities use the same actual
family and normalized survivor mass. Original test labels remain
independent and no numerator or denominator test is removed.

A continuous companion inequality uses only the actual late mass in L:

    S-D >= (2/5)*(late_L-1/360-4lambda)_+,
    rho >= max(0,
       (2/5)*(late_L-1/360-delta/18)_+-17delta/90). (B)

More precisely, if x_L is the additional assigned late mass in L from
the full b=1 family, the first right-hand side in(B) may be replaced by
(2/5)*(x_L-4lambda)_+. Every assignment of the late union to its
original labels satisfies this statement.

The exact-face aligned-sector floor2/675 is sharp: the actual finite
dispersed-beta families below have qJ tending to1 and rho tending to
2/675. This does not assert that the delta error coefficient is sharp.

These are restrictions on realizable source parameters. They do not
prove J<403, K<403, Gamma19<403 or a covering contradiction. The
old53 J expression at403 has signed mass coefficient

    -1084633310562408667108430507811834753021649
     /405362309383496544679226798448256980000000
      = -2.6757132704616646... .                  (C)

Thus merely inserting the larger mass lower bound(A) into that
expression would reverse the required inequality. A new comparison
consumer is still needed to turn this compatibility result into an
additional403 region.

## Source hypotheses and the four forced slots

Only the concentration consequences in71 and the source geometry in136
are used here;136's separate rho bound is not imported. They give

    z<=3/4+delta/4,
    alpha1>= (1-delta)/4,
    sum_root1 beta>= (1-delta)/4,
    sum_root1 late>= (1-delta)/72,
    1/2<=h<=1/2+delta/18,
    h0<=1/6+delta/18, h1>=1/3-delta/18,
    eta_i>=1/9-delta/18 for i in root1,
    0<=S0-D<=17delta/90.

The final inequality uses delta<=1/18, which holds on the stated domain.
The two orientations merely exchange the two root0 cells.

Let P,A,B be the first-five slots of original5,15,45. If any leading
label is absent or ineffective in its respective source stage, the
remaining pure5, alpha or beta raw budget is at most1/20. The preceding
concentration bounds exclude this. Original15 and45 must lie in root1,
and P,A,B are distinct. This uses the total root1 beta mass, not the
mass in one preselected beta cell.

Original25 is present and its first-five parent Q differs from P,
since otherwise the pure5 capacity loss is at least1/25 while
z-3/4<=delta/4<1/25. If Q=A, original15 loses at least1/25 of its
five-coordinate width. The whole alpha budget is then at most
1/5-1/25+1/20=21/100, below(1-delta)/4. If Q=B, the identical
argument applies to the whole beta budget. Hence P,A,B,Q are four
distinct slots. Denote the remaining slot by H.

This proof also gives beta_L>=1/5-delta/4 by charging all b>=2 beta
labels to their complete tail1/20. It makes no assertion that the
other beta coordinates vanish.

## One assigned budget controls every late label

Partition actual additional late deletion in root1 into disjoint
pieces assigned to original labels3^a5^b, a>=3,b>=1. The ordering is
arbitrary. For its assigned mass q_(a,b) and full raw cap c_(a,b),

    c_(a,b)=3^-a*5^-b,
    0<=q_(a,b)<=c_(a,b),
    sum c_(a,b)=1/72,
    sum(c_(a,b)-q_(a,b))=lambda.                 (D)

Absent labels and all exponents beyond a finite family's height have
assigned mass zero. Thus(D) retains their full unused geometric tails.
Assignments outside root1 also contribute zero to this root1 ledger.

For original135, (D) implies q_(3,1)>=1/135-lambda>0, so it is present
and lies in root1. If M=L, its first-five slot cannot be P,A,B:
original5,15 or45 respectively contains its whole rectangle at the
appropriate source stage. If its slot were Q, its raw intersection
with original25 would be exactly1/675. This would give
c_(3,1)-q_(3,1)>=1/675>lambda, again impossible. Consequently the
135 rectangle uses H, and its assigned mass there is at least
1/135-lambda. No survivor mass or mixed7 assumption enters this step.

For the entire b=1 family assigned in L, every contribution outside H
must be in Q. Each such label loses at least one fifth of its full
raw budget to original25. Hence

    q_(a,1)<= (4/5)c_(a,1)
             <=4*(c_(a,1)-q_(a,1)).

Summing only these assigned pieces costs at most4lambda. The additional
source deletion in(root1,H), and therefore in the full H column, is
at least(x_L-4lambda)_+. Moreover

    x_L>=late_L-1/360,
    sum_(a>=3,b>=2)c_(a,b)=(1/18)*(1/20)=1/360,
    0<=x_L<=sum_(a>=3)c_(a,1)=1/90.              (E)

All pieces used in this estimate are assigned source deletions; no
disjointness of the original forbidden cylinders is assumed.

## The two complete mixed7 families pay this loss

Let g denote either1/135-lambda in the aligned leading case, or
(x_L-4lambda)_+ in the complete same-cell case. Then0<=g<=1/90.
For an arbitrary old cofactor5 cylinder, its source loss relative
to h/5 in P,A,B,Q is at least

    h/5, h1/5, eta_L/5, h/25,

respectively, and its loss in H is at least g. Each of the four
displayed lower bounds is at least1/90 on the stated domain.
Therefore every independently chosen cofactor5 carrier has raw mass
at most h/5-g.

For a cofactor15 carrier in root1, the corresponding four losses are
h1/5,h1/5,eta_L/5,h1/25, again all at least1/90. A root0 carrier
has mass at most h0/5, losing at least(h1-h0)/5>=1/90 relative to
h1/5; a carrier on the killed ternary root has zero mass. Thus every
cofactor15 carrier has mass at most h1/5-g, independently of the
cofactor5 choice or its seven residue.

The complete normalized seven-cylinder caps are

    u_e=6/(5*7^e), sum_(e>=1)u_e=1/5.

Replace only the cofactor5 and cofactor15 terms in48's old cap sum C
by these smaller bounds. All other cofactor families are unchanged,
and the complete raw cap sum is reduced by2g. The actual mixed7 union
bound consequently gives

    S>=s-(C-2g)/5=D+2g/5.                       (F)

This includes every positive seven depth, all missing labels and all
cofactor tails. It uses no overlap credit and no common choice of the
two independent old carriers. Combining(F) with S0-D<=17delta/90,
lambda<=delta/72 and(E) proves(A) and(B), including delta=0.

## An actual finite source with dispersed beta

The following witnesses show that(A) applies beyond48/50's same-cell
scalar corrections, and attain its aligned-sector floor as delta tends
to zero. They are actual families with one original residue per nonunit
modulus in the finite box0<=a,b,e<=N. They are not asserted to cover
the integers.

Use cells C=(0,3,1,4,7) mod9. Put

    T_a(c)=[c+3^(a-1)] mod3^a, a>=3,
    F_(j,b)=[j*5^(b-1)] mod5^b,
    G_(j,e)=[j*7^(e-1)] mod7^e.

The pure3 source labels are[2]mod3,[6]mod9 and T_a(0). Pure5 uses
F_(1,b); the alpha labels use root1 times F_(2,b). For beta, original45
uses C2 times F_(3,1), and every b>=2 label uses C3 times F_(3,b).
Original135 uses T_3(1) times F_(4,1). Every other deep mixed35 label
uses T_a(7) times F_(3,b). All these late rectangles avoid the earlier
source exclusions, and their distinct(a,b) rectangles are disjoint.

Write

    t_N=(1-3^(2-N))/18, q_N=(1-5^-N)/4.

The actual source parameters are

    deficit=(9t_N,0,0,0,0), alpha=(0,q_N), z=1-q_N,
    beta=(0,0,1/5,q_N-1/5,0),
    late=(0,0,1/135,0,t_N*q_N-1/135),
    eta=(1/9-t_N,1/9,1/9,1/9,1/9),
    s=5/9-t_N-q_N.

In particular beta_L=1/5, well outside50's concentrated-beta
neighborhood. Its global correction, and48's global scalar correction,
both vanish in all three root1 cells on these witnesses.

For every positive seven depth, choose the following original old
carriers and seven digit class. Pure7 uses class6.

| Cofactor | Old carrier | Seven class |
|---|---|---:|
|3|root0|1|
|9|C1|2|
|3^a, a>=3|[2*3^(a-1)] mod3^a|2|
|5^b|F_(4,b)|3|
|3*5^b|root1 times F_(4,b)|1|
|9*5^b|C1 times F_(4,b)|5|
|3^a*5^b, a>=3,b>=1|[7+2*3^(a-1)] mod3^a times F_(4,b)|5|

Distinct classes and seven depths have disjoint G cylinders. Within
class1 the two types have different ternary roots. Class2 uses cell1
and disjoint deep cylinders in cell0. Class3 has distinct F cylinders.
Within class5 the a=2 carriers use cell1 and the deep carriers use
cell4; the latter are separated by their ternary or five depths.
Thus all mixed7 rectangles are disjoint before source restriction and
avoid the pure7 source.

The five actual old-carrier category totals, for pure3, pure5,
3*5^b,9*5^b and deep mixed35 respectively, are

    (1-q_N)/3,
    (5/9-t_N)*q_N-1/135,
    q_N/3-1/135,
    q_N/9,
    t_N*q_N.

The only loss is the original135 rectangle in the first F4 cylinder,
charged once to the pure5 category and once to the root1/5 category.
Those are distinct complete mixed7 families. The deep F4 cylinders
with b>=2 are disjoint from that first F4 cylinder and all source
F1/F2/F3 cylinders, so there is no additional omitted tail loss.

For the actual raw five-cell vector n, this gives exactly

    H_N=1/3+2q_N/3-2/135,
    S=s-kappa_N*H_N,
    kappa_N=(1-7^-N)/(5+7^-N),
    pi_(0,1)=1-7^-N, pi_empty=7^-N,
    qJ=(1-3^(2-N))^2*(1-5^-N)^4*(1-7^-N).

The limiting raw cell vector is

    n=(1/24,1/12,7/270,1/20,53/1080).

Its roots both have mass1/8, its largest cell has mass1/12 and its
complete old cap sum is1/2. Hence s=1/4 and D=S0=3/20, since the
limiting carrier is(root0,cell1). Consequently

    S ->1/4-(1/5)*(1/2-2/135)=3/20+2/675,
    rho ->2/675.

This supplies an actual matching sequence for the aligned-sector
floor, while beta tends to(0,0,1/5,1/20,0). It does not imply
simultaneous attainment of any independent test envelope.

Original15,27,45,135 are all effective at305's source stages. Thus
none of305's four sufficient ineffective branches is invoked. At
N=10 there are1330 original labels, delta=0.0003052213641188871...,
rho=0.0029646618565059356..., and(A)'s lower bound is
0.0029036143643842906.... At N=12 there are2196 labels and the lower
bound is0.002956373951530501..., while its actual rho is
0.0029631513408079776.... No upper bound on rho was used to force
the source slots.

The checker verifies disjointness, every source stage and all five
actual mixed7 category masses by exact cylinder intersections at
N=3,4,10,12. At N=3 it also
computes the union of all63 original CRT residue classes on the full
period105^3=1157625 and checks the independently obtained normalized
survivor mass. The all-height result itself follows from(D)–(F), not
from the finite examples.

## Relation to the outstanding source domain

After301/305, a containing domain still requiring other arguments has
effective9, effective15 and at least two effective labels among27,45,135.
After the local309 result, sources outside its J region satisfy

    (1-qJ)+(S-S0)>10^-14.

This describes a containing outstanding domain, not an exact complement:
other published K boxes and300's numerical source thresholds can remove
additional parts. It also does not identify a J comparison with the
combined K or the later Gamma19 target.

The new compatibility constraint excludes, within qJ>=1-delta and
delta<=1/1000, every aligned45/135 parameter point with
rho<2/675-7delta/36. It strengthens130's exact-face anti-alignment to
an imperfect source without imposing any upper bound on rho. It also
extends beyond306's small total-error domain, which forces the two
leading cells to differ only after assuming delta+rho<=1/1000.
No new403 comparison is claimed for the remaining aligned sector.

The [portable helper](../frontier/j_leading_alignment_surplus.py) and
[certificate](../certificates/source_norms/j_leading_alignment_surplus.json)
record exact fractions, source pins, the four finite families and(C).
The universal source inequalities and the matching limiting family above
are ordinary mathematical proofs, not Lean verification.

```sh
python3 -I -O docs/reports/erdos7-odd-covering/frontier/j_leading_alignment_surplus.py --check
```

This check reconstructs the finite actual families and complete geometric
identities. It does not rerun an original source scan, solve an LP, price
a dual bank or infer a universal inequality from finitely many examples.
