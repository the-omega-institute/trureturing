[Index](../../marked_head_profile.md) · [Previous](32-unequal-source-norms-sharpen-the-uniform357-input.md)

<a id="retained-and-removed-original-events-control-convex-costs"></a>
### Retained and removed original events control convex costs

For the complete original odd-modulus families and actual AP probabilities
of the preceding sections, the joint row potential satisfies

    F17^-(403;nu13)+F19^-(403;physical mu17)
      <=8734942982720765439105436604602413448183379
          /19866883931462419217748041163036838905600
       =439.673529721869... .

The three steps below strengthen the uniform357 source square, bound the
whole linear costs under the actual normalized probability, and apply the
same observation to the quadratic AP cost. Every original modulus label
and all infinite comparison tails are retained. These are ordinary proofs
with exact rational certificates, not Lean results or an unrestricted
Erdős #7 resolution. The prior OS estimates are in the
[preceding section](32-unequal-source-norms-sharpen-the-uniform357-input.md).

### Retained or removed mass pays for six original cofactors

On the same complete actual uniform357 survivor law, a joint square and
deletion estimate gives

    Gamma357<=765767/21465=35.675145585837... .          (KRM1)

The improvement from OS1 is11968/21465. The new observation is the
complementarity of the same original modulus5 test's retained and
removed mass. It applies to six actual pure3 cofactor labels, keeping
every other original label and the full remaining tail. The effective
cofactor expression below is part of a joint bound; it is not a
standalone upper bound for SD3's weighted deletion term.

#### One enlargement loss and one actual retained event

Fix an original zero7 test A0, its zero5 block B0 and its original
modulus5 event J as in OS. Retain the raw pure product eta times P5,
the actual raw35 restriction lambda35, and h=Haar(P5 intersect J).
In each cell l put

    q_l=lambda35(cell_l intersect J),
    e_l=eta_l*h-q_l>=0,
    c_l=2 b_l+1, k_l=C-b_l^2,
    K=sum_l c_l eta_l, R=sum_l c_l e_l.

Thus e_l is the actual mixed35 removed mass inside the same J, not a
new independently chosen event. Write A0=B0+V. The positive increment
satisfies2 B0 V+V^2>=c_l 1_J. Enlarging its integral from actual35 to
the pure product overcounts by at least R. The ensuing pure-product
comparison still saves the strip(1/5-h)K from OS2. Hence

    integral_lambda35 A0^2<=U_(r,j)-(1/5-h)K-R.          (KRM2)

These two deductions occur successively in one upper-bound chain.
The first measures actual removed mass; the second compares the
remaining pure-product integral with auxiliary nested intervals.
No five-coordinate residues of different labels are identified.

#### Six actual cylinders spend one common loss budget

Choose the six actual pure3 cofactor cylinders E_a, a=1,...,6, used
in a mixed7 deletion comparison. They may have unrelated residues.
For each such cylinder set

    K(E_a)=integral_(eta restricted to E_a)c,
    T(E_a)=integral_(lambda35 restricted to E_a)c 1_J,
    R(E_a)=h K(E_a)-T(E_a)>=0.

Every point belongs to at most six of the chosen cylinders. Therefore

    sum_a R(E_a)<=6 R,  sum_a K(E_a)<=6 K,
    sum_a T(E_a)+6 R>=h sum_a K(E_a).                  (KRM3)

The same pointwise floor(C-A0^2)_+<=k-c 1_J bounds each deletion
integral by integral_E k d lambda35-T(E). Multiply KRM2 by6 and add
the six retained-event deletion estimates. KRM3 eliminates the hidden
intersections. The h-dependent part is

    -6(1/5-h)K-h sum_a K(E_a).

Its slope is6K-sum_a K(E_a)>=0. Thus its worst value on[0,1/5]
occurs at h=1/5. This maximization is made for actual cylinders,
before replacing their masses by upper caps. The six cylinders can
then be maximized independently as an upper bound, without asserting
that their maximizing choices occur together in an actual family.

For C>=16 the resulting first two terms are

    max_r sum_(root(l)=r)[k_l n_l-c_l eta_l/5],
    max_l[k_l n_l-c_l eta_l/5].

Empty or missing choices contribute zero. These maxima are positive:
their common total sum is at least7s-(7/5)sum_l eta_l>=35/36>0.
This uses s>=1/4 and sum eta_l<=5/9 from the existing domain.

For a cylinder of depth a=3,...,6 in cell l, the actual marginal
density is at most d_l. Consequently

    k_l lambda35(E_a)-c_l eta(E_a)/5
       <=(k_l d_l-c_l/5)eta(E_a)
       <=(k_l d_l-c_l/5)3^-a.

The last inequality uses k_l d_l-c_l/5>=7/20>0, since d_l>=1/4,
b_l<=3 and C>=16. It does not replace eta(E_a) by an equality.
The complete coefficient partition is

    sum_(a=3..6)3^-a=40/729,
    sum_(a>=7)3^-a=1/1458,
    40/729+1/1458=1/18.                               (KRM4)

Keep the four positive5-cofactor terms of SD3 unchanged. Define V_C
as their sum together with the first two corrected maxima above and

    (40/729)max_l(k_l d_l-c_l/5)
                      +(1/1458)max_l(k_l d_l).

The joint SD5 numerator now obeys

    s E_M[(L^2-C)1_(B^c)]
       <=(6/5)U_(r,j)+(7/15)U+V_C/5-C*s.              (KRM5)

The positive7 tests still use the independent global U. Every
original mixed7 cofactor selection is covered by KRM3, and its full
seven-coordinate cap sum is unchanged. Explicitly, if tau_e is the
pure7 cap at original depth e, then sum_e tau_e<=1/5, so
sum_e tau_e sum_(a=1..6) R(E_(a,e))<=(6/5)R. This is paid by the
same6R/5 in KRM2 after multiplication by its SD5 coefficient.
Only the displayed combined expression is claimed; V_C alone need
not dominate actual deletion.

#### Continuous extension and the complete consumer

Every branch in V_C is separately affine in the five original
parameter groups. Its maxima are separately convex, as are the
unchanged selected and global square bounds. The target margin in
KRM5 is therefore separately concave. At C=765767/21465, all12960
vertex/layout margins are nonnegative. The six zero-margin vertices
are398,410,422,616,628,640. The two missing-class source bounds
5273/258 and14543/438 are strictly smaller than C. Vertex interpolation
therefore proves KRM1 for every point of the continuous domain, with
all original finite heights and full comparison tails retained.

The bound is exact for this comparison formula: the available active
branches at vertex398/layout(0,1) give a target margin at most

    53 C/360-765767/145800.

This is a formula boundary, not actual-family sharpness. Independent
exact reconstruction checks all12960 rows and the two fallback
fractions. The original-labelled measure argument above supplies the
ordinary mathematical bridge; neither this check nor the source
certificate constitutes a Lean proof.

Propagate KRM1 through the same globally fixed caps and full tails
in YC4, with g=765767/21465-5761/159. All original AP costs and the
actual AP13 probability remain unchanged. This gives

    Gamma13<=74165888355209854559/475902450448556274
           =155.842627591653...,
    T13(81)<=120936974732058762228421006729484351/1221820521001849487003452539372000
           =98.980965414539...,
    F17^-(403;nu13)+F19^-(403;physical mu17)
      <=405160558934792762657900543938490087303057/903040178702837237170365507410765404800
       =448.662826405776... .                        (KRM6)

All1296 downstream vertices, the eight other branch records and
the complete tails pass at these targets. The three continuous
coefficients remain positive. The19 functional still uses normalized
physical mu17, distinct from the killed input in BM/RC.

Safe finite allowances are303.756 for the smaller unequal core and
304.018 for box20/current8. KRM6 remains144.644458820316... above
the larger exact allowance. The uniform negative-Q criterion and
arbitrary later-prime continuation remain open. These are ordinary
proofs with exact rational certificates, not a Lean endpoint or an
unrestricted Erdős #7 resolution.

### Convex cost floors retain the same original-unit5 event

Use the actual finite odd congruence family, complete original test
labels, actual source probability and AP11/AP13 construction of
DP10--DP11 and ABP2--ABP5 in the preceding sections. The original17
and19 tests remain independent. Assume the preceding ordinary source
and row-comparison lemmas, including the six-cofactor source square
bound G357=765767/21465. Then the following ordinary argument and
exact rational certificate bound the complete joint row potential by

    F17^-+F19^- <= J,
    J=4384639291527583263791309219261526466458877
       /9933441965731209608874020581518419452800
     =441.401812851365... .

Every original exponent height and all geometric tails are included.
This is a bound on the existing joint potential, not a proof of the
unrestricted odd-covering problem. The actual-label argument and exact arithmetic have been checked independently;
no Lean verification is claimed.

The matching bound without the new same-event rebates is

    J_floor=401036447272518196227558019141230049564607
             /903040178702837237170365507410765404800
           =444.095907059841... .

Against the SAME six-source comparison448.66282640577685, the gain
from retaining the nonzero deletion floors is4.566919345934977; the
additional gain from the same-unit5 event is2.6940942084768045. Their
sum is7.2610135544117815. G357 and U16 are held fixed in all three
quantities, so no source-square gain is subtracted a second time.

#### One increasing convex source cost

Fix a nonnegative increasing convex cost f on[1,infinity), with at most
quadratic growth, as for every delivered cost. Let A0
be the selected original zero7 complete35 test and b_l its zero5
ternary baseline in cell l. The ten possible baselines are

    b_l=1+1_(root(l)=r)+1_(l=j), r in{0,1}, j in{0,...,4}.

Let J5 be A0's one actual original unit-cofactor5 test. Set
h=Haar(P5 intersect J5)<=1/5, q_l=lambda35(cell_l intersect J5), and
v_l=eta_l*h-q_l>=0. No nonunit positive5 test is identified with J5.
For the original zero7 Jensen cost put

    psi0(v)=sum_(n>=1) p_n [f(nv)-f(n)]/n,
    p_1=29/35, p_n=36/(5*7^n) for n>=2,
    a_l=psi0(b_l+1)-psi0(b_l).

Convex secant monotonicity gives a_l>=f(b_l+1)-f(b_l)>=0: for each
integer n>=1 the n increments between nb and n(b+1) are at least the
increment between b and b+1, and sum_n p_n=1.

When the nonnegative positive5 increment of psi0(A0) is enlarged from
actual35 to the pure3 times pure5 product, the removed J5 part costs
at least V=sum_l a_l*v_l. The ensuing pure-product comparison with
this unit label's cap h, instead of1/5, additionally saves
(1/5-h)A where A=sum_l eta_l*a_l. These losses occur at distinct steps
of one inequality. The baseline term remains on actual35.

The cap comparison used here is valid for increasing convex costs:
for indicators with prescribed caps, the count hinge(sum I_i-k)+
is at most the sum of indicators outside any k-element set. Choosing
the k largest caps and integrating gives the sum of the remaining
caps. Nested intervals attain all these hinge bounds simultaneously.
Discrete convexity expresses the required cost as a nonnegative sum
of count and hinge costs. Shrinking only the unit interval from1/5 to
h therefore saves at least its width times a_l on cell l. All other
original residues remain separate labels.

For a fixed candidate norm C>=0 write

    k_l=(C-f(b_l))_+,
    c_l=k_l-(C-f(b_l+1))_+.

Then0<=c_l<=k_l, c_l<=f(b_l+1)-f(b_l)<=a_l, and the SAME test has

    (C-f(A0))_+ <= k_l-c_l*1_J5.

For every final constant in the certificate, exact arithmetic gives
C>=f(4). Thus the actual delivered constants use simply
k_l=C-f(b_l), c_l=f(b_l+1)-f(b_l). Every delivered constant uses this unclipped identity; the clipped
formula states the general pointwise bound.

#### Paying five actual pure3 cofactors once

At a fixed positive7 depth retain the actual cofactor cylinders E_i
for pure3 cofactors3,9,27,81,243. Put

    A(E)=integral_E k d lambda35,
    K(E)=integral_E c d lambda3,
    T(E)=integral_(E intersect J5) c d lambda35,
    V(E)=h*K(E)-T(E)>=0.

At most five E_i overlap, so sum_i V(E_i)<=5 sum_l c_l*v_l<=5V.
The COMPLETE positive7 deletion coefficient is1/5. Therefore the
single source overcount V pays the lost same-event masses at every
positive7 depth, even when the cofactor residues depend on that depth.
After combining before any maximization, the h-dependent expression
is bounded by

    -(1/5-h)A+(1/5)sum_i[A(E_i)-h*K(E_i)].

Its slope is A-(1/5)sum_i K(E_i)>=0, because sum_i K(E_i)<=5A.
Hence h<=1/5 replaces it by(1/5)sum_i[A(E_i)-K(E_i)/5]. This is a
bound on the combined source/deletion expression, not a separate
rebate applied after unrelated maxima.

Write n_l=lambda35(cell_l), eta_l=lambda3(cell_l), w_l=9 eta_l,
and d_l for the original residual five availability in cell l. The
effective complete cofactor cap is

    W(C)=max_r sum_(root(l)=r)(k_l*n_l-c_l*eta_l/5)
       +max_l(k_l*n_l-c_l*eta_l/5)
       +(13/243)max_l(k_l*d_l-c_l/5)
       +(1/486)max_l(k_l*d_l)
       +sum_l k_l*w_l/36
       +max_r sum_(root(l)=r)k_l*w_l/36
       +max_l k_l*w_l/36+max_l k_l/72.

For the three deep selected cofactors, actual cylinder mass is at
most3^-i and actual35 density is at most d_l. The coefficient has the
correct sign since d_l>=1/4 and c_l<=k_l give
k_l*d_l-c_l/5>=k_l/20>=0. The split13/243+1/486=1/18 includes the
complete pure3 tail. All positive5 cofactors retain their old caps.
The empty root/cell alternative is also covered; no selected cylinder
is assumed nonempty. At final C>=f(4), let cmax=f(4)-f(3). Convexity
gives k_l>=cmax and c_l<=cmax. With x=sum_l eta_l and n_l>=0,

    sum_l(k_l*n_l-c_l*eta_l/5)>=cmax*(s-x/5)
                                      >=(5/36)*cmax>=0.

The last bound follows from the original parameter box: s-x/5 is
separately affine and its1296 vertex minimum is3/20>=5/36. Thus
both its largest root sum and its largest cell summand are nonnegative,
so empty cofactor labels are covered. The deep coefficient also obeys
k_l*d_l-c_l/5>=c_l/20>=0. At final C>=f(4), max_l k_l=C-f(1), as in
the original signed floor. Set c_l=0 throughout W to obtain the
floor-only comparison W_floor.

#### Fixed constants on every layout and continuous parameter point

Let B_f be the complete original7 raw source cost. Its zero7 summand
is the old zero5 bound of psi0. Replace only that summand by the bound
with the actual baseline b fixed before maximizing. Call the result
S_(f,b). The source calculation fixes this baseline before taking the other maxima.
All other original7 blocks keep independent maximizing baselines.
The resulting sufficient norm inequality is

    M_(f,b,C)=C*s-S_(f,b)-W(C)/5 >=0.

It implies E_(actual uniform357) f(A)<=C for every complete original
test A. The same statement with c=0 gives the floor-only bound.

For fixed C and b, k and c are constants. The raw source remainder
is separately convex in the original five parameter groups; writing
out its original7 sum cancels the removed zero7 bound exactly, so no
subtraction of two unrelated convex maxima is used. Every branch of
W is separately affine. Thus M is separately concave, and checking
all6*3*6*6*2=1296 product vertices for each of the ten b proves it on
the entire continuous product domain. This is vertex interpolation
of the proved envelopes, not enumeration of residue families.

The certificate's 41 constants are fixed rational numbers, independent of the
five parameter groups. Every final constant is checked directly at all1296
vertices and all ten baselines. A parameter-dependent root is not inserted
into the continuous consumer. The complete formula has no norm-search state
or finite-height cutoff among its hypotheses.

#### Forty AP tuple costs and the complete shared tail

For h equal to phi17, phi19 or(v-5)+, use the SAME independent
auxiliary counts N11,N13 as ABP2 and N=N11*N13. For each original
tuple a=(e,f), set

    Q_a(v)=E[1_(e<N11,f<N13)*(h(Nv)-h(N))/N].

This is nonnegative, increasing, convex and vanishes at1. Its fixed
source norm is one C_a. The finite exceptional set is
I_h={(e,f):(e+1)(f+1)<K_h}, with K17=K19=8 and K5=5; its cardinalities
are16,16,8. Every Q_a includes the exact active N>=K_h tail. Outside
I_h the cost is exactly A_h*M_a*(v-1), where
M_a=Pr(e<N11,f<N13). Since sum_a M_a=E N=49/36, the entire complement
uses the single shared source norm C_v for v itself:

    H_h=E h(N)+sum_(a in I_h)C_a
             +A_h*(49/36-sum_(a in I_h)M_a)*(C_v-1).

All coefficients are nonnegative. This is40 finite tuple directions
plus one exact linear direction, not a finite-height truncation.
Each fixed C_a bounds the NORMALIZED actual probability nu357:
E_nu357 Q_a(A_a)<=C_a. Centered Jensen and these bounds give H_h
on that same probability. In particular D*C_a is only an algebraic
numerator for C_a=(D*C_a)/D; it is NOT a bound on the actual raw357
integral. The latter has actual mass u>=D and is at most u*C_a.
The unchanged final AP survival lower bound is Delta/D, so

    M_nu13(h)<=D*H_h/Delta.

Each original AP tuple and each original17/19 test can maximize
independently. No common maximizer has been introduced. The final
constants and exact expectations/tail coefficients are recorded in
`certificates/fixed_cost_norms.json` and the fixed-cost certificate.

#### Whole consumer, extension and inherited branches

Substitution into the unchanged DP10 consumer gives

    F17^-+F19^- <= C0+[AC*U16+D*H]/Delta,
    H=H_phi17+(15/17)H_phi19+(13299/1360)H_(v-5)+,
    AC=2371/2880, C0=185694867601/8599322160.

Use the existing six-source decomposition U16=DCOEF16*D+U16,0.
The target margin at J is separately concave because its D coefficient

    (J-C0)*131/132-AC*DCOEF16-H
      =689014904250995655844577602862363635678631513952091
         /2865554953196843863665323517414073419216360000000
      =240.447283512159... >0.

The other terms are negative multiples of the existing separately
convex U16 remainder and survival-loss costs. All1296 exact margins
are nonnegative, with equality at398,410,422,616,628,640. Repeated
vertex interpolation proves the target on the full domain. For the
floor-only J_floor the analogous coefficient is241.73456818210218>0.
The full source-square Gamma13 and T81 bounds are unchanged.

The other eight actual missing3/effective9 branches use their old
complete-tail bounds. Their maximum is377.2220002962648, attained by
`9-absent-or-ineffective/5-present/7-present`, strictly below J. Thus
the new target covers all twelve original branches.

#### Exact verification and boundary

The [fixed-cost verifier](../../verify_fixed_cost_frontier.py) directly checks
1,062,720 final norm inequalities:41 costs,1296 parameter vertices,ten
baselines and the floor-only/same-event comparisons. It also reconstructs
all1296 consumer rows and the eight inherited branches. The
[fixed norm inputs](../../certificates/fixed_cost_norms.json) and
[complete certificate](../../certificates/fixed_cost_frontier_certificate.json)
record the rational constants, exact tails and comparison bounds. Semantic
parts preserve the full logical JSON and bind every physical part by SHA-256.

An independent implementation reconstructs the convex costs from integer
values and exact geometric tails and reproduces the final bound, all final
norm inequalities, six controlling vertices and positive continuous
coefficient. These computations validate the stated finite envelopes; the
actual-family argument above and its preceding source lemmas remain the
ordinary mathematical bridge. They are not Lean verification.

The Gamma13 and T13(81) bounds in KRM6 are unchanged. The larger exact
allowance remains304.018367585460..., so the new joint bound is still
137.383445265904... above it. The uniform negative-Q criterion and arbitrary
later-prime continuation remain open.

### Six actual cofactors for a quadratic hinge mixture

This argument strengthens only the quadratic AP costs. It leaves all
linear-cost bounds and the fixed source square norm unchanged. It is
an ordinary mathematical argument; no Lean verification is claimed.

Let g_T(v)=(v^2-T)_+, where T>=0. If n>=1 and b>=0, then

    g_T(n(b+1))-g_T(nb)
      = n^2 [g_(T/n^2)(b+1)-g_(T/n^2)(b)]
      >= n^2 [g_T(b+1)-g_T(b)].

Indeed, for fixed ordered squared endpoints x<y, the function
(y-t)_+-(x-t)_+ is nonincreasing in t. Positive weighted sums, positive
dilations and additive constants preserve this increment inequality.
The complete original AP block costs are positive centered dilation
mixtures of g_16. Their exact degree-two tails have the same property.
Thus every one of the five U16 costs f satisfies the displayed bound.

For the selected original-zero7 Jensen cost,

    psi0(v) = sum_(n>=1) p_n [f(nv)-f(n)]/n,
    p_1=29/35,  p_n=36/(5*7^n) for n>=2,

the complete geometric sum has E[N7]=6/5. Consequently, putting
c_l=f(b_l+1)-f(b_l) and a_l=psi0(b_l+1)-psi0(b_l),

    a_l >= (6/5)c_l.

Only one original modulus5 event J is held common. Write h for its
pure5 mass, eta for the actual pure3 measure, and lambda35 for the
actual35 restriction. Other positive5 labels keep independent residues.
In each cell l let e_l=eta_l*h-lambda35(cell_l intersect J)>=0.
Define

    A=sum_l eta_l*a_l,   K=sum_l eta_l*c_l,
    R_a=sum_l a_l*e_l,   R_c=sum_l c_l*e_l.

Then A>=(6/5)K and R_a>=(6/5)R_c. Convexity gives a source increment
at least a_l*1_J. Therefore the existing enlargement and single-unit
auxiliary-strip comparison gives

    actual source <= S - (1/5-h)A - R_a.

This enlarges the positive original5 contribution once, and only then
shrinks the one unit5 auxiliary interval. The actual zero5 contribution
stays on lambda35. No other original5 label is assigned J's residue or h.

For each of six actual pure3 cofactor events E_i, define

    K(E_i)=integral_(eta restricted to E_i) c,
    T(E_i)=integral_(lambda35 restricted to E_i) c*1_J,
    R_c(E_i)=h*K(E_i)-T(E_i)>=0.

The six events have pointwise multiplicity at most six. The complete
positive7 coefficients tau_e satisfy sum_e tau_e<=1/5. Hence, with
each original positive7 label allowed its own six events,

    sum_e tau_e sum_i R_c(E_(e,i)) <= (6/5)R_c <= R_a,
    sum_e tau_e sum_i K(E_(e,i)) <= (6/5)K <= A.

For C>=f(4) and baseline b_l<=3, convexity and J's actual inclusion
give the deletion floor bound

    (C-f(A0))_+ <= k_l-c_l*1_J,
    k_l=C-f(b_l).

The source and deletion comparisons can therefore be added before
discarding any actual intersections. The positive removed terms spend
at most R_a, and the remaining h-dependent expression is

    -(1/5-h)A - h sum_e tau_e sum_i K(E_(e,i)).

Its h derivative is nonnegative by the second multiplicity estimate.
Thus its upper bound occurs at h=1/5. This step is taken for actual
events; only afterwards are their individual cylinder masses capped.
It produces the same corrected cylinder coefficients k_l*d_l-c_l/5
as in the five-label bridge, now for six actual pure3 cofactor labels.

The root and cell terms, empty-event handling, positive5 cofactors and
nonnegative signed cylinder checks are unchanged. The only change in
the effective cofactor expression is

    (13/243) max_l(k_l*d_l-c_l/5)
      + (1/486) max_l(k_l*d_l)

replaced by

    (40/729) max_l(k_l*d_l-c_l/5)
      + (1/1458) max_l(k_l*d_l).

The selected sum is over actual depths 3,...,6 and the remaining sum
is the full a>=7 geometric tail. Both pairs sum to 1/18. Their exact
difference is

    (1/729)[max_l(k_l*d_l-c_l/5)-max_l(k_l*d_l)] <= 0.

The effective
cofactor expression is only a component of the combined source and
deletion inequality; it is not an independent deletion upper bound.

The argument is uniform in all finite original heights and original
residues and uses complete geometric tails. It makes no independence
assumption between the six actual events. It does not extend to an
arbitrary linear convex cost, whose psi0 increment need only dominate
one copy rather than 6/5 copies of the original increment.

Once the five normalized source constants are fixed globally, the
same separate convexity, complete quadratic AP comparison and probability
normalization used above apply. No gain is subtracted from G357 a second time, and D*C remains
a normalization numerator rather than a raw integral bound.


#### Complete quadratic AP norm and the final consumer

Let h(v)=(v²-16)+. Its original AP tuple costs are the Q_a defined above,
now with eventual quadratic growth. Exactly five tuples satisfy
(e+1)(f+1)<4. Their fixed normalized357 bounds are:

| Original tuple | C_a |
| --- | ---: |
| (0, 0) | 2744058848688721/71898009183000 |
| (0, 1) | 458272300751/37349615160 |
| (0, 2) | 10894224355/7469923032 |
| (1, 0) | 69177165971/5672426760 |
| (2, 0) | 4150293559/2431040040 |

Every C_a is at least Q_a(4). The six-cofactor margin is nonnegative at
all1296 parameter vertices and ten layouts, with minimum zero for each
cost. The preceding separate-convexity argument extends these fixed
constants to the complete continuous domain.

For every tuple outside this set, N>=4 whenever the tuple is active.
Thus `Q_a(v)=E[N 1_a-active](v²-1)` exactly. Since the number of active
original tuples is N, the sum of their weighted masses is E[N²]=253/108.
Subtracting the five finite contributions leaves158957/1104246.
The complete comparison is therefore

    H16=2739361/28710396+sum_a C_a
                           +(158957/1104246)(765767/21465-1)
       =15289185796003513/215694027549000.

This uses the quadratic moment253/108, not the linear moment49/36. The
fixed source square bound is used once for the entire complement.
Let

    H41=H_phi17+(15/17)H_phi19+(13299/1360)H_(v-5)+
       =34223534699968380911680651169693124683
          /218275404408935878292461743570000000.

Using DP10, the new complete consumer is

    C0+D[(2371/2880)H16+H41]/Delta.

Both H16 and H41 are norms on the normalized actual357 probability.
The factor D/Delta comes from the unchanged AP survival lower bound;
D times a norm does not assert a bound on a raw integral.

At the announced final target J, its continuous-domain margin is

    rho*D-(J-C0)L, where Delta=(131/132)D-L,
    rho=(J-C0)*131/132-[(2371/2880)H16+H41]
       =1144878307082119880245569550076944010833595647050557
          /5731109906393687727330647034828146838432720000000
       =199.765547299115... >0.

D is separately concave and L separately convex. Thus this margin is
separately concave; its1296 nonnegative vertex values prove the result
on the full parameter product. The six equality vertices are
398,410,422,616,628,640. The eight inherited missing-class branches have
maximum377.222000296264..., strictly below J.

The fixed-cost certificate directly checks all1,127,520 final source
margins:41 linear costs with both comparisons, plus five quadratic
costs, each at1296 vertices and ten layouts. It also checks the complete
AP tails, final consumer and eight inherited branches. The values in
KRM6 for Gamma13 and T13(81) remain valid; this step does not assert a
stronger global T13(81) bound. The exact larger allowance is therefore
still304.018367585460..., leaving the current joint bound
135.655162136408... above this sufficient criterion. The uniform
negative-Q criterion and arbitrary later-prime continuation remain open.
