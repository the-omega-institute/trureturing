[Index](../../marked_head_profile.md) · [Previous](34-original-layout-distance-forces-a-jensen-loss.md)

# Actual AP(4,5) with an original-layout Jensen loss

The construction starts from the same uniform complete actual357
survivor law, applies physical AP11/T4 and physical AP13/T5 without
intermediate conditioning, then conditions ONCE on the actual survivor
set. This is a different probability from AP(4,6). Subsequent17/T8 and
19/T8 remain the same prescribed physical/killed constructions starting
from this new probability. In particular19's physical input remains
nu13 K17, with no substitution of the killed17 subprobability.

## Admissibility and complete comparison counts

For prime p and1<T<p-1, put s_*=(p-2)/(p-1), delta=(T-1)/(p-2),
C=1/(1-delta), c=C/s_*=(p-1)/(p-1-T). The pure survivor bases have
mass at least s_*, and the physical kernel is normalized even on rows
with mixed fraction1. Both choices are admissible:

|p,T|delta|C|c|weighted square propagation|
|---|---:|---:|---:|---:|
|11,4|1/3|3/2|5/3|23/15|
|13,5|4/11|11/7|12/7|61/42|

The auxiliary comparison variables, independent of each other but not
identified with actual forbidden events, have

    Pr(Np=1)=1-cp/p,
    Pr(Np=n)=cp*(p-1)/p^n, n>=2.

Their product N=N11*N13 has E N=4/3 and E N^2=1403/630.
Every original label keeps its own arbitrary residue choice. No residue is identified with another label's residue. The
per-original-tuple convex comparisons depend on these cap distributions,
not on the special old value c13=2. The finite exceptional tuple sets
remain16,16,8,5 for phi17,phi19,g5,h16. Their complements are exactly
summed through the appropriate full first or second moment. Hence the
generic cap replacement is legitimate; old numerical constants for
those directions must all be recomputed.

## General survival inequality and its new specialization

Let H(t) bound the complete source hinge E(A-t)+ for every original
old test, on ONE actual nu357. AP's assigned bad masses satisfy

    b11<=H(T11)/(10-T11),
    b13<=E[N11*H(T13/N11)]/(12-T13).

For every A>=0,n>=2,T>=0,

    (nA-T)+ <= n*(A-T/2)+ +(n-2)*T/2.

When A>=T/2 the two sides agree. Otherwise the left side is bounded
by(n-2)T/2. Also

    E[N11;N11>=2]=21*c11/110,
    E(N11-2)+=c11/110.

Therefore

    rho >= 1-H(T11)/(10-T11)
      -[(1-c11/11)*H(T13)
        +(21*c11/110)*H(T13/2)+c11*T13/220]/(12-T13).

Normalized later physical kernels preserve earlier violations, so the
same final physical law permits this union bound. It is not a claim of
independence between the two actual bad events.

Writing B_t=D*H(t) for the existing SAME-source raw upper expressions,
the new specialization is

    Delta45=(919/924)*D-B4/6-(4/33)*B5-B_(5/2)/22,
    rho45>=Delta45/D.                                 (S45)

At(4,6) the general formula exactly recovers the old131/132,1/6,14/99,
7/132 coefficients. The source raw hinge evaluator allows fractional
t, so5/2 is covered by its increasing-convex complete-tail argument.
D is separately concave and each B_t separately convex, which gives
separate concavity of Delta45. Positive vertex margins extend through
the whole existing product domain. The eight other source branches
require their own S45 evaluations before a uniform rho claim.

## Eight fallback branches and T81

For a fallback branch with certified pure masses u3,u5,u7 and density
d_b, set A_b=d_b*u3*u5*u7 and oldcaps=(1/u3,1/u5,1/u7). On that same
source use H_b(t)=A_b*E(X3*X5*X7-t)+ in S45. After adding cap5/3 at11
and cap12/7 at13, supported hinges are bounded by

    A_b*E(X3*X5*X7*N11*N13-t)+/rho_b.

Use them in the unchanged row-potential formula, with a NEW same-law
Gamma13 bound. The row constants C0,AC,phi17,phi19 and g5 coefficient
come only from the later17/19 kernels and may be reused. A bound using
old AP(4,6) Gamma13 in this slot is invalid.

For T81, recompute probabilities p_n=Pr(N=n),n<9, and
q0=1-sum_(n<9)p_n, q2=1403/630-sum_(n<9)n^2*p_n. For any same-source
square bound G0,

    U81 <= sum_(n<9)p_n*n^2*Hsq357(81/n^2)+G0*q2-81*q0.

Here Hsq357 bounds E(A^2-t)+. On the parameterized main branches, use
the same previously proved fixed formula choice at each n, with the
NEW p_n and full q0,q2. Then divide by Delta45/D. For fallback branches,
Hsq357(t) may be bounded by min(A_b*E(X3*X5*X7)^2_minus_t_plus,G0-1)
for t>=1. All n<9 satisfy81/n^2>1. The tail term is complete because
NA>=9 there, so the positive part is exactly N^2*A^2-81.

No old PR16,PR81,TAIL2,TAIL81_2,DCOEF or CAP13 module-level value may
be imported as a new-law number merely because the helper function is
shared. Recompute every cap-dependent quantity explicitly.

## Finite-core errors

The generic KC2 physical/killed kernel comparison applies without
change of proof at T13=5. Its sensitivity constants must change:

    ell13=max(C13^2,C13/delta13)=121/28,
    ell13/s_*^2=36/7,
    (ell13+C13)/s_*^2=540/77.

The old ell13 was121/30; the new one is LARGER. Thus a lower Haar cap
alone does not justify reusing or monotonically decreasing old tail
budgets. Recompute the explicit errors.

Let e0w,e0m compare the original357 source with its retained core.
Use KC2 with(11,4,D0,G0) for e11, and with(13,5,(5/3)D0,(23/15)G0)
for e13 in the weighted case; use J=1 for each mass-error case. Then

    ew=(61/42)*[(23/15)*e0w+e11,2]+e13,2,
    em=e0m+e11,0+e13,0,
    eNw=(ew+G45*em)/rho45,
    eNm=2*em/rho45.

G45 and rho45 must be uniform bounds for BOTH the full and retained
actual AP(4,5) families. A valid normalized density is
D45=(20/7)*D0/rho45, or any independently stronger new-law bound.

With these new D45,G45, the17/19 KC2 mask errors and complete test tails
are evaluated exactly as before. The remaining coefficients depend only
on the later kernels and the objective Q=integral(L^2-484):

    e_mask=(59/45)*e17,2+e19,2+483*(e17,0+e19,0),
    e_incoming=(5251/2880)*eNw+242*eNm,
    e_test=(18/5)*D45*(complete ordered-test-pair tail).

The factor242 remains valid: full and core incoming laws are normalized
probabilities, so their signed difference has mass zero and its integral
against a[0,1]-valued final survival function is at most half its L1 norm.
The same exponent boxes retain their label counts and periods, while the
actual13 kernel coefficients in the reference are changed.

After establishing a NEW total error epsilon, the sufficient joint
allowance at tau81 is

    403-T81_45-epsilon.

Old allowances304.018... and0.000667 cannot be inserted without checking
the new T81 and total error. A chosen safety allowance0.000667 can be
reused only if its inequality against the recomputed error is verified.


## Final quadratic costs and coupled layout loss

The original-layout distance and N5=2 Jensen argument are unchanged by
the two later physical AP caps. Use the actual law AP11/T4, AP13/T5,
with caps ((11,5/3),(13,12/7)), throughout. The source remains the same
actual357 law. This statement does not identify either law with AP(4,6).

For each exceptional original tuple (e,f), put K=N7 N11 N13, with
the event N11>e,N13>f. Its zero7 quadratic cost is exactly

    psi(v)=A(v²-1)+sum_{k=1}^3 p_k/k*((16-k²v²)_+-(16-k²)),
    A=(6/5) E[N11;N11>e] E[N13;N13>f].

Here Pr(Np=1)=1-cp/p and Pr(Np=n)=cp(p-1)/p^n for n>=2, with
c11=5/3,c13=12/7. Thus the low product atoms p_k and the entire first
moment determine psi without any height truncation. It is exactly
quadratic for v>=4. Checking the second differences at 2,3,4 together
with its polynomial tail gives the following valid integer curvature
constants m, so psi(v)-m v² is convex on positive integers.

| Original tuple | Curvature m | Final normalized cost C |
|---|---:|---:|
| (0,0) | 2972/3003 | 25586082093656026/692018338386375 |
| (0,1) | 5/13 | 50628620561/4841616780 |
| (0,2) | 37/845 | 673872533/542084400 |
| (1,0) | 24/55 | 388077041138/32758264539 |
| (2,0) | 248/4235 | 282194944/169802325 |

On the same raw pure3 measure eta, complete tests with shallow layouts
b,c satisfy the previously established distance lower bound

    integral (B-B')² d eta >= d_eta(b,c),
    d_eta(b,c)=sum_l eta_l(b_l-c_l)²
       -(max(0,max(b-c))²+max(0,max(c-b))²)/18.

It follows on N5=2 that the original positive-five contribution can be
replaced by (2/25) max_c[A_c-2m d_eta(b,c)], keeping its original test
labels and the cost/distance choice coupled. The previous removed-mass
and cap-shrink losses stay at their separate stages. Cancel the old
N5=2 maximum explicitly; the replacement is a maximum of affine width
branches. This preserves the required separate convexity and therefore
the continuous-domain extension by vertex margins.

The exact checker independently reconstructs psi from low product atoms
and the complete moment, compares it with the source API, reconstructs
the full pure3 depth tail, and checks all 64,800 final norm margins.
The retained G357=1730443/48600 is independent of the later AP caps.
The complete AP second moment is 1403/630; the five exceptional tuples
have complete complement 1600217/12882870. Their base expectation is
21566863/263178630. Consequently

    H16=1105494866024740603/16608440121273000.

The linear H41 comes from the complete AP(4,5) 46-direction result:

    H41=138463094971779285596846788846625500699/
        933733674416003479362197458605000000.

Use only the new-law survivor bound

    Delta=(919/924)D-B4/6-(4/33)B5-B(5/2)/22.

The combined consumer has the following certified upper-bound constants

    Gamma13=302778048367321641239/1995737886953762094
           =151.71233173784836,
    J=635194866152821699517974252365456181477722101/
      1457986034966471039964009460664853061488000
     =435.6659466682952.

The J controllers are398,410,422,616,628,640. All eight other source
branches are covered after recomputing their new-law survival and costs.

The separate propagation program recomputes every cap-dependent T81
probability and full tail, giving T81=96.35912837401835. It then uses
the new physical13 kernel sensitivity, all incoming and killed-kernel
errors, and the complete ordered-pair test tail. For the common exponent
box20 and later current heights8,8, the total error is
0.0005933431995295886 and the sufficient allowance is
306.6402782827821. The remaining numerical gap is129.02566838551306.
All three continuous extension coefficients are positive.

These are ordinary proofs and exact rational computations. The target
negative-Q inequality, arbitrary later-prime continuation, and the
unrestricted Erdős #7 claim remain open; no Lean result is asserted.

## Exact verification and remaining obligations

The checker rebuilds all 41 linear directions and five quadratic directions
from explicit rational constants, checking 596,160 margins over the complete
1,296-vertex parameter domain and ten original shallow layouts. Linear costs
use the established five-cofactor deduction; the five quadratic costs use
six cofactors and the coupled N5=2 replacement. The complete exceptional
AP inventories have sizes16,16,8,5, with their respective complements summed
through full first or second moments. The source square bound is retained
from the original-layout result in chapter34. All probabilities in the new
consumer and error calculations use the actual AP(4,5) law.

Continuous extension uses separate concavity of D and separate convexity of
raw source costs. The checker verifies positive coefficients for the joint
bound, Gamma13, T13(81), and survival. The layout replacement is kept as a
single maximum of coupled affine branches before invoking convexity.
It does not subtract unrelated maxima. The eight complementary original
source classes have independently reconstructed new-law survival and costs.

Run from this report directory:

    python3 -I -O frontier/cover-geometry/verify_ap_schedule.py --check

The default command is also read-only; only --write regenerates the target
certificate. Source identities, complete logical predecessor certificates,
local modules, and final norm inputs are pinned. The certificate contains
full norm minima/controllers, consumer checksums, eight branch bounds and
both complete core errors. No search root or fitted solver is executed.

The exact box20 sufficient threshold is403-T13(81)-epsilon. The stored
safe_allowance is rounded strictly downward to a multiple of10^-6, so a
non-strict joint bound at that safe threshold still gives strict negative Q.
The analogous unequal exponent box is included with its own error.

The current upper bound exceeds both sufficient thresholds. This does not
prove that the actual finite-core maximum exceeds either threshold: the gap
is between available estimates. A uniform negative-Q bound, an argument for
arbitrary later primes, and the unrestricted odd-covering conclusion are
still missing. These numerical and ordinary-mathematical results have not
been formalized as a Lean endpoint.
