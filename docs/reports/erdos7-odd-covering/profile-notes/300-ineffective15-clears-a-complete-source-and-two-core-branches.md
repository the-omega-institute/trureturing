[Index](../marked_head_profile.md) · [Original allocated source](53-allocated-seven-thresholds-sharpen-actual-survival.md) · [Complete numerator corrections](49-full-linear-and-quadratic-carriers-refine-the-frontier.md) · [Complete core errors](35-ap45-layout-costs-and-complete-core-tails.md#finite-core-errors)

# Ineffective15 clears a complete source and two core branches

On the entire actual effective9 source domain with
`alpha0+alpha1<=1/20`, the original complete comparison satisfies

    K <= 2887355561300209986661398218987915074355362289
         /7653284250953788882516169712396470521824000
       = 377.27013222334894053377968328496520150792... < 403.

This domain includes every effective9 family whose original forbidden15
is absent or disjoint from the actual pure3 by pure5 survivor. That
sufficient branch is preserved by both original core restrictions. After
adding their entire original errors, both strict negative-Q sufficient
criteria hold, with margins greater than 25.72 and 25.50 respectively.
All other original source parameters, independent test residues, shallow
carriers and exponent heights remain arbitrary. No continuation beyond
the existing19 stage or unrestricted Erdős7 conclusion follows here.

## The original signed comparison retains every correction

Use exactly the actual source, complete test functions and allocation
of53. Write `b` for its original offset, `q=23/42`, `S` for actual survivor
mass, `s` for its raw upper bound, and `pi_c` for its original carrier
mixture. For the complete K numerator put

    L = AC*H16 + H41 + A81,
    C_c = AC*Q_full(theta;c) + G_full(theta;c)
            + cG*m_g(theta) - R81(theta).

Here `Q_full` and `G_full` are precisely the original49 five-quadratic
and41-linear conditional margins, `m_g` is the independent source-square
margin, and `R81` is its unchanged full raw81 source cost. In particular,
the signed `-R81` contribution is included. No new alignment of the
independent original tests is assumed.

For a fixed target T, the sufficient actual-source margin is

    [q*(T-b)-L]*S + sum_c pi_c*[(T-b)*M_c+C_c].       (1)

Throughout the target interval used below, `T>b` and
`a(T)=q*(T-b)-L<0`. Since `S<=s`, expression(1) is bounded below by the
carrier mixture of

    F_T(theta;c) = a(T)*s + (T-b)*M_c + C_c.           (2)

The raw mass s is separately affine. The true allocated margin M_c is
separately concave. The true `Q_full`, `G_full` and `m_g` are separately
concave with positive coefficients; the complete raw source R81 is
separately convex, so `-R81` is separately concave. Thus(2) is separately
concave in every original parameter group, including when a(T) is
negative. This is a statement about the original functions, established
in49 and53; it is not inferred from concavity of a numerical table.

The published49 certified lower margins at all1296 product vertices
remain valid in(2). At its24 refined vertices retain the improved41
linear and five-quadratic margins; elsewhere retain the same older
certified margins. The independent raw81 term is recomputed everywhere.
Every vertex retains all18 original partial and full carriers.

## Six floors already certify complete complementary source regions

At T=403, minimize(2) over all original deficit, beta, late and carrier
vertices while leaving the three alpha vertices and two z endpoints
separate. Each of the six floors is a minimum over3888 original
source/carrier pairs. Their decimal displays are

| Alpha vertex | z=3/4 floor | z=1 floor |
| --- | ---: | ---: |
| (0,0) | 7.97751518919491... | 38.09811909173174... |
| (1/4,0) | -7.62092641006970... | 22.51012628480227... |
| (0,1/4) | -8.43601357118192... | 22.21467510689130... |

Write these as `m_kz`, with `k=0,1,2` and `z=0,1` indicating the table
column. Set `t=4*(1-z_actual)`, `u0=4*alpha0`, `u1=4*alpha1`.
Successive separate concavity bounds every actual margin from below by

    (1-t)*[(1-u0-u1)*m_01+u0*m_11+u1*m_21]
      + t*[(1-u0-u1)*m_00+u0*m_10+u1*m_20].           (3)

Consequently either `z_actual>=819/1000` or
`alpha0+alpha1<=121/1000` suffices for the complete source bound K<=403,
without restrictions on the other original parameter groups. The exact
endpoints supplied by(3) are `z_actual>=0.818807700047020834...` and
`alpha0+alpha1<=0.121508228146117419...`; the checker stores and verifies
their rational values. These two conclusions are source inequalities.
Preservation under core restriction is not asserted for arbitrary
points in either numerical region.

## The smaller alpha simplex gives the strict bound377.2701322

Let K0 be the original53 complete upper, and let
`A=q*(K0-b)-L`. At each original vertex i and carrier c, retain the
certified upper-mass signed gap g and its own denominator payment e:

    g_ic = A*s_i + (K0-b)*M_ic + C_ic,
    e_ic = q*s_i + M_ic > 0.

For every target T under consideration,

    F_T(theta_i;c) >= g_ic - (K0-T)*e_ic.              (4)

An occupied vertex of `alpha0+alpha1<=1/20` is the combination of one
fifth of the corresponding original occupied alpha vertex and four
fifths of its zero-alpha vertex. All other coordinates and the carrier
are the same. Combine their pairs(g,e) with these weights. For each of
the23328 original source/carrier records put

    g_mix=(4/5)*g_zero+(1/5)*g_original,
    e_mix=(4/5)*e_zero+(1/5)*e_original > 0,
    T_ic=K0-g_mix/e_mix.

The maximum of all T_ic is the rational target stated at the beginning.
At that target, every `g_mix-(K0-T)*e_mix` is nonnegative and
`a(T)=-18.11508563793214186...<0`; also `T>b`. Successive separate
concavity in(2) therefore proves the bound on the entire smaller alpha
simplex and every remaining original parameter group. The independent
coarser floor(3) supplies a strictly positive403 margin
`4.694809437119545446...` throughout that simplex.

No relaxed vertex is asserted to be attained by an original family.
Every signed gap remains paired with its own original denominator;
neither separate extrema nor a mass lower bound replace this pair.

## Absent or ineffective15 forces the smaller simplex

The original root-level mixed3 by5 labels have the form `3*5^b`,
`b>=1`. Distinct original moduli give at most one such class for each b.
When15 is absent or has empty intersection with the actual pure3 by
pure5 survivor, it contributes no effective root deletion. The remaining
root caps satisfy

    alpha0+alpha1 <= sum_(b>=2) 5^-b = 1/20.

This is a complete geometric tail, valid at every finite original
height. The source result also applies to other actual families with
the same alpha bound; only the stated sufficient branch is used below.

## This sufficient branch survives both original core restrictions

Let r3,r5 be the reductions of the forbidden15 residue. If the original
forbidden3 does not remove all of r3, the deeper pure3 classes remove at
most `sum_(a>=2)3^-a=1/6` of Haar mass, so r3 has pure3 survivor mass
at least `1/3-1/6=1/6>0`. Likewise, unless the forbidden5 removes r5,
that root has pure5 survivor mass at least
`1/5-sum_(b>=2)5^-b=3/20>0`. CRT gives their product survivor mass at
least `1/40>0` inside15.

Thus a present15 is ineffective exactly when its3-root or5-root is
already removed by the original forbidden3 or5. Deleting other classes
cannot undo this obstruction while those two shallow labels remain.
An absent original15 remains absent in an original subfamily.

The original core interfaces in `ap_schedule_core.core_errors` use

    primes:   (3,5,7,11,13,17,19),
    box A:    (20,20,20,20,20,20,20), current=(8,8),
    box B:    (17,10,8,7,6,6,6),       current=(6,6).

They retain every original class within their coordinate cutoffs, with
its original residue. Both retain3,5,9 and15 whenever present. The
effective9 condition is preserved:9 remains, and deleting other pure3
constraints enlarges the survivor on which its effectiveness is tested.
Consequently both retained families remain in the sufficient
absent/ineffective15 branch and hence have total alpha at most1/20.
This argument does not claim that an arbitrary numerical
`alpha0+alpha1<=1/20` constraint survives truncation.

Keep the original53 global Gamma13 and rho bounds for both full and
retained families. The original source35/KC error formula recomputes
every source-law, mask and complete test-tail term unchanged. Applying
the new common-law K upper to each retained family gives

| Core | Complete original error | K upper + error | Margin below403 |
| --- | ---: | ---: | ---: |
| box A/current8 | 0.0005750003181418988... | 377.27070722366708243... | 25.72929277633291756... |
| box B/current6 | 0.2205204082126680259... | 377.49065263156160855... | 25.50934736843839144... |

Both margins are strictly positive by exact arithmetic. These satisfy
the existing strict negative-Q sufficient criterion for the preserved
branch, with the same actual AP11/T4, AP13/T5 and killed17/8,19/8
construction. No later-prime continuation is supplied by this result.

The [helper](../frontier/ineffective15_complete_source_comparison.py)
and [certificate](../certificates/source_norms/ineffective15_complete_source_comparison.json)
reconstruct the original39 source, every original53 allocated carrier,
the complete49 corrections, all23328 mixed signed comparisons, the six
source floors, and both entire original core errors. Canonical replay
uses published inputs and exact standard-library arithmetic:

```sh
python3 -I -O docs/reports/erdos7-odd-covering/frontier/ineffective15_complete_source_comparison.py --check
```

The ordinary concavity and retention arguments give the quantified
mathematical scope; the exact checker verifies their finite numerical
premises. This is not a Lean proof or an unrestricted Erdős7 resolution.
