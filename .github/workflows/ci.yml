# BOOTSTRAP-NOT-ACTIVE until this workflow and the C# harness exist on the actual
# default branch (dev). D5-T0017 requires one trusted admin injection; no candidate
# workflow can machine-authorize that first placement. After injection, the dev
# baseline workflow below adjudicates every later PR.
name: admission

on:
  push:
    branches: [dev]
  pull_request_target:
    branches: [dev]

permissions:
  contents: read

# Superseded runs cancel cleanly instead of lingering and racing. A PR scopes to
# its number (concurrent PRs never cancel each other); a branch push scopes to the
# ref. This removes the false SIGTERM(143) failures a new head produced while an old
# head's run was still executing — the cancel is now an explicit, expected cancel.
concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

# Trust topology: candidate engineering checks are non-authoritative. Admission is
# issued only by the harness checked out at the content-addressed dev event-base SHA.
# D5-T0017's candidate judge is a post-injection observation, not predecessor machine
# authorization. It can run only after an admin has placed this workflow on dev, and it
# emits both an annotation and a job-summary ledger entry when exercised.
jobs:
  candidate-engineering:
    name: Candidate harness engineering checks
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out candidate
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
          path: candidate

      - name: Resolve pinned Lean toolchain
        id: lean
        shell: bash
        env:
          LEAN_TOOLCHAIN_FILE: candidate/lean-toolchain
        run: |
          set -euo pipefail
          test -s "$LEAN_TOOLCHAIN_FILE"
          echo "file=$LEAN_TOOLCHAIN_FILE" >> "$GITHUB_OUTPUT"
          echo "hash=$(sha256sum "$LEAN_TOOLCHAIN_FILE" | cut -d ' ' -f 1)" >> "$GITHUB_OUTPUT"

      - name: Cache elan and pinned Lean toolchain
        uses: actions/cache@v4
        with:
          path: ~/.elan
          key: ${{ runner.os }}-${{ runner.arch }}-elan-${{ steps.lean.outputs.hash }}

      - name: Install pinned Lean toolchain
        shell: bash
        env:
          LEAN_TOOLCHAIN_FILE: ${{ steps.lean.outputs.file }}
        run: |
          set -euo pipefail
          toolchain="$(tr -d '\r\n' < "$LEAN_TOOLCHAIN_FILE")"
          test -n "$toolchain"
          if [[ ! -x "$HOME/.elan/bin/elan" ]]; then
            curl --proto '=https' --tlsv1.2 -sSf https://elan.lean-lang.org/elan-init.sh \
              | sh -s -- -y --default-toolchain none
          fi
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
          if ! "$HOME/.elan/bin/elan" toolchain list 2>/dev/null | grep -qF "$toolchain"; then
            "$HOME/.elan/bin/elan" toolchain install "$toolchain"
          fi
          "$HOME/.elan/bin/elan" default "$toolchain"
          "$HOME/.elan/bin/lake" --version

      - name: Install pinned SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.103

      - name: Restore candidate locked dependencies
        run: |
          dotnet restore candidate/Meta/StrataLint/StrataLint.sln --locked-mode
          dotnet restore candidate/Meta/StrataLint/CompileFailProof/CompileFailProof.csproj --locked-mode
          dotnet restore candidate/Meta/StrataLint/BannedApiCompileFailProof/BannedApiCompileFailProof.csproj --locked-mode

      - name: Build candidate with warnings as errors
        run: dotnet build candidate/Meta/StrataLint/StrataLint.sln --no-restore --configuration Release --warnaserror

      - name: Run candidate golden and integration tests
        run: dotnet test candidate/Meta/StrataLint/StrataLint.sln --no-build --no-restore --configuration Release --verbosity normal

      - name: Run candidate selftest twice and compare bytes
        working-directory: candidate
        shell: bash
        run: |
          set -euo pipefail
          dotnet Meta/StrataLint/StrataLint.Cli/bin/Release/net10.0/StrataLint.dll selftest > "$RUNNER_TEMP/candidate-selftest-1.txt"
          dotnet Meta/StrataLint/StrataLint.Cli/bin/Release/net10.0/StrataLint.dll selftest > "$RUNNER_TEMP/candidate-selftest-2.txt"
          cmp "$RUNNER_TEMP/candidate-selftest-1.txt" "$RUNNER_TEMP/candidate-selftest-2.txt"

      - name: Prove missing capability does not compile
        shell: bash
        run: |
          set +e
          output="$(dotnet build candidate/Meta/StrataLint/CompileFailProof/CompileFailProof.csproj --no-restore --configuration Release 2>&1)"
          status=$?
          set -e
          printf '%s\n' "$output"
          test "$status" -ne 0
          grep -F "CS7036" <<<"$output"
          grep -F "MetaClear" <<<"$output"

      - name: Prove banned deterministic APIs do not compile
        shell: bash
        run: |
          proof="candidate/Meta/StrataLint/BannedApiCompileFailProof/BannedApiViolations.cs"
          set +e
          output="$(dotnet build candidate/Meta/StrataLint/BannedApiCompileFailProof/BannedApiCompileFailProof.csproj --no-restore --configuration Release 2>&1)"
          status=$?
          set -e
          printf '%s\n' "$output"
          test "$status" -ne 0
          mapfile -t expected_lines < <(
            grep -nF "// banned-api-proof" "$proof" | cut -d: -f1
          )
          mapfile -t actual_lines < <(
            sed -n 's#.*BannedApiViolations.cs(\([0-9][0-9]*\),[0-9][0-9]*): error RS0030:.*#\1#p' <<<"$output" | sort -nu
          )
          test "${#expected_lines[@]}" -gt 0
          test "${#actual_lines[@]}" -eq "${#expected_lines[@]}"
          test -z "$(grep -F ': error ' <<<"$output" | grep -vF ': error RS0030:' || true)"
          for index in "${!expected_lines[@]}"; do
            test "${actual_lines[$index]}" = "${expected_lines[$index]}"
          done

  baseline-admission:
    name: Content-addressed dev baseline admission
    # 16 GB arm runner (free for public repos): the judge's Lean inspection loads
    # the mathlib environment; 7 GB x64 runners get VM-killed (runner shutdown).
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 45
    steps:
      - name: Check out candidate with history
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
          persist-credentials: false
          path: candidate

      - name: Resolve dev baseline content address
        id: baseline
        shell: bash
        run: |
          set -euo pipefail
          if [[ "$GITHUB_EVENT_NAME" == "pull_request_target" ]]; then
            sha="${{ github.event.pull_request.base.sha }}"
          else
            sha="${{ github.event.before }}"
            if [[ -z "$sha" || "$sha" =~ ^0+$ ]]; then
              sha="$(git -C candidate rev-parse "$GITHUB_SHA^")"
            fi
          fi
          if [[ -z "$sha" || "$sha" =~ ^0+$ ]]; then
            printf '%s\n' "event has no content-addressable dev baseline" >&2
            exit 1
          fi
          echo "sha=$sha" >> "$GITHUB_OUTPUT"
          echo "DEV_BASELINE_SHA=$sha" >> "$GITHUB_ENV"

      - name: Check out content-addressed dev baseline
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.baseline.outputs.sha }}
          fetch-depth: 1
          persist-credentials: false
          path: baseline

      - name: Verify baseline checkout
        shell: bash
        run: |
          set -euo pipefail
          actual="$(git -C baseline rev-parse HEAD)"
          test "$actual" = "$DEV_BASELINE_SHA"
          test -f baseline/Meta/StrataLint/StrataLint.sln
          printf '%s\n' "Baseline judge content address: $DEV_BASELINE_SHA" >> "$GITHUB_STEP_SUMMARY"

      - name: Cache elan and pinned Lean toolchain
        uses: actions/cache@v4
        with:
          path: ~/.elan
          key: ${{ runner.os }}-${{ runner.arch }}-elan-${{ hashFiles('candidate/lean-toolchain', 'baseline/lean-toolchain') }}

      - name: Install pinned SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.103

      # Per-sha keys plus prefix restore let both trees and their versioned
      # inspection memos accumulate without pinning the oldest cache forever.
      - name: Restore candidate and baseline Lean build artifacts
        uses: actions/cache/restore@v4
        with:
          path: |
            candidate/.lake
            baseline/.lake
            ~/.cache/mathlib
          key: ${{ runner.os }}-${{ runner.arch }}-lake-${{ hashFiles('candidate/lake-manifest.json', 'baseline/lake-manifest.json') }}-${{ hashFiles('candidate/lean-toolchain', 'baseline/lean-toolchain') }}-${{ github.event.pull_request.head.sha || github.sha }}-${{ steps.baseline.outputs.sha }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-lake-${{ hashFiles('candidate/lake-manifest.json', 'baseline/lake-manifest.json') }}-${{ hashFiles('candidate/lean-toolchain', 'baseline/lean-toolchain') }}-

      - name: Run the shared baseline harness gate
        shell: bash
        run: |
          set -euo pipefail
          gate="$GITHUB_WORKSPACE/baseline/.github/scripts/harness-gate.sh"
          if [[ ! -x "$gate" ]]; then
            if git -C candidate log --format=%H "$DEV_BASELINE_SHA" -- .github/scripts/harness-gate.sh | grep -q .; then
              printf '%s\n' "baseline harness-gate.sh disappeared after its recorded introduction" >&2
              exit 1
            fi
            gate="$GITHUB_WORKSPACE/candidate/.github/scripts/harness-gate.sh"
            echo "::warning title=harness-gate bootstrap::The predecessor predates the shared gate; using the candidate only for this one-time introduction observation."
          fi
          "$gate" \
            --candidate "$GITHUB_WORKSPACE/candidate" \
            --judge-root "$GITHUB_WORKSPACE/baseline" \
            --base "$DEV_BASELINE_SHA"

      - name: Save candidate and baseline Lean build artifacts
        if: always()
        uses: actions/cache/save@v4
        with:
          path: |
            candidate/.lake
            baseline/.lake
            ~/.cache/mathlib
          key: ${{ runner.os }}-${{ runner.arch }}-lake-${{ hashFiles('candidate/lake-manifest.json', 'baseline/lake-manifest.json') }}-${{ hashFiles('candidate/lean-toolchain', 'baseline/lean-toolchain') }}-${{ github.event.pull_request.head.sha || github.sha }}-${{ steps.baseline.outputs.sha }}
