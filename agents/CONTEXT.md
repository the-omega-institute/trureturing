# Agent Context

Theory: D5 studies the golden integer/Zeckendorf coordinate system and its formal, narrative, and experimental consequences without promoting either open heart O-5/O-6. W1 uses Fibonacci weights `1,2,3,5,...`; W2 permits no adjacent occupied weights; W3 says the canonical W digits decode uniquely to the original natural number.

Map: `D5/` is the only M0 formal instance; `Blueprint/` and `Evidence/` mirror its addresses; `Chronicle/` is time-indexed and its history belongs to Git; `Library/` ingests; `Papers/` emits; `tools/` holds the harness and `tools/tests/` its tests; `Meta/` holds only FILEMAP, registry/domain data, and the digestion ledger; `agents/` holds charters. `Metallic/`, `Moduli/`, and non-D5 theory roots remain gated by D5-T0009. Controlled domains and strata live only in `Meta/domains.yaml`.

1. Route: write a flat manifest with `theory`, `plane`, `domain`, `module`, `generality`, `selector`, `artifact`, `tag`, and for F/B/formal-E an optional `subdomain` (a `/`-joined CamelCase chain of any length; adjacent segments must differ), then run `dotnet run --project tools/StrataLint.Cli/StrataLint.Cli.csproj --configuration Release -- route <manifest>`. GID grammar is `THEORY/[PLANE/]PATH[.selector][--tag]`; F omits `F`, E requires a selector plus artifact kind, and P uses `--frozen` for its frozen manifest. Given a supported, human-gate-approved semantic manifest, at most one canonical representation exists and admission occurs exactly once; unsupported or unapproved manifests receive zero admissions by fail-closed design.
2. Edit: create only the returned path and use its plane-specific `skeleton`. F receives the canonical Lean header; changed unfrozen D5 content and first freezes also require `utility:` between anchors and digest (spec A5.1), even without a state file. Ordinary certified instances and bounded enumeration require a validated refutation with `result` and `claim`; consumer or terminal labels do not exempt them. A companion theorem may turn a positive/existential counterexample into `Not claim`. Classification and source fidelity still need independent review. Machine fields are ASCII; modules/types are CamelCase; theorem names are snake_case; prose may be Unicode; never hand-write status. The sole normative specification is `docs/develop/spec/golden-ledger-repo-spec.md`.
3. Check: run `make -C tools engineering`, then `make current`. CI runs `make -C tools ci-build` once, then engineering (`CI_BUILD_ROUND` from the build receipt) and current independently. Local engineering prepares the same build when starting a round. Engineering retains all candidate test TRX and bound build evidence under `build/ci`; current produces `.lake/build/stratalint/raw-lean-report.json` and runs Scribe, filemap, and current invariants with the built candidate DLL. `make delta BASE=<40-hex-commit-sha>` joins both completed branches from the same build round and checks only cross-tree constraints. `make preflight` defaults to push (engineering + current); `make preflight MODE=pr BASE=<40-hex-commit-sha>` requires a clean committed source tree and checks an isolated merge-tree candidate. The .NET rule engine never invokes Lean. Stage summaries preserve raw exits; make itself only reports success or recipe failure.
4. Deposit: `make deposit ATOM_ID=... GID=... BASE=...` resolves BASE to an immutable SHA for `deposit-header-check`. Only baseline-frozen membership earns historical exemption. The current Lean report (including typed refutation evidence when required) is checked before freeze; exact designated-result atom coverage is required at final FirstFreeze admission. A candidate-only state pin does not skip the utility precheck.
