### 受影响与不受影响的范围

- **受影响的只是转写**:G1–G4 的四条结论、`κ(α) = (α+1)/(2(α+2))`、G4 的两族反例
  `(1,0,1,0)` 与 `(1,1,1,1)`,**都是针对论文的乘积定义推出的**,本身不受此勘误影响;
- **受影响的是可形式化性**:任何按增订原文的比值形式建模的 Lean 实现,都会得到与 G1 不一致的卷积,
  从而或者证不出 G1,或者「证对了错题」。故形式化**必须**采用上面这条更正后的定义。

