# 十、对仓库而言，最值得新增的是“带内部事件的返回证明”

这次的结构与仓库现有模块可以清楚对接。

`CongruenceKernel` 研究的是：两个状态在每一个相同迭代时刻，是否仍具有相同观察。这适合判断四寄存器投影为什么不是确定商。

但宏返回一步对应的微步数：

$$
\ell(m,d)
$$

依赖当前状态，因此不是简单的逐步同步商。

新的接口应当同时证明：

$$
\boxed{
T^{\ell(m,d)}\iota(m,d)=\iota(R(m,d)),
}
$$

以及：

$$
\boxed{
\text{该段微轨迹中的输出序列}
=
\operatorname{Emit}(m,d).
}
$$

再把历史不变量：

$$
\mathcal I(m,d)
$$

交给通用归纳安全性定理。

仓库 `InvariantSafety.lean` 已经证明：初态包含、转移保持、蕴含安全这三个条件，足以控制全部有限执行。

当前我们已经为一个具体程序提供了这些条件的内容：

$$
\boxed{
\text{控制修正的守恒量}
+
\text{减法循环的终止性}
+
\text{试除历史的不变量}
+
\text{输出事件的保持}.
}
$$

FRACTRAN 也已有作为形式化归约中间语言的先例；例如 Hilbert 第十问题的 Coq 形式化使用了 FRACTRAN，而不是只将其作为趣味程序。([arXiv][3])

但本轮读取的仓库源码并未因此自动包含这些新证明。此次没有修改仓库，也没有运行 Lean 内核。

