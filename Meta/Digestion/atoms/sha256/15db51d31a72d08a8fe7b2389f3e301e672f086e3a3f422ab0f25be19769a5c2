## 8. 名称的数学定义

把一个分类或名字写成：

$$
q:X\to Q.
$$

两个对象具有相同名字，当：

$$
q(x)=q(y).
$$

于是名字产生一个等价关系：

$$
x\sim_qy
\iff
q(x)=q(y).
$$

名字真正做的是：

$$
\boxed{
X
\longrightarrow
X/{\sim_q}.
}
$$

因此每一个名字都包含两部分：

1. 它保存了什么；
2. 它把什么识别为相同。

信息逃逸不来自“名称存在”，而来自：

$$
\boxed{
q\text{ 非单射}.
}
$$

---

