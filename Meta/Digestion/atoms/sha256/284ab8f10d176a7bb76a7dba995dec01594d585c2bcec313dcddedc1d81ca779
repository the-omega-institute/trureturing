# 八、无穷尺度的临界，不是“某个有限盒子更复杂”，而是模型类别发生变化

本轮读取的项目模块：

```text
D5/S3/Analytic/Boundary/ZetaPrimeProductCommonBoundary
```

已经把 ζ 配分函数、素数激活、有限支撑性、局部熵和 Fisher 敏感性的收敛边界放在一起。

其核心可以用一个简单模型理解。

对每个素数 \(p\)，独立选择指数：

$$
\Pr(A_p=k)
=
(1-p^{-s})p^{-sk},
\qquad s>0.
$$

每个单独坐标都合法、归一化，而且随 \(s\) 光滑变化。

其非零概率为：

$$
\Pr(A_p>0)=p^{-s}.
$$

