**更正命题 29.12(候签定理 4.116:闭式序列的 Stieltjes 反证与 12×12 行列式边界)**〔errata;勘正候签定理 4.116〕。定义 `q_n := closedFormQ n`,即源卷式 (7.3)。存在整数向量 `c : Fin 11 → ℤ` 使 `∑_{i,j : Fin 11} c_i c_j q_{i+j+3} < 0`。令 `A = H_11^{(3)} = (q_{i+j+3})_{0≤i,j≤10}`。若 `H_∞^{(0)} = (q_{r+s})_{r,s≥0}` 全非负,则选取行指标 `0,…,10` 与列指标 `3,…,13` 所得的 `A` 也全非负;于是 `A` 的所有主子式非负,又因 `A` 为实对称矩阵,故 `A ⪰ 0`,从而 `cᵀAc ≥ 0`,与上述严格负值矛盾。因此 `(q_n)` 的无穷 Hankel 矩阵不全非负。另一方面,若 `(q_n)` 是 Stieltjes 矩序列并由 `[0,∞)` 上的正测度 `μ` 表示,取 `p(t)=∑_{i=0}^{10} c_i t^i`,则 `cᵀAc = ∫_{[0,∞)} t³p(t)² dμ ≥ 0`,仍与上述严格负值矛盾;故 `(q_n)` 不是 Stieltjes 矩序列。原猜想 9.5 是 B 子句与 Q 子句的合取;Q 子句为假,故该合取为假;B 子句不由本论证处理,仍未被建立。原猜想中的计数序列 `(Q_n)` 与闭式序列 `(q_n)` 的对应依据原论文 Theorem 7.1,其组合识别由式 (7.3)、二项式下标越界时取零的约定及 §2.7 的 `Q_0 = 1` 构成;该组合识别不属于本库已形式化的内容,故由 `(q_n)` 转及 `(Q_n)` 时以此文献结果为前提。最后,`A` 是 `M = H_12^{(1)} = (q_{i+j+1})_{0≤i,j≤11}` 删去首行首列所得的主子矩阵,故把 `c` 前添一个零即给出 `M` 的一个负方向;但有负方向不确定 `det M` 的符号。因此本论证不确立目标不等式 `det (q_{i+j+1})_{0≤i,j≤11} < 0`;该子句由本论证保留、未被建立。∎
