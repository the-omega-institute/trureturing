## 6.3 整数约束为什么特别重要？

在整数模型中，指数不能只增加 \(0.2\) 层。每次必须完整支付：

$$
\ln p.
$$

增加第 \(a\) 层的单位收益是：

$$
\boxed{
r(p,a)
=
\frac{
\ln\left(
\frac{1-p^{-(a+1)}}{1-p^{-a}}
\right)
}{
\ln p
}.
}
\tag{6.5}
$$

一个局部整数指数 \(a\ge1\) 最优，需要：

$$
r(p,a)\ge\lambda\ge r(p,a+1).
$$

指数为零时，需要：

$$
\lambda\ge r(p,1).
$$

于是可以从观察到的配置，反推出允许的资源价格。

