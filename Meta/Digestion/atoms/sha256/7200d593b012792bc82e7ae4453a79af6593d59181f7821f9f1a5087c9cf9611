## 8.2 局部收益递减，不代表全局余量单调

令：

$$
E=\ln n,\qquad a=v_p(n).
$$

沿一次乘 \(p\) 转移，Robin 对数余量满足：

$$
\boxed{
\begin{aligned}
\Delta(pn)-\Delta(n)
={}&
\ln\frac{\ln(E+\ln p)}{\ln E}\\
&-
\ln\left(
1+\frac1{p+p^2+\cdots+p^{a+1}}
\right).
\end{aligned}
}
\tag{18}
$$

第一项是全局规模预算的变化，第二项是局部因数权重的变化。

两者的比较依赖当前整个整数，而不仅是被修改的数位。

所以，不能只证明“指数加一合法”，就得到“安全性保持”。

