**定理 140.8（有限未来方向响应的可区分族）。** 对每个 $R\ge20$、$k\ge2$，置
$$
q=\left\lfloor\sqrt{R/5}\right\rfloor\ge2.
$$
存在 $(q+1)^{q-1}$ 条合法实际历史，其当前语法状态相同，并且仅接入同一段 $R-5$ 个零，就产生两两不同的有限方向响应。因此
$$
B_{\rm out}(R,k)\ge(q-1)\log_2(q+1),
\qquad
B_{\rm leg}(R,k)\ge(q-1)\log_2(q+1).
\tag{140.18}
$$

**证明。** 定义严格递减奇长度
$$
L_i=2(q-i)+3\quad(1\le i\le q),
$$
即 $2q+1,2q-1,\ldots,3$。对
$$
\mathbf h=(h_1,\ldots,h_{q-1})\in\{0,\ldots,q\}^{q-1},
$$
取词
$$
v_{\mathbf h}=1\,0^{L_1}1\,(01)^{2h_1}\,0^{L_2}1\,
\cdots\,(01)^{2h_{q-1}}\,0^{L_q}1,
\tag{140.19}
$$
并在左侧补零至恰长 $R$，所得窗口记为 $w_{\mathbf h}$。未补零长度是
$$
m(\mathbf h)=q^2+3q+1+4\sum_{i=1}^{q-1}h_i
\le5q^2-q+1\le R.
\tag{140.20}
$$
其中长串总长度为 $q^2+2q$，基本一端点有 $q+1$ 个，每段填充长 $4h_i$。所以所有参数选择都得到窗口。词不含 $11$，可以在两侧接零成为对所有 $k\ge2$ 都合法的实际配置；当前零后缀为零，语法状态均为一。

除了 $q$ 个长候选，填充段只产生长度一候选，每个都受下一条长候选支配。左补零没有窗口内的一位左端点，不增加完整候选。长候选严格递减，均不受后来候选支配，因此规范队列恰为这 $q$ 条长串。

记长候选的初始端点为 $(a_i,b_i)$。尾部固定为 $1\,000\,1$，所以
$$
a_q=-5,\qquad b_q=-1,
\qquad a_{i+1}-a_i=L_i+1+4h_i.
\tag{140.21}
$$
给每个窗口接相同全零输入，令 $f_{\mathbf h}(n)$ 为接入 $n$ 个零后的方向，$n=0$ 表示当前方向。没有新完整候选出现。第 $i$ 条候选在第 $n$ 次更新后仍完整恰当且仅当 $a_i-n\ge-R$，故首次过期时间是
$$
t_i=R+a_i+1.
\tag{140.22}
$$
首条左端点为 $-m(\mathbf h)$，由（140.20），
$$
t_1=R-m(\mathbf h)+1\ge R-5q^2+q\ge q.
$$
于是
$$
2\le q\le t_1<\cdots<t_q=R-4.
\tag{140.23}
$$
在最后一次过期之前，获胜者按 $1,\ldots,q$ 的顺序切换；被删除短串不可能插入，因为它们的支配者不会先过期。

非切换更新中，锚不变，新零使方向翻转。第 $i$ 次真实候选切换时，两个锚之间零位数是
$$
2h_i+L_{i+1},
$$
为奇数。因此新锚的运输值比旧锚继续运输的值再翻一次，与新读零导致的一次翻转相抵，该次实际方向不翻。故对于 $1\le n<t_q$，
$$
D_{\mathbf h}(n):=
 f_{\mathbf h}(n)\oplus f_{\mathbf h}(n-1)\oplus1
=1
\quad\Longleftrightarrow\quad
n\in\{t_1,\ldots,t_{q-1}\}.
\tag{140.24}
$$
初始方向也不依赖参数，等于 $(q-1)\bmod2$：各填充含偶数个零，后续 $q-1$ 条长零串各奇。

最后一次过期后转为无锚默认输出，不能与两个真实锚之间的切换混同。因此只观察
$$
f_{\mathbf h}(0),\ldots,f_{\mathbf h}(R-5),
$$
恰在 $t_q-1$ 截止。由（140.24）恢复有序的 $t_1,\ldots,t_{q-1}$，加上共同已知的 $t_q=R-4$，即恢复
$$
h_i=\frac{t_{i+1}-t_i-L_i-1}{4}
\qquad(1\le i<q).
\tag{140.25}
$$
所以这些有限响应两两不同。

若确定状态将其中两条历史合并，同一全零输入及同一确定更新就会产生相同响应，与（140.25）矛盾。故可达状态至少 $(q+1)^{q-1}$ 个，固定二进制位数至少其二进对数，得到（140.18）。区分只用有限合法零串，不依赖无限后缀事件；所有初始 $j$ 都相同，也不借语法差异制造下界。证毕。

