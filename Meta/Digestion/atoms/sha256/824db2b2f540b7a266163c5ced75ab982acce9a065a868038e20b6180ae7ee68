**候签定理 4.132(A393170 自指逆级数的奇偶律)**〔open;**档位:第一档**(第 5⁵ 条)——OEIS 评注,页面仍标 conjecture,open 因无人看而非因难;**文献核对(2026-09-09)**:R30 搜题席(nyxid-oracle / ChatGPT Pro)检索 `"A393170" proof`、`"A393170" "a(n) is odd iff n is a power of 2"`、定义式的精确短语、`site:arxiv.org "A393170"`、`site:mathoverflow.net "A393170"` 与 scholar 域精确 ID 查询,**唯一相关命中为 OEIS 本身**,未见证明、预印本或后续讨论;**本仓核对由 orchestrator 亲跑三维**(名字维 `A393170` 零命中;结论形状维 `Odd … ↔ … 2 ^` 命中 27 个模块、其中 10 个涉幂级数,逐个排除;定义形状维「自指逆级数」命中 4 个,逐个排除——最接近的 `D5/S1/Recurrence/Invariants/AbsoluteReciprocalSquareParity` 做的是 A380710,方程为 `A = 1 + X·A·abs(1/A²)`,非本条);落 `D5/S1/Recurrence/Invariants/SelfReferentialInverseSeriesParity`(**随该桶既有同类归族**,不落 `D5/S3/Arith/`;该桶实测 25 个 `.lean`,SL-003 上限 48);**拟议逃逸见证(写在探针之前,不事后追认)**:**奇偶分离约化**——在特征二下记 `B = 1/A`、`C = 1/(A+x)`,定义条件对偶 `n` 说 `B` 的正偶次系数为零、对奇 `n` 说 `C` 的奇次系数为零,故可写 `B = 1 + x·U(x²)`、`C = V(x²)`;由 `1/C = A + x = 1/B + x` 得 `C(1 + xB) = B`,代入后按奇偶分离两端,偶部给 `V + x²UV = 1`、奇部给 `V = U`,于是 `B = 1 + xC`;再代回 `B = 1/A`、`C = 1/(A+x)` 并乘以 `A(A+x)`,在特征二下 `xA + xA = 0`,得 `A² + A = x`。**该约化是本条的内容**:它把一族以「某个逆级数的系数为零」表述的条件,塌成一个不含逆的二次方程;两侧推论(单位常数解的支撑为 `{0} ∪ 2 的幂`,与 `n > 0` 相交即 2 的幂)判为绑定即得。**复用声明**:`A² + A = x` 这一方程本身已由 `D5/S3/Arith/ArtinSchreierTracePowersOfTwo.artinSeries_square_add` 冻结,其解 `artinSeries` 的支撑亦已刻画;**唯一性**须按 `D5/S1/Recurrence/Invariants/` 一族既有的公开 `generating_unique` 形式给出(上游 `ArtinSchreierTracePowersOfTwo` 的对应引理为 `private`,取不到,重证不违先库后证但须写成公开的一般形式,使后来者可用)〕。原文(A393170 评注,逐字):设 `A(x)=Σ_{n≥0} a(n)xⁿ` 由「对每个 `n ≥ 1`,`[xⁿ] 1/(A(x) − n·x) = 0`」定义。则对 `n > 0`:`a(n)` 为奇 ⟺ `n` 是 2 的幂。**产地(第 9′ 条)**:选题由 R30 搜题席产出并附上述检索留痕;上述约化的每一步由 orchestrator(claude 主循环)独立复算后写入本条,非转述;落点与复用声明由 orchestrator 依本仓既有同类模块的落地形状确定;实施与评审席另派。
