## 定理8.1：有限历史中的坏状态检测下界

对终点计数 \(a\)，令总事件数为 \(L\)。若存在某个合法前缀状态被标记为坏，则：

$$
\boxed{
\Pr(\text{整条历史曾经过坏状态})
\ge
\frac1{\binom{L}{\lfloor L/2\rfloor}}.
}
\tag{21}
$$

### 证明

任取一个坏前缀 \(b\)，令 \(t=|b|\)。

由式（8）：

$$
\Pr(B_t=b)
=
\frac{\prod_i\binom{a_i}{b_i}}{\binom Lt}
\ge
\frac1{\binom Lt}
\ge
\frac1{\binom{L}{\lfloor L/2\rfloor}}.
$$

经过任意坏状态的概率，不小于经过这个具体坏状态的概率。证毕。

所以，若能严格证明：

$$
\Pr(\text{经过坏状态})
<
\frac1{\binom{L}{\lfloor L/2\rfloor}},
$$

就能推出根本没有坏状态。

