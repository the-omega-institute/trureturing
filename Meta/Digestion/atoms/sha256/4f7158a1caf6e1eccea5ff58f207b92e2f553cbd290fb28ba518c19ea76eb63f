**命题 142.9（指定响应的精确续接下界具有任务依赖性）。** 固定历史上的目标响应 $F$，并设确定性状态表示由 $M:\mathcal H\to\mathcal S$、输出 $o$ 及合法新位的更新 $U_x$ 组成，满足
$$
o(M(h))=F(h),\qquad M(h\cdot x)=U_x(M(h)).
\tag{142.17}
$$
若两条历史可由某个共同合法的有限续接串产生不同目标响应，则它们的状态不同。若有 $N$ 条两两如此可区分的历史，则表示至少需要 $N$ 个状态；固定长度二进制编码至少需要 $\lceil\log_2N\rceil$ 位。

这里使用定理120.4—120.5中状态与完整响应、历史与剩余响应的分离原理，并将区分论证限制在两条历史共同合法的有限续接上；所需结论是指定目标的必要状态数下界。

这一界只约束式（142.17）指定的响应。因此，第139节命题139.12的摘要反例及第140节定理140.8、140.9的精确续接存储下界，约束定义139.3的同一个窗口选择器；它们不限制所有初始化持久观察者。

