**命题 140.7（来源状态的实际建立与窗口初始化）。** 规范窗口记忆可仅由一次实际顺序读取建立；精确语法状态 $j$ 仍须由其自己的实际记录建立。

**证明。** 将零后缀和规范队列的更新记为 $\widehat T_x$。临时全零窗口 $0^R$ 的状态是 $s_\circ=(\varnothing,R)$。若实际窗口从旧至新为 $w_1\cdots w_R$，则
$$
\widehat M(w)
=\widehat T_{w_R}\circ\cdots\circ\widehat T_{w_1}(s_\circ).
\tag{140.17}
$$
事实上，读到前 $n$ 位时，临时窗口是 $0^{R-n}w_1\cdots w_n$；由定理140.4对 $n$ 归纳，其状态就是对应复合映射的值。临时前缀全零，不提供伪造的一位左端点；$n=R$ 时它全部移出，正好得到实际窗口及其运输相位，不需要保留原始整窗或查询更早的运输历史。

这个临时前缀不等于实际历史，因而（140.17）没有恢复窗口外的真实连续一状态。若 $R<k-1$ 且窗口全为一，真实 $j$ 可以有多个值。例如 $R=2,k=4$，两份实际历史末尾为 $011$ 与 $0111$，窗口同为 $11$，但 $j$ 分别是二和三，下一位一的合法性不同。实际持续记录可以按（140.9）维护 $j$；若从当前处真实向过去读取直到最近零，因为禁止 $1^k$，至多读 $k$ 位即可直接确定 $j$。读取及初始校准是取得这些状态的操作前提，不由存储编码上界免费提供。证毕。

式（140.16）计数的是持久状态字段，未给临时执行索引、输入调度和机器工作区规定峰值用量；也不声称具体编码最省。

