## 定义 464.1（拓扑知识）

[
\boxed{
K_{\tau}(P)
===========

\operatorname{Int}_{\tau}(P).
}
]

在状态 (x) 上：

[
x\in K_\tau(P)
]

当且仅当存在可观察开邻域 (U)：

[
x\in U\subseteq P.
]

含义是：

> 主体能够用当前观察精度找到一个全部支持 (P) 的证据邻域。

---

