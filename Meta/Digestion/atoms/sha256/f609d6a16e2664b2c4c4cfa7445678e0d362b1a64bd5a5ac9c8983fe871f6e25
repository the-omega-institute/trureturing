### 43.10 四个测试逐个不可删

表只展示最后一点的重要性；完整四点下界是：对每个 $j\in\{0,1,2,3\}$，记录 $(0,-j)$ 与 $(0,-4)$ 在 $R\setminus\{(0,j)\}$ 上均给 $\operatorname{ok}(0)$，在 $(0,j)$ 处却分别给 $\bot$ 和 $\operatorname{ok}(0)$。所以删任一点都会失去分离性，$\tau(R)=4$。这同时反驳普遍三测试上界及“只要测试张成仿射包就足够”的主张，也说明把 $\bot$ 与 $\operatorname{ok}(0)$ 混同会消灭必要见证。

