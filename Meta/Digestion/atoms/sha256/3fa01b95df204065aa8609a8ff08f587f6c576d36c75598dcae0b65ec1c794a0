**因此，现在缺少的已经可以表述为一个仓库原生的离散证明任务：构造并证明一个对该转移系统有效的算术安全不变量。**

但这里也必须分清工具与内容：

仓库的 `invariant_safety` 证明了“满足这些条件的不变量足以保证安全”，**它不会自动为这个具体算术系统产生满足条件的 \(J\)**。

若直接取：

$$
J=\operatorname{Safe},
$$

仍然需要证明式（15）。那恰好是尚未完成的全局算术控制，不能被定义本身消除。

---

