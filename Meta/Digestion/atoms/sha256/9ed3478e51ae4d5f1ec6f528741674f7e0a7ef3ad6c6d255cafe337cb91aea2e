## 33. 鲁棒信念集、主动实验与 2027 状态接口

第32章把单一后验 $μ_t$ 的适用范围收紧到固定协议。本章继续处理一个更强的情形：通道本身只知道属于一个模型族，因而当前状态不是一个后验点，而是一组仍然可能的后验。这里的“鲁棒”只表示对这组模型取最坏情形；它不等同于已经完成的分布鲁棒学习，也不自动给出无限时域的最优策略。以下命题是 `repo-derived/open` 的条件性结果；本章没有新增 Lean 声明，机器状态仍由新生成的 residual-open 账目表示。

