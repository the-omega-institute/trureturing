**命题 142.7（基底遍历不足以排除选择的合法三周期例）。** 对每个 $k\ge2$，存在 $\mathcal Y_k$ 上遍历的平稳关系律，允许确定的双侧可测零缺陷选择；其公平扩展不遍历。

**证明。** 取
$$
r^{(0)}=(001)^{\mathbb Z},\qquad
r^{(1)}=(010)^{\mathbb Z},\qquad
r^{(2)}=(100)^{\mathbb Z},
\tag{142.15}
$$
并在这三个点上赋均匀概率。左移依次循环三点，因此该律平稳，且不变子集只能为空集或全部三点，故基底遍历。三个序列均不含 $11$，所以属于每个 $\mathcal Y_k$。

定义
$$
h(r^{(0)})=0,\qquad h(r^{(1)})=1,\qquad h(r^{(2)})=0.
\tag{142.16}
$$
三个起态的运输量依次为 $1,1,0$。于是
$$
1=0\oplus1,\qquad
0=1\oplus1,\qquad
0=0\oplus0,
$$
逐一验证（142.12）。在其余合法路径上将 $h$ 定义为零，便得到全空间上的可测函数；该关系律集中在三个已验证的点上，所以其平移输出几乎必然零缺陷。公平扩展的六个正质量点分成 $a=h(r)$ 与 $a=1\oplus h(r)$ 两个不变三环，各有质量 $1/2$，所以不遍历。这是与命题142.6的 Parry 律不同的合法来源。证毕。

