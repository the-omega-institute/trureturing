### 处置

本勘误追加为新 atom;上文原增订的 atom **保持不变**(第〇节),其账目状态不因本勘误改变。
形式化席的报告(flight `gribinski-g1g4/attempt-1`,verdict `refuted`)记录了该反例;
该席**在建模之前停手**,未改定义、未加强假设、未产出 Lean 文件 —— 处置正确,予以照录。

**教训**:增订正文里的公式是**形式化的直接输入**。此前 V5–V7 一族因为 Lean 已先证完、
增订只是回写结算,故未暴露此类风险;G1–G4 是先写增订、后形式化,转写错误就直接传到了实施席。
**凡「先写增订、后形式化」的条目,增订里的每个定义式都应在派席前由 orchestrator 用一个具体数值实例复算一次。**
