**定理 139.14（读数、合法性及后继共同闭合）。** 固定 $R,k$ 和定义139.3的选择规则，记忆（139.32）决定当前方向及锚、下一位的语法合法性以及取得该位后的新记忆。两份实际历史若有相同 $\Sigma$，则在任何共同合法的有限后续位串下，每一步的这些响应都相同。

**证明。** 当 $C\ne\varnothing$，按 $(b-a-1,b)$ 的字典序取最大者 $(a_*,b_*)$，即可恢复所选候选及锚。方向值为
$$
G(C,\ell)=
\left[\ell+
\sum_{\substack{(a,b)\in C\\b>b_*}}(b-a-1)\right]\pmod2.
\tag{139.33}
$$
所选闭合一以后的每个零，唯一地属于一个较后的完整零串，或末端未闭合零后缀；这些集合互不相交并穷尽运输所需的零位。一位的运输量为零，因此（139.33）正是定义139.3的 XOR。$C=\varnothing$ 时取方向零、锚为无候选符号 $\bot$。

新位 $x$ 的合法性为
$$
x=0\quad\text{或}\quad(x=1\text{ 且 }j<k-1).
\tag{139.34}
$$
取得合法 $x$ 后，在新原点的候选集合为
$$
C_x'=\{(a-1,b-1):(a,b)\in C,\ a>-R\}\cup N_x(\ell),
$$
$$
N_x(\ell)=
\begin{cases}
\{(-\ell-2,-1)\},&x=1\text{ 且 }1\le\ell\le R-2,\\
\varnothing,&\text{其他情形}.
\end{cases}
\tag{139.35}
$$
其余更新为
$$
\ell_x'=\begin{cases}\min\{R,\ell+1\},&x=0,\\0,&x=1,\end{cases}
\qquad
j_x'=\begin{cases}0,&x=0,\\j+1,&x=1.\end{cases}
\tag{139.36}
$$

旧候选保留的充要条件是左端点不等于移出的最旧位置，即 $a>-R$；坐标换原点后都减一。任何新候选必须由新一闭合此前非空零后缀。其旧左端点是 $-\ell-1$，新右端点是旧坐标零，故新坐标为 $(-\ell-2,-1)$。该左端点仍在窗口内的条件恰为 $\ell\le R-2$。$\ell=0$ 时无正长度零串，$\ell=R$ 时没有观察到左端点一，$\ell=R-1$ 时左端点会同时移出窗口，都不能新增候选。这证明（139.35），也包括 $R=1,2$ 时新增条件为空的边界。末端零长度和连续一语法状态直接给（139.36）。

若 $h\cdot x$ 表示在实际历史 $h$ 后接入新读数，令 $T_x(C,\ell,j)=(C_x',\ell_x',j_x')$，则上述证明给出
$$
\Sigma(h\cdot x)=T_x(\Sigma(h)).
\tag{139.37}
$$
当前响应通过（139.33）恢复，合法性通过（139.34）恢复，后继通过（139.37）恢复。对共同后续位串的长度归纳，就得到全部有限续接响应相同。证毕。

完整窗口连同精确语法状态 $(w,j)$ 也是充分记忆：删除最旧位、接入新位并更新 $j$ 即可重建全部候选。定理139.14给出另一份充分结构，没有证明它是最小结构。命题139.12只否定所列获胜摘要对原选择器的精确更新能力，不推出任何实现都必须保存完整窗口或全部候选，也不给最小状态数、位数或计算时间下界。

当 $R<k-1$ 且窗口全为一时，窗口本身未必决定真实 $j$。该语法记录必须来自此前实际连续记录，或真实读取足够过去后的建立，不能作为免费补回的隐藏信息。固定词表构造中的表同样是规则描述的一部分，其存在性不自动提供表的取得、存储或验证。允许任意历史查询、改变选择规则或增加读取方向时，还须重新证明扩展任务在当前摘要纤维上不变。

