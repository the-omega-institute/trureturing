完整归纳断言如下：若节点的已选前缀为 $(e_1,\ldots,e_{j-1})$，其分母为 $D=\prod_{i<j}p_i^{e_i}$，且 $V$ 是 $F\prod_{i<j}(1-X_i)$ 中该前缀的系数多项式，那么此调用恰一次访问每个剩余指数 $(e_j,\ldots,e_k)\in\{0,1,2\}^{k-j+1}$，加入的系数是 $V\prod_{i=j}^k(1-X_i)$ 的相应系数，叶分母是 $D\prod_{i=j}^kp_i^{e_i}$。

