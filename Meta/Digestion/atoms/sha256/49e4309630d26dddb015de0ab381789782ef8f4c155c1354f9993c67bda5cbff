## 1.1 一个隐藏在控制标签里的“在途单位”

考虑控制状态 \(11\) 下的两步操作：

$$
\frac{29}{33},\qquad\frac{77}{29}.
$$

第一步：

$$
(a,b,c,e;11)
\longrightarrow
(a,b-1,c,e;29).
$$

第二步：

$$
(a,b-1,c,e;29)
\longrightarrow
(a,b-1,c,e+1;11).
$$

两步合起来，是把一单位从 \(3\) 寄存器搬到 \(7\) 寄存器：

$$
b\mapsto b-1,\qquad e\mapsto e+1.
$$

但在中间状态 \(29\)：

$$
b+e
$$

暂时少了一单位。

所以，若只读数据寄存器，会以为总量丢失；把控制标签一起计入，就得到：

$$
\boxed{
b+e+\mathbf1_{\{\chi=29\}}
}
$$

在这两步中保持不变。

