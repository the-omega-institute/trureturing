## 501. 大素数处只剩通用尾项

设：

$$
H=\{h_1,\ldots,h_k\}
$$

且 \(p>\operatorname{diam}(H)\)。

则所有 \(h_i\bmod p\) 不同，所以：

$$
\nu_p(H)=k.
$$

Hardy–Littlewood 局部因子为：

$$
L_p(H)
=
\frac{1-k/p}{(1-1/p)^k}.
$$

展开：

$$
\boxed{
L_p(H)
=
1-
\frac{k(k-1)}{2p^2}
+
O_k(p^{-3}).
}
$$

关键是 \(1/p\) 项精确消失。

所以：

$$
\sum_p|L_p(H)-1|
$$

对每个固定 \(H\) 收敛。

---

