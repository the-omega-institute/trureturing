# 197. 对角化本质上制造的是行为像缺陷

这是统一观察者理论中一个极重要的重新定位。

设：

$$
e:X\times X\to Y.
$$

把每个 \(a\in X\) 看作一行预测器：

$$
R(a):X\to Y,
\qquad
R(a)(x)=e(a,x).
$$

设：

$$
\delta:Y\to Y
$$

无不动点：

$$
\forall y,\quad
\delta(y)\neq y.
$$

定义对角行为：

$$
d(x)=\delta(e(x,x)).
$$

