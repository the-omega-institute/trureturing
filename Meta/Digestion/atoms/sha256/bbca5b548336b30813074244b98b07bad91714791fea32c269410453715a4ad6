**命题 50.7（支撑、量词与解释边界；repo-derived）。** 每个有限训练词在两模型下都有正概率，因此没有只凭有限词、对两个模型都零失败的确定性辨识器。50.5与§48对同一模型族的全词障碍相容；它没有承诺未知模型下的每词精度。若 $\varepsilon\ge1/8$，不辨识模型的单状态恒定读出 $1/2$ 已能满足本族的全部有限词预测精度。

证明。任一有限词上的确定性模型判定只能给一个身份，另一模型对该词仍赋正概率。对50.3的规则，全匹配与全不匹配词分别选 $A,B$，两侧均有正错误概率，尽管可用增加 $M$ 控制。另一方面，定义50.1的最终翻转使每个非空词的 $q_w^{(p)}\in[1/4,3/4]$，故
$$
|t_p(w)-1/2|=(1-2p)|q_w^{(p)}-1/2|\le1/8;
$$
空词误差为零。这也直接说明辨识并非粗容差预测的必要步骤。

§48的逐词障碍量化每一个相容历史，50.5允许各固定模型下至多 $\delta$ 的失败概率；不是把§49的已知静态混合问题仅换一组量词，§49的 $r=0$ 与这里的正翻转模型本身不同。此处只估计真实模型的下一零报告条件概率，不是保证下一比特实际正确，也不是整个联合记录律的总变差保证。预测先验仍为 $1/2$，$r$ 已知且固定，候选 $p$ 只有两个；任意预测先验、未知 $r$、连续参数族、模型漂移和反复选模都不在此构造的假设内。

RRO定理2.2及§6.6的纤维下降条件说明精确观察商需要额外的常值与更新条件；本构造是允许错误事件的近似观察者，不自动成为精确RRO商，RRO也不为它增加概率前提或认证。已知 $p$ 的小 $r$ 或重叠域下界不确定这里的样本、状态、静态表位数或总成本最优性；特别地，这里的 $r=1/4$ 分别大于 $p_A(1-p_A)=3/16$ 与 $p_B(1-p_B)=2/9$，不能套用对应重叠域的结论。证毕。

