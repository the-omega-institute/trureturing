# 三、那个缺少的坐标，可以是“相关性”，但升起来后是四面体

这里有一个重要而有用的几何区别。

设两个二值随机变量为 \(X,Y\in\{-1,+1\}\)。如果只记录：

$$
m_x=\mathbb E[X],
\qquad
m_y=\mathbb E[Y],
$$

得到的可见空间是一个正方形：

$$
(m_x,m_y)\in[-1,1]^2.
$$

但这两个数不足以恢复联合分布。

还需要：

$$
\boxed{
c=\mathbb E[XY].
}
\tag{5}
$$

