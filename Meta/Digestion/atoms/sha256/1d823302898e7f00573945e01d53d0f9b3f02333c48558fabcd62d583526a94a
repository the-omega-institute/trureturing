**候签定理 4.136(Hanna:A222014 的奇偶律 —— 阶乘积和之第二实例)**〔open;**档位:第一档**(第 5⁵ 条)——OEIS 评注,2024-12-06 加入,页面仍标 Conjecture;**文献核对(2026-09-10)**:R31 搜题席(nyxid-oracle / ChatGPT Pro,`chatgpt-pro-pool`)检索 `"A222014" proof`、编号 + 作者、该 parity 句原文、`arXiv:A222014`、`DOI:A222014`,**未命中证明**;该席另比对 2026 年 OEIS Open 的 492 项自动证明 benchmark 元数据,本编号未命中(此项只排除「刚被解决而 OEIS 未同步」一类假阳,不单独证明开放性);MathSciNet 与 Google Scholar 原生界面该席无法核验,标 `ASSUMED-UNVERIFIED`。**本仓去重由 orchestrator 亲跑(补齐该席标为 `ASSUMED-UNVERIFIED` 的一维)**:`A222014` 在 `origin/dev` 的 `D5/**`、`Blueprint/**`、`Library/**`、`docs/develop/theory/**` 上**零命中**;**同批 8 个候选中 A397588 与 A373561 经同一扫描各得 4 命中(`D5/S1/Recurrence/ConvolutionRecurrenceOddPowersOfTwo.lean`、`D5/S3/Factorization/A373561.lean` 已形式化),故该席排名前二者按第 5⁵ 条硬规则①不派席**;**数值自检(orchestrator 亲跑)**:OEIS 条目所列 22 项中奇数下标为 `[0,1,3,7,15]`,恰为 `2^k−1`,命题在该范围内未被证伪;落 `D5/S1/Recurrence/Parity/FactorialSquareExponentSumParity`(**随 A222013 同桶**:本条须 import 该桶已冻的 `FactorialProductSumCatalanParity` 之 `mod_two_identity` 与唯一性,同层 import 合法);**拟议逃逸见证(写在探针之前,不事后追认)**:**指数参数化的次数收缩** —— 已冻结的 `FactorialProductSumCatalanParity.summand` 把指数**写死**为 `F ^ (r·(r+1)/2)`(第 26 行),而本条的定义方程指数为 `r²`,故其次数收缩引理(`term_coeff_eq_zero` 一族)对本条**不可实例化**;本条的内容即把该收缩重做为**以指数函数为参数**的一般形 `F ^ (e r)`(要求 `e` 在 `r ≥ 1` 时严格递增且 `e r ≥ r`),使 `r·(r+1)/2` 与 `r²` 同为其实例 —— 这是第 4.8 条「**第二个实例才上收**」的正例:A222014 恰是第二个实例,故此时才抽象,不预建空壳。**绑定即得的部分要诚实分栏**:模 2 后 `r! ≡ 0`(`r ≥ 2`)杀掉全部高阶项,仅余 `r = 0, 1` 两项,而这两项在 A222013 与 A222014 中**逐字相同**,故约化终点同为 `A + xA² = 1`;由该方程到「奇数位置恰为 `2^k − 1`」这一步**已由已冻结的 `mod_two_identity` 与 `hanna_conjecture` 给出**,本条**直接引用、不重证**(第 3.1 条),作具名伴随并记一条有向边。**已冻结模块不改**:A222013 那份写死指数的副本按第 1.3 条 永不解冻,新的一般形与它并存,不追溯替换〕。原文(A222014 评注,_Paul D. Hanna_,2024-12-06,逐字):"Conjecture: a(n) is odd iff n = 2^k - 1 for some k >= 0.";定义为 `A(x) = Sum_{n>=0} n! * x^n * A(x)^(n^2) / Product_{k=1..n} (1 + k*x*A(x)^n)`。**产地(第 9′ 条)**:选题由 R31 搜题席产出(8 条第一档候选,按「从原始定义到 Lean 可闭合证明最短」排序,该席主动砍掉 A146557 与 A389540 —— 理由是二者的证明依赖另一条**未经证明**的 OEIS FORMULA,此判断由 orchestrator 采纳);仓内去重、数值自检、以及上述「指数写死 ⟹ 不可实例化 ⟹ 上收为参数化一般形」的判形分析由 orchestrator(claude 主循环)亲跑亲写,非转述;实施与评审席另派。
