**候签定理 4.138(Hanna:A396843 的全奇性)**〔open;**档位:第一档**(第 5⁵ 条)——OEIS 条目建立时(2026-07-01)即写入的猜想,页面仍标 Conjecture;**文献核对(2026-09-10)**:R32 搜题席(nyxid-oracle / ChatGPT Pro,`chrono-chatgpt-pro-pool`)检索 `"A396843" proof`、`"A396843" "Paul D. Hanna"`、精确短语 `"all terms are odd" "A396843"`、`site:arxiv.org "A396843"`、`"A396843" DOI`,**未发现独立证明、arXiv 论文或相关 DOI**,命中的数学来源基本回到 OEIS 本身;MathSciNet / Google Scholar 该席无法穷尽,标 `ASSUMED-UNVERIFIED`;该席另做公开 GitHub issue / PR 搜索,`A396843` 均 0 命中;**本仓源码级去重由 orchestrator 亲跑**:`A396843` 在 `origin/dev` 的 `D5/**`、`Blueprint/**`、`Library/**`、`docs/develop/theory/**` 上**零命中**;**数值自检(orchestrator 亲跑)**:OEIS 所列 23 项**全为奇数**;**三类排除逐条确认**——①该页**未给出**本猜想的证明(仍标 Conjecture);②页面另列的 FORMULA(`A − 3A²` 为奇函数、以及一条 Catalan 型公式)在下述证明中**完全未被使用**,故不存在「未证 FORMULA → 目标」的桥;③非 `conjecture is true (see … link)` 型 —— 该页被明确标 false 的是**另一条** mod-9 猜想,与本靶无关;落 `D5/S1/Recurrence/Parity/CompositionalSquareAllOddCoefficients`(域 `Recurrence` 绑 S1,**落点已核 `Meta/domains.yaml`**,不重蹈评注 29.10 所记的非法地址;与 A222014 / A376527 同桶);**拟议逃逸见证(写在探针之前,不事后追认)**:**特征二下的显式解 + 逐系数唯一性**。原定义为 `A(x·A(x) − 3x·A(x)²) = x²`,`A(x) = Σ_{n≥1} aₙxⁿ`。模 2 后 `−3 ≡ 1`,记 `F = Σ āₙxⁿ ∈ 𝔽₂⟦x⟧`、`H = x(F + F²)`,定义式化为 `F(H) = x²`。**(一)逐系数唯一性**:由 `x²` 项得 `ā₁ = 1`;对 `n ≥ 2` 考察 `x^{n+1}` 的系数——特征二下 `F² = Σ āⱼx^{2j}`,故 `[x^{n+1}]H = āₙ + (ā_{n/2} 若 2∣n,否则 0)`,第二项只涉旧系数;而 `F(H) = Σ_{m≥1} āₘHᵐ` 中,`m = 1` 项把新未知数 `āₙ` **恰好带入一次**,`m ≥ 2` 时 `H` 的最低次为 2 故新系数最早只能出现在 `x^{n+1}` **之上**,外层 `āₙHⁿ` 的最低次为 `2n > n+1`;于是该方程形如 `āₙ + Qₙ(ā₁,…,ā_{n−1}) = 0`,每个 `āₙ` 由前项唯一决定。**(二)显式解**:取 `S(x) = x/(1+x) = x + x² + x³ + ⋯`。在 `𝔽₂⟦x⟧` 中 `S + S² = x/(1+x)²`,故 `H_S = x(S+S²) = x²/(1+x)²`;又 `1 + H_S = 1/(1+x)²`(特征二),于是 `S(H_S) = H_S/(1+H_S) = x²`。由(一)的唯一性得 `F = S`,即**每个 `āₙ = 1`**,亦即全部 `aₙ` 为奇数。∎ **该显式解与唯一性论证是本条的内容**:它把一个复合方程的系数问题塌成「验一个闭式候选 + 唯一性」,不由任何已冻结论或 Mathlib 引理实例化得到;**证明只用 NAME 中的定义方程,不用 Catalan、不用 Lagrange 反演、不用解析**。**orchestrator 的独立复算(亲跑,非转述)**:在 `𝔽₂` 上以截断多项式符号验算,`S = x/(1+x)` 的系数自 `x¹` 起全为 1,`H_S = x² + x⁴ + x⁶ + ⋯`,而 `S(H_S)` 的系数向量**精确等于 `x²`**;故(二)成立。原文(A396843 评注,_Paul D. Hanna_,**2026-07-01**,逐字):"Conjecture: all terms are odd.";定义(NAME,逐字):"G.f. A(x) satisfies A( x*A(x) - 3*x*A(x)^2 ) = x^2."。**产地(第 9′ 条)**:选题与完整证明骨架由 R32 搜题席产出(该席本轮只交 3 条并说明「多数看似一天能证的条目已被证、已进仓、或需不诚实的桥」);**骨架第(二)步由 orchestrator 独立符号复算通过后才立案**——这是本仓选题的硬判据(派席前须能自行复算);OEIS 原文取证、仓内去重、数值自检与落点域校验均由 orchestrator 亲跑;实施与评审席另派。
