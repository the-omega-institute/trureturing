### 算法 23.A（未来词分区细化）

输入：

- 状态列表 \(Y\)；
- 后继表 \(\tau(y)\)；
- 当前读出 \(q(y)\)。

过程：

1. 按 \(q(y)\) 对状态分组，得到 \(c_0\)；
2. 在第 \(m\) 轮为每个状态计算
   \[
   (q(y),c_m(\tau y));
   \]
3. 对签名排序或哈希，生成 \(c_{m+1}\)；
4. 若新旧分区相同则停止，否则继续。

输出：

- 完成类映射
  \[
  c_*:Y\twoheadrightarrow Z_q;
  \]
- 商转移
  \[
  \overline\tau(c_*(y))=c_*(\tau y);
  \]
- 稳定深度 \(m_*\)。

