## 5.2 仓库已经给出另一种真实的非交换残差

本轮读取的 `PrimeSwapCurvature.lean` 定义：

$$
U_p(m,z)
=
(\alpha m+b_pz,\;g_pz).
$$

其中 \(m\) 是记忆坐标，\(z\) 是标量坐标。

虽然标量更新总是交换：

$$
g_pg_qz=g_qg_pz,
$$

但记忆更新的交换差为：

$$
\boxed{
\bigl(U_qU_p-U_pU_q\bigr)_m
=
\mathcal C_{pq}\,z,
}
$$

$$
\boxed{
\mathcal C_{pq}
=
(\alpha-g_q)b_p-(\alpha-g_p)b_q.
}
\tag{9}
$$

仓库已经证明它的反对称性、共同记忆原点变换下的不变性，以及在非共振条件下的原点相容判据。源码同时明确说明：这是代数定理，没有自动建立 ζ 零点或物理曲率的联系。

例如取：

$$
\alpha=\frac12,\qquad b_2=b_3=1,
$$

并使用5040处的两种下一层权重倍率：

$$
g_2=\frac{63}{62},
\qquad
g_3=\frac{40}{39}.
$$

则：

$$
\boxed{
\mathcal C_{23}
=
\frac{63}{62}-\frac{40}{39}
=
-\frac{23}{2418}\ne0.
}
$$

只读 \(z\) 的观察者，看见的是严格交换的标量过程；保留 \(m\) 后，则能看见操作顺序的差异。

这正是：

$$
\boxed{
\text{完整模型有路径记忆，投影模型却表现得平坦。}
}
$$

但 \(b_p,\alpha\) 是这里明确选择的记忆实现。5040本身没有唯一规定它们，也可以构造 \(\mathcal C_{pq}=0\) 的实现。

所以，**我们找到的是“投影平坦”的机制，不是证明宇宙底层必然非平坦。**

