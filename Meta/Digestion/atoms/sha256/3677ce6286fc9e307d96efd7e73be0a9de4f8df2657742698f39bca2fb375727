### 证明

Poisson 核逐点满足

$$
\frac{1-a}{1+a}
\le P_a(\theta)\le
\frac{1+a}{1-a}.
$$

平滑后的谱密度是

$$
f_a(\theta)=\int P_a(\theta-\phi)\,d\mu(\phi),
$$

所以它具有相同上下界。

对任意系数向量 \(x=(x_0,\ldots,x_N)\)，令

$$
q_x(e^{i\theta})=\sum_{j=0}^Nx_je^{ij\theta}.
$$

则

$$
x^*T_N^{(a)}x
=
\int|q_x(e^{i\theta})|^2f_a(\theta)\,\frac{d\theta}{2\pi}.
$$

再用 Parseval 等式

$$
\int|q_x|^2\frac{d\theta}{2\pi}
=
\sum_j|x_j|^2
$$

即可。证毕。

---

前文的 Schur 高度 \(h_N\)，描述新读出相对于旧读出的最小残差平方。相同证明还给出

$$
\boxed{
h_N^{(a)}\ge\frac{1-a}{1+a}
\qquad\forall N.
}
\tag{6}
$$

所以，一个原本随阶数越来越接近退化的矩阵族，经固定平滑以后，可以变成**全部阶数都有统一余量**的矩阵族。

但这份余量从哪里来？

令

$$
m_a=\frac{1-a}{1+a}.
$$

由于 \(f_a\ge m_a\)，平滑测度可以精确拆成

$$
\boxed{
d\mu_a
=
m_a\,\frac{d\theta}{2\pi}
+
(1-m_a)\,d\nu_a,
}
\tag{7}
$$

其中 \(\nu_a\) 仍是概率测度。

也就是说，这个下界可以理解为一个明确的均匀背景成分。

