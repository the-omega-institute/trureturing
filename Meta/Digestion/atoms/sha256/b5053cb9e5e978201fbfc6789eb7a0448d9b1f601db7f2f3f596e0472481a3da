## 还可以得到一个明确的衰减阶数上界

经典零点计数给出

$$
N(T)=O(T\log T).
$$

已有显式研究远强于本轮需要的这个增长阶。([arXiv][6])

取

$$
T=\frac{n}{(\log n)^2}.
$$

消去所有高度不超过 \(T\) 的不同谱点，数量为

$$
K=O\!\left(\frac n{\log n}\right).
$$

剩余谱点满足

$$
|z-1|\le\frac1T.
$$

将它代入上一证明，得到在 RH 前件下

$$
\boxed{
\log h_n
\le
-2n\log n
+
4n\log\log n
+
O(n).
}
\tag{32}
$$

这是上界，不是已证明的精确渐近等式。

