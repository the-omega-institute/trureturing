## 8.3 解析零点计数的间隔

设 \(f\) 是目标解析函数，\(g\) 是近似模型，\(\Gamma\) 是一个闭边界。

若：

$$
\boxed{
\sup_{z\in\Gamma}|f(z)-g(z)|
<
\inf_{z\in\Gamma}|g(z)|,
}
\tag{29}
$$

则两者在边界内部的零点总数相同，计入重数。

其机制是：连接 \(g\) 与 \(f\) 的直线同伦在边界上始终不为零；绕数是整数又连续，所以不能改变。

仓库：

```text
D5/S3/Weil/ZetaAnalytic/RoucheZeroCount.lean
```

已经形式化了矩形边界上的非零同伦、对数导数积分连续性和相应零点计数稳定性。具体定理仍要求解析性、严格边界误差以及零点计数所需的假设，不能省略。

这一点特别贴合你的“边界包含信息”判断：

> **有时候，不必恢复内部全部细节；只要边界误差小于边界非零间隔，就能确定内部一个离散不变量——零点数。**

但这个不变量只记录数量。要证明全部零点在指定直线上，还需要更精细的区域选择、对称性或实根证书。

