### 证明

顶点读出能区分二维黄金块的两个不同特征方向，但完全看不见 \(u,v,\chi\)。

加入底角 \(v_0\) 后，它对 \(u\) 和 \(\chi\) 都有非零读数；由于二者对应特征值 \(0,-2\)，时间演化可以将它们分开。但：

$$
\langle v_0|v\rangle=0,
$$

所以 \(v\) 仍然不可见。

再加入对角底角 \(v_2\)，仍然有：

$$
\langle v_2|v\rangle=0.
$$

它没有补上最后这个缺口。

若改为加入相邻底角 \(v_1\)，则：

$$
\langle v_1|v\rangle\ne0,
$$

最后的隐藏方向被读到，全部五维均可区分。证毕。

所以：

$$
\boxed{
\text{多加一个读数有没有用，取决于它与隐藏方向是否线性独立。}
}
$$

一个与旧观察高度相关的新读数，可以增加数据量，却不增加真正的区分能力。

