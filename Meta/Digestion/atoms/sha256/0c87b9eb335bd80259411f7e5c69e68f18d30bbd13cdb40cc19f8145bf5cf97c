## 13.1 目标族最小商

设目标族：

$$
K_i:X\to Y_i,
\qquad i\in I.
$$

联合目标：

$$
K_*(x)=(K_i(x))_{i\in I}.
$$

规范最小商：

$$
Z_*=X/ker(K_*).
$$

仓库 `TargetFamilyMinimalQuotient` 已机器验证：该商是同时决定整个目标族的最粗充分状态；任何能决定全部目标的其他表示都必须至少同样精细。

---

