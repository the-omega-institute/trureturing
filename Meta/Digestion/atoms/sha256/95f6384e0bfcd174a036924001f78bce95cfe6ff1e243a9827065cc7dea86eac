## 1.1 多价格空间中的临界墙

暂时允许每个素数方向拥有不同的价格：

$$
\boldsymbol\lambda=(\lambda_p)_{p\in S},
$$

其中 \(S\) 是有限素数集合。目标变成：

$$
J_{\boldsymbol\lambda}(a)
=
\sum_{p\in S}
\left[
\ln(1+p^{-1}+\cdots+p^{-a_p})
-\lambda_pa_p\ln p
\right].
$$

固定指数配置 \(a\) 最优，等价于：

$$
\eta(p,a_p+1)\le\lambda_p\le\eta(p,a_p)
$$

对每个 \(a_p>0\) 成立；若 \(a_p=0\)，只需：

$$
\lambda_p\ge\eta(p,1).
$$

因此，多价格空间被一组墙：

$$
\boxed{\lambda_p=\eta(p,k)}
\tag{3}
$$

切成一个个最优配置区域。

而原来的极大丰问题，要求：

$$
\lambda_2=\lambda_3=\lambda_5=\cdots=\lambda.
$$

也就是在这个多参数几何中，只沿着一条**共同价格直线**运动。

这带来一个重要区别：

$$
\boxed{
\text{多参数空间中可以独立调到多个临界墙；}
}
$$

$$
\boxed{
\text{同一个标量价格能否同时碰到它们，受到额外算术限制。}
}
$$

这些限制，正是新的研究入口。

