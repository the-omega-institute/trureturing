# 九、近临界高维区域加入时间后，会暴露什么新约束？

上一轮发现，某个很窄的价格窗口中，可以有 \(M\) 个近临界素数方向。

现在把每个方向的第一层看成一个待执行事件。它们之间没有同轴依赖，因此状态空间是：

$$
\{0,1\}^M.
$$

完整状态数为：

$$
2^M.
$$

但固定串行时间 \(t\) 的切面只包含：

$$
\boxed{
\binom Mt
}
\tag{34}
$$

个状态。

于是，同一个高维区块至少可以从三个角度观察：

$$
\boxed{
\text{全部时间合并： }2^M\text{ 个状态};
}
$$

$$
\boxed{
\text{固定工作量： }\binom Mt\text{ 个状态};
}
$$

$$
\boxed{
\text{固定并行轮数：需要另外限定处理器或可并行方向数量。}
}
$$

如果一个候选选择了 \(r\) 个事件，那么在“每轮每个处理器至多执行一个事件”的模型中：

$$
\boxed{
P\,D\ge r,
}
\tag{35}
$$

其中 \(P\) 为处理器数量，\(D\) 为轮数。

对于一般存在依赖的事件集合，还必须：

$$
D\ge\text{最长依赖链长度}.
$$

所以：

> **把串行时间压缩成更少轮数，通常需要更多并行资源；把状态编码压短，并不意味着这些事件已经免费完成。**

这里不是在证明任何数学证明都至少需要 \(r\) 次操作。闭式公式或统一定理可能一次覆盖很多状态。

式（35）约束的是明确指定的事件执行模型，不能被滥用为 RH 证明的复杂度下界。

