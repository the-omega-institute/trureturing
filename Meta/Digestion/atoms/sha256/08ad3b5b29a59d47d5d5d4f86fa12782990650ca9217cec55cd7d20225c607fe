**定理 140.6（候选数与精确编码上界）。** 规范队列有 $q$ 个记录时，
$$
\frac{q(q+1)}2+q+1\le R,\qquad
q\le Q_R:=\left\lfloor\frac{\sqrt{8R+1}-3}{2}\right\rfloor.
\tag{140.15}
$$
令
$$
L=\lceil\log_2(R+1)\rceil,\qquad J=\lceil\log_2k\rceil.
$$
存在持久状态编码满足
$$
B_{\rm out}(R,k)\le Q_R(2L+1)+2L,
$$
$$
B_{\rm leg}(R,k)\le Q_R(2L+1)+2L+J.
\tag{140.16}
$$
特别地，后者为 $O(\sqrt R\log(R+1)+\log k)$，共同常数不依赖 $R,k$。

**证明。** 当 $q\ge1$，端点排列满足
$$
a_1<b_1\le a_2<b_2\le\cdots\le a_q<b_q.
$$
零内部互不重叠。严格递减的正长度至少是 $q,q-1,\ldots,1$，故至少有 $q(q+1)/2$ 个零位；$a_1,\ldots,a_q,b_q$ 是 $q+1$ 个不同一位，与全部零内部不交。它们均在窗口中，得到（140.15）的第一式。$q=0$ 时该数值不等式由 $R\ge1$ 单独成立，不表示空队列也含一个一。解二次不等式得到 $Q_R$。

预留 $Q_R$ 个按时间排列的槽，每槽以两个 $L$ 位数编码两个相对端点，再用一位编码实际相位，共 $2L+1$ 位。队列长度与 $\ell$ 各用 $L$ 位，精确 $j$ 用 $J$ 位。未用槽位可固定，不承载额外历史。定理140.4给这些记录的精确更新，故得到两种编码上界。若还把固定参数 $R,k$ 作为可配置数据保存，另加 $O(\log(R+1)+\log k)$ 位，不改变上述阶。证毕。

