# 六、一个直接连接5040的统计定理：临界响应可以完全藏在第五个方向

这是本轮最值得保留的新连接。

我们构造两个有限候选域：

$$
X_4=\{m:m\mid5040\},
$$

$$
X_5=\{n:n\mid55440\}.
$$

它们分别有60和120个候选。

**这里每个 \(m\) 被当成一个候选整数，用它自己的 \(Z(m)\) 评分；不是直接把 \(1/m\) 当作出现概率。**

定义：

$$
P^{(j)}_{\beta,\lambda}(n)
=
\frac{e^{\beta J_\lambda(n)}}{\mathcal Z_j(\beta,\lambda)},
\qquad n\in X_j.
$$

其中 \(\beta>0\) 是人为引入的选择强度；\(\beta\) 越大，分布越集中到最优候选。此处不声称它已经对应物理温度。

再定义平滑最优值：

$$
\Phi_j(\beta,\lambda)
=
\frac1\beta\ln\mathcal Z_j(\beta,\lambda).
$$

