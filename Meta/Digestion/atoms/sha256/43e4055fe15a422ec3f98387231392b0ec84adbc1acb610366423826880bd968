## 定理 8.1：有限立方体的最危险状态出现在阈值链上

按比例：

$$
\eta_i=\frac{\rho_i}{\ell_i}
$$

从大到小排序。

那么 \(F\) 在全部子集中的最小值，可以在下面至多 \(k+1\) 个集合中取得：

$$
\boxed{
\varnothing,\quad
\{1\},\quad
\{1,2\},\quad\ldots,\quad
\{1,\ldots,k\}.
}
\tag{22}
$$

相等比例可以分组；至少有一个最优集合不会截断一个并列组。

### 证明

取一个全局最小集合 \(S_*\)，令：

$$
E_*=E+\sum_{i\in S_*}\ell_i,
$$

$$
\lambda_*=h'(E_*)>0.
$$

由 \(h\) 的凹性：

$$
h(x)\le h(E_*)+\lambda_*(x-E_*).
$$

因此，\(F\) 被一个仿射函数 \(L\) 从上方支持，并且：

$$
L(S_*)=F(S_*).
$$

因为 \(S_*\) 已经使 \(F\) 全局最小：

$$
L(S)\ge F(S)\ge F(S_*)=L(S_*).
$$

所以 \(S_*\) 也最小化这个线性函数。

其第 \(i\) 个选择项的系数是：

$$
\lambda_*\ell_i-\rho_i.
$$

因此最小者必须选择所有：

$$
\eta_i>\lambda_*,
$$

并拒绝所有：

$$
\eta_i<\lambda_*.
$$

严格凹性还排除了在可自由切换的单个方向上恰好等号导致的非阈值最优情况。

所以至少有一个最优集合是排序后的前缀。证毕。

