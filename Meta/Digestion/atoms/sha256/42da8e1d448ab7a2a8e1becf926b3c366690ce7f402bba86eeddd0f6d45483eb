记 $r_\pi(x)$ 为实际读取数。对正深度的正确策略，分别计
$$
R(\pi)=\sup_{x\in K}r_\pi(x),\qquad
D(\pi)=\sup_{x\in K}t_{r_\pi(x)}.
$$
上确界允许为无穷。$D$ 只限制最后一次观察的逻辑索引，不限制计算或输出完成的物理时间。时钟、状态表示与存储、整数运算、输出和静态程序或解码表各自收费，沿用ML主卷定义22.1、§28与§36的分项费用约定。深度零直接输出空字，$R=0$，不赋予最后读取索引或观察截止值。