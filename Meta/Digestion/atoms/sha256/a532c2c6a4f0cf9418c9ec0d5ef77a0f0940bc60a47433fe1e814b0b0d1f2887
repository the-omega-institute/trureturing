## 545.1 为什么不能直接跨越巨大尺度比

若：

$$
a_{\max}\gg a_{\min},
$$

直接变换的：

$$
|r|
=
\tanh(D/2)
$$

接近 \(1\)。

此时：

* 解析极点 \(-1/r\) 接近单位圆；
* Taylor 系数衰减缓慢；
* 需要大量 moments；
* 误差对有限数据极为敏感。

通过等对数尺度梯：

$$
\boxed{
a_0\to a_1\to\cdots\to a_J
}
$$

则每一步都处于小 \(r\) 区域。

所以多尺度 observer bank 的最优初始设计不是线性等距，而是：

$$
\boxed{
\text{log-uniform spacing}.
}
$$

---

