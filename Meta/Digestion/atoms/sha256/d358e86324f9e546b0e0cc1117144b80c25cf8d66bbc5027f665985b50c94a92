# 十、接下来真正需要证明什么？

上一轮已经把有限指数盒子的最危险状态缩减到按边际收益排序的一条候选链。现在多了一个新的工具：

$$
\boxed{
\text{对候选状态周围的整个盒子，构造有方向、可精化的安全包络。}
}
$$

具体可采用：

$$
Q_{2r}(S)\le\Delta(n_S),
$$

或更强的：

$$
Q_2(S)+\text{由低阶关联推出的正修正}
\le\Delta(n_S).
$$

再结合式（14）或式（15），决定是否已经需要继续展开高阶模式。

如果当前下界严格大于零，就可以一次认证整个允许区域；如果没有严格大于零，只说明证书还不够强，并不代表已经找到反例。

要走到 RH，仍需证明这些证书能够统一覆盖全部相关尺度。经典理论允许把反例搜寻集中到极大丰数，但其素数范围与指数深度仍然无界。([arXiv][1])

同时，Gronwall 定理给出：

$$
\liminf_{n\to\infty}\Delta(n)=0.
$$

所以，即使所有状态都安全，也不能期望一个固定正间隔让所有粗糙近似都自动足够。([arXiv][1])

本轮的新结果准确地改善了其中一部分：

$$
\boxed{
\text{遗漏高阶模式的误差，已经有可证明的结构与定量控制。}
}
$$

尚需新的数论内容的是：

$$
\boxed{
\text{局部收益、绝对基准与不断扩展的素数方向，}
\quad
\text{如何始终留下严格正余量。}
}
$$

