# 五、Zeckendorf 是这套几何的规范坐标图，而不是另一条动力学

对每个寄存器：

$$
a_p=\sum_jb_{p,j}G_j,
$$

其中：

$$
G_0=1,\quad G_1=2,\quad G_{j+2}=G_{j+1}+G_j,
$$

$$
b_{p,j}\in\{0,1\},
\qquad b_{p,j}b_{p,j+1}=0.
$$

于是：

$$
(4,2,1,1)
\longleftrightarrow
(101,\;01,\;1,\;1),
$$

数位按低权重到高权重排列。

但第157步的完整表还需要：

$$
v_{29}=1.
$$

所以完整数据不是四行，而是：

$$
\boxed{
\text{四条数据行}
+
\text{一条非零控制行 }29.
}
$$

仓库 `PrimeAxisEncoding.lean` 已经定义：

```lean
primeAxisEncoding : PrimeAxisTable ≃ ℕ+
```

并证明规范表加法对应整数乘法。源码中这些等价构造是 `noncomputable` 的数学对象，不能直接当成已经优化好的执行引擎。

