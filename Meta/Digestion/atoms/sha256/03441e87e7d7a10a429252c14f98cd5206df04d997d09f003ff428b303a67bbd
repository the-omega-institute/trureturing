### 97.2 反向 BFS 的正确性

把状态对写成一个成对节点

$$
(x,y)\longmapsto(\tau x,\tau y).
$$

先把当前读数不同的节点放入源集合：

$$
M_0
=\{(x,y):q(x)\ne q(y)\}.
$$

再沿成对更新边反向扩张：

$$
M_{k+1}
=
M_k
\cup
\left\{(x,y):(\tau x,\tau y)\in M_k\right\}.
$$

