# 九、Zeckendorf应当用来规范区块，而不是替代区块约束

这一例子中：

$$
4=1+3,\qquad5=5,
$$

所以 \(2\) 轴发生：

$$
101\longrightarrow0001.
$$

而：

$$
2=2,\qquad3=3,
$$

所以 \(3\) 轴发生：

$$
01\longrightarrow001.
$$

二者各自都是合法规范化。

如果最终两层都加入，则无论先加 \(2\) 还是先加 \(3\)，最终指数都是：

$$
(5,3,1,1,1,1).
$$

这给出一个局部交换方形：

$$
B\to2B\to6B,
\qquad
B\to3B\to6B.
$$

但是有三件事必须分别证明：

$$
\boxed{
\text{规范化得到同一整数};
}
$$

$$
\boxed{
\text{哪些中间状态属于真实最优路径};
}
$$

$$
\boxed{
\text{所有被保留或被合并的中间状态都安全}.
}
$$

第一件事由算术交换性与规范表示支持。

第二件事由共同价格阈值支持。

第三件事由式（22）的安全界支持。

不能用第一件事替代后两件事。

仓库的 `Newman.lean` 已经证明：终止且局部合流的重写系统具有唯一正规形。它可以用于认证区块内部的表示归一化与最终状态一致性。

但唯一正规形并不自动保证所有中间状态的 Robin 余量为正。因此，**规范化证书与安全证书必须同时保留。**

