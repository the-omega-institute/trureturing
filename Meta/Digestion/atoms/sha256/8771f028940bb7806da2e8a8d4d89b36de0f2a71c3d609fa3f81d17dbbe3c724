# 一、统一对象应当是“事件及其因果关系”，而不只是整数加一个时间标签

先固定最简单、最清楚的执行规则：

$$
\boxed{
\text{一次串行事件，只允许把当前整数乘以一个素数。}
}
$$

从 \(1\) 出发：

$$
1\longrightarrow p_1\longrightarrow p_1p_2\longrightarrow\cdots.
$$

令事件：

$$
e_{p,k}
$$

表示“采用素数 \(p\) 的第 \(k\) 层指数”。

同一素数方向必须满足：

$$
\boxed{
e_{p,1}\prec e_{p,2}\prec e_{p,3}\prec\cdots.
}
\tag{1}
$$

不同素数方向暂时没有额外依赖，允许交换次序。

这种用偏序表达因果依赖、用合法事件集合表达状态的方法，与并发计算中的事件结构相对应：一个合法配置必须包含其事件的全部前置条件。([arXiv][1])

