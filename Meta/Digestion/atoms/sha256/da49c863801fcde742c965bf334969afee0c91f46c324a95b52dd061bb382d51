## 84.2 Sticky 分支

若负模式沿一条嵌套通道链集中，则：

* \(p\)-方向由 Euler/Hecke primitive 分解；
* \(m\)-方向由单位流 Fourier 分解；
* \(t\)-方向由 Mellin/Riemann 积分；
* \(\alpha\)-方向由六状态有限 Weil 系统；

进行 Yu Deng 式历史压缩。

这里 \(\alpha\)-方向的复杂度是严格有限的：

$$
\dim=6,
$$

不会随展开阶数产生新的状态类型。

---

