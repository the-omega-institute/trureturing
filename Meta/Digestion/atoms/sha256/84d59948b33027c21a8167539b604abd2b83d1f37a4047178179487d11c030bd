# 九、熵与信息逃逸应当怎样参与这个离散证明？

现在目标明确了，观察者评价也应当围绕它来设计。

对一个抽象观察结果 \(o\)，记其纤维为：

$$
\mathcal F_o=\{x:O(x)=o\}.
$$

证明安全需要的是：

$$
\boxed{
\forall x\in\mathcal F_o,\quad\operatorname{Safe}(x).
}
$$

或者构造一个显式的下界：

$$
\inf_{x\in\mathcal F_o}\Delta(N(x))
\ge L(o)>0.
$$

这里需要控制的是**纤维内最坏状态**，不是平均状态。

因此：

* 条件熵小，不自动排除一个低概率坏状态。
