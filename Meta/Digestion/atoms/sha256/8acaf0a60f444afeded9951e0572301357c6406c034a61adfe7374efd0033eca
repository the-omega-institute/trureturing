### 证明

假设存在这样的递推。

任取 \(r\)。为每个：

$$
j=0,\ldots,r-1
$$

选择彼此互异的两个素数 \(a_j,b_j\)，并使所有模数：

$$
a_jb_j
$$

彼此互素。

由中国剩余定理，可以选择足够大的 \(N\)，满足：

$$
N+j\equiv0\pmod{a_jb_j}
$$

对所有 \(j\) 成立。

于是每个 \(N+j\) 都至少含有两个不同素因子，因此不是素数幂：

$$
\Lambda(N+j)=0,
\qquad
j=0,\ldots,r-1.
$$

由递推：

$$
\Lambda(N+r)=0.
$$

再递推可知：

$$
\Lambda(n)=0
\qquad
\forall n\ge N.
$$

但在 \(N\) 之后仍然存在素数 \(p\)，且：

$$
\Lambda(p)=\log p>0,
$$

矛盾。证毕。

---

