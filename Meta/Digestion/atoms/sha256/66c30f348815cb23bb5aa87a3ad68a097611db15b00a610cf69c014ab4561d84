# 二、零点没有被这个相位比值投影掉

从式（V1）反解：

$$
\boxed{
\frac{A'(z)}{A(z)}
=
\frac{1-S_h(z)}
{ih(1+S_h(z))}.
}
\tag{V4}
$$

所以，只要保留同一个解析函数 \(S_h\)，就可以恢复 \(A'/A\)；再加上 \(A(0)=1\)，便能在零附近恢复 \(A\)，继而由解析延拓唯一性恢复实际函数。([DLMF][3])

