* 改成任意小的 \(\kappa\ne0\)，实 \(\theta\) 方向就出现一个非零谷底。

这是一幅严格的二维局部图景。

但不能由此说产生了热力学相变。有限态的回返振幅出现零点，与热力学极限中的非解析性仍有区别。

同样，Rouché 型定理能够保证小区域里的复零点总数稳定，**不能保证零点继续停在某一条指定的实直线上**。项目的 `RoucheZeroCount` 保留的正是边界扰动与重数计数条件。

---

