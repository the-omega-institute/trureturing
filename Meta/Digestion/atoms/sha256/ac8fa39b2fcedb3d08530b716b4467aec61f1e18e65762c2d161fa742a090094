**再计算 Laplace 变换。**

令

$$
M(T)=m_\Lambda(e^T).
$$

在初始收敛区域 \(\Re s>1/2\)，直接交换绝对收敛的积分和求和，得到

$$
\boxed{
\widehat M(s)
=
-A(s+\tfrac12)
\frac{\zeta'(s+\tfrac12)}{\zeta(s+\tfrac12)}
-
\frac{3/2}{s-1/2}
+
B(s),
}
\tag{22}
$$

其中 \(B(s)\) 是只涉及 \(n=2,3\) 的起始边界整函数。

这一步使用的算术输入是

$$
-\frac{\zeta'(z)}{\zeta(z)}
=
\sum_n\Lambda(n)n^{-z}
\qquad(\Re z>1).
$$

([DLMF][1])

在 \(s=1/2\) 处，

$$
A(1)=\frac32,
$$

所以 ζ 的极点贡献与第二项相消。

因此式（22）在每个正实点附近都解析。

