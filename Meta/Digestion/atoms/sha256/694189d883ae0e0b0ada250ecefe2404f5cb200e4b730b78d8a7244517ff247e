# 三、连续检查可以进一步缩减到素数幂事件

定义重新缩放后的响应：

$$
\boxed{
H(x)=\sqrt x\,F(\ln x).
}
\tag{7}
$$

由式（1）：

$$
H(x)=B(x)+P(x),
$$

其中：

$$
B(x)
=
x(\gamma+1-\ln x)-\ln(2\pi)+xR_0(x),
$$

$$
\boxed{
P(x)
=
\sum_{m\ge2}
\frac{\Lambda(m)}m(x-m)_+.
}
\tag{8}
$$

这里：

$$
(x-m)_+=\max\{x-m,0\}.
$$

所以，每个素数幂事件贡献一个折线项。

