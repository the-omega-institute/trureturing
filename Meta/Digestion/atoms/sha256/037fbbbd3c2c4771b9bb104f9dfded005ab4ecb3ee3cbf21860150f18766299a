**离散过程可能直接跨过一个连续切面，而不落在切面上。**

因此，正确对象应当是穿越该切面的边：

$$
\boxed{
\mathcal C_{E_*}
=
\{d\to pd:
d^2<5040\le(pd)^2\}.
}
\tag{23}
$$

每条从1到5040的历史恰好穿过其中一条边。

设：

$$
F(d)=\text{从1到 }d\text{ 的合法前缀历史数},
$$

$$
B(pd)=\text{从 }pd\text{ 到5040的合法后续历史数}.
$$

则：

$$
\boxed{
840
=
\sum_{(d\to pd)\in\mathcal C_{E_*}}
F(d)B(pd).
}
\tag{24}
$$

