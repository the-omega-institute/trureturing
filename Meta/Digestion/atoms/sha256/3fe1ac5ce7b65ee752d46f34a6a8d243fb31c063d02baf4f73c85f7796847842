# 一、ζ 的确光滑，但“光滑”并不排除临界结构

首先校准一个关键判断：

$$
\boxed{
5040\text{ 揭示的是一个算术极值边界，}
\quad
\text{不是 }\zeta(s)\text{ 在 }s=5040\text{ 处不光滑。}
}
$$

ζ 的亚纯延拓在整个复平面中只有 \(s=1\) 这一个极点；除它之外，ζ 都是解析函数，包括在它的零点处。完成化函数

$$
\xi(s)
=
\frac12s(s-1)\pi^{-s/2}\Gamma(s/2)\zeta(s)
$$

则是整函数。([DLMF][1])

真正需要改变的不是“ζ是否光滑”的认识，而是：

> **我们不能仅凭函数曲面看起来光滑，就认为它的零点、对数响应、最优解以及约束稳定性也都没有尖锐结构。**

