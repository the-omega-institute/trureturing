### 证明

区块内的最大值是最优配置的值，最小值是将每个可选方向都取成相反选择所得的值；它们恰好相差 \(C\)。

任何常数对两端至少有一端误差不小于 \(C/2\)，取中点则达到这个误差。证毕。

因此：

$$
\boxed{
\text{隐藏的状态信息可以很多，}
\quad
\text{某个标量目标的近似误差却可以很小。}
}
$$

反过来，如果要证明的是一个比 \(C\) 更小的严格安全余量，这种压缩又可能完全不够。

这与仓库中的两类结果应当分别连接：

* `RefinementTargetRiskMonotonicity` 证明精化读出会缩小目标风险，但没有自动给出风险影响的数值大小。
* `TermBoundDoesNotBoundFamilySum` 明确证明：单项趋于零，不保证不断增长的项族总和也按同样尺度趋于零。当前模型中，单项为 \(O(1/T)\)，有 \(O(T/\ln T)\) 项，总量降为较慢的 \(O(1/\ln T)\)。

