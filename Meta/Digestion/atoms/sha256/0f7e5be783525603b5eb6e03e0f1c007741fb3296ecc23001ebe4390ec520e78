## 推论 6.2：若 RH 成立，全波最终避开每个有界指数窗口

对任意固定 \(p,A\)：

$$
\boxed{
\mu_r\{n:v_p(n)\le A\}
\longrightarrow0.
}
\tag{19}
$$

### 证明

定理 6.1 保证在这个集合的充分大部分，\(R(n)\) 有一个严格小于 1 的统一上界。

有限初始部分在 RH 下也全部满足 \(R(n)<1\)。

因此整个集合上存在：

$$
R(n)\le c<1.
$$

再选择一个更接近临界值的 \(m\)，满足 \(R(m)>c\)。比较分子与分母中的指数权重：

$$
\mu_r\{v_p\le A\}
\le
C\left(\frac c{R(m)}\right)^r\to0.
$$

证毕。

