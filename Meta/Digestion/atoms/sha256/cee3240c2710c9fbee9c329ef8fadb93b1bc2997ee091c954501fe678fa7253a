### 115.1 从预测闭合到控制闭合

第 111 节的预测商只要求：在指定未来操作族中，当前记忆能决定未来读数。行动闭合增加了一个不同的要求：同一个公开状态必须允许同一个策略动作。可以把两者写成两个因子化条件。

预测闭合要求某个未来读出 $$F$$ 满足

$$
q\circ F=\overline F\circ q.
$$

控制闭合要求实际行动 $$\alpha$$ 满足

$$
\alpha=\pi\circ q.
$$

前者说“先演化再读出”和“先压缩再用有效更新”一致；后者说“所需行动”本身能够由压缩后的状态决定。当前状态分类即使满足前一个条件，也可能不满足后一个条件，因为行动可能依赖没有进入 $$q$$ 的历史变量。

因此，稳定经典对象至少要通过两道检查：

$$
\boxed{
\text{预测闭合} + \text{控制闭合}.
}
$$

如果研究目标只有被动预测，控制闭合可以暂不要求；如果对象要参与反馈、干预或实验设计，控制闭合就是必要条件。

