# 结论

这次的关键推进是：

$$
\boxed{
\text{空间状态可以理解为因果事件的前缀，}
\quad
\text{时间可以理解为对该前缀或历史的某种计数。}
}
$$

但“某种计数”必须被明确指定。串行工作量、并行深度、程序微步和能量成本，不是同一个钟。

对于5040，同一个事件结构已经暴露出：

$$
\boxed{
8\text{ 个事件},\quad
4\text{ 轮因果深度},\quad
60\text{ 个前缀状态},\quad
840\text{ 条串行历史}.
}
$$

固定八步后，最优者变成180180；同时定价时间与规模后，又出现包含5040的临界三角区域，以及四状态共同最优的时空面。

