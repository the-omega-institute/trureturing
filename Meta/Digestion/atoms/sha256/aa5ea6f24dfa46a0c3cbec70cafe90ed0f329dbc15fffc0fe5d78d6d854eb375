## 3.1 临界配置形成什么图形？

令：

$$
\mathcal T(\lambda)
=
\{(p,k):\eta(p,k)=\lambda\}.
$$

记 \(N_-\) 为只采用严格有利层：

$$
\eta(p,k)>\lambda
$$

得到的整数。

那么全部最优配置恰好是：

$$
\boxed{
N_-\prod_{(p,k)\in A}p,
\qquad
A\subseteq\mathcal T(\lambda).
}
\tag{8}
$$

因为临界层采用或不采用，都不改变目标值。

由定理3.1：

$$
|\mathcal T(\lambda)|\le2.
$$

于是：

| 同时临界的方向数 | 最优配置数 | 配置间的局部图形 |
| -------: | ----: | -------- |
