# 五、信息逃逸可以在时间切面上得到一个精确等式

这一节把“过去、现在、未来一起分析”变成可计算的信息关系。

固定时间 \(t\)，记：

* \(U\)：完整过去字串；
* \(V\)：完整未来字串；
* \(B\)：当前各素数的完成计数。

在固定终点 \(a\) 下：

$$
B=\operatorname{count}(U),
$$

$$
B=a-\operatorname{count}(V).
$$

所以，过去与未来都能恢复 \(B\)。

另一方面，给定 \(B=b\) 后，任何合法过去都可以与任何合法未来拼接，且概率因式分解。因此：

$$
\boxed{
U\perp V\mid B.
}
\tag{14}
$$

**当前的完整计数 \(B\)，正是分隔过去与未来的充分边界状态。**

现在让观察者只保留：

$$
Q=q(B).
$$

