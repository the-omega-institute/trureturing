## 5.2 导数界也可以离散递推

如果不想处理积分，令：

$$
z=\frac1{\ln E}.
$$

可以写：

$$
A_m(E)=E^{-m}P_m(z),
$$

其中：

$$
P_1(z)=z,
$$

$$
\boxed{
P_{m+1}(z)=mP_m(z)+z^2P_m'(z).
}
\tag{16}
$$

前几项是：

$$
P_2(z)=z+z^2,
$$

$$
P_3(z)=2z+3z^2+2z^3,
$$

$$
P_4(z)=6z+11z^2+12z^3+6z^4.
$$

所有系数都是非负整数。

因此，连续尺度模型最后给出的误差证书，可以通过**整数多项式递推＋有理区间**计算，不需要数值积分。

