### 宇称不变态

$$
[\rho,\Pi]=0.
$$

第二种状态可以同时包含偶、奇两个 sector，只是没有二者之间的 coherence。

例如：

$$
\rho
=
\frac12
\left(
|+\rangle\langle+|
+
|-\rangle\langle-|
\right)
$$

是完全宇称不变的，但：

$$
\operatorname{Tr}(\rho\Pi)=0,
$$

不是 \(+1\)。

所以无限全局完成最自然的值不是“偶”：

$$
+1,
$$

而是中性值：

$$
\boxed{0}.
$$

对于交替序列：

$$
1,-1,1,-1,\ldots,
$$

任何平移不变线性均值 \(L\) 都满足：

$$
L(a)=L(-a)=-L(a),
$$

因此：

$$
\boxed{
L(a)=0.
}
$$

这给出一个天然的：

$$
-1,\ 0,\ +1
$$

结构：

* \(+1\)：有限偶 sector；
* \(-1\)：有限奇 sector；
* \(0\)：不选择分支的全局不变完成。

---

