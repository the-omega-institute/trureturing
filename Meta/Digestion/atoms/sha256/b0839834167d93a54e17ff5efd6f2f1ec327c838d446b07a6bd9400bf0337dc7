**定义 139.0（双向关系源与严格过去缺陷）。** 固定整数 $k\ge2$，令
$$
\mathcal Y_k=\{r\in\{0,1\}^{\mathbb Z}:r\text{ 不含 }1^k\}.
$$
这里 $k$ 计数被禁止的关系一串长度；与第66节的参数对应为 $k+1$，即本节空间是该节 $Y_{k+1}$ 的双向版本。相应绝对提升同时禁止 $0^{k+1}$ 和 $1^{k+1}$。移位取 $(\sigma r)_i=r_{i+1}$，关系位 $r_i$ 连接顶点 $i,i+1$，运输量为
$$
c_i=1-r_i\in\mathbb F_2.
$$

令 $\mu_k$ 为下述平稳 Parry 律。取唯一的 $\lambda\in(1,2)$ 满足
$$
\sum_{a=1}^k\lambda^{-a}=1,
$$
并置
$$
p=\lambda^{-1},\quad q=1-p,\quad
h_j=\sum_{a=1}^{k-j}p^a,\quad
S=\sum_{j=0}^{k-1}p^jh_j,\quad
\pi_j=\frac{p^jh_j}{S}
\qquad(0\le j<k).
$$
后缀状态 $j_t$ 是读 $r_t$ 前连续一的长度。零位使状态归零，一位使状态增加一，转移概率为
$$
P(j,0)=\frac p{h_j},\qquad
P(j,j+1)=\frac{ph_{j+1}}{h_j}\quad(j<k-1).
\tag{139.1}
$$
在 $j=k-1$ 时只能读零。取平稳状态律 $\pi$，即定义了本节的来源概率。

对 $R\ge1$，一份严格过去窗口规则是固定确定函数
$$
f:\{0,1\}^R\longrightarrow\mathbb F_2.
$$
其在顶点 $t$ 的调用、运输缺陷和最优缺陷分别为
$$
f_t(r)=f(r_{t-R},\ldots,r_{t-1}),\qquad
D_t=f_{t+1}\oplus f_t\oplus c_t,
$$
$$
\delta_k(f)=\mu_k(D_0=1),\qquad
d_k(R)=\min_f\delta_k(f).
\tag{139.2}
$$
全部二进制窗口上的函数只有有限多个，故最小值确实取得；不合法窗口上的值不影响来源平均。

以下两类上界的量词不同。最长完整零串构造给同一规则 $F_R$，使它对每个 $k\ge2$ 同时满足 $\delta_k(F_R)\le\min\{1,512/R\}$。最小词构造则对每个固定的 $k,R$ 选取一份确定词序及端点位表，在 $R\ge128$ 时取得 $d_k(R)\le3/R$。后者没有断言同一张表适合全部 $k$。两类规则都不在观察时添加随机种子，也不把当前待读位 $r_t$ 放进 $f_t$。

