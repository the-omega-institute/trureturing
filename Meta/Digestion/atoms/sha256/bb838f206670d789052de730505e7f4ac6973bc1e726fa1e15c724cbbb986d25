# 二、“时空来自因果”有坚实的数学方向，但还差一个尺度结构

你的判断与因果集理论有明确的接点。

在满足适当因果区分条件的光滑洛伦兹时空中，因果次序可以确定其共形几何；再加入体积元素，才能恢复完整的度量信息。这不是对任意有向图都成立的无条件结论。([Springer][1])

为什么因果本身还不够？

若：

$$
g'=\Omega(x)^2g,
\qquad \Omega(x)>0,
$$

则：

$$
g'(v,v)=\Omega(x)^2g(v,v).
$$

符号不变，因此光锥与时间方向的分类保持，但长度、固有时间和体积会改变。

所以：

$$
\boxed{
\text{因果次序}
\quad\text{不能单独确定所有尺度。}
}
$$

这里缺少的不是一个全局常数，而可以是一个位置相关的尺度场 \(\Omega(x)\)。

对离散模型，合理的研究起点应当是：

$$
\boxed{
\text{事件偏序}
+
\text{计数或权重}
+
\text{传播规则}
+
\text{观察映射}.
}
\tag{1}
$$

四原语可以组织它：

* **FLOW** 规定事件之间怎样传播；
