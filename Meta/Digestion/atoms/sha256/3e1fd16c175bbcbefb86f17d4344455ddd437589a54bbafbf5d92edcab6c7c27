## 456. 新素数估值不能从旧素数恢复

对有限素数集合 \(S\)，观察：

$$
O_S(n)
=
(v_p(n))_{p\in S}.
$$

取新素数：

$$
q\notin S.
$$

比较：

$$
n=1
$$

和：

$$
n=q.
$$

对所有 \(p\in S\)：

$$
v_p(1)=v_p(q)=0.
$$

但：

$$
v_q(1)=0,
\qquad
v_q(q)=1.
$$

所以不存在函数 \(f\)，使：

$$
v_q=f\circ O_S.
$$

即：

$$
\boxed{
v_q
\notin
\operatorname{Cl}
\{v_p:p\in S\}.
}
$$

每个新素数地址都切开旧观察共同核中的新状态对。

这正是仓库 residual join law 的算术实例。

---

