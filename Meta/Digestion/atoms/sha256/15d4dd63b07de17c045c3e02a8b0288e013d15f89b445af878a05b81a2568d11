## 定义 5.1：完整离散状态

一个状态为：

$$
\boxed{
x=(a,k),
}
$$

其中 \(a\) 是有限非增素数指数分拆，\(N(a)>5040\)，\(k\ge1\) 是预算检查精度。

初态集合为：

$$
\boxed{
I_0=\{(a,1):a\in\mathcal S_{5040}\}.
}
$$

它包含 19 个状态。

允许两类转移：

$$
(a,k)\longrightarrow(a',k),
$$

其中 \(a'\) 由 \(a\) 合法增加一个格子得到；以及：

$$
(a,k)\longrightarrow(a,k+1).
$$

第一类增加算术状态规模，第二类提高观察临界预算的精度。

两类转移都完全离散，每个状态只有有限个后继。

---

