# 六、于是，“遗漏关系的安全估计”可以变成最小割

令二值变量 \(x_i\) 表示是否选择方向 \(i\)。

把式（15）写成：

$$
Q_2(x)
=
F_0+\sum_i g_ix_i+\sum_{i<j}c_{ij}x_ix_j.
$$

定义：

$$
w_{ij}=-\frac12c_{ij}>0,
$$

$$
a_i=g_i-\sum_{j\ne i}w_{ij}.
$$

利用：

$$
x_ix_j=\frac{x_i+x_j-|x_i-x_j|}{2},
$$

得到：

$$
\boxed{
Q_2(x)
=
F_0+\sum_i a_ix_i
+\sum_{i<j}w_{ij}|x_i-x_j|.
}
\tag{18}
$$

这个形式可以精确转成源—汇最小割：

* 每个素数方向对应一个顶点；
* 不同方向之间的边容量为 \(w_{ij}\)；
* 单点项 \(a_i\) 用源、汇连接及一个常数偏移表示。

这是二值次模能量的经典图割构造，不是新的图论定理；此处的工作是证明当前算术下界恰好具有可用形式。([ISTA Publication Database][3])

由式（16）：

$$
\boxed{
\min_xQ_2(x)>0
\Longrightarrow
F(x)>0\quad\forall x.
}
\tag{19}
$$

因此，图的最小割可以给出一个覆盖所有二值组合的安全证书。

