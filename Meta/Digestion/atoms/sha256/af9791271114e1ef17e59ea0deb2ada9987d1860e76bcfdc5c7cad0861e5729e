## 7.1 完整编码不会丢失任何状态

将每个指数写成：

$$
a_i=\sum_jb_{i,j}G_j,
\qquad
b_{i,j}b_{i,j+1}=0.
$$

这给出分拆状态的规范数据表示。

仓库中的 `PrimeAxisEncoding` 已有规范素数表与正整数的双射，以及规范表加法对应整数乘法的结构。

因此，合法加格子的转移可以实现为：

$$
\text{某个指数加一}
\longrightarrow
\text{Zeckendorf 规范化}.
$$

这解决数据正确性与操作正确性。

但**只有把大量不同状态归约到一个可证明安全的抽象状态，才会减少全局证明负担**。

