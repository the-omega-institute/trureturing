# 384. 外部行为不能决定内部状态的冗余复制

给定一个系统状态 (x)，可以把它替换成多个隐藏副本：

[
(x,h),
\qquad
h\in H,
]

并定义输出忽略 (h)：

[
O'(x,h)=O(x).
]

过程可以任意更新隐藏标签，只要公开状态分量仍模拟原系统。

于是：

[
\beta'(x,h)=\beta(x)
]

对全部 (h) 成立。

所以外部行为无法决定：

* 内部状态有多少副本；
