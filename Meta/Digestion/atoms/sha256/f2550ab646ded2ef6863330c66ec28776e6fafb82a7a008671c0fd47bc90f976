**有限归约定理。** 对每个 $0<\rho<1$，以上构造的 $B$ 满足
$$
\boxed{
M(\rho)=
\min_{\substack{3\le r\le B,\ r\text{ 奇}\\
1\le j\le(r-1)/2,\ \gcd(j,r)=1}}
\ \max_{1\le m\le(r-1)/2}
2(1+\rho^{2m-1})
\left|\sin\frac{\pi jm}{r}\right|.
}
\tag{50.41}
$$
证明中所有被排除的比值距离严格大于 $U$ 或至少为二；而第50.13节的候选距离不超过 $U$，因此它所在的精确阶必在保留范围。有限集非空，其最小值是全局下确界并被实际单位根取得。任何全局最小者均属于此有限集合或其中元素的共轭。故最小者集合也是有限的。

对于代数 $\rho$，有限候选值均为实代数数，给定参数的有效代数表示后可以精确比较。对于任意抽象实数 $\rho$，这是有限数学表达，不等于免费获得该实数或决定每个可能相等的比较。若 $\rho$ 可计算，可用有限候选近似最小值；没有声称所有参数处的唯一胜出者、精确相等关系或取得成本都自动可判。

下表列出（50.32）和（50.40）的三个精确有理证书。它们不是最优搜索界。

| rho | 可用奇阶 q | delta | 可用 B |
|---:|---:|---:|---:|
