**命题 42.2（全词轨迹的整数等式；repo-derived）。** 定义42.1的每个整数除法均有正除数，并且
$$
0<U_j<Bm,\quad V_0(j)-W_0(j)=P(Bm-U_j)>0,\quad
V_1(j)-W_1(j)=(A-P)(Bm-U_j)>0,
$$
故 $0<W_b(j)<V_b(j)<ABm$。若 $q_j=r+(1-2r)j/m=U_j/(Bm)$，则
$$
\frac{W_b(j)}{V_b(j)}=B_b(q_j),\qquad
\frac{V_0(j)}{ABm}=p+(1-2p)q_j.
$$
因此 $T_b(j)$ 正是 $F_b(q_j)$ 在该网格上的最近点编号（同距取小），对每个有限词 $w$ 有
$$s(\varnothing)=j_0,\quad s(wb)=T_b(s(w)),\quad
\widehat h_{s(w)}=h(q_{s(w)}).
$$

**证明。** $R\le U_j\le(B-R)m<Bm$，代入两行定义立即给出符号和上界；两条差式还给出 $W_b<V_b$。将 $q_j=U_j/(Bm)$ 代入 $B_0,B_1$，分别约去同一正因子 $Bm$，得到两行的 $W_b/V_b$。又 $F_b(q_j)=r+(H/B)(W_b/V_b)$，网格坐标是 $mB_b(q_j)$，故带余除法及严格不等式恰是最近整数规则。$j_0$ 给出 $q_{j_0}=1/2$，逐字归纳得到轨迹等式和读出等式。以 $p=r=1/4,\varepsilon=1/16$ 得 $m=28$，在 $j=6,b=0$ 时 $(V_0,W_0)=(192,120)$，余数为 $96$，所以等距仍取 $17$；以 $p=1/3,r=1/5,\varepsilon=1/64$ 得 $m=54$，有 $T_0(36)=40$、$T_1(18)=13$。证毕。

**命题 42.3（完全计费的顺序位模型；repo-derived）。** 令 $s$ 为六个输入整数对的定长二进制编码总位长，并令
$$b=s+\lceil\log_2(N+1)\rceil.$$
在固定有限控制、多带顺序位读写机中，把输入参数、当前输出、商余、循环计数、当前编号、带头位置、全部暂存和复制移动都计费。则规范初始化可用 $O(s^2)$ 位操作和 $O(s)$ 峰值空间；每个报告及其新输出可用 $O(b^2)$ 位操作，所有持久和临时工作合计峰值 $O(b)$ 位。程序本身是固定的，参数表不藏在控制中。

**证明。** 由 $m\le16BCA+2$，$m$ 以及 $u,v$ 的编码长度都是 $O(s)$。命题42.2给出 $U_j<Bm$、$V_b,W_b<ABm$，并且
$$mW_b<ABm^2,\qquad e_b<V_b,\qquad2e_b<2ABm,$$
故常数个参数、编号、商余、当前读出和工作缓冲均为 $O(b)$ 位。初始化只需固定深度的乘、除、比较和向上取整；学校乘除法按 $O(b^2)$ 位操作计费。之后每步重算 $U_j,V_b,W_b$，一次带余除法产生新编号，再重算 $V_0/(ABm)$；顺序带不保存历史，故仍只有常数个 $O(b)$ 缓冲区。把每个参数读写、带头移动、计数和当前完整输出列入同一收费口径，得到所述峰值和单步上界。这里 $s$、$b$ 和二进制固定缓冲是模型假设；RRO 的模量或维数结论只启发这种成本分离，不能推出 Bayes 更新的复杂度。证毕。

**命题 42.4（表表示与按需表示的边界；repo-derived）。** 若保存每个报告边界的完整读出和区间证书，显式表占 $O(Nb)$ 位；只保存两条转移数组而按编号重算读出时，数组占 $O(N\log N)$ 位。按需执行器在报告边界只保存一个 $\lceil\log_2N\rceil$ 位编号，外加命题42.3的工作和当前输出。$N$ 是名义报告边界的载体数；除非另行定义，微步控制配置和临时整数不计作 $N$ 个状态。两种表量是表示相关的上界，不给出硬件无关的时间最优或总成本最优结论。

例如 $p=1/4,\varepsilon=1/8,r=2^{-h}$（$h\ge2$）时 $m=16(2^h-2)$。显式两条转移数组为 $\Theta(h2^h)$ 位，按需执行器的峰值为 $O(h)$ 位、每报告为 $O(h^2)$ 位操作；这只比较所声明的存储和顺序位模型，不能外推到其它共享存储或输出收费规则。轨迹数值保持了命题41.4的预测值，但实现成本不是原 Bayesian 动力的成本结论。

**命题 42.5（精确执行恒等式与全区间商的反例；repo-derived）。** 令 $\rho$ 为 $[r,1-r]$ 上该网格的最近编号映射（同距取小），$\iota(j)=q_j$。正确的恒等式是
$$T_b=\rho\circ F_b\circ\iota.$$
一般不能把它改写为全区间上的 $\rho\circ F_b=T_b\circ\rho$。具体取 $p=r=1/4,\varepsilon=1/8,m=32$，令
$$x=13/50,\qquad y=27/100.$$
两点均有 $\rho(x)=\rho(y)=1$，但
$$F_0(x)=77/152,\qquad F_0(y)=79/154,\qquad
\rho(F_0(x))=16,\quad\rho(F_0(y))=17.$$
这是对全后验区间的量词反例；两点都在该参数的固定先验可达不变量 $[3/10,7/10]$ 之外，因而不声称存在可达历史见证。

**证明。** 直接用 $q_j=1/4+j/64$ 比较绝对距离得到两个输入的编号和两个输出的编号。若全区间商等式成立，同一 $\rho$ 纤维中的 $x,y$ 必须给出同一 $T_0$ 值，矛盾。RRO §2.2 的精确因子化要求操作在实际纤维上常值，§6.6 又把下降的取值域限定为实际观察像；本反例只说明把网格近似扩成整个后验区间会越过该条件。证毕。

**定义 42.6（两个观察对象的边界）。** 命题42.2描述的是有限网格机 $(S,j_0,T_0,T_1,\widehat h)$ 的精确执行，并由命题41.4把其读出误差传给原任务。它没有把 $q\mapsto q_j$ 声称为原 Bayesian 动力的精确观察商：后者还要求在实际纤维上所有后继和所需输出满足 RRO §2.2、§6.6 的常值与下降条件。命题42.5的全区间反例只否定未经限定的商交换式；若只量化真实可达历史，必须另行证明这些历史的纤维条件，不能由该反例或网格位数自动推出。

## 追加锚（本行以下为增补区）
