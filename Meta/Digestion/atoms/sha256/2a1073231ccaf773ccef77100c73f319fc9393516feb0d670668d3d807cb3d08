**命题 52.9a（两状态仪器的线性过滤与非仿射条件化）。** 另取与书签模型不同的两状态空间 $\{1,2\}$，令输出仪器及初态为
$$
K_0=\operatorname{diag}(1,1/2),\qquad
K_1=\operatorname{diag}(0,1/2),\qquad
K_0+K_1=I,\qquad p=(t,1-t),\quad0\le t\le1.
$$
每次按此联合仪器报告并更新。报告零后，第一状态的后验质量为 $2t/(1+t)$，不是初态参数 $t$ 的仿射函数。对每个整数 $m\ge0$，连续报告 $m$ 个零的未归一化后继、词概率与后验分别为
$$
\begin{gathered}
pK_0^m=(t,2^{-m}(1-t)),\qquad
\ell_m=t+2^{-m}(1-t),\\
p_m=\left(\frac{t}{t+2^{-m}(1-t)},
\frac{2^{-m}(1-t)}{t+2^{-m}(1-t)}\right).
\end{gathered}
$$
每个有限 $m$ 的 $\ell_m$ 严格为正。$t>0$ 时 $p_m\to(1,0)$；$t=0$ 时所有 $p_m=(0,1)$ 而 $\ell_m\to0$。无限全零事件的概率为 $t$。

证明。矩阵非负且输出和为 $I$，所以它们是定理15.3范围内的合法联合仪器；其对角性表明隐藏状态保持。一次零分支的未归一化质量为 $(t,(1-t)/2)$，总质量 $(1+t)/2$，归一化得所述 $2t/(1+t)$。该函数在 $t=0,1$ 分别为零、一；若为仿射，必为 $t$，但在 $t=1/2$ 的值为 $2/3\ne1/2$，矛盾。

空词给 $K_0^0=I$；每多一个零便把第二个对角系数乘 $1/2$、第一个保持一，归纳得 $K_0^m=\operatorname{diag}(1,2^{-m})$，所以三条有限公式成立。若 $t>0$，分母趋于 $t$ 且第二分子趋零，故后验趋于 $(1,0)$；若 $t=0$，有限分母为 $2^{-m}>0$，后验始终为 $(0,1)$，而分母趋零。令 $E_m$ 为前 $m$ 个报告全为零的柱事件，则 $E_{m+1}\subseteq E_m$，且 $\Pr(E_m)=\ell_m$。概率的从上连续性给
$$
\Pr\!\left(\bigcap_{m\ge0}E_m\right)
=\lim_{m\to\infty}\ell_m=t.
$$
当 $t>0$ 时该无限事件有正概率，第二状态与它的联合概率为 $\lim_m2^{-m}(1-t)=0$，所以在该事件上条件化亦给第一点质量。$t=0$ 时无限事件概率零，有限后验的极限不定义在该零概率事件上的后验。最后，忽略报告后的状态更新为 $p(K_0+K_1)=p$；重复后仍为 $p$。因而选择零报告所得到的后验集中不等于无条件准备第一状态。$\square$

本例的未归一化线性更新、实际正锥及正质量归一化属于定理15.3的合同；它与式（52.19）的书签仪器不同。存在有限线性过滤表示不使归一化条件化成为仿射，也不提供免费取得坐标、重复制备或最优控制的执行权限。

