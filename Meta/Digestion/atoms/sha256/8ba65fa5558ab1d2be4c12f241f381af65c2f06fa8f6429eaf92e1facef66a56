### 44.19 假设：实际像上的专门化（FT-A1）

将 FT-D1 专门化到一个具体记录生成映射时，记录域须取该映射的实际像 $X$，右测试域须取右上下文映射的实际像 $R$，配对须服从给定的合法联合域；相关联合域或额外约束 $K$ 不能由边际像的形式乘积代替。全 $P$ 上的尖锐下界用于受限 $X$ 时，区分记录须有 $X$ 内的实际见证，所用测试与配对也须属于实际测试像及合法联合域。全 $P$ 的下界因此不自动成为这些受限域的下界，也不自动成为完整时空操作语言的结论。

使用非共线、水平包含或极点成员关系的有效性结论时，须给出相应实际像的有效证书或成员判定。FT-D1 至 FT-P12 的抽象结论仅以各条已声明的集合、消费者及守卫为假设；某个具体生成映射实现全 $P$ 或所给 $R$ 不是这些抽象结论的附加前提。等价核、逐记录结果像及更丰富操作语言的同余仍是不同的数学条件，如 FT-CE3 所示。
