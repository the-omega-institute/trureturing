### 情形二：\(r>0\)

程序先减去一个循环单位与一个余数单位，进入 \(q=19\)，再用式（4）把 \(a=M\) 搬回 \(c\)。

最后调整并搬回剩余的 \(b\)，得到：

$$
\boxed{
k\nmid M
\Longrightarrow
\mathsf X(M,k)
\longrightarrow
\mathsf X(M,k-1).
}
\tag{9}
$$

候选数 \(M\) 保持不变，试除数减少一。

所以完整试除过程等价于：

```text
从当前 k 开始：
    若 M mod k = 0：
        返回 M，以及 k - 1
    否则：
        k := k - 1
```

因为 \(1\mid M\)，这个阶段必定在有限步骤后结束。

---

