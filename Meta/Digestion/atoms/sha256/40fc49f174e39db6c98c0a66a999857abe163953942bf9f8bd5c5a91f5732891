**推论 52.2a（固定路径的有限深度分割）。** 观察到时刻 $0,1,2,3$ 为止的预测分割类数依次为
$$
2,\qquad4,\qquad6,\qquad6,
$$
此后均为六。

证明。表中第一位取零、一两值；前两位恰有 $00,10,01,11$ 四个实际值；前三位有表中的六个不同值。关系 $q_{j+2}=q_j$ 对 $j\ge1$ 成立，使第四位及全部后续位由前三位决定，故不再细化。不能将该关系延伸到 $j=0$：类 $B$ 的 $q_0=1,q_2=0$，类 $E$ 的 $q_0=0,q_2=1$。$\square$

此任务只含固定 $F$。若另外允许强制动作
$$
T_L(q,a,b)=(a,a,b),\qquad T_R(q,a,b)=(b,a,b),
\tag{52.4}
$$
则初始读数、强制左动作后的读数、强制右动作后的读数联合给出 $q,a,b$。具体地，先合法读取原 $q$，再执行 $T_L$ 并读取 $a$，继而执行 $T_R$ 并读取 $b$；两动作都保持 $a,b$，故三个记录来自同一实际状态的同一条合法续接，并不需要独立复制两个反事实分支。若两状态在这个任务中行为相同，该实际记录词强迫三个坐标均相同，故八态全部分开；相同状态显然有相同行为。这是扩充后的动作任务，不能用其八类结论改变固定任务的六类商。各次读取与调用仍须有相应实际权限及费用合同。

