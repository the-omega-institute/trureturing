**候签定理 18.12(奇素数阶 GK_{p+1} 完美一因子分解)**〔open;落 `D5/S3/ConceptDynamics/GraphColoring/OddPrimePerfectOneFactorization`;文献归属 Kotzig 1964,Scribe 取 `FromLiterature` 并附 `Library/Arith/kotzig1964-perfect-one-factorization.md`〕。设 p 为素数且 p ≠ 2(`Fact p.Prime`),顶点类型 `Option (ZMod p)`(`none` 记 ∞)。对 a : ZMod p 定义配偶 `partner a : none ↦ some a, some a ↦ none, some x (x ≠ a) ↦ some (2a − x)`,并令 `factor a := SimpleGraph.fromRel (fun u v => v = partner a u)`,`pairGraph a b := factor a ⊔ factor b`。则:(i) 对每个 a,`factor a` 的顶图是 `SimpleGraph.Subgraph.IsPerfectMatching`;(ii) 任意两个不同顶点 u ≠ v 恰在一个 `factor a` 中相邻(∞—some x 归 x;some x—some y 归中点 (x+y)/2);(iii) 对 a ≠ b,`pairGraph a b` 是 `SimpleGraph.IsHamiltonian`。伴随命题(具名,bind-only 或数值):`pairGraph a b` 中每个顶点邻集基数恰为 2;`translationStep a b := 2(b − a)` 的加法阶为 p。

