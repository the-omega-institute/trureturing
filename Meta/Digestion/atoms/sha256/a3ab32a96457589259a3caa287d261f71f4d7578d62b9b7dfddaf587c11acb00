**证明。** 令 $t_0=0$ 并固定同一个初值 $b$。考虑从位置零开始的两个长度 $R+1$ 的关系词
$$
u=0^{R+1},\qquad v=1\,0^R.
\tag{142.6}
$$
二者对全部 $k\ge2$ 都合法，在两侧添零后均为双侧合法配置。在同一时刻 $t=R+1$，最近 $R$ 位都是 $0^R$，而两个运输状态分别为
$$
b\oplus((R+1)\bmod2),
\qquad
b\oplus(R\bmod2),
$$
所以相差一位。同一窗口不能恢复两个不同的状态；这也适用于允许恢复函数依赖该固定时刻的情形。

定理141.9只对（142.2）的输出类取最小值，而（142.3）的输出不属于这一固定窗口类。其固定更新映射
$$
U(a,r)=a\oplus(1-r)
$$
将从 $t_0$ 到当前的全部翻转奇偶压缩在当前位中；历史跨度可以任意增长，状态集仍只有两个元素。因此，初始化后不再保留 $b$ 的单独副本，只要保留 $a_t$，更新合同依然闭合。

若每个时刻都要求由同一个最近 $R$ 位函数重建输出，且输出不依赖其他继承信息，则正好回到（142.2），不论实现是否使用缓存，均受（142.5）约束。若在某时刻 $T>t_0$ 丢失当前状态并重新指定 $b'$，则可从 $T$ 起再次按（142.3）得到零缺陷，但跨重置边界的缺陷为
$$
D_{T-1}=b'\oplus a_{T-1}\oplus c_{T-1}.
\tag{142.7}
$$
它为零的充要条件是新种子等于原更新所要求的当前位。重新初始化本身既不保证这一条件，也不恢复旧的真实参考。证毕。

