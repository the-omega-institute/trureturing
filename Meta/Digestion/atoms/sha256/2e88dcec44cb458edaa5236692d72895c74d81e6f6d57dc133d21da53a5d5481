## 定理2.1：最优损失的精确几何公式

$$
\boxed{
\max_mJ_\lambda(m)-J_\lambda(n)
=
d_\lambda(n,n_\lambda).
}
\tag{4}
$$

### 证明

将目标展开成逐层增量：

$$
J_\lambda(n)
=
\sum_{p,k}x_{p,k}(n)\,g_\lambda(p,k).
$$

最优配置采用全部正项，拒绝全部负项。

相对于它：

* 漏掉一个正收益层，损失 \(g_\lambda(p,k)\)；
* 多采用一个负收益层，损失 \(-g_\lambda(p,k)\)；
* 改变一个零收益层，不产生损失。

三者恰好统一为式（3）。证毕。

因此：

$$
\boxed{
\text{离最优值有多远}
=
\text{在多少层上作出了非最优选择，按损失权重相加。}
}
$$

这比普通的汉明距离更适合当前问题。两个整数即使相差很多素数方向，只要这些方向都非常接近临界，其目标距离仍然可以很小。

