**注 3.4.12(可证伪预测与逃逸见证,写在跑之前)**〔第 5⁗ 条〕。逃逸内容为纤维映射 `A |-> A intersect D` 到 `D.powerset` 的单射：由 `A = (A\D) union (A intersect D)` 重构原集合，故每纤维至多 `2^r`；活路径上的两条计数命题为 `b <= x` 与 `N-x <= 2^r*(M-b)`。多项式不等式只作其后归一化。定理 3.4.10 判形 content，准入依据 escape-witness；定理 3.4.11 为具名伴随(bind-only),方向边 `Frankl coordinate-deletion induction interface → union_closed_after_deletion` 与 `… → quantitative_and_half_frequency_transport`(消费者 → 前置)。只依赖钉版 Mathlib；不得使用 `native_decide`、`Lean.ofReduceBool`、`sorry` 或新公理。 结算:两条由一个实施席同 PR `deposit`(绑定理 3.4.10 `quantitative_and_half_frequency_transport`)+ `cover`(3.4.11)落地,三席评审后合入;落点 `D5/S1/Phase/CoordinateDeletionFrequencyTransport`(探针实测 Phase 桶 13/24 直接文件)。
