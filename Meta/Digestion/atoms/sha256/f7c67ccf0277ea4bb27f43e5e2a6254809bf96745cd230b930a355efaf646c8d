# 五、二阶联合项是负的，因此这里出现了次模结构

对任意：

$$
S\subseteq T,\qquad i\notin T,
$$

由 \(h'\) 递减：

$$
F(S\cup\{i\})-F(S)
\ge
F(T\cup\{i\})-F(T).
$$

所以：

$$
\boxed{
F\text{ 是次模集合函数。}
}
\tag{14}
$$

次模性可以理解为：增加一个新方向对安全余量的边际变化，会随着已有方向增加而降低。

但 \(F\) 不一定单调增加。次模性与单调性不同。

一般有限次模函数具有成熟的最小化理论；它并不要求函数是单调的。相关组合多项式算法已有经典结果。([arXiv][2])

