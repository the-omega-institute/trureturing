## 4.3 推广到全部整数

在绝对收敛域内：

$$
\boxed{
\mathcal Q(z,s)
=
\sum_{n\ge1}z^{\Omega(n)}n^{-s}
=
\prod_p(1-zp^{-s})^{-1}.
}
\tag{14}
$$

例如实参数满足：

$$
s>1,\qquad0<z<2^s
$$

时，上式收敛。取 \(z=1\) 就恢复 ζ 的 Euler 乘积。这依赖唯一分解与绝对收敛求和，而不是额外的物理假设。([DLMF][3])

于是：

$$
\boxed{
\zeta(s)=\mathcal Q(1,s)
}
$$

可以被理解为：**对这个特定执行模型，把全部步骤数求和后的一个切面。**

但若改成按所有有序历史计数，就会额外出现历史重数，必须另外处理，不能仍然声称得到同一个 ζ 函数。

