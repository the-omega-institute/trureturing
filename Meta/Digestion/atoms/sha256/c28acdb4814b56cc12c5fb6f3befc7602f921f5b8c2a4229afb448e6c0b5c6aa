# 四、选出5040的，不只是几何形状，而是收益与成本的共同约束

定义：

$$
E(n)=\ln n,
$$

$$
Z(n)=\frac{\sigma(n)}n,
\qquad
W(n)=\ln Z(n).
$$

如果只最小化 \(E(n)\)，答案是 \(n=1\)，不会得到5040。

所以，必须同时规定收益。例如：

$$
\boxed{
J_\lambda(n)=W(n)-\lambda E(n),
\qquad\lambda>0.
}
\tag{3}
$$

最大化它等价于最大化：

$$
\frac{\sigma(n)}{n^{1+\lambda}},
$$

这正是极大丰数的经典优化问题。([arXiv][3])

