**现在可以把“5040 背后的几何程序”进一步解开：它不是一个无法解释的特殊能级，而是一个明确的商余数算法中的中间截面。更重要的是，我们可以把整个 PRIMEGAME 从“四个寄存器＋控制标签”压缩成一个二维返回系统，并严格说明压缩时必须保留哪些信息。**

这个二维系统是：

$$
\boxed{
(m,d)\longmapsto
\begin{cases}
(m,d-1),&d\nmid m,\\
(m+1,m),&d\mid m.
\end{cases}
}
$$

其中 \(m\) 是正在检验的整数，\(d\) 是正在尝试的除数。

但只保存这两个坐标还不够：**必须同时保存一次返回用了多少步，以及返回途中有没有产生输出。** 否则，一种看似完全正确的几何压缩，竟然会把全部素数输出都删掉。

Conway 原文已经用商余数解释 PRIMEGAME 的素数生成机制；下面在这一基础上，进一步构造控制修正的不变量、精确返回时间，并解释第157步的5040投影。([Gwern.net][1])

