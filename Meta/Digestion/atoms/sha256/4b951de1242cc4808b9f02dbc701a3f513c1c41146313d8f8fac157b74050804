### 51.7 第一种失败：有限来源闭包上，有限距离不能恢复全距离

在同一个 $U^*$ 上定义
$$
e_N^0(U^*)=\min_{V\in I_0}\max_{n+k<N}\rho^{n+k}|V_{n,k}-U^*_{n,k}|.
\tag{51.28}
$$
这里最小值由实际见证 $E(b^{(N)})\in I_0$ 达到并等于零：
$$
e_N^0(U^*)=0\qquad\forall N.
\tag{51.29}
$$
但（51.24）给出
$$
\boxed{\inf_{V\in I_0}\|V-U^*\|_\rho\ge c_A>0
=\sup_Ne_N^0(U^*).}
\tag{51.30}
$$
因此这里失败的是有限值与完整下确界的交换以及共同完整见证；这条论证没有声称第一目标的正下确界等于 $c_A$，也没有声称它不达到。相同截断作为完整来源的有限优化器满足窗口误差为零而完整误差至少 $c_A$，证明了 $X_{\rm fin}$ 上有限损失逼近完整损失不能关于来源一致收敛，所以 Context45.5 的来源统一损失逼近结论不能无条件迁移到这里。$X_{\rm fin}$ 不具备乘积紧性：它包含全部截断 $b^{(N)}$，却不包含其乘积极限 $a^*$，故在 Hausdorff 空间 $X_A$ 中不闭。在完整紧来源 $X_A$ 上，（51.27）说明 $E$ 到输出范数的连续性以及完整损失 $a\mapsto\|E(a)-U^*\|_\rho$ 的连续性在 $a^*$ 处失败。由于该极限位于 $X_{\rm fin}$ 之外，不能用这个截断序列断言 $E$ 限制在 $X_{\rm fin}$ 上不连续。

