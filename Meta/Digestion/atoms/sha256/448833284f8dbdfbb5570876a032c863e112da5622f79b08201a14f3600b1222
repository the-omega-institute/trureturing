## 定理 4.1：局部 Hankel 行列式就是负边界尾项

$$
\boxed{
\det H_{p,a}=-p^{-(a+1)}.
}
\tag{7}
$$

### 证明

令：

$$
z=z_p(a),\qquad u=q^{a+1}.
$$

则：

$$
z_p(a+1)=z+u,
$$

$$
z_p(a+2)=z+u+qu.
$$

因此：

$$
\begin{aligned}
\det H_{p,a}
&=z(z+u+qu)-(z+u)^2\\
&=zu(q-1)-u^2.
\end{aligned}
$$

又因为：

$$
(1-q)z=1-u,
$$

所以：

$$
\det H_{p,a}
=-u(1-u)-u^2=-u.
$$

证毕。

于是，无论指数 \(a\) 多大，只要它有限：

$$
\det H_{p,a}\ne0.
$$

这说明单个方向始终存在两个线性独立的未来响应，而不是一个。

对 5040，四个局部块的行列式为：

| 素数 \(p\) | 初始指数 \(a\) | \(\det H_{p,a}\) |
| -------: | ---------: | ---------------: |
