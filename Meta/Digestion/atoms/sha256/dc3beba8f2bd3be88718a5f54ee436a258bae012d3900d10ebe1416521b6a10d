**命题 50.6（紧凑控制器与分立证书的总成本上界；repo-derived）。** 在报告边界计定义50.4的名义状态，其上界为
$$
\frac32M(M+1)N_AN_B+N_A+N_B.
$$
训练的动态字段用
$$
O\bigl(\log(M+1)+\log N_A+\log N_B\bigr)
$$
位，另含常数模式与待配身份；选择后只有一个模型标签和一个所选状态编号。这个名义上界不是可达状态计数、最小状态定理或内部指令微配置计数。

采用ML命题41.6的显式二进制记录、学校整数乘除与收费随机访问模型。令 $b_p$ 为包含所声明参数和编号编码的生成记录位宽，则分立表及证书占
$$
O(N_Ab_A+N_Bb_B)
$$
位；构造和完整验证分别需要
$$
O(N_Ab_A^2+N_Bb_B^2)
$$
位工作，另加 $\delta$ 初始化。两份完全通过的证书共使用 $11(N_A+N_B)+4$ 次标量不等式比较，结构、参数、正分母与编号检查额外计费。输入参数和 $O(\log(M+1))$ 位控制器常数的存储也计费，不归入动态标签。

为明确 $\delta=a/b$ 的输入长度，令 $\ell(x)=1+\lfloor\log_2x\rfloor$，将每个正整数先用 $\ell(x)$ 个一和一个零声明长度，再写其 $\ell(x)$ 个二进制数位。两数拼接的总长度
$$
D=2\ell(a)+2\ell(b)+2
$$
包含长度及分隔符，而不是某个文本序列化的字节数。重复倍增比较求 $L$ 有 $L\le D$，需要 $O((L+1)D)$ 位工作和 $O(D)$ 临时空间；$M$ 再由固定整数乘除算得。在收费随机访问下，每份训练报告收费
$$
O(b_A+b_B+\log(M+1)),
$$
含计数、模式处理及最终阈值比较，全部训练收费 $O(2M(b_A+b_B+\log(M+1)))$。每份后续报告收费 $O(b_{\rm selected})$。只有顺序存储时，训练读取改付 $O(N_Ab_A+N_Bb_B+\log(M+1))$ 的完整表扫描费用，选择后为 $O(N_{\rm selected}b_{\rm selected})$；顺序验证的目标记录扫描费用按ML命题41.6另加。

证明。每个 $k$ 有 $k+1$ 种匹配数、三种待配字段及 $N_AN_B$ 种标签对，故训练名义数为
$$
3N_AN_B\sum_{k=0}^{M-1}(k+1)=\frac32M(M+1)N_AN_B.
$$
最终第二份报告直接进入所选标签的不交并，故没有报告边界上的 $k=M$ 训练状态，后段贡献 $N_A+N_B$。这不把模型身份或时钟当作免费外部输入。另一种保存冗余报告时钟和占位字段的编码可给较松上界
$$
2(2M+1)(M+1)N_AN_B+N_A+N_B;
$$
它与这里的编码不同，不能当作同一个状态计数。

各动态整数的取值范围给出所列位数。所选标签先保留，训练元组在首次选择后读出前即消失；后段不会积累新的历史字段，所以这不是仅凭行为商对仍可读历史作忽略。表没有展开为 $N_AN_B$ 行，故逐表应用ML命题41.6，空间、构造和验证相加；比较数为 $(11N_A+2)+(11N_B+2)$。每报告只需各活动表的常数次记录访问，计数与乘以固定阈值系数需要 $O(\log(M+1))$ 位工作，得到逐步和训练总价。

由于 $a\ge1$、$a<b<2^{\ell(b)}$，至多 $\ell(b)$ 次倍增即停止，倍增中的整数至多为 $2b$，长度 $O(D)$。每次倍增和比较用 $O(D)$ 工作，给出初始化界，计算 $M$ 的常整数乘除也被此界吸收。在线临时空间可取 $O(b_A+b_B+\log(M+1))$，输出空间随实际读出另计；初始化临时空间、输入及参数、输出、静态表和持久动态标签是不同项目。删除未选动态标签不要求删除静态表。这些是指定访问模型的上界，不是运行时间测量或最优性证明。证毕。

