# 八、尾部真值在朴素乘积拓扑中必然处处不连续

设：

$$
X=\prod_{n\ge1}X_n
$$

带乘积拓扑。

定义有限编辑等价：

$$
x\sim_{\mathrm{fin}}y
\iff
x,y\text{ 只在有限坐标上不同}.
$$

设：

$$
F:X\to\{0,1\}
$$

满足有限编辑不变性：

$$
x\sim_{\mathrm{fin}}y
\Longrightarrow
F(x)=F(y).
$$

