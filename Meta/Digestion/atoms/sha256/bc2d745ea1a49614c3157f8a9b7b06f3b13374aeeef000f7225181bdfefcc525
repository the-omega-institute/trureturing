## 定义 185.1　沿参照轨迹的变换

选择一条经典标定轨迹 \(q(t)\)，满足

$$
\ddot q(t)=-\Phi'(q(t)).
$$

定义

$$
\dot s(t)
=
\frac12\dot q(t)^2-\Phi(q(t)).
$$

令

$$
\boxed{
\psi(x,t)
=
\exp\left\{
\frac{iM}{\hbar}
\left[
\dot q(t)(x-q(t))+s(t)
\right]
\right\}
\chi(x-q(t),t).
}
\tag{185.2}
$$

对每个固定 \(t\)，它是平移与质量条件相位的乘积，因此为酉映射。

这不是把内部质量先测成某个数。可以在 \(M\) 的各谱分支上同时实施同一个算子公式。

---

