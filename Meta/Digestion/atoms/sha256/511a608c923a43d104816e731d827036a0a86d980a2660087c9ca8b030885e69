**定义 139.9（公平绝对提升）。** 在关系路径之外独立取公平位 $A_0$，并沿全部整数顶点规定
$$
A_{t+1}=A_t\oplus c_t.
\tag{139.25}
$$
对每个 $t$，条件于完整关系路径后 $A_t$ 仍公平。令 $Z_t=(j_t,A_t)$。其平稳律为 $\widetilde\pi=\pi\otimes\operatorname{Unif}(\mathbb F_2)$。给定当前绝对位与关系过去，更早的绝对位由（139.25）决定；当前绝对位与完整关系路径独立。因此提升过去不添加关系未来信息，$Z_t$ 是按（139.1）、（139.25）转移的平稳 Markov 链。

