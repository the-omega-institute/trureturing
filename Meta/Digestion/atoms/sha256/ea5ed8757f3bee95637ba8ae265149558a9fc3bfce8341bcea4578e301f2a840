## 9.3 这不是“最小能量原则”自动驱动程序

一个完整减法回路的机器整数净变化为：

$$
N\mapsto N\left(\frac25\right)^d.
$$

所以：

$$
\ln N\mapsto\ln N-d\ln\frac52.
$$

但被检验的数学整数：

$$
m=a+c
$$

没有改变。

这说明：

$$
\boxed{
\text{机器内存的对数大小}
\neq
\text{当前数学输入}
\neq
\text{算法已取得的语义进展}.
}
$$

在固定候选中，终止性来自剩余量下降、试除数下降；并不是所有微步都在最小化同一个 \(\ln N\)。

