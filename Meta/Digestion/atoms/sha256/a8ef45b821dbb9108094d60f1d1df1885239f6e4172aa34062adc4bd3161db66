# 八、欧拉常数确实又会出现，但出现在可计算的运行时间副项中

这里有一条比“数字巧合”更具体的联系。

设 \(p\) 是素数。定义 \(L(p)\) 为：

> 从规范入口 \(\iota(p,p-1)\) 开始，到输出 \(2^p\) 为止，实际执行的分数次数。

注意，它不是从 \(N_0=2\) 开始的累计时间，也不是计算机上的墙钟时间。

因为 \(p\) 是素数，试除：

$$
p-1,p-2,\ldots,2
$$

全部失败，最后 \(d=1\) 成功。

由式（11）：

$$
\boxed{
L(p)
=
(p-1)(6p+2)
+
2\sum_{d=2}^{p-1}\left\lfloor\frac pd\right\rfloor.
}
\tag{19}
$$

定义：

$$
S(x)=\sum_{d\le x}\left\lfloor\frac xd\right\rfloor.
$$

它也等于：

$$
S(x)=\sum_{n\le x}\tau(n),
$$

因为两边都在数：

$$
\{(u,v)\in\mathbb N_+^2:uv\le x\}.
$$

因此：

$$
\boxed{
L(p)=6p^2-6p-4+2S(p).
}
\tag{20}
$$

