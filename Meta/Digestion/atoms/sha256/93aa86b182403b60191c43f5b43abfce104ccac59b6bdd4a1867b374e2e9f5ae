**命题 42.4（表表示与按需表示的边界；repo-derived）。** 若保存每个报告边界的完整读出和区间证书，显式表占 $O(Nb)$ 位；只保存两条转移数组而按编号重算读出时，数组占 $O(N\log N)$ 位。按需执行器在报告边界只保存一个 $\lceil\log_2N\rceil$ 位编号，外加命题42.3的工作和当前输出。$N$ 是名义报告边界的载体数；除非另行定义，微步控制配置和临时整数不计作 $N$ 个状态。两种表量是表示相关的上界，不给出硬件无关的时间最优或总成本最优结论。

例如 $p=1/4,\varepsilon=1/8,r=2^{-h}$（$h\ge2$）时 $m=16(2^h-2)$。显式两条转移数组为 $\Theta(h2^h)$ 位，按需执行器的峰值为 $O(h)$ 位、每报告为 $O(h^2)$ 位操作；这只比较所声明的存储和顺序位模型，不能外推到其它共享存储或输出收费规则。轨迹数值保持了命题41.4的预测值，但实现成本不是原 Bayesian 动力的成本结论。

