## 2.1 一个有限维操作模型

设状态空间为有限维向量空间 \(V\)，初态为：

$$
a\in V.
$$

每个允许操作 \(i\) 有一个线性实现：

$$
T_i:V\to V.
$$

最终观察为：

$$
c:V\to\mathbb R
$$

或 \(\mathbb C\)。

对按时间顺序执行的字串：

$$
w=i_1i_2\cdots i_m,
$$

定义：

$$
T_w=T_{i_m}\cdots T_{i_1},
\qquad
s(w)=cT_wa.
$$

这是一个明确的有限线性模型；并不是说所有 CUT、ADMIT 或真实物理操作天然都是同型线性算子。

定义可达子空间：

$$
\mathcal R
=
\operatorname{span}\{T_wa:w\text{ 为有限操作串}\},
$$

以及永远不可见的子空间：

$$
\boxed{
\mathcal N
=
\{v\in\mathcal R:
cT_wv=0\ \text{对全部未来 }w\}.
}
\tag{1}
$$

仓库已有“可达空间模去未来不可见空间”的商构造。当前读取的定理针对单一离散动力学；下面是对多个操作族的相同思路作直接推广。

