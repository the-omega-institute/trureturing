# 九、换到能量切面时，不能假装每条路径都经过一个精确能量点

时间切面：

$$
\Omega(d)=t
$$

有一个方便性质：每条八步历史恰好经过一次。

但能量是：

$$
E(d)=\ln d.
$$

若选定：

$$
E_*=\frac12\ln5040,
$$

一般不存在整数状态恰好满足：

$$
E(d)=E_*.
$$

因为5040不是平方数。

**离散过程可能直接跨过一个连续切面，而不落在切面上。**

因此，正确对象应当是穿越该切面的边：

$$
\boxed{
\mathcal C_{E_*}
=
\{d\to pd:
d^2<5040\le(pd)^2\}.
}
\tag{23}
$$

每条从1到5040的历史恰好穿过其中一条边。

设：

$$
F(d)=\text{从1到 }d\text{ 的合法前缀历史数},
$$

$$
B(pd)=\text{从 }pd\text{ 到5040的合法后续历史数}.
$$

则：

$$
\boxed{
840
=
\sum_{(d\to pd)\in\mathcal C_{E_*}}
F(d)B(pd).
}
\tag{24}
$$

