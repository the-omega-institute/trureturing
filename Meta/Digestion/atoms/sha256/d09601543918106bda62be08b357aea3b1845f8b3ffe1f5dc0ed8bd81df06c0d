# 六、未来边界怎样改变当前动力学，而不意味着“未来反向施力”？

给定终点 \(a\)，当前完成计数为 \(b\)，剩余事件数为 \(L-t\)。

从 \(b\) 到终点的完整后续历史数为：

$$
h_t(b)=
\frac{(L-t)!}{\prod_i(a_i-b_i)!}.
$$

因此，下一个事件属于第 \(i\) 类的概率是：

$$
\boxed{
\Pr(B_{t+1}=b+e_i\mid B_t=b)
=
\frac{h_{t+1}(b+e_i)}{h_t(b)}
=
\frac{a_i-b_i}{L-t}.
}
\tag{17}
$$

这是一条只依赖当前状态和剩余时间的精确更新。

对5040的初态：

$$
\boxed{
\Pr(\text{第一步乘 }2,3,5,7)
=
\left(\frac12,\frac14,\frac18,\frac18\right).
}
$$

它不是“从四个可用方向中均匀选一个”。

**两种选择都可以定义程序，但它们产生不同的历史概率。**

这种按照指定终点重新加权路径的做法，与 Markov 桥和 Doob 条件变换同属一个数学机制：给定末端条件后，当前转移要乘上剩余路径权重的比值。([arXiv][3])

这里没有推导出逆因果。我们只是选择：

$$
\text{所有最终到达 }a\text{ 的路径}
$$

作为条件样本空间。

如果不知道终点 \(a\)，就不能未经说明直接使用式（17）。这再次表明，**ANCHOR 不只是一个当前数值，还可能包含整个研究问题所固定的边界条件。**

