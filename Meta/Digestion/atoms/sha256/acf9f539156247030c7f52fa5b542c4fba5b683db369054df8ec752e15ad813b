## 定理 5.1：二阶图模型是全立方体的安全下界

$$
\boxed{
Q_2(S)\le F(S)
\qquad\forall S.
}
\tag{16}
$$

### 证明

按某个次序逐一加入 \(S\) 中的方向。

加入方向 \(i\) 时，将它相对基点的变化展开成：

$$
\Delta_iF(\text{已有集合})
=
\Delta_iF(\varnothing)
+
\sum_{j\text{ 先于 }i}
\Delta_j\Delta_iF(\text{更早的集合}).
$$

二阶差分只来自 \(h\)，其随基准能量增加而增加，因为：

$$
h'''(E)
=
\frac{2(\ln E)^2+3\ln E+2}
{E^3(\ln E)^3}>0.
$$

所以每个平移后的二阶差分都不小于基点处的 \(c_{ij}\)。

逐步求和，得到：

$$
F(S)-F(\varnothing)
\ge
\sum_{i\in S}g_i+\sum_{i<j\in S}c_{ij}.
$$

证毕。

这个证明很重要：

$$
\boxed{
\text{我们不是假装高阶项不存在，}
\quad
\text{而是证明它们的合计不会把实际值压到二阶下界以下。}
}
$$

在四方向情形中，若 \(Q_3\) 再加入全部三阶项，还能得到：

$$
\boxed{
Q_2(S)\le F(S)\le Q_3(S).
}
\tag{17}
$$

右侧是因为唯一可能遗漏的四阶项为负。

