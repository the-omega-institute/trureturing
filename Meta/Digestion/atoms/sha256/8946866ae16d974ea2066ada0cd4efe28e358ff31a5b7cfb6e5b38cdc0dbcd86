## 定义 171.2（能力改变）

设控制轮廓：

[
K_{\mathrm{ctl}}(x).
]

若：

[
I(Ux)=I(x)
]

但：

[
K_{\mathrm{ctl}}(Ux)\neq K_{\mathrm{ctl}}(x),
]

则主体保持身份，但能力发生改变。

这形式化：

[
\boxed{
\text{同一个人可以成为一个不同能力结构的人}.
}
]

---

