## 7.2 欧拉常数控制的是全素数方向的累计标尺

Mertens 乘积公式给出：

$$
A(P)\sim e^\gamma\ln P.
$$

因此：

$$
\ln A(P)
=
\gamma+\ln\ln P+o(1).
$$

这来自不断扩张的全部素数方向，而不是只由 \(2,3,5,7\) 决定。([arXiv][4])

所以，目前的困难可以说得很准确：

> **单个素数方向的衰减、有限边界和最小作用量都已经可精确求解。尚需控制的是：不断增加的素数方向，与各方向离散边界之间，能否始终满足式（7.4）。**

不能把 \(P(n)\) 与 \(\ln n\) 不加误差地互换，也不能因为局部衰减都是指数型，就省略全部方向的累计效应。

