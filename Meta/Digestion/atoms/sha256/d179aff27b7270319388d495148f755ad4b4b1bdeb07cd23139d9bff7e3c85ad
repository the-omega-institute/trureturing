# 第四百六十二部　\(\cosh\) 完成差的 Lorentz 奇偶分解

对任意：

$$
f\in C_c^\infty((-L,L)),
$$

定义两个 Laplace boundary channels：

$$
\boxed{
\mathcal C_a(f)
=
\int_{\mathbb R}
\cosh(ax)f(x)\,dx,
}
\tag{462.1}
$$

$$
\boxed{
\mathcal S_a(f)
=
\int_{\mathbb R}
\sinh(ax)f(x)\,dx.
}
\tag{462.2}
$$

其中：

* \(\mathcal C_a\) 只读取 \(f\) 的偶部分；
