## 定义 46.1　辐射对落入历史的记忆能力

令 \(X\) 区分若干初始历史，例如具有相同宏观质量、角动量和电荷，但微观制备不同的输入。

对每个历史，外侧辐射态为

$$
\rho_R^x.
$$

定义

$$
I(X:R)
=
S\!\left(\sum_xp_x\rho_R^x\right)
-
\sum_xp_xS(\rho_R^x).
$$

它衡量辐射对这些输入历史保留了多少区别。

---

