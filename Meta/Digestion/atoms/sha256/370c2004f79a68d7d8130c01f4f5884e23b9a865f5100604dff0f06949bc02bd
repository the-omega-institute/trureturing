# 239. 上下文性是局部—全局 image defect

设：

* `M` 是全部测量标签；
* `\mathcal C` 是一族可共同执行的测量上下文；
* 每个测量 `m` 有结果集 `O_m`。

对上下文：

$$
C\in\mathcal C,
$$

定义联合结果空间：

$$
\Omega_C
=
\prod_{m\in C}O_m.
$$

一个经验模型给每个上下文一个概率分布：

$$
e_C\in\Delta(\Omega_C).
$$

