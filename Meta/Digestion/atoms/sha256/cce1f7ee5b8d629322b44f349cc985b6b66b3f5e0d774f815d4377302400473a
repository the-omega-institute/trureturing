# 六、进一步算出：原版 PRIMEGAME 的时钟具有三次增长律

这里还能继续，不必停在每次试除的成本。

对 \(n\ge2\)，记：

$$
\ell(n)=n\text{ 的最小素因子},
$$

$$
b_n=\frac n{\ell(n)}
$$

为最大真因数。

从候选起点

$$
M(n,n-1)
$$

运行到

$$
M(n+1,n)
$$

的总成本记为 \(C(n)\)。

