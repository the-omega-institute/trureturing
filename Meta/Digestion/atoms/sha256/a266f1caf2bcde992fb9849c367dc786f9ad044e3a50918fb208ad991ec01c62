# 607. dyadic 根塔：从一对到圆

令：

$$
m_j=2^j,
$$

并定义：

$$
r_{H,j}
=
L_{2^jH}^{-1/2^j}.
$$

则：

$$
r_{H,j}
\nearrow
\varphi^{-H}.
$$

因为若：

$$
x=\delta_H^{2^{j+1}}\in(0,1),
$$

则：

$$
\frac1{2^{j+1}}\log(1+x^2)
<
\frac1{2^j}\log(1+x).
$$

第 \(j\) 层根集为：

$$
\boxed{
\mathcal Z_{H,j}
=
r_{H,j}\,
\mu_{2^j},
}
$$

其中：

$$
\mu_{2^j}
=
\left\{
e^{2\pi ik/2^j}
\right\}.
$$

于是：

* \(j=1\)：一对根；
