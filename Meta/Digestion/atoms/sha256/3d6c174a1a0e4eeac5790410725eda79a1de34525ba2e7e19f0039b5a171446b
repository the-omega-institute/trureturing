**定义 42.7（六整数参数与输入长度；repo-derived）。** 本节的参数输入是按固定顺序排列的六个正整数
$$
(P,A,R,B,E,C),
$$
组成三对整数参数
$$
(P,A),\qquad (R,B),\qquad (E,C),
$$
并定义
$$
p=P/A,\qquad r=R/B,\qquad\varepsilon=E/C.
$$
参数域为
$$
2P<A,\qquad 2R<B,\qquad E>0,\qquad C>0,
$$
即 $0<p<1/2$、$0<r<1/2$、$\varepsilon>0$；六个整数均为正数。这里每个正整数 $n$ 用
$$
\operatorname{code}(n)=1^{\ell(n)}0\,\operatorname{bin}_{\ell(n)}(n),\qquad
\ell(n)=\lfloor\log _2n\rfloor+1,
$$
作自分隔二进制编码，六个码字按上述顺序串接；因此分隔和长度信息也计入输入。令 $s$ 为这六个码字的总位长，即完整的六整数输入编码长度。于是命题 42.3中的 $s$ 不是六个输入整数对（十二个整数）的长度，而是 $(P,A,R,B,E,C)$ 三个整数对（六个整数）的完整编码长度；命题 42.3–42.4以及本节的全部复杂度界均以此 $s$ 为输入规模。定义 42.1–42.6的网格、转移和读出公式保持不变。

## 追加锚（本行以下为增补区）
