**定理 127.1。** 对式（127.1）—（127.7）的固定 $L,q,P,\pi$，下列条件等价：

1. 对每个严格正微观初态 $p_0$，$t\mapsto D_c(T_tp_0)$ 在 $[0,\infty)$ 上不增。
2. $PLQ=0$。
3. $[P,L]=0$。
4. 存在标签上的随机半群 $S_t$，使 $\mathsf CT_t=S_t\mathsf C$ 对所有 $t\ge0$ 成立。
5. 式（127.22）的 $K(t)$ 对所有 $t\ge0$ 为零。

