# 440. 权限扩大单调增加攻击可达域

设权限包 (P) 允许行动幺半群：

[
M(P).
]

若：

[
P\subseteq Q,
]

假设：

[
M(P)\subseteq M(Q).
]

从锚点 (a) 出发的可达域：

[
\operatorname{Reach}_{P}(a)
===========================

{F_m(a)\mid m\in M(P)}.
]

