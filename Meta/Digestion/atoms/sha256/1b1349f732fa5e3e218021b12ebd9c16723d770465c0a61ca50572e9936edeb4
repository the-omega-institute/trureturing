# 八、把一段时间压缩掉时，必须保留其最危险的内部前缀

前面研究的是状态和最优值。若目标是执行过程安全，还必须考虑路径内部。

设一段执行：

$$
x_0\to x_1\to\cdots\to x_L,
$$

某个安全评分为 \(D(x)\)。定义每步增量：

$$
g_j=D(x_j)-D(x_{j-1}).
$$

仅保存总变化：

$$
\sum_jg_j
$$

不够。

例如两段增量：

$$
(+2,-1),
\qquad
(-1,+2)
$$

都使最终评分增加1。

但若初始余量是 \(1/2\)，第一条路径始终安全；第二条路径在第一步后已经为负。

