### 111.1 精确预测记忆的定义

设有限状态集合为 (X)，当前读出为

$$
q:X	o O,
$$

一步更新为

$$
F:X	o X,
$$

候选记忆接口为

$$
r:X	o M.
$$

源码中的 `IsExactPredictiveMemory q F r` 要求存在两个因子化：当前读出可以由记忆恢复，且更新后的记忆仍只依赖旧记忆。用普通数学语言写成，就是存在

$$
\bar q:M\to O,
\qquad
\bar F:M\to M
$$

使得

$$
q=\bar q\circ r,
\qquad
r\circ F=\bar F\circ r.
$$

第一条保证当前观测不会被记忆丢掉；第二条保证记忆接口对下一步操作闭合。它正是前面反复出现的

$$
qF=\bar Fq
$$

在有限记忆接口上的版本，只是这里同时保留了当前读出 (q)。

