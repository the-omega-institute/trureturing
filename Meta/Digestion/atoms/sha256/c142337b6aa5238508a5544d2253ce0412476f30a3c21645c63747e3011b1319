## 定理 461.1（局部完成差）

若 \(\nu,\mu\) 是同一尺度 \(L\) 上的两个偶正实谱完成，则：

$$
\boxed{
H_{\nu,a}(t)-H_{\mu,a}(t)
=
\left[
R_a(\nu)-R_a(\mu)
\right]
\cosh(at)
}
\tag{461.5}
$$

对全部：

$$
|t|<2L
$$

成立。

### 证明

差：

$$
h=H_{\nu,a}-H_{\mu,a}
$$

满足：

$$
-h''+a^2h=0.
$$

所以：

$$
h(t)=Ae^{at}+Be^{-at}.
$$

两谱均为偶谱，故 \(h\) 为偶函数，从而 \(A=B\)。又：

$$
h(0)=R_a(\nu)-R_a(\mu).
$$

因此得到式 (461.5)。∎

这说明：

$$
\boxed{
\text{无限维局部谱完成自由，
在 resolvent correlation 中只剩一个 }\cosh\text{ 系数。}
}
$$

但这个 \(\cosh\) 系数在偶观察者与奇观察者中具有相反符号；这正是此前尚未显现的关键。

---

