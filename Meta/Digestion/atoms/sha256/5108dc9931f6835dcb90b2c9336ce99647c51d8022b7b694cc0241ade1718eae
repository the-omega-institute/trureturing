## 定理 250.1（Bayes 错误保证）

若在 \(\tau_\varepsilon\) 输出 MAP 状态，则：

\[
\mathbb P[\widehat X\neq X]
\le
\varepsilon.
\]

### 证明

给定停止历史，MAP 条件错误恰为：

\[
1-\max_x\pi_{\tau_\varepsilon}(x)
\le\varepsilon.
\]

再取全期望。 \(\square\)

