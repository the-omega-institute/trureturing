# 十、这与仓库已有定理应当怎样衔接？

本轮读取到了三类直接相关的部件。

**不可见空间。**
`MaximalUnobservableSubspace` 能表达“某个差异在全部未来读数中都不会出现”。这适合形式化四棱锥的暗模式与新增观察的作用。

**黄金阈值。**
`GoldenTwoShadowBound` 证明的是，在相应算子条件下：

$$
D+D^2\le I
\iff
\|D\|\le\varphi^{-1},
$$

并包含相关逆算子界的等价关系。它不是说任意系统的 \(D+D^2\le I\) 自动成立。

这与四棱锥中：

$$
\lambda^2-2\lambda-4=0
$$

产生黄金比例是两种不同机制。它们都涉及二次方程，但不能直接互相替代。

**次模优化。**
仓库的 `SubmodularGreedyApproximation` 处理的是单调次模函数在基数限制下的贪心**最大化**保证。当前需要的是余量**最小化**，因此不能直接套用那个 \(1-1/e\) 定理。

本轮给出的二阶下界、最小割转换和特殊阈值链，需要作为各自有明确前提的新接口接入。

