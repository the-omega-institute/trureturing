## 4.1 余数不为零

若 \(r>0\)，程序先执行：

$$
(m,r,0,d-r-1;17)
\longrightarrow
(m,r-1,0,d-r-1;19).
$$

然后把 \(2\) 寄存器中的全部 \(m\) 单位搬回 \(5\) 寄存器，再把剩余工作量重新装入 \(7\) 寄存器。

最终得到：

$$
\boxed{
\iota(m,d-1).
}
\tag{7}
$$

待检验整数不变，试除数减一。

