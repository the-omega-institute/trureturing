## 定理 516.1（有限步可强制到达）

(x\in W_n) 当且仅当主体存在策略，能够保证在至多 (n) 步内到达 (G)。

### 证明

对 (n) 归纳。

(n=0) 时恰为 (x\in G)。

若 (x\in\operatorname{CPre}(W_n))，存在行动使所有后继进入 (W_n)，再由归纳假设至多 (n) 步到达目标。

反向同理分析策略第一步。

(\square)

---

