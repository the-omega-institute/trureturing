# 本轮最终结论

这一轮使“不同名字怎样描述同一个完成影像”获得了一个比此前更完整的答案。

在 RH 成立时，移动法向平方影：

$$
Q_t(w)
$$

不是一串偶然为正的 Taylor 系数。

它是：

$$
\boxed{
Q_t(w)
=
Q_t(0)\det(I+wA_t),
\qquad
A_t\ge0.
}
$$

所以每一个法向 jet：

$$
\mathcal J_m(t)
$$

都是一个外幂扇区的正迹：

$$
\boxed{
\mathcal J_m(t)
=
\mathcal J_0(t)
\operatorname{Tr}
\left(
\bigwedge\nolimits^mA_t
\right).
}
$$

一阶名字读取单个谱模式。

二阶名字读取谱模式之间的反对称关系。

更高阶名字读取更高外幂关系。

因此 completed \(\xi\) 不只是拥有一列数，而是把一整座表示关系塔压缩进了一个标量行列式。

---

离线零点则会破坏这一正外幂实现。

选择观察者高度等于离线零点纵坐标：

$$
t=\gamma,
$$

该缺陷立即变成：

$$
\boxed{
a_{\mathrm{off}}=-\delta^{-2}.
}
$$

其局部平方影因子为：

$$
\boxed{
\left(
1-\frac{w}{\delta^2}
\right)^{2m}.
}
$$

所以此前所谓：

* 负信息；
