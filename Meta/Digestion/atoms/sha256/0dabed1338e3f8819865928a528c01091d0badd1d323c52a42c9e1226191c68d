# 六、过去的信息怎样压进一个不变量，而不必保存整个历史？

仅仅到达 \(d=1\)，并不在任意初态下证明 \(m\) 是素数。

例如，如果直接把程序放到：

$$
\iota(4,1)=13\cdot5^4\cdot7,
$$

它会在26步后产生：

$$
2^4.
$$

这并不违反从 \(N_0=2\) 出发的素数生成定理，因为这个人为入口跳过了试除2的历史。

因此，需要一个真正的历史相容条件：

$$
\boxed{
\mathcal I(m,d):
\quad
2\le m,\quad1\le d<m,\quad
\forall e,\ d<e<m\Longrightarrow e\nmid m.
}
\tag{14}
$$

它表示：

> 所有比当前 \(d\) 更大的真因子候选，已经被排除。

