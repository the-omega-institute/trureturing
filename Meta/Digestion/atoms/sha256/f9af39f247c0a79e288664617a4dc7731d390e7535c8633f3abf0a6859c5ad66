**候签定理 4.135(Zizka:除数个数整除 squarefree kernel 则二者相等,A070226 / A120737)**〔open;**档位:第一档**(第 5⁵ 条)——OEIS 评注,页面仍标 Conjecture;**文献核对(2026-09-10,orchestrator 亲跑)**:取 `https://oeis.org/search?q=id:A070226&fmt=json` 的条目原文,该猜想以下述逐字措辞列出,**条目内未给出证明、亦无指向证明的引用**;`A000005(k) = d(k)`(除数个数),`A007947(k) = rad(k)`(squarefree kernel,即全部不同素因子之积);**本仓核对由 orchestrator 亲跑**:`A070226` / `A120737` / `squarefree kernel` / `radical.*divisor` 在 `origin/dev` 的 `D5/**/*.lean` 与 `Library/**` 上**各零命中**;**数值自检(orchestrator 亲跑)**:`k ∈ [1, 200000]` 中满足 `d(k) ∣ rad(k)` 者 **26** 个,其中 `rad ≠ d` 的**反例 0 个**,命题在该范围内未被证伪;落 `D5/S1/Arith/DivisorCountRadicalCoincidence`(**只 import Mathlib、零仓内前置,取最低兼容地层**,理由同 4.134);**拟议逃逸见证(写在探针之前,不事后追认)**:**双向计数夹逼** —— 设 `k` 有 `r` 个不同素因子、指数 `eᵢ ≥ 1`,则 `rad(k) = ∏pᵢ` 是 squarefree 且**恰 `r` 个**素因子,而 `d(k) = ∏(eᵢ+1)` 是 **`r` 个各自 ≥ 2 的因子之积**;由 `d ∣ rad` 与 rad squarefree 得 `d` 的**不同**素因子个数 ≤ `r`,由 `r` 个 ≥2 的因子相乘得 `d` 的**计重**素因子个数 ≥ `r`;两界夹逼迫使二者皆等于 `r` 且诸素因子互异,于是每个 `eᵢ+1` 本身是素数、互不相同、且整除 `rad`,而 `rad` 恰为 `r` 个不同素数之积,故 `d = rad`。**该夹逼是本条的内容**:它把一个整除关系升级为等式,支点是「计重下界」与「互异上界」在同一个 `r` 上相撞,不由任何已有引理实例化得到;`k = 1` 单独处理〕。原文(A070226 评注,_Ctibor O. Zizka_,逐字):"Conjecture : If A000005(k) divides A007947(k) for some k, then A007947(k) = A000005(k)."。即:对一切 `k ≥ 1`,若 `d(k) ∣ rad(k)` 则 `rad(k) = d(k)`。**产地(第 9′ 条)**:选题、OEIS 原文取证、仓内去重、数值自检与上述证明骨架均由 orchestrator(claude 主循环)亲跑亲写,**非转述、无搜题席**;实施与评审席另派。
