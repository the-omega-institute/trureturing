# 五、信息逃逸应该按“遗漏的影响”计量，而不只按状态数计量

在上面的近临界区块中，若给所有二值选择等概率，则完整选择具有：

$$
M(T,\varepsilon)
$$

比特信息。

如果一个观察者最多有 \(K\) 个可能读数，那么：

$$
\boxed{
H(\text{完整选择}\mid\text{观察})
\ge
M(T,\varepsilon)-\log_2K.
}
\tag{15}
$$

所以，固定容量的观察者会遗漏越来越多的状态区分。

但对于目标 \(J_{\lambda_T}\)，整个区块的数值范围只有 \(C(T,\varepsilon)\)。

若只允许用一个常数近似整个区块的目标值，最佳最坏误差恰好为：

$$
\boxed{
\frac12C(T,\varepsilon).
}
\tag{16}
$$

