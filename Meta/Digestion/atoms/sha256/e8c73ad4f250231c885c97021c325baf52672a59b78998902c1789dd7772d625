**谱代价方面，**`PathSpectralClassical` 已将正定矩阵的对数行列式散度写成谱和与连续路径积分。它能连接局部矩阵、非负代价和变分结构，但其前提与结论都必须保留，不能将一般非负性直接替换为 Robin 所需的尖锐下界。

本轮新的纸面推导主要是：

$$
\boxed{
\text{最小路径作用量}
\longleftrightarrow
\text{边界衰减}
\longleftrightarrow
\text{条件化信息代价}
\longleftrightarrow
\text{整数资源最优性}.
}
$$

我也对 60 个有限路径实例数值复核了端到端逆矩阵公式和最小作用量公式；其全称结论来自正文证明，而不是这些数值测试。新增结果尚未在本轮写入仓库或完成 Lean 内核验证。

---

