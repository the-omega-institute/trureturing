**证明。** 每一对支持路径互为逐位补，因此每个时刻的位均公平。常值路径从不改变读数；$B,E$ 只在首次更新时改变读数；$C,D$ 每次都改变读数，给式（52.9）。三组支持分别是 $\{000,111\}$、$\{100,011\}$、$\{010,101\}$，互不相交。取其中一组支持为事件，其在相应律下概率一、在另外两律下概率零，所以按总变差距离 $\sup_E|\mu(E)-\nu(E)|$ 的定义，任意两者距离至少一；概率差至多一，故距离恰为一。移位保持常值两路径，交换两条交替路径，而将首次翻转两路径送到常值两路径，故得到所述平稳性。$\square$

前三次读数在此确有作用：只观察 $(q_0,q_1)$ 时，$\mu_{\rm once}$ 与 $\mu_{\rm alt}$ 都在 $01,10$ 上各半；第三次读数才分开这两个联合律。所有单时刻均值及其式（52.6）更新均相同，仍不足以恢复这项差别。

