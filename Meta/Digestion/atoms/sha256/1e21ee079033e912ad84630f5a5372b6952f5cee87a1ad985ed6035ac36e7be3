## 定理5.1：Hermite–Sylvester 实根判据

$$
\boxed{
P\text{ 的全部根为实数}
\iff
\mathsf H(P)\succeq0.
}
\tag{19}
$$

### 证明要点

若全部根为实数，对任意实多项式：

$$
q(X)=\sum_{i=0}^{d-1}c_iX^i,
$$

有：

$$
c^T\mathsf H(P)c
=
\sum_{j=1}^dq(r_j)^2\ge0.
$$

反过来，若存在一对非实共轭根 \(z,\bar z\)，可以通过实系数插值构造 \(q\)，使：

$$
q(z)=i,\qquad q(\bar z)=-i,
$$

并在其余不同根处取零。

那么这对根对二次型的贡献为负，故矩阵不能正半定。重根情形按重数累加即可。完整的经典判据可由 Lagrange 插值证明。([arXiv][5])

这里非常重要的是：

$$
\boxed{
\sum_jq(r_j)^2
\quad\text{不是}\quad
\sum_j|q(r_j)|^2.
}
$$

如果未经证明就把前者换成后者，正性会变成自动成立，但原来的实根约束也被改掉了。

