# 四、但 \(\pi\) 处的零点具有更强保护：交换 \(5\) 与 \(7\) 即可保证

这次不再要求所有历史等权。

令 \(\mathsf S\) 把历史中唯一的 \(5\) 与唯一的 \(7\) 交换。

由于它们都大于其他事件类型 \(2,3\)，交换后与其他事件形成的逆序总数不变；改变的只有 \(5,7\) 之间的次序。

因此：

$$
\boxed{
A(\mathsf S\gamma)-A(\gamma)=\pm1.
}
\tag{12}
$$

于是：

$$
(-1)^{A(\mathsf S\gamma)}
=
-(-1)^{A(\gamma)}.
$$

