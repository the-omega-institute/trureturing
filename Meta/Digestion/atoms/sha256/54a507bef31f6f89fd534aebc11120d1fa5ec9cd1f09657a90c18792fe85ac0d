**候签定理 4.140(OEIS A397621:Pascal 行模 2 的有限线性复杂度)**〔conjecture〕。OEIS A397621 `%N` 原文:"a(n) is the linear complexity over GF(2) of the binary expansion of n (as computed by the Berlekamp-Massey algorithm).";`%O A397621 1,5`;其 `%I` 为 revision #49,Aug 10 2026 16:43:37,作者行 `_Darío Clavijo_, Aug 02 2026`;该条目的目标 `%F` 原文:"Conjecture: a(A001317(n)) = A080079(n).";当前条目仍将该句标为 Conjecture,未在该句下给出证明或反例。A001317 `%N` 原文:"Sierpiński's triangle (Pascal's triangle mod 2) converted to decimal."、`%O A001317 0,2`;A080079 `%N` 原文:"Least number causing the longest carry sequence when adding numbers <= n to n in binary representation."、`%O A080079 1,2`;A080079 的 `%F` 另给出 `a(n) = -n + 2*2^floor(log(n)/log(2)).`。以下取自由指标为 (n\ge1),因为 A080079 的声明域从 1 开始;条目 `%F` 中另有 `a(0)=0` 的扩展不把 n=0 纳入本式。令 (M) 为严格大于 n 的最小二的幂,令 (H=M/2),写 (n=H+r),则 (0\le r<H),并令 (d=M-n=H-r);这一定义对 n 为二的幂也成立,此时 r=0,d=H,而 n=1 同时给出 r=0,d=1。令 (s_n=(\binom n0,\binom n1,\ldots,\binom nn)\in\mathbb F_2^{n+1}),按系数次序读出;由于二项式系数对称,它与 A001317(n) 的最高位优先二进制词相同。对非零有限词 (s=(s_0,\ldots,s_{N-1})),有限线性复杂度取最小的 (L\ge1),使存在 (c_0,\ldots,c_{L-1}\in\mathbb F_2) 满足 (s_{i+L}=\sum_{j=0}^{L-1}c_js_{i+j}) 对所有 (0\le i<N-L),零词的复杂度取 0。候签断言：对每个 n≥1,(L(s_n)=d=A080079(n))。路线核验如下。上界中取 (S(x)=(1+x)^n),(C(x)=(1+x)^d);在 (mathbb F_2[x]) 中 (C(x)S(x)=(1+x)^M=1+x^M)。有限词长度为 (N=n+1),递推只需验证乘积在系数位置 (d,d+1,\ldots,n) 为零;这些位置既不含常数项也不含唯一的高次项 (x^M),且 d≤N,故 C 给出阶 d 的齐次递推。这里没有把无限序列的递推误当有限前缀结论,所需条件正是 (M>n) 与上述有限长度区间。下界中 Frobenius 恒等式给出 (S(x)=(1+x)^r(1+x^H));于是 (s_n) 恰为一个长度 (r+1)、首项为 1 的词 (w),接着 (d-1=H-r-1) 个零,再接同一个词 w。一般地,若有限非零词形为 (w\,0^k\,w) 且 w 首项为 1,任何阶 (1\le L\le k) 的齐次递推在第二个 w 的首个 1 处只读到前面的 k 个零,因而预测 0,矛盾;故其复杂度大于 k。应用 k=d-1 得 (L(s_n)\ge d),与上界合为等式。这里下界对全部 (L<d) 成立;d=1 时没有正整数 L<d,下界是空的而上界直接给出复杂度 1;当 r=0 时零间隔长度为 d-1=H-1,没有额外例外。零间隔恰无长度当且仅当 d-1=0,即 n=M-1=2^q-1(q\ge1)。拟议逃逸见证为两个被主推导实际使用的命题：(i)块间隔引理：首项为 1 的 (w\,0^k\,w) 词满足有限线性复杂度 (>k);(ii)Frobenius 块结构：在 (M=2H,n=H+r,0\le r<H) 下, ((1+x)^n=(1+x)^r(1+x^H)) 的系数词正是 (w\,0^{H-r-1}\,w)。二者都不是有限线性复杂度定义或一般长度界的改写,而是该 Pascal 行分类的下界输入。文献边界：T. H. Randrianarisoa, “Coding Theory using Linear Complexity of Finite Sequences”, arXiv:1802.10034v2,pp.3–4,Definition 6 与 Proposition 1,提供有限词线性复杂度的通用定义及 (L\le N) 的长度界;这些属于文献已有内容,不列为本候签见证。Beltrá、Cardell、Requena,“Binomial sequences over prime fields”,arXiv:2606.25109v1,§3.2.2,Theorem 16,讨论随 n 变化的无穷二项式序列及其特征多项式,不是有限的第 n 行,也不承担本式。A001317 对 Pascal 行与 ((1+x)^n) 系数的说明以及 A080079 的距离公式是定义/来源输入;精确的有限复杂度等式及其块间隔下界仍是本候签的内容。适用边界为 GF(2)、有限词长度 n+1、n≥1;不把 n=0 的 A080079 扩展值写入 A397621 的偏移域。
