### 140.1 固定选择器与两种续接合同

这里区分三个最小化或续接问题：$d_k(R)$ 在每个顶点使用同一个确定的最近 $R$ 位函数，最小化其平均运输缺陷；本节的 $B_{\rm out}(R,k)$、$B_{\rm leg}(R,k)$ 在逐路径精确复现指定 $F_R$ 的条件下，最小化持久状态位数；第142节则从实际取得的初值运输当前参考位。缓存实现仍须输出同一个窗口函数，因此不改变前一个输出合同。初始化运输改变了目标响应，不要求复现 $F_R$ 的选锚与过期行为；一位足以运输任意指定初值，而该位是否代表真实绝对方向，取决于是否取得实际校准。

