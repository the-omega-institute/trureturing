# 一、5040位于一条真正的“时间瓶颈边界”

沿用事件规则：

> 一次事件，只把当前整数乘以一个素数。

对一般整数：

$$
N=\prod_{i=1}^{k}p_i^{a_i},
$$

同一素数方向的事件必须按顺序发生：

$$
e_{i,1}\prec e_{i,2}\prec\cdots\prec e_{i,a_i}.
$$

不同方向没有额外依赖。因此，整个因果结构是 \(k\) 条链的并。

记：

$$
L=\sum_i a_i,
\qquad
a_{\max}=\max_i a_i.
$$

其中 \(L\) 是串行工作量，\(a_{\max}\) 是最长依赖链。

