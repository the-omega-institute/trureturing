**证明。** 由 $m\le16BCA+2$，$m$ 以及 $u,v$ 的编码长度都是 $O(s)$。命题42.2给出 $U_j<Bm$、$V_b,W_b<ABm$，并且
$$mW_b<ABm^2,\qquad e_b<V_b,\qquad2e_b<2ABm,$$
故常数个参数、编号、商余、当前读出和工作缓冲均为 $O(b)$ 位。初始化只需固定深度的乘、除、比较和向上取整；学校乘除法按 $O(b^2)$ 位操作计费。之后每步重算 $U_j,V_b,W_b$，一次带余除法产生新编号，再重算 $V_0/(ABm)$；顺序带不保存历史，故仍只有常数个 $O(b)$ 缓冲区。把每个参数读写、带头移动、计数和当前完整输出列入同一收费口径，得到所述峰值和单步上界。这里 $s$、$b$ 和二进制固定缓冲是模型假设；RRO 的模量或维数结论只启发这种成本分离，不能推出 Bayes 更新的复杂度。证毕。

