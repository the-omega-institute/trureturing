# 第六百二十二部　无限层级的第一种完成：逆极限

设每一层完成都产生一个更精细的状态空间：

$$
X_0
\longleftarrow
X_1
\longleftarrow
X_2
\longleftarrow
\cdots,
$$

限制映射记为：

$$
p_n:X_{n+1}\to X_n.
$$

定义无限完成线程：

$$
\boxed{
X_\infty
=
\varprojlim X_n
=
\left\{
(x_0,x_1,\ldots):
p_n(x_{n+1})=x_n
\quad\forall n
\right\}.
}
\tag{622.1}
$$

它保留了所有有限层信息。

一个原始完整对象 \(x\in X\) 产生线程：

$$
\boxed{
\iota_\infty(x)
=
(q_0(x),q_1(x),q_2(x),\ldots).
}
\tag{622.2}
$$

仓库已经形式化：该状态到逆极限线程的映射成为等价，当且仅当有限层联合能够分离状态，并且每个兼容线程都由唯一全局状态实现。

所以：

$$
\boxed{
\text{层级无限}
\not\Rightarrow
\text{自动存在完成态}.
}
$$

至少还需要：

$$
\boxed{
\begin{aligned}
\text{兼容性}
&:\quad
\text{相邻层能够限制};\\
\text{分离性}
&:\quad
\text{全部层联合不会混淆不同对象};\\
\text{实现性}
&:\quad
\text{每个形式兼容线程对应实际对象}.
\end{aligned}
}
$$

---

