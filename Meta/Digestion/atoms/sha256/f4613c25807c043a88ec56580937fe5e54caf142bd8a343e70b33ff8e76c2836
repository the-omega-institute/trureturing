### 证明

按每条完整历史穿越能量切面的唯一一条边分类。

固定该边后，合法前缀与合法后续可以独立拼接，数量相乘。再对所有穿越边相加。证毕。

本轮精确枚举发现，这个能量切面包含：

$$
\boxed{40\text{ 条穿越边},}
$$

其加权历史总数仍然是840。

它不同于中间时间切面的12个状态。

这40条边也不能直接被叫作40个 Schmidt 模式：**固定能量的穿越切面与固定时间槽的张量分割，不是同一种分解。**

因此，转换切面时至少要保留：

$$
\boxed{
\text{从哪一侧到哪一侧、}
\quad
\text{跨越了哪条边、}
\quad
\text{该边对应多少完整路径}.
}
$$

这比只把“时间坐标”改名为“能量坐标”严格得多。

