### 40a.2 固定零标签的实际学习状态

固定平方外部维数 q≥1，隐藏宽度 m≥1，U∈R^(m×q)、V∈R^(q×m)。采用同一个训练集 X=I_q、Y=0，同一个未平均损失

$$\mathcal L(U,V)=\frac12\|VU\|_F^2.$$

所有训练步都使用这一个损失；没有重置、改标签或自由选择梯度。步长 h_n>0 已知。读数为完整输出矩阵 W_n=V_nU_n，可由固定基向量输入取得；矩阵读数与单个标量读数的费用应分别计算。

