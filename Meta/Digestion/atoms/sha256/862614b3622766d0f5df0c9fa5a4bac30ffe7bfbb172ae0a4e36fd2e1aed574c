# 九、推广到完整指数盒子：高维组合可以缩成一条增长链

设：

$$
n_0=\prod_pp^{l_p}\ge3,
$$

并给每个有限活跃素数规定：

$$
l_p\le a_p\le u_p.
$$

这是一个有限指数盒子，状态数量为：

$$
\prod_p(u_p-l_p+1).
$$

把每个可能增加的指数层看成一个项目：

$$
(p,k),\qquad l_p<k\le u_p.
$$

它的成本是：

$$
\ell_{p,k}=\ln p,
$$

收益是：

$$
\rho_{p,k}
=
\ln\frac{1+p^{-1}+\cdots+p^{-k}}
{1+p^{-1}+\cdots+p^{-(k-1)}}.
$$

按：

$$
\eta(p,k)=\frac{\rho_{p,k}}{\ln p}
$$

从大到小排列全部新增层。

