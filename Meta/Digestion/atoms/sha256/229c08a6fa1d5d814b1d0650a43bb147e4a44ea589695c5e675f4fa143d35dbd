# 十、有限观察中，甚至不必解决所有阈值相等问题

对固定：

$$
\lambda\ge\varepsilon>0,
$$

只有有限多个边际层可能进入，因为：

$$
\eta(p,k)<\frac2{p^k}.
$$

因此：

$$
\eta(p,k)\ge\varepsilon
\Longrightarrow
p^k<\frac2\varepsilon.
$$

在这个有限列表上，可以为每个阈值计算严格有理区间。

如果两个区间暂时重叠，就先把它们归为一个候选区块，而不是强行用浮点大小决定次序。

由于不可能有三个阈值精确相同，持续精化之后，在任意固定有限列表上，最终可以把临界值隔离成每块至多两个候选的分组。

这不要求判定某个双阈值究竟是相等，还是极其接近。

随后：

* 能分开的区块，按认证顺序处理；
* 尚不能分开的双阈值，保留四个可能角；
* 若满足式（23），用两端安全证书覆盖整个区块。

这给项目一个具体的算法原则：

$$
\boxed{
\text{精度不足时，扩大安全覆盖域；}
\quad
\text{不要把未经认证的次序当成事实。}
}
\tag{30}
$$

“精确相等是否成立”与“这个不确定区块是否安全”是两个不同问题。后者有时可以先解决。

