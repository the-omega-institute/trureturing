## 定理7.1：全局候选单调性

对原版 PRIMEGAME 的每一步：

$$
\boxed{
\mathcal M(s')=
\begin{cases}
\mathcal M(s)+1,&\text{执行 }55/1,\\
\mathcal M(s),&\text{执行其他规则}.
\end{cases}
}
\tag{16}
$$

### 证明

大部分规则直接保持 \(a+c\)。

需要单独解释的是：

$$
19\to23\to19.
$$

第一步从 \(a\) 中取走一单位，所以用 \(\mathbf1_{\{\chi=23\}}\) 暂存；第二步把它放入 \(c\)，同时删除指标。

唯一真正增加候选的是 \(55/1\)：它使 \(c\) 增加一，而新控制标签11不产生修正。

逐条检查即得。证毕。

如果四个数据寄存器恰好是：

$$
(4,2,1,1),
$$

那么：

$$
\boxed{
\mathcal M=5\text{ 或 }6.
}
\tag{17}
$$

而本轮精确运行从 \(N_0=2\) 开始的前431个状态，直到：

$$
s_{430}=\iota(7,6),
$$

发现唯一一次该数据投影是：

$$
\boxed{
s_{157}=(4,2,1,1;29).
}
$$

此后 \(\mathcal M\ge7\)，由单调性不可能再回到5或6。

因此：

$$
\boxed{
\pi_4(N_t)=5040
\iff
t=157
}
\tag{18}
$$

对这条固定程序、固定初态的完整无限轨迹成立。

这不是“跑了很多步所以相信不会再出现”，而是：

$$
\boxed{
\text{有限前缀的精确证书}
+
\text{覆盖全部未来的单调不变量}.
}
$$

