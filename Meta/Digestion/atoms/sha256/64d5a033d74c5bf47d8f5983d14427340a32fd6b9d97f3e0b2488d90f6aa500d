### 50.4 两条必要条件，以及它们为何不够

第一列直接给出必须保留的位置：
$$
\boxed{
A\rho^n>c\quad\Longrightarrow\quad n\in S
}
\tag{50.10}
$$
是 $\|T^*-\mathcal E(a^S)\|_\rho\le c$ 的必要条件。若 $n\notin S$，该位置的第一列误差就是 $A\rho^n$。

再定义延迟权重
$$
\boxed{W_S=A\sum_{i\in S}\rho^{-(i+1)}.}
\tag{50.11}
$$
达到距离 $c$ 还必须有 $W_S\ge1$。证明如下：假设 $W_S<1$。对饱和稀疏来源，
$$
F_S(z)=1-A\sum_{i\in S}z^{i+1},
\qquad
P_0(z)=\sum_{k\ge0}p_{0,k}z^k
=A\sum_{j\in S}\frac{z^j}{F_S(z)^{j+1}}.
\tag{50.12}
$$
在 $|z|\le\rho^{-1}$ 上有
$|1-F_S(z)|\le W_S<1$。和式有限且不等式严格，可以扩大到某个 $R>\rho^{-1}$ 仍满足
$A\sum_{i\in S}R^{i+1}<1$。于是 $P_0$ 在该闭圆盘邻域解析，
$$
p_{0,k}=O(R^{-k})=o(\rho^k).
$$
由（50.2），首行加权误差为
$$
\rho^k(u_k-p_{0,k})
=c+\rho^{2k}\left(b-\frac{p_{0,k}}{\rho^k}\right)>c
$$
对充分大的有限 $k$ 成立。结合共同最优性，任何同支持来源都不能达到 $c$。

但是（50.10）与 $W_S\ge1$ 合在一起仍不充分。第50.8节给出同时满足它们、却已在 $(0,6)$ 失败的精确反例。远处增加一个大权重位置，不能自动修复它尚未进入递推之前的中间深度误差。

