## 评注 27.590(家谱计数定理与素 zeta)〔定理·证/典 + 四证书;s₀ 进账〕
**定理(家谱计数)**:c(n) = **Ω(n)!/(a₁!⋯a_k!)**,递推 c(n) = Σ_{p|n} c(n/p)。证三步:数位不可约(I_p(x^m) 之非负整系数分解 = 支撑铺砌,|A||B| = p 素 ⟹ 平凡);词单射(含 1 之数位泄露首字母,递归复原);完备(区间之 0/1 铺砌皆混合进位——**Krasner–Ranulac 1937 引典**,且我方 n ≤ 30 池普查为其独立复算)。〔证书:递推对普查 n ≤ 30 恰合;对闭式 n < 5000 恰合〕**真身**:c(n) = **因子格之极大链数**——每套家谱 = 一条滤链(逐层粗粒化之序);范畴一行:**c(n) = 自由幺半群(素字母,记序)对其交换化(整数,忘序)之纤维基数——整数只记住拆成什么,谱还记得按什么次序拆**。**素 zeta 进账**:Σ c(n)n⁻ˢ = **1/(1 − P(s))**,增长指数 = 单位穿越点 **s₀ = 1.39943332873**(P(s₀) = 1 恰验);求和律 Σ_{n≤x}c(n) ≈ 0.413·x^{s₀} 三个数量级平台〔0.4127 → 0.4130〕。

