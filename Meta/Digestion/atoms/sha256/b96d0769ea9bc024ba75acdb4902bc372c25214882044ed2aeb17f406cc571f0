# 八、为什么必须保留“联合可实现性”，不能把所有最坏界机械相加？

还有一个与项目非常契合的注意点。

在一个区块里：

* 最大规模变化；
* 最大阈值偏离；
* 最大曲率缺口；
* 最坏当前余量；

不一定由同一个状态同时实现。

如果分别取四个最坏值，再把它们拼成一个“超级坏状态”，可能得到一个根本不可实现的组合，导致证书过于粗糙。

反过来，如果这些最坏值确实能够同时实现，又不能只证明每项单独安全，就认为总和安全。

仓库的 `JointResourceSafetyCriterion` 明确要求：局部上限向量本身必须联合可实现，才能把总安全性精确归约为总预算比较。其有限反模型也说明，各项单独安全不蕴含同时安全。

式（22）的好处正在这里：

$$
s=\sum_{i\in S}u_i,
\qquad
\sum_{i\in S}d_i
$$

来自**同一个集合 \(S\)**。

我们没有把规模与偏离当成两个独立、任意组合的参数。

在 \((E,W)\) 平面上，这个区块的凸包是：

$$
\boxed{
(E_B,W_B)
+
\sum_i
\left[0,\,(u_i,u_i\eta_i)\right].
}
\tag{27}
$$

它是有限线段的 Minkowski 和。高维二值选择被投影成这个二维凸多边形后，仍然保留了“哪些规模与收益可以同时出现”。

