**评注 29.11(候签定理 4.102 之落点勘注:`D5/S3/Arith/` 已超 SL-003 准入线,按第 4.8 条 裂入既有子桶惯例)**〔errata;勘正候签定理 4.102 所写落点〕。4.102 的落点子句写作「落 `D5/S3/Arith/DerangementTwoAdicValuation`」。**该目录当前不可再收新文件**:实测 `git ls-tree --name-only origin/dev D5/S3/Arith/` 得 **51** 个直接子项,而 SL-003 的**准入**上限为 **48**(上限从属主派生,不抄常数:`grep -oE 'DirectoryFileLimit[[:space:]]*=[[:space:]]*[0-9]+' tools/StrataLint.Engine/Rules/RepositoryRules.Structure.cs` 得 48;仓库容忍线 96)。该桶已处于「容忍但不可准入」区间,**任何往其直接层新增文件的 lane 必被判红** —— 同一形态当日已实证一次:一个实施席把报告写进 `docs/reports/`(其时 72 个文件),判词逐字 `SL-003 docs/reports: directory contains 49 files (admission limit 48, repository tolerance 96; split per CLAUDE.md 8)`。**更正后的落点为 `D5/S3/Arith/Derangements/DerangementTwoAdicValuation`。** 依据两条:(一)第 4.8 条「桶满则裂,只裂不迁」——新条目入新子桶,既有 51 个**不迁动**;(二)**该桶下裂子桶是既有惯例而非新发明**,实测 `D5/S3/Arith/` 已有 `AbsoluteValues`、`BlindPrimes`、`Coding`、`Congruence`、`CounterSequences`、`Descartes` 等子目录,故本更正与该桶既有形态一致,不引入新结构。域仍为 `Arith`(`Meta/domains.yaml` 中绑 `stratum: S3`),SL-011 无碍;子桶不改变域归属。**为何以勘注而非由实施席自选**:atom 明文规定落点,实现擅自偏离在本仓是**拒过四条 lane** 的判例;而落点本身不可用时,正确处置是**以卷内勘注更正规范**,不是要求实现服从一个撞门的地址 —— 此优先序已由评注 29.10 立(域注册表与容量规则是机器强制的真源,卷内散文与之冲突时改散文)。**其余不变**:4.102 的三条子句((i) `Odd (numDerangements n) ↔ Even n`;(ii) `n ≥ 2 → padicValNat 2 (numDerangements n) = padicValNat 2 (n − 1)`;(iii) `numDerangements n = b ^ k → k ∣ padicValNat 2 (n − 1)`)、产地 `FromRepo`、以及 Describe 引 Sun 2025(OEIS A000166)与 Miska 2016 为语境,均照原文执行。**orchestrator 的独立复算(亲跑,非转述)**:以 Mathlib 递推形 `D_{n+2} = (n+1)(D_n + D_{n+1})`(即 `D_m = (m−1)(D_{m−2} + D_{m−1})`)迭代至 n = 21,验得 (i) 对 n < 20 全部成立;(ii) 对 2 ≤ n ≤ 21 **零反例**(样本 n = 2…11 的 `(v₂(D_n), v₂(n−1))` 依次为 (0,0)(1,1)(0,0)(2,2)(0,0)(1,1)(0,0)(3,3)(0,0)(1,1))。并记下证明骨架:由 (i),**相邻两个错排数奇偶相反,故 `D_{m−2} + D_{m−1}` 必为奇数**,其 2-adic 赋值为 0;代入上述递推即得 `v₂(D_m) = v₂(m−1)`,(iii) 由 (ii) 直接得(`k·v₂(b) = v₂(n−1)`)故判 bind-only。**仓内去重(亲跑)**:`numDerangements`、`A000166`、`DerangementTwoAdic` 在 `origin/dev` 的 `D5/**/*.lean` 上各 **0 命中**。**产地(第 9′ 条)**:落点不可用由 orchestrator 在派席**之前**量出(第 4.8 条 与 SL-003 上限),非事后由机器判红发现;本勘注、复算与去重均由 orchestrator(claude 主循环)亲跑亲写;实施与评审席另派。
