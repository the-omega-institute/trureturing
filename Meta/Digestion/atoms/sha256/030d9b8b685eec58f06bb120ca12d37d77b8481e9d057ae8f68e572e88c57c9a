**定义 142.0（窗口输出与初始化运输）。** 沿用第139节的双侧关系空间 $\mathcal Y_k$、平稳 Parry 律 $\mu_k$，其中 $k\ge2$，关系路径禁止 $1^k$。关系位 $r_t$ 连接顶点 $t,t+1$，令
$$
c_t=1-r_t\in\mathbb F_2,
\qquad
D_t(a,r)=a_{t+1}\oplus a_t\oplus c_t.
\tag{142.1}
$$
固定窗口输出指存在同一个确定函数 $f:\mathcal L_{k,R}\to\mathbb F_2$，使
$$
a_t=f(r_{t-R},\ldots,r_{t-1})
\qquad(t\in\mathbb Z).
\tag{142.2}
$$
初始化运输则指定实际起点 $t_0\in\mathbb Z$ 和初值 $b\in\mathbb F_2$，此后逐次取得实际关系位并更新持久状态。前者的输出只依赖最近 $R$ 位；实现可以缓存内部状态，但输出仍须等于（142.2）。后者允许当前状态保存从初始化以来取得的信息，不要求它能由最近 $R$ 位重新构造。

