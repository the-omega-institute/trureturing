## 定理 9.1：指数盒子的精确链归约

令：

$$
M=\sum_p(u_p-l_p).
$$

从 \(n_0\) 开始，按上述顺序逐层加入，得到：

$$
n_0,n_1,\ldots,n_M.
$$

则：

$$
\boxed{
\min_{\substack{l_p\le a_p\le u_p}}
\Delta\left(\prod_pp^{a_p}\right)
=
\min_{0\le j\le M}\Delta(n_j).
}
\tag{23}
$$

### 证明

先暂时把全部指数层看成可独立选择的二值项目，忽略“必须先加前一层”的限制。

扩展后的目标仍具有定理 8.1 的形式，所以最小值在边际收益排序前缀上取得。

对同一个素数，\(\eta(p,k)\) 随 \(k\) 严格递减。因此，这种全局排序一定先加入该素数的较低层，再加入较高层。

也就是说，放宽问题的最优前缀自动满足原来的指数前缀约束。

所以放宽后的最小值可以在原盒子内取得，两个最小值相同。证毕。

这把候选数量从：

$$
\prod_p(u_p-l_p+1)
$$

降到至多：

$$
1+\sum_p(u_p-l_p).
$$

但这是**候选数量和精确比较层面的结论**。边际值涉及对数，程序仍需用严格区间处理比较与可能的并列；不能把普通浮点排序直接当成形式证明。

