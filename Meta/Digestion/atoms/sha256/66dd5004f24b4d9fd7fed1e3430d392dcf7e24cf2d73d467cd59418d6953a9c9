## 定理3.1：零交换差与端点时钟

在允许任意素数乘法的模型中，以下两项等价：

$$
\mathcal C_{p,q}(n)=0
\quad\forall n,p,q;
$$

存在函数 \(\tau\)，使：

$$
\boxed{
\tau(pn)-\tau(n)=c_p(n),
}
\tag{9}
$$

并且每条路径的总成本只由端点决定。

### 证明

若存在 \(\tau\)，两条路径的成本都等于：

$$
\tau(pqn)-\tau(n),
$$

所以交换差为零。

反过来，任意两条到达同一个整数的素因子序列，都可通过相邻交换互相转换。每次相邻交换都不改变成本，因此总成本与次序无关。把从1到 \(n\) 的总成本定义为 \(\tau(n)\)，就得到式（9）。证毕。

两个例子是：

$$
c_p(n)=1
\Longrightarrow
\tau(n)=\Omega(n);
$$

$$
c_p(n)=\ln p
\Longrightarrow
\tau(n)=\ln n.
$$

但第二个是**带权成本钟**，不是单位步骤钟。

如果一般程序存在一条正耗时回路：

$$
x\longrightarrow\cdots\longrightarrow x,
$$

那么任何只依赖状态的函数 \(\tau(x)\)，都不可能把这条回路的正时长恢复出来。否则：

$$
0=\tau(x)-\tau(x)
=\text{正的回路时长},
$$

矛盾。

