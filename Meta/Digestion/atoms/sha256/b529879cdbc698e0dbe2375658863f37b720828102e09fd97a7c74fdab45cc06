# 七、5040 的四方向例子：高阶奇偶项确实存在，但可以被包住

取：

$$
n=5040,\qquad
p_1,p_2,p_3,p_4=2,3,5,7.
$$

一共考察：

$$
5040\prod_{p\in S}p,
\qquad
S\subseteq\{2,3,5,7\}.
$$

这是 16 个整数，不是此前的 60 个因数状态，也不是全部四素数平滑数。

一阶增量为：

| 新增方向  |             \(g_i\) |
| ----- | ------------------: |
