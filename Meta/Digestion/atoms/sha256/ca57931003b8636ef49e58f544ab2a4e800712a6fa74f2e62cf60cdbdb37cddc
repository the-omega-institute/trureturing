**定理 50.5（一次前缀成功蕴含以后每一步准确；repo-derived）。** 对每个固定正有理 $\varepsilon$ 与有理 $0<\delta<1$，定义50.4给一台只依赖已知候选族和这两个容差、而不依赖真实 $p$ 的有限确定性预测者。令 $y_n$ 为读完 $n$ 份报告后的读出，则对每个 $p\in\mathcal P$，
$$
\mathbb P_p\left(
\forall n\in\mathbb Z,\ n\ge2M:\quad
\left|y_n-\mathbb P_p(b_{n+1}=0\mid b_{1:n})\right|\le\varepsilon
\right)\ge1-\delta.
$$
特别地，首次承诺读出在消耗报告 $2M$ 后预测报告 $2M+1$。

证明。令 $G_p=\{\widehat p=p\}$，它只由长度 $2M$ 的训练前缀决定。对每个属于 $G_p$ 的前缀，真实候选表从公平先验标签起、已经逐一处理全部 $2M$ 份报告。因此选择时留下的标签正是已知真实模型的ML §41轨迹在该完整前缀后的标签。任意继续报告时，两者从相同标签应用相同确定性转移，逐词归纳仍相等；标签没有重置，训练变量的删除也不改变标签。

ML定理41.4的全词区间不变量和命题41.5的证书充分性遂给出，对每个这样的前缀、每条续接及其每个有限长度，所选读出与真实模型 $t_p$ 的差不超过 $\varepsilon$。所以 $G_p$ 逐路径包含于定理中的全部时刻成功事件，且
$$
\mathbb P_p(\text{全部 }n\ge2M\text{ 成功})
\ge\mathbb P_p(G_p)\ge1-\delta.
$$
这是同一个前缀可测事件上的逐路径蕴含，无须时间并集界，也没有要求同一词同时属于互不相容的 $G_A,G_B$。对 $n\ge2M$ 的完整词 $w$，只要其前缀属于 $G_p$，事件 $\{b_{1:n}=w\}$ 已蕴含 $G_p$，因此
$$
\mathbb P_p(b_{n+1}=0\mid b_{1:n}=w,G_p)
=\mathbb P_p(b_{n+1}=0\mid b_{1:n}=w).
$$
不在 $G_p$ 中的词不作这个额外条件化。正确分类是预测成功的充分条件，不是必要条件；机器没有从自身选择获得“模型为真”的可观察证书。证毕。

