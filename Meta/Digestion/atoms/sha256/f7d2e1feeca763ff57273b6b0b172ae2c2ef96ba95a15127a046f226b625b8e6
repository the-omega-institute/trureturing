## 重根也不会逃过检验

若 \(a\) 是 \(m\) 重非实零点，则：

$$
F^{(j)}(a)=0\quad(j<m),
\qquad F^{(m)}(a)\ne0.
$$

把第一个读出改成：

$$
\left.\frac{d^{m-1}}{dz^{m-1}}\right|_{z=a},
$$

第二个仍为在 \(0\) 处取值。

相应的自配对仍为零，而交叉项为：

$$
\boxed{
-\frac{F^{(m)}(a)}a\ne0.
}
\tag{P26}
$$

因此仍然有一个严格负的二阶行列式。

这里的“两个读出”包含一个可能的高阶导数读出，并不意味着两次低精度实验就能找出任意离线根。

