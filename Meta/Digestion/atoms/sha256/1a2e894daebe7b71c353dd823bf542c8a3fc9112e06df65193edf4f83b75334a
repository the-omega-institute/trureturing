## 6.1 临界信息量与复零点分别读出不同内容

设：

$$
r=|\mathcal T(\lambda_*)|\le2.
$$

在 \(z=0\)：

$$
\boxed{
F_\beta(0)\longrightarrow2^r.
}
$$

因此：

$$
\frac1\beta\ln\mathcal A_\beta(\lambda_*)
=
P_*+\frac{r\ln2}{\beta}+o(\beta^{-1}).
$$

这项有限修正记录最优态的数目。

而复零点记录每个临界方向的能量差：

$$
\boxed{
z_{p,m}
=
\frac{(2m+1)\pi i}{\ln p},
\qquad m\in\mathbb Z.
}
\tag{17}
$$

即使两个素数在真实价格上同时临界，它们的两列复零点也不会恰好重合。

否则：

$$
\frac{\ln p}{\ln q}
=
\frac{2m+1}{2\ell+1}\in\mathbb Q,
$$

这与不同素数的乘法独立性矛盾。

所以：

$$
\boxed{
\text{实参数上的双重临界，}
\quad
\text{仍会在复参数中分解为两列不同的简单零点。}
}
\tag{18}
$$

利用紧集上的一致收敛，可以通过 Rouché 定理，将这些简单零点转移到充分大有限 \(\beta\) 的完整模型。仓库 `RoucheZeroCount` 已有严格边界扰动保持零点计数的接口，但实际使用时仍要供给解析性和边界误差。

这条结论是辅助算术配分函数的性质，不能直接把这些零点称为 ζ 的非平凡零点。

