**命题 141.2（反极加权移位图）。** 在公平绝对提升的平稳律 $\nu_k$ 下，每个长度 $q+1$ 的绝对词满足
$$
\nu_k[x_0\cdots x_q]=\tfrac12\mu_k[\rho(x)].
\tag{141.4}
$$
以 $V_{k,n}$ 为顶点，每个合法长度 $n+1$ 的绝对词 $y$ 给从其前 $n$ 位到后 $n$ 位的有向边，边权为
$$
w_k(y)=\tfrac12\mu_k[\rho(y)].
$$
这些权重之和为一，且
$$
d_k(R)=
\min_{\substack{\mathcal A\subseteq V_{k,n}\\
\mathcal A\cap C\mathcal A=\varnothing,\ \mathcal A\cup C\mathcal A=V_{k,n}}}
\sum_{y\in V_{k,n+1}}w_k(y)
\mathbf1_{\{\mathbf1_{\mathcal A}(y_0,\ldots,y_{n-1})
\ne\mathbf1_{\mathcal A}(y_1,\ldots,y_n)\}}.
\tag{141.5}
$$
割质量计入两个方向；平稳性使它等于从 $\mathcal A$ 指向其补集的质量的两倍。

**证明。** 条件于完整关系路径，绝对起点公平，故任一指定关系词的两种绝对提升各占其概率的一半，得到（141.4）。每个关系词恰有两个绝对提升，求和即得边权归一化。取 $\mathcal A=g^{-1}(1)$，（141.1）等价于每个互补词对恰选一个；（141.3）又将运输缺陷精确变成相邻词标签不同，所以由定理141.1取最小值得（141.5）。

前后两个长度 $n$ 块具有相同平稳分布，故进入 $\mathcal A$ 的质量等于离开它的质量，给最后断言。这只使用真实相邻块的联合律，不要求短块过程本身为 Markov 链。证毕。

边权亦有显式矩阵形式。沿用第139节的 $\lambda,h,S$，令 $u_j=\lambda^{-j}/S$。令 $M_0(j,0)=1$，令 $M_1(j,j+1)=1$ 对 $j<k-1$ 成立，其余矩阵元均为零。则 $u^{\mathsf T}h=1$，且
$$
\mu_k[a_0\cdots a_{q-1}]
=\lambda^{-q}u^{\mathsf T}M_{a_0}\cdots M_{a_{q-1}}h.
\tag{141.6}
$$
证明是在每条状态路径上，将初始质量 $u_jh_j$ 与 Parry 转移乘积相乘；中间 $h$ 因子相消后，再对路径求和。代入（141.4）即得实际 Parry 权重，不能将其替换为均匀词权。

反极性在补变换不变的来源下蕴含 $\nu_k(\mathcal A)=1/2$，但平衡本身不够。例如在独立公平绝对位下，$g(x)=x_0\oplus x_1$ 平衡，却满足 $g(Cx)=g(x)$。因此（141.5）的反极限制不能仅由普通等质量二分条件替代。

