### 定理 V5(Open Problem 7.1 在 `n = 2`:把论文的下界升级为等式)

**原文**:`r_n = sup_{P∈U_n} ( min{ |z| ∈ ℂ : P(z)=0 } )`;`r_1 = a`;
「By Example 6.2, `r_2 ≥ (a+√(a(a+1)))/2 > a`」;`r_{2m+1} = a`;并猜想 `{r_n}` 有上界。
**论文只给了 `r_2` 的下界。**

**待证**:对一切 `a > 0`,

```
r_2 = ( a + √(a(a+1)) ) / 2
```

**拟议逃逸见证**(第 5⁴ 条,写在探针之前):对 `u,v ∈ [0,1]`,`p_{u,v}` 的**最小根模**的上确界估计
——即「对一切 `u,v ∈ [0,1]`,`p_{u,v}` 至少有一个根的模 `≤ M`,其中 `M = (a+√(a(a+1)))/2`」这一条
**新的不等式**,以及取等点 `u = v = M/s`(`s = √(a(a+1))`,此时 `p = (x+M)²`)。
该不等式不由本卷任何已冻结定理经实例化、投影或规范化得到。

**与本卷定理 V3 的连结**:V3 已证 `c_2(a) = (√(a²+a) − a)/2`,而 `a + c_2(a) = (a+√(a²+a))/2`
**正是**此处的 `M`;且 `√(a(a+1)) = √(a²+a)`。论文给那条下界所引的 Example 6.2,正是 V3 一族所核对过的例子。

**数值预登记**(`u,v` 各 401 点网格,`a ∈ {0.05, 1/24, 0.25, 1, 2, 7.3}`):最小根模的上确界与上式
在网格分辨率内吻合;`a = 1/24` 处网格恰命中,得 `0.125000000`,与闭式值相等。

