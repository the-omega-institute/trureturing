**定理 446.8（加法语言与完整档案的独立障碍）。** 在全部正整数上，允许任意次数无守卫 $+1$ 时，全未来 Möbius 观察分离所有正整数。在 $\operatorname{Div}(5040)$ 内只许有界 $+1$，则不分离所有状态；若在任一固定约数盒内允许全部正加数且每次结果仍须整除容量角，则一步严格响应已经分离全部状态。$G_{\rm obs}$ 一般不是乘除的充分状态，整数或 $\mu$ 观察也不替代原完整 CSA 守卫。

