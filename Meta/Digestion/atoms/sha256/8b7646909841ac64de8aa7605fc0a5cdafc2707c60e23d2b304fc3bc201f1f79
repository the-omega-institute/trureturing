* 编码更短，不自动使算术余量增大。

它们仍然有用，但应当通过一个额外定理连接到目标：

$$
\boxed{
\text{观察残差信息的界}
\Longrightarrow
\text{纤维内最坏算术误差的界}.
}
\tag{19}
$$

此前我们得到的：

$$
\ln Z(n_T)
=
H(D\mid D_J)-\mathbb E\ln D_T
$$

就是这样的接口之一。它说明要同时计入隐藏熵和隐藏规模，而不能只优化其中一个。

这也提示“证明压缩”比“数字压缩”更值得关注：

> **好的抽象状态，不一定把整数编码得最短；它应当使该纤维内所有整数共享一个足够尖锐的安全证书。**

---

