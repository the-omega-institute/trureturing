# 四、四棱锥的图结构，竟然真的包含 Fibonacci 递推

现在研究另一条联系：四棱锥的骨架图。

取四个底面顶点组成环：

$$
v_0-v_1-v_2-v_3-v_0,
$$

再加入一个与四角全部相连的顶点 \(a\)。

这就是图论中的：

$$
K_1\vee C_4,
$$

即一个顶点与四边环的连接图。轮图及这类锥图的谱是标准图论研究对象。([arXiv][3])

下面假定所有八条边的权重都等于 1。这个假设决定了具体传播模型，不能仅由几何外形推导出来。

记邻接矩阵为 \(A\)。定义归一化的底面平均模式：

$$
|b\rangle
=
\frac12
\left(
|v_0\rangle+|v_1\rangle+|v_2\rangle+|v_3\rangle
\right).
$$

顶点模式为 \(|a\rangle\)。

直接计算：

$$
A|b\rangle=2|b\rangle+2|a\rangle,
$$

$$
A|a\rangle=2|b\rangle.
$$

所以，在这个二维对称子空间中：

$$
\boxed{
A_{\mathrm{sym}}
=
\begin{pmatrix}
2&2\\
2&0
\end{pmatrix}
=
2
\begin{pmatrix}
1&1\\
1&0
\end{pmatrix}.
}
\tag{8}
$$

右边正是 Fibonacci 递推矩阵的两倍。

若把更新除以 2，写成：

$$
\binom{u_{t+1}}{v_{t+1}}
=
\begin{pmatrix}
1&1\\
1&0
\end{pmatrix}
\binom{u_t}{v_t},
$$

便有：

$$
u_{t+2}=u_{t+1}+u_t.
$$

其特征值为：

$$
\varphi,\qquad-\varphi^{-1}.
$$

因此：

$$
\boxed{
\operatorname{spec}(A_{\mathrm{sym}})
=
\{2\varphi,-2\varphi^{-1}\}.
}
\tag{9}
$$

**这一次，黄金比例不是由观察图形“像不像”得到的，而是由一个明确的二维递推方程得到的。**

