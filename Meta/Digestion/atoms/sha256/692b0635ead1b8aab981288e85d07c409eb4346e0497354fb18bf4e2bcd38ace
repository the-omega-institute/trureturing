### 证明

由 \(M_G\asymp G\log G\)，

$$
(1-r)G\to0.
$$

所以式（32）的每个分母都一致趋于 \(1\)：

$$
r+(1-r)^2|\rho|^2=1+o(1).
$$

同时

$$
C_G\to c_0,
\qquad
1-r^2\sim2(1-r).
$$

代入式（32）即得。证毕。

这给出一个明确的临界位置：

$$
\boxed{
\kappa=\frac{c_0}{2}
\Longrightarrow
\mathcal S_G(r)\to0.
}
\tag{38}
$$

其两侧响应符号不同。

由于

$$
\kappa\asymp\frac{M_G}{T_{\mathrm{obs}}},
$$

这条过渡曲线比较的是：

$$
\boxed{
\text{保留了多少谱信息}
\quad\text{与}\quad
\text{要求观察多长的时间}.
}
$$

其中 \(M_G\) 按零点重数计数；它不应与首次返回定理中“不同谱点数”无条件混同。

