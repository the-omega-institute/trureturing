## 10.1 零点几何也随之改变

对实数 \(s\)，\(C_a(z,s)\) 的全部零点自动是：

$$
\boxed{
z=-p_i^s,
}
$$

重数为 \(a_i\)。

对5040、\(s=1\)，就是：

$$
-2\text{ 四重},\quad
-3\text{ 二重},\quad
-5,\quad-7.
$$

而 \(Q_a(z,s)\) 的局部零点是：

$$
z=p_i^s e^{2\pi ij/(a_i+1)},
\qquad j=1,\ldots,a_i,
$$

通常不全为实数。

**这给出一条必须坚持的校验原则：**

$$
\boxed{
\text{若换一种历史权重以后，多项式突然全实根，}
\quad
\text{首先要检查是否已经换了待研究的函数。}
}
$$

不能用式（27）自动的实根性，去证明原始 ζ、Jensen 多项式或 Robin 不等式的性质。

这两种生成函数都很有用，但它们代表不同的观察与计数规则。

