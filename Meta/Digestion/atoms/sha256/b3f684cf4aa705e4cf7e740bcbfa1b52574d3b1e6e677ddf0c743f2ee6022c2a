# 一、“四边形加一个高度”与“压缩到平面”是相反的操作

取底面：

$$
B=[-1,1]^2\times\{0\},
$$

顶点：

$$
A=(0,0,1).
$$

整个四棱锥可以表示为：

$$
\boxed{
\mathcal P
=
\left\{
((1-h)x,(1-h)y,h):
-1\le x,y\le1,\ 0\le h\le1
\right\}.
}
\tag{1}
$$

这里 \(h\) 是高度参数。

当 \(h<1\) 时，截面仍然是一个正方形，只是缩小了：

$$
|X|,|Y|\le1-h.
$$

已知 \(h\) 时，可以恢复原来的底面坐标：

$$
x=\frac X{1-h},
\qquad
y=\frac Y{1-h}.
$$

但在顶点 \(h=1\)：

$$
((1-h)x,(1-h)y,h)=(0,0,1),
$$

所有底面位置都被映到同一点。

因此：

$$
\boxed{
\text{向顶点收缩}
=
\text{越来越多底面状态被合并，最终全部合并。}
}
$$

如果再把三维点投影到平面：

$$
\pi(X,Y,h)=(X,Y),
$$

高度信息也会丢失。例如，整条中轴：

$$
(0,0,h),\qquad0\le h\le1
$$

都投影到同一个平面点。

