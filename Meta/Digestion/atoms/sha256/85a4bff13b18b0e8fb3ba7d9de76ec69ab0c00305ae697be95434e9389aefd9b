## 5.1 乘法格点上的路径差严格为零

令：

$$
T_p(n)=pn.
$$

那么：

$$
T_pT_q(n)=T_qT_p(n).
$$

对任何标量函数 \(V(n)\)，定义沿边的变化：

$$
\alpha_p(n)=V(pn)-V(n).
$$

则：

$$
\boxed{
\alpha_p(n)+\alpha_q(pn)
-\alpha_q(n)-\alpha_p(qn)=0.
}
\tag{8}
$$

因为两条路径都从 \(n\) 到 \(pqn\)，总变化只取决于端点。

在这种明确的“路径差”意义下，算术乘法网络是平坦的。

但另一个量：

$$
V(pqn)-V(pn)-V(qn)+V(n)
$$

可以不为零。它是混合二阶差分，不是式（8）的闭路路径差。

例如，Robin 余量中的：

$$
h(E)=\ln\ln E
$$

满足：

$$
h''(E)<0.
$$

所以不同素数方向的联合预算修正为负。

**负的混合二阶差分，不能不加说明地被称作非零时空曲率。它说明目标函数不是可分离的线性预算，而不说明算术操作本身不交换。**

