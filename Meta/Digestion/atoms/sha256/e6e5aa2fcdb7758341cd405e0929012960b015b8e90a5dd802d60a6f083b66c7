### 定理 V6(Open Problem 7.2 在 `n = 2`:证明论文自己的猜想)

**原文**:`R_n = sup_{P∈U_n} ( max{ |z| ∈ ℂ : P(z)=0 } )`;`R_1 = a`;
「Example 6.6 shows `R_n ≥ a+n−1`」;**「Our conjecture is that `R_n = a+n−1`.」**

**待证**:对一切 `a > 0`,`R_2 = a + 1`。

**拟议逃逸见证**:「对一切 `u,v ∈ [0,1]`,`p_{u,v}` 的每个根的模 `≤ a+1`」这一条**新的上界**,
连同在 `u = v = 1` 处 `p = (x+a)(x+a+1)` 取等。论文另记 `P ∈ U_n` 的**实**零点落在
`[−(a+n−1), n−1]`;`n=2` 时把它加强为**全体复零点**的模界,并证明该界可达,即本条的内容。

**数值预登记**(同一网格):最大根模的上确界**八位小数恰为 `a+1`**,取等点为 `u = v = 1`。

