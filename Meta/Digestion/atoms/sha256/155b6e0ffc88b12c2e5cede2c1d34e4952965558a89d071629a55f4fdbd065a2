### V5 / V6 —— `D5/S3/Zeros/PochhammerDeformation/QuadraticRadii`(PR #5943)

```lean
theorem quadratic_outer_radius (a : ℝ) (ha : 0 < a) : R2 a = a + 1
theorem quadratic_inner_radius (a : ℝ) (ha : 0 < a) :
    r2 a = (a + Real.sqrt (a * (a + 1))) / 2
```

`R2` / `r2` 定义为 `rootNorms` 在 `U2 a` 上取的上确界之上确界 / 下确界之上确界,
即论文 (37) / (36) 的形式化。见证:`R2` 取 `u=v=1`,`(x+a)(x+a+1)`;
`r2` 取 `u=v=M/s`,`(x+M)²`,其中 `s=√(a(a+1))`、`M=(a+s)/2`,并经已冻结的
`quadratic_interval_closed_form` 证得 `M = a + c₂(a)`。

**一处诚实的追加(第 5⁴ 条:观测到的见证与拟议不同须如实记,不得事后改标)**:
上文预登记只给了正规形 `p_{u,v}` 这个**记号**,并未预登记「`U2` 成员**当且仅当**是该形式」这一步。
实施席另证了 `quadratic_normal_form` 作为具名引理,正因如此,两个上确界才是对**整个 `U2` 类**取的,
而非对参数族取的。**这条引理是本次实际使用的额外逃逸见证**,在此补记;
它不改变 V5 / V6 的陈述,但它是使那两条陈述成立的必要一步。

