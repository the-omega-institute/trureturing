**定理 437.8。** 此全域问题的最小值取到，且每个最优路径只使用 $H=\{c_0,a,b\}$。其值由
$$
W_0(c_0)=0,\quad W_0(a)=W_0(b)=+\infty,\qquad W_t(i)=c_t(i)+\min_{j\in H}\{W_{t-1}(j)+\kappa\mathbf1_{i\ne j}\}
$$
给出，最终值为 $\min_{i\in H}W_T(i)$。每条最优路径离开 $c_0$ 后不再返回。
