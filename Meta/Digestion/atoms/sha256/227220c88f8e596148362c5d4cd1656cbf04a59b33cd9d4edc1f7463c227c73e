## 定理 5.1：精确事件更新公式

$$
\boxed{
\begin{aligned}
Y_{i+1}
&=Y_i+d_iV_i-B_i,\\
V_{i+1}
&=V_i-A_i+\frac{\Lambda(m_{i+1})}{m_{i+1}}.
\end{aligned}
}
\tag{19}
$$

### 证明

事件之间 \(Y''=-\kappa\)。一次积分给出终点跳跃前的斜率，两次积分给出终点数值。

事件发生时，\(Y\) 连续，而导数增加：

$$
\Lambda(m_{i+1})/m_{i+1}.
$$

得到式（19）。证毕。

这条递推展示了两个互补过程：

$$
\boxed{
\text{事件之间：已知背景持续降低斜率；}
}
$$

$$
\boxed{
\text{素数幂事件：向斜率加入一个正脉冲。}
}
$$

脉冲并不瞬间提高 \(Y\)；它改变的是未来增长速度。因此，一个刚好出现在端点的素数幂，不能瞬间“修复”已经为负的端点值。

