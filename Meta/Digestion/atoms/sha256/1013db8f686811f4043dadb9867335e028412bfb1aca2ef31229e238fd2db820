**定理 5.3(参考系税恰等式)[证]。**以 N 级阶梯为守恒参考,激发交换酉协变实现比特翻转 X;参考态 |φ⟩ = Σc_n|n⟩。纠缠保真度化为 F_e(c) = ‖Jc‖²,J = Dirichlet 最近邻平均算子(J c)_m = (c_{m−1}+c_{m+1})/2(边界零)。故

$$1 - F_e^{\mathrm{opt}}(N) = \sin^2\!\frac{\pi}{N+1}\ \ (\text{恰等式}),\qquad 1 - F_e^{\mathrm{flat}}(N) = \frac{3}{2N}\ \ (\text{恰等式}),$$

最优参考态张成二维简并空间(箱基态正弦与其带缘孪生)。

*证明。*Kraus 元 K_m 由 U 之矩阵元线性读出,Tr(X†K_m)/2 = (c_{m−1}+c_{m+1})/2(内点;边界行仅单邻),F_e = Σ_m|Tr(X†K_m)/2|² = ‖Jc‖²。J 之谱为 cos(πk/(N+1)),k = 1..N(Dirichlet Jacobi,标准);J² 谱 cos²,对 k ↔ N+1−k 二重简并,顶值 cos²(π/(N+1)),即最优税 sin²(π/(N+1))。平坦态:内部行贡献 (N−2)/N,两端行各 1/(4N)·2,合 1 − 3/(2N)。∎(N = 8 处 sin²(π/9)·81 = 9.475 与实测逐位;边界为记账制。)**判读:跨守恒操作之最低价 = 资源箱零点色散;仪器判例双枚(单输入可被完美匹配吸收作弊至满分 ⟹ 计价必须信道级;守恒本征输入零敏 ⟹ 检验须打相干扇区)。**

