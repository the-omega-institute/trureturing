## 定理 1617.1（正 Fredholm 极限保持负实零点）

设 \(A_N\) 为有限秩正算子，并令：

$$
F_N(w)=\det(I+wA_N).
$$

若：

$$
F_N\longrightarrow F
$$

在复平面紧集上一致收敛，且：

$$
F(0)=1,
$$

则：

$$
\boxed{
\operatorname{Zero}(F)
\subseteq(-\infty,0].
}
\tag{1617.1}
$$

### 证明

每个 \(A_N\) 的特征值均为：

$$
\lambda_{N,j}\ge0,
$$

故：

$$
F_N(w)
=
\prod_j(1+w\lambda_{N,j}).
$$

其零点全部为：

$$
-\lambda_{N,j}^{-1}\le0.
$$

在任何与负实轴不相交的圆盘内，所有 \(F_N\) 均无零点。

由 Hurwitz 定理，局部一致极限 \(F\) 在该圆盘内要么无零点，要么恒等为零。

但：

$$
F(0)=1,
$$

排除了恒零情形。∎

因此：

$$
\boxed{
\text{正有限谱的局部一致极限，
不会生成非实平方节点。}
}
$$

---

