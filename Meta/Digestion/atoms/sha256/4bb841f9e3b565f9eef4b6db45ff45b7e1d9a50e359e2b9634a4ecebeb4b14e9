# 202. 观察紧致性定理

设：

* \(X\) 是紧致空间；
* 每个有限上下文记录空间 \(\mathcal B(c)\) 是 Hausdorff；
* 每个：

$$
\beta_c:X\to\mathcal B(c)
$$

连续；

* 上下文族对有限 join 封闭。

设给定一个兼容记录族：

$$
b=(b_c)_c.
$$

并假设每个有限上下文记录都可实现：

$$
b_c\in\operatorname{Im}\beta_c
\qquad
\forall c.
$$

