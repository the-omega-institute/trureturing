**候签定理 4.114(Levens–Lewis–Tenner §6.1 之否定回答:2-boolean 性质不持久)**〔open;落 `D5/S1/Permutations/TwoBooleanPersistenceRefutation`;拟议逃逸见证:B₂ 的最长元 w=(−1,−2)(单行记法 w(1)=−1、w(2)=−2)其约化字恰为 s₀s₁s₀s₁ 与 s₁s₀s₁s₀(长度 ≤ 3 的 15 个字与长度 4 的 16 个字由内核 decide 穷举),每个生成元恰出现两次,故 w 为 2-boolean;而 w 在 [±2] 上按 −2<−1<1<2 取值 2,1,−1,−2,与 4321 序同构,故 w 全局包含 4321 ∈ A_φ;于是 {w ∈ S₂^B : 2-boolean} ≠ GAV₂({3421,4312,4321,456123}),定义 4.1 的持久性不成立;须以定义 2.1/2.4 的具体带符号排列与生成元(s₀ 变号、s₁ 对换)实现 B₂ 与其约化字,不得把 B₂ 定义成所需的表〕。存在 n(n=2)与 w ∈ S_n^B,使 w 的每个约化分解中每个生成元至多出现两次,而 w 全局包含 4321;故「2-boolean」性质不持久。
