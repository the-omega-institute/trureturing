### 40a.3 固定损失中的四次可见信号

**命题40a.1。** 两个实际状态有相同 W，且 A_2=A_1+cI、B_2=B_1-cI。设

$$L=B_1W+WA_1,\qquad \mathcal C=WW^TL-LW^TW.$$

先以步长η更新，再以步长τ更新，且两步均使用固定零标签损失，则

$$\boxed{W_2^{(2)}-W_2^{(1)}=c\eta^3\tau\mathcal C.}\tag{40a.2}$$

**证明。** 第一步 W_1 相同，因为标量平移在 B W+W A 中相消。两个后继对角块之差为 c(I-η²W^TW) 及 -c(I-η²WW^T)。第二步的梯度就是共同 W_1，故由(40a.1)直接相减，得到

$$\Delta W_2=c\eta^2\tau(W_1W^TW-WW^TW_1).$$

代入 W_1=W-ηL+η²WW^TW，零次与二次项分别抵消，括号等于η𝒞。证毕。

等步长 h 时信号为 c h^4𝒞。这里的四次是离散算法对步长的依赖，不是连续输出轨道的第四阶时间导数。该公式包含𝒞=0、c=0和所有退化参数。

作为对照，J=diag(I,-I) 满足 H(W)J+JH(W)=0。连续 Gram 方程 Γdot=-H(W)Γ-ΓH(W) 对 Γ→Γ+cJ 不变，且该平移不改变 W。实际梯度流由多项式向量场给出局部唯一解；共同存在区间上平移关系保持，完整连续输出相同。因此不能把离散可识别性反推成连续流的可识别性。

**条件风险。** 若 W,A_1,B_1 已知，只有 c 未知且真实可行 c 域恰为长度ℓ的闭区间，第二步输出具有 Frobenius 噪声预算ε，则当𝒞≠0时，估计 c 的精确最坏风险为

$$\min\left\{\ell/2,\frac{\epsilon}{|\eta|^3|\tau|\|\mathcal C\|_F}\right\}.$$

证明：把去基线后的输出投影到𝒞，得到标量有界噪声，取相容区间中点达到上界；用两个合法 c 的输出中点作为共同数据达到下界。𝒞=0时风险为ℓ/2。该结论不覆盖其他 Gram 坐标也未知的端到端估计。等步长的逆放大为 h^-4，步长越小并不自动使识别更稳。

