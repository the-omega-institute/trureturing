## 定理1.1：有限处理器下的精确执行时间

假设每个事件耗时一轮，每个处理器一轮执行一个事件，同一条链不能在一轮内执行两次，允许任务在处理器之间迁移。

使用 \(P\) 个处理器，最少轮数是：

$$
\boxed{
D_P(a)=
\max\left\{
a_{\max},
\left\lceil\frac LP\right\rceil
\right\}.
}
\tag{1}
$$

### 证明

任何执行都至少需要 \(a_{\max}\) 轮，因为最长链上的事件必须串行。

也至少需要 \(\lceil L/P\rceil\) 轮，因为每轮最多完成 \(P\) 个事件。

反过来，令右边为 \(T\)。把每条链的标签连续列出，形成长度 \(L\) 的列表，再循环分配到 \(T\) 个轮次中。

每条链长度不超过 \(T\)，所以它不会在同一轮出现两次；总长度不超过 \(PT\)，所以每轮不超过 \(P\) 个事件。最后按各链所分配轮次的先后，安排该链的第一个、第二个事件，即得到合法执行。证毕。

对5040：

$$
a=(4,2,1,1),\qquad L=8,\qquad a_{\max}=4.
$$

于是：

$$
\boxed{
D_1=8,\qquad D_2=D_3=D_4=4.
}
$$

只用两个处理器就能达到最快的四轮：

| 轮次 | 处理器一    | 处理器二    |
| -- | ------- | ------- |
