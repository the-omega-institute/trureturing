**候签定理 4.137(Hanna:q-Catalan 三角平方和的奇偶律,A376527)**〔open;**档位:第一档**(第 5⁵ 条)——OEIS 评注,2024-10-11,页面仍标 Conjecture;**文献核对(2026-09-10,orchestrator 亲跑)**:取 `https://oeis.org/search?q=id:A376527&fmt=json` 与 `id:A227543` 的条目原文;A376527 的猜想以下述逐字措辞列于 COMMENTS,**条目内未给出证明、亦无指向证明的引用**;**三类排除逐条确认**——①**证明不在条目自身**(该页只给出渐近式 `a(n) ~ c·16^n/n^(9/2)`,与奇偶无关);②**不依赖未经证明的 OEIS FORMULA**:所需的「行和 = Catalan」在 A227543 条目中写作「Row sums are the Catalan numbers (A000108), **set q=1 in the g.f. to see this**」,即由母函数代入 `q=1` 直接得到,不是待证猜想;③非 `conjecture is true (see … link)` 型;**本仓去重由 orchestrator 亲跑**:`A376527` 在 `origin/dev` 的 `D5/**`、`Blueprint/**`、`Library/**`、`docs/develop/theory/**` 上**零命中**(`A227543` 有 1 处提及);**数值自检(orchestrator 亲跑)**:OEIS 所列前 15 项中奇数下标为 `[0,1,3,7]`,恰为 `2^k − 1`;落 `D5/S1/Recurrence/Parity/QCatalanSquareSumParity`(**随 A222013 / A222014 同桶**:域 `Recurrence` 绑 S1,且本条须 import 同层 `D5/S1/Recurrence/Invariants/CatalanCompositionSquareParity` 的已冻结果;**落点已核对 `Meta/domains.yaml`,不重蹈评注 29.10 所记的非法地址**);**拟议逃逸见证(写在探针之前,不事后追认)**:**行多项式递推的 q=1 特化** —— 由 A227543 的定义方程 `A(x,q) = 1 + x·A(qx,q)·A(x,q)` 写 `A = Σ_n R_n(q)·x^n`,比较 `x^{n+1}` 的系数得 `R_{n+1}(q) = Σ_{i+j=n} q^i·R_i(q)·R_j(q)`,`R_0 = 1`;**代 `q = 1` 后该递推塌成 Catalan 卷积递推** `R_{n+1}(1) = Σ_{i+j=n} R_i(1)·R_j(1)`,故由已冻的 `CatalanCompositionSquareParity.catalan_equation` 与 `catalan_unique`(零常数项解唯一)得 `R_n(1) = C_n`。**该特化是本条的内容**:它把一个双变量的 q-形变递推塌成单变量卷积,从而把「三角的行和」与仓内已冻的 Catalan 级数对接;这一步不是任何已冻结论的实例化。**绑定即得的部分诚实分栏**:模 2 下 `u² ≡ u` 故 `a(n) = Σ_k T(n,k)² ≡ Σ_k T(n,k) = R_n(1) = C_n`(平方和与行和同奇偶),再由已冻的 `binary_catalan (n) : coeff n (catalanSeries.map (Int.castRingHom (ZMod 2))) = 1 ↔ ∃ k, n = 2^k` 读出结论(注意 `catalanSeries = X * catalanUnit` 带一位指标平移,须显式处理);此两步**直接引用、不重证**(第 3.1 条),作具名伴随并各记一条有向边〕。原文(A376527 评注,_Paul D. Hanna_,2024-10-11,逐字):"Conjecture: a(n) is odd iff n = 2^k - 1 for some k >= 0.";定义为 `a(n) = Sum_{k=0..n*(n-1)/2} A227543(n,k)^2`,其中 A227543 是由 `A(x,q) = 1 + x*A(q*x,q)*A(x,q)` 定义的 Carlitz–Riordan q-Catalan 三角。**产地(第 9′ 条)**:本条由 orchestrator(claude 主循环)选题——它曾在 R31 搜题席的候选中出现并被我以「需要一座源到代数的桥」为由否决,**该否决过粗**:读定义后可见所需的行和关系是母函数在 `q=1` 处的代入,且其唯一性论证仓内已冻,故本轮重估并立案;OEIS 原文取证、仓内去重、数值自检、落点域校验与上述证明骨架均由 orchestrator 亲跑亲写,非转述;实施与评审席另派。
