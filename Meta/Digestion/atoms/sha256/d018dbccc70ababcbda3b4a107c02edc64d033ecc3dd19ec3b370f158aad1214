### 136.2 已证工具的加入与可达关系

取有向图 $G=(V,E)$，顶点是指定问题状态，边是当前允许的一步操作，每条边具有非负费用。有限路径必须首尾匹配，包括费用为零的空路径；费用按边费用相加，记全部这种路径费用的下确界为 $d_G(x,y)\in[0,+\infty]$，无可达路径时取 $+\infty$。

增加一族辅助边 $e:x\to y$。每条辅助边带有原图中从 $x$ 到 $y$ 的一条有限路径 $p_e$，作为其语义正确性的展开证书。辅助边费用也非负，新图记为 $G^+$。

