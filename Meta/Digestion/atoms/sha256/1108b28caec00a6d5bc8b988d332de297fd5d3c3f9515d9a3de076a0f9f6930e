## 5.2 但这不等于“每次乘一个素数”的算术图

如果我们要求每次只把某个指数增加或减少 1，那么 \(p=2\) 的局部图应当是：

```text
0 —— 1 —— 2 —— 3 —— 4
```

这是 \(P_5\)，而不是 \(\Gamma_3\)。

5040 的因数整除覆盖图因此是：

$$
\boxed{
\mathfrak D_{5040}
=
P_5\square P_3\square P_2\square P_2.
}
\tag{5.2}
$$

两张图有同样的 60 个状态，但边不同。

我直接枚举并核对得到：

| 图                      | 顶点 |  边数 | 一条边表示什么             |
| ---------------------- | -: | --: | ------------------- |
