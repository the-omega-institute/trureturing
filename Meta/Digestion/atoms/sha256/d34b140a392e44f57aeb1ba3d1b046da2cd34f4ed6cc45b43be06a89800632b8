## 2.3 整除关系也不是“编码位包含关系”

从：

$$
2^4\mid2^5
$$

可知指数增大、整除条件保持。

但：

$$
4=1+3\leftrightarrow101,
$$

$$
5=5\leftrightarrow0001.
$$

原来的两个 \(1\) 消失了，换成更高位置的一个 \(1\)。

所以：

$$
\boxed{
5040\mid n
\not\Rightarrow
\text{\(n\) 的编码中逐位包含 5040 的编码}.
}
\tag{4}
$$

而且这次变化中：

$$
\text{数位中的 }1\text{ 的数量： }2\to1,
$$

$$
\text{算术能量： }4\ln2\to5\ln2.
$$

因此，“符号更少”和“算术能量更低”不是同一件事。

