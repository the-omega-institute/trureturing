**证明。** 若 $M(h)=M(h')$，给两者输入同一个共同合法新位，则由相同更新映射得到相同后继状态。按续接串长度归纳，两条路径的每个后继状态和输出都相同。这与某个后续目标响应不同相矛盾，证明可区分历史必须分属不同状态。对两两可区分族计数，得到状态数及二进制位数下界。

第140节即使只观察方向输出，也要求每一步与指定窗口函数 $F_R$ 完全一致；它的下界不依赖额外的锚查询。命题142.1则把目标定为延续已经初始化的参考，其状态与输出均为当前一位，更新为 $U_x(a)=a\oplus(1-x)$。它无须复现 $F_R$ 的候选过期、竞争者替换或重新选锚，因此没有义务保留区分这些窗口响应的历史信息。若另要求自判禁词合法性，须再满足对应的语法状态合同；一位运输结论没有承担该附加任务。故指定选择器的精确续接位数、固定窗口长度及任意持续运输的存储位数不是同一个最小化问题。证毕。

