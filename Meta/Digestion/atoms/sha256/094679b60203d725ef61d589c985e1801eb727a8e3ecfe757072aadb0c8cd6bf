### 124.2 完整分隔集与有限树上的共同世界

设 $T$ 是有限非空树，节点 $v$ 的变量集合为 $S_v$，各变量 $x$ 的值域为 $X_x$。局部合法关系为
$$
R_v\subseteq\prod_{x\in S_v}X_x.
$$
约定只给实际使用变量 $S=\bigcup_vS_v$ 赋值。对每条边 $uv$，完整分隔集为 $S_u\cap S_v$。运行交集条件是：对每个变量 $x$，含有 $x$ 的节点构成连通子树，或为空。

