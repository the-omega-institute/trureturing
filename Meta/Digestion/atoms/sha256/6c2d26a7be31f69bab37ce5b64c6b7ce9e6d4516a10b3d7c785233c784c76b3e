### 52.6 联合矩的实际取得与统计边界

在实际许可读取 $q$、读取不引入 $F$ 之外的额外状态改变、并许可执行 $F$ 的合同下，先读 $q_0$，执行一次后读 $q_1$，再执行一次后读 $q_2$。同一条轨迹的三读数逐点给出 $q_0q_1,q_1q_2$，而实际状态已推进到 $F^2s$。式（52.6）连续使用两次还给出当前工作态的逐点特征
$$
\phi(F^2s)=\phi(s)R_4^2=(1,q_2(s),q_1(s),q_2(s)).
$$
这是因为 $q_3=q_1$、$q_4=q_2$；再取这些当前位的实际乘积，也得到 $\psi(F^2s)$。所以保留三读数后可以计算已推进状态的特征，而不能将它称为未改变原状态的即时读取。一个三元组给出一个样本，不给未知初始混合律的精确期望。重复制备、复位、相同制备律和访问权限均须另行提供；两项联合矩必须来自同一次实际路径，分别取得的边缘不能补成这个样本。

