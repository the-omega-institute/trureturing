# 第一千四百八十七部　命名失败必有有限区分实验

设一个接口：

$$
r:H\to R
$$

不能充分解码 profile：

$$
\Gamma:H\to(I\to\operatorname{PMF}(O)).
$$

“不充分”等价于存在：

$$
h,h'\in H
$$

满足：

$$
r(h)=r(h'),
$$

但：

$$
\Gamma(h)\neq\Gamma(h').
$$

由函数外延性，存在某个有限干预：

$$
i\in I
$$

使：

$$
\Gamma(h)(i)\neq\Gamma(h')(i).
$$

若输出为离散 PMF，则进一步存在：

$$
o\in O
$$

满足：

$$
\boxed{
\Gamma(h)(i)(o)
\neq
\Gamma(h')(i)(o).
}
\tag{1487.1}
$$

所以：

