* 某个原先声称的全局 liminf 上界实际上为假。

这不是 RH 定理，但给当前 moment 路线一个严格警告：

$$
\boxed{
\text{先闭包再迭代}
\neq
\text{先迭代再逐层闭包}.
}
$$

对 RH 矩问题，这意味着不能混淆：

1. 每个有限矩阵 PSD；
2. 每层存在某个有限原子正模型；
3. 这些模型彼此兼容；
4. 无限层存在单一正测度；
5. 无限层是否具有统一严格 gap。

RH 可以对应：

$$
H_N\succ0
\quad
\forall N,
$$

但：

$$
\lambda_{\min}(H_N)\downarrow0.
$$

所以不能把“没有统一 gap”误认为“存在离线零点”，也不能先对近似条件闭包后再宣称得到全局正实现。

---

