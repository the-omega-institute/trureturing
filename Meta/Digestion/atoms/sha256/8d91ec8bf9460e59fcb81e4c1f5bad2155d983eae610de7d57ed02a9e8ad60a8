# 七、5040 的实际意义：它把程序放进了“6 除以 6”，而不是标准的下一轮

现在使用：

$$
5040=2^4\,3^2\,5\,7,
$$

即：

$$
\mathsf S(4,2,1,1;1).
$$

对一般无控制标记状态：

$$
\mathsf S(a,b,c,d;1),
$$

前处理得到：

$$
\boxed{
\mathsf S(a,b,c,d;1)
\longrightarrow
\mathsf X(a+c+1,\ a+b),
}
\tag{23}
$$

只要 \(a+b>0\)。

因此对 5040：

$$
M=4+1+1=6,
$$

$$
k=4+2=6.
$$

所以：

$$
\boxed{
5040\longrightarrow\mathsf X(6,6).
}
$$

它首先测试的是：

$$
6\bmod6=0.
$$

实际精确执行得到：

| 从 5040 起的步数 | 寄存器状态            | 含义                       |
| ----------: | ---------------- | ------------------------ |
