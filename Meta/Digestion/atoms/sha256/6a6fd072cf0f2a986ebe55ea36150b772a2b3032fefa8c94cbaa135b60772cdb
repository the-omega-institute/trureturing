**命题 124.20（完整边界求值的上下文替换）。** 设边界集合有限，或以下全部求和具有有限支撑。半环 $S$ 上两个组件具有相同边界求值 $V_1(b)=V_2(b)$。若上下文通过该完整边界接入，实际整体求值为
$$
W_i(z)=\sum_bV_i(b)C(b,z),
$$
则 $W_1=W_2$。非交换半环中乘法顺序必须保持；把该代数式认作真实实现求和，还须有保持权重和重数的实现分解。

