# 八、为什么有限零点计算与低信息逃逸仍然不够？

这可以给出一个定量答案。

截断响应：

$$
F_H(t)
=
\sum_{|\Im\rho|\le H}
\frac{e^{(\rho-\frac12)t}}{\rho(1-\rho)}.
$$

由一般零点计数估计：

$$
\sum_{|\Im\rho|>H}
\frac1{|\rho(1-\rho)|}
=
O\!\left(\frac{\ln H}{H}\right).
$$

因此，无条件地，对 \(|t|\le Y\)：

$$
\boxed{
|F(t)-F_H(t)|
\le
e^{Y/2}
O\!\left(\frac{\ln H}{H}\right).
}
\tag{30}
$$

这个界足以支持每个固定窗口中的严谨计算。

但它不是全实轴的一致界，因为：

$$
e^{Y/2}
$$

随观察范围增长。

换成算术尺度 \(T=e^Y\)，误差界变成：

$$
O\!\left(\sqrt T\,\frac{\ln H}{H}\right).
$$

所以，固定一个很大的零点高度 \(H\)，也不能仅凭这个界控制任意大的算术尺度。

这里有一条与观察者理论直接相关的原则：

$$
\boxed{
\text{每个有限窗口都能逐渐看清}
\quad\not\Rightarrow\quad
\text{同一个有限窗口足以控制全部尺度}.
}
$$

若先使用 RH，把：

$$
|e^{(\rho-\frac12)t}|=1,
$$

那么尾项就可以对全部 \(t\) 一致控制。但以此为证明 RH 的出发点会构成循环。

本轮读取的仓库 `ZeroSumEnumerationInvariance` 已证明：在给定穷尽枚举和收敛前提下，零点和与枚举次序的选择无关。它也明确注明，这个结果没有额外提供求和收敛定理。

在当前研究中，应该继续补足的是：

$$
\boxed{
\text{目标相关的尾项大小}
\quad\text{与}\quad
\text{观察范围增长之间的统一关系}.
}
$$

而不是只证明“换一种枚举不改变答案”。

---

