### 40a.9 物理、量子与任务边界

本节的“状态”是足以预测指定学习算法未来输出的Gram。原始参数的共同隐藏行正交变换不会改变Γ，因此此识别不恢复每个参数坐标。连续梯度流的同一投影纤维可以永久闭合，有限步算法则可能使它分裂；这是精确演化律的区别。

若将Γ归一化为量子密度矩阵，原合同步骤仍需成功滤波、失败标志、制备来源与整体尺度，才能保留上一轮的操作对应。当前固定损失梯度取决于未知W，实施该反馈需要测量与经典控制器，不能把本节无扰动的数学矩阵读取等同于未知单份量子态上的非扰动测量。本节不从此证明任何量子过程的diamond误差或物理实现。

这里用微积分与有限差分检验具体的状态对应；没有把所有状态机、ML或量子系统宣布为同一种物理系统。

