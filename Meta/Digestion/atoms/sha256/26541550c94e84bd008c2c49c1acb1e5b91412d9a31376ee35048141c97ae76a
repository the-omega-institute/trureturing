## 定理 264.2（最小回滚标签数）

定义最大过程纤维大小：

[
\boxed{
m_U
===

\max_{y\in Y}
|U^{-1}(y)|.
}
]

则精确回滚所需的最小日志字母表大小为：

[
\boxed{
|M|_{\min}=m_U.
}
]

### 下界

对固定 (y)，所有属于 (U^{-1}(y)) 的状态具有相同过程后状态。

若日志不能为它们分配不同标签，联合记录就不单射。

因此：

[
|M|
\ge
|U^{-1}(y)|
]

对全部 (y) 成立。

### 上界

在每个纤维 (U^{-1}(y)) 内分别编号：

[
1,\ldots,|U^{-1}(y)|.
]

全部使用统一字母表：

[
{1,\ldots,m_U}.
]

则 ((U,L)) 单射。 (\square)

---

