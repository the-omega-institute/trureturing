## 4.1 它也有精确的熵解释

固定 \(n\)，在其因数上取：

$$
\mu_n(d)=\frac1{dZ(n)}.
$$

对任意因数概率分布 \(\nu\)：

$$
H(\nu)-\mathbb E_\nu[\ln d]
=
\ln Z(n)-D_{\mathrm{KL}}(\nu\Vert\mu_n).
$$

因此：

$$
\boxed{
J_\lambda(n)
=
\max_\nu
\left[
H(\nu)-\mathbb E_\nu\ln d-\lambda\ln n
\right].
}
\tag{4}
$$

所以5040的最优性可以被解释为：

> **在因数状态丰富程度、平均状态规模与整个整数的规模之间，取得指定价格下的最佳平衡。**

这不是无条件的最大熵，也不是对任意通信任务的最大压缩率。

