**证明。** 新窗口在旧坐标中为 $[1-R,0]$。令 $A_+$ 表示新选择恰在位置零闭合，$A_-$ 表示旧选择的左端点恰为 $-R$，$B$ 表示新窗口没有完整候选。窗口移动只会移出左端点为 $-R$ 的候选，并加入右端点为零的候选。在 $A_+^c\cap A_-^c\cap B^c$ 上，旧选择仍被保留，新增候选未被选中，两窗按同一排序选择同一物理锚。新运输只比旧运输多 XOR 一条边 $c_0$。因此
$$
\{D_0=1\}\subseteq A_+\cup A_-\cup B.
\tag{139.14}
$$
旧空而新非空时，新选择必是新增候选；两窗都为空时的可能缺陷由 $B$ 保留，所以空分支没有遗漏。

设 $R\ge18$，$n=\lfloor(R-2)/16\rfloor$。新增事件只能在 $r_{-1}=0,r_0=1$ 上发生。条件于此边界，记刚闭合串长为 $Z_0$，此前的零串长为 $Z_1,\ldots,Z_{n-1}$，分隔一串长为 $W_1,\ldots,W_{n-1}$。由引理139.1，它们独立且服从（139.5）。置
$$
T_+=Z_0+\sum_{i=1}^{n-1}(W_i+Z_i).
$$
所考察最早零串的左端点恰为 $-T_+-1$。若 $T_+\le R-2$，全部 $n$ 条串及其两个端点都在新窗口内。新串获胜要求 $Z_0$ 不短于这些竞争者。因此由引理139.2，
$$
\Pr(A_+)\le\frac2n+e^{-(R-2)/16}.
\tag{139.15}
$$
计算是在只给边界、尚未条件化容纳成功的律中进行，再将边界事件概率上界取为一。

对过期事件，将旧窗口平移为 $[0,R-1]$。必要边界是 $r_0=1,r_1=0$。条件于此边界，正向周期 $(Z_i,W_i)$ 独立，$0\le i<n$。置
$$
T_-=\sum_{i=0}^{n-1}(Z_i+W_i).
$$
若 $T_-\le R-2$，全部 $n$ 条零串都完整地处于旧窗口：最后一条闭合一的位置至多
$$
\sum_{i=0}^{n-2}(Z_i+W_i)+Z_{n-1}+1\le T_-.
$$
第一条被选中要求它不短于后继竞争者。平手优先较近端点会排除部分过期平手，使用包含平手的较大事件仍为上界。因此
$$
\Pr(A_-)\le\frac2n+e^{-(R-2)/16}.
\tag{139.16}
$$
末尾完整一串只定义一个充分容纳事件；若它伸出窗口，属于已付出的跨度失败项，不是选择器额外读取的数据。

最后，从任意后缀 $j$ 读 $0101$ 都合法，且该词包含完整零串 $101$。其条件概率为
$$
\Pr(0101\mid j)=\frac{p^4h_1}{h_j}\ge p^5\ge\frac1{32}.
$$
把新窗口从左至右分成 $\lfloor R/4\rfloor$ 个完整四位块。窗口没有候选蕴含每块都不是 $0101$；对各块依次条件化于此前全部过去，得到
$$
\Pr(B)\le(31/32)^{\lfloor R/4\rfloor}.
\tag{139.17}
$$
这里不需要块独立。合并（139.14）—（139.17）即得（139.13）。

对 $R\ge18$，
$$
\left\lfloor\frac{R-2}{16}\right\rfloor\ge\frac{R-2}{32},
\quad 2e^{-(R-2)/16}\le\frac{32}{R-2},
\quad (31/32)^{\lfloor R/4\rfloor}\le e^{-R/256}\le\frac{256}R.
$$
所以
$$
\delta_k(F_R)\le\frac{160}{R-2}+\frac{256}R
\le\frac{436}R\le\frac{512}R.
$$
对 $1\le R<18$ 使用概率不超过一；再与所有 $R$ 都有的概率界一合并，得到（139.12）。规则仅比较已经完整取得的零串、再 XOR 窗口内边位，因此对全部 $k$ 确为同一确定函数。证毕。

