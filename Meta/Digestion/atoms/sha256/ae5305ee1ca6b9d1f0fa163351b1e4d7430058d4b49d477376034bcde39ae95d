### 43.13 命题：分离不保留逐记录结果像（FT-CE3）

取整条竖线 $L=\{(0,d):d\in\mathbb Q\}$、$S=L\setminus\{(0,0)\}$、$p=(0,0)$。FT-P2 与 FT-P4 给出两者的恒等核及 $\operatorname{Sep}(S,L)$，但

$$
\{F_p(r):r\in L\}=\{\bot,\operatorname{ok}(0)\},
\qquad
\{F_p(r):r\in S\}=\{\operatorname{ok}(0)\}.
$$

证明。FT-P2 与 FT-P4 给出所述恒等核及分离性；$p$ 仅在高度零失败，其余高度成功为零。分离保留的是记录之间的等价分类，这并不保留每条记录的结果像，也不自动建立更丰富操作语言的同余。

