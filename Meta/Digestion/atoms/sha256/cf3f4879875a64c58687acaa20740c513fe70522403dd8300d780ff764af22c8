### 定理 K2(Conjecture 2 在 `k ≤ 2` 截断,任意实位移)

设 `h₀, h₁, h₂ ≥ 0` 且 `h₁² ≥ h₀·h₂`,`μ > 0`、`α, β ≥ 0`,

```
H_t(X) = h₀ + h₁·t·X + h₂·t·(t+3)·X²/2
```

**结论**:对每个 `m ∈ ℕ`,`coeff_m( H_{μ+α}·H_{μ+β} − H_μ·H_{μ+α+β} ) ≥ 0`。

**增量所在**:论文的 Theorem 3 只覆盖**整数**位移;本条对**任意非负实位移** `α, β` 成立。

