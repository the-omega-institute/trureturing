# 十七、有限素数观察者天然看不见奇偶

这也是你的直觉可能通向矛盾的真正位置。

设观察者只检查有限素数集合：

$$
S=\{p_1,\ldots,p_r\}.
$$

它只能读取：

$$
\mathcal O_S(n)
=
(v_p(n))_{p\in S}.
$$

在 \(S\) 之外选取两个不同的大素数 \(q,r\)。

则：

$$
\mathcal O_S(q)=0,
$$

而：

$$
\mathcal O_S(qr)=0.
$$

有限观察者看来二者完全相同：都没有被任何已观察素数整除。

但：

$$
\Omega(q)=1,
$$

所以：

$$
\lambda(q)=-1.
$$

而：

$$
\Omega(qr)=2,
$$

所以：

$$
\lambda(qr)=+1.
$$

因此：

$$
\boxed{
\mathcal O_S(q)=\mathcal O_S(qr),
\qquad
\lambda(q)\neq\lambda(qr).
}
$$

这证明：

