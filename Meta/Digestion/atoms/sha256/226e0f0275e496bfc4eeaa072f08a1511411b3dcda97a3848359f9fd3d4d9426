**证明。** 给定关系词和一位绝对起点，递推 $x_{i+1}=x_i\oplus(1-r_i)$ 唯一确定全词；两个起点给互补词。长度 $k$ 的连续关系一恰对应长度 $k+1$ 的恒定绝对块，所以合法性正好对应。由 $\rho(Cx)=\rho(x)$ 得（141.1）。反过来，在（141.2）的逆式中更换互补提升时，$x_R$ 和 $g(x)$ 同时翻转，XOR 不变，所以逆式良定义。两式互相代回即为恒等。沿实际路径，$Y_t=A_t\oplus f_t$，再用绝对位运输得到（141.3）。

这是一份合法词函数之间的双射；若使用总关系函数，须先按定义141.0取商，不能让不合法输入上的任意值产生虚假的一一对应。证毕。

