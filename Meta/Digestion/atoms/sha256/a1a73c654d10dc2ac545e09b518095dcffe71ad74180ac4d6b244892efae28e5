# 五、这个有限形状仍然保留完整的 RH 判定信息

定义奇偶组合

$$
f_T^\pm
=
\frac{g_{T,+}\pm g_{T,-}}{\sqrt2}.
$$

它们分别为偶函数、奇函数，并满足

$$
\boxed{
Q_W(f_T^\pm)=q_a\pm B_a(T).
}
\tag{18}
$$

因此，两片联合矩阵

$$
G_a(T)=
\begin{pmatrix}
q_a&B_a(T)\\
B_a(T)&q_a
\end{pmatrix}
$$

半正定，当且仅当

$$
|B_a(T)|\le q_a.
$$

