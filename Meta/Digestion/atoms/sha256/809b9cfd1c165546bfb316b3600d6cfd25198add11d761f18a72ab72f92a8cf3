# 七、改进临界区块证书：不只看最大价差，而看加权偏离总量

上一轮使用：

$$
\max_i\eta_i-\min_i\eta_i
$$

控制一组近临界方向。现在可以保留更细的结构。

固定基点 \(B\ge3\)，选取不同素数 \(p_i\)。令：

$$
u_i=\ln p_i,
$$

$$
\eta_i=\frac{W(Bp_i)-W(B)}{u_i},
\qquad
U=\sum_i u_i.
$$

定义加权平均：

$$
\bar\eta=\frac{\sum_i u_i\eta_i}{U},
$$

以及中心化偏离：

$$
d_i=u_i(\eta_i-\bar\eta).
$$

于是：

$$
\sum_i d_i=0.
$$

定义：

$$
\boxed{
\Xi=\frac12\sum_i|d_i|.
}
\tag{21}
$$

这个量不是只取最坏的两个阈值，而是把每个方向的规模与偏离同时计入。

令：

$$
B_{\mathrm{top}}=B\prod_i p_i.
$$

对任意选择集合 \(S\)，记：

$$
s=\sum_{i\in S}u_i.
$$

