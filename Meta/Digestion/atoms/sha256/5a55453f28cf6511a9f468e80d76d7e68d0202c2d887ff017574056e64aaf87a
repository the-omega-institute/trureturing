**证明。** 若有这样的 $h$，令
$$
A=\{(a,r):a=h(r)\}.
$$
图集可测，而给定 $r$ 后锚在两个元素上均匀，所以 $\nu(A)=1/2$。在（142.12）成立的关系路径上，
$$
\begin{aligned}
(a,r)\in T^{-1}A
&\Longleftrightarrow a\oplus(1-r_0)=h(\sigma r)\\
&\Longleftrightarrow a=h(r)\\
&\Longleftrightarrow(a,r)\in A.
\end{aligned}
$$
故 $T^{-1}A=A$ 模零成立。遍历性迫使 $\nu(A)\in\{0,1\}$，与 $\nu(A)=1/2$ 矛盾。

平移协变输出的零缺陷方程恰为（142.12），所以后一断言随之成立。这里使用的是整个公平扩展的遍历性，不是仅用基底遍历；结论也不排除指定原点重构或外加随机参考。这一图集反证只排除每个可测选择的缺陷为零，不单独给出对所有选择一致的正下界；固定窗口的定量界仍由定理141.9承担。证毕。

