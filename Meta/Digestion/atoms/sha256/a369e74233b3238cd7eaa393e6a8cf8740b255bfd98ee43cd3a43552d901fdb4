## 定义 6.1：加权独立集配分函数

对上述带能量的约束图，定义：

$$
\mathcal Z(s)
=
\sum_{I\in\operatorname{Ind}(\mathfrak F)}
e^{-sE(I)},
\qquad s\in\mathbb R.
$$

对 5040：

$$
E(I)=\ln d(I).
$$

所以立即得到：

$$
\boxed{
\mathcal Z_{5040}(s)
=
\sum_{d\mid5040}d^{-s}.
}
\tag{6.1}
$$

这是一个标准形式的**加权独立集模型**：相邻顶点不能同时占据，每个被占据顶点贡献一个乘法权重。

