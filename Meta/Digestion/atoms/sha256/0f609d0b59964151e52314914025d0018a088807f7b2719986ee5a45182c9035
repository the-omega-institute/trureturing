**第三，如果 RH 不成立，这个正性边界会出现在最近的解析极点之前；在边界本身，每个有限阶矩阵仍然严格正定。只有继续增加分辨率与观察时长，负方向才会显现。**

这说明，真正的临界不必先表现为函数奇点，也不必先表现为某个有限矩阵恰好奇异。

本次核对项目快照为 `82667f32ca8a…`。仓库已有 Poisson 粗粒化接口，但其中卷积结合律、半群律和读出表达仍作为独立前件。本轮将一个具体的单位圆模型算出来，不把抽象接口当成实际 ξ 的全局证明。

以下新增综合推导尚未进行 Lean 编译。

