**定理 20.3（整个配置恒等的读取只须检验全部旧词）。** 在定义20.1的同一载体上，设 $A^+=A\cup B$，其中 $B$ 恰为本定理新增的读取规则族，没有其他未检验的新增选择器或控制规则。每个 $b\in B$ 只在各自域 $D_b$ 合法，执行时对整个相关联合配置恒等；旧规则保持不变。定义严格读取字段
$$
\widehat r_b(s)=
\begin{cases}
\operatorname{absent}(r_b^-(s),c_b^-(s)),&s\notin D_b,\\
\operatorname{step}(r_b(s),c_b(s)),&s\in D_b.
\end{cases}
\tag{20.4}
$$
未准入时的报告和费用仍仅指实际合法发生者。先设没有新增根读数。对旧有限词 $w$，令 $D_w$ 表示其每一步都执行且到达后继的域；$w$ 为空词时 $D_w=S$。则
$$
sE^+t\quad\Longleftrightarrow\quad
sEt\ \text{且}\quad
\forall w\in A^*\ \forall b,
\quad s\in D_w\Longrightarrow
\widehat r_b(F_ws)=\widehat r_b(F_wt).
\tag{20.5}
$$
若另外把 $u$ 加为当前公开接口，则右侧还须对每个这样的 $w$ 要求
$$
u(F_ws)=u(F_wt).
\tag{20.6}
$$

证明。必要性取每个旧执行前缀 $w$ 后的读取分支；扩张行为必须保留该分支的合法性、实际报告和费用。当前新接口同理由前缀末端根得到。因为 $sEt$，$w$ 在两边同时可执行。

反向考虑任意有限混合词。迄今每个已执行的新增读取都保持整个状态恒等，所以删去这些读取后，两边当前状态恰为同一旧执行词的后继。到新增读取时，式（20.5）给相同模式及完整字段；若未准入，二者在同处终止，若执行，状态均不改变。到旧规则时，旧核 $E$ 保其完整响应；若执行，两状态沿共同旧词前进一步。新增根接口由式（20.6）保持。对混合词长度归纳即得所有响应相同，故 $sE^+t$。删除读取仅是证明状态路径的辅助操作，其报告、费用及在原混合词中的顺序仍逐项保留。$\square$

“整个配置恒等”排除了任何实际发生的档案追加、预算扣减、指针移动、权限耗用或控制器更新；保留对象本身不变不够。若读出结果实际加入保留档案，该步骤须使用定理20.2，不能为套用式（20.5）而删除记录。纯接口开放而没有操作状态变化时使用式（20.6）。若全部新严格读取字段已在旧充分摘要上因子化，且该摘要已保所有旧后继，则式（20.5）在每个后继自动满足；冗余自读不增加所需区别，这一充分性亦由定理19.9(d)直接得到。行为核不变不表示动作菜单、原始事件词或最优取得费用不变。

