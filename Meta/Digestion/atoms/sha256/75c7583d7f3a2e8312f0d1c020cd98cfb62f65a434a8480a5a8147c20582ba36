预留 $Q_R$ 个按时间排列的槽，每槽以两个 $L$ 位数编码两个相对端点，再用一位编码实际相位，共 $2L+1$ 位。队列长度与 $\ell$ 各用 $L$ 位，精确 $j$ 用 $J$ 位。未用槽位可固定，不承载额外历史。定理140.4给这些记录的精确更新，故得到两种编码上界。若还把固定参数 $R,k$ 作为可配置数据保存，另加 $O(\log(R+1)+\log k)$ 位，不改变上述阶。证毕。

