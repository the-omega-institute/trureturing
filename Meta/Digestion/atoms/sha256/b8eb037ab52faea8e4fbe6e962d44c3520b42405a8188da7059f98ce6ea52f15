## 7.2 RH 的图论表达

定义外部顶点势函数：

$$
E(a)=\ln N(a),
\qquad
W(a)=\ln Z_a(1).
$$

Robin 判据为：

$$
Z_a(1)<e^\gamma\ln E(a),
\qquad N(a)>5040.
$$

最小 Robin 反例若存在，必为超丰数；超丰数具有连续小素数和非增指数结构。因此，将检查限制到上述素数标号分拆不会漏掉反例。([arXiv][4])

于是：

$$
\boxed{
\mathrm{RH}
\iff
W(a)<\gamma+\ln\ln E(a)
\quad
\text{对外部增长图中全部 }N(a)>5040\text{ 的顶点}.
}
\tag{7.1}
$$

也就是：

> 