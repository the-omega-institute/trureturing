## 5.1 完整的模式分解

另外两个底面差异模式对应 \(x\) 与 \(y\)，其特征值都为零。

所以整个五维矩阵可以正交分解为：

$$
\boxed{
A
\cong
2
\begin{pmatrix}
1&1\\
1&0
\end{pmatrix}
\oplus[0]\oplus[0]\oplus[-2].
}
\tag{15}
$$

这个公式把所有结构放在一起了：

$$
\boxed{
\text{两维黄金可见部分}
+
\text{三维底面不可见部分}.
}
$$

其中最后的 \([-2]\) 就是奇偶交替模式。

这与仓库的 `FutureWordOrthogonalResidual` 直接对应：相同的观察结果，意味着状态差落在可见读出张成空间的正交补中。

如果输入与读出都放在顶点，最小线性响应维数确实只有 2；不是因为五个状态中的其他三个被证明“不存在”，而是它们对这个接口不可达或不可见。仓库的 Hankel 最小维数理论正是区分这两种情况。

