**定义 3.3(多滤链命名系统)**。**多滤链命名系统**为 𝒩 = (N, X, ν, (h₁, …, h_m)),各 hᵢ: N → ℕ,携**主滤链条款**:至少存在一个 i 使 {a ∈ N : hᵢ(a) ≤ Q} 对每一 Q 有限。联合预算集 N_{Q⃗} = {a : hᵢ(a) ≤ Qᵢ, ∀i} 由主栏保有限。点 x ∈ X 之**硬度曲面** β(x; ·) 为可达三元组 (Q⃗, dist(x, ν(N_{Q⃗}))) 之帕累托前沿;m = 1 时退化为 [界面] 之单参硬度。

