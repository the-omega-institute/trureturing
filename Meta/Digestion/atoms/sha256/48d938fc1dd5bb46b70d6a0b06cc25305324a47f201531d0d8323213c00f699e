## 模型 B

所有允许行动都是恒等：

[
F_u(x)=x.
]

两个模型在：

* 状态概念；
* 分类器；
* 特征重要性；
* 当前输出；

上完全相同。

但在模型 A 中存在补救，在模型 B 中不存在。

所以：

[
\boxed{
\text{观察性的“为什么得到这个结果”}
\not\Rightarrow
\text{因果性的“怎样改变这个结果”.}
}
]

行动补救必须依赖干预模型，而非仅依赖分类器边界。

---

