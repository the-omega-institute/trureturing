**定义 50.4（两张分立表与一次选择的有限控制器）。** 固定正有理 $\varepsilon$ 及 $0<\delta<1$，取命题50.3的 $M$。对 $p_A,p_B$ 分别使用ML主卷定义41.2的同容差观察者，记转移、读出、初态为 $T_b^p,y^p,s_*^p$。其网格大小写为 $g_p$，保留 $M$ 专用于训练对数：
$$
k_A=\min(1,8\varepsilon),\quad k_B=\min(1,12\varepsilon),\quad
K_p=1+k_p,\quad
g_p=2\left\lceil\frac{2K_p^3}{k_p}\right\rceil,\quad N_p=g_p+1,\quad
s_*^p=g_p/2.
$$
两张表分别保存；不物化它们的笛卡尔积转移表。

训练阶段的报告边界状态为 $(k,c,u,j_A,j_B)$，其中 $0\le k<M$ 为完成的对数、$0\le c\le k$ 为匹配数、$u\in\{\varnothing,0,1\}$ 为待配报告，$j_p\in\{0,\ldots,g_p\}$。初态为 $(0,0,\varnothing,s_*^A,s_*^B)$。每份报告 $b$ 先将两个标签各更新为 $T_b^p(j_p)$。若 $u=\varnothing$，将 $u$ 改为 $b$；否则完成一对，将匹配数增加 $\mathbf1\{u=b\}$。非最终对完成后令 $k$ 增一、$u=\varnothing$。最终第二份报告，即第 $2M$ 份报告，先完成两标签更新，再完成计数，再按50.3选择模型。

最终转移直接进入与训练状态不交的状态集
$$
(\{A\}\times\{0,\ldots,g_A\})\ \sqcup\
(\{B\}\times\{0,\ldots,g_B\}).
$$
先保存所选模型的更新后标签，随后删除训练控制状态，再作首次所选读出；将全部训练字段置为同一个固定哨兵与删除等价。不得保留依赖历史的对计数、匹配数、待配位或未选标签。之后仅保留所选模型身份与其标签，每份报告照该表更新再读出，既不重置标签，也不重新选模或累积时钟。固定参数、$M$ 及两张静态表可以保留，按静态存储另计。训练中、选择前的读出定义为 $1/2$，不附加 $\varepsilon$ 承诺。这给出无外部时钟、无原始历史、无隐藏精确后验的有限确定性机器。

