**这次可以从 \((4,2,1,1)\) 中提取出一条此前没有充分利用的关系：**

$$
\boxed{4=2+1+1.}
$$

在我们已经定义的事件模型中，它表示：

$$
\boxed{
\text{最长的一条因果链，恰好等于其余三条链的总长度。}
}
$$

这条等式同时影响三件事：**并行执行的时间瓶颈、过去—未来切面的状态数量，以及把整个历史写成一个波时所需的最小记忆维数。**

由此可以证明：

$$
\boxed{
5040\text{ 的840条完整历史，在中间时刻需要12个独立的边界模式。}
}
$$

而且，这12个模式不是等权的。最小模式只有总概率的 \(1/70\)，却不能因为它小就从全称安全证明中删除。

下面建立这套关系。这里研究的是明确指定的离散算术执行模型；它可以帮助组织因果与量子表示，但不预先认定它就是现实物理时空。

