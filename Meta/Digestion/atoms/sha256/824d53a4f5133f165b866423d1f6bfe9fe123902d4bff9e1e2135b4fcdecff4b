# 八、怎样把这个几何直觉变成项目中的可用证明工具？

现在可以把“多出来的坐标”定义为：

$$
\boxed{
\text{当前观察没有保留的混合模式。}
}
$$

对于四个二值方向，任意目标函数都有展开：

$$
F(x)
=
\sum_{S\subseteq\{1,2,3,4\}}
\widehat F(S)\prod_{i\in S}x_i.
$$

总共：

$$
1+4+6+4+1=16
$$

个模式。

这五层分别是常数项、单方向项、二方向关系、三方向关系和四方向奇偶项。它是一种自然的分层，但不能因为层数或形状像金字塔，就把它当成某种物理实体的证明。其数学基础是 Boolean Fourier 展开。([arXiv][1])

若观察者只保留 \(|S|\le d\) 的项，定义：

$$
F_{\le d}(x)
=
\sum_{|S|\le d}\widehat F(S)\prod_{i\in S}x_i.
$$

则：

$$
\boxed{
|F(x)-F_{\le d}(x)|
\le
\sum_{|S|>d}|\widehat F(S)|.
}
\tag{23}
$$

证明只需要三角不等式，因为每个奇偶基函数的绝对值为 1。

因此，要证明某个安全目标 \(F(x)>0\)，一个真正有效的条件是：

$$
\boxed{
\min_xF_{\le d}(x)
>
\sum_{|S|>d}|\widehat F(S)|.
}
\tag{24}
$$

这不是要求把每个隐藏模式都完全恢复出来，而是要求**隐藏模式的最坏总影响不足以改变安全符号**。

对式（16）的 5040 四方向未来模型，如果已经保留全部三阶及以下模式，那么遗漏的只有一个四阶模式，其精确最大误差为：

$$
\boxed{
\frac1{16934400}.
}
$$

但若目标改为 Robin 余量 \(\Delta\)，就必须重新计算或估计 \(\Delta\) 自己的混合系数，不能直接照搬 \(Z\) 的误差。

这正好对应仓库中的 CUT 与 residual：

$$
\text{CUT 选定可见模式，}
$$

$$
\text{残差是未进入可见子空间的部分，}
$$

$$
\text{安全证明需要控制残差对目标的最坏影响。}
$$

仓库已有可见投影与正交残差的等价定理；这里需要继续补的是这些特定算术目标的定量系数界。

