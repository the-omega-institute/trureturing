**定理 120.5（历史的最小预测记忆）。** 更新与输出
$$
\delta_a(r_h)=r_{ha},\qquad \widehat o(r_h)=r_h(\varepsilon)
$$
定义良好，并以初态 $r_\varepsilon$ 实现全部响应 $s$。任何实现相同 $s$ 的可达确定系统，都有唯一保初态、读数和更新的满射到 $M$。两个可达且全部未来读数分离状态的实现，通过唯一这样的双射等价。

