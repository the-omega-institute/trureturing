## 4.1 定义各阶联合项

对非空集合 \(S\)，定义锚定混合系数：

$$
\boxed{
c_S
=
\sum_{T\subseteq S}
(-1)^{|S|-|T|}F(T).
}
\tag{11}
$$

它满足精确展开：

$$
F(S)
=
F(\varnothing)
+
\sum_{\varnothing\ne T\subseteq S}c_T.
$$

一阶系数是单方向增量。

二阶系数是：

$$
c_{\{i,j\}}
=
F(\{i,j\})-F(\{i\})-F(\{j\})+F(\varnothing).
$$

它测量“联合结果相对两项独立叠加多了什么”。

