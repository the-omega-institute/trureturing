### 记号(沿用本卷定理 V1–V4 与 `D5/S3/Zeros/PochhammerDeformation/QuadraticInterval.lean`)

固定实数 `a > 0`。`L_a : ℝ[x] → ℝ[x]` 是把 Pochhammer 基映到单项式基的线性算子:

```
L_a( ∑_{k=0}^n b_k · x(x−1)⋯(x−k+1) / (a)_k )  =  ∑_{k=0}^n b_k · z^k
```

`U_n = { P ∈ ℝ[x] : deg P = n, 且 L_a(P) 的全部零点为实且落在 [−1,0] }`。

**正规形(可复算,本增订的公共引理)**:设 `deg P = 2`,把 `L_a(P)` 写成 `(z+u)(z+v)`、
`u, v ∈ [0,1]`。由 `P(x) = uv + (u+v)·x/a + x(x−1)/(a(a+1))` 乘以 `a(a+1)` 得

```
p_{u,v}(x) = x² + ( (a+1)(u+v) − 1 )·x + a(a+1)·u·v        …… (V5.0)
```

于是 `U_2` 中多项式的零点集,恰为 `{ p_{u,v} 的零点 : u,v ∈ [0,1] }`(相差非零常数因子)。

