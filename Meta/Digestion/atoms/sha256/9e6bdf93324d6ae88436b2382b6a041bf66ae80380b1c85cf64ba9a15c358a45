### 43.16 命题：仅有成员 oracle 的统一算法障碍（FT-P9）

若算法对任意 $R$ 只有查询“$r\in R$ 吗”的成员 oracle，除此没有特殊有效证书，则不存在对所有 $R$ 总停机的统一等价判定器，也不存在总停机的统一有限基抽取器；后一个不可能性甚至只用都有有限基的空集、单点输入。

证明。假设存在这样的等价判定器，先以 $R=\varnothing$、$p=(0,1)$、$p'=(1,1)$ 运行。它必须在有限次成员查询后返回相等。选择一个从未被查询的 $(c,0)$，改令 $R=\{(c,0)\}$。有限次查询的所有否定回答保持不变，同一算法沿相同执行路径返回相等，但该单点上的两输出是 $\operatorname{ok}(c)$ 与 $\operatorname{ok}(c+1)$，矛盾。对基抽取器，在空集上唯一合法输出是空基；选其有限查询中未问到的 $(c,0)$，单点 oracle 给相同回答，使它仍输出空基，而 FT-P3 证明这不是单点的基。此论证限定在所述 oracle 接口，不否定带特殊有效证书的测试集可有统一处理方法。

