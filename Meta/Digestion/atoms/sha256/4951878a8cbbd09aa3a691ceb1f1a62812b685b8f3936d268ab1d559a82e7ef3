## 115. 最小分离成本

若要实现谱值：

$$
F(z_{\rho_i})=v_i,
$$

且 \(G\) 可逆，则最小范数插值函数满足：

$$
\boxed{
\|F_{\min}\|^2
=
v^\ast G^{-1}v.
}
$$

对一个离线 mirror pair，取：

$$
v=(1,-1,0,\ldots,0).
$$

那么：

$$
\boxed{
\operatorname{SepCost}^2
=
v^\ast G^{-1}v.
}
$$

所以“能否把一对离线零点从其余全部零点中取出”不再是哲学问题，而是：

$$
\boxed{
\text{一个无限 Gram operator 是否具有受控右逆}.
}
$$

---

