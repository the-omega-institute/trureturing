## 定义 2.4　素数—黄金表

定义 \(\mathcal B\) 为所有数组：

$$
b=(b_{p,j})_{p\in\mathbb P,\ j\ge0}
$$

组成的集合，满足：

$$
b_{p,j}\in\{0,1\},
\qquad
b_{p,j}b_{p,j+1}=0,
$$

并且整张表只有有限多个非零元素。

令：

$$
a_p(b)=\sum_jb_{p,j}G_j,
$$

$$
N(b)=\prod_pp^{a_p(b)}.
$$

