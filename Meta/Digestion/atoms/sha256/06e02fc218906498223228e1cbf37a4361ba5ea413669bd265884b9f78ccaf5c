## 定理 3.1：完整减法回路

若：

$$
c\ge d\ge1,
$$

从：

$$
(a,0,c,d;13)
$$

出发，经过：

$$
4d+2
$$

次分数执行，准确到达：

$$
\boxed{
(a+d,0,c-d,d;13).
}
\tag{5}
$$

### 证明

先执行 \(d\) 次第一种两步操作：

$$
(a,0,c,d;13)
\longrightarrow
(a+d,d,c-d,0;13).
$$

耗时 \(2d\)。

随后通过 \(11/13\) 转入控制11；执行 \(d\) 次第二种两步操作，把 \(3\) 寄存器的 \(d\) 单位全部搬回 \(7\) 寄存器；最后通过 \(13/11\) 返回控制13。

总耗时：

$$
2d+1+2d+1=4d+2.
$$

试除数 \(d\) 恢复，剩余量减少 \(d\)。证毕。

其净作用是：

$$
\boxed{
a\mapsto a+d,\qquad c\mapsto c-d.
}
$$

因此，\(a+c=m\) 始终保持。

---

