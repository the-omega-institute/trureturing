## 一个明确的非零盲区向量

取 \(X=4\)，定义

$$
v_5=v_{10}=1,
\qquad
v_6=v_{12}=-1,
$$

其他坐标为零。

则 \(v\ne0\)，但

$$
\boxed{
Y_v(x)=0
\quad\text{对几乎所有 }x\in[4,8].
}
$$

这不是实际 \(\Lambda(n)-1\) 的另一组取值，而是用来刻画观察算子本身的零空间。

**因此，观察不到一个方向，不等于该方向在原始数据中不存在。**

