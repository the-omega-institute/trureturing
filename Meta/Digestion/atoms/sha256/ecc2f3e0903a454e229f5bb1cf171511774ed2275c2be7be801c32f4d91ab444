### 50.5 任意指定有限支持的充要有限判据

选择整数 $H\ge1$ 满足
$$
\rho+\rho^{2H}\le1.
\tag{50.13}
$$
总可以这样选择，因为 $0<\rho<1$。例如可取
$$
H=\left\lceil\frac{\log(1-\rho)}{2\log\rho}\right\rceil.
$$
该商为正，故所给整数至少为一。实际精确计算不必依赖浮点对数，可以直接检验（50.13）。

