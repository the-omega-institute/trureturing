\[
A(\theta)
=
\sum_n a(n)e^{-in\theta}.
\]

则：

- \(k=2\) 的 Fourier 变换是 power spectrum：
  \[
  |A(\theta)|^2;
  \]
- \(k=3\) 的 Fourier 变换是 bispectrum：
  \[
  A(\theta_1)A(\theta_2)
  A(-\theta_1-\theta_2);
  \]
- \(k=4\) 给出 trispectrum；
- 一般 \(k\) 给出满足总频率守恒的 polyspectrum。

因此：

