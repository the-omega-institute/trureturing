# 第六百六十六部　关系性“无克隆”原理

这里与量子无克隆有相似性，但必须严格区分。

量子无克隆定理说明：由于量子演化的线性结构，不存在一个普适过程能够复制任意未知量子态。([数字对象标识符][1])

当前结构不是这一量子定理，而是一个更基础的**参考系不可重建定理**。

设：

$$
q:\mathcal A\to Y
$$

是一个完全 \(G\)-不变的内部读数：

$$
q(ga)=q(a).
$$

由于 \(G\) 在 \(\mathcal A\) 上传递，\(q\) 必为常函数。

因此不存在解码器：

$$
d:Y\to\mathcal A
$$

满足：

$$
\boxed{
d(q(a))=a
\qquad
\forall a\in\mathcal A,
}
\tag{666.1}
$$

除非 \(\mathcal A\) 只有一个元素。

同样，不存在：

$$
C:Y\to\mathcal A\times\mathcal A
$$

满足：

$$
C(q(a))=(a,a)
$$

对所有 \(a\) 成立。

---

