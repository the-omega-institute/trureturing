## 1684.1 精确采样公式

对任意：

$$
f\in K_B,
$$

由该正交基展开：

$$
\boxed{
\|f\|_{H^2}^2
=
\sum_{j=1}^{m}
\frac{
2\pi
}{
\theta_B'(x_j^\alpha)
}
|f(x_j^\alpha)|^2.
}
\tag{1684.5}
$$

所以一个边界相位值 \(\alpha\) 所对应的全部 \(m\) 个 lift，已经足够精确重建整个隐藏模型空间。

这给“边界辐射能够编码内部”一个非常严格的有限维版本：

$$
\boxed{
\text{单一相位输出不充分；
该相位下的全部原像及其导数权重，则构成完整内部采样基。}
}
$$

---

