### 50.16 严格分离与一个保证三阶最优的精确区间

三阶根的精确值为
$$
F_\rho(e^{\pm2\pi i/3})=\sqrt3(1+\rho).
$$
其他有限奇数阶至少为五，给下界 $L_5=2\cos(\pi/10)$；无限阶和偶数阶给二。因此
$$
\boxed{
M(\rho)\ge
\min\left\{\sqrt3(1+\rho),2\cos\frac\pi{10}\right\}
>\sqrt3.
}
\tag{50.42}
$$
Context49 的式（49.39）给出的 $\sqrt3\,c$ 一致分离常数因而对每个固定正 $\rho$ 都不是最佳常数。结合有限候选上界，有
$\sqrt3<M(\rho)<2$。

如果
$$
0<\rho\le\frac{2\cos(\pi/10)}{\sqrt3}-1,
$$
三阶候选不超过 $L_5$，而所有其他有限奇数阶的实际值严格大于各自 $L_r$。故在整个该范围
$$
\boxed{M(\rho)=\sqrt3(1+\rho),}
\tag{50.43}
$$
唯一比值是一对共轭三阶根。这是已证明的充分区间；没有证明它是三阶最优的最大区间，也没有在其端点认定最优阶数发生转变。例如 $\rho=1/20$ 时，
$$
3(21/20)^2=1323/400<7/2<(5+\sqrt5)/2=L_5^2,
$$
所以 $M(1/20)=21\sqrt3/20$。这是由精确不等式证明的实例，不需要扫描所有较高阶根。

