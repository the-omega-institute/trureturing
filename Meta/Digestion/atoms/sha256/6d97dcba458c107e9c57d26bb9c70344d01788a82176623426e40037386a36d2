**第三种是目标保留证书。**

对实际 ξ，不能用一个固定 \(a<1\) 的正模型替代原对象。必须证明

$$
a_*=1,
$$

或者等价地证明取消平滑后，全部实际相关仍然合法。

Schur 消元的次序一致性可以帮助组织这些推导，但它不会自动给出实际正性。

项目的动态闭包也强调保留全部有限操作后的读出。这里则进一步表明：**即使逐个固定时长的读出趋近，也不能自动推出无限返回事件、平均等待时间或统一谱余量被保留。**这些需要另外的极限与一致性条件。

本轮的精确有理例子、Poisson 矩阵界、首次返回振幅缩放与正弦包络均已做有限核对。代码见[核验脚本](sandbox:/mnt/data/discrete_resolution_criticality_verify.py)，结果见[核验记录](sandbox:/mnt/data/discrete_resolution_criticality_verify.json)。其中没有计算实际 ξ 的 \(a_*\)，也没有使用任何实际离线零点作为数据。

