# 十、怎样与仓库的四原语真正结合？

本轮在仓库中检索 `FRACTRAN` 与 `PRIMEGAME` 未得到命中；因此，不把下面的新语义层说成仓库已经完成的模块。

不过，检索快照 `ef1923c` 中的现有部件可以直接承接它。

仓库 `PrimitiveKernel` 保留 CUT、FLOW、ADMIT、ANCHOR 的四种职责。

在当前模型中，可以这样实例化：

| 原语角色       | FRACTRAN 中的具体对象                   |
| ---------- | --------------------------------- |
