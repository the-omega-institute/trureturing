## 35. 为什么整体平均为零不够

对全局反射态：

$$
\mathbb E
\left[
\sum_oX_o
\right]
=
0.
$$

甚至可能有一个纠缠态满足：

$$
\left(
\sum_oB_o
\right)|\Omega\rangle=0.
$$

但这不推出每个：

$$
B_o=0.
$$

这和总自旋为零一样：整体为零不代表每个局部自旋为零。

因此不能只看：

$$
\left(\sum_o\delta_o\right)^2.
$$

必须看局部平方之和。

---

