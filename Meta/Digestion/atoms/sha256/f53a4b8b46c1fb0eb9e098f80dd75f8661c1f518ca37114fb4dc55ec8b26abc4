## 8.1 这与极大丰数闭包是什么关系？

对给定价格 \(\lambda\)，式（7）构造的是：

$$
\text{当前整数的最优倍数}.
$$

它依赖于 \(\lambda\)，也会保留当前已经存在但在该价格下不经济的指数。

前一轮的：

$$
\operatorname{CAcl}(B)
$$

则是“整除 \(B\) 的最小极大丰完成”，其价格由 \(B\) 的约束决定。

两者有关，但不是同一个闭包，不能在证明中直接互换。

