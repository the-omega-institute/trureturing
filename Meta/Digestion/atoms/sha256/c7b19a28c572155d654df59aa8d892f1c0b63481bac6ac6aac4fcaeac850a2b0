**定理 141.8（对规则和禁词长度共同一致的比较）。** 仍令 $n=R+1$、$L=n+G$。扭转路径前 $n$ 次转移的完整状态边缘，与实际平稳提升的同长度状态块的总变差至多
$$
E_{L,G}=2L\varepsilon_G+\varphi^{2-L}.
\tag{141.13}
$$
因此对全部 $k\ge2$、$R\ge1$、$G\ge1$，
$$
\boxed{
 d_k(R)\ge\frac1{R+1+G}
 -2(R+1+G)(3/4)^{\lfloor G/3\rfloor}
 -\varphi^{2-(R+1+G)}.
}
\tag{141.14}
$$
右侧允许为负；不需要另加正规化常数接近一的前提。

**证明。** 对完整前缀状态路径 $p=(s_0,\ldots,s_n)$，记
$$
a(p)=\prod_{i=0}^{n-1}Q(s_i,s_{i+1}).
$$
实际平稳路径概率与未归一化扭转边缘分别为
$$
P(p)=\widehat\pi(s_0)a(p),\qquad
W(p)=a(p)Q^G(s_n,Cs_0).
\tag{141.15}
$$
后一式由对全部 $G$ 步闭合路径求和得到，且 $\sum_pW(p)=Z_L$。

令 $B_L=\{(a,j):j<L\}$。引理141.6说明 $s_0\notin B_L$ 时 $W(p)=0$：若乘积正，则存在一条正权闭合路径，违反支持结论。对 $s_0\in B_L$，混合界及 $\widehat\pi(Cs_0)=\widehat\pi(s_0)$ 给
$$
\left|Q^G(s_n,Cs_0)-\widehat\pi(s_0)\right|\le\varepsilon_G.
$$
从每个固定起态出发的全部 $n$ 步路径权 $a(p)$ 之和为一，所以
$$
\sum_p|W(p)-P(p)|
\le |B_L|\varepsilon_G+\widehat\pi(B_L^c)
\le2L\varepsilon_G+\varphi^{2-L}=E_{L,G}.
\tag{141.16}
$$
关键是只有 $2\min\{k,L\}\le2L$ 个起态能贡献正扭转权；若直接用全部 $2k$ 个状态计数，便得不到所需的统一结论。式（141.16）比较完整路径分布，没有只比较单点状态而遗漏窗口内的联合关系。

因为 $P$ 的质量为一，$W$ 的质量为 $Z_L$，有 $|Z_L-1|\le E_{L,G}$。又 $Z_L>0$，且
$$
\sum_p\left|\frac{W(p)}{Z_L}-W(p)\right|=|1-Z_L|.
$$
三角不等式给
$$
\sum_p\left|\frac{W(p)}{Z_L}-P(p)\right|\le2E_{L,G}.
$$
本节总变差是归一化概率之半 $\ell^1$ 距离，故得到（141.13）。这个归一化论证没有除以最小平稳质量，也无需先证明 $E_{L,G}<1$。

缺陷 $D_R$ 只用关系位 $r_0,\ldots,r_R$，即前 $n=R+1$ 次转移，因而是上述完整前缀路径的事件。实际平稳律下它的概率是 $\delta_k(f)$，扭转律下由旋转不变性为 $\delta_{\mathbb T_L}(f)$。由总变差比较及命题141.7，
$$
\delta_k(f)\ge\delta_{\mathbb T_L}(f)-E_{L,G}
\ge\frac1L-E_{L,G}.
$$
此界对每个合法确定规则成立，再取最小值得（141.14）。证毕。

