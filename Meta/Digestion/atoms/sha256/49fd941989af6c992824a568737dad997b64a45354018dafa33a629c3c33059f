## 定义 1.1：第 \(k\) 层的边际收益率

将指数从 \(k-1\) 增至 \(k\)，支付的规模成本是 \(\ln p\)，收益为 \(f_p(k)-f_p(k-1)\)。

定义：

$$
\boxed{
\eta_{p,k}
=
\frac{f_p(k)-f_p(k-1)}{\ln p}
=
\frac{
\ln\left(1+\dfrac1{p+p^2+\cdots+p^k}\right)
}{
\ln p
}.
}
\tag{1}
$$

这是一组由离散标签 \((p,k)\) 索引的正实数。

这里的谱是**边际收益阈值的集合**，不是未经构造就认定的物理 Hamiltonian 谱。

