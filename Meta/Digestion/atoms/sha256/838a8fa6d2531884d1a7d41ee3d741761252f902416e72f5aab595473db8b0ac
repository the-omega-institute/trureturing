## 185.1 有限层析实验

对固定：

$$
|\Im s|\le T
$$

和窄带区域，按成本顺序加入：

$$
D=5,8,12,13,\ldots
$$

计算：

$$
\Lambda(s,\chi_D)
$$

的共同近零集合。

目标测量：

$$
N_{\mathrm{tor}}(T)
=
\min|\mathcal D_T|,
$$

以及：

$$
C_{\mathrm{tor}}(T)
=
\min\sum_{D\in\mathcal D_T}c(D).
$$

理论只保证每个紧窗口存在有限观察族，并未保证所需判别式小或数量有统一上界。

---

