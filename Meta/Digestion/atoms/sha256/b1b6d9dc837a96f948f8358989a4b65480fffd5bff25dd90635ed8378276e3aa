## 2.2 优先级把几何区域分成了不同控制区域

第 \(i\) 条规则实际执行的区域不是简单的 \(a\ge\beta_i\)，而是：

$$
\boxed{
G_i=
\{a:a\ge\beta_i\}
\setminus
\bigcup_{j<i}\{a:a\ge\beta_j\}.
}
\tag{4}
$$

在 \(G_i\) 内：

$$
T(a)=a+\alpha_i-\beta_i.
$$

所以整个程序是：

$$
\boxed{
\text{由整数阈值切分状态空间，}
\quad
\text{在每个区域执行固定平移。}
}
$$

每一步只需要加法、比较和有限逻辑组合。零测试也可以由优先级产生：前面的规则在寄存器非零时执行，后面的规则就可以作为“它为零时”的分支。

**这就是一种严格的几何编程语义：几何位置决定可用指令，程序优先级决定实际运动。**

它不能直接等同于没有优先级的普通 Petri 网或向量加法系统；后者的行为与可判定性质需要分别分析。

