# 十一、这次对项目最有价值的新增接口

本轮读取了快照 `50720bac…` 的有限时钟、顺序测量与 Rouché 稳定性相关模块。新增推导尚未写入或编译为 Lean 定理。

现在最值得明确形式化的是三条关系。

**第一，调度保真。**

不能只证明两种表示具有相同合法历史，还要证明：

$$
\mathbb P_{\mathrm{encoded}}(\mathcal Z\gamma)
=
\mathbb P_{\mathrm{original}}(\gamma).
$$

否则，虽然终点和时间都保留，相消零点仍可能改变。

**第二，对称性保护。**

把：

$$
\mathsf S\Pi_A=-\Pi_A\mathsf S
$$

与：

$$
\mathsf S\rho\mathsf S=\rho
$$

组合成：

$$
\operatorname{Tr}(\rho\Pi_A)=0,
$$

并保留式（15）的误差版本。

这样可以区分：零点是偶然的精细配重，还是受指定交换对称保护。

**第三，实际算术运输。**

形式化：

$$
\varepsilon(n),
$$

平方／素数平方分类，以及：

$$
\mathcal H=U+VP+W.
$$

这比简单增加一个“与 ζ 相似”的量子函数更有用，因为它明确证明了最终读取的是哪一项已有算术对象。

本轮已经精确枚举 5040 的全部840条历史，交叉核对了两种调度概率、交换对称及八格时钟分解；还检验了总长度不超过8的495组四寄存器占据配置，以及前600个整数的历史奇偶公式与有限 Dirichlet 重排。

这些是辅助的整数、有理数和多项式检查，不代替纸面证明，也不等于 Lean 内核验证。

---

