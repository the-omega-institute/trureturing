## 1.1 两种约束同时存在，但约束的是不同对象

事件层的约束是：

$$
x_{p,k+1}\le x_{p,k}.
$$

即不能采用第五层，却不采用前四层。

Zeckendorf 数位的约束则是：

$$
b_{p,j}b_{p,j+1}=0.
$$

它禁止同一行中相邻黄金数位同时为1。每个指数仍然都有唯一合法表示。([Lean Prover Community][2])

例如：

$$
4
$$

在事件层中表示四个已完成步骤：

$$
1111,
$$

在 Zeckendorf 坐标中表示：

$$
101,\qquad4=1+3.
$$

前者表达因果前缀，后者表达数值。它们是可以同时使用的两层结构。

