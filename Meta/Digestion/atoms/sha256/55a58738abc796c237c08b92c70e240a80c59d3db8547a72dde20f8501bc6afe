## 579.1 完成态并不决定唯一未完成态

从：

$$
\xi
$$

可以忘掉不同类型的数据：

1. 去掉：

   $$
   s(s-1);
   $$
2. 去掉：

   $$
   \Gamma(s/2)\pi^{-s/2};
   $$
3. 只保留 Euler product；
4. 截断到有限素数；
5. 只保留一个 Archimedean 边界方向；
6. 只保留一个 Galois 嵌入；
7. 只保留一个 regulator 弧；
8. 只保留黄金局部指数观察。

所以：

$$
\boxed{
\text{完成态必有未完成表示，}
}
$$

但：

$$
\boxed{
\text{未完成态不是唯一对象。}
}
$$

必须给出一个有类型的忘却映射：

$$
\boxed{
U:\operatorname{Complete}\to\operatorname{Partial}.
}
\tag{579.3}
$$

随后完成问题才是：

$$
\boxed{
\text{由 }U(\xi)\text{ 恢复 }\xi
\text{ 所缺的最小 ledger 是什么？}
}
$$

否则“存在未完成态”只是分解恒真式，缺乏科学内容。

---

