## 8.2 多项式正性的间隔

$$
\lambda_{\min}(\mathsf H(J_{d,n})).
$$

它趋于零时，有限实根证书开始失去稳定性。

如果一个计算矩阵 \(\widetilde H\) 满足：

$$
\|H-\widetilde H\|_{\mathrm{op}}
<
\lambda_{\min}(\widetilde H),
$$

且右边为正，就能认证 \(H>0\)。

但不同次数的 Jensen 多项式会产生不同的矩阵，不能未经证明就把它们当成同一个无限矩阵的嵌套主块。

