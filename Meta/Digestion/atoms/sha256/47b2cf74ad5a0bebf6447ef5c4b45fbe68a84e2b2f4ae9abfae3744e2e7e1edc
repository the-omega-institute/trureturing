**命题 141.11（连续窗口的精确参考移位）。** 固定整数 $a,b\ge0$、$a+b=R$，并固定同一个确定函数 $F:\mathcal L_{k,R}\to\mathbb F_2$。在每个整数顶点 $t$ 使用
$$
F_t(r)=F(r_{t-a},\ldots,r_{t+b-1}),
$$
即使用这一连续关系窗并在顶点 $t$ 报方向。定义在顶点 $t+b$ 的严格过去规则
$$
\widetilde F_{t+b}
=F_t\oplus\bigoplus_{j=t}^{t+b-1}(1-r_j).
\tag{141.24}
$$
这是合法窗口规则之间的双射，且逐路径保持运输缺陷：
$$
\widetilde F_{t+b+1}\oplus\widetilde F_{t+b}\oplus c_{t+b}
=F_{t+1}\oplus F_t\oplus c_t.
\tag{141.25}
$$
因此在平稳来源中，这些相同总长度的连续窗口合同具有相同最优缺陷。

**证明。** 原窗口正是新顶点 $s=t+b$ 的严格过去窗 $[s-R,s-1]$。所加运输量只用这个窗口内从 $t$ 到 $t+b$ 的边，故不需要未读位置。给定窗口，再 XOR 同一个运输量即恢复原规则，所以对应可逆；不合法输入值若出现，仍按定义141.0取商。

具体地，对按从旧到新编号的合法词 $w=(w_0,\ldots,w_{R-1})$，变换后的同一个确定函数为
$$
\widetilde F(w)=F(w)\oplus\bigoplus_{j=a}^{R-1}(1-w_j),
\qquad
F(w)=\widetilde F(w)\oplus\bigoplus_{j=a}^{R-1}(1-w_j).
$$
因此固定 $a,b$ 时，正反变换都不依赖调用时刻。

记 $P_{t,b}=\bigoplus_{j=t}^{t+b-1}c_j$。相邻两个运输和满足
$$
P_{t+1,b}\oplus P_{t,b}=c_t\oplus c_{t+b},
$$
$b=0$ 时两边都为零。把此式代入（141.24）即得（141.25）；平稳性使移位后的缺陷概率相同。证毕。

同一个函数的量词不可省略。若允许随时刻任意改变严格过去函数，在 $R=1$ 时可令时刻零的函数为常值零、时刻一的函数为 $w\mapsto1-w_0$，其余时刻取常值零；于是 $F_0=0$、$F_1=1-r_0$，直接给 $D_0=0$。这不是 $d_k(R)$ 中同一函数反复调用的竞争规则，也不反驳上述固定函数之间的对应。

纯未来窗是 $a=0$，严格过去窗是 $b=0$。这个对应只比较同一连续 $R$ 边窗口及其中参考顶点的运输，不比较不同总长度、非连续读取集合，或与输出顶点隔着未取得关系的窗口。语义上的双射和缺陷等式不免除实际窗口取得、等待时延、校准与费用。

这里 $d_k(R)$ 的下界针对同一个平移协变确定窗口函数：每处都由最近 $R$ 个关系位重新给出方向。若另允许一次初始化后持续保留一位内部参考，令
$$
a_{t+1}=a_t\oplus(1-r_t),
$$
则每次运输缺陷都为零。这个参考包含初始化以来的运输历史，一般不是最近 $R$ 位的函数，因而不在 $d_k(R)$ 的规则类中，与其正下界不矛盾。保留一位的存储量本身不能替代窗口来源合同；该初始位若只是对隐藏公平锚的猜测，其全程正确概率仍为 $1/2$。

公平绝对提升和扭转循环均为证明中的共同概率实现，没有交给内部规则额外参考位、独立优先级或随机种子。规则仍仅依赖实际关系记录，所以第139节命题139.15的真实公平锚恢复率恰为 $1/2$；最优运输首项一没有把这项恢复任务改成可识别任务。

本节来源转移与固定表上界分别由第139节的显式 Parry 结构和有限表平均承担；其中经典来源背景的原始文献见该节 Parry 引文。扭转循环的正性、旋转不变性、支持截断及整个路径的归一化比较均由本节证明，不以独立位置优先级、普通平衡割或其他采样密度结论替代。

