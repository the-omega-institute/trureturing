**定义 140.0（精确持久状态）。** 固定 $R\ge1$、$k\ge2$，实际关系历史禁止 $1^k$。本节始终续接定义139.3的同一个最长完整零串规则 $F_R$：严格过去窗口为 $[-R,-1]$，完整候选由两个已读一端点界定，内部为正长度零串；最长者获胜，平手取闭合端点较晚者；在该端点之后的顶点指定方向零，再沿窗口内关系运输到当前顶点。无候选时方向为零、锚为 $\bot$。

一份确定性精确状态表示由实际历史映射 $M:\mathcal H_k\to\mathcal S$、方向读数 $o:\mathcal S\to\mathbb F_2$ 及新位更新映射 $T_0,T_1$ 组成，满足
$$
o(M(h))=F_R(w(h)),\qquad
M(h\cdot x)=T_x(M(h))
\tag{140.1}
$$
对每条实际历史 $h$ 及其合法新位 $x$ 成立。所有影响后继的持久历史信息均计入状态；不另给原始窗口重读、历史缓存、相位或变化控制状态。$R,k$ 及固定映射本身作为固定参数。

方向合同只要求（140.1），实际输入由外部保证合法。合法性合同另要求状态决定下一位 $x$ 是否满足禁止 $1^k$ 的语法。分别记这两类表示所需最少固定二进制状态位数为 $B_{\rm out}(R,k)$ 与 $B_{\rm leg}(R,k)$：若可达状态集大小为 $N$，以 $\lceil\log_2N\rceil$ 位计，再在满足合同的有限表示中取最小值。完整窗口及精确语法状态给有限表示，故这两个最小值存在。以下计数是持久状态的编码量，不是执行峰值工作区或计算时间。

所构造的状态还恢复当前所选候选和锚；下界仅观察方向输出，因此不依靠额外锚查询。

