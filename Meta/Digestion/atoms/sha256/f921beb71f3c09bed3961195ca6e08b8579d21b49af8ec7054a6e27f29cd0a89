## 最小例子

取：

$$
x_{k+1}=y_k,\qquad y_{k+1}=x_k.
$$

消去 \(y\)：

$$
\boxed{x_{k+2}=x_k.}
$$

其时间生成函数是：

$$
\boxed{
X(z)=\frac{x_0+zy_0}{1-z^2}.
}
\tag{25}
$$

两个时间相位 \(+1,-1\) 都保留下来。

若只看静态保留块 \(A=0\)，并错误地用 \(x_{k+1}=0\) 替代，就把整个二周期结构删除了。

