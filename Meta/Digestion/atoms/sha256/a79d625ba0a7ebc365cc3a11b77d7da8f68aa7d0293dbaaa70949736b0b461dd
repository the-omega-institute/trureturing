## 7.3 一般二重碰撞也具有相同首阶形态

假设某个实点 \((\tau_0,z_0)\) 满足：

$$
H_{\tau_0}(z_0)=0,
\qquad
\partial_zH_{\tau_0}(z_0)=0,
\qquad
\partial_z^2H_{\tau_0}(z_0)\ne0.
$$

令：

$$
w=z-z_0,\qquad\delta=\tau-\tau_0.
$$

由 Taylor 展开与式（25）：

$$
\boxed{
H_\tau(z)
=
A\left(w^2-2\delta\right)
+
O(w^3,w\delta,\delta^2),
}
\tag{28}
$$

其中：

$$
A=\frac12\partial_z^2H_{\tau_0}(z_0)\ne0.
$$

所以在非退化条件下：

$$
w=\pm\sqrt{2\delta}+O(\delta).
$$

这给出局部碰撞的平方根尺度。

但必须保留一个全局区别：

$$
\boxed{
\Lambda_{\mathrm{DN}}=0
$$

并不要求 \(H_0\) 在某个有限位置恰好有一个二重根。临界也可能体现为：不存在一个对全部高度统一有效的稳定裕量。

“每个固定零点附近都很正常”与“全部零点有共同的稳定缓冲区”不是同一命题。

另外，热流变形保留了重要的解析对称性，但并不自动保留原来的 Euler 乘积结构。Tao 特别指出，这正是这类变形与素数算术重新连接时的困难之一。([What's new][6])

