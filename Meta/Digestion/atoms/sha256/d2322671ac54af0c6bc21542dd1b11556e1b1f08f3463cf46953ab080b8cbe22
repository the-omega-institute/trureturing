## 1495.1 名字越完备，切口越稠密

每个 \(\nu_q\) 的不连续边界是单点：

$$
\partial\nu_q=\{q\}.
$$

全部有理切口的边界集合为：

$$
\bigcup_{q\in\mathbb Q}\{q\}
=
\mathbb Q,
$$

它在 \(\mathbb R\) 中稠密。

所以离散名字要逼近连续对象，真正必须变得稠密的是：

$$
\boxed{
\text{名字的边界。}
}
$$

不是把连续变成离散，而是把未被任何有限名字消除的连续性推入越来越稠密的切口之间。

---

