# 三、三张切片的合法区域，不是一个方形，而是被抛物线切掉的一部分

只看前两个归一化系数，记

$$
x=r_1,\qquad y=r_2.
$$

三张切片的联合矩阵为

$$
T_2=
\begin{pmatrix}
1&x&y\\
x&1&x\\
y&x&1
\end{pmatrix}.
$$

逐对检查只会得到

$$
|x|\le1,\qquad |y|\le1.
$$

但这不够。

