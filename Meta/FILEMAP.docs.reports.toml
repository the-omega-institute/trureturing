schema_version = 5
# Experimental programs, results, and licenses; recursive patterns cover report directories.
files = [
{ pattern = "docs/reports/**/*.b64", require = ["test-worktree-contract"], kind = "data", admission_plane = "content", produced_by = "none", consumed_by = ["agent"], verified_by = ["SnapshotDecoder"], artifact_id = "none", runtime_disposition = "committed-source" },
{ pattern = "docs/reports/**/*.c", require = ["test-worktree-contract"], kind = "program", admission_plane = "content", produced_by = "none", consumed_by = ["agent"], verified_by = ["repository-policy"], artifact_id = "none", runtime_disposition = "committed-source" },
{ pattern = "docs/reports/**/*.cpp", require = ["test-worktree-contract"], kind = "program", admission_plane = "content", produced_by = "none", consumed_by = ["agent"], verified_by = ["repository-policy"], artifact_id = "none", runtime_disposition = "committed-source" },
{ pattern = "docs/reports/**/*.h", require = ["test-worktree-contract"], kind = "program", admission_plane = "content", produced_by = "none", consumed_by = ["agent"], verified_by = ["repository-policy"], artifact_id = "none", runtime_disposition = "committed-source" },
{ pattern = "docs/reports/**/*.json", require = ["test-worktree-contract"], kind = "data", admission_plane = "content", produced_by = "none", consumed_by = ["agent"], verified_by = ["SnapshotDecoder"], artifact_id = "none", runtime_disposition = "committed-source" },
{ pattern = "docs/reports/**/*.lean", require = ["test-worktree-contract"], kind = "program", admission_plane = "content", produced_by = "none", consumed_by = ["agent"], verified_by = ["repository-policy"], artifact_id = "none", runtime_disposition = "committed-source" },
{ pattern = "docs/reports/**/*.md", require = ["test-worktree-contract"], kind = "data", admission_plane = "content", produced_by = "none", consumed_by = ["agent"], verified_by = ["SnapshotDecoder"], artifact_id = "none", runtime_disposition = "committed-source" },
{ pattern = "docs/reports/**/*.mjs", require = ["test-worktree-contract"], kind = "program", admission_plane = "content", produced_by = "none", consumed_by = ["agent"], verified_by = ["repository-policy"], artifact_id = "none", runtime_disposition = "committed-source" },
{ pattern = "docs/reports/**/*.py", require = ["test-worktree-contract"], kind = "program", admission_plane = "content", produced_by = "none", consumed_by = ["agent"], verified_by = ["repository-policy"], artifact_id = "none", runtime_disposition = "committed-source" },
{ pattern = "docs/reports/**/*.txt", require = ["test-worktree-contract"], kind = "data", admission_plane = "content", produced_by = "none", consumed_by = ["agent"], verified_by = ["SnapshotDecoder"], artifact_id = "none", runtime_disposition = "committed-source" },
]
